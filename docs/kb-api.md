# KB: API reference (REST + WebSocket)

Moved VERBATIM out of CLAUDE.md (tier-2 pass).

## API Reference

### REST Endpoints

#### Published pages (2.364.0)
- `GET /p/:id` — serve a published page. Middleware-exempt; module gates: private ⇒ cookie, public ⇒ open. Response always carries `Content-Security-Policy: sandbox allow-scripts …` (opaque origin — the XSS guard).
- `GET /api/pages` — list; `GET /api/pages/by-path?path=` — record for a source file (the explorer dialog's state probe).
- `POST /api/pages/publish` `{path, name?, public?}` — copy-in publish, upserts by source path (stable /p/<id>).
- `POST /api/pages/:id` `{public?, name?}` — flags; `DELETE /api/pages/:id` — unpublish.

- `GET /api/home` — home directory (+ authEnabled flag)
- `POST /api/login` / `POST /api/logout` — password auth (rate-limited; sets/revokes HttpOnly cookie token); `GET /login` login page
- `GET /api/backend-status` — CLI readiness for onboarding: {claude,codex} × {installed, version, loggedIn} (file-existence detection, never spawns CLIs)
- `GET /api/files?path=` — list directory
- `GET /api/file/info?path=` — file metadata (size, isBinary) without reading content
- `GET /api/file/content?path=` — read text file (max 10MB)
- `GET /api/file/raw?path=` — serve raw file (PDF, images)
- `GET /api/file/binary?path=&offset=&length=` — read binary chunk (max 1MB)
- `GET /api/file/excel?path=` — parse Excel → JSON
- `GET /api/file/docx?path=` — parse Word → HTML
- `GET /api/file/csv?path=&offset=&limit=&sep=` — streaming CSV/TSV row range (virtual scroll)
- `POST /api/format` — server-side code formatting (Python/Go/Rust/Shell via ruff/black/shfmt/gofmt/rustfmt)
- `GET /api/download?path=` — download file
- `POST /api/upload` — multipart upload (fileNames JSON field for Unicode filenames, preservePaths=1 for folder uploads with recursive mkdir)
- `GET /api/file/serve/*` — path-based raw file serving (enables `<base href>` for HTML preview relative paths)
- `POST /api/file/write` — write file
- `POST /api/mkdir` — create directory
- `POST /api/rename` — rename
- `DELETE /api/file?path=` — delete
- `POST /api/archive` — create archive from same-parent paths[] (`.zip`/`.tar[.gz|.bz2|.xz]`, execFile zip/tar, 409 on existing dest unless overwrite)
- `GET /api/archive/list?path=` — archive contents (unzip -l / tar -tvf parse; 20k entry cap)
- `POST /api/archive/extract` — extract archive to dest (never overwrites unless overwrite:true — unzip -n / tar -k)
- `POST /api/archive/extract-entry` — extract ONE entry to a temp file (spawn streaming, 200MB cap) → client opens via normal viewer pipeline
- `POST /api/file/copy` / `POST /api/file/move` — recursive copy/move to FULL dest path (fs.cpSync; move falls back to copy+rm across devices; 409 on conflict unless overwrite). `progress:1` (2.215.0) → returns `{opId}` immediately and runs as a polled op: `GET /api/file/transfer-status?id=` {done,total(bytes),status,error,dest}, `DELETE` cancels (best-effort; local worker copies can't abort mid-flight). Cross-host relays count bytes INLINE on the tar/cat stream (du -sb pre-computes total, BSD -sk×1024 fallback); local/same-host branches poll du on dest. In op mode the 409-exists check surfaces as op error 'exists' — the client maps it back onto the confirm-overwrite flow. Paste always requests progress; the client row renders only if the op outlives 400ms (small pastes look unchanged)
- `GET /api/download-zip?path=` — stream folder as zip download (spawn `zip -r - `, no temp file)
- `GET /api/file/stat?path=&du=1` — extended properties (mode/uid/entryCount; optional bounded du -sb)
- `GET /api/dir-complete?path=` — directory autocomplete (500ms timeout, supports `~`)
- `GET /api/fonts` — system monospace fonts via `fc-list` (cached, fallback for client-side detection)
- `POST /api/custom-grids` — add custom grid preset `{rows, cols}`
- `DELETE /api/custom-grids` — remove custom grid preset `{rows, cols}`
- `GET /api/sessions` — all sessions (lock-first discovery + tmux detection)
- `GET /api/active` — WebUI-managed (dtach) sessions only
- `GET /api/usage` — rate limit data (PASSIVE since 2.60.0 — read from data/usage-cache/ written by the vibespace-usage statusLine hook; NO background /api/oauth/usage poll unless the opt-in setting `accounts.activeUsagePolling` is on, default off + danger-confirm. A cheap LOCAL read, so the taskbar polls it every 8s). Also gates the /v1/models OAuth fetch behind the same toggle
- `GET /api/usage-stats?from=&to=&backend=&account=&host=&pivot=` — the Usage-window ledger aggregation (UsageHistory.scan()+aggregate): totals + daily series + groups by day/model/account/billing/project/mode/hour/weekday/session/**origin**, deduped by requestId, account-attributed, Claude AND Codex (2.63.0). `pivot` = comma list of `dimA:dimB` 2-D crosses (≤8 since 2026-09-10 — the window asks for `session:origin` + `project:origin` on TOP of whatever the panel dashboard needs, and a cap that silently dropped a panel's pivot left it rendering a hole) → response `pivots['a:b']` = [{key, cells:{bKey: finalized-bucket}}] for split-series dashboard panels (2.99.3). **`origin` (2026-09-10) = which KIND of transcript the request came from** — `'main'` (the conversation itself) | `'subagent'` | `'workflow'` | `'unknown'` (a row whose transcript is gone, or a remote scanner too old to stamp it); the `session` dim's rows also carry `project` (the conversation's cwd) so the window's session table needs no second lookup. `account` = comma list of bucket keys (account ids / `__global__`) — the window's Account chips pass a linked pair `<subId>,__global__` when the CLI login IS that account; from/to also drive the Custom… date-range picker. `GET|POST /api/usage-stats/pricing` — read / PATCH-merge the per-model + per-account price table (data/usage-history/pricing.json v2; tier match = longest-key substring, editor lists all tiers). `GET /api/usage-stats/rid-info?rid=&mid=` — the msg-meta popup's per-message billing lookup (2.266.1): `usageHistory.eventForRid(rid)` (exact, or `h:<host>:` suffix) then `eventForMid(mid)` (2.284.1; live claude records carry only message.id) → `{found:false}` | `{found:true, acct, aname, atype, pool, poolName, host, hostName, be, model, effort}` — `be`/`model`/`effort` added 2026-09-06 for codex parity: codex passes rid = the ledger's synthetic `cx:<thread>:<cumulative total>` key and mid = the 0.153 `resp_…` response id (walker v3 bakes both); `be` lets the client name the global bucket's login per harness ('ChatGPT login' vs 'CLI login'); `model`/`effort` are CONSUMED by the popup as fallback rows when the record's own meta lacks them (chat-view `_showMsgMeta`, appended after the Billing row — never duplicated over the sync rows).
- `POST /api/usage/refresh` `{account: '__global__'|subId}` — **USER-INITIATED on-demand quota fetch (2.70.0)**: the ONLY source of model-scoped weekly buckets (Fable) since the statusline payload carries just 5h/7d. Human-gated equivalent of the CLI's own /usage command: NEVER on a timer, ≥60s/account server-side + honors the 429 backoff (endpoint 429s hard with retry-after up to 3600s). **Gated by `accounts.onDemandQuotaRefresh` (2.72.0, user-requested after the ban question): 'manual' (DEFAULT — ⟳ button only) / 'auto' (also once on popup open when scoped data >30min stale) / 'off' (route 403s, ⟳ hidden, hint says so); first ⟳ click shows a one-time explainer dialog (localStorage vibespace.quotaRefreshAck). Result persisted to data/usage-cache/<key>.json (`source:'on-demand'`, `scopedFetchedAt`) so it survives restarts and the statusline hook's preserve-merge. §ban-safety: do NOT wire this to any scheduler. Popup: ⟳ in the Claude section title, scoped bars show their own 'as of' age, empty-scoped shows a hint note. **B-855a c1 (2026-09-17): the LOCAL answer names its rung — `{success:true, via:'session'|'cli-panel', rung:'control'|'panel', identityVerified, why, sessionId?, skipped:[{sessionId, why}]}` (`skipped` = live sessions the control rung would not ask: OTel-divergent or inside a re-point's lag shadow); an identity REFUSAL of the panel answers `{error:'not recorded — <both identities>', rung:'panel', identityVerified:false, why}` and makes NO further vendor call.** **2.369.114: a live-session reading the window guard ARCHIVED is not a refresh — it is listed in `skipped` and the ladder continues to the isolated panel rung.**
- `POST /api/usage/repair-identity` — **B-855a c2 (2026-09-17), HUMAN-TRIGGERED: the standing identity repair by hand** (the same `repairSidecarsByApiPhase` every boot runs after the one-shot migrations): derives each roster account's API phase from its slot-verified rate-limit readings, re-stamps a contradicting established-window sidecar, archives + rebuilds a contradicting usage-cache snapshot, re-admits the last 24 h of archived own readings; cookie-authenticated, an agent's `vsst_`/`jbt_` Bearer ⇒ `403 {code:'agent-forbidden'}`; answers `{success, counts:{evidence, noEvidence, stamped, restamped, confirmed, kept, replaced, emptied, stripped, readmitted}, ms, identities:[{key, name, apiPhase, n, of, sidecar, sidecarWas, cache, readmitted}]}` and reloads the panel memory (idempotent; archive-never-destroy in data/archive/readings-foreign-usage-cache.ndjson). Since the final verifier pass the same repair also runs HOURLY on a timer (quiet unless it changed something), and `POST /api/usage/refresh`'s `why` on a phase refusal may add that the weekly window "may have MOVED" with the count of the account's own candidates that already share the panel's window; an answer on a SHARED phase is `identityVerified:false` with `why` naming who shares it.
- `GET /api/usage/probe-log?key=<accountId>&rung=panel|control&limit=200` — the raw /usage probe ring (2.369.109): the last N probes with the verbatim reply, the parse, the write verdict and the machine-wide oauthAccount around the spawn (`{records:[…], file}`; src/server/usage-probe-log.js; read-only, the file is data/usage-probe-log.ndjson + .1; since B-855a c1 a panel record also carries `configDir`, `configOrgBefore/After`, `identity`, `identityVerified`)
- `POST /api/telemetry` — client event ingest ({events:[...]}, ≤20/batch; dropped silently when telemetry.enabled=false); `GET /api/telemetry/summary?days=` — grouped errors + byName/byDay/byVersion for the ⚙ Diagnostics report (src/telemetry.js). FLEET (2.101.0): `POST /api/telemetry/ingest` = the central-collector receive endpoint — enabled ONLY when env `VIBESPACE_TELEMETRY_INGEST_TOKEN` is set (shared Bearer token = on-switch + gate, timing-safe compare; cookie-auth EXEMPT in auth.js since senders have no cookie); batches land in central-YYYY-MM.ndjson stamped with the sender's instance id (original ts/version preserved, clamped). `GET /api/telemetry/central-summary?days=` (cookie-authed) = per-instance totals + cross-instance grouped errors → the Diagnostics report's Fleet section. Sender side: telemetry.forwardUrl/forwardToken settings fall back to VIBESPACE_TELEMETRY_FORWARD_URL/_TOKEN env (helm-configurable fleet-wide; user setting wins); _forward sends Bearer. Helm values telemetry.{forwardUrl,forwardToken,ingestToken}
- `GET /api/ci-heavy` — the heavy release-gate markers for the ⚙ Diagnostics report (src/server/ops-routes.js; read-only, tiny local JSON, no git): `{runs:[{sha,result,startedAt,endedAt,ms,suites,absent,failed,flaky,reason,unlocked,scope,selected,lanes}], running:[…]}`. **2026-09-15**: `scope` is `'full'` or `'affected'` (the push-time tier runs only the suites the pushed range touches; `selected` = how many; a marker written before the field is a full run) and `lanes` = the parallel lane count the run used — a FULL green is still owed once per 24 h and `npm run ci:status` says which tier the next push launches.
- `GET /api/sysinfo` — container-aware system snapshot (cgroup v2→v1→host memory, workspace df, loadavg, top-8 RSS procs) for the System rail panel. **MEMORY IS THE WORKING SET, never raw `memory.current` (2.312.0, a fleet user's permanent false 100%): a cgroup's total COUNTS RECLAIMABLE PAGE CACHE, so any file-heavy pod (transcript reads, a dataset pass) pins it at the limit forever — 29GB of cache, 2.7GB of actual processes, `memory.events` showing 770 reclaims and ZERO oom_kills. A gauge that can never fall is worse than no gauge: the real leak it exists to catch cannot stand out. `used = memory.current − inactive_file` (the kubectl-top/eviction definition, verified byte-for-byte against `kubectl top` on the affected pod), with `raw`+`cache`+`anon` carried alongside so the panel NAMES the difference instead of contradicting other tools; bare hosts use MemAvailable, not freemem. The REMOTE probe had always been right (`MemTotal − MemAvailable`) — this was the local twin drifting, the exact class §CS-SEPARATION exists to prevent, and the reason any new "how much memory" reader must go through memInfo().** `?host=<id>` = the same shape from the machine — **ONE implementation since 2.314.0 (a missed CS twin the user caught)**: the daemon bundles src/sysinfo.js and serves a capability-gated `sysinfo` op (containerized machines report their cgroup limit; macOS = vm_stat working set via the same module); the ssh script in server.js is the FALLBACK RUNG for daemon-less hosts only (raw-host semantics: MemTotal=limit). Any new "how much memory" reader goes through the module, never a re-derivation. Test: scripts/test-sysinfo-op.mjs (real daemon, incl. the 2.300.0 reply-op routing rule — an op-less reply times out silently). History stays local-only (the sampler runs in this server). **2.226.3 lesson: the 2.223.1 commit shipped a PARTIAL state (draw fn without DOM builder + dispose calling a nonexistent helper) — charts invisible AND leaving the System panel threw, bricking rail switching; all three _panelDispose call sites now catch (rail-dispose-failed telemetry) and scripts/test-sys-panel.mjs guards the cycle; src/sysinfo.js also runs a 45s memory-pressure watch → WS `sysinfo-alert` broadcast at ≥80%/≥92% of the container limit (30min cooldown per level, instant on escalation; toast + rail badge client-side — 2.216.0, userL's 32Gi pod OOM kill took every dtach session with zero warning)
- `POST /api/port-forward/:id/path` / `DELETE …/path` — main-domain PATH MOUNT (2.358.0, owner request: services under the app's own domain instead of one random frp subdomain each): body {name} → the forward serves at `/svc/<name>/` BEHIND VibeSpace auth (registered after auth.middleware; the ws upgrade branch in server.js's single dispatcher checks requestAuthed like /ws). src/server/path-mounts.js proxies to the forward's server-loopback port resolved PER REQUEST (mounts survive restarts with zero re-establishment; works for remote machines through the tunnel): prefix strip + query preserved, Host rewritten, X-Forwarded-Prefix/-Proto/-Host set, absolute-path Location redirects rewritten under the mount, no-slash form 302s to the canonical `/svc/<name>/`, unknown mount = 404, mounted-but-down = honest 502, ws upgrades spliced raw. INHERENT LIMIT (UI hint says so): apps emitting absolute asset paths must support a base path (vite `base`, jupyter `base_url`, code-server natively) — bodies are deliberately NOT rewritten (that's the embedded-browser proxy's job). Names `[a-z0-9][a-z0-9-]{0,40}`, unique across forwards (clash names the holder). **PER-MOUNT PUBLIC/PRIVATE (2.359.0, owner's sharing case): body {public:true} makes the mount reachable WITHOUT a VibeSpace login (shareable link); DEFAULT stays private — exposure is an explicit confirmed act. SECURITY MODEL: the /svc prefix is EXEMPT from auth.middleware (a blanket cookie gate would make public mounts impossible), so the per-mount `requestAuthed` checks inside path-mounts (HTTP handler AND ws upgrade) are the ONLY gate for private mounts — both sides pinned (private-no-cookie → 401 incl. ws, public-no-cookie → 200). UI: lock toggle on the mount's address row, going public requires a danger-confirm naming the full URL; 'public' chip when open.** Test: scripts/test-path-mounts.mjs (17, real http+ws round trips; in the ci gate).
- `POST /api/port-forward/:id/publish` also accepts body `{sub}` (2.358.0) — a user-CHOSEN FIXED subdomain for the frp relay (frps routes any name under subDomainHost; validated `[a-z0-9][a-z0-9-]{0,62}`, persisted as publicSub so restarts keep it — same slot the broker's random name used; a taken name fails loudly at the relay). Only HTTP backends ride subdomains (https/tcp fall through to IP:port mode as before); the UI publish flow prompts for it via showInputDialog.
- `GET /api/sysinfo/procs` — FULL process table for the System panel's process manager (2.354.0, btop-like): `{procs:[{pid,ppid,user,pcpu,pmem,rss,state,etime,cmd}], total, sampled, serverPid?}` — capped by capProcs (top-by-RSS ∪ top-by-CPU, ≤350 transported; `total` is the uncapped count and the UI names the truncation). `pcpuNow` rides each row when live sampling is available (Linux /proc utime+stime delta between successive calls, keyed pid:starttime against pid recycling — the counter-capture class); `sampled:false` (ssh fallback rung, macOS) means CPU% is ps's lifetime average and the UI says so. `?host=<id>` = same shape via the ladder: capability-gated `proc-list` device op (the daemon runs the SAME src/sysinfo.js listProcs — successive panel polls give the daemon its delta baseline, so remote CPU% is live too) → ssh rung `ps axo <PS_COLUMNS>` parsed orchestrator-side by the shared parsePsProcs. The columns and their interpretation live ONLY in src/sysinfo.js. Test: scripts/test-sysinfo-op.mjs (real daemon, row-shape parity local vs op) + the restore-smoke route battery.
- `POST /api/sysinfo/signal` — signal a process from the panel: `{host?, pid, sig}` (sig enum-whitelisted TERM/KILL/INT/HUP/STOP/CONT, pid integer-validated >1 BEFORE any shell string is built). Local = process.kill with a self-guard (the server's own pid → error naming the Update/systemctl flow) + a 450ms settle then `gone` honesty flag; remote = ONE verdict shell script (`kill -SIG` → sleep → **`ps -p` existence probe** → OK-GONE/OK-ALIVE/EPERM/ESRCH) over dm.runCmd, ssh _hostShell as the fallback rung. **TWO REVIEW-PINNED INVARIANTS (2.354.0, adversarially confirmed + E2E'd on a real host): ① existence is probed with `ps -p`, NEVER `kill -0` — kill(2) with sig 0 performs the SAME permission check as a real signal, so with `kill -0` every remote permission-denied kill misreported as "no such process" while the process kept running (the local rung splits errno correctly — this was local/remote divergent honesty on the ONE kill path); ② rung choice happens at CONNECT time only — once a device link exists, a runCmd failure throws ("may or may not have landed — check and retry") instead of falling to ssh, because the signal may already be delivered and a blind re-send double-signals.** Errors are 400s with the verdict spelled out (permission denied / already gone) — the panel toasts them, incl. a null-response guard (fetchJson returns null on network failure; unguarded, STOP/CONT showed a false success toast). Pins: test-sysinfo-op (ps -p/kill -0 greps).
- `GET /api/vnc/status` / `POST /api/vnc/start` — in-container desktop (src/vnc.js; served by src/routes/desktop-apps.js since 2026-09-13, answers unchanged): status {available, running, port}; start = spawn-or-adopt. WS `/api/vnc` = the RFB↔TCP bridge (cookie-authed at upgrade). **WS UPGRADE INVARIANT (2.104.0, real bug)**: the main wss is `noServer` and ONE `server.on('upgrade')` dispatcher routes /ws, /proxy/, /api/vnc — NEVER construct `WebSocketServer({server, path})`: ws's own listener abortHandshake(400)s every non-matching upgrade (it silently killed /proxy/ site WebSockets for months and the VNC bridge on arrival) **SINCE 2026-09-13 the WS `/api/vnc` upgrade is served by THE ONE desktop bridge src/server/desktop-stream.js as stream id `desktop-singleton` (`upgradeId('/api/vnc')`), the same relay as `/api/desktop/<id>/stream` — server.js's `bridgeVncSocket` is gone.**
- **DESKTOP APPS (docs/design-desktop-apps.zh.md, P8-1 2026-09-13; every route takes `host` and REFUSES a non-local one — 400 `{error, code:'unsupported-host'}` — v1 is local-only; every failure is `{error, code}`):** `GET /api/desktop/apps` → `{apps:[record + live{cpuPct,rssBytes,sampledAt} + idle{idleMs,limit,remainingMs,expired}], registry:[row + available/path/reason/parkedUntil], availability:{backend, via, fallbackWhy, ladder:[{backend, ok, via, why}], xpra, bins}, cap:{used, cap}, idleTimeoutMin}` · `POST /api/desktop/apps` body `{appId}` | `{exec, args?, cwd?, label?}` → the record (state `launching`; 400 bad-request/exec-not-found/cwd-missing/needs-wayland, 409 cap/runaway-parked/no-backend/backend-not-wired) · `GET /api/desktop/apps/:id` → the record (404 not-found) · `POST /api/desktop/apps/:id/stop` → the record (`exited`, `stoppedBy:'user'`) · `POST /api/desktop/apps/:id/keep-alive` → the record with `idleTimeoutMs:0` · **P8-2 chunk x1 (2026-09-21):** `GET /api/desktop/apps/:id/windows` → `{ok, why, windows:[{id, title, cls, instance, x, y, w, h, mapped}]}` (the app's windows on its display — on an xpra display xpra's own wrappers filtered; an on-demand snapshot, the live title rides the picture) · `GET /api/desktop/:id/xpra-ui/` → 302 to `…/xpra-ui/index.html?path=/api/desktop/<id>/stream&…` and `GET /api/desktop/:id/xpra-ui/*` = the installed xpra's UPSTREAM html5 client served behind the cookie auth (nosniff, no-store, no listing, no dotfiles; 409 `not-xpra` for a record on another rung, 503 `xpra-ui-unavailable` without a www tree; `?netem=rtt:200,kbps:1000` on the bare dir is remembered for the id ONLY under `VIBESPACE_DESKTOP_NETEM=1`, echoed in `X-VibeSpace-Netem`) — the D21 (c) (a) validation slice. **P8-2 chunk x2 (2026-09-22): the window no longer shows that page — it loads `…/xpra-ui/js/Protocol.js` (with its `lib/*.js` + `Utilities.js`) through this route as its protocol WORKER (same-origin, cookie-authed) and draws the session itself; the html5 page stays reachable as the validation slice.** `availability` gained `stream` (the winner's kind) + `prefs` (the instance order); the record gained `stream` ('rfb'|'xpra', from the table) + `probe` ('rfb'|'http', the READY probe's kind). Setting `desktop.backendPrefs` (string, default '') = a comma list of rung ids that REORDERS the ladder for NEW apps (unknown ids ignored; a running app keeps its backend). Record shape (§4, facts only): `{id, label, exec, args, cwd, env?, source:'registry'|'adhoc', appId?, backend, via, recipe (r2: the bring-up recipe name the table gave it), fallbackWhy, display, port, pids:{x,app,server,wm}, starts:{…}, startedAt, readyAt?, adoptedAt?, endedAt?, state:'launching'|'ready'|'exited'|'failed', exitCode, lastError, stoppedBy?, idleTimeoutMs, lastInputAt}`; `live` (in GET/broadcast views) = `{cpuPct, rssBytes, pids, sampledAt}` over the session SET (r2), `availability.recipe` names the winner's recipe. r2: `port` is a MEASURED fact — the record turns `ready` only when the 127.0.0.1 listener is held by the recorded picture server's session, else `failed` naming the port and pid. **P8-2 x3 (2026-09-22):** every route above is driven on a real server with cookie auth by test-desktop-xpra; `PATCH /api/settings` takes `desktop.idleTimeoutMin` and `desktop.backendPrefs` live, both read at LAUNCH and stamped on the record (a running app keeps what it was born with); `availability.xpra` = `{version, raw, www}` of the installed xpra (the html5 tree the window loads its protocol worker from); an xpra session's `port` is a 127.0.0.1-only listener (the binding check). **P8-2 chunk x4 (2026-09-22): the record view carries `fitMode` ('follows' | 'fixed' | 'client' | 'shared' | null — from the rung's `fit` column by `via`), `fitBy` ('keeper' | 'client' | null), `fb {w, h, at}` (the framebuffer as xdpyinfo states it, measured by the keeper) and `fit {ok, wid, w, h, moved, at, why, ms, via}` | `{ok:false, why, at}` (the last app-fit act, or a refusal by name; `via` 'wm' = the CLIENT maximised through the window manager, 'xdotool' = moved/resized directly — 2026-09-22; under a WM `wid` is the CLIENT inside the frame, never the frame); `GET /api/desktop/apps/:id/windows` rows gained `depth` (1 = a top-level; under a reparenting WM the app's client is depth 2 inside a nameless depth-1 frame). x4 close-out: the record carries `appTitle` (string | null — the fitted app window's own title on a keeper-fitted rung, APP-CONTROLLED text, textContent only), and a window whose name xwininfo cannot print (COMPOUND_TEXT, a failed conversion) is still a row with `title: null` (it used to be dropped).** **2.369.158 (HiDPI, design-desktop-apps §7.6):** `POST /api/desktop/apps` takes `dpr` (the launching client's devicePixelRatio, a number 1..3; absent ⇒ 1; anything else ⇒ 400 bad-request `dpr must be a number from 1 to 3`); the record carries `scale` (1 | 1.5 | 2 — fixed at launch from the setting `desktop.appScale` (enum auto | 1 | 1.5 | 2, default auto = 2 when the dpr is 1.5 or more, else 1 — never the text-only 1.5 (r2); xpra stream only, 1 elsewhere)) and `dpi` (the display's font dpi, 96 × scale / GDK_SCALE — `xpra start --dpi`; the window's client sends the same value in its hello / display-configure). On the xpra stream the client's desktop size and every geometry packet are DEVICE px (pane CSS px × devicePixelRatio) and its hello lists `size-constraints` in `metadata.supported`.
- **WS `GET /api/desktop/<id>/stream[?viewer=<id>]`** — THE desktop bridge (src/server/desktop-stream.js): cookie auth at the upgrade INSIDE the bridge (401), 404 when nothing serves the id, an unknown kind 501 by name, `rfb` ⇒ RFB↔TCP to 127.0.0.1:<port> with the /api/vnc backpressure numbers, **`xpra` (P8-2 chunk x1, 2026-09-21) ⇒ ws↔ws to the xpra server's own WebSocket on 127.0.0.1:<port> (subprotocol `binary`, binary frames both ways, one message per packet, the same backpressure / 20 s ping / named close — an upstream `disconnect` packet's reason lands in the close line; MEASURED xpra rule (x2, 2026-09-22): the server sends clipboard tokens to ONE client — the first with clipboard enabled, then whoever last sent input (its `ui driver`) — a viewer that never touched the app receives no copies; input for the idle clock = the packet types a human produces (`XPRA_INPUT_TYPES`), an unreadable packet counts; the policy verdict is applied PER PACKET on this kind too (2026-09-22 — a refused Watch viewer's hello / ping_echo / damage-sequence / map- and configure-window / keyboard-config reach xpra, only its input packets are cut out; the r1 all-or-nothing-per-chunk rule dropped the hello and the pane never got a picture); `?netem=rtt:N,kbps:N` shapes the relay ONLY under `VIBESPACE_DESKTOP_NETEM=1`)**; **P9b: `?viewer=<1–64 [A-Za-z0-9._-]>` names the pane, and when an agent holds a window lease the bridge asks the window-targets engine per client message — Watch ⇒ the viewer's input messages are CUT OUT of the relay (the update request beside them still goes through), taken over ⇒ the holder viewer only, everybody else `held`; the singleton `/api/vnc` is never gated;** INPUT is reported to the keeper (idle clock, throttled 2 s) only for client RFB messages that ARE input — KeyEvent / PointerEvent / ClientCutText / the QEMU extended key event, walked statefully across frames (r2: noVNC sends a FramebufferUpdateRequest on every repaint, which used to count; a stream the sieve cannot follow falls back to every-frame-counts; **r3 2026-09-14: the sieve's fixed-length table is DERIVED from the shipped noVNC — EnableContinuousUpdates is 10 bytes, not the 4 the round-2 table said, PointerEvent is 6 or 7 by its ExtendedMouseButtons marker bit, ClientFence is variable, xvp (250) is 4 — because a wrong FIXED length does not flip the sieve to `opaque`, it misaligns every later KeyEvent silently, and on a server that accepts ContinuousUpdates (TigerVNC's Xvnc, the fleet image's rung) the idle stop could have reaped a session the user was typing in; test-desktop-apps §7 parses every `RFB.messages.*` encoder out of node_modules/@novnc and drives each shape followed by a KeyEvent that must count**). Broadcast **`desktop-apps-updated` `{apps:[…]}`** on every keeper change (launch/ready/exit/stop/adopt/idle/runaway/keep-alive; the live CPU/RSS every guard sample). Settings: `desktop.idleTimeoutMin` (30, 0 = never), `toolbar.showDesktopAppsButton`. **r-fix 2026-09-21:** `?viewer=` is bound to the socket that claimed it — a second upgrade naming a LIVE id is refused 409 `viewer id held`; `POST /api/desktop/apps/:id/takeover` answers the lease with `takenBy: {tag, at}` (opaque) and that tag, not the id, is what `window-leases-updated` broadcasts. **x3 (2026-09-22), measured on a real server:** no cookie ⇒ 401 and an unknown id ⇒ 404 at the upgrade; the xpra upstream port is bound to 127.0.0.1 only (a TCP connect to the box's non-loopback address is refused) — the relay is the ONLY path from a browser to the picture; a `clipboard-token` from the client counts as input (lastInputAt moves); a real rencodeplus hello is answered through the relay in ~0.5 s. **P8-2 chunk x4 (2026-09-22): a client's SetDesktopSize (RFB 251) on an rfb stream is REPORTED to the keeper as `(id, w, h, viewerId)` as it passes (it is not input; it never moves the idle clock) — and the keeper fits the app to the size the server actually took; since r8 a REFUSED viewer's SetDesktopSize is cut and never reported (the display is the holder's).** **r6 (2026-09-22):** on the xpra kind a REFUSED viewer (watch-mode / held) may send only the picture's packets (`XPRA_WATCH_TYPES`: hello, ping/ping_echo, damage-sequence, map/unmap/configure-window, buffer-refresh, clipboard-contents-none) — input and every control packet are dropped; its keymap packets (keyboard-config/keymap-changed, r7) and display-size packets (display-configure/configure-display/desktop_size, r8) are dropped too and the last of each HELD, replayed to xpra at that viewer's takeover (the log line names them); `shutdown-server` / `exit-server` are dropped from EVERY viewer (the keeper owns xpra's lifecycle) and the xpra socket refuses a hello `request` of stop/exit/detach/run/info/screenshot/print; a header declaring more than 16 MiB closes that viewer 1009 `packet-too-large` (r7). **x5 (2026-09-22, design-desktop-apps §7.5 — ONE ACTIVE VIEWER per app window):** the upgrade also names the window's stable PUBLIC pane key `?pane=<1–64 [A-Za-z0-9._-]>` (a socket without `?viewer=` is seated as `anon-<n>`); among a window's viewers exactly one is ACTIVE (everything relayed), every other one is BLOCKED — the bridge opens NO upstream for it (no ws to xpra, no TCP to the VNC server): 0 packets either way, its hello + the held kinds are kept and sent first (hello → keymap → display size → geometry) the moment it becomes active; a socket that becomes blocked while connected is CUT: upstream closed, the socket closed **4001 `blocked: active on another client`** (the pane reconnects as a blocked viewer); a blocked socket of a session that ended is closed **1001** `the app session ended`. While an agent DRIVES the window every viewer is Watch: `configure-window` and `unmap-window` LEFT `XPRA_WATCH_TYPES` — a refused viewer's configure-window is cut and HELD (`XPRA_GEOMETRY_TYPES`, replayed at its takeover), unmap-window cut; the rfb kind: blocked = no TCP, cut = TCP destroyed + 4001. The singleton `/api/vnc` is never governed.
- `GET /api/layouts` — layouts + autosave
- `POST /api/layouts/:name` / `DELETE /api/layouts/:name` — manage named layouts
- `POST /api/layouts-autosave` — save workspace state
- `POST /api/editor/open` — editor helper HTTP→WebSocket bridge
- `POST /api/editor/signal` — signal editor completion
- `GET /api/settings` / `POST /api/settings` / `PATCH /api/settings` — schema-driven settings (sparse storage, broadcasts `settings-updated`)
- `GET /api/user-state` / `POST /api/user-state` — unified star/archive/archivedFolders/rename state (broadcasts to all WS clients; sessionGroups/groupFolders keys are DORMANT since 2.30.0 — migrated once into data/tasks.json)
- `GET /api/tasks` / `POST /api/tasks` / `PATCH /api/tasks/:id` / `DELETE /api/tasks/:id` — Task Group CRUD (wire paths kept as `/api/tasks*`; `data/task-groups.json` authoritative since 2.39.0, broadcasts `tasks-updated`)
- `POST /api/tasks/:id/bind` / `:id/unbind` — granular session tag add/remove (`{sessionKey}`, atomic server-side)
- `POST /api/tasks/:id/progress` — append progress note `{note, session?}` (capped 500)
- `POST /api/tasks/:id/export` `{path}` / `POST /api/tasks/import` `{path}` — P4 repo task files (absolute-path, committable markdown w/ frontmatter; import parses frontmatter+objective+plan into the store, preserving existing sessions/folders/progress)
- `GET /api/session-status` / `POST /api/session-status` — session state/urgency indicators (user set/override/clear by `sessionKey`; broadcasts `session-status-updated`)
- `GET /api/agent/task-context` — SessionStart hook payload (Bearer `vsst_`, NO taskId param — belonging derived from the token via `groupsForSession`): if the session belongs to injectable group(s) → rendered task state + contextDir file index + rules; if NO task → a one-time baseline `<vibespace-session-tools>` intro teaching `vibespace-status` (2.38.0, `_toolsIntroSeen` flag; claude only — codex gets it via prompt-context). `needTask:false` so it never 403s for a no-task session
- `GET /api/agent/prompt-context` — UserPromptSubmit hook payload (Bearer `vsst_`): first-prompt task context when SessionStart didn't deliver (codex) OR, for a no-task session, the one-time baseline tools intro (2.38.0) + pending status-override notice (consumed once, rendered `<system-reminder>`)
- `GET /api/agent/task` / `POST /api/agent/task-progress|task-backlog` — vibespace-task endpoints (Bearer `vsst_`, scoped to the session's live Task-Group belonging via `resolveAgentGroup`: 403 if in 0 groups, 400 if in >1 without `--group`, explicit `--group` must be one it belongs to); all writes flow through TaskGroupManager (TASK.md regen + broadcast); progress notes tagged with the reporting session key. task-backlog (2.122.0; claim model 2.123.0) takes `{add: text, detail?, priority?}` (auto-claims for the caller; `priority` ∈ high|normal|low, default normal, anything else → 400 naming the set — also accepted by `{edit: ref, text?, detail?, priority?}`, which echoes its item by id) / `{done|drop|claim|unclaim: <B-xxxx id|1-based OPEN-list index|unique substring>}` / `{show: ref}` (read-only full item, any status — the pasted-id path), stamps addedBy/resolvedBy/claimedBy with the calling session key, returns the open items (the same list GET task's `backlog` field carries — indexes stay consistent; since 2.369.154 BOTH are in `sortBacklog` order = priority high→normal→low, newest first, and a 1-based number resolves in that order; every item carries `priority`; GET task also answers `you` = the caller's session key so the CLI can mark owned items; **r2: a number means the position in the list GET task last served THIS session (remembered per session+group, in memory; never the store order — `show`/`edit` widen only id/text matches to resolved items); no listing yet ⇒ the live sorted order; out of range, deleted or (for done/drop/claim/unclaim) already resolved ⇒ 400 naming the item, nothing changed; `done`/`drop` now echo `item` like edit/claim**). **`/api/agent/task-status` was REMOVED in 2.39.0** (a Task Group has no status; sessions report their own state via `/api/agent/session-status`); **`/api/agent/task-plan` was REMOVED in 2.121.0 with the checklist feature** — the route stays as a 410 responder so old CLI copies (remote hosts) get guidance instead of a confusing 404 **Cleanup nudge (2.369.154):** the answer of `add` / `edit` / `claim` carries `nudge: {text, owned, stale, threshold}` when the caller holds (claimed or parked, open) ≥ setting `tasks.backlogNudgeAt` items (default 20, 0 = off) — computed after the write; `done` / `drop` / `unclaim` / `show` never carry it; `text` ≤ 500 B, English, the same paragraph the injection prints. **r2:** `GET /api/agent/prompt-context` carries that paragraph on EVERY turn the session is over the threshold (inside the per-turn `<vibespace-reminder>`, or its own `<vibespace-reminder>` block on a turn that delivers something else; skipped for a group whose full context rides the same prompt — that context's backlog note carries it), several groups = ONE paragraph ≤ 500 B; `GET /api/agent/task-context` is now capped inline at 9600 B like prompt-context (tail trimmed, pointer to `show --full`).
- `GET /api/agent-hooks` / `POST /api/agent-hooks/install|uninstall` — hook registration status + one-click repair/remove (Manage Agents dialog); uninstall strips ONLY the vibespace entry. **2.369.123: GET also answers `cliConfig` = `{ safe, files:[{harness,id,rel,format,keys}], receipts:[{harness,key,rel,path,want,state: applied|differs|missing|unreadable, current?, reason?}], off:[{harness,key,rel,path}], lastWrite:{at,reason,receipts}|null }` — the managed CLI-config rows' FRESH receipts for this machine (design-harness-settings D2: read off disk on every call, never persisted; lastWrite is in memory only)**
- `POST /api/agent/group-admin` — Task Group ADMIN for designated MANAGER sessions (2.132.0, issue #21; SCOPE + TEACHING 2.152.0): verbs create/update/bind/unbind/list (organize-only; NO delete/orchestration). DOUBLE-GATED, both default-off: setting `agents.allowGroupManagement` + per-session "Group manager" toggle (Session Properties → sessionConfigs[key].groupManager, read server-side from user-state — **the toggle silently never saved until 2.152.0: sidebar-state setSessionConfig's field whitelist dropped `groupManager`, third strike of the 2.43.0 'account' whitelist bug class; keep that list in sync with EVERY per-session config writer**). **Manager scope = ALL groups (2.152.0, user directive):** group-admin verbs were always unscoped; `resolveAgentGroup` additionally lets a manager target ANY group via explicit `--group <id>` on the REGULAR verbs (unknown id → 404 + group-list pointer; non-managers keep belonging enforcement; shared helper `isManagerSession(key)`). **Managers are TAUGHT their powers**: one-shot `<vibespace-group-manager>` block (task-context claude / prompt-context codex, `s._mgrIntroSeen`) + a per-turn reminder clause — before 2.152.0 nothing ever told the agent. contextDir/folders restricted to `agents.groupManagementRoots` (comma prefixes, default ~); EVERY op audited into the group's activity log attributed to the calling session. CLI: `vibespace-task group-list/create/update/bind/unbind`. Tests: scripts/test-group-admin.mjs (incl. cross-group + teaching assertions)
- `GET /api/hosts/:id/agent-tools` / `POST …/agent-tools/install|uninstall` — VibeSpace integration on a HOST (2.129.0): per-tool sha256-vs-local state + remote hook registration + node + keeper files; install = tar-over-stdin + hook-register, uninstall = register `--uninstall` then rm our files only. **2.369.123: GET also answers `cliConfig` = the managed keys' receipts from the helper's read-only `--status` (`[{harness,key,state,current,want,reason}]`; `[{state:'unknown', reason:'helper predates --status'}]` when the shipped helper is older than the plan env — the probe is grep-gated on the env NAME so an old helper is never run); install answers `{installed, cliConfig}` with the receipts of that install; the helper is driven by `VIBESPACE_CLI_CONFIG` (base64 plan) on install, on every ssh spawn prelude and on every dial run-cmd.** `vibespace-claude-subscription-login.mjs` is also a transport helper: a remote Add-subscription terminal receives that one file even when agent-visible Integration is OFF, so a Mac host can capture its own Keychain login without making agent hooks/tools visible.
- `GET /api/accounts` — accounts list (API keys: tail fingerprints only; subscriptions: email/plan/loggedIn + `loginState` = `{state:'ok'|'expiring'|'expired'|'logged-out'|'unknown', refreshExpiresAt, accessExpiresAt, msLeft}`, the LOGIN SESSION's own deadline read from the credential file (src/login-expiry.js; a pool's row carries its members' worst + `worstId`/`worstName`; harnesses that cannot read a deadline answer 'unknown' = no claim — do NOT confuse it with the STRING `loginState` the /relogin routes return about a login TERMINAL run); `localOnly` marks macOS Keychain-backed Claude subscriptions) + `subscription` global-login status + importable `cliKey`; `POST /api/accounts` `{name,key}` add API key; `POST /api/accounts/subscription` `{name}` — allocate an isolated creds dir + return a shell-quoted `loginCmd` running `data/bin/vibespace-claude-subscription-login.mjs` (sets BOTH `CLAUDE_CONFIG_DIR`+`CLAUDE_SECURESTORAGE_CONFIG_DIR`=dir so OAuth creds+identity stay isolated and ~/.claude stays untouched; on macOS the helper reads Claude's hashed per-dir Keychain service in the SAME interactive terminal after a successful official login, validates `claudeAiOauth`, then atomically writes only that root field to `.credentials.json` mode 0600 — a launchd server cannot reliably read the interactive Keychain item later; fresh Keychain data replaces stale fallback only after validation, no token enters argv/logs, and a sanitized `.vibespace-login-status.json` lets finalize/remote watchers stop on the exact attempt's success or failure); macOS Keychain shadows are NEVER config-exported or remote-shipped because two copies can fork a rotating refresh token, and their account dirs are NEVER auto-merged/renamed because the Keychain service hash includes the dir path — use an on-host login instead; `POST /api/accounts/subscription/:id/finalize` — read back identity/status once the login landed (a SUCCESSFUL login here, at `POST /api/accounts/:id/relogin-finalize` and at `POST /api/accounts/codex-subscription/:id/finalize` also sweeps the login-expiry watch IMMEDIATELY — same reply shape, but the inbox warnings about the login you just fixed are retracted within seconds instead of on the next 5-min poll; a failing sweep is logged, never returned. Two things about that gate, both round-2 defects: the re-login route reads `reloginResolve`'s REAL answer — `!!r.account?.loggedIn || r.outcome === 'moved'`, because that method returns `{outcome}` and a NESTED `account`, never a top-level `loggedIn`; and because manage-agents POLLS that route every 3 s for up to 5 min with a repeating answer, it sweeps only when the answer's credential FINGERPRINT changes, so a poll loop is one sweep and the login the user finishes mid-poll is still one more. The Add-flow finalize sweeps AGAIN after a successful `mergeSubscription`: the first sweep runs while the fresh credentials are still in the throwaway record's dir, so only the second one can see the survivor's new deadline); `POST /api/accounts/import-cli` — capture ~/.claude.json primaryApiKey; `POST /api/accounts/default` `{id|null}`; `PATCH/DELETE /api/accounts/:id` (delete rm's a subscription's dir); broadcasts `accounts-updated`
- `GET /api/usage` — …also `accounts: {<subId>: {fiveHour, sevenDay, scopedWeekly, name, email, fetchedAt}}` (per-subscription passive usage) + `globalLogin: {email, loggedIn, accountId}` (2.62.0 — the machine's own CLI-login identity; `accountId` = the named claude subscription it IS when an email matches, in which case the server merges `__global__`↔`<subId>` usage NEWEST-WINS both ways so one real account never shows two conflicting values). **READING PROVENANCE (2026-09-07 readings-by-slot):** each account snapshot also carries its writer's `source` (`passive`/`on-demand`/`control`/`rate-limit-event`/`limit-banner`/`wall`/`remote-statusline`) and, when the OTel observation had an opinion, `corroborated: true|false` — a LABEL only, the reading's KEY is the credential slot. The response gained `logins: {<subId>: {state: live|expired|wiped|missing|unreadable, usable, since}}` (src/login-state.js, the SAME predicate the pool's slot validation and the repair migration use) so a panel can date a signed-out member's last REAL reading — and warn when the newest cached reading POSTDATES the login's death, because that reading is provably not that account's. Usage-popup switcher chips (Auto / CLI login / each named sub; per-device localStorage `vibespace.usageAccount`) pick which account the taskbar pies show; a sub whose creds dir lacks identity gets a "set email…" backfill in Manage Agents (`accounts.setEmail`, PATCH `{email}`) since the email is what enables the link. **CODEX PARITY (2.64.0):** + `codexGlobalLogin` (identity of ~/.codex/auth.json via `accounts.codexGlobalStatus()` — same JWT parse as named cxs accounts) + `codexAccounts: {<cxsId>|'__global_codex__': snapshot+name+email}` — codex rate limits bucketed PER ACCOUNT (`summarizeCodexRateLimits()`: live wrapper meta keyed by session._accountId; rollout-tail fallback attributes via thread-id→session-meta map; email-linked global↔named merge newest-wins; `summarizeCodexRateLimit()` = back-compat overall). Codex popup section gets the same switcher chips (`vibespace.usageAccountCodex`, chips carry `data-be="codex"`); Manage-Agents codex roster shows global email + `= "Name"` link hint + per-account usage donuts (`_acctUsageHtml`, shared with claude — 2.70.0: conic-gradient mini donuts 5h/7d(+scoped 'Fa') matching the taskbar pies, hole punched with ::after in --bg-dialog; replaced the wide label+bar+pct rows) + set-email for API-key-mode logins (no id_token → no email)
- `POST /api/agent/session-status` — agent self-report (Bearer `vsst_` per-session token from spawn env; EXEMPT from cookie auth in auth.middleware — the only cookie-less API besides /dav); `{state, urgency, reason}` / `{clear}` / `{show}`; rekeys `webui:<id>` records once the real backend id exists
- `GET /api/agent/stop-check` — stop-time bookkeeping nudge arbiter (Bearer `vsst_`): {block, reason} — block only when the session's status is >10min stale AND no nudge in 30min (in-memory cooldown); gated by `agents.stopBookkeepingNudge` (default on). Consumers: the claude Stop hook (blocking) + the codex wrapper at turn/completed
- **For-you item `action` (2.369.157, design-reset-credits §5)**: an item a SERVER producer files may carry `action: {type, …flat facts}` (user-todos `normalizeAction`: kebab `type`, ≤ 12 flat string/number/boolean/null fields) in `GET /api/user-todos` / the `user-todos` broadcast. First type: `reset-credit` `{sessionId, accountKey, resetsAtSec, creditsLeft, reason}` (the codex reset-credit rung's `ask` mode). The panel's `.ut-action-reset` button opens the one confirm dialog, which spends through `POST /api/accounts/:id/reset-credit` (below) and resolves the item after the decision. The agent route never accepts `action`.
- **Peer-card `resetCredit` (2.369.157)**: a chat `create` op whose message has `originKind: 'peer-message'` (the wall card, the auto-resume arm card) may carry `resetCredit: {available, mode, accountKey?}` (`offerOf`-sanitized; mode `off|ask|auto`; `accountKey` = the usage identity the credits belong to, a plain id or dropped) — the card's "Use a reset credit" button opens the confirm dialog on that account. In-memory only, like every peer card.
- **`GET /api/accounts/:id/reset-credit[?sessionId=]` / `POST /api/accounts/:id/reset-credit {sessionId?}` (2.369.157 p2, src/routes/reset-credit.js, design-reset-credits §5)** — the MANUAL use of a stored reset credit. `:id` = the usage identity key (an account id, the machine login's key, a pool ⇒ its current member). GET → the confirm dialog's preview `{key, name|null, backend, vendor, creditsLeft, resetsAtSec, periodSec, remainingPct, sessionId, cooldownUntilSec, code?, error?}` (always 200; `code` = the refusal the POST would answer). POST writes ONE `codex-reset-credit` verb on a live chat session of that identity (the preferred `sessionId` when it is one) through the engine's `writeResetCredit` — the spend ceiling (reason `codex-reset-credit`) + the 10-min floor — and answers `{ok:true, sessionId}` or `{error, code, cooldownUntilSec}`: `not_supported` 400 (the harness's caps row has no `resetCredit` — claude: interactive `/limit-reset` only) · `no_live_session` 409 (r2: no running chat session HOLDS the account's login — a codex pool conversation holds its spawn member until its cold restart, so it stays the carrier after a switch, and the spend is charged to `:id`'s own identity) · `no_credits` 409 · `cooldown` 429 · `spend_refused` 429 · `agent_forbidden` 403 (a `vsst_`/`jbt_` Bearer — human-triggered only). The outcome arrives asynchronously: the wrapper's `reset_credit_result` → a server notice (a person's failure: "Reset credit not used on <account> — the vendor answered <outcome>", never the switch/wait ladder) and the fresh count rides `GET /api/usage`'s `codexAccounts[key].resetCredits`, which the roster repaints from.
- `POST /api/agent/user-todo` — vibespace-ask endpoint (Bearer `vsst_`, scoped to the calling session): `{add:{text,detail,urgency,kind?}}` (2.369.118: `kind: 'action'|'notice'`, `vibespace-ask --notice`; notices sit in the popup's own section with a grey count, never the red badge) / `{list:true}` / `{resolve:"id or unique text"}`; rekeys webui:→backend like session-status. `GET /api/user-todos` (cookie-authed) → `{todos:{open,resolved}}` (an item may carry `i18n: {text:{key,params}, detail:[{key,params}…], source:{key}}` — a server producer's words as STRUCTURE, worded by the client; `text`/`detail` stay English, a3 r4); `POST /api/user-todos/:id` `{status: done|dismissed|open}` — the panel's ✓/✕/↺. Broadcasts `user-todos-updated {todos}` (2.65.0). UI: taskbar `#taskbar-user-todos` inbox (chrome element, hideKey `taskbar.showUserTodos`) + `#user-todos-popup` grouped-by-session panel with jump (sidebar `_getSessionStateKey` canonical matching → goToWindow/attach/resume), toast on new items
<!-- session-groups CRUD routes removed 2026-06-09: were unreachable dead code with a conflicting data shape; groups are managed via full-state POST /api/user-state -->
- `GET /api/bookmarks` / `POST /api/bookmarks` — file explorer bookmarks (broadcasts to all WS clients)
- `POST /api/kill-pid` — kill an external/tmux agent CLI by PID (`{pid, host?}`). Both branches ask THE identity before SIGTERM (B-3185 r4): local = `isCliProcess(pid,'claude'|'codex')` from src/cli-identity.js, remote = `hosts.killRemotePid` → `killPidShell` (the same rule as embedded shell text). 400 `PID is not a claude/codex process` when it is not one — never a whole-argv substring and never `ps -o comm=`. **FOUR remote outcomes since B-3185 r5** — `VS_OK` (killed) and `VS_GONE` (no rung can see the pid) return; `VS_NOTAGENT` (alive, evidence read, not an agent CLI) and `VS_UNKNOWN` (alive, but NEITHER argv nor exe was readable there) both THROW, because "nothing was signalled" is never a success. The existence probe is a ladder of POSITIVE evidence (`kill -0` → `[ -d /proc/N ]` → `ps -p N`): the old single `ps -p N -o args=` capture is empty for EVERY pid under busybox `ps` (no `-p`), so a busybox-shell host answered `{success:true, gone:true}` for a live CLI while nothing had been terminated
- `GET /api/session-messages?claudeSessionId=&cwd=` — parse JSONL file into chat message history (`?backend=&backendSessionId=` for every harness; S9: `backend=opencode` on a STOPPED conversation is served by the opencode-serve reader — records rebuilt from the local `opencode serve` — and a refusal now ANSWERS instead of hanging the async handler: `404 {error, code:'NotFoundError'}` when the store does not know the id, `502 {error, code}` when the serve is unreachable/parked/not installed; the chat view's history fetch throws on non-OK and shows its retry pill)
- `GET /api/subagents?backend=codex&threadId=<root>[&host=]` — the conversation's SUB-AGENT ROSTER (B-7473), the click-through FALLBACK for a codex collab row whose child thread id is not in the transcript (0.153.4 puts `agent_thread_id` on every SubAgentActivity item, so the common path never calls this). `→ {subagents:[{agentPath, nickname, threadId, depth, startedAt}]}` from each child's OWN `session_meta.source.subagent.thread_spawn` in the local tree (a parent rollout never lists its children), sorted by spawn time, 10s cached per root. The walk runs in the transcript WORKER (`collectCodexThreadMetasAsync`, B-7473 integration 2026-09-06) — the sync twin measured 193 ms on a modest tree and this is a request path. Non-codex backend → `{subagents:[], reason:'unsupported-backend'}`; a `host` (remote conversation) → `{subagents:[], reason:'remote-machine'}` — NEVER a bare empty list, which would read as "this sub-agent never existed"; missing threadId → 400
- `GET /api/subagent-messages?claudeSessionId=&cwd=&agentId=` — read subagent JSONL + meta from subagents/ directory (candidate list ALSO covers `subagents/workflows/wf_*/agent-<id>.jsonl` so a dynamic-workflow agent opens in the same viewer; ws `sub-agent-` attach mirrors this)
- `GET /api/workflow?runId=&claudeSessionId=&cwd=` — a dynamic-workflow (ultracode) run's detail. If the TERMINAL snapshot `<projDir>/<claudeSessionId>/workflows/wf_<runId>.json` exists → normalized `{workflowName, status, agentCount, totalTokens, totalToolCalls, durationMs, phases:[{title, agents:[{label,model,state,agentId}]}], result|error}`. Else if the run dir `subagents/workflows/wf_<runId>/` exists → a LIVE skeleton `{status:'running', live:true, agentCount, doneCount, phases:[{title:'Agents (live…)', agents:[{agentId,state:progress|done}]}]}` built from `journal.jsonl` (started/result per agent) + agent files (no phase/label/tokens mid-run). Snapshot wins over live. runId-validated; targeted-then-session-scan finder (runId is globally unique). 404 only when neither exists
- `GET /api/custom-themes` / `POST /api/custom-themes` / `DELETE /api/custom-themes/:name` — custom theme CRUD (broadcasts `custom-themes-updated` to all WS clients)
- `GET /api/sync/:store` — SyncStore snapshot `{version, data}` for initial page load (stores: `drafts`, `settings`)
- `GET /proxy/<url>` — full-rewriting web proxy via node-unblocker (HTML/CSS URL rewrite, JS XHR/WS rewrite, header stripping)

#### Agent messaging — Channels v1 (2.362.0)
- `GET /api/agent/msg/peers` (vsst_) — ACL-visible sessions: {name, conversationId, level, groups, state, machine}.
- `POST /api/agent/msg/send` (vsst_) — {to: name|cid, text ≤16KB} → delivered {lane, machine?} | stashed. 30s/pair floor, 10min dup dedupe, uniform not-found.
- **Channels P3 (vsst_; every route resolves the caller's principal FIRST, reach is decided before any answer, hidden ≡ nonexistent = 404 `not-found`):** `GET /api/agent/channels/list` → `{conversations:[{key, adapterId, adapter, id, title, level, tracked, unread, canSend, sendWhy, sendAs, identityMarking, policy, assigned, authority, awaiting}]}` (never a body) · `GET /api/agent/channels/read?conv=<adapter>/<id>&limit=&since=` → `{conversation, records}` · `POST /api/agent/channels/reply` `{conv, text, why?, replyTo?, attachments?}` → `{ok, proposal, decision:{mode, reasons}}` — PROPOSES; 409 `send-not-available` (+`why`) on a conversation offering no identity, and NO proposal is created · `GET /api/agent/channels/status[?id=]` → own proposals with receipts (somebody else's id = not-found) · `POST /api/agent/channels/request` `{conv, why}` → `{ok, request}` (only on a `requestable` row; hidden = not-found). Docs topic `channels`.
- `POST /api/sessions/:id/msg-reachability` (cookie) — {level: inherit|visible|messageable} per-session widening override.
- agentd op `peer-post` {cid, text} → `peer-post-result` {ok, reason, peerName} (capability 'peer-post').
- **The wrapper stdin frame carries a TYPED ORIGIN (2026-09-07)**: rung 1.5 of the
  delivery ladder writes `{type:'peer-message', text, fromName, cardText, kind}`
  where `kind` is `'notification'` (VibeSpace itself speaking — a Background Work
  event, a system notice; set by `jobs.js _deliverTo`) or `'peer'` (a person's
  message from another session — `POST /api/agent/msg/send`, and the DEFAULT for
  any absent/unknown value, so an older caller never silently changes lane).
  `deliverToConversation(cid, text, {kind})` echoes the resolved kind in its
  result. The LADDER only tags; the receiving WRAPPER picks the lane, because
  only it knows whether a turn is running. On codex a busy `'notification'` is
  **steered** (`turn/steer` with just that text — the queue is neither read nor
  written) and answers `peer_message_result {ok:true, mode:'steered'}`; a busy
  `'peer'` still answers `mode:'queued'`; idle is `mode:'turn'` for both. A
  refused steer (the turn ended between the check and the RPC, or a
  review/compact turn) FALLS BACK to the queue/turn lane and says so:
  `{ok:true, mode:'queued'|'turn', steerFailed:<reason>, steerDetail?}`. The ACP
  wrapper has no steer method at all, so it answers `peer_result {ok:true,
  mode:'queued', steer:'unsupported'}`. Which lane a harness uses is DERIVED
  from its capability row — `backend-caps notificationDelivery({peerDelivery,
  inputModes})` → `steer | queue | cli-inbox | stash` — never a backend id.

#### Host-capability plugins (⚙ → Plugins; src/plugins.js)
- `GET /api/plugins` — `{plugins:[…]}` for the panel (tailscale / frp / opencode-serve): def + `enabled` + that plugin's `status()`.
- `GET /api/plugins/:id/status` · `POST /api/plugins/:id/{install|start|stop|login|enabled|mode|config}` — the per-plugin lifecycle (all cookie-authed; each returns `{error}` with a reason, never a silent no-op).
- `POST /api/plugins/:id/prompted` `{prompted}` (2026-09-07) — "we already offered this plugin". The OpenCode background service is off by default and its first-use dialog is asked ONCE **per instance**, so the flag is store state (data/plugins.json) that broadcasts — never per-browser localStorage.
- Broadcast `plugins-updated` carries `{plugins, services}`; `services[<id>]` is the compact `{enabled, desiredUp, prompted, installed, running, starting, parked, envForced, reason}` row. `/api/home`'s harness rows carry the same row as `service` when a harness descriptor declares `store.servicePlugin`.

#### OpenCode serve actions (S9 remainder, B-eac2 — src/routes/opencode.js)
EVERY route takes `host` (query for GETs, body for POSTs) and hands it STRAIGHT to the ONE access layer (`src/server/opencode-access.js`): this machine's own facts / the `opencode-serve` agentd op on a paired device / the shipped `data/bin/vibespace-opencode-op` over ssh. `hostId` is a parameter, never a branch, and nothing downstream asks "is this remote?" again.
- `GET /api/opencode/state?host=` — `{installed, ready, parked, parkedKind, version, port, caps, liveLaneHealthy, pendingQuestions, busySessions, lastError}` for the serve on that machine.
- `POST /api/opencode/revert` `{id, messageID, partID?, cwd?, host?}` — roll the conversation back to a message: OpenCode restores the working tree to the snapshot taken before it and stages every later message for removal. Answers `{ok, session}` (the session's `revert` field is the staged state) and broadcasts `opencode-updated {kind:'revert', sessionId, revert}`. **v1 route only.** Measured on a real 1.18.29 serve with /proc: `POST /session/:id/revert` restores the tree and boots NO OpenCode instance (16 threads / 0 indexer threads before and after), while a single `POST /api/session/:id/revert/clear` (the v2 stage/clear/commit trio) boots one: 16→37 threads, 0→19 `fff` indexer threads, +165 MB. The v2 trio is therefore deliberately NOT wired — see openIssues in the S9 row of docs/design-harness-plugins.md.
- `POST /api/opencode/unrevert` `{id, cwd?, host?}` — OpenCode's own "clear the staged roll-back": the files come back and the messages are live again. Broadcasts `opencode-updated {kind:'unrevert'}`.
- `GET /api/opencode/questions?sessionId=&refresh=&host=` — the `question` tool's PENDING requests (`[{id:'que_…', sessionID, questions:[{question, header, options:[{label, description}], multiple?, custom?}], tool:{messageID, callID}}]`). `refresh=0` reads the live lane's warm map; the default re-reads the authoritative `GET /question`, which is what makes a pending card survive a page reload.
- `POST /api/opencode/question/:requestId/reply` `{answers, host?}` — `answers` is either OpenCode's POSITIONAL array-of-arrays (one array of selected labels per question, in order) or the ask card's question-text map, which the shared converter turns into the positional form. Broadcasts `opencode-updated {kind:'question-replied', requestId, sessionId}`. Both this and the reject route re-read `/question` once when the live lane's warm map does not hold the request (a server that restarted since the card was rendered): the question is the only thing that names the conversation, which the broadcast, the cache drop and the own-write ledger all need.
- `POST /api/opencode/question/:requestId/reject` `{host?}` — dismiss it. Broadcasts `opencode-updated {kind:'question-rejected', requestId, sessionId}`.
- `GET /api/opencode/status?host=` — `{statuses:{<ses_id>: {type:'idle'|'busy'|'retry', …}}}`, the serve's IN-PROCESS view (a conversation another opencode process is driving is absent from it — that is why 'external' liveness is derived from the session list's own `time.updated` as well).
- `GET /api/opencode/todos?id=&host=` — the agent's own todo list for a conversation.
- agentd op `browser-serve` `{action, params}` → `browser-serve-result` `{result}` (capability `'browser-serve'`; agent browser P4). `action` ∈ version / status / start / stop / cdp-url, `params.profileId` = `bp-<8 hex>` (start also `idleMs`, `headed`); the daemon BUNDLES src/browser-serve.js and runs the same `runBrowserServeOp()` against its own facts (the machine composes and owns `~/.agent-browser/vs-bp-<id>`, answers its OWN loopback CDP url; the hub `tcpForward`s the port). A refusal is the runner's own `{ok:false, code, error}` inside `result`, never a thrown `error`. `DeviceManager.browserServe(action, params)` is gated on the capability — an old daemon is never asked.
- agentd op `opencode-serve` `{action, params}` → `opencode-serve-result` `{result}` (capability `'opencode-serve'`). The daemon BUNDLES src/opencode-serve.js and calls the same `runOpencodeOp()` against its own facts, so a paired device behaves exactly like this machine (keeper, caches and live lane included). Op names/params/result keys live in the SHARED table `src/opencode-remote.js`; `data/bin/vibespace-opencode-op` mirrors it for checkout-less ssh hosts and `scripts/test-opencode-remote.mjs` fails a one-sided edit. `pty-open|pty-close|pty-resize` are LOCAL-ONLY and refuse loudly on a remote machine (the serve streams its ptys over a loopback websocket the hub cannot reach there).
### Permission rules + local oracles (2026-09-07, owner rulings 10 & 6 — BOTH READ-ONLY)
- `GET /api/permission-rules?backend=&scope=session|instance&sessionId=&cwd=&accountId=&host=` — "where does this rule come from". Answers the ONE typed record `{backend, scope, cwd, host, ok, reason, detail, layers:[{id,label,scope,file,present,note,rules:[{kind:'allow'|'deny'|'ask'|'value', key, value, note}]}], truncated}`; `ok:false` carries a code from the closed set `unsupported-harness | remote-session | not-installed | store-unavailable | wrapper-old | no-live-session | would-connect | read-failed | no-config | unknown` and NEVER any layers (an empty tree would read as "this agent has no rules"). Which rung runs is decided by the `backend-caps` `permissionRules {source, session, instance, liveVerb}` row, never by a backend id: claude = the settings hierarchy read off disk under the session's cwd + HOME (managed → user → project → local + every `managed-settings.d/*.json` drop-in as its own source, 1 MiB per file); codex session-scope = the session's own wrapper (`config/read {cwd, includeLayers:true}` — layers + origins, i.e. which layer WON each key; it must be the session's own app-server because a `sessionFlags` layer exists that a fresh child cannot see); codex instance-scope = **NOT OFFERED** (`permissionRules.instance:false`): the only way to answer it is a fresh `codex app-server` child, and that child was measured with `strace -f -qq -e trace=network` under an EMPTY CODEX_HOME opening 7 INET connects incl. chatgpt.com:443 before it will read a local TOML — the same class this repo rejects `codex doctor` for, so the read answers `would-connect` carrying the measurement (proof + verdict: src/local-oracles.js `codex-app-server-config-read`, whose `blocks:` field names that caps row so re-enabling it without a new measurement fails the build); opencode = the serve's **v1 `GET /config`** (never the v2 `/api/permission/saved` twin — re-measured 2026-09-07 on 1.18.29: v1 left the serve at threads 15 / inotify fds 0 / flat RSS across four calls, the v2 twin took it to threads 37 / inotify fds 2 / RSS 345→507 MB; the 2.369.50 law). A REMOTE session answers `remote-session` instead of describing this machine's files — for EVERY harness including a live remote codex one, whose frame would reach the remote wrapper but whose sidecar advert is written on the other machine (so the second gate could only ever answer the misleading `wrapper-old`); the client sends `host` from `s.host` and a `cwd` stripped of the host-label (2.225.2), pinned by a headless-chrome leg that drives the real Session-Properties door. There is **no write route and no write verb anywhere behind this** (codex's `config/value/write`/`config/batchWrite` optimistic concurrency turns one careless write into data loss).
- `POST /api/local-oracle/:id` `{accountId?}` — run ONE registered local oracle and return `{ok, id, label, backend, argv, exitCode, json, jsonError, stdout, stderr, streams, truncated}` (256 KiB cap; both streams always travel — `codex login status` answers on stderr). POST, not GET, because it spawns a process and a GET is pre-fetchable by browsers and crawlers — "never on a timer" must not lose by accident. `id` selects a FROZEN registry entry (src/local-oracles.js) and its argv; a caller can never supply a command. Refusals: 404 `unknown-oracle`, 400 `not-installed` (the CLI is absent) / `no-runner` (no command is wired for that harness — a different fact) / `account` / `spawn` / `timeout`. Only commands MEASURED at zero INET connects ship (kb-design-lessons §9): `codex login status`, `codex mcp list --json`, `codex features list`. `claude auth status`, `claude agents --json`, `codex doctor --json` and `codex app-server` (the retired instance-scope rung) were measured and REJECTED (5/5/16/7 INET connects to vendor endpoints) and are surfaced in the UI as disabled rows with the reason. A rejection carrying `blocks:'<backend>.<capsPath>'` explains a MISSING BUTTON and is shown ALWAYS — the older "only when a harness has no oracles" rule would have hidden precisely the one that matters, since codex has three shipped oracles.

### OTel truth ingest (2.361.0)
- `POST /otel/v1/logs` — OTLP JSON from LOCAL claude CLIs (spawn-injected env). Cookie-exempt; the ONLY gate = loopback remoteAddress + per-boot `x-vibespace-otel` header token. api_request events → truth map (rid→acct) + corrective attribution + data/usage-history/otel-truth.ndjson.
- `POST /otel/v1/metrics`, `POST /otel/v1/traces` — tolerant 200 (not consumed; exporter configured logs-only).

## WebSocket Protocol (`/ws`)
Client → Server: `create` (incl. `fork` flag — S9: a harness whose store forks server-side (opencode: POST /session/:id/fork on the local serve) mints the new id BEFORE the spawn and resumes it; any failure answers `{type:'error', code:'fork-failed', message:'Fork failed: …', reqId}` and no session is spawned; explicit-cwd creates are PREFLIGHTED (2.226.0) — a missing dir refuses with `code:'cwd-missing'`+cwd+hostName unless `recreateCwd:true` (B-7812 danger flow: server mkdirs + arms a one-shot `<vibespace-cwd-notice>` in prompt-context, persisted via session-meta `cwdRecreated`)), `input`, `chat-input`, `permission-response`, `set-permission-mode`, `set-model`, `set-effort`, `set-response-style`, `set-goal`, `review-start`, `queue-op`, `rename-session`, `size-override`, `interrupt`, `resize`, `attach`, `kill`, `tmux-attach`, `state-set`, `state-resync`, `layout-sync`, `desktop-create`, `desktop-delete`, `desktop-rename`
Server → Client: `created`, `output`, `msg` (normalized: op=create/edit/meta), `subagent-message` (parentToolUseId for tool card status), `streaming-label` (`label`, `kind` — `'compacting'` while a `/compact` turn runs, 2.365.0; null otherwise), `turn-state` (B3 §2.5: `{sessionId, state:'idle'|'running'|'requires_action', authoritative:true}` — the HARNESS's own turn state, pushed on every change; today only claude publishes one, and only when `CLAUDE_CODE_EMIT_SESSION_STATE_EVENTS` was on its spawn env. A session that never emits one simply never sends this frame and the client keeps the derived state — `null` is "not reported", never `idle`), `tools-in-progress` (`{sessionId, ids:[tool_use_id]}` — the RESOLVED set of tools the harness says are EXECUTING right now, from claude's `set_in_progress_tool_use_ids` delta records; the client toggles `.chat-tool-inflight`, because a PENDING card is not a RUNNING one. **DORMANT: no harness emits the source record today** — 2.1.257 hands it to a host callback (`onInProgressToolUseIDs`) and it never reaches a stream-json consumer, so `caps.inProgressTools` is false everywhere and this frame is never sent; test-stdout-registry re-measures the live wire every run), `compact-progress` (`{sessionId, event:'hooks_start'|'compact_start'|'compact_end', hookType, hint, result, error}` — card-less like `api_retry`: the spinner label rides `streaming-label`, and this frame is what tells the "Compact now" guidance card to replace its hardcoded 1–2-minute apology with the CLI's real stage, and at the end with the real OUTCOME (`result:'success'` / `error`) rather than reverting to an apology for something that finished. ONE frame shape, and on 2.1.257 exactly ONE producer: the CLI's `system/status` records (`{status:'compacting'}` → compact_start, a `hook_started` inside that stage → hooks_start, `{status:null, compact_result|compact_error}` → compact_end) — which is also the only lane that sees an AUTO compaction; PLUS a terminal `compact_end` with `result:null` whenever the TURN ends (`result`/`compact_boundary`, or the harness's own idle `session_state_changed`) while a compaction is still in flight. `_streamingKind === 'compacting'` is a claim about RIGHT NOW that the client mirrors as a held stage gating the whole guidance card, so EVERY place the server retires that claim says so (one named `retireCompaction`): a PreCompact hook that BLOCKS the compaction emits no outcome record at all, and the ws-handler `/compact` send site sets the kind before the CLI has said anything — before this frame the client kept "Compacting: running <hook> hooks…" forever and every later "Prompt is too long" card lost its rewind-and-retry sentence (2026-09-07). `result:null` on purpose: "ended" is not "succeeded". The declared `compact_progress` record feeds the same frame but has never been observed on our stdout), `page-published` (2.366.0, 2.366.1 also fires on flag changes/removal: `{sessionId, page}` — publish/republish, a visibility or name change, or removal (`page.removed:true`) of a page of this session, by the agent CLI or the file-browser dialog), `goal-updated` (goal/goalStatus/goalElapsed/statusMsg), `exited`, `attached` (includes `messages`, `totalCount`, `isStreaming`, `streamingLabel`, `streamingKind`, `chatStatus`, `taskState`, `goal`, `goalStatus`, `goalElapsed`, `queue`, `queueSupported`, `queueVerbs`, `queueKnown`, `responseStyleLive`, `turnState`, `inProgressTools`), `active-sessions`, `editor-open`, `editor-close`, `effective-size`, `user-state-updated`, `bookmarks-updated`, `settings-updated`, `custom-themes-updated`, `state-sync`, `state-snapshot`, `layout-sync`, `desktop-updated`, `tasks-updated`, `session-status-updated`, `accounts-updated`, `harness-caps-updated` (S9: `{backend, caps:{fork}}` — a harness capability VERIFIED at runtime, today opencode fork from the serve OpenAPI probed lazily after the first discovery; the client merges it into BACKEND_META caps and re-renders the sidebar; `/api/home harnesses[].caps` carries the same verdict for later loads), `harness-store-updated` (2.369.50: `{backend, parked, parkedKind:'runaway'|'crash'|null, reason}` — a harness STORE that broke; today the opencode serve keeper parking after crashes or after its /proc RUNAWAY guard stopped it. The client stores `reason` on BACKEND_META[backend].storeReason (user actions quote it) and toasts a NEW reason once; `/api/home harnesses[].storeReason` carries the same string for later loads. Pushed only on a CHANGE of reason, so it is not a heartbeat), `server-notice` (2.226.0 probe→user channel: {key,text,level} → client toast + notification history; USE THIS for any new server-side probe instead of inventing a channel — serverNotice(key,text,{level}) in server.js is key-deduped per boot and keeps undelivered keys for retry), `sysinfo-alert`, `error`. Resume no-convo breaker: a failed `claude --resume` arms a 10-min per-conversation cooldown (2.207.1 anti-bootloop); when the transcript verifiably exists the client pops an unmissable retry-now confirm that bypasses it via `ignoreNoConvo` (2.237.0 — the 2.227.3 retry bar went unnoticed at the window bottom, real report). Session `exited` carries `reason`+`detail` from classifyCliDeath (2.226.0 — known CLI canned errors surfaced in the read-only bar / exited overlay; new death signatures belong in that one classifier), `killed` (2.369.16: reply to the REQUESTER of a `kill` — `{sessionId: <the id the client SENT>, resolvedId: <after the stale-id remap>, ok:true}` after the attached-only `exited` broadcast, or `{ok:false, reason:'not-found'}`; the client's `app.killSession` re-sends an unacknowledged plain kill on every reconnect until one of these lands, 3-minute watchdog disarms), `attach-ack` also carries `{progress:{done,total}|null, queued}` every 10s while the session's first-attach history rebuild is queued/running (2.369.16), `attach-ack` (synchronous proof-of-life before the possibly slow `attached`). **Unknown client→server type (Plugin Ph1, the switch's new `default:`)**: `{ type:'error', code:'unknown-type', message:'Unknown message type: <type>', unknownType, reqId? }` — reqId echoed when the request carried one (so `ws.request` callers fail fast), **never a sessionId** (session-scoped `error` frames flip live windows into the exited/read-only path; an unknown type is not a dead session); server side = rate-limited log (one line per type per 10 min with the running count) + telemetry event `ws-unknown-type`. Before this the type was silently ignored. **`session-vcs` (design-unknown-records, 2026-09-21: `{sessionId, host, kind, branch, cwd, at}` — the claude `vcs_state_changed` fact, pushed by session-brain.noteVcsState from either feed to EVERY connected client (not only the session's attached ones — the explorer that must re-list may be a client's only window); the client re-lists every open File Explorer whose directory is that cwd or under it on the same host; the card's git chip rides `active-sessions` (`vcs`), the timeline row rides session-status history; `attached` also carries `backgroundTasks` = the harness's last published level set or null).**

**Capability-gated session verbs (§2.13, 2026-09-07)**: `review-start` is accepted only when `capsOf(session.backend).review` (codex today) and the codex thread-name writeback on `rename` only when `capsOf(...).renameWriteback` — both were `session.backend === 'codex'` branches. The client mirrors (`BACKEND_META[id].caps.review` / `.renameWriteback` / `.forkAtMessage`) are deep-compared against the server rows by test-harness-contract, which also pins all SIX call sites — including the CLIENT trigger that decides whether `rename-session` is sent at all (`sidebar-state.js renameSession`, gated on the mirror row since round 3) — against a re-introduced backend-id branch, and pins the rename trigger positively so the gate cannot be deleted instead. **The manual codex quota verbs (2.369.151; the reset verb REMOVED in 2.369.157 r2 — it was a second writer outside the engine's `writeResetCredit`, with no spend ceiling, no per-identity floor and no origin, so its failure walked the switch/wait ladder as an auto attempt; a person now spends a credit only through `POST /api/accounts/:id/reset-credit`, and an old client's `codex-reset-credit` frame gets the `default:` `{type:'error', code:'unknown-type'}` answer — what follows describes 2.369.151)**: `{type:'codex-reset-credit', sessionId}` (consume a stored rate-limit reset credit) and `{type:'codex-read-limits', sessionId}` (an on-demand account/rateLimits/read) are written as the SAME-named wrapper stdin verb only for a live CHAT session whose harness row says so — reset credit needs `capsOf(session.backend).resetCredit === true`, read limits needs `quotaProbe === 'rpc-rate-limits'` (the rung usage-pool-engine's probeQuotaForKey writes the same verb on); both were one `session.backend === 'codex'` branch. Codex is served both; claude / shell / an unknown id get `{type:'error', sessionId, code:'not-codex-chat'}` (a scoped refusal the client keeps in its explicit allow-list). The reply rides the event stream as `rate_limits_updated`. Pinned in test-harness-contract (the SITES census + a positive read + the unchanged verdict per harness). No client surface sends `codex-reset-credit` today (the ⟳ sends `codex-read-limits` — and its CLIENT half picks the live session through the client caps mirror `backendFeatureCaps(s.backend).quotaRefresh === 'session-rpc'`, never `(s.backend || 'claude') === 'codex'` (2.369.151; SITES row 'client codex ⟳ trigger' + a positive pin; the census checker also catches the DEFAULTED `(x || 'claude') === 'codex'` spelling it used to read as clean); the engine's auto-consume writes the stdin verb itself, through the spend authorizer — `codex-reset-credit` in SPEND_REASONS).
**Input queue (`queue-op`, 2026-09-06; VERB TABLE 2026-09-07)**: `{type:'queue-op', sessionId, op, id, afterId?, text?}` — act on a message the user sent DURING a running turn. `op` is one of the closed set `QUEUE_VERBS` = `remove | steer | steer-all | reorder | edit | run-now | run-all`, validated against the harness's `backend-caps` `inputModes.queueVerbs` table (NEVER a backend id; the old `{steer, queueOps}` booleans are a DERIVED view of that table), then formatted by the adapter's `formatQueueOp` and written to the wrapper's stdin. **The frame speaks RELATIVE user intent and the wrapper synthesises the app-server's absolute shapes** (design-harness-features §2.1, owner decision 1): `reorder` carries `afterId` (the id it lands BEHIND; `null` = the front of the queue — a POSITION, so the key is present only when the client sent one) and the wrapper turns it into `thread/queue/reorder {queuedSubmissionIds}` — the FULL order — computed from a freshly paged list, so an item another writer queued in between keeps its place; `edit` carries only the new `text` and the wrapper rebuilds the whole `input`, preserving image/localImage/audio/localAudio/skill/mention BY EXCLUSION and clearing the replaced element's `text_elements` — and, when the text really changed, it also emits a `webui_user_retracted` for that row's `clientUserMessageId` (merge review 2026-09-07: the bubble already on screen claims the PRE-EDIT words, codex will commit the NEW ones and can therefore never retire that claim, and an unretired claim DELETES an unrelated codex-only record of the same words later; the bubble itself stays, per this verb's client law that an edit leaves the bubble's meaning untouched); `run-now` REQUIRES its id (`formatQueueOp` throws without one) because `thread/queue/start` with no id drains the whole queue — `run-all` is its own verb for that. Refusals: a verb the harness does not serve is refused with a sentence naming what happens to the message anyway (plus a machine `reason`); the wrapper answers `busy` (a turn is running), `not-editable` (an agent-to-agent message), `anchor-gone`, `stale-order`, `incomplete`, `empty`, `empty-text`, `gone`. A SECOND gate follows it: the RUNNING wrapper's own `caps.inputQueue` advert (`wrapperCaps()` over the sidecar the wrapper writes) **or** the in-band proof that it has already published a `queue_changed` (a REMOTE wrapper's sidecar lives on ITS machine) — a session spawned before this release passes the caps row and would have its frame dropped silently (2.361.1/2.364.1), so it is refused with "its agent predates the input-queue update … Terminate + Resume". Every refusal answers `{type:'error', code:'queue-op-unsupported', scope:'action', sessionId, op, id, error, message}` naming the harness and what it CAN do — **`op` and `id` are ECHOED** because this reply never becomes a `queue_op_result`, and the strip marked that row pending BEFORE dispatching (the client feeds the echo to `setQueueOpResult`, an id-less batch verb ending every pending row). Two SIZE gates guard the one verb that carries user text: `edit` is refused (`reason:'text-too-long'`) above **20000 characters** — the wrapper's own `QUEUE_EDIT_MAX_CHARS`, above which it publishes no `text` and therefore offers no edit control — and the built frame is refused (`reason:'frame-too-large'`) above **64 KiB**, the same threshold the `input` case treats as the pty-stdin shredding line (JSON escaping can multiply a control-char string sixfold, so the character cap does not imply the byte one). The queue-op frame has NO frame-file bypass; it is bounded instead. **A per-session `error` is a SCOPED refusal (renders in-chat, window untouched) only when it carries `scope:'action'` or one of the client's explicit codes (`input-rejected`, `not-codex-chat`, `queue-op-unsupported`)** — "any code is scoped" is wrong: `ended-during-attach` is coded AND fatal, and must keep taking the view-only rescue + Resume bar. Wrapper → server events (inside the normal record stream): `queued_input {msg_id, turn_id}`, `queue_changed {items:[{id, msgId, preview, text?, ts, kind, from}], turn_id, verbs:[…]}` (the WHOLE queue on every change — the app-server's own `thread/queue/changed` carries no items, so the wrapper re-`list`s, PAGING THROUGH `nextCursor` to the end since the verb table (2026-09-07): a full-order verb computed from a truncated page is refused by the app-server, and a truncated publication lies about what is queued; `text` is the item's FULL text for editable user items ≤20000 chars, so the edit control opens the message and not the 120-char preview; `verbs` is the running wrapper's own verb advert, the only one a REMOTE session's orchestrator can see), `queue_op_result {op, id, msg_id, ok, reason, detail}`. A `queue_op_result` also reaches the client as `{op:'meta', subtype:'queue-result', queueOp, id, ok, reason, text}` so the strip row can end its pending state and wear the refusal. The normalizer turns `queue_changed` into `{op:'meta', subtype:'queue', items}` (session state — never a transcript message) and stamps the originating user bubble with `queueState: 'queued'|'steered'|'removed'|null` through the ordinary `edit` op. `attached` and `created` both carry `queue` (the normalizer's `queueState()`), **`queueSupported`** and **`queueVerbs`** (the verb list the RUNNING wrapper serves — its sidecar `caps.queueVerbs`, or the in-band list from its `queue_changed`; a wrapper that adverts a queue with no list is taken to serve the pre-table `remove/steer/steer-all`, so an older session keeps its controls and is refused only the new verbs) — attach resolves `queueSupported` from the wrapper's sidecar advert OR the normalizer having seen a `queue_changed`; create is always `false`. **`queueVerbs` is `null` — never `[]` — when the server knows NOTHING** (create always; attach when neither the sidecar nor an in-band list is readable): an empty ARRAY is a real answer (a wrapper that named no verbs), and the client INTERSECTS with it, so answering `[]` for "unknown" hid the entire strip from a session the server would have served the legacy three for. On the in-band path the client applies the same mapping the server does — a `queue_changed` carrying no `verbs` means `remove/steer/steer-all` (`LEGACY_QUEUE_VERBS`, one frozen array in the PURE `src/backend-caps.js`, imported by both ends). A reconnecting or newly created window therefore starts from a fact. A `queued_input` from a wrapper that never published a queue falls back to the old system card instead of stamping a chip that could never clear. **`interrupt` (Stop) CLEARS THE QUEUE on every harness (owner decision 2026-09-07)**: both wrappers emit a `queue_op_result {op:'remove', ok:true, msg_id, reason:'stopped'}` per dropped entry BEFORE the emptied `queue_changed` (the normalizer clears the chip of anything that leaves the queue with NO result — that means "it ran"), and a dropped agent-to-agent/job message is handed back to the delivery ladder (`peer_message_result`/`peer_result` `ok:false`). codex deletes every queued item BEFORE it sends `turn/interrupt` — the app-server drains its own queue when the turn ends, interrupt included, so the other order loses the race; a `thread/queue/delete` that fails answers `ok:false` (the item is still queued and WILL run) rather than a silently "cleared" queue, a delete the app-server REFUSES (`{deleted:false}` = it drained the item first, so it RAN) answers `ok:false, reason:'gone'` and its peer entry is NOT re-stashed, and the whole sweep is BUDGETED (~2.5s/RPC, ~6s total) so a wedged app-server never holds Stop — whatever is left is reported `ok:false, reason:'timeout'` and the interrupt goes out. **A SECOND `interrupt` frame (double-click, or a second attached client) is the SAME Stop**: the codex wrapper's sweep is single-flight (the second frame rides it — a second sweep used to report the first one's removals as `gone`) and `turn/interrupt` coalesces per turn while its RPC is unanswered, so the pair produces ONE list, ONE delete per item and ONE interrupt. The client half: the Stop button disables itself and reads "Stopping…" until the turn ends or an 8s fallback re-arms it.
**Live response style (`set-response-style`, 2.369.54)**: `{type:'set-response-style', sessionId, style}` — change a RUNNING chat session's response style (`style:''` clears it and hands the choice back to the agent's own config). Three gates, all required and all capability-based: the harness's `backend-caps` `responseStyle {live, values}` row must say `live` (claude reads `--settings outputStyle` once at spawn, so it is refused here and the chip's menu offers "Restart now to apply" instead), the value must be in that row's `values` (codex's `Personality` enum, read from the app-server's own JSON schema), and the RUNNING wrapper must advert `caps.responseStyle` in the sidecar IT writes (the 2.361.1/2.364.1 skew rule — a session spawned before this release would drop the stdin verb silently). Every refusal answers `{type:'error', code:'style-not-live', scope:'action', sessionId, error, message}` (scoped: renders in-chat, leaves the live window alone). On success the server writes `session._outputStyle`, persists it to session meta and broadcasts `{type:'response-style-updated', sessionId, outputStyle, live:true}` — the chip moves on THAT echo, never optimistically, so a refused switch never leaves a lie on the status bar. The EFFECTIVE value also rides the `active-sessions` payload (`outputStyle`, null = the agent's own config decides) so Session Properties can name the value AND its origin.
**Live response style (`set-response-style`, 2.369.58)**: `{type:'set-response-style', sessionId, style}` — change a RUNNING chat session's response style (`style:''` clears it and hands the choice back to the agent's own config). Three gates, all required and all capability-based: the harness's `backend-caps` `responseStyle {live, values}` row must say `live` (claude reads `--settings outputStyle` once at spawn, so it is refused here and the chip's menu offers "Restart now to apply" instead), the value must be in that row's `values` (codex's `Personality` enum, read from the app-server's own JSON schema), and the RUNNING wrapper must advert `caps.responseStyle` in the sidecar IT writes (the 2.361.1/2.364.1 skew rule — a session spawned before this release would drop the stdin verb silently). Every refusal answers `{type:'error', code:'style-not-live'|'style-wrapper-old', scope:'action', sessionId, error, message}` (scoped: renders in-chat, leaves the live window alone). **The two codes mean different things to the UI** (2.369.58 r2, the 2.363.1 one-code-many-meanings law): `style-wrapper-old` = THIS session's wrapper will never serve the verb, so the client stops offering the live switch and shows "⟳ Restart now to apply"; `style-not-live` = every other reason (dead session, spawn-only harness, out-of-enum value, or a sidecar the wrapper has not written YET), and the client changes no capability belief. On success the server writes `session._outputStyle`, persists it to session meta and broadcasts `{type:'response-style-updated', sessionId, outputStyle, live:true}` — the chip moves on THAT echo, never optimistically, so a refused switch never leaves a lie on the status bar. The EFFECTIVE value also rides the `active-sessions` payload (`outputStyle`, null = the agent's own config decides) so Session Properties can name the value AND its origin. **`attached` also carries `responseStyleLive`** (the running wrapper's `caps.responseStyle` advert, same shape and same reason as `queueSupported`): the client needs BOTH facts to decide whether to offer the live switch or the "Restart now to apply" row, and with only the caps row every pre-release codex session got a refusal it could do nothing about. It is TRI-STATE: `null` when the wrapper has not written its sidecar yet (unknown ≠ "cannot"). `created` deliberately OMITS the key — the sidecar does not exist yet, so the client's third state (undefined = try it) stands until the refusal or the echo answers it.
**Command list + init facts (`msg` op=meta, subtype `slash-commands`, 2026-09-07 / §2.6 of docs/design-harness-features.md)**: `{op:'meta', subtype:'slash-commands', data:{commands:[name…], terminal:[name…]}}` — the harness's CURRENT slash-command list and the terminal-BOUND subset the composer must hide (claude's init frame `slash_commands` + `terminal_slash_commands`; the same op re-fires on the CLI's `system`/`commands_changed` push, on ACP's `available_commands_update` and on the codex wrapper's served list — ONE client path). **REPLACE, never append**: a command absent from the payload is gone. It is a `meta` op and NOT an `edit` on the init card on purpose — ChatView does not re-render a COMPLETE system card, so an `edit` never re-runs its side effects and the composer would keep the boot list all session. The ATTACH/HTTP twin is `chatStatus.initFrame` (session-store `chatStatus()`, `{tools, agents, skills, betas, slashCommands, terminalSlashCommands, mcpServers[{name,status}], mcpServerErrors, plugins, pluginErrors, pluginWarnings, outputStyle, version, memoryPaths{auto,team}}` — claude only, `null` elsewhere; keys the CLI did not send are ABSENT, never zero-filled), which also carries `slashCommands` already reconciled with the newest `commands_changed` — **by POSITION**: the push replaces the frame's list only when it is newer than the newest `init` (a resume/wrapper respawn re-inits AFTER a mid-run push, and the twin must land where the live stream did), and an init that declares no list at all leaves the push standing (live, the op is simply not re-emitted for it). Same facts ride the init card's `initData.frame`.
**Background tasks (`msg` op=meta, subtype `background-tasks`, design-unknown-records 2026-09-21)**: `{op:'meta', subtype:'background-tasks', data:{tasks:[{id, type, description}]}}` — claude's `system/background_tasks_changed`, a LEVEL signal (the FULL live set, emitted whenever membership changes). The normalizer (the reconcile's ONE owner) SOFT-closes every running BACKGROUNDED task card the set no longer names before emitting it (`taskInfo.status:'finished'`, `closedBy:'level'` — a foreground call is never a member of the set and is never touched; the outcome record that follows overwrites the guess, r3 2026-09-21); the status bar reads its count ("N background tasks") and closes nothing itself; `attached.backgroundTasks` carries the same list for a window that opens mid-run (null = the harness never published one — the card-derived set stands).

**The queue after a RESTART — `queueKnown` + `queue-resync` (2026-09-09)**: a queue publication is a `queue_changed` record on the wrapper's stdout, that stdout is an 800KB head-dropped RING, and that ring is what a restarted server rebuilds the session normalizer from — so after a restart `queueState()` is `[]` **whether the wrapper's queue is empty or holds 25 items, and the two are the same bytes on the wire**. `attached` and `created` therefore carry **`queueKnown`** beside `queue`: `false` = the list is a PLACEHOLDER (this server has never heard this wrapper publish), `true` = a fact. `created` is always `false` (the app-server's queue belongs to the THREAD, so a RESUMED thread hands a brand-new wrapper items it never filled). A payload from before the field simply omits it and the client reads it as `true`. **The client applies the rows either way** — an unknown queue IS the empty list on the wire, and one round trip of no rows beats a row nobody can act on — but suppresses the INFERENCE: only a KNOWN list retires the `Queued` chip of a rendered bubble it does not list. **`queue-resync`** is the answer: a stdin verb (`{type:'queue-resync'}`, `BackendAdapter.formatQueueResync`; the base class REFUSES, so a harness with no queue cannot format it) that makes the wrapper re-publish its queue **including an EMPTY one**, FORCED past `publishQueue`'s own fingerprint dedup — "still empty" is precisely the sentence a deduping publisher cannot say, which is also why the `refreshQueue()` a wrapper runs after a `gone` verdict corrects nobody. It makes **no RPC** (the wrapper re-states what it already believes; an attach may never wait on a wedged app-server), and a resync that lands while the Stop sweep owns the publish is OWED and paid after the sweep's own closing refresh. The attach ASKS only while `queueKnown` is false (self-limiting: the answer flips the flag) and only behind BOTH gates — the harness's `inputModes.queueVerbs` row AND the RUNNING wrapper's `caps.queueResync` sidecar advert. The second gate is load-bearing in an unusual way: the ACP wrapper answers an unknown stdin verb with a VISIBLE error card, so asking an older process would put a red notice in the user's chat on every attach; a wrapper that cannot answer (old build, or REMOTE — its sidecar lives on ITS machine) simply leaves the client on the honest "no rows" state until its own next publication. The answer arrives as an ordinary `queue_changed` through the normal consumer → normalizer → `{op:'meta', subtype:'queue'}` path, so it corrects every attached client at once. **And `queue_op_result {ok:false, reason:'gone'}` REMOVES the row** (client-side) instead of marking it refused: absence is a fact from the process that owns the queue, and a refusal marker there is permanent — the wrapper's own follow-up refresh is fingerprint-deduped away. Every other refusal keeps the row.
**Permission-rule read (`read-permission-rules`, 2026-09-07 — the ONE new stdin verb, READ-ONLY)**: not a ws message — the server writes it to the wrapper's stdin from `GET /api/permission-rules` when the caps row says `permissionRules.liveVerb` AND the RUNNING wrapper adverts `caps.permissionRules` in its sidecar (the 2.361.1/2.364.1 pair; an older wrapper is refused `wrapper-old` with its start time and "Terminate + Resume", never handed a frame it would drop silently). Frame: `{type:'read-permission-rules', requestId}` from `adapter.formatReadPermissionRules` — **both wrappers serve it in the same batch** (design §6 landing discipline). The codex wrapper answers `event_msg {type:'permission_rules', ok:true, requestId, cwd, source:'config/read', config, origins, layers, truncated}` — only the permission-bearing keys travel (`approval_policy, approvals_reviewer, sandbox_mode, sandbox_workspace_write, permissions, default_permissions, include_permissions_instructions`, mirroring `CODEX_PERMISSION_KEYS` in the PURE module and pinned for parity because the wrapper is a shipped single file) plus ONLY this session's own `projects.<cwd>.trust_level`, the layers WITHOUT their config blobs, and a 32 KiB ceiling that says when it capped; the user's model/provider/plugin world and other people's project paths never enter an agent-visible journal. The ACP wrapper answers a TYPED refusal `{kind:'permission_rules', ok:false, requestId, reason:'unsupported-by-protocol', detail, mode, modes}` — ACP v1 has no config-read method, and the one permission fact the protocol DOES carry (the session's live mode) travels with the refusal; it adverts the cap on purpose, because a wrapper that adverts nothing is indistinguishable from one too old to answer and the server would refuse with the wrong reason. Answers are routed by `requestId` (never "the next record wins") and a wrapper that never answers times out into a typed `read-failed` record.
Client → Server: `create` (incl. `fork` flag — S9: a harness whose store forks server-side (opencode: POST /session/:id/fork on the local serve) mints the new id BEFORE the spawn and resumes it; any failure answers `{type:'error', code:'fork-failed', message:'Fork failed: …', reqId}` and no session is spawned; explicit-cwd creates are PREFLIGHTED (2.226.0) — a missing dir refuses with `code:'cwd-missing'`+cwd+hostName unless `recreateCwd:true` (B-7812 danger flow: server mkdirs + arms a one-shot `<vibespace-cwd-notice>` in prompt-context, persisted via session-meta `cwdRecreated`)), `input`, `chat-input`, `permission-response`, `set-permission-mode`, `set-model`, `set-effort`, `set-goal`, `review-start`, `queue-op`, `rename-session`, `size-override`, `interrupt`, `resize`, `attach`, `kill`, `tmux-attach`, `state-set`, `state-resync`, `layout-sync`, `desktop-create`, `desktop-delete`, `desktop-rename`
Server → Client: `created`, `output`, `msg` (normalized: op=create/edit/meta), `subagent-message` (parentToolUseId for tool card status), `streaming-label` (`label`, `kind` — `'compacting'` while a `/compact` turn runs, 2.365.0; null otherwise), `turn-state` (B3 §2.5: `{sessionId, state:'idle'|'running'|'requires_action', authoritative:true}` — the HARNESS's own turn state, pushed on every change; today only claude publishes one, and only when `CLAUDE_CODE_EMIT_SESSION_STATE_EVENTS` was on its spawn env. A session that never emits one simply never sends this frame and the client keeps the derived state — `null` is "not reported", never `idle`), `tools-in-progress` (`{sessionId, ids:[tool_use_id]}` — the RESOLVED set of tools the harness says are EXECUTING right now, from claude's `set_in_progress_tool_use_ids` delta records; the client toggles `.chat-tool-inflight`, because a PENDING card is not a RUNNING one. **DORMANT: no harness emits the source record today** — 2.1.257 hands it to a host callback (`onInProgressToolUseIDs`) and it never reaches a stream-json consumer, so `caps.inProgressTools` is false everywhere and this frame is never sent; test-stdout-registry re-measures the live wire every run), `compact-progress` (`{sessionId, event:'hooks_start'|'compact_start'|'compact_end', hookType, hint, result, error}` — card-less like `api_retry`: the spinner label rides `streaming-label`, and this frame is what tells the "Compact now" guidance card to replace its hardcoded 1–2-minute apology with the CLI's real stage, and at the end with the real OUTCOME (`result:'success'` / `error`) rather than reverting to an apology for something that finished. ONE frame shape, and on 2.1.257 exactly ONE producer: the CLI's `system/status` records (`{status:'compacting'}` → compact_start, a `hook_started` inside that stage → hooks_start, `{status:null, compact_result|compact_error}` → compact_end) — which is also the only lane that sees an AUTO compaction; PLUS a terminal `compact_end` with `result:null` whenever the TURN ends (`result`/`compact_boundary`, or the harness's own idle `session_state_changed`) while a compaction is still in flight. `_streamingKind === 'compacting'` is a claim about RIGHT NOW that the client mirrors as a held stage gating the whole guidance card, so EVERY place the server retires that claim says so (one named `retireCompaction`): a PreCompact hook that BLOCKS the compaction emits no outcome record at all, and the ws-handler `/compact` send site sets the kind before the CLI has said anything — before this frame the client kept "Compacting: running <hook> hooks…" forever and every later "Prompt is too long" card lost its rewind-and-retry sentence (2026-09-07). `result:null` on purpose: "ended" is not "succeeded". The declared `compact_progress` record feeds the same frame but has never been observed on our stdout), `page-published` (2.366.0, 2.366.1 also fires on flag changes/removal: `{sessionId, page}` — publish/republish, a visibility or name change, or removal (`page.removed:true`) of a page of this session, by the agent CLI or the file-browser dialog), `goal-updated` (goal/goalStatus/goalElapsed/statusMsg), `exited`, `attached` (includes `messages`, `totalCount`, `isStreaming`, `streamingLabel`, `streamingKind`, `chatStatus`, `taskState`, `goal`, `goalStatus`, `goalElapsed`, `queue`, `queueSupported`, `turnState`, `inProgressTools`), `active-sessions`, `editor-open`, `editor-close`, `effective-size`, `user-state-updated`, `bookmarks-updated`, `settings-updated`, `custom-themes-updated`, `state-sync`, `state-snapshot`, `layout-sync`, `desktop-updated`, `tasks-updated`, `session-status-updated`, `accounts-updated`, `harness-caps-updated` (S9: `{backend, caps:{fork}}` — a harness capability VERIFIED at runtime, today opencode fork from the serve OpenAPI probed lazily after the first discovery; the client merges it into BACKEND_META caps and re-renders the sidebar; `/api/home harnesses[].caps` carries the same verdict for later loads), `harness-store-updated` (2.369.50: `{backend, parked, parkedKind:'runaway'|'crash'|null, reason}` — a harness STORE that broke; today the opencode serve keeper parking after crashes or after its /proc RUNAWAY guard stopped it. The client stores `reason` on BACKEND_META[backend].storeReason (user actions quote it) and toasts a NEW reason once; `/api/home harnesses[].storeReason` carries the same string for later loads — BOTH from the ONE predicate `storeFailureReason()` in src/server/cli-env.js, which is true only for a PARK: a background service that is simply OFF (the opt-in `opencode-serve` plugin, or the `VIBESPACE_OPENCODE_SERVE=0` ops switch) is NOT a broken store and carries no reason, or the red toast would fire on every page load (2026-09-07). Its off-ness rides `/api/home harnesses[].service` instead. Pushed only on a CHANGE of reason, so it is not a heartbeat), `server-notice` (2.226.0 probe→user channel: {key,text,level} → client toast + notification history; USE THIS for any new server-side probe instead of inventing a channel — serverNotice(key,text,{level}) in server.js is key-deduped per boot and keeps undelivered keys for retry), `sysinfo-alert`, `error`. Resume no-convo breaker: a failed `claude --resume` arms a 10-min per-conversation cooldown (2.207.1 anti-bootloop); when the transcript verifiably exists the client pops an unmissable retry-now confirm that bypasses it via `ignoreNoConvo` (2.237.0 — the 2.227.3 retry bar went unnoticed at the window bottom, real report). Session `exited` carries `reason`+`detail` from classifyCliDeath (2.226.0 — known CLI canned errors surfaced in the read-only bar / exited overlay; new death signatures belong in that one classifier), `killed` (2.369.16: reply to the REQUESTER of a `kill` — `{sessionId: <the id the client SENT>, resolvedId: <after the stale-id remap>, ok:true}` after the attached-only `exited` broadcast, or `{ok:false, reason:'not-found'}`; the client's `app.killSession` re-sends an unacknowledged plain kill on every reconnect until one of these lands, 3-minute watchdog disarms), `attach-ack` also carries `{progress:{done,total}|null, queued}` every 10s while the session's first-attach history rebuild is queued/running (2.369.16), `attach-ack` (synchronous proof-of-life before the possibly slow `attached`). **Unknown client→server type (Plugin Ph1, the switch's new `default:`)**: `{ type:'error', code:'unknown-type', message:'Unknown message type: <type>', unknownType, reqId? }` — reqId echoed when the request carried one (so `ws.request` callers fail fast), **never a sessionId** (session-scoped `error` frames flip live windows into the exited/read-only path; an unknown type is not a dead session); server side = rate-limited log (one line per type per 10 min with the running count) + telemetry event `ws-unknown-type`. Before this the type was silently ignored. **`session-vcs` (design-unknown-records, 2026-09-21: `{sessionId, host, kind, branch, cwd, at}` — the claude `vcs_state_changed` fact, pushed by session-brain.noteVcsState from either feed to EVERY connected client (not only the session's attached ones — the explorer that must re-list may be a client's only window); the client re-lists every open File Explorer whose directory is that cwd or under it on the same host; the card's git chip rides `active-sessions` (`vcs`), the timeline row rides session-status history; `attached` also carries `backgroundTasks` = the harness's last published level set or null).**

**Input queue (`queue-op`, 2026-09-06)**: `{type:'queue-op', sessionId, op:'steer'|'remove'|'steer-all', id}` — act on a message the user sent DURING a running turn. Validated against the harness's `backend-caps` `inputModes {queue, steer, queueOps}` row (NEVER a backend id), then formatted by the adapter's `formatQueueOp` and written to the wrapper's stdin. A SECOND gate follows it: the RUNNING wrapper's own `caps.inputQueue` advert (`wrapperCaps()` over the sidecar the wrapper writes) **or** the in-band proof that it has already published a `queue_changed` (a REMOTE wrapper's sidecar lives on ITS machine) — a session spawned before this release passes the caps row and would have its frame dropped silently (2.361.1/2.364.1), so it is refused with "its agent predates the input-queue update … Terminate + Resume". Every refusal answers `{type:'error', code:'queue-op-unsupported', scope:'action', sessionId, error, message}` naming the harness and what it CAN do. **A per-session `error` is a SCOPED refusal (renders in-chat, window untouched) only when it carries `scope:'action'` or one of the client's explicit codes (`input-rejected`, `not-codex-chat`, `queue-op-unsupported`)** — "any code is scoped" is wrong: `ended-during-attach` is coded AND fatal, and must keep taking the view-only rescue + Resume bar. Wrapper → server events (inside the normal record stream): `queued_input {msg_id, turn_id}`, `queue_changed {items:[{id, msgId, preview, ts, kind, from}], turn_id}` (the WHOLE queue on every change — the app-server's own `thread/queue/changed` carries no items, so the wrapper re-`list`s), `queue_op_result {op, id, msg_id, ok, reason, detail}`. The normalizer turns `queue_changed` into `{op:'meta', subtype:'queue', items}` (session state — never a transcript message) and stamps the originating user bubble with `queueState: 'queued'|'steered'|'removed'|null` through the ordinary `edit` op. `attached` and `created` both carry `queue` (the normalizer's `queueState()`) **and `queueSupported`** (attach: the wrapper's sidecar advert OR the normalizer having seen a `queue_changed`; create: always `false` — a just-spawned wrapper has reported nothing yet, and its baseline `queue_changed` turns the controls on moments later), so a reconnecting or newly created window starts from a fact. A `queued_input` from a wrapper that never published a queue falls back to the old system card instead of stamping a chip that could never clear.

**OpenCode ask cards (S9 remainder, B-eac2)**: `permission-response` gained `via` (+ `host`). A card built from an OpenCode `question` tool declares `via:'opencode-serve'`, and the ws layer answers it on the SERVE's own route instead of a session's stdin — the conversation may have no live process here at all (a stopped conversation opened read-only can still show a pending ask). The card itself stays harness-neutral: it forwards what the record that created it declared, never a backend id. A refusal answers `{type:'error', scope:'action', code:'opencode-question', requestId, error}` — the card flips itself to "answered" optimistically, so silence would be a lie, and `scope:'action'` is load-bearing: without it a session-scoped `error` frame is read as "attach failed" and flips the LIVE window into the read-only Resume bar (the 2.363.1 law — a refusal rejects ONE action, it never condemns the conversation). Server → client `opencode-updated` `{kind:'revert'|'unrevert'|'question-replied'|'question-rejected'|'store', sessionId, requestId?, revert?, host?, reason?}` is the multi-client echo: every browser re-polls the session list and any open window on that conversation says in-line what happened. `kind:'store'` is the LIVE LANE's own invalidation (cache invalidation must NOTIFY): the SSE stream or the sqlite watch saw a change nobody asked about — including one made by a TUI on the same store — and the push is what makes it appear without waiting for that browser's 5s poll. It is COALESCED server-side (`OPENCODE_CHANGE_COALESCE_MS`, 2s) and streamed message parts are deliberately excluded, because each push costs every client one discovery sweep.

**OpenCode serve terminal (S9 remainder piece (c))**: `create` gained `opencodePty:true` — the session's pty is one the OpenCode SERVE owns (`POST /pty` on the serve, streamed over `GET /pty/{id}/connect`) instead of a local dtach spawn. src/server/opencode-pty-bridge.js connects that websocket SERVER-SIDE and hands the session layer the same `{pid,onData,onExit,write,resize,kill}` shim the daemon pipe-session uses, so xterm, input, resize and the buffer file work unchanged. The serve's port, its Basic auth header and its connect ticket NEVER reach the browser.

**Virtual session attach**: `attach` with `sessionId` starting with `sub-` routes to subagent handler. `sub-{parentToolUseId}` returns live-buffered messages from parent session's `subagentBuffers`. `sub-agent-{agentId}` loads completed agent's JSONL from disk. Both respond with standard `attached` payload with normalized messages. Live virtual sessions receive normalized `msg` ops via per-subagent normalizers. `attach` with `viewOnly:true` loads JSONL history without an active session (for stopped session history viewing).


## Channels — the communication panel (P0a, docs/design-communication-panel.zh.md)

EVERY route takes `host` and passes it down: `hostId` is a PARAMETER, never a
branch (decision 15). v1 serves THIS machine only, and that is a NAMED refusal
(`501 host-not-served`) for anything else — a silent local answer to a
question about another machine is the failure the rule exists to stop.

- `GET /api/channels[?host=]` — the index DIGEST: adapters (with their
  resolved lane), conversations (title/kind/participants/lastAt/unread/tracked),
  each conversation's resolved `convCaps`, its `offers` (read / send-as-user /
  send-as-bot, each `{offered, why}`), its `identityWarning`
  `{level, marking, verbatim}` and its `freshness` claim
  `{kind, state, seconds, why?}` — `state:'off'` with `why`
  (`untracked` / `adapter-disabled` / the scan resolver's own reason) for a row
  nothing will ever fetch (r3), rendered "not polling" / "not scanning" — plus
  `unreadTotal`. Each conversation's `lane` is `{via, why, source}` as the
  resolver answered it, and on a scan lane `source` is exactly what the ingest
  was gated on (r3): `source:null` means nothing was ingested. **Never message bodies, and
  never a composed human SENTENCE**: this payload is broadcast to every client
  at once while the language is per DEVICE, so the client renders the words
  from the structure (`channelCaps.freshnessText` / `identityWarningText`).
  Each adapter's `auth` is `{state: connected|expired|unknown, why, expiresAt}`
  — a RESOLUTION, never a stub.
- `GET /api/channels/:adapterId/:convId[?host=]` — one conversation's summary
  plus its adapter row (what the window's context bar shows). 404 with a reason.
- `GET /api/channels/:adapterId/:convId/messages?before=&beforeId=&limit=` —
  ONE page of normalized `ChannelRecord`s, oldest-first by `(at, vendorId)`,
  strictly before the BOUNDARY RECORD `(before, beforeId)`. `limit` is clamped
  to 200. **The boundary is a PAIR, not an instant**: `at` is not unique (a
  Lark burst shares a millisecond, Gmail's `internalDate` is second-derived)
  and paging on it alone stranded every record of such a group at or after a
  page boundary. `beforeId` is that record's `vendorId`, which invariant 2
  makes unique per conversation; omitting it is still terminating but lossy.
  **The reader seeks as far back as the writer keeps (r3)**: it used to read
  only the newest 2 MiB, so a page boundary past that answered `[]` for ever
  while retention kept the records; it now walks earlier windows until the
  page is full or byte 0 is reached.
- `POST /api/channels/:adapterId/:convId/read` `{at?}` — mark read; the engine
  re-derives `unread` from the log rather than trusting a counter, and with no
  `at` the instant is the NEWEST RECORD's — for future-dated records (with
  `now()` a future-dated vendor record keeps `unread` above zero for ever) AND
  for past-stamped ones (r3: the r2 spelling was `max(now(), newest.at)`, so
  for every real adapter a message stamped before the mark but fetched after
  it was silently marked read). With no record at all the mark is left where
  it was. **404 on an id this instance does not hold** — a route may not mint
  an index row — and a mark that changed nothing broadcasts nothing.
- `POST /api/channels/:adapterId/:convId/track` `{tracked}` — the opt-in that
  decides whether anything is ingested at all. Tracking refreshes that
  conversation's `convCaps` (one of its three named refresh triggers),
  BROADCASTS the persisted change (r3 — it used to rely on the pass it kicks,
  which a spent request budget returns from before notifying), and then kicks
  a pass. 404 on an id nobody has discovered.

- **P1a — the adapter's own consent flow and options** (design §10.1, §12.4,
  §13; every failure answers `{error, code, detail}` with a status BY CODE:
  `409 needs-credentials` = the row's integration resolves to none — the
  client opens the Integrations card FIRST; `501 not-supported` = an
  undeclared capability / a kind with no consent flow; `401 auth-expired`;
  `502` = a vendor refusal; `404 no-such-adapter`).
  - `POST /api/channels/adapters/:kind/connect` `{credentialKey?, newAccount?}`
    — THE ACCOUNT MODEL (2026-09-22): mints a NEW account of the kind (the
    first record's id IS the kind, every further one `<kind>:<8 hex>`) when
    `newAccount:true` or no record of the kind exists, stamped with
    `credentialKey` = `cluster:<presetKey>` | `own` (validated against what
    the integration offers RIGHT NOW — `400 unknown-credential` naming it and
    the offered keys otherwise; omitted = the integration's current pick) and
    begins its consent flow under THAT credential. Without `newAccount` on a
    kind that already has an account it is the P1a path: the FIRST account is
    re-authorized, a `credentialKey` there judged exactly as by
    `/reauthorize` (token-less ⇒ re-bind; bound ⇒ `400 credential-bound`).
    Answers `{adapter, flow}` where `flow` is `{flowId, mode,
    running, consentUrl, redirectUri, port, listening, refusal, pasteBack:true,
    startedAt, expiresAt}` — never the flow's `state`. A fixed-mode port
    already held answers `refusal:{code:'port-busy'}` on the flow and the
    flow keeps running on paste-back. `409 needs-credentials` when the
    account's key resolves to none (a withdrawn preset says `preset-gone`
    naming it — never another client).
  - `POST /api/channels/adapters/:id/reauthorize` `{credentialKey?}` —
    re-begins ONE account's consent flow by its adapter id (never by kind)
    under ITS credential. A key RE-BINDS a TOKEN-LESS account (never
    authenticated / disconnected — nothing is minted under its key) after
    validation (`400 unknown-credential`), and is `400 credential-bound
    {error, detail:{key, bound}}` on an account HOLDING a token bound to
    another credential (its stamp, else what the token itself names) — never
    dropped (verifier r1). A legacy record with no key is stamped first by its
    token's own evidence, else the integration's current pick.
    `404 no-such-adapter`.
  - `POST /api/channels/adapters/:id/auth/finish` `{url}` — PASTE-BACK: the
    redirect URL the user's browser landed on; `400 auth-failed` with the
    reason (a `state` mismatch says "restart the flow"); `501 no-flow` when
    nothing is running.
  - `POST /api/channels/adapters/:id/auth/cancel` — `{ok, cancelled}`.
  - `POST /api/channels/adapters/:id/disconnect` — drops the token (the
    FIRST account's record and conversations stay), cancels a running flow,
    retracts the failure item; a FURTHER account (`<kind>:<hex>`) is REMOVED
    with its index rows — only its own — and answers `{ok, removed:true}`.
  - `PUT /api/channels/adapters/:id` `{enabled?, options?}` — enable/disable
    and/or the adapter's DECLARED options (an undeclared key or a value
    outside its `choices` is a `400` naming it; `''` restores the default; a
    change re-runs discovery).
  - **P1b:** `PUT /api/channels/adapters/:id` also takes `{push: {enabled?,
    claimedExclusive?}}` — the push switch (an opt-in lane, Gmail, is OFF
    until `enabled:true`) and the exclusivity DECLARATION (`exclusive` /
    `shared` / `unknown`, a closed set — anything else is a `400` naming it;
    `400 no-push-lane` on an adapter that declares none). Sending
    `claimedExclusive` is a RE-DECLARATION: it clears a demotion, zeroes the
    miss counters and retries the lane once. Answers `{ok, push}`.
- **P2 — assign / filter / estimate** (design §7, §10.2; every failure
  `{error, code}` with a status BY CODE: `404 not-found` on an id nobody
  discovered — never a minted row; `400 bad-assignment` / `bad-filter` /
  `no-such-filter` / `filter-in-use`; `409 authority-capped` when
  `authority:'send'` is asked for where either cap forbids it, the reason in
  `error`):
  - `PUT /api/channels/:adapterId/:convId/assignment` `{assignment, estimateAtSet?}`
    — `assignment` = `{principal:{kind:'agent'|'group', id, name?}, mode:
    'all'|'filtered', filterId?, notify:'wake'|'digest', digestMinutes?,
    authority:'draft'|'send', dailyWakeCap?}` (defaults all / wake / 30 min /
    draft / 40); `assignment:null` unassigns (removes ONLY the
    `origin:'assignment'` reach grant). Answers `{ok, assignment}` AS IT READS
    (`authority` clamped, `authorityClamped`, `authorityWhy`, `authorityStored`).
  - `PUT /api/channels/:adapterId/:convId/filter` `{filter, estimate?}` —
    `filter` = `{name?, match:'any'|'every', rules:[{kind, …}]}` over the
    CLOSED rule set; `filter:null` clears it (refused `filter-in-use` while a
    filtered assignment names it). Answers `{ok, filter}` with the saved
    `estimateAtSet`.
  - `POST /api/channels/:adapterId/:convId/estimate` `{filter}` (`null` = all
    messages) — runs the PURE estimator SERVER-SIDE over the stored log
    (≤ 5000 records; the client never sees the corpus). Answers `{ok,
    estimate:{matched, total, matchedPerDay, totalPerDay, windowDays,
    sampled, truncated}}`.
  - The digest (`GET /api/channels` / `channels-updated`) carries per
    conversation `assignment` (as it reads), `filter`, `stats` `{hits7d,
    msgs7d, wakes24h, wakes7d, lastWake:{at, n, ok, lane, why, whys, cid},
    pending, lastRefusal}`, `authorityCaps` `{offersSend, sendWhy,
    policyRequiresReview}` and `wakeLatency` `{lane:'push'|'poll'|'scan'|
    'reconcile', seconds, coalesceSeconds, kick?, source?, why?}` — STRUCTURE;
    the editor says the words.
  The digest (`GET /api/channels`) now also carries `available` (connectable
  kinds not yet connected: `{kind, label, integration, credential:{source,
  why, missing, clusterLabel, credentialKey}, credentials, receive, sendAs}`) and per adapter
  `connectable`, `integration`, `credential`, `flow`, `lastAuthError`,
  `lastAuthAt`, `failureItem {id, code, at}`, `options`, `optionsSchema` and
  `auth.user`/`auth.scopes`/`auth.credentialSource` — never a token.
  **The account model (2026-09-22):** per adapter `credentialKey`
  (`cluster:<presetKey>` | `own` | null = legacy, follows the row's pick),
  `credentialLabel` (a preset's env label; `own` is worded by the client),
  `credential` = the facts resolved FOR THAT KEY, `credentials` = every
  credential a further account of this kind may bind to (`[{key, label,
  source, presetKey}]`, key + label only — the wizard shows a credential step
  only when more than one is offered), `credentialDefault` (the row's CURRENT
  pick = what a further account is bound to unless the wizard says otherwise —
  never this account's own key; c2), `auth.credentialKey` and
  `auth.tokenHeld` (does the record hold a token — false = never
  authenticated / disconnected, the ONE state the Connect verb may re-bind);
  `available[]` carries the same `credentials` list and `credentialDefault`.
  **P1b:** each adapter row carries `push` (`null` when it declares no push
  lane): `{enabled, optIn, transport, claimedExclusive, state, lastStateWhy,
  lastEventAt, demotedAt, demotedWhy, demoted{missed,total,rate,threshold,at},
  redeclaredAt, missRate{rate,total,missed,enough,threshold}, contentSince,
  droppedUntracked}` — the STRUCTURE; the sentence is the client's
  (`channelCaps.pushLaneText`). Never the raw sample batches.

WS: `channels-updated` `{changed:[convId], digest}` — ONE broadcast per
ingest pass (or per mutation), carrying the recomputed RESULT so a client
repaints without a fetch. Never one per message.

**P3 — outbox · policy · reach (design §8, §9):** every answer is typed;
`answer3` maps `not-found` → 404, `send-not-available` / `bad-state` / `reconcile-not-available` → 409,
malformed input → 400, `failed` / `unknown` → 502.
  - `GET /api/channels/outbox[?conv=<adapterId>/<convId>&limit=]` — the
    proposals (newest first, ≤ 500) + `awaitingTotal` / `unknownTotal`; each
    proposal is `proposalView`: `{id, adapterId, convId, key, title, text,
    originalText, replyTo, why, attachments, draftedBy:{kind, id, name},
    authority, at, updatedAt, state, policy:{mode, reasons, detail}, sendAs,
    identity:{sentAs, marking, where, text}, identityWarning, ttlAt,
    awaitingSince, edited, approvedBy, reason, result:{vendorMessageId, at,
    sentAs, lane}, receipt, receiptDelivery, history, canDecide}`.
    **a3 i18n (2026-09-18): STRUCTURE beside every sentence.** `proposalView`
    also carries `outcome` (`{kind: boot|threw|lost|send-not-available|not-landed|refused|expired|rejected, code, detail, userReason}`,
    PURE `channel-policy.outcomeOf`) and `reconcileWhyCode` — `reason` stays
    the English contract string agents read; the card words `outcome`. The
    digest's conversation row carries `adapterLabel` and
    `assignment.authorityWhyCap` (`{code: policy-review|send-not-offered, sendWhy}`);
    an adapter row carries `builtin` + `auth.self` (the built-in row's login
    is this instance, `auth.user` null) and `credential.whyCode/whyParams`;
    `lane.why`, `auth.why`, `lastPass.code`, a wake's `lane`/`refused` are
    CODES the client words (`channel-caps` authWhyText / laneWhyText /
    errorCodeText / deliveryLaneText / wakeRefusalText). Route failures are
    worded by `code` on the client (`src/lib/channel-words.js routeErrorText`).
  - `POST /api/channels/:adapterId/:convId/propose` `{text, replyTo?, why?,
    attachments?}` — the USER's own draft (authority `send`), judged by the
    same policy + guards → `{ok, proposal, decision}`; 409
    `send-not-available` creates nothing.
  - `POST /api/channels/outbox/:id/approve` `{text?}` (a different text =
    approve edited) — re-resolves `convCaps` first; `{ok:true, proposal}` when
    sent, else `{ok:false, code:'send-not-available'|'failed'|'unknown',
    error, proposal}`.
  - `POST /api/channels/outbox/:id/reject` `{reason?}` → `{ok, proposal}`.
  - **P4:** `POST /api/channels/outbox/:id/reconcile` — a PERSON asks whether
    a LOST send landed (the only way out of `unknown`) → `{ok:true, resolved,
    state, answer:'landed'|'not-landed'|'unknown', reason?, proposal}`
    (`resolved:false` = still unknown, the ask counted); 409
    `reconcile-not-available` when the adapter's declared idempotency cannot
    answer (`none`), 409 `bad-state` on anything but an unknown proposal.
    `proposalView` gained `honestyLine` (the exact line that will/was
    appended, else null), `canReconcile` / `reconcileWhy`, `attemptAt`,
    `wire:{text, honestyLine, at}`, `sendHandle`, `reconcile:{n, lastAt,
    lastBy, lastAnswer, lastWhy, resolvedAt}`, `result.observed` /
    `result.honestyLine` / `result.reconciled`; the receipt gained
    `reconciled` + `honestyLine`.
  - **P4:** `PUT /api/channels/adapters/:id` also takes `{senderHonestyLine:
    true|false|null}` (null = follow `channels.senderHonestyLine`) →
    `{ok, senderHonestyLine:{record, effective}}`; the digest row carries
    `senderHonestyLine` (null on a read-only adapter) and `identityObserved`.
  - `PUT /api/channels/:adapterId/:convId/policy` `{mode:'direct'|'review'|null}`
    (null = the adapter's default) → `{ok, policy:{mode, source, declared}}`.
  - `PUT /api/channels/:adapterId/:convId/reach` `{principal:{kind:'agent'|
    'group', id, name?}, level:'visible'|'requestable'|null}` — writes (or with
    null removes) the USER-origin grant only → `{ok, reach:{entries:[{id,
    principal, scope, level, origin, at, by}], requests}}`.
  - `POST /api/channels/reach-requests/:id/approve|deny` → `{ok, request,
    grant}` (approve = exactly ONE visible grant, origin `request`).
  - The digest carries per conversation `policy` `{mode, source, declared}`,
    `reach` `{entries, requests}`, `outbox` `{awaiting, unknown, latestAt}`,
    the assignment's `receiptWake`, and at the top `awaitingTotal`.

WS: `channel-outbox-updated` `{changed:[proposalId], outbox}` — one
broadcast per proposal-store change carrying the recomputed outbox view; the
inline section and the Outbox window repaint from it, never from a poll.

## Integrations & keys (P0b, docs/design-communication-panel.zh.md §14)

**a3 i18n (2026-09-18):** `publicView` carries `whyCode`/`whyParams` beside
`why` and `clusterWhyCode`/`clusterWhyParams` beside `clusterWhy` (the closed
set: no-values · no-preset · preset-gone{key} · ambiguous{n,prefer} ·
undecryptable{fields} · store-unreadable · rebound{from,to} · no-store ·
lookup-failed — PURE `integration-registry.credentialWhyText` words them),
plus `usedBy` (a declared phrase; `consumers` stays for tooling and leaves
the card). Every declared `label` / `help` / `callbackNote` / prerequisite /
`test.describe` / `test.caveat` / `wiredIn` is an i18n KEY (`i18nKey(…)` in
the registry; the client renders `t(key)`); `TRIM_NOTE` is said once per card.

All behind the cookie auth. Every body is `publicView(id)` — THE masked view
(`src/server/integration-store.js`) — and **no route ever returns a secret
field's plaintext**: not GET, and there is no "reveal". A set secret is
`masked: {field: '••••' + last 4}` (the tail only when the value is ≥ 12
chars). Every failure answers `{error, code, detail}` with a real status so
the client can toast it (`fetchJson` never throws).

- `GET /api/integrations` — `{integrations: [publicView…], storeError}`. Each
  view: `{id, label, fields (declarations only: key/label/secret/required/
  placeholder/help), setup {callbackUrl, callbackNote, prerequisites} | null,
  source: 'user'|'cluster'|'none', why, clusterKey (the preset IN EFFECT),
  savedClusterKey (the selector the user saved), clusterLabel,
  clusterAvailable, clusterWhy, clusterOptions [{key,label}] (key + label
  ONLY — never a preset's secret), delegate {multi, prefer} | null, fromEnv,
  set {field: bool}, masked {secretField: mask|null}, values {nonSecretField},
  missing [requiredFieldKeys], hasOwnValues, testedAt, lastOk, lastError,
  testKind, testDescribe, testCaveat, testButton, consumers, wiredIn, docs,
  storeError}`. `storeError` is `{code, message}` or null — `store-unreadable`
  (data/integrations.json exists but cannot be read; every write below is
  refused with 500 of the same code until it can) outranks the key-file codes
  (`key-unreadable` / `key-malformed`), and a PER-ROW `values-undecryptable`
  `{code, message: <the remedy>, fields}` (r3) says THIS row's stored values
  cannot be opened with the current key file — such a row answers
  `source:'none'` with `why` naming the fields, is NEVER handed to the
  cluster default, and reports `hasOwnValues: true` (a fact about the
  record: Clear my keys is the way out; the list's top-level `storeError`
  stays about the store / key file); a `cluster` row's `why` is non-null
  exactly when its saved default was re-keyed to the instance's only one
  (r2, the non-delegating radio's boolean intent).
- `GET /api/integrations/:id` — `{integration: publicView}`; 404 on an unknown
  row.
- `PUT /api/integrations/:id` — ONE verb, three payload shapes, each routed to
  ITS function: `{values: {field: value}}` → `setIntegration` (omitted =
  untouched, `''` = cleared, else trimmed + validated FIRST — 400
  `invalid-values` with `detail: {field: why}`); `{use: 'cluster'}` →
  `useClusterDefault` (409 `no-cluster-default` BY NAME when the env offers
  none); `{clusterKey: 'key'|null}` → `setClusterKey` (the delegating row's
  dropdown / a multi-default row's picker; stores the SELECTOR only and drops
  the user's own values; 400 `no-such-preset` listing what IS available).
  Answers `{ok:true, integration: publicView}`.
- `POST /api/integrations/:id/test` — the human's click, bounded (15 s), one in
  flight per id, dispatched to the runner the row's CONSUMER registered; the
  store constructs no vendor request. Answers `{ok, error, detail, testedAt,
  caveat, kind, integration}`; a row whose consumer has not landed answers
  501 `not-wired` naming the phase (`detail.wiredIn`) and records nothing.
- `DELETE /api/integrations/:id` — "drop my keys": `clearUserValues`, ALWAYS
  allowed, lands wherever precedence lands (cluster default or none).
  `{ok:true, integration}`. (r2: "always" is a statement about precedence,
  not about the file — PUT, POST test and DELETE alike answer 500
  `store-unreadable` while data/integrations.json cannot be read, because
  a write may not start from a state that is not the file's.)

WS: `integrations-updated` `{id, why: 'set'|'cluster-key'|'use-cluster'|
'clear'|'test', integration: publicView}` — ONE broadcast per write, carrying
the recomputed masked view so every client repaints that one card without a
fetch; it can carry nothing but the masked view by construction.

Cluster env (read ONLY by `src/server/integration-store.js`; the standing
census fails the names anywhere else): `VIBESPACE_INTEGRATIONS` = JSON
`[{id, key?, label?, values: {field: value}}]` (the helm Secret) and
`VIBESPACE_INTEGRATION_<ID>_<FIELD>` (single-field form; the JSON form wins
when both name a row). The `gmail` row reads neither: it delegates to
`VIBESPACE_GDRIVE_CLIENTS` through `MountManager.drivePresets()`.

## Background Work (2.342.0)
- Agent (vsst_ or jbt_ Bearer): POST /api/agent/jobs (create; vendor-vet + schedule floors), GET /api/agent/jobs (view-filtered list), GET /api/agent/jobs/:ref?wait&tail&answers (poll/long-poll, uniform not-found), POST /api/agent/jobs/:ref/{stop|start|rm|progress|ask|access} (control needs canControl; access needs owner + not user-locked; jbt_ tokens: progress/ask on SELF only).
- User (cookie): GET /api/jobs (full), GET /api/jobs/:id?tail, POST /api/jobs/:id/{stop|start|rm|answer|access} (access accepts lock:true/false), GET/POST /api/jobs-secrets (names only on GET; UPPER_SNAKE, 0600 store), GET /api/jobs-escapes (read-only systemd/crontab listing).
- WS broadcast: `jobs-updated {id, removed?}` to cookie clients only.
- TRIAGE (2026-09-14, design §13): every job snapshot carries `ack {acked, by: notified|agent-read|user-opened|null, at}` (server-computed over the FULL record) and `run.lastLine` (the last non-empty, secret-redacted log line of a terminal run, ≤200 cp). `POST /api/jobs/:id/seen` (cookie) stamps `user-opened` on a terminal one-shot and answers `{success, changed, ack}` (a no-op elsewhere); an owner-lineage `GET /api/agent/jobs/:ref` (poll/show/logs) stamps `agent-read` — a jbt_ self-read never does. A new run clears `ack`; a stash drained at injection stamps `notified` on every job it named (at or after that job's terminal instant).
- ARCHIVE (design §13 rule 5): terminal one-shots leave `data/jobs.json` for the append-only `data/jobs-archive.json` (newest 2000; a record keeps runs/notifyLog/ack and gains `archivedAt` + `archivedWhy` ∈ done | failed-acknowledged | missed-acknowledged | interrupted-acknowledged | unverified-acknowledged) on a sweep at boot and every 5 min on the engine's own tick (settings `jobs.archiveDoneAfterHours` default 24 / `jobs.archiveFailedAfterDays` default 7 since the ACK; 0 = never; never a service, a cron parent, a live pid, an open interaction, an unacknowledged failure, or a cron child whose schedule is still active). One `jobs-updated {archived:[ids]}` broadcast per sweep. `GET /api/jobs` carries `archivedCount`, `held` (the typed held-notification digest) and `ownerSession {conversationId, sessionId}` per row; `GET /api/jobs?archived=1` lists the archive (same snapshot shape + `archived:true`, `archivedAt`, `archivedWhy`); `GET /api/jobs/:id` reads through to the archive; `POST /api/jobs/:id/rm` on an archived id deletes it for good (record + log dir, broadcast `{id, removed:true, archived:true}`), any other act answers 400 by name. Agent: `GET /api/agent/jobs?archived=1` (view-filtered archive; `vibespace-job list --archived`), `GET /api/agent/jobs/:ref` reads through (`archived:true`, never a 404 for housekeeping), `POST …/:ref/rm` on an archived ref for control-holders. `rm` on an archived id answers 400 `archive write failed: <error>` and KEEPS the row when the archive cannot be rewritten — the removal is durable or it did not happen (2026-09-16).
- HELD NOTIFICATIONS ARE TYPED (2026-09-15, design §13 5b): every `data/job-notifications.json` stash entry carries `held {kind: spend-cap|rate-floor|not-reachable|off, why?, identity?, cap?, retryAfter?}` (an older entry with no `held` reads as not-reachable); `GET /api/jobs` and EVERY `jobs-updated` broadcast carry `held {total, byConversation: {cid: {count, kinds, reason}}}` (structure only — the client says the words through jobs-layout `heldText`); the delivery ladder's spend refusal `{ok:false, refused:'spend', why, retryAfter, identity:{key,name}, cap}` is what types it. `data/spend-budget.json` stamps are `{at, reason}` (a SPEND_REASONS member; a bare number from an older build reads as reason null / producer `unknown`), and a refusal / the 80 % notice names the window's top producers (`Spent by: job-notification ×9, auto-resume ×1.`).
- `POST /api/jobs/seen` `{ids: [..≤500]}` (2.369.121, the panel's "Mark all seen"): every id that is a terminal one-shot not yet acknowledged is marked `ack {by:'user-opened'}`; ONE save + ONE `jobs-updated {acked:[ids]}` broadcast; answers `{success, requested, acked, ids}`; unknown / running / archived ids are skipped silently. Session folds persist in user state `jobsPanelFolds` keyed by the session key (`s:<cid>` / `w:<sid>` / `manual`) beside the family keys.
- Owner auto-notify (2.344.0): create body accepts `notify: "on"|"off"` (absent = inherit group/global); create RESPONSE carries `notify: {enabled, source: job|group|global, mode: live-message|resume-inject|off, reason?, detail?}`; job snapshots carry `notify` (value or 'inherit') + `lastNotify {ts, lane: message|channel|stash|off|suppressed, ok, to?, reason?}`. Task Group PATCH (`POST /api/tasks/:id`) accepts `jobNotify: true|false|null` (tri-state). 2.344.2: post-create override without recreation — agent `POST /api/agent/jobs/:ref/notify {value: on|off|inherit}` (owner-only canEdit, returns fresh preview; CLI `vibespace-job notify <id> on|off|inherit`); user side rides the access act (`POST /api/jobs/:id/access` accepts `notify`). 2.345.0: `POST /api/agent/jobs/:ref/{subscribe|unsubscribe}` (canView; jbt_ self-tokens excluded) — per-conversation subscription to a visible job's notifications (cap 10, cron parent covers children); snapshot +`subscribersCount`. 2.346.0: act `announce` {text} (jbt_ self-act OR canControl — puts text before the owner, view alone insufficient); create accepts `notifyOk` (scheduled successes notify); stash spill files under data/job-notifications-read/. 2.350.0: GET /api/agent/docs/:topic (index|jobs|task|status|ask|msg|pages|**browser**; vsst_/jbt_) = full per-tool manuals from docs/agent/*-manual.md; /api/agent/jobs-docs = jobs alias; user-todo items carry jobId (resolveByJob clears them on panel answer/expiry/rm). 2.347.0: subscribe accepts {filter: regex≤200} (per-subscriber, case-insensitive over notify text, fail-closed; re-subscribe updates); GET list accepts ?mine=1 / ?subscribed=1; list+detail responses carry `mine` + `mySubscription`; snapshots carry full creation params (cmd argv/cwd/envKeys, envFrom, restart, timeoutMs, untilOutput, notifyUser/notifyOk, catchUp, stopWithOwner, singleInstance). Settings keys: `agents.jobNotify` (bool, default true), `agents.vibespaceChannel` (bool, default false, experimental). Store: `data/job-notifications.json` (conversation-keyed offline stash, engine-owned atomic writes).

## Auto-continue after a usage limit (2.368.0; GENERIC over every harness 2026-09-08)

- WS client→server: `{type:'auto-resume', sessionId, enabled}` — the live per-session toggle (also persisted as session config so it rides the next resume). Server→client: `auto-resume` with `{enabled, explicit, globalDefault, armed, resetsAt, reason, resume, watch, lane, bucket}`; the same object rides `attached` as `autoResume`, and `attached.outputStyle` reports what the live session was spawned with.
- The four fields added 2026-09-08 are what the surfaces gate on, so none of them has to know a backend id: **`resume`** = this harness's resume-verb FORM (`'message'` claude | `'turn-start'` codex | `'prompt'` ACP | `null` = it cannot restart a turn, so it is never armed and the chip is not drawn); **`watch`** = armed but with NO timed continue promised (the reset is past the 26 h ceiling — only a reading saying the quota is back can continue it, and the chip must print no clock); **`lane`** / **`bucket`** = WHICH wall the wait is about (the harness's own `limitId` and bucket name), so a reading about a sibling limit lane or a different bucket can never be read as "the wall is gone".
- Client caps mirror: `capsOf(backend).autoResume = {signal, resume, supported}` — DERIVED from the descriptor (`deriveAutoResume` in src/backend-caps.js, written by the harness registry at load), mirrored key-for-key in `src/lib/agent-meta.js` and deep-compared by scripts/test-harness-contract.mjs.
- Settings: `claude.autoResumeOnLimit` (default for new sessions on EVERY limit-reporting harness, off — the key keeps its legacy prefix because renaming a persisted key is a migration) and `claude.outputStyle` (default style for new sessions).

## This instance's public address (2.367.0)

- `GET /api/instance-url` — `{frpConfigured, frpMissing, subDomainHost, authEnabled, published, desired, auto, url, sub, error, settingUrl, envUrl, effectiveUrl, source}` where `source` ∈ `frp` | `setting` | `env` | `frp-auto` | `null`.
- `POST /api/instance-url/publish {sub?}` — map the whole instance through the frp relay (refused when auth is off or the relay is unconfigured). `DELETE /api/instance-url/publish` — unmap; everything falls back to `agentd.publicUrl`, which was never written. WS broadcast: `instance-url` with the same status.

## Per-session git worktree + claude's agent→user channel (owner rulings 9 and 8(c))

**`create` gains `worktree: true`** (claude only, gated on the `backend-caps` `worktree` row — never a backend id). The server PREFLIGHTS it before spawning, because `--worktree` makes the CLI exit 1 outside a git repo BEFORE the session exists and the window would die instantly with the reason buried in the buffer. Two refusals, both `scope:'action'` (they render in chat and leave live windows alone):
- `{type:'error', code:'worktree-unsupported', scope:'action', reqId}` — the harness has no such flag.
- `{type:'error', code:'worktree-not-a-git-repo', scope:'action', reqId, cwd, hostName?}` — the message names the cwd, the machine, and the CLI's own escape hatch (`git init`, or a `WorktreeCreate` hook in settings.json for another VCS).

The probe runs against a machine HANDLE (`hostId` is a parameter): locally a bounded `git -C <cwd> rev-parse --is-inside-work-tree`, remotely ONE `hosts._hostShell` round trip that answers both halves of the CLI's own `hook OR repo` gate. A probe that cannot ANSWER (timeout, unreachable) is not a refusal — the spawn proceeds and the CLI speaks for itself. The flag itself is emitted by the ADAPTER (`worktreeSpawnArgs`): new ⇒ yes, fork ⇒ yes, **plain resume ⇒ never** (the CLI re-enters its own recorded worktree; a second flag = a second tree). Remote spawns carry it with no transport special-casing at all — it is just another `spec.args` entry that `buildRemoteExec` quotes (`scripts/test-remote-shell.mjs` drift guard). **`--tmux` is never emitted on any path.**

**`active-sessions`, `attached` and `created` all carry `worktree` (boolean) and `worktreePath` (string|null)** — `worktree` is what THIS RUN is, `worktreePath` is the directory the CLI itself announced in its `system/init` frame. `created` carries them for the 2.368.4 reason: the creator window never receives an `attached`, and it is the window that has to draw the badge. **On the client they reach every surface through `LIVE_SESSION_FACTS` in src/lib/sidebar.js** — `_merge()` rebuilds the session row rather than spreading it, so a live fact that is not on that list never reaches the card, Session Properties or the fork; a new payload key must be added there (or to the renamed/derived set) or the drift guard in scripts/test-worktree-userchan-ui.mjs fails.

**`worktree-path` (server → client)**: `{type:'worktree-path', sessionId, worktree: boolean, worktreePath: string|null}` — the init-frame ARBITER, pushed on a CHANGE only (an unchanged re-attach frame is a no-op, so a reconnect storm cannot flap the badge). `worktree:false` is the CLI saying this run is NOT isolated (its worktree was gone — "The worktree binding has been cleared." — or a resume could not create one); the client drops the live badge and **does not** write that back over the user's saved pick.

**"Same cwd" is the QUESTION, not the verdict (2026-09-07 round 4).** The arbiter used to retire the fact whenever the announced cwd equalled the launch cwd — a NEGATIVE inference, and a plain RESUME of a worktree conversation satisfies it: a resume launches in the DISCOVERY cwd, and for such a conversation that IS the worktree (the CLI wrote its transcript from in there, so the JSONL's own `cwd` and the project-dir encoding both name it — measured: `claude --worktree` in `/tmp/vs-wtrepo-probe` produced `~/.claude/projects/-tmp-vs-wtrepo-probe--claude-worktrees-probe9`). So every resumed worktree session was reported as not isolated, permanently, because boot-restore reads `_worktree: !!meta.worktree`. The positive question — *is the announced directory a LINKED worktree?* — is answered by git itself (`rev-parse --git-dir --git-common-dir` differ inside one, agree in a plain checkout), asked at most ONCE per attach per directory, bounded and async, against the machine the session runs on (`hostId` is a PARAMETER: a local child process, or ONE `hosts._hostShell` round trip answering in `__VS_WT_YES__`/`__VS_WT_NO__` markers — never an exit code, because "not a repo" and "git is missing" are different answers). **A probe that cannot ANSWER touches NOTHING** (the ws-create preflight's tri-state rule) and says so in the log + a `worktree-probe-unknown` event. Known limit: a `WorktreeCreate` hook can isolate a run under another VCS, and such a directory also answers "not a git repo" — a resume that re-enters it in place retires the badge.

**`user-file-published` (server → client)**: `{type:'user-file-published', sessionId, toolCallId, files:[{path, name, link?, pageId?, size?, error?}]}` — the result of ONE `SendUserFile` tool call. `link` is a **RELATIVE** `/p/<id>` (the 2.366.1 URL law: the server never guesses an absolute URL; the browser joins it with its own origin). Pages are session-owned and **private by default**. Every failure (missing file, too large, unreadable, publishing unavailable) rides the same message as a per-file `error` and lands ON the card — a file the agent believes it delivered and the user never received is not allowed to be silent. LOCAL sessions only: a remote session's files are on another machine, so the card renders with no link rather than a guessed one. The client keys the rows by `toolCallId` and also re-reads `GET /api/pages?conversationId=` once on attach, so a reloaded history shows the same links a live session does.

**Published pages accept a `mediaType`** (`publishContent({..., mediaType})`, echoed on every page object): `''` = the HTML page path, unchanged (shell + `/raw` + compat prelude). Anything else is normalized through an allowlist (unknown ⇒ `application/octet-stream`) and served from `serveBytes` with `X-Content-Type-Options: nosniff`, `Content-Security-Policy: sandbox; default-src 'none'` (script-less), an explicit `Content-Disposition` (`inline` only for allowlisted viewable types — **SVG downloads, it is a scriptable document**), and a `Content-Length`.

## Published pages + design kit (2.364.0 / 2.366.0)

- `GET /p/:id` — SHELL (2.366.1: our own HTML on the real origin, framing the content; CSP `default-src 'none'; frame-src 'self'`), `GET /p/:id/raw` — the published HTML (CSP `sandbox allow-scripts …` + the compat prelude). Both auth-exempt routes, gated per page: private ⇒ cookie. `GET /api/pages[?sessionId=&conversationId=]` (the chip passes BOTH — a resume mints a new session id), `GET /api/pages/by-path?path=`, `POST /api/pages/publish {path,name,public}` (hub-local file), `POST /api/pages/:id {public,name}`, `DELETE /api/pages/:id` — cookie-authed management; every call notes the browser origin for absolute share URLs.
- `POST /api/agent/pages/publish?title=&path=[&public=1|0]` — body = raw HTML (≤25 MB); auth Bearer vsst_ (session) or jbt_ (job); upsert by `<session's host|local>:path` (the session's host is authoritative — no client-supplied host); `public` omitted = keep the page's current visibility; response `{page:{id,url,path,public,replaced,…}}`. `GET /api/agent/pages` — pages of the caller's session OR its conversation (earlier incarnations of the same chat; a job sees its owner conversation's pages; a caller with neither scope gets `[]`). `GET /api/agent/design-kit` — `{ok,version,source,dir,files(sha256),error}` (builds on first call). `GET /api/agent/design-kit/file/:name` — `seed-canvas.mjs` | `payload.template.html` | `SKILL.md` | `SKILL.orig.md` (remote hosts mirror the kit).
- `GET /api/design-kit/status[?refresh=1]` — cookie-authed kit status for the chat status bar.

## Agent browser profiles (P1, 2026-09-16 — docs/design-agent-browser-v2.md §3.3–§3.5, §5.1)

Every route takes `host` (query on GET, body otherwise) and REFUSES a non-local one BY NAME (`400 unsupported-host`); every failure answers `{error, code}` with a status by code (`not-found` 404 · `label_taken`/`leased`/`running`/`cap`/`runaway-parked`/`ambiguous` 409 · `not_owner` 403 · `launch_failed` 502 · `unavailable` 503). Src: `src/routes/browser.js`; the decisions are PURE `src/browser-profiles.js`, the lifecycle `src/server/browser-keeper.js`.

- `GET /api/browser/profiles` — the digest: `{profiles, leases, browsers, pins, cap:{used,cap}, floor}` (a browser record never carries its CDP url here).
- `POST /api/browser/profiles` `{label, provider?, proxy?, notes?}` — an instance-owned profile (`provider` chromium only in P1; `sharing` other than owner and any `allowedDomains` are refused, D6/§6.3).
- `GET /api/browser/profiles/:id` · `DELETE /api/browser/profiles/:id` (refused while leased or running; the directory is KEPT — D8) · `POST /api/browser/profiles/:id/stop`.
- `POST /api/browser/attach` `{sessionId, profile}` — a LIVE session's conversation (`_browserKey`) leases the profile: `{lease, created, resumed, others, profile, browser, env, pinTab}` — no CDP url. `POST /api/browser/detach` `{sessionId, profile?}`.
- `GET /api/browser/session/:sessionId` — that conversation's leases, pin and the pin's origin. `POST /api/browser/pin` `{sessionId, profile|null}` — the conversation's pin (origin `chosen`), the running session re-pointed through `browser-env.repointPin`, `appliesFrom` says when it takes effect.
- Agent (Bearer `vsst_`, auth-exempt under `/api/agent/`): `GET /api/agent/browser/profiles` (digest + `me`), `POST /api/agent/browser/use` `{profile, wrapper?}` (only `wrapper:true` — the `vibespace-browser --` form — receives `cdpUrl`), `POST /api/agent/browser/new`, `POST /api/agent/browser/detach`, `GET /api/agent/browser/status`, `POST /api/agent/browser/pin`.
- Broadcast `browser-profiles-updated` (the same digest) on every registry commit.
- **P2 (§4.2) `GET /api/browser/stream?session=<webuiId>[&profile=<handle|id>]`** — a cookie-authed WebSocket UPGRADE (401 before the upgrade), bridged server-side to that session's agent-browser stream server (the port never reaches a browser). Server→viewer: `hello {viewers, mode, holder, target:{kind, profileId, alias, label, isDefault, chosen}, protocol}` first, then `viewers {n}`, the upstream's own `status` / `tabs` / `url` / `console` / `command` / `result` records verbatim (ordered, every viewer), `frame {data: base64 JPEG, metadata:{deviceWidth, deviceHeight, …}}` (latest-wins, per-viewer fps-gated), bridge `status {state: connecting|upstream-open|upstream-closed|ended|error, code?, error?}`, `config-ack {maxFps, upstreamMaxFps}`, `pong`, `refused {code: watch-mode|unknown-type|bad-message, error}`. Viewer→server: `config {maxFps}` (per viewer; the MAX goes upstream), `ack {seq}`, `ping`, `input_mouse|input_keyboard|input_touch` (refused `watch-mode` in P2). A remote session is refused by name (`unsupported-host`).
- **P2 (§3.8 layer ③) `POST /api/browser/nudge` `{sessionId}`** → `{queued:true, was, now, rides}` — queues the zero-spend `browser-profile` notice (it rides the user's next message, no billed turn); 409 `nothing-to-remind` when the agent has used no browser yet or is already on the pin; `host` refused by name, 503 without the keeper. The `active-sessions` payload carries `browserProfileActive` (null = never used, '' = the ephemeral browser, else a profile id — stamped by every successful `/api/agent/browser/resolve` / `use`) beside `browserProfileId` (the pin): the status-bar Browser chip's pair, each a scalar `LIVE_SESSION_FACTS` row with its own digest.
- **P3 (§4.3) stream control verbs** — viewer→server `takeover` / `handback` / `confirm {id, decision: confirm|deny}` (decided by the keeper, never forwarded upstream); server→viewer `hello` also carries `you` (this viewer's id), `mine`, `since`; `mode {mode: watch|takeover, holder, mine, since, cause: takeover|explicit|idle|viewer-left|detach, url}` to EVERY viewer of that browser on a takeover/handback; `mode-ack {ok, mode, mine, already?, heldMs?}`; `refused {code: held|not_taken|…}`; `confirmation {id, action, category, at, expiresAt, remainingMs, expired}` (replayed to a late viewer) / `confirmation-resolved {id, decision}` / `confirmation-ack {ok, id, decision, code?, error?}`. `input_*` are forwarded only from the holder; a `mode` record with `cause` reaches every viewer whichever surface flipped it.
- **P3 `POST /api/browser/handback` `{sessionId, profile?}`** → `{ok, cause:'explicit', heldMs, profileId, input}` (409 `not_taken` when nobody drives; no `profile` = whichever of the conversation's browsers is taken; the announcement goes through the gated ladder under `browser-handback`); **`POST /api/browser/confirm` `{sessionId, profile?, id, decision}`** → `{ok, id, decision, pending}` (404 `no_confirmation` — answered or auto-denied; 409 `no-browser` for a session without a browser of its own; 400 `bad-request` for a malformed id/decision). `GET /api/browser/session/:id` + `GET /api/agent/browser/status` carry `inputs` (per browser), `input` (`{input:'user'|'agent', takenAt}` | null) and `pending`. `POST /api/agent/browser/resolve` answers **409 `browser_paused` `{error, takenAt, lastUserInputAt, handles}`** while the user drives that browser. The `active-sessions` payload carries `browserInput` ('user' | 'agent' | null).
- **P4 (§7.1–§7.3):** `GET /api/browser/providers[?host=]` → `{providers:[{id, tier, wired, label, keyScope, canSwitchTo, ownsDir, leaseKind, remote, starts, headed, binary, control:{ok}|{ok:false, code, error}, onHost?:{ok, host}|{ok:false, host, code, error}, proof}], proof, host, hostKnown, cloak:{ok, image, egress, cdpUrl}|{ok:false, code, error}, forwards:[{hostId, remotePort, localPort, connections, refs}]}` — `host` names the machine the answer is ABOUT (not refused; `hostKnown` says whether it is paired). `GET /api/agent/browser/providers[?host=]` (Bearer) answers the same rows. `POST /api/browser/profiles` / `POST /api/agent/browser/new` take `host` (a PAIRED machine; `400 unsupported-host` names an unpaired id) and `cdpPort` (`provider:'cdp'` only; `400 cdp_port_required`). New codes: `provider_needs_local_key` / `provider_local_only` / `provider_lacks_capability` 400, `cdp_unreachable` / `op_failed` / `no_cdp` / `stop_failed` 502, `host_needs_daemon` 409, `host_unavailable` 503. `GET /api/browser/profiles` carries `providers`; a browser record carries `hostId`, `external`, `forward:{remotePort, localPort}` and never `cdpUrl`/`remoteCdpUrl`; `attach`'s `env` carries `AGENT_BROWSER_CDP=<hub loopback url>` (no directory) for a reached browser.
- **P4 second half, the install action (§7.4 failure form (1)):** `GET /api/browser/install` → the PURE verdict, 200 either way: `{ok:true, spec:'cloakbrowser@<measured version>', version, package, proof, prefix, state}` or `{ok:false, code: install_local_only|already_installed|install_running|install_precondition_unmet, error, path?, proof?, prefix, state:{running, startedAt, finishedAt, exitCode, spec, pid, log, error}}` · `POST /api/browser/install` → `{ok:true, started:true, spec, version, prefix, log, pid}` or the typed refusal (400 install_local_only · 409 already_installed / install_running / install_precondition_unmet · 503 install_unavailable = no npm on PATH); `host` is the MACHINE parameter and a non-local one is refused 400 `unsupported-host`. The switcher view (`GET /api/browser/switcher`) carries the same verdict as `install`.
- **P4 second half (§7.4/§7.5):** `GET /api/browser/switcher?profile=<id|label>` → `{profile, chip, rows:[{id, label, tier, current, enabled, code, reason, needsConfirm, integrationId, source, sourceLabel, action:{openIntegration, label}|null, seats:{state:'known-fresh'|'known-stale'|'unknown', tier, total, lastTotal?, at, age, text}|null, fingerprint, fingerprintChange:'gains'|'loses'|null, chip}], seats:{<integrationId>:{state, total, used, source, clusterKey, provider, …}}, siteHints:[{host, tier, backend, by, at, why}], blocked:[{id, url, host, why, evidence, by:'agent', browserKey, sessionId, profileId, tier, at, text}], leases, switching, versions:{recorded, dir, majors}}` · `POST /api/browser/switch` `{profile, provider, sessionId?, confirmDowngrade?, makeDefault?}` → `{ok, mode:'switch', from, to, seed, seedMinted, fingerprint, fingerprintChange, ladder, seats, reopened:[{browserKey, url, ok, targetId, error}], browser, chip, profile}` or `{ok, mode:'proposal', from, to, reason, affected, text, detail, filed}`; a refusal is `{error, code, provider?, integrationId?, action?, waysOut?, needsConfirm?, holders?}` — `backend_unavailable` / `switch_refused` / `switch_export_only` / `hint_tier_with_backend` / `host_underivable` 400, `backend_no_key` / `backend_seat_taken` / `backend_seat_ceiling` / `downgrade_refused` / `downgrade_unknown` / `switch_noop` / `browser_restarting` / `no_profile` 409 · `DELETE /api/browser/blocked/:id` → `{removed}` · `GET /api/browser/site-hints` → `{siteHints}`, `POST /api/browser/site-hints` `{site|url, backend|tier, why?}` (by `user`; the site rides `site` because `host` is the MACHINE parameter on every browser route) → `{hint, siteHints}`, `DELETE /api/browser/site-hints/:host`. Agent (Bearer): `GET /api/agent/browser/backend[?profile=]` (the switcher view + `attachments` with chips + `me`; a session on the ephemeral browser: 409 `no_profile`), `POST /api/agent/browser/backend` `{provider, profile?, confirmDowngrade?}` (direct only when this conversation is the sole lease-holder and nobody drives; else `mode:'proposal'` filed to the owner's inbox), `POST /api/agent/browser/blocked` `{url, why?, evidence?, tier?, profile?, remember?}` → `{claim, text, hint, remembered, next}`, `POST /api/agent/browser/site-hint` `{site|url, tier|backend, why?}`; `POST /api/agent/browser/audit` also takes `url` (a navigation stamps the lease's `lastUrl`). The profile digest (`GET /api/browser/profiles` + `browser-profiles-updated`) carries `chips`, `seats`, `majors`, `blocked`, `siteHints`, `switching`; `POST /api/browser/profiles` / `POST /api/agent/browser/new` take `defaultBackend`; `attach`/`resolve` answer 409 `browser_restarting` while a profile's backend is being switched. Setting `browser.cloak.executablePath`.
- **P1 second half (§3.7/§3.8):** `POST /api/agent/browser/resolve` `{handle?, argv?, wrapper?}` → `{ok, kind: none|child|attachment, handle, env, unset?, pinTab, cdpUrl? (wrapper only), profile, lease, others, handles, isDefault}` or a typed refusal `{error, code, handles, default?, was?, now?}` with `profile_changed` (409, once per set change) · `profile_required` (409) · `not_attached` (404) · `ambiguous` (409) · `profile_path_refused` (400); `POST /api/agent/browser/new-child` → `{handle, parent, n, env, unset, handles}`; `POST /api/agent/browser/audit` `{profile, verb, ok}`; `POST /api/agent/browser/use` takes `alias` and answers `{…, alias, attachments, handles, defaultId}` (`bad_alias` 400 / `alias_taken` 409); `POST /api/agent/browser/new` takes `adoptDir` (registers an existing directory in place; `adopt_failed` 409); `GET /api/agent/browser/status` + `GET /api/browser/session/:id` carry `attachments`, `handles`, `children`, `defaultProfile`, `fingerprint`, `told`. UI: `POST /api/browser/attach` takes `alias`; `POST /api/browser/pin` / attach / detach queue the zero-billed session-status notice and persist the pin to session meta; `POST /api/browser/adopt` `{sessionId, label}` → `{profile, adopted, note, pin, appliesFrom, …}`. Task Groups: `PATCH /api/tasks/:id` accepts `browserProfileId` (`bp-<8 hex>` | null). The `active-sessions` payload carries `browserKey`, `browserVariant`, `browserProfileId`, `browserPinOrigin`. The ws `create` accepts `browserProfileId` + `spawnOriginHint.browserProfile` (`task-group` | `instance` | `chosen`). **r-fix 2026-09-21:** a `child` answer's `env` carries `AGENT_BROWSER_CONFIG=<data/browser-env/<key>.<n>.json>` on rung D (the child's OWN profile-less config) or `unset` names the parent's config when none could be written and it names a profile — never the parent's pinned directory; the same for `POST /api/agent/browser/new-child`.
- **P5 (§4.5 / D35) the ACTION TRACE (2026-09-18; client half 2026-09-21):** `GET /api/browser/actions?sessionId=&conversation=&browserKey=&profile=&from=&to=&limit=` → `{sessionId, conversation, browserKey, keyFrom: session|conversation|given|null, traceOn, entries:[{id, at, sessionId, browserKey, profileId, scope, action, kind, text, params (REDACTED — a fill's value is «N chars»), ok, error, durationMs, position:{kind: point|box|target|keys|scroll|navigation|input, …}, before|after:{file, bytes, w, h, scale, scrollX, scrollY, seq, at}|null, afterSame, url}]}` — matched by the LIVE session's key, else the bindings store's key for the CLI's `conversation` id (a stopped conversation reviewed after the fact), else a validated `browserKey`; at least one of the three (400); nothing to match by ⇒ `entries: []`, never every entry; `profile` = a profile id, `ephemeral`, or empty for every scope; oldest first, `limit` ≤ 1000. `GET /api/browser/actions/:id` → `{entry}` (never bytes). `GET /api/browser/actions/:id/frame/before|after` → image/jpeg + nosniff + `Cache-Control: private` (a frame is a secret of a logged-in page, §6.4).
- **P5 (§6.4 / §8 step 3 / D7 / D8) HOUSEKEEPING:** `GET /api/browser/housekeeping` → `{profiles:[{id, label, dir, provider, host, legacy, record, bytes|null, lastUsedAt, ageMs, held, live, state: not-ours|in-use|live|recent|stale|kept, why, canForget, trace:{n, bytes, last}, recordings:[{file, bytes, mtime, live}], recordingBytes, recording|null, recordingRefused|null}], ephemeral:{scope, trace}, orphans:[{name, dir, mtime, bytes|null, ageMs}], orphansBase, orphansWhy|null, forgotten:[ledger rows], sweep|null, limits:{retentionMs, bytesPerProfile, staleDays, graceMs, recordingFloor}, traceOn, taps, version}` — NOTHING here is a deletion. `POST /api/browser/housekeeping/sweep` → `{ok, removed, bytesRemoved, recordingsRemoved, recordingBytesRemoved, plan, recordings, at}`. `PATCH /api/browser/profiles/:id` `{record?, label?, notes?}` → `{profile, changed:{field:{was, now}}}` (anything else 400 by name). `POST /api/browser/profiles/:id/forget` → `{ok, removed, from, to, ledger}` (the dir RENAMED beside itself + a ledger row BEFORE the record goes; 409 `leased`/`running`, 400 `not_ours`). `POST /api/browser/orphans/adopt {dir, label}` → `{profile}` (400 `label_required`, 409 `label_taken`/`adopt_failed`). `POST /api/browser/orphans/forget {dir}` → `{ok, from, to, ledger}`. `POST /api/browser/forgotten/:fid/delete` → `{ok, row}` — THE one permanent deletion, a human's click. `GET /api/browser/recordings/:profileId` → `{profileId, recordings, live}`; `GET …/:profileId/:file` → video/webm|mp4, no-store. Every route refuses a non-local `host` by name.
- **P5 broadcasts:** `browser-trace-appended {sessionId, profileId, browserKey, entry:{id, at, action, kind, text, ok, scope}}` (no bytes) on every entry; `browser-housekeeping-updated {sweep?|forgotten?|deleted?|adopted?}` on every housekeeping change; the live-view stream carries a `trace {entry}` record to that session's viewers.
- **P6 (§6.2 / §6.5 / D6) HARD MEDIATION (2026-09-21):** `POST /api/browser/profiles` and `POST /api/agent/browser/new` accept `sharing: owner|instance` — `instance` answers 400 `sharing_refused` with `why: mediation_unavailable` (no proxy in this process) / `host` (a paired-machine profile) / `unknown`; `PATCH /api/browser/profiles/:id {sharing}` flips it (409 `leased` while any session holds a lease; 400 `bad-request` on the legacy record). The digest (`GET /api/browser/profiles`, `browser-profiles-updated`) carries `mediation: {available, port|null, grants:[{profileId, browserKey, targets, sessions, contexts, connections, createdAt, lastUsedAt, upstreamKnown}]}` (never a token or a raw url) and every profile / lease view carries `mediated`. A mediated attach (`POST /api/browser/attach`, `POST /api/agent/browser/use`, `POST /api/agent/browser/resolve`) answers `mediated:true`, `note` (the one-line sentence) and — wrapper form only — `cdpUrl` = the SCOPED `ws://127.0.0.1:<port>/m/<token>/devtools/browser`; `env` = `AGENT_BROWSER_SESSION` + `AGENT_BROWSER_NAMESPACE=vs-<profileId>-<browserKey>` + `AGENT_BROWSER_CDP=<scoped url>` + `AGENT_BROWSER_IDLE_TIMEOUT_MS` (no `AGENT_BROWSER_PROFILE`). New codes: `mediation_unavailable` 503 (a mediated profile in a process with no proxy), `mediation_no_cdp` 502 (its browser answered no CDP url), `pin_refused` 409 (`POST /api/browser/pin` / `POST /api/agent/browser/pin` on a mediated profile — attach it with `use`), `pinned` 409 (PATCH to `instance` while a conversation pins it). The proxy itself (loopback, no auth — like the stream port): `GET /m/<token>/json/version` · `GET /m/<token>/json[/list]` · `WS /m/<token>/devtools/browser` · `WS /m/<token>/devtools/page/<targetId>` (403 out of scope); unknown token 404 everywhere; 503 while the profile's browser is stopped; a refusal is a CDP error `{id, sessionId?, error:{code:-32000, message:'<target_out_of_scope|session_out_of_scope|method_refused|browser_paused|context_out_of_scope|bad_message>: …'}}`; close codes 1008 `lease_gone` / 1012 `browser_restarting`|`browser_stopped` / 1001 `server_shutdown`. **r-fix 2026-09-21:** `POST /api/agent/browser/new {adoptDir}` answers 403 `adopt_outside_roots` (only `~/.agent-browser/**` or `data/browser-profiles/**`), `adopt_legacy_refused` (the `default-profile` jar) or `adopt_registered` (somebody else's record — `use` it) before anything is adopted.
- **P10 (§7.6 tier 3 / §6.6, D27 (b)) THE USER'S REAL DESKTOP AS A WINDOW CLASS (2026-09-21):** while `window.realDesktopTargets` reads true, `GET /api/agent/window/targets` also lists `{handle:'dw-<pid>', label, pid, pids, origin:'desktop', yourDesktop:true, state:'ready', display:'your desktop', a11y, lease, mine}` rows and carries `desktop:{enabled, count, setting, note}` + `verbsDesktop:{snapshot|screenshot|click|type|key|'click-at'|watch: {via, ok, why, code?, note?}}` (`scope` = `'vibespace-launched + your desktop'`); `attach` answers `origin` / `yourDesktop` / `note`; on that class `act` with `verb:'key'` or `at` ⇒ 409 `desktop_injection_refused` `{verb, class:'desktop'}`, `watch` ⇒ 409 `no_live_view` `{origin, yourDesktop, lease}`, `screenshot` ⇒ image/png via x11grab of the window's own pixmap (`X-Window-Shot` + `via`) or 503 `capture_needs_portal` / `capture_unavailable`; any verb with the switch off ⇒ 403 `desktop_consent_off` `{setting}`; a listed pid that died ⇒ 404 `desktop_window_gone`. **Cookie-authed (the user):** `GET /api/window/desktop` → `{enabled, setting, leases:[<lease view> + label + yourDesktop], idleMs, note}` · `POST /api/window/desktop/:handle/pause` `{viewerId?}` → `{ok, already, lease}` (409 `no_lease` / `held`) · `POST …/resume` `{viewerId?}` → `{ok, cause:'explicit', heldMs, byHolder, lease}` (409 `not_taken`); a non-`dw-` handle is 400. Browser side: `GET /api/browser/providers` (and the agent's `…/agent/browser/providers`) rows carry `cdp` / `allowedDomains` / `pinTab` / `consent` and read the switch through the keeper's `desktopConsent()` — `local-window`'s control is `provider_needs_consent` until it is on, `ok` after; `POST /api/browser/profiles` with `provider:'local-window'` ⇒ 403 `provider_needs_consent` before the switch, 409 `tier3_is_a_window_target` after (tier 3 is never a profile); a switch to it ⇒ `switch_refused` naming the window target. **r-fix 2026-09-21:** `POST /api/agent/window/open` takes `{appId, label?}` ONLY — `exec`/`args`/`cwd` ⇒ 403 `exec_is_human` `{apps:[{id,label,available,reason}]}` (an exec is the user's, design-desktop-apps §5); `GET …/targets` carries `apps` (the registry ids). Every lease view names a takeover as `takenBy: {tag, at}` (opaque `tk-…`; the viewer id is never on the wire) and a `window_paused` refusal's `viewer` is that tag.
- **P9b (§4.3 / §6.6) THE USER'S SIDE OF A WINDOW LEASE (2026-09-21), cookie-authed, in the desktop family:** `GET /api/desktop/apps/:id/lease` → `{id, origin:'vibespace', lease:{handle, sessionId, sessionName, browserKey, since, origin, orphaned, input:'agent'|'user', takenAt, takenBy:{viewerId, at}|null, lastUserInputAt, handedBackAt, handbackCause, idleMs, snapshotAt, refs}|null, idleMs}` · `POST …/:id/takeover` `{viewerId}` → `{ok, already, lease}` (409 `held` another viewer drives · 409 `no_lease` no agent holds it · 400 without a viewerId) · `POST …/:id/handback` `{viewerId?}` → `{ok, cause:'explicit', heldMs, byHolder, lease}` (409 `not_taken`); 503 `no-engine` when the engine is absent. **Broadcast `window-leases-updated` `{leases:[<lease view>]}`** to every client on every lease or input change (the window-live pane reads it). **x5 (2026-09-22) — ONE ACTIVE VIEWER:** `GET /api/desktop/apps/:id/viewers` → `{type:'desktop-app-viewers', id, active:<pane>|null, viewers:[{id:<pane>, label, since}]}` · `POST …/:id/viewers/takeover` `{viewerId}` (RESUME HERE — the pane's live socket id) → `{ok, already, viewers, lease}` (409 `no_viewer` when that socket is not open · 400 without a viewerId · 404 unknown app; with an agent's lease it is the engine's human takeover — a hold by another human MOVES without a handback, nothing announced or billed); `POST …/:id/takeover` also answers `viewers` (the human who took over an agent's window is the active viewer). **Broadcast `desktop-app-viewers`** (the same payload) to every client on every join / leave / Resume here / re-election / session end — viewers named by their PUBLIC pane key, never a socket's id. **2.369.156:** during the keeper's GRACE (the active pane's socket closed while others watch — held 5 s by default) `active` still names that pane though no row of it is listed (every other client stays blocked); the stream upgrade takes `?prev=<pane>` beside `?viewer=`/`?pane=` (the pane this tab's previous window of the app had — the grace's reload successor, a hint only); a socket WITHOUT `?viewer=` on an app stream is a read-only WATCH seat (never elected, never listed) — the hosted `/api/desktop/:id/xpra-ui/` page is such a socket. The agent side's `/api/agent/window/attach` answers `orphanedFrom` when it took a lease whose session was gone, every agent verb answers 409 `window_paused` `{takenAt, lastUserInputAt, viewer}` while the user drives, and `…/watch` answers `{form:'window-live', origin, lease, note}`.
- **P9 first half (§4.9 / §5.1.1 / §6.6, D27 (a)) WINDOW TARGETS (2026-09-21), agent-only (Bearer `vsst_`), every route takes `host` (non-local ⇒ 400 `unsupported-host`):** `GET /api/agent/window/targets` → `{targets:[{handle, label, appId, exec, state, display, backend, origin:'vibespace', startedAt, pids, a11y:{pid,name,children}|null, lease:{sessionId, since, input, snapshotAt, refs}|null, mine}], a11y:{ok, apps, why}, verbs:{snapshot|screenshot|click|type|key|'click-at': {via, ok, why, backend?, note?}, probe}, scope:'vibespace-launched', note}` · `POST /api/agent/window/open` `{appId | exec, args?, cwd?, label?}` → `{handle, app, lease, attached:true, next}` · `POST …/attach` `{handle}` → `{handle, app, lease, resumed, next}` (409 `window_leased` `{holder, since}`) · `POST …/detach` `{handle}` → `{handle, detached, appRunning, note}` · `POST …/snapshot` `{handle, budget?, text?}` → `{handle, label, apps, nodes:[{ref, role, name, depth, parent?, bounds?, actions?, editable?, text?, states?}], census:{nodes, component, action, editableText, text, buttons, buttonsWithAction, byRole, actionNames}, unreadable:[{ref?, path?, why}], truncated, budget, callTimeoutMs, ms, nodesPerSec, empty}` · `POST …/act` `{handle, verb:'click', ref}` → `{ok, handle, did:{verb, by:'node', ref, index, action, role, name}, ms}` | `{verb:'click', at:'x,y', button?}` → `did:{by:'point', x, y, button, backend}` | `{verb:'type', text, ref?, replace?}` → `did:{by:'node', ref, chars, replaced}` | `{verb:'key', chord}` → `did:{verb:'key', chord, backend}` · `GET …/screenshot?handle=` → image/png + `X-Window-Shot: {handle, w, h, x, y, cropped}` · `POST …/watch` `{handle}` → `{handle, label, openSpec:{action:'openDesktopApp', id}, streamPath, lease, note}`. Codes: 404 `not-found` / `not_attached` / `ref_unknown` / `app_gone`; 400 `bad-request` / `bad_chord` / `action_unknown`; 409 `window_leased` / `window_paused` / `node_has_no_action` (+ `node`) / `node_not_editable` / `no_focused_node` / `ref_stale` / `no_injection_backend` (+ `backends` rows) / `action_refused` + the keeper's launch codes; 503 `a11y_unavailable` / `helper_missing` / `python3_missing` / `screenshot_unavailable`; 504 `helper_timeout`; 502 `helper_error` / `action_failed` / `ref_unreadable` / `inject_failed` / `screenshot_failed`. Manual topic: `GET /api/agent/docs/window`.

