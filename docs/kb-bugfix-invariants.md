# KB: Bug-fix invariants (incident essays)

Moved VERBATIM out of CLAUDE.md (tier-2 pass).

## EVERY SESSION CREATE AND EVERY KILL BLOCKED THE EVENT LOOP FOR 11-17 SECONDS (2026-09-09, userW's fleet pod)

**THE REPORT.** userW right-clicked the response-style chip, chose "Restart now to apply (Terminate + Resume)", and the chat window went blank for ~30 s. The server log (2.369.67, the same code as master) reads: `[session] killed` at T+0 · `[ws] heartbeat round tainted (tick 14s late, longest loop gap 13s)` at T+14 s · `[session] created` at T+30 s · another `longest loop gap 14s` immediately after. It is not that restart: seven days of that pod's log show **EVERY session create and EVERY kill — chat or plain shell terminal — followed by an 11-17 s event-loop block, 27 of 27**. The pod: 61 claude lock files under `~/.claude/sessions`, 61 live claude CLIs, **tmux NOT installed**, 3,074 processes (2,780 zombies), server RSS 1.6 GB with 107 threads.

**THE MECHANISM — THE COST WAS NEVER THE WAIT, IT IS THE FORK.** A 240 s CPU profile of the integrator's own production server (1.5 GB RSS, 57 threads) shows **17.3 s of SYNCHRONOUS `spawn` self-time in the parent**, 15.6 s of it from session discovery: `findTmuxTargetAsync` 10.7 s, `discoverClaudeSessions`' pgrep block 3.7 s, `getTmuxPaneMapAsync` 1.2 s. A micro-benchmark of `child_process.spawn('true')` in a node process gives **1.8 ms per spawn at 45 MB RSS, 18.8 ms at 543 MB, 72.5 ms at 1.5 GB**: fork(2)/posix_spawn copies the parent's page tables **on the calling thread**, so an "async" `execFile` still costs the parent that much wall time before any child runs, and `Promise.all` over N spawns serialises N forks back to back in ONE tick. On userW's pod: 61 locks ⇒ `tmux list-panes -a` (which FAILS, tmux is missing) ⇒ an EMPTY pane map ⇒ `findTmuxTargetAsync` spawns `ps -p <pid> -o ppid=` for EVERY lock ⇒ ~61 forks × ~130 ms ≈ the observed block. The same sweep runs on every `/api/sessions` poll (4.5 s cache, clients poll at 5 s) and again after every create and kill, plus `pgrep -P <childPid>` per live session on every 15 s cache miss. **The 2.235-era comment directly above `execFileP` already diagnosed "each sync fork is 100-300ms under load" and fixed only the WAIT (sync→async), not the FORK** — 2.242.0 moved the blocking, it did not remove it.

**THE FIX — NO SPAWN PER ITEM, and the rule is not "few".**
1. **The parent of a lock's pid** comes from `/proc/<pid>/stat` field 4 through THE process reader (`cli-identity.readPpid`), parsed after the LAST `)` because comm may contain spaces and parens — the same rule `procStartTicks` uses for field 22. `findTmuxTargetAsync` also short-circuits on an EMPTY pane map, which is the shape on every machine without tmux.
2. **The children of a live session's `_childPid`** come from the union of `/proc/<pid>/task/<tid>/children` (`cli-identity.readChildPids` — children are recorded against the THREAD that forked them, so asking only the main thread is half an answer). The per-session `pgrep -P` is gone **and so is its 15 s `_childPidCache`**, which existed only to blunt the fork and was also a correctness cost: a wrapper that forked a new claude inside the window stayed unmapped.
3. **TMUX IS OPTIONAL.** `getTmuxPaneMapAsync` asks `tmuxOnPath()` — `fs.statSync` over the PATH entries, memoised 60 s, never a `which` (that would be one more fork per sweep on exactly the machines this fix is for) — and caches the map for `TMUX_MAP_TTL_MS` = 4 s so a create/kill burst shares one. One `tmux list-panes` per sweep where tmux exists; **zero children where it does not**.
4. **The no-/proc rung is ONE `ps -eo pid=,ppid=` FOR THE WHOLE SWEEP** (`parentIndex`, memoised `PROC_TABLE_TTL_MS` = 2 s, deliberately under the discovery cache's own 4.5 s), never one exec per pid — macOS is "full support" in the README and a procfs-only reader would answer `null` for every pid there.
5. `isLockClaude` keeps the `procStart` file read first and, where there is no usable stamp, now asks THE identity (`isCliProcess`) on any procfs machine — which is the DAEMON snapshot's own rule (`pidLooksClaude`), i.e. one implementation — leaving the `ps -o comm=` twin reachable only where there is no /proc at all.
6. The SYNC tmux twins (`getTmuxPaneMap`/`findTmuxTarget`) are DELETED. They had **no callers anywhere** — the kb line "sync variants remain for boot paths" described boot-restore's OWN `execFileSync` calls — and they spelled the retired per-pid fork. Dead code that spells a forbidden shape is how the shape comes back.

**THE DAEMON ALREADY DID IT RIGHT, AND THE LOCAL PATH DID NOT MATCH IT.** `agentd`'s `computeDiscoverySnapshot` (the `agentd.localDiscovery` sweep) scans locks with `pidLooksClaude` — pure /proc — and spawns nothing. But the orchestrator-side `discoverClaudeSessions` ran `getTmuxPaneMapAsync` + `findTmuxTargetAsync` per lock **on the devSnap branch too**, so turning the flag on bought a device round trip and then paid the forks anyway. Both paths now cost the same nothing.

**MEASURED (same fixture, same box, one variable).** 50 locks + 50 live sessions: **101 spawns / 360.8 ms → 1 spawn / 6.6 ms**. At 1.5 GB RSS (Buffer.alloc, touched): **7,140.6 ms → 74.2 ms** cold (the 74 ms IS the one tmux fork) and **3,540.9 ms → 1.0 ms** warm. At userW's shape — 61 locks, no tmux, 1.5 GB — **8,441.8 ms / 123 spawns → 3.0 ms / 0 spawns**, and 4,115.6 ms / 62 spawns → 1.1 ms / 0 on the warm sweep. `spawnSync('/bin/true')` in that same 1.5 GB process: 67.2 ms.

**HONEST BOUNDARY.** The pod itself was not touched (read-only forensics) and no fix was verified against it; what is reproduced here is the MECHANISM at the pod's shape on a box of the same size. The 2,780 zombies on that pod are unexplained by this work and unaddressed by it — they make every `ps`-style read slower but they are not what forked. Nothing changes for ssh/device discovery: `hosts.js` runs remotely, `data/bin/vibespace-usage-scan` is untouched, and boot-restore's sync variants stay (a boot path has no loop to block yet).

**INVARIANTS.**
- **"Async" fixes the WAIT, never the FORK.** A spawn is paid by the PARENT, in proportion to the parent's own memory; measure it at the size the server actually runs at, not at a test harness's 45 MB.
- **A per-item child process on a polled path is a defect, not a cost** — the bound must be per SWEEP. `Promise.all` does not make forks concurrent; it makes them consecutive inside one tick.
- **A cache in front of a fork is a symptom.** Removing the fork removed the 15 s staleness with it; a cache that exists to blunt a cost nobody should be paying hides both.
- **Optional dependencies must be optional at ZERO cost**: probe by statting PATH, never by spawning, and remember the answer.
- **Where two implementations of one fact exist, the cheaper one is the rule** — the daemon's lock scan was already fork-free and the local sweep was not; "hostId is a parameter" is about the DECISION, not just the data.
- **Delete dead code that spells a retired shape**, and prune its allowlist rows with it (test-writer-sweep §17 fails on a dead entry, which is how the two `ps -o ppid=` rows were found).
- **A zero that comes from doing no work proves nothing**: the census asserts the sweep discovered all 55 locks and verified every one RUNNING *before* it asserts the spawn count, and the negative control is `master`'s own module over the SAME fixture (111 spawns) with the same PATH — an emptied PATH would have broken master's identity rung for a reason that has nothing to do with forks.
- **An instrument that patches `child_process` after the module under test destructured it counts zero for ever** — this suite fell into that trap while being written (its wiring pin read `execs=0` with the fix reverted), so the installer is hoisted above every require and two positive controls prove the census can see both a direct spawn and session-store's OWN destructured `execFile`.
- **A fixture binary must be one that survives being renamed**: `/bin/sleep` can be a uutils multi-call binary that dispatches on argv[0] and exits 1, which silently gave the first draft of the no-`procStart` leg five dead pids (a copy of `/bin/sh` blocking on stdin is the shape test-local-discovery-device already uses, for the same reason).

Gates: **test-discovery-spawn** (40 asserts: the /proc reads, the no-/proc rungs DRIVEN over a re-rooted procfs, the census over 55 locks + 50 live sessions, master's own module as the negative control, and four product mutations each verified red) + **test-architecture §45** (the grep-derived spawn census over session-store / discovery-facts / cli-identity, PRINTED, every survivor allowlisted with a reason, dead rows red, runs inside `npm run build`) + test-writer-sweep / test-local-discovery-device / test-discovery-interpret / test-restore-smoke / test-session-schema / test-fixture-isolation unchanged and green.

**r2 — THE SAME QUESTION HAD A SECOND READER, AND IT WAS THE ONE ON THE KILL PATH (2026-09-09, the round-1 verifier; reproduced first).** Round 1 deleted the per-session `pgrep -P` from the discovery sweep and left its TWIN: `refreshWebuiPids()` in server.js loops `for (const [id, s] of activeSessions)` and runs one **synchronous** `execFileSync('pgrep', ['-P', String(meta.childPid)])` per live session — the SAME "what did this wrapper fork" question, answered the retired way, and it is on the incident's own trigger: `src/ws-handler.js` calls it **IN-BAND on every kill** (between `activeSessions.delete(...)` and `broadcastActiveSessions()`) and `src/ws-create.js` calls it 3 s after every create. Synchronous is strictly worse than the sweep's `execFileP`: the caller waits out the fork AND `pgrep`'s own runtime (~80 ms while it walks a 3,000-process `/proc`), one after another. **MEASURED** on this box, the real loop body vs this branch's `readChildPids`, same fixture (N wrapper processes each with two real children, real meta files), one variable: N=10 → **761.5 ms / 10 spawns vs 0.8 ms / 0**; N=22 → **1,671 ms vs 1.1 ms**; and at the pod's shape — **61 sessions at a real 1,560 MB RSS — 9,180.6 ms / 61 spawns vs 2.3 ms / 0**, with the two arms producing the **IDENTICAL 244 pids**. So after round 1 a create or a kill on that pod still blocked the loop for ~9 s while the sweep it had just fixed cost 3 ms. The comment two lines above the loop had claimed since the cache was written that it needs "no pgrep/process-tree traversal" — **a comment stating an invariant three lines from its violation is a bug report**, and this is that class again.

**AND THE ENFORCEMENT WAS BLIND TO IT BY CONSTRUCTION.** §45's file set was a literal array of three modules and server.js was not in it. Mutation-proved on the real tree: inserting a fresh per-session `execFileSync('ps', ['-p', …, '-o', 'ppid='])` INSIDE `refreshWebuiPids` left the census **`ALL PASS`**, while the same shape inserted into session-store's `findTmuxTargetAsync` went red — so round 1's claims (1) "the per-session `pgrep -P` … DELETED" and (5) "a new per-item spawn reddens the build" were both false for this reader. **A hand-written file list is the tool this class has already defeated** (test-writer-sweep §17 r7 is the first strike; this is the second), so both halves of §45 now DERIVE their scope and PRINT what they walked:
- **(a) THE PID-QUESTION CENSUS, repo-wide.** Every tracked JS file the server or the daemon runs (`git ls-files` through the sanitized git env — never a readdir, because `data/bin` holds a 64 MB untracked `rclone`; the only exclusions are `scripts/`, whose suites deliberately SPELL the forbidden shapes as controls — and an assert proves no in-scope file requires anything from there — and `public/`, the built bundle, whose tracked copy would carry cli-identity's own allowlisted calls under a second filename), every spawn whose argv names a process-table tool (`ps`/`pgrep`/`pidof`/`pstree`/`lsof`/`fuser`, the spawn line plus the two after it so a wrapped call is not missed). **16 sites**, each with a row saying why it is not per-item; that is the half that would have caught server.js. The boot-restore `fuser`/`pgrep -f` rows are named BOOT-ONLY with the reason (`restoreSessions` runs once, before this server serves anybody — deliberately out of scope, since the incident is create/kill/poll).
- **(b) THE SWEEP-MODULE CENSUS.** Every spawn, in the modules that answer the sweep's own named facts — and the file set is resolved by **looking up where each fact is DEFINED** (`discoverClaudeSessions`, `refreshWebuiPids`, `readChildPids`, `readPpid`, `findTmuxTargetAsync`, `getTmuxPaneMapAsync`, `listOpenCodexRolloutPaths`), so moving a function keeps its enforcement and a fact that grew a twin or lost its home goes red in four places at once (verified by renaming `refreshWebuiPids`). This is the half that catches a per-session `git` or `stat` — a fork (a) cannot see by construction.

**WHAT THE SHAPE CENSUS CANNOT SEE, SAID OUT LOUD — r2's answer, whose first half r3 had to correct (see below).** r2 CLAIMED (a) could only miss a spawn that is not a process-table question, and that was wrong: it matched CALLEE NAMES, so a process-table tool spawned through a promisified alias was invisible to it. Since r3 the rule is the TOOL in argv[0], and the blind spot is a tool name held in a VARIABLE. (b) is blind to a module that neither defines a sweep fact nor is one — a per-session `execFile('git', ...)` added to `src/ws-handler.js`'s kill case would pass both. That gap is covered by the OTHER kind of census, which is file-blind on purpose: **test-discovery-spawn counts every `child_process` entry point the real code takes** while a real `discoverClaudeSessions` runs over 55 locks (§3) and while the real `refreshWebuiPids` runs over 20 live sessions (§8), so a fork on either hot path is counted wherever it was written. The shape census is the fast, always-on half; the consequence census is the one that cannot be reasoned around. Neither alone is the enforcement.

**r2 INVARIANTS.**
- **A fix is scoped to the QUESTION, not to the file you found it in.** "Who are this wrapper's children" had two readers; fixing the one in the module named after discovery left the one on the kill path — which is the trigger the incident report actually names.
- **A synchronous spawn costs the fork AND the child's runtime**, so the cheapest-looking call site can be the worst one: `pgrep` walking a 3,000-process `/proc` is ~80 ms of blocked loop that no amount of "it's just one call" reasoning covers.
- **A census's file set must be DERIVED and PRINTED, never typed** — and when the derivation cannot cover a whole class (a `git` spawn is not a process-table question), that is a second census, not a wider regex.
- **A grep-derived scope needs a self-proof**: (a) asserts it really covers the incident's own four modules and that excluding `scripts/` is honest (nothing in scope requires it), because a census that silently narrows is a census of nothing.
- **A negative control on a shared tree is a DELTA.** Both splice controls measure strays-after minus strays-before, so they describe what THEY added and stay diagnostic on a tree somebody else has already broken.
- **server.js is a bootstrap that cannot be required, which is not a reason to leave it untested** — the gate slices the SHIPPED function text out of the file and runs it with its free names injected, with `git show master:server.js`'s copy of the same function as the negative control over the same fixture.
- **A "0 spawns" assert needs the fixture to have something to find**: the leg asserts the shells really forked 2N grandchildren and that both arms name the same 4N pids, so the fix is proven to lose nothing — and it asserts `session._childPid` is still stamped, which is the other thing that function is for.

Gates after r2: **test-discovery-spawn 54** (§8 = the shipped `refreshWebuiPids` over 20 live sessions with two real children each, master's own copy as the control, plus `VS_WEBUI_N`/`VS_WEBUI_RSS_MB` to re-take the pod-shape number) + **test-architecture 135** (§45 rewritten as the two derived censuses with four negative controls; four product mutations verified red: the `ps` reader back in `refreshWebuiPids` reddens BOTH halves, a non-process-table per-session spawn reddens only (b), the reverted product fix reddens §8, and a renamed sweep fact reddens the derivation).

**r3 (the round-2 verifier, reproduced first): the census matched CALLEE NAMES, so the retired shape walked back in through a promisified alias — on the kill path.** r2 derived and printed the census's FILE set and left its CALL-NAME set typed: `SPAWN_CALL` is the alternation `execFileSync|execFileP|execFile|execSync|spawnSync|spawn|execImpl`, each requiring a `(` immediately after, so **`execFileAsync(` and `sh(` match none of them**. Two live process-table spawns were therefore absent from the census's own printed 16-site inventory and carried no reason row — `src/ws-handler.js:1353`'s `execFileAsync('pgrep', ['-f', session.socketPath])` (the file's OWN idiom: defined at :192 and handed to ws-create through ctx at :242) and `src/incident.js:98`'s `sh('ps', ['-eo', 'pid,ppid,lstart,etime,rss,stat,args'])`. **MUTATION-PROVED on the real tree**: a per-session `pgrep -P` loop spelled that way, spliced into ws-handler's kill case between `refreshWebuiPids()` and `broadcastActiveSessions()` — the incident's own trigger — left `test-architecture` **ALL PASS (135)**, `test-discovery-spawn` **ALL PASS (54)** and `test-ws-contract` **ALL PASS (5)**. The file-blind consequence census could not see it either (the fork is inside neither `discoverClaudeSessions` nor `refreshWebuiPids`), and the same alias spelled INSIDE `refreshWebuiPids` was invisible to both halves too — it went red only through the ad-hoc string assert "no `pgrep` survives in server.js code", which exists for exactly one file. **This falsified r2's own stated boundary**, which claimed (a) misses only spawns that are *not* process-table questions.

**THE FIX, BOTH HALVES.**
- **THE DETECTOR DERIVES FROM THE TOOL, NOT FROM THE CALLEE.** The signal this census claims to census is the ARGV, so `TOOL_FIRST` (`<ident>('ps'|'pgrep'|'pkill'|'pidof'|'pstree'|'lsof'|'fuser',`) unions with `SPAWN_CALL` at the site rule — a promisified alias is still a fork. The trailing comma keeps `_trimGapDom('top')`-shaped calls out and `top` is deliberately not in that list; the union still covers `execFile('top', …)`. MEASURED over the same 243 tracked files: **16 sites → 18, zero false positives**, exactly the two live ones. A fifth negative control drives the retired shape twice — as a bare line (the rule) and spliced into ws-handler's REAL kill case (the consequence, `+1` stray).
- **AND THE ROW IS REMOVED RATHER THAN REASONED ABOUT.** The kill path's `pgrep -f <socketPath>` — "which dtach process owns this socket" — now asks THE process reader: `cli-identity.pidsMatchingCmdline`, /proc-first like `readPpid`/`readChildPids`, reading every `/proc/<pid>/cmdline` into ONE reused 64 KiB buffer (a read that FILLS it re-reads that pid whole, so a giant argv is answered rather than truncated), with the no-/proc rung ONE `pgrep -f` for the WHOLE question. Matching is a literal byte substring where `pgrep -f`'s is an ERE, i.e. **strictly narrower** — the safe direction for a caller that SIGTERMs what it finds, and it cannot miss the real holder, whose argv carries the path verbatim; a needle containing a space is matched against the joined form because `/proc` separates argv words with NUL. `src/incident.js` keeps its row with a reason (ONE per HUMAN-TRIGGERED capture, whole-table form, on no poll).

**MEASURED, same box, 3,400 processes, median of 7, one variable** — the scan is FLAT in the server's own size while the fork it replaces is not:

| RSS | this scan (block = wall) | `pgrep -f` block / wall |
|---|---|---|
| 58 MB | 23.7 ms | 2.0 ms / 87.6 ms |
| 559 MB | 23.7 ms | 24.4 ms / 110.6 ms |
| 1,564 MB | 23.6 ms | 69.4 ms / 155.2 ms |

At the shape the incident was reported on (1.6 GB) that is 69 ms of blocked loop down to 24 and 155 ms of latency down to 24. **HONEST COST, stated rather than hidden**: on a freshly booted 58 MB server it is 24 ms of block where a fork was 2 — bought back four times over in end-to-end latency, and a predictable cost that stops growing with the server is the whole point of the class this fix belongs to.

**r3 INVARIANTS.**
- **A census must derive the thing it claims to census.** r2 derived the FILE set and left the CALL-NAME set typed, so the same class walked back in one alias away: the signal here is the tool in argv[0], not what the caller named its wrapper.
- **State the boundary as narrowly as the code allows, and re-state it when it moves.** With the fix, (a) sees a process-table tool spelled as a LITERAL whatever the callee is called, and is blind to a tool name held in a VARIABLE; (b) is still blind to a module that neither defines a sweep fact nor is one. Both gaps are covered from the other side by the file-blind consequence census, which now counts the kill-path socket lookup too.
- **Prefer removing a row to reasoning about one.** An allowlist entry is a promise every future reader must re-check; a rung that starts no child process is a property.
- **Publish the trade, including the shape where the fix is worse.** A flat cost that beats a growing one at the size the product actually runs is a decision, and a decision has to name what it gave up.
- **An assert that reads `undefined === undefined` is not an agreement** — §9's "both arms found the same pids" now requires the arms to have found the pids at all (it passed hardest when neither ran).
- **Slice by the anchor that is unique to the region you mean.** `if (session.socketPath) {` also opens the broken-stdin detector 780 lines earlier; slicing THAT one builds a body full of names the leg never injects, i.e. a red that says nothing about the fork under test.
- **A census reads CODE**: the comment above the fix names `pgrep -f` on purpose (it says what this replaced and what it cost), so the leg blanks whole-line comments before asking whether a `pgrep` survives — otherwise the sentence explaining the fix fails the fix.

Gates after r3: **test-discovery-spawn 65** (§9 = the SHIPPED kill-path body sliced out of ws-handler and run with a fake `process` that records SIGTERMs instead of sending them, over three real processes carrying the needle, with `git show master:src/ws-handler.js`'s copy as the negative control on the same fixture — 0 spawns / 23.6 ms against 1 spawn / 84 ms, the IDENTICAL pid set, the session's own attach pty excluded, plus the no-/proc rung driven over a re-rooted procfs) + **test-architecture 139** (TOOL_FIRST, the two new rows, negative control ⑤ in both forms, and the tightened boundary). Product mutations verified red: master's kill-path body back in place reddens §9 twice (ALL PASS 65 → 2 FAILED); the verifier's own alias mutation in the kill case reddens §45 twice by name and line; removing `TOOL_FIRST` from the site rule reddens exactly three asserts (the dead `src/incident.js` row plus both halves of control ⑤).

## A PARSE THAT COULD NOT SEE A CAP RETIRED IT (2026-09-09, quota-model r6 — found by an adversarial verifier against this branch's own r5)

**THE SHAPE.** r5 gave a producer the right to RETIRE a model-scoped limit the cache file holds and the current read does not name (`authoritativeScopes`), because `mergeLimitSets` can only ADD and a cap the vendor stops reporting was otherwise resurrected for ever with its last, possibly exhausted, number. The gate was `u.scopedWeekly?.length ? ['model'] : null` — **a fact about the ARRAY**: it says a model cap was seen, never that ALL of them were.

Seven call sites are fed by THREE parsers. Driven over ONE account state — plan 20 %/20 %, a Fable cap at 10 %, and a `seven_day_opus` at **100 % stated with no `resets_at`** — they answer differently:

```
parseCliUsageText      -> plan, model:fable, model:opus     (the ⟳ CLI-panel rung, 1 of 7)
parseOAuthUsage        -> plan, model:fable                 (the bare-token ⟳ + the four host legs, 5 of 7)
parseGetUsageResponse  -> plan, model:fable                 (the engine's control-channel probe, 1 of 7)
```

So the panel establishes the spent cap and any of the other six then **retires it**, end to end through the real modules and the real write path: `accountRemaining` `{remaining:0}` → `{remaining:80, known:true}`. That is inc-msof8i22 — "the pool stayed on an account whose Opus was spent" — re-opened by the mechanism built to end its mirror image.

**REACHABILITY IS STRUCTURAL, NOT INCIDENTAL.** The control-channel probe is the FIRST rung of `POST /api/usage/refresh`, ahead of `refreshViaCliPanel`, so on any account with a live local claude chat session the "authoritative" list always came from a parser that omits reset-less named caps. No preserve-merge protects it either: the panel replaces `scopedWeekly` wholesale when non-empty, `writeUsageCacheForKey` falls back to `prev` only when the list is EMPTY, and the bare-token leg hands the parse to the write path verbatim. And the only signal on a real cap deletion was a `console.log`.

**THE FIX — COMPLETENESS IS A PROPERTY THE PARSE STATES.** Each producer counts what it DROPPED (a `weekly_scoped` entry it could not NAME; a bucket-shaped field it could not read a number from; a `Current week (…)` line its own regex did not match) and only a parse that dropped nothing marks itself, through the ONE rule every site asks — `quotaModel.authoritativeScopesOf(parse)`. r5's second condition is kept deliberately: an enumerated but EMPTY list still states nothing, because it cannot be told apart from "my scoped parsing broke" and retiring a spent cap is the money-losing direction.

**AND A BUCKET WITHOUT A RESET IS STILL A BUCKET.** Both named-field loops required `v.resets_at` while the array branches beside them in the SAME parsers accept an entry without one — one parser, two answers for one payload shape (measured: 3 of this instance's reset-less scoped anchor readings carry `source: 'control'`, i.e. that array branch really produces them). r3/r4 had already settled what such a bucket means — a STATED SPEND is decisive and counts, it merely may not name a DEADLINE — so the requirement is gone. It is the routine shape: **704 of 5631 scoped readings and 615 of 7978 seven-day readings on this instance carry no reset at all** (692 of the scoped ones from the ⟳ panel). Honest boundary: every reset-less bucket ever recorded here sits at u=0, so the exact spent-and-reset-less combination is LATENT on this instance, not live — but it is the shape 2.305.0 found in a real payload, and two suites' pinned assertions ("a scoped field with no reset is still skipped") were pinning the pre-r3 belief that such a bucket is useless. They are inverted on purpose, each with its essay and a replacement negative control.

**INVARIANTS.**
- **Completeness is stated by the producer, never inferred from a non-empty array.** "I saw one" and "I saw all of them" are different claims, and only the second may delete somebody else's.
- **A claim about a READ must not be expressible in the store that read is written to.** The mark is a NON-ENUMERABLE, SYMBOL-KEYED own property: `JSON.stringify` drops it, a spread drops it, a preserve-merge of a stored file drops it — every one of those losses meaning "no authority", the safe direction — and no JSON a vendor, a device or a file could hand us can FORGE it. (This store's oldest law, from the established-window incident: a fact only ONE producer may state does not belong in the object all the others rewrite.)
- **When the parse result and the write payload are different objects, the gate belongs to the parse** — `refreshViaCliPanel` writes `{...cliPanel, …}` and must ask `cliPanel`.
- **A call site whose failure mode is SILENCE owes a functional test, not another pin.** Asking the spread type-checks, satisfies the wiring pin's shape, and kills the retirement at the one producer that most deserves it; mutation-tested, it reddened nothing else in the suite. §⑱f therefore drives the REAL `refreshViaCliPanel` through the REAL `setupUsage` with a fake `claude` on `CLAUDE_CMD` and reads the consequence.
- **A filter written when a list was ADDITIVE becomes a lie when that list is promoted to an AUTHORITATIVE enumeration.** The `!resets_at` skips predate the retirement by years and were correct then; the promotion is what made them dangerous, and nothing re-read them.
- **A test file's own scratch copies must be git-ignored by a rule that covers the FAMILY, not by one line per section** (r5's `vs-qmr4-mut-*` prefix was never added, so a SIGKILL could strand a file in `src/` that dirties the tree and blocks every push — §⑱0 now asks git itself, and SKIPs loudly with git's own failure when there is no repo to ask).

**GATE.** test-quota-model 293 (§⑱a the cross-producer parity over one vendor state, with the pre-fix skips restored one parser at a time as its negative controls; §⑱b the end-to-end ladder with the verifier's own numbers reproduced by a pre-fix world AND a single-mechanism control showing the parser half is what made the gate lethal; §⑱c completeness is falsifiable per drop shape; §⑱d the mark is per-read and unforgeable; §⑱f the REAL panel refresher, with "ask the spread" as its negative control; §⑱e the retirement line names the claim it drops). Product-source mutation matrix: OAuth parser drop → 8 red, control parser drop → 3 red, authority ignoring the claim → 9 red, unconditional completeness → 1 red, panel site asking the spread → 2 red, log line dropping the numbers → 1 red; restored → 0.


## A WALL LANDED ON THE MEMBER THE TURN STARTED ON (inc-mttbrtc0-6049, 2026-09-08 23:47Z, production journal + anchors)

**THE SHAPE.** The pool re-pointed one session's credential link twice inside a SINGLE turn — `data/slot-transitions.jsonl` carries all three rows, per session, with timestamps (23:44:48, 23:46:00, 23:46:36). The CLI re-read the credentials (2.1.257's `rpe()` re-reads on mtime, which is exactly what a re-point bumps) and its NEXT request, made with the member we had just moved TO, came back rejected with that member's own 5h window ("resets 8:30pm"). `demoteWalledAccount` attributed it to the TURN-PINNED slot — the member the turn had STARTED on — and wrote it with `captureRateLimitEvent(source:'wall')`, which had **no window check at all**. The anchor streams recorded the whole thing (identities anonymised):

```
victim      23:43 on-demand         5h u=0.91  resets 00:19   <- its own
            23:46 rate-limit-event  5h u=1     resets 00:20   <- its own wall
            23:48 WALL              5h u=1     resets 03:30   <- FOREIGN
            23:53 on-demand         5h u=1     resets 00:20   <- owner restored it BY HAND
true owner  23:42 on-demand         5h u=0.84  resets 03:30   <- ITS own window
            23:47 rate-limit-event  5h u=0.84  resets 03:30
```

Two harms, and the second is money: the panel showed the victim "resets 8:30pm" until the owner refreshed manually (he read it as a banked reset being consumed), and the victim was **demoted for three hours on another member's wall** — a usable member excluded from the pool.

**WHY EVERY EXISTING GUARD MISSED IT.** `guardReadingTarget` (reading-lag rule ②) judges by the WEEKLY window only, because 2.369.73 r2 MEASURED that a five-hour reset names a TIME and not an ACCOUNT (47 distinct 5h resets shared across identities — they snap to the clock). That refutation stands and is untouched. And the wall was exempted from the guard outright, for a reason that is half right: a wall's `resetsAt` is often a bounded GUESS (`now + 5h`), and a guess compared against a real window differs every time, so guarding walls would have archived every exhaustion mark on the instance. The turn pin itself was justified by "a re-point cannot reach a running CLI" — which **the readings-by-slot essay itself refutes**.

**WHAT IS ACTUALLY SOUND.** A window that is RUNNING cannot change its reset before it ends. So a stated reset that contradicts the target's own still-future window REFUTES that target — and refutation needs no uniqueness, which is exactly why the r2 measurement does not bite. But refutation identifies NOBODY. Measured over every anchor stream on this instance (comparisons where the previous stated reset was still in the future at the next reading): a RUNNING 5h reset moves >120 s in **4.31 %** of consecutive readings (105/2434) and a running 7d in **1.46 %** (99/6783), while an EMPTY one moves in **32.97 % / 41.13 %** — which is B-8b12, and is why `windowState` fences empty windows off first. Inspected, that running-window residue is dominated by two things that are not "the window moved": the banner's own bounded guess, and the **A-B-A signature of this very defect** (a foreign reading lands and the account's own next reading puts it straight back — the victim's `+11400s` wall followed by `-11400s` on-demand above IS the incident). We do not get to call the residue zero.

**THE FIX — TWO INDEPENDENT WITNESSES, AND ONLY THEIR AGREEMENT MOVES A BYTE.** The LEDGER identifies (`slotAt(webuiId, <when the rejection arrived>)` — our own first-class record of who held that link at that instant, and the evidence the turn pin throws away); the WINDOW only corroborates. Either silent ⇒ the turn pin stands, unchanged. They disagree ⇒ write NOTHING and say why, because refusing can never write a foreign window onto a member. Only a vendor-STATED reset counts (`parseLimitBanner` returns `{kind}` and no time, so the banner path is untouched and the exemption's real half is preserved). The decision is PURE (`quotaModel.wallAttribution`); the engine composes it in `wallTargetFor`. **Signals are attributed BEFORE they are aggregated**: merging by kind and keeping `Math.max(resetsAtMs)` IS the mechanism — the two rejections above (a legitimate 00:20 at 23:46 and a foreign 03:30 at 23:48) collapsed into one bucket that then carried the later member's window and was written onto the earlier one. **The demotion follows the write**, because fixing the panel while leaving the demotion behind would fix the cosmetics and keep the cost. A refused wall is ARCHIVED with its reason into the same archive-never-destroy store the window guard uses, so one grep answers "what did we refuse and why" for readings and rejections alike.

**INVARIANTS.** A REJECTION IS A READING — it is evidence about itself and gets the same by-window attribution as a value reading; being the only producer nobody checked is how it became the only one that could write a foreign window unopposed. A rule that can REFUTE is not thereby a rule that can IDENTIFY — say which one you have, and let the thing that identifies do the identifying. When two witnesses disagree, the safe action is the one that writes nothing, not the one that picks a favourite. A guess is not a statement: only a vendor-STATED value may be read as evidence, and the producer that states none must keep the old path exactly. Attribute per SIGNAL, then aggregate — an aggregation that runs first silently merges two members' facts into one. And a fix that moves money must be asserted on the money: the regression drives the REAL producer twice with a real re-point between them and asserts BOTH halves (the window and the demotion), with a PRE-FIX patched copy of the product module as the negative control, which reproduces the incident. scripts/test-readings-attribution.mjs §18 + §18b (417).

**ROUND 2 — the adversarial verifier found three, all reproduced first, all measured.**

**① THE INCIDENT'S OWN DAMAGE STILL LANDED, because a rejection is written TWICE.** Round 1 put the attribution rule on the turn-end aggregate (`demoteWalledAccount` → `wallTargetFor`). But a rejected `rate_limit_event` is also written the instant it arrives, per RECORD, in `recordRateLimitEvent` — that write is what makes the pool act in the same tick — and that one asked nothing: `guardReadingTarget` is structurally unreachable for a rejection (`win` is deliberately `null`), so the foreign 5h window landed on the pinned member exactly as before, and the turn-end pass, which correctly filed the wall on the true owner, had no reason to go back and repair a bucket it never touched. **The round-1 suite could not see it because its fixture sent BOTH rejections on the SAME bucket**, so the pin's own turn-end write happened to overwrite the foreign value. On two different buckets — a member walling on its weekly while the NEXT member walls on its five-hour, which is what a mid-turn re-point produces — the victim keeps a stranger's reset: measured on the real engine, `victim.fiveHour = {utilization:1, resetsAt:<the other member's>}`, `accountRemaining` `{remaining:0, known:true}`, ~3 h of exclusion. That is the incident, after the round-1 fix. **The fix is `wallAttributionFor`, the rule over KEYS, asked by both writers** (`wallRecordTarget` per record, `wallTargetFor` at the aggregate) over the same two witnesses, so the two writes of one rejection can never disagree about where it belongs — and attributing at the WRITE is what makes a repair unnecessary: nothing foreign is ever stored to be repaired. A rejection we refuse is archived under its own moment (`rejection:<bucket>`, distinct from the turn-end `wall:<bucket>`), because refusing both writes is two facts, not one line written twice; the session-scoped half (pool switch, wall signal) still runs on the archive path, since we refused to write a NUMBER, not to notice a wall. **And the whole rejection moves together.** One wall reaches us through up to three producers and only the `rate_limit_event` carries a window — both banner paths state no time at all (`parseLimitBanner` returns `{kind}`; owner ruling: never parse text for times), so they have no evidence of their own and fall back to the turn pin, i.e. the member the record write has just PROVED innocent. Making the rule per-record without this would have marked TWO members for ONE rejection. A proven re-file is therefore recorded on the session for the rest of the turn (`_turnWallRefile`), the banner follows it and says so, and it dies in `noteTurnEnd` beside the other two turn pins — because a re-point only reaches the CLI on its next request. With no proof this turn the banner marks the pin exactly as it always has: this is a redirect on evidence, not a new rule. **Two things went wrong while wiring that and both are now mutation-tested.** (i) The proof must move the WRITE and not the SIGNAL: `demoteWalledAccount` resolves `member` — and therefore the identity group every earlier signal is filtered against — from the LAST signal's key, so redirecting the banner's `noteWallSignal` silently dropped the pinned member's OWN legitimate wall from the entire turn-end pass (measured: `sessionWalledMembers` held only the re-file target, and the victim's own weekly demotion never happened). The signal keeps the pin's key; the turn-end pass reaches the same destination by asking the same proof. (ii) The proof may only be cleared AFTER the demotion, which is its last reader — clearing it in `noteTurnEnd` beside the other two pins, where it obviously belongs, put the banner's mark straight back on the member the turn had just proved innocent.

**② NOT EVERY WINDOW REFUTES EQUALLY WELL — round 1 asserted the residue was small and cited a number measured on a different quantity.** Round 1's 4.31 % measured the drift between two CONSECUTIVE readings seconds apart; the rule actually compares a reading against its account's own last `on-demand` stamp (what `establishedWindows()` serves from `.window-<key>`), which can be hours old. Re-measured with the predicate the rule uses, over all 7798 claude anchor rows on this instance, empty-window fence applied, each account judged against its own stamp:

```
                  judged  refuted           of the refuted: states ANOTHER
                                            account's own window at that instant
7d      own producer   4109       0            —
        session prods  1160       0            —
scoped  own producer   4029       0            —
        session prods   700       0            —
5h      own producer   1706      13  (0.76 %)  —
        session prods  1055     137 (12.99 %)  101  (73.72 %)
```

A **weekly** window (7d, and every model-scoped bucket, which is weekly by construction) has never once contradicted its own account, across 9998 judged readings, seven identities and a month — the same conclusion 2.369.73 r2 reached from the other direction. A **five-hour** window is EVIDENCE, not proof: three quarters of the readings it refutes state another account's own window at that instant (true positives, mis-filings of exactly the kind this rule catches), and the honest bound on the residue is the OWN-PRODUCER row — 13 readings where an account's own `/usage` panel, which cannot be mis-filed (its key and its credential dir are one decision), contradicted its own previous stamp. Inspected, all 13 are ONE account alternating A-B-A-B between two resets 90 minutes apart at a constant 0.89 utilization: that is not a window moving, it is **B-9213 inside the plan bucket** — more than one limit collapsed into one field. So `refutationStrength(kind)` is now a function of the window kind and nothing else: `decisive` (weekly — may refuse a write on its own), `corroborating` (five-hour — may only confirm an identification the LEDGER made independently), `none` (anything unmeasured; a new backend's bucket must earn `decisive` with its own measurement, never inherit claude's). **The asymmetry is the point, and it is money in both directions**: refusing a wall is not the free conservative option it looks like — a mark that never lands leaves the pool sending turns to a member the CLI has just refused, which is what `guardReadingTarget` warned about when it exempted walls in the first place. At 13 % of 5h walls, round 1 would have bought the panel fix with a new leak (reproduced end to end: with every kind decisive, an ordinary 5h wall on a member whose panel stamp disagrees is REFUSED — `utilization` stays 0.1 and the journal says "refusing to mark"). The incident itself is a five-hour wall and is still fixed, because there the ledger identifies independently and the window only corroborates.

**③ THE SHIPPED STATUSLINE DELETED THE CANONICAL HALF — the sixth loss on one line.** `data/bin/vibespace-usage` is the highest-frequency writer on the instance (up to once per 8 s per account) and an allowlisted exception to the one write path, because it ships as a single file to hosts with no checkout. It rebuilt the cache object from a hand-written list of fields to PRESERVE, and that list did not name `limits`. Measured against the REAL tool over a migrated copy of a real cache file: **three limits in, zero out**, the `overage` object gone with them, and the next reader re-deriving the limits under THIS producer's name and clock — so the model claimed a model-scoped Fable cap had been measured by the statusline at a time the same file's `scopedFetchedAt` contradicts, and the panel section that renders per-limit provenance went blank for exactly the account class this producer touches. That preserve list has now been forgotten six times on this one file: `scopedWeekly` (2.70.0), the org identity, `spend` (B-87fe), `corroborated`, the established window (which is why THAT moved to a sidecar), and `limits`. **The fix is not a seventh entry.** The tool no longer enumerates what to keep — it SPREADS the stored object, states only what it measured, and DROPS the one limit it is able to measure (the plan one), whose reading it has just written into the legacy buckets, where `liftCacheObject` reconstructs it stamped with that object's own `source`/`fetchedAt` — the statusline's own, and the only provenance it is entitled to. Every other limit survives byte-identical with the provenance of the producer that DID measure it; a producer that cannot express a window state or another limit's age can never forge one; and the tool never AUTHORS the array on a not-yet-migrated file (a one-element list claiming to be all of them would delete that file's scoped cap the moment anything lifted it). One field is deliberately NOT inherited: `corroborated` is a verdict about a different reading, and this producer has no observation to re-earn it. The reconstruction rung is claude-only — codex names its own limits and none of them is called `plan`.

**ROUND-2 INVARIANTS.** **A rule belongs at every moment the thing it judges is written, not only at the one you were looking at** — count the writers before deciding where the guard goes (this one had two, three tests away from each other). **And when one fact reaches you through several producers, moving one of them means moving all of them** — the producers that carry no evidence must follow the one that does, or a fix that stops marking the wrong member starts marking two. **But move the WRITE, not the bookkeeping the aggregate identifies the subject from** — a key that also selects which other facts are in scope is not a free thing to redirect. **And a piece of per-turn evidence dies after its LAST reader, not where its siblings die**: `_turnWallRefile` looks like the two turn pins and is cleared two statements later than they are, because unlike them it is consumed by the turn-end pass itself. **A fixture whose two inputs collide can hide the defect it was written for**: §18's two rejections shared a bucket, so a later write masked the earlier one; §18d sends them on different buckets and the pre-fix negative control reproduces the incident. **A measurement is only evidence about the quantity it measured** — "consecutive readings seconds apart" is not "a reading against a stamp that can be hours old", and a number quoted for the wrong quantity is worse than no number, because it ends the argument. **Separate a rule's TRUE positives from its false ones before calling it unsound**: three quarters of the 5h refutations were the defect being caught; the honest bound came from the one producer that cannot be mis-filed. **Refusing is not automatically the safe side** — name the harm in BOTH directions and measure the rate at which you would inflict each. **Evidence has a strength, and the strength belongs to the evidence's KIND, not to the caller's judgement**; an unmeasured kind is worth nothing, so the next backend has to measure its own. **A list of what to KEEP is a promise every future edit must remember; a spread of what was there is a property** — after the second loss, invert the default instead of extending the list. **A producer may only stamp provenance on a number it actually took** — the cleanest way to guarantee that is to make it structurally unable to express the others. **An assert that cannot fail is not an assert**: the first version of the five-hour control ran in a fixture that stamps no 5h window, so the rule short-circuited on "no evidence, no refusal" and it passed whatever the strength rule said — it now asserts its own precondition first, and goes red under mutation. **A wiring pin should freeze the mechanism, not the count**: "exactly two call sites carry `slot:`" froze how many rejection paths exist rather than what each must do, and the third one broke it; it now asserts that no wall signal resolves the slot freshly, and that a site with no slot to state carries its lane instead.


**ROUND 3 — THREE WAYS A COUNTING BUCKET STOPPED COUNTING (2026-09-09, an adversarial verifier on this branch; each reproduced end to end before being fixed).** The model exists to stop an EMPTY window being read as evidence. Round 3 is its mirror: three mechanisms that dropped buckets which WERE evidence, each costing money in the same shape — the bucket vanishes from `accountRemaining` / `weeklyDeadline` / `bucketRems` / the anchor stream, and the pool keeps spending against a member the CLI has already refused.

**① A STATED SPEND WAS NOT DECISIVE.** `windowState` read the reset BEFORE the utilization (`if (used == null || resetsAt == null) return 'unknown';` ahead of `if (used > 0) return 'running';`), so "we know the spend but not the deadline" collapsed into `'unknown'` — and `bucketCounts` drops `'unknown'` exactly as it drops `'empty'`. Reachable on ordinary data, not a corner: the `· resets` clause of a claude panel line is OPTIONAL (`parseCliUsageText`; scripts/test-cli-usage-parse.mjs pins the reset-less shape), `refreshViaCliPanel` deliberately refuses to project the 5-hour one ("its window starts with the first request; a stale one would be a lie"), `parseOAuthUsage` maps a missing reset to 0 — and **four of the nine live claude cache files on this instance** hold `fiveHour` with no reset. Measured end to end through the real write path: a member 95 % through its burst window read `{remaining: 80}` and `decidePoolSwitch` answered **`healthy`**. The clause order is now the claim it should always have been — the deadline answers *has this window started*, so it is asked only after `usedPct > 0` has failed to settle the question. `windowRemaining` already handled a missing reset (it uses it only for the roll-over test) and `deadline()` simply skips a running window that states none.

**② A STORED VERDICT OUTLIVED THE READING IT DESCRIBED.** `state` is a verdict the projection computes from a (usedPct, resetsAt, measuredAt) triple — but it is emitted onto the legacy bucket, and that object is SPREAD forward by every producer (`{...(cache[ev.kind] || {})}` in rate-limit-capture, `bump()` in markLimitBanner). So a producer that re-measured a bucket carried the old verdict onto the new numbers. Measured: a 5h bucket stamped `unknown` while it had no reset kept that stamp through a fully dated 95 % reading (`{remaining: 80}` instead of 5 — this is what made ① permanent rather than transient); the routine post-weekly-roll shape, correctly stamped `empty`, kept it while three real `rate_limit_event`s climbed the 7d to 90 % and the Fable cap to 100 % — `{remaining: 90}` instead of 0, `weeklyDeadline` **null**, `bucketRems` listing only the 5h row. That is **inc-msof8i22 / 2.305.0 re-opened**: a spent model cap invisible to the pool's exhaustion test, and invisible to the estimator too, since `usage-anchors` filters on the same predicate. The fix is TWO HALVES and they are not redundant — each catches a case the other structurally cannot. The PRODUCERS drop the verdict whenever they re-state a number (`restated()` in rate-limit-capture; unconditional in `bump`, which always restates): this is the only half that catches a re-measurement DOWN to 0 % onto a reset that has since PINNED — the window really did open, the member barely used it, `usedPct > 0` says nothing, and keeping the `empty` stamp costs that member its real weekly deadline. The READER keeps a clock-free belt in `fromLegacy` — `if (STATES.includes(b.state) && !(usedPct > 0))` — because a stated spend refutes `empty` by definition and needs no clock; that half heals a stamp arriving from a FILE an older build wrote (on the next write) or from a producer nobody enumerated. **What must NOT change**: a `status`-only event re-states no number, the reading is unchanged, and the verdict SURVIVES — that is the entire reason the stamp is trusted rather than re-derived, since a legacy bucket read back off disk usually carries only the FILE's `fetchedAt`, a later clock that would compute `elapsed > jitter` and flip a genuinely empty window to `'running'` — B-8b12 backwards.

**③ THE DERIVED VIEW WAS MORE OPTIMISTIC THAN THE SET IT PROJECTS.** `scopedWeekly` gives a model limit's BUDGET window a home and the plan limit's own windows have theirs — but a model limit's FIVE-HOUR window had none at all: `isBudgetKind` excludes `'5h'` from the scoped projection, and `fiveHour` was `toBucket(windowOfKind(plan, '5h'))`, the plan limit alone. `account-pool-auto` reads the VIEW, not the accessor. Driven with the two real codex push shapes censused from this instance's buffers (plan 7d 33 %; `codex_bengalfox` / "GPT-5.3-Codex-Spark" 5h 90 % + 7d 10 %): the accessor answered `{remaining: 10, by: 'codex_bengalfox', kind: '5h'}` and the pool answered **`{remaining: 67}`** — a 57-point drift at the exact seam B-9213 is about, and a **REGRESSION**: the base commit's last-writer-wins collapse happened to keep the Spark 5h and answered 10. §⑫ had only ever tested the 0 % direction ("a Spark bucket at 0 % never grants headroom the plan bucket lacks"); the inverse was untested and broken. `fiveHour` now means what every reader has always used it for — THE BINDING BURST CONSTRAINT on the account, the min over every counting 5-hour window in the set. One rule, no backend branch (claude's model caps are weekly-only, so it can only ever pick the plan window there), windows that make no claim are not candidates, and with no counting 5h window anywhere the plan's own bucket is still emitted so the panel keeps seeing a limit the vendor reported.

**ROUND 3, SECOND PASS — THE FIX FOR ③ CONTAINED ③ (found by driving ③'s own property with a passed reset).** "The binding burst constraint" was first spelled as *the highest `usedPct`*, which is not what binding means. A window whose reset has PASSED rolled over since we read it, so its stale utilization means nothing and it is FULL — that is `windowRemaining`'s rule, and `bucketRemaining` (the pool's) applies it to whatever bucket the projection emits. So a 90 %-spent-but-rolled-over model window DISPLACED a plan window with a real 50 % left, and the view came out **more optimistic than the set** (measured: accessor 50 %, pool 90 %) — the exact defect ③ exists to prevent, living inside the fix for it, and invisible because ③'s matrix used only future resets. The ranking is now `windowRemaining` on the SAME clock `remaining()`/`deadline()` take (`toLegacyView(set, {nowSec})`, defaulting to now), so the invariant holds by construction rather than by a second, weaker ordering. And ties now go to the PLAN window because it is SEEDED first rather than left to iteration order — every window of a set read long after it was measured ties at 100, so without the seed a Spark bucket displaced the plan limit again, undoing §⑭'s own invariant. **The lesson is about the guard, not the ordering**: a property is worth exactly the shapes it is driven over, so the matrix now carries a rolled-over window and §⑯d asserts that the property goes red on this mutant — it could not before. Mutation-test your own new legs, and when a leg compares two answers, check they were asked with the same clock (the first version compared a real-clock projection against a fixed-clock accessor, which would have measured the leg's inconsistency rather than the product's).

**ROUND-3 INVARIANTS.** **A predicate's clause order is a claim about which fact is decisive** — put the fact that alone settles the question first, and let the others narrow what is left. **A stored verdict is only as good as the reading it describes**; the producer that replaced the numbers is the one that must drop it, and the reader's belt is for the files and producers nobody enumerated — **two mechanisms need two negative controls, each pointed at a case only it can catch** (a control both halves satisfy measures nothing; round 3's first draft had exactly that, and rewriting the controls is what found the DOWN-to-0 % case). **A lossy projection must lose CONSERVATIVELY, and that is a property to drive, not a list of fields to remember**: the guard against the next missing window kind is `accountRemaining(toLegacyView(set)) <= remaining(set)` over a matrix of shapes, with the plan-only projection as its control. And a census must not flatter itself — a reader that calls the model's predicates but consumes the derived view is `view-only`, not `migrated`; it is exactly as right as the projection, which is what makes that property test load-bearing.

**GATES.** test-quota-model 179 (§⑮ drives the REAL shipped tool over a file carrying `limits`, with the round-1 literal as the negative control; §⑯ the three round-3 defects + §⑯d the one the fix for ③ introduced — each with a PATCHED COPY of the real module as its negative control, the patch asserted to hit, and the view-vs-accessor property over a 10-shape matrix that now carries a rolled-over burst window), test-readings-attribution 445 (§15 the rejection is judged at the write + two controls for the cases that must NOT change, §18d the two-bucket shape with a PRE-FIX patched copy of the engine, §18c the strength table with round 1 as its control), test-auto-resume-loop 205, test-rate-limit-capture 46, test-auto-resume 222, test-pool-auto 74, test-account-pool 33, test-account-verdicts 45, test-usage-anchors 12, test-usage-estimator 83, test-quota-source 112, test-codex-pool 61, test-new-member-wake 119, test-login-expiry 257, test-migrations 14, test-vendor-whitelist 63 (zero new vendor surface), test-architecture 110. Mutation-tested: the strength split (6 red), the per-record call (the §18d control), the statusline spread (the §⑮ control), the re-based wiring pin (2 red on a fresh slot resolution), the banner redirect, the banner's signal key (which reproduces the dropped-own-wall regression), and the ordering of the proof's clear.

**ROUND 4 — A FREE MEMBER IS NOT AN UNKNOWN ONE (2026-09-09, the round-3 verifier; reproduced end to end against the base commit before being fixed).** Round 3 fenced the not-yet-started window off from every answer a window gives. But a window answers TWO questions and only one of them is B-8b12's. **"When does this budget end?"** — an empty window has no answer; its reset is `now + duration` on every read, so ranking on it makes it the earliest deadline forever and arms auto-resume on a number that has already moved. `bucketCounts` / `countingWindows` exist for that question and are UNCHANGED. **"How much is left?"** — an empty window has a precise answer: ALL of it, because the vendor STATED a spend of 0 %. Dropping that turns a definite claim into ignorance, and ignorance is not neutral here: it is `known:false`, which the pool ranks at `UNKNOWN_REMAINING_PCT` (50) and `quotaVerdict` reports as "no usage data" (`usable:null`).

**MEASURED.** A brand-new claude account whose panel prints `0% used` on both plan buckets — the ORDINARY shape, since the `· resets` clause of a panel line is optional, `refreshViaCliPanel` deliberately refuses to project the 5-hour reset, and a fresh account has no prior reset to carry forward — read back to the pool as "no usage data": `accountRemaining` `{remaining:100,known:true}` → `{null,false}`, `quotaVerdict` `usable:true` → `usable:null` (so the engine's own pool gate, `verdicts.find((x) => x.v.usable === true)`, finds nobody and reports the WHOLE POOL blocked), `rankPoolMembers` eff 100 → 50, and `decidePoolSwitch` off an EXHAUSTED member (4 % left) went from `{to:'new', toRemaining:100, reason:'exhausted'}` to `{to:null, reason:'no-settleable'}` — the pool keeps spending on a spent member while a fully free one sits beside it. A REGRESSION against the base commit, and the branch's own backfill proves it on real data: over a copy of this instance's `data/usage-cache` (12 scanned / 12 stamped / 31 limits / idempotent) it flips exactly ONE of twelve files — `__global_codex__.json`, the only one whose every window is fresh, i.e. the measured B-8b12 shape — from `{usable:true,known:true}` to `{usable:null,known:false}`. The other eleven are untouched because their 7d is running, which is why every gate suite stayed green.

**THE FIX = TWO QUESTIONS, TWO PREDICATES**, written side by side in the PURE module so one rule in two spellings cannot drift: `windowStatesSpend` / `bucketStatesSpend` (`num(usedPct)` / `num(utilization)` — `num()` and not `Number()`, because `Number(null)` is 0 and "the vendor said nothing" must never read as "the vendor said zero"), `remaining()` mins over `spendingWindows`, and `bucketRemaining` gates on the legacy twin. Measured on this instance — 35 cache buckets, 14,286 anchor buckets, ZERO missing or null — so the shape where the new predicate is stricter than the pre-model one does not occur, and refusing it is the direction that cannot invent headroom. Including empty windows cannot go the other way: `remaining` is a MIN, so a 100 %-free bucket can never lift another limit's 5 %; it can only flip `known:false` → `known:true`. B-9213's harm was a Spark push OVERWRITING the plan limit and the per-limit MERGE is what fixes that. **§⑫'s own negative control had said so since round 1** — *"an empty bucket is 100 % free, so it never HID an exhaustion — the harm was always the deadline"* — the branch's own test file stated the principle while the code applied the exclusion to both answers.

**TWO SECOND-ORDER DEFECTS, both found by this round's OWN controls.** ① `toLegacyView`'s `toBucket` fabricated `utilization: 0` for a window with no stated spend, so a producer that stated only a reset came back off disk as `{utilization: 0}` and the pool read 100 % free and KNOWN while the typed accessor beside it correctly answered "no claim" — the view MORE optimistic than the set, which is exactly what §⑯c exists to prevent and could NOT see, because its property compared two KNOWN answers. The bucket is still emitted (a limit the vendor reported must stay visible) but carries no `utilization`, and the property grew the clause `(v.known && !a.known)`: claiming to know is itself an optimism. ② A `bucketRems` row publishes its `resetsAt` only when the bucket may name a deadline — the row exists because the bucket states a REMAINING. Today a 100 %-free bucket can never reach the `dead` list `quotaVerdict` turns into `blockedUntil`, so this is safe by CONSTRUCTION rather than by that argument, which is the point: the argument would otherwise have to be re-made by every future reader of the field.

**INVARIANTS.** **One predicate per question.** A window is evidence about two different things and a single fence answers only one of them correctly; before reusing a predicate, name the question it was written for. **Ignorance is not the safe default when a fact was stated** — "no usage data" costs a member the ranking, the usability verdict and its settleability, so refusing to read a stated 0 % is a money decision, not a conservative one. **When one harm ends up with two independent belts, each owes its own negative control**: round 3's `state` machinery and this split now both hold the pool's remaining honest, so §⑯a/§⑯b's controls were re-pointed at what only THEY protect (the ANCHOR stream — the estimator's whole input and the corpus the window fingerprint identifies accounts from — and the weekly deadline), and new §⑯a-x/§⑯b-x remove BOTH to reproduce round 3's own published numbers (80 % free; the spent Fable cap invisible at 90 % with a null deadline). **A property that bounds A against B guards the SEAM, never the shared rule underneath it** — §⑯c could not see this defect because the view and the accessor AGREED; the rule itself needs a leg that names an answer. **And the test file's own words are evidence**: a negative control whose message contradicts the code beside it is a bug report nobody read. Mutation-tested against the product source: reverting `bucketRemaining` → 14 red, `remaining()` → 5 red, `toBucket` → 2 red, the `bucketRems` reset gate → 1 red, restored → 0. **scripts/test-quota-model.mjs 214** (+ test-pool-auto 74, test-account-verdicts 45, test-readings-attribution 445, test-auto-resume-loop 205, test-spend-paths 261 all green).

**ROUND 5 — WHO MEASURED THIS NUMBER, AND WHO MAY SAY A LIMIT IS GONE (2026-09-09, the round-4 verifier; both reproduced against a copy of this instance's own post-migration cache file before anything was changed).** Both defects live in `writeCacheObject`'s LIFT, and they are the two halves of one gap: the lift is handed a LEGACY object, and a legacy object is ONE reading spread over a carry-forward of everything else. It could not tell the halves apart, so it stamped the whole file with this producer's name and clock (①), and `mergeLimitSets` — correctly, for a producer that knows about one bucket — let nobody ever say a limit had stopped existing (②).

**① PER-LIMIT PROVENANCE WAS OVERWRITTEN ON EVERY CLAUDE WRITE — the write path's own guarantee #4, and the panel rows added to enforce it, both lying.** `captureRateLimitEvent` spreads the identity's freshest file, changes the ONE bucket its event names, and hands the whole object here; `liftCacheObject` then rebuilds EVERY limit on it via `fromLegacy`, which stamps `source: srcName, fetchedAt: at` on all of them, and `mergeLimit` prefers the copy with the newer (or equal — the test is `>=`) `fetchedAt`, so the freshly-clocked copy always wins. Measured with the real modules over a copy of this instance's `usage-cache/sub-*.json` (post-migration, three limits, anonymised). BEFORE: `plan: on-demand@1788974610137 | model:fable: on-demand@1788974610137 | overage: on-demand@1788911228197`. ONE `rate_limit_event` about the five-hour bucket — a record that says nothing whatever about Fable — AFTER: **all three re-stamped `rate-limit-event`, the Fable cap dated to now, and its window's `measuredAt` moved onto a `utilization: 1` nobody re-measured.** Every claude producer reaches it (`src/rate-limit-capture.js`, the limit-banner mark — so the panel said the Fable cap came from a limit hit — `writeUsageCacheForKey`, `refreshViaCliPanel`, the host harvest); the codex path is CORRECT because it passes a TYPED SET. User-visible through `limitRows` → `via {source}` / `Updated {ago}` in `usage-meter`, i.e. the standing rule *"a producer WE SHIP always stamps its own `source` at the write"* and *"'Updated 3min ago' alone is the sentence the readings-by-slot incident could not survive"* — violated by the feature added to enforce them. 659 green asserts said nothing, because the §17 panel leg in test-readings-attribution feeds a HAND-BUILT `limits` array and never goes through the write path.

**THE FIX IS A DERIVATION, NOT A LIST.** "Which fields did I measure?" asked of each producer is a preserve list, and a preserve list is the single tool that has now failed on this store SIX times (`scopedWeekly`, the org identity, `spend`, `corroborated`, the established window, `limits`). The FILE can answer it without being asked: PURE `windowClaimKey` / `limitClaimKey` / `sameLimitClaim` (quota-model) spell what a reading CLAIMS — the numbers, deliberately excluding `measuredAt` and `state`, which are the provenance halves — and `carryUnmeasuredLimits` (the write path, lift-only) hands back the PREVIOUS object for anything whose claim is unchanged. **Granularity is the WINDOW, not the limit** — the claude plan limit holds two windows and a `rate_limit_event` names exactly one bucket, so resolving per limit still re-clocked a week-old 7-day number with this event's clock, and `windowState` reads that clock (found by this round mutation-testing its own first leg; it has its own single-mechanism control). It is **not** applied to a typed `set`: a producer that builds a LimitSet states exactly the limits it carries — that is what the typed shape is FOR — and re-deriving one there would be a second, weaker answer to a question the producer already answered. **The direction it errs in is deliberate**: a producer that genuinely re-measured a number and got the same value keeps the OLDER age (the row says "measured then", not "confirmed now"), which is the conservative half — a number that looks staler than it is costs a re-read; a stale number wearing a fresh timestamp is the incident. The FILE-level `fetchedAt` is untouched, so every freshness-ranked mechanism (the anchor sweep, the identity-group "freshest file" base) sees exactly what it saw before — **the file's `source` and a limit's `source` answer two different questions** ("who wrote this file last" vs "who produced THIS number"), and after this fix they may legitimately disagree.

**② A LIMIT THE VENDOR STOPS REPORTING WAS RESURRECTED FOR EVER, WITH ITS LAST NUMBER.** `mergeLimitSets` keeps every previous limitId unconditionally — the whole of the B-9213 fix, and right for the producers that know about one limit — so NOBODY could say a limit was gone. Reproduced on the real write path + the real pool: two model-scoped caps (`OldModel` at 100 %, `Fable` at 10 %), then the next panel read naming only `Fable` — on disk `scopedWeekly` still carries `OldModel` at 1, `toLegacyView` re-emits it, and `accountRemaining` answers **`{remaining:0, known:true}`** for an account whose live reading says 80. The base commit answers 80: the producer's list landed verbatim there, so this is a **REGRESSION of this branch's own making**. Harm is bounded by the stale cap's own `resetsAt` (`bucketRemaining` returns 100 once it passes) — up to one week of a healthy member excluded — and the trigger is a cap retirement or rename, which is why it is latent here: across all eight anchor streams on this instance the only scoped name that has ever appeared is `Fable`. `replace: true` is not the answer; it is documented as repair-only and it drops EVERYTHING, including limits this producer never had an opinion about.

**SO THE PRODUCER SAYS IT, AND ONLY A PRODUCER THAT CAN.** `authoritativeScopes` names the scopes THIS write enumerated; a prev limit of such a scope that the write does not name is RETIRED, loudly (removing a limit removes a CLAIM, and this one gates the pool). It is passed at exactly the seven sites whose OWN parse produced the list — `refreshViaCliPanel`, the bare-token ⟳, the four host ⟳ legs and the engine's control-channel `get_usage` probe — each gated on `u.scopedWeekly?.length` / `parsed.scopedWeekly?.length`, i.e. on the list THIS read produced and never on the preserve-merged object (all six of those call sites carry `merged.scopedWeekly = u.scopedWeekly?.length ? … : prev.scopedWeekly`, so a carried-forward list states nothing about what the vendor still reports). The single-bucket producers, the shipped statusline and the migration never claim it, and **forgetting to pass it leaves today's behaviour** — a stale cap lingers until its own reset — which is the safe direction to fail in.

**ROUND-5 INVARIANTS.** **A producer may only stamp provenance on a number it actually took — and when the producer cannot tell you which those were, DERIVE it from the file instead of asking for a list.** After the sixth loss on one preserve list, the answer is not a seventh entry: a claim key over the numbers turns "did I measure this?" from a promise into a property. **Provenance has the granularity of the thing that was measured**, and here that is the WINDOW, not the limit — a rule resolved one level too coarse still re-clocks a number nobody looked at. **The file's `source` and a limit's `source` are different questions and are allowed to disagree.** **A merge that can only ever ADD means nobody can ever say a thing is GONE** — give the right to retire to the one producer that enumerates, gate it on that producer's OWN parse, and make forgetting it the harmless direction. **A number that looks staler than it is costs a re-read; a stale number wearing a fresh timestamp is the incident** — when a rule must err, name which way and say why. **And a leg that feeds a hand-built store bypasses the code that fills it**: both defects lived under 659 green asserts because the panel-provenance leg built its `limits` array by hand — drive the REAL producer through the REAL write path, or the store's own rules are untested. Mutation-tested against the product source: removing the carried-forward rule → 7 red, removing the retirement → 4 red, resolving per LIMIT instead of per WINDOW → 2 red, removing one call site's wiring → 1 red (the pin), restored → 0. **scripts/test-quota-model.mjs 249** (§⑰a/b/c, each with a PATCHED COPY of the real module as its negative control and the patch asserted to hit, plus a POSITIVE control that a real `rate_limit_event` retires nothing) + test-readings-attribution 445, test-rate-limit-capture 46, test-pool-auto 74, test-account-verdicts 45, test-usage-anchors 12, test-quota-source 112, test-auto-resume 222, test-auto-resume-loop 205, test-new-member-wake 119, test-spend-paths 261, test-architecture 110 — all green.


## THE INBOX ✓ SLID THE NEXT ROW UNDER THE POINTER (inc-mtw02kbq-kj96, 2.369.90)

Owner: "inbox里点击对勾后改变了这个对话的消息数量会导致位置变化，当我想连续点击时造成误点". The "For you" popup re-rendered from scratch on every `user-todos-updated` broadcast; a ✓ moved its row out of the open groups (and shrank the group), so the rows below slid up and the second rapid click at the same screen position hit a different item (bundle: four ✓ at y≈365 within 1.4 s, a broadcast between each).

FIX = an append-only layout for the popup's lifetime (PURE `src/lib/user-todos-layout.js`): `openLayout` from the sorted groups on open, `nextLayout` keeps every known id in its slot — resolved rows rendered in place, dimmed and struck through with ↺ — appends new ids at the end of their group, drops purged ids; `entriesFor` renders it; the group count shows open rows only and the "Recently resolved" tail skips rows still holding their slot above. The sorted order is rebuilt on the next open (any render while the popup is hidden drops the layout).

不变量=**a list the user is clicking INTO must not reflow under the pointer on the click's own consequence** — resolve in place, re-sort on the next open. Negative control = the pre-fix grouping (`todos.open` → groups), which demonstrably slides row 3 into row 2's slot. Gate: test-user-todos-layout 12 (incident replay, reopen-in-place, arrivals append, close rebuilds, the pre-fix control, wiring pins).

## THE TEST SUITE WROTE A FABRICATED CONVERSATION INTO THE REAL ~/.claude, AND THE PRODUCTION LEDGER BILLED IT (2026-09-09, owner: a Fable conversation, and Fable usage, that never happened)

**WHAT THE ARTIFACTS SAY.** `data/usage-history/events-2026-{07,08,09}.ndjson` on this instance held **79,533 rows** for two session ids nobody ever started — `e2e00000-0000-4000-8000-000000000001` and `…0002` — every one `"model":"claude-fable-5"`, `"acct":null`, `"atype":"global"`, most with `"cwd":null`. Beside them, 245 more rows whose `cwd` is a throwaway `/tmp/vs-…` directory. `_cursors.json` (5.2 MB, 30,424 entries) carried **222** entries naming `~/.claude/projects/-tmp-vs-chatpage-test-*` and `-tmp-vs-chat-e2e-cwd-*` files that have not existed for weeks. The two sids are `SID` in `scripts/test-chat-paging.mjs` and `scripts/test-minimap-jump.mjs`.

**THE MECHANISM, IN THREE FACTS.** ① Those two suites build a synthetic transcript — hand-written `assistant` records that name a model and carry a `usage` block although no API request ever happened — and wrote it to `path.join(os.homedir(), '.claude', 'projects', …)`. They had to: they spawn a worktree server with `env: { ...process.env, PORT, VIBESPACE_SKIP_AGENT_HOOKS }` and **no `HOME`**, and a server can only discover a transcript that lives under the home it is RUNNING with. ② While the suite runs, the machine's PRODUCTION VibeSpace instance is walking that same directory — `src/usage-walker.js` reads every `assistant` record with a `message.usage` and appends a permanent ledger row. ③ `acct` is null on all of them, so `costBetweenMulti` files them under the machine login `__global__` and counts them into the `costSince` of its anchor pairs — i.e. into the **learned burn rate the quota estimator spends against** (`src/usage-anchors.js` → `rates.json` → `src/usage-estimator.js`). The Usage window's Fable row, and the estimator's idea of how fast this machine burns, were both partly fiction.

**THE WINDOW IS SECONDS AND THE DAMAGE IS FOREVER.** Both suites DO clean up: `process.on('exit', cleanup)` removes the project dir on the happy path. That is why the directory is almost never there when you look — and why nobody found it by looking. The fixture exists for the suite's runtime (44 s for test-chat-paging, 12 s for test-minimap-jump), which is all the production instance's 3-minute rescan and its per-request walk need. Caught in the act while this fix was being written: at 14:25 UTC `-tmp-vs-chatpage-test-620924` appeared in `~/.claude/projects` (another worktree running master's copy of the suite), and at 14:26 UTC the production ledger grew by 5,406 rows — one full run's worth. `cleanup` also runs from `'exit'` only, which **does not fire for a default-terminated SIGINT/SIGTERM**; measured with a same-source A/B (SIGTERM at 8 s, throwaway HOME both arms): the pre-fix suite left **2,056,584 bytes** under `$HOME/.claude/projects/-tmp-vs-mmjump-test-<pid>`, the fixed one **0**.

**A THIRD SUITE HAD THE ANSWER ALREADY.** `test-desktop-resume-paging.mjs` — same class, same synthetic transcript, bigger — passes `HOME: fakeHome` to its server and writes its fixture there. It is the template the other two were fixed to. And `test-chat-e2e.mjs` (one REAL haiku turn per push, so the CLI writes a REAL transcript under the developer's home) removed its project dir at the very END of the happy path, after four `process.exit(1)` returns and with no signal handler — 155 of the 222 dead cursors are its throwaway cwds. Measured on a throwaway HOME: even on the happy path it left 324 bytes of CLI state the cleanup never named.

**THE FIX IS TWO-SIDED, AND ONLY THE SECOND HALF SURVIVES A LEFTOVER.**
① **ISOLATION.** `scripts/scratch.mjs` gains `scratchHome(name, fs)` and `fixtureSid(suffix)`; every suite that spawns a server and writes a transcript passes `HOME: fakeHome`, removes that home in `cleanup`, and installs SIGTERM/SIGINT/SIGHUP handlers beside `process.on('exit')`. `test-chat-e2e` is isolated too — **measured, not assumed**: claude 2.1.263 in the chat-wrapper's exact flag shape serves a full turn under a throwaway `$HOME` with the oat riding `CLAUDE_CODE_OAUTH_TOKEN` alone (`system/init` → `assistant` → `result/success`, 2.4 s, no onboarding gate, transcript in that home), and the account this suite seeds is oat-only by construction, so `resolveForSpawn` takes the `oatOnly` branch and `HOME` is not in `AGENT_ENV_DROP`. Its cleanup is now unconditional and it sweeps its own stale leftovers at start, exactly like the wire probe's r5/r6 contract.
② **NO PRODUCTION READER COUNTS A FIXTURE.** `src/fixture-guard.js` (PURE) is the ONE declaration of the convention — the `vs-` scratch prefix under a tmp root, and the `e2e00000-0000-4000-8000-` session-id family. `src/usage-walker.js` and `src/session-store.js` ask it; the shipped single-file scanner `data/bin/vibespace-usage-scan` carries an INLINE COPY (a checkout-less ssh host cannot require `src/`), driven over one table by `scripts/test-usage-walk-parity.mjs`. ① alone is not enough: a suite killed by a signal, an OOM, or a `git worktree` cleanup that never reached its handler leaves the fixture behind, and the production instance picks it up seconds later.
③ **THE HISTORY.** Migration `2026-09-purge-test-fixture-ledger` (`src/fixture-ledger-purge.js`) archives every fixture row to `data/archive/fixture-ledger-rows-<date>.ndjson` with a reason, drops the dead cursors and the synthetic attribution entries, VOIDS the `costSince` of every anchor whose interval contained a removed row (the reading is real; the cost measured beside it is not — the same move `src/reading-repair.js` makes), and drops `rates.json` so the estimator re-learns. **Measured on a COPY of this instance's stores (2026-09-09 14:39 UTC):** 573,802 rows in → **79,778 removed** (79,533 fabricated / 245 real-turn-in-a-fixture-cwd) → 494,024 out, 79,780 archive lines (conservation checked by line count), 222 cursors, 0 attribution, **1,575 anchors voided across 7 files**, rates dropped, 0 fixture rows left anywhere, idempotent on a second run, 1.26 s.
④ **THE STANDING SWEEP.** `scripts/test-fixture-isolation.mjs`, LAST in the fast tier, runs the pure rule over the real `~/.claude/projects` and PRINTS it. Its censuses are grep-derived over all 197 suites and printed: (a) no suite WRITES under the real `~/.claude` (reading it is fine — this suite and the corpus measurement in test-codex-effort-meta do exactly that), (b) every suite that spawns `server.js` AND writes a transcript NAMES a `HOME` in that spawn env, (c) the suites carrying a synthetic sid mint it, handle signals and census the real home themselves. Its strongest control is the RETIRED BYTES: `git show master:scripts/test-chat-paging.mjs` through the shared sanitized git env, asserting the census catches both offenders exactly as they shipped — and a tree git cannot read is a loud SKIP, never a red.

**THE COST, STATED.** Skipping the whole convention also stops ledgering the handful of REAL turns the CI probes run in a throwaway cwd: 245 rows of genuine machine-login spend over the ledger's lifetime, 0.04 % of its 573,802 rows, against 79,533 fabricated ones. Those transcripts are deleted by their own suites seconds later, so the ledger was keeping rows for conversations the product itself removes. Token counts are reported PER CLASS (`syntheticTokens` 982,140 vs `fixtureCwdTokens` 6,147,245 — the latter dominated by cache reads) precisely so the fabricated half cannot hide inside the real one.

**WHAT COULD NOT BE ISOLATED.** `scripts/probe-claude-stdout.mjs` measures what the INSTALLED CLI puts on our stdout with the machine's real credentials, so a throwaway `$HOME` would measure a different install. It stays the ONE entry in `REAL_HOME_FIXTURE_PREFIXES`, with its reason, and the sweep spares its dirs only while they are younger than `FIXTURE_STALE_MS` — a constant the probe now READS from the guard instead of declaring its own copy, because two hand-written copies of one threshold is how the sweep and the probe would come to disagree about the same directory (the probe's own r6/r7 lesson, one layer up). Older one-off fixture names outside the `vs-` convention (`-tmp-vsv-probe`, `-tmp-otel-cap-cwd-*`, a handful of hand-run experiments) are deliberately OUT of scope: the guard is narrow on purpose and the sweep says so rather than widening silently.

**INVARIANTS.** **A test fixture that a production reader can see is production data.** The isolation and the refusal are two halves of one fix, and only the refusal survives the case where no cleanup runs — so build both, and never accept "it cleans up afterwards" as the whole answer for a window in which another process is reading. **A suite that spawns a server must NAME the home that server gets**; inheriting one is a decision nobody wrote down. **`process.on('exit')` is half a cleanup** — the signals that end a test run in anger do not fire it. **A synthetic record that carries a `usage` block is a claim about money**, so the id family it uses belongs in a shared constant that the production walk refuses by name — the only evidence that survives a fixture with no cwd. **Decide with a MEASUREMENT whether isolation is possible before assuming it is not** (the real CLI serving a turn under a throwaway home on the oat alone is a two-minute experiment that turned "it must keep the real home" into "it never needed it"). **Report a repair's token counts per CLASS** when the classes mean different things; one number lets fabrication hide inside real spend. And **an exemption must be paid for**: the sweep exempts itself from two of its own censuses because its CONTROLS spell the forbidden shapes inline — so it asserts instead that its real-home section contains no write call and that every child it starts is `git`, and that each exemption is still LIVE (a dead one fails, and one of these proofs was already falsified once, within the same afternoon, by adding the control that spawns `git`).

**GATES.** `scripts/test-fixture-isolation.mjs` (54: the pure rule, the sweep decision, a planted fixture under a throwaway home with the walk refusing it and a real project dir still walking as its negative control, the three grep censuses with pre-fix synthetic controls and the retired-bytes control) · `scripts/test-usage-walk-parity.mjs` (40: both spellings refuse the same fixture tree and agree over an 11-name × 6-id table, non-vacuously) · `scripts/test-migrations.mjs` (29: the purge over a disk-shaped fixture — an unparseable line is never removed, the two classes carry different reasons, only the anchors whose interval contained a removed row are voided, a clean instance is untouched and not notified) · plus the per-suite census inside test-chat-paging, test-minimap-jump, test-desktop-resume-paging and test-chat-e2e.

**r2 — THE CENSUS THAT CERTIFIED A CLEAN SHEET WHILE AN `rm -rf` OF THE REAL HOME STOOD ONE ALIAS AWAY** (round-1 verifier, reproduced first).

**THE DEFECT.** Census (a) — the assert whose whole job is *no suite writes under the REAL `~/.claude`* — matched a write only when its FIRST ARGUMENT was literally the bound name or `path.join(<bound name>, …)`. `scripts/test-chat-e2e.mjs` binds `REAL_PROJECTS = path.join(os.homedir(), '.claude', 'projects')`, then does `const p = path.join(REAL_PROJECTS, d.name)` and `fs.rmSync(p, { recursive: true, force: true })` — a recursive DELETE under the developer's own home, ONE hop of aliasing away, and the census could not see it. Reproduced by extracting the shipped `realHomeWriters` VERBATIM out of the suite's own bytes and running it over that file: `bound names ["REAL_PROJECTS"]`, `census hits []`; the live suite printed `census (a): walked 195 suites (+2 exempt …)` followed by `✓ no suite writes under the REAL ~/.claude`. The round-1 commit and the sweep's own header both sell that census as the enforcement of the isolation rule, and the report disclosed only its HOME-spelling limit, never its write-TARGET-spelling limit. **HONEST BOUNDARY:** today's blast radius is bounded by the suite's own filter (`isFixtureProjectDir` ∧ the `chat-e2e` substring ∧ `ageMs > FIXTURE_STALE_MS`), so no real conversation was ever at risk — the defect is that the write is UNDECLARED, UNPINNED and STRUCTURALLY INVISIBLE, so widening that filter, or any future indirect write in any of the 197 suites, lands with no red.

**THIS CENSUS HAS NOW BEEN WIDENED TWICE, AND BOTH MISSES WERE "ONE MORE HOP."** Round 1 widened the HOME spelling (it knew only `os.homedir()`; the one suite that genuinely writes there spells it `process.env.HOME || os.homedir()`). This round is the TARGET spelling. So the resolver no longer stops at a hop count: it takes the FIXPOINT of "a name derived from a name already known to be rooted at the real home" (the known name as the HEAD of a right-hand side — plain alias, `X + '/y'`, `` `${X}/y` `` — or anywhere inside a `path.join`/`path.resolve`), and the write matcher asks whether the call's FIRST ARGUMENT mentions such a name at all (`[^,)]` cannot cross into a later argument, so `fs.writeFileSync(elsewhere, X)` is still not a write TO X). Deliberately over-inclusive, and the cost was MEASURED rather than assumed: over all 197 suites the transitive rule flags exactly one file the narrow one missed (test-chat-e2e) and no others.

**THE EXEMPTION IS SCOPED TO A BLOCK, NOT TO THE FILE.** test-chat-e2e's sweep is legitimate — it removes ITS OWN pre-fix leftovers from the real home, exactly like the wire probe — so it joins `WRITE_EXEMPT`. But an exemption keyed to a FILE turns the census off for the whole file, and the exemption is the dangerous half of a census. Its write is one block, so it pays with the tighter property the shape allows: sentinels (`// >>> real-home sweep` … `// <<< real-home sweep`) mark it, and the payment RE-RUNS THE CENSUS'S OWN RULE over the file with that region CUT OUT, requiring the remainder to be EMPTY — a second real-home write anywhere else in that suite goes red even though the suite is on the list — plus a pin that the block only DELETES and only behind all three gates (the declared convention, the suite's own name, the shared staleness threshold). test-stdout-registry keeps its FILE-scoped exemption because its writes are spread and the property that bounds ITS damage is a different one (the declared `vs-wire-probe-` prefix + a cleanup that survives a signal).

**r2 INVARIANTS.** **A text census resolves exactly the spellings its author happened to look at** — when the same class of miss recurs, stop adding the next spelling and make the resolver TRANSITIVE, then measure the widening's cost across the whole corpus instead of arguing it. **A grep-derived guard has TWO spelling surfaces, not one** (where the dangerous root comes from, and how the dangerous value reaches the call), and fixing only the one the incident happened to expose leaves the other as the next incident. **An exemption is the dangerous half of a census** — scope it to the smallest unit the code shape allows (a sentinel-marked block, re-checked with the census's own rule), because a file-scoped exemption silently blesses every future write in that file. **A guard that certifies a clean sheet must state what it CANNOT see**, or its green line is a stronger claim than the code behind it.

**r2 GATES.** `scripts/test-fixture-isolation.mjs` 69 — census (a) now walks 194 suites (+3 exempt), the exemption is asserted LIVE (which IS the regression assert: revert the resolver and that line goes red on the real file) and PAID FOR in two halves, and the widening carries seven controls: the one-hop shape byte-for-byte from test-chat-e2e, a two-hop shape, a template-literal derivation both bound and inline, an aliased READ that must STILL not be flagged, a real-home path used as CONTENT in a later argument, and the region rule's three cases (inside is covered, a second write outside is not, a missing sentinel FAILS rather than passing vacuously). Five mutations, each red in exactly the assert whose job it is: the shipped narrow resolver (red on the live exemption AND four controls), the exemption removed (red on the census itself), a second real-home write outside the sentinels (payment ①), the sweep's own-name gate dropped (payment ②), and the sentinels removed (both payments).

**r3 — THE SWEEP DEMANDED A REMOVAL NOTHING IN THIS TREE IS ALLOWED TO PERFORM** (r2 verifier, reproduced first).

**THE DEFECT.** `fixtureLitter` gave an in-flight tolerance only to a DECLARED prefix (`if (why && ageMs <= staleMs)`); every other fixture name fell straight through to `offenders.push(...)` regardless of age. So a fixture project dir a CONCURRENT checkout put in the real home turned the MANDATORY pre-push gate red in every OTHER worktree on the box. REPRODUCED live at e1dd1e83 while running the full fast tier: `LITTER: [{"name":"-tmp-vs-chat-e2e-cwd-paw5IT","ageMs":261348,"declared":false}]` → `✗ test-fixture-isolation` → exit 1. ATTRIBUTION, measured not assumed: the mkdtemp suffix (`paw5IT`) is a shape only `master:scripts/test-chat-e2e.mjs`'s `fs.mkdtempSync(...)` can mint — the fixed tree mints `/tmp/vs-chat-e2e-cwd-<pid>` through `scratch()` and contains no `mkdtempSync` at all. This box hosts ~160 checkouts of this repo and the heavy tier is DETACHED, so overlapping runs are the normal case, not the edge one.

**WHY IT IS A DEFECT AND NOT THE FEATURE — three independent reasons.** (a) The same branch writes the OPPOSITE rule thirty lines away: test-chat-e2e's own sweep spares a leftover younger than the threshold because *"it may be a CONCURRENT copy of this suite — two worktrees pushing minutes apart really do overlap."* Every sweeper in this tree has that floor, so at the moment the gate went red, **nothing in the repo was permitted to remove the directory it was demanding be removed** — the human was told to run `rm -rf` for a dir the automation would clear on its own. (b) The repo already records this exact shape as an incident (test-stdout-registry round 5: an ABSOLUTE-ABSENCE assertion turned the mandatory gate red on the very case the sweep exists to spare), and its fix was to make the readers honour the sweeper's own rule. (c) At the moment it goes red the offender is provably INERT under this branch: `runUsageWalk` ingests 0 events from it and discovery skips it by the same predicate — the red was blocking a push while protecting nothing. Self-heal exists but is PARTIAL: only test-chat-e2e sweeps, and only names containing `chat-e2e`; a stale leftover from the other three families has no sweeper anywhere.

**THE FIX IS THE FLOOR THE TREE ALREADY HAD, MADE OPT-IN.** `fixtureLitter(entries, { graceMs })` — an UNDECLARED fixture dir younger than `graceMs` is SPARED and NAMED (`declared:false` + `IN_FLIGHT_WHY`, which says why nothing may remove it yet), and becomes an offender the moment it goes stale. The grace is passed by EXACTLY ONE caller — §4, the whole-directory standing sweep, at the shared `FIXTURE_STALE_MS` — and by nothing else. The four PER-SUITE censuses keep absolute strictness on purpose: they hand this function entries already diffed against a listing taken before their own run, whose mtimes are by construction ≈ now, so a grace there would spare the suite's OWN litter — the vacuous direction. That is also why the branch is guarded on `graceMs > 0`: with the default 0, an entry whose mtime equals `now` has `ageMs === 0` and `0 <= 0` would have quietly relaxed every strict caller. Deliberately NOT done: teaching the census to delete the other three families' stale residue — that turns a read-only measurement into a writer and would break the §4 read-only property census (a) is paid with; the printed `rm -rf` remedy stays for genuinely stale litter.

**r3 INVARIANTS.** **A gate may not demand a removal that every sweeper in the tree is forbidden to perform** — when a check and a cleaner disagree about the same directory, the check is the one that is wrong, and the threshold it must adopt is the cleaner's, not a new one. **An absolute-absence assertion over a SHARED resource is a claim about every other process on the box**; second strike of that shape here, so the tolerance is derived from the existing constant rather than invented. **A relaxation is scoped by its blast radius, not by convenience** — it belongs to the one caller that looks at a resource it did not diff, and a WIRING PIN (grep-derived, printed) asserts BOTH halves: no other suite passes it, and the sweep still does. **`0` is a real value in a threshold comparison** — an opt-in bound must test for opt-in, or the default silently relaxes every strict caller. **A spare is not a shrug**: the entry is printed with its age and a reason naming what is and is not known about it.

**r3 GATES.** `scripts/test-fixture-isolation.mjs` 78 — §2b drives the reproduced 4-minute leftover through the real rule (spared, `declared:false`, reason names the concurrency), with three negative controls (the SAME dir past the threshold is still an offender; the per-suite call shape with NO grace flags a brand-new dir including `ageMs === 0`; a DECLARED prefix keeps its own reason and is never absorbed into the grace) plus a pin that the decision reports the grace it applied; §6 is the wiring pin with both controls. END-TO-END on the reproduction itself (the real suite bytes, `os.homedir()` pointed at a throwaway home carrying the planted leftover): 4 min old ⇒ exit 0 and the entry NAMED; the same dir back-dated 15 min ⇒ exit 1 with the `rm -rf` remedy. Four mutations, each red in exactly the assert whose job it is: the grace branch removed (the r2 shape), `graceMs > 0` dropped (the `0 <= 0` trap), §4's option removed (the relaxation unwired), and a per-suite census relaxing itself.


## THE QUEUE STRIP SHOWED A MESSAGE THAT HAD RUN AN HOUR EARLIER, AND CLICKING ✕ PAINTED IT RED (2026-09-09, owner report on a live codex session)

**WHAT THE ARTIFACTS SAY.** One codex session (`queueVerbs` full, wrapper caps complete). Its buffer: `04:28:23.432Z queue_changed [A]` → `04:28:35.394Z queue_op_result steer ok` → `04:28:35.395Z queue_changed []` → **58 minutes with no queue record at all** → `05:20` the server restarts → `05:25:51.216Z queue_op_result op:remove id:A ok:false reason:'gone'`. That last line is the owner clicking ✕ on a row for a message that had left the queue nearly an hour and one restart earlier; the answer was the toast *"That message is no longer queued — it already ran."* and the row went **red** (`data-queue-state="refused"`) and stayed there. Two separate things are wrong: a client was holding a row nobody could act on, and the one verdict that could have retired it MARKED it instead.

**THE SERVER COULD NOT HAVE HELPED — IT WAS GUESSING.** A queue publication is a `queue_changed` record on the wrapper's stdout, and stdout is a RING in every wrapper that owns a queue (`MAX_BUFFER = 800000`, head-dropped). A restarted server rebuilds the normalizer from exactly that buffer merged with the harness transcript — and the harness transcript never carries our queue records at all. Measured on the report's own session: a read-only replay of the restart rebuild (`CodexSessionMessages(...).raw()` → `createMessageManager('codex').convertHistoryAsync`) holds **6894 records and ZERO** `queue_changed` / `queue_op_result` / `queued_input`. So `queueState()` was `[]` and `queuePublished()` was false — and the attach payload's `queue: []` was **byte-identical to the answer for a wrapper whose queue really is empty**. The failure mode was not staleness, it was *ignorance presented as a fact*, and it is wrong in BOTH directions: this session's ghost row, and its mirror image where a wrapper really is holding a queued message and the strip shows nothing.

**AND THE CLIENT KEPT ITS OWN COPY ACROSS A RECONNECT.** `_reattach`'s SAME-EPOCH branch — the one attach path that does not rebuild the view — was written for `chatStatus` and stopped there. It dropped every other live fact the payload carries: the input queue, `queueSupported`/`queueVerbs`, turnState, inProgressTools, autoResume, outputStyle, responseStyleLive, spawnOrigin. It is reachable twice over — the delivery-stall watchdog calls `_reattach()` with the epoch already current, and an ordinary reconnect with no server restart is same-epoch by definition — and reproduced from the real module: a re-attach carrying `queue: []` leaves the ghost row on the strip and never even tells the composer. **HONEST BOUNDARY:** which of the client's reconnects that particular window took at 05:20 is not recoverable from the artifacts; what IS reproduced is that this branch keeps a strip the server no longer knows about, that a `gone` verdict paints the row red instead of removing it, and that after a restart the server cannot answer the question at all.

**THE FIX — the process that owns the fact answers for it.** ① The payload says WHICH it is: `queueKnown` (`src/ws-handler.js`'s `queueAdvert` block; `created` sends false too — the app-server's queue belongs to the THREAD, so a RESUMED thread hands a new wrapper items it never filled). ② A new stdin verb `queue-resync` — "state your queue again, out loud" — served by both wrappers that own a queue, formatted by the ADAPTER (`formatQueueResync`; the base class refuses), making **no RPC** (the wrapper re-states what it already believes, so an attach never waits on a wedged app-server) and using **`force`**, because `publishQueue` dedups on the wrapper's own fingerprint and "still empty" is precisely the sentence it cannot otherwise say. A resync that lands while the Stop sweep owns the publish is OWED and paid after it. ③ The attach ASKS, only while it does not know, behind the harness caps row AND the running wrapper's own `queueResync` advert — the second gate matters more than usual here because the ACP wrapper answers an unknown verb with a VISIBLE error card, so asking an older process would put a red notice in the chat on every attach. ④ The client: `_setQueue(items, {known})` applies the rows either way (an unknown queue IS the empty list on the wire, and one round trip of no rows beats a row nobody can act on) but suppresses the INFERENCE — on a KNOWN list it retires the `Queued` chip of every rendered bubble the queue does not list, on an unknown one it retires nothing, because we did not learn that the message left, we learned that we do not know. ⑤ A `'gone'` verdict REMOVES the row (and the local twin retires that bubble's chip): absence is a fact from the one process that owns the queue, and a `refused` marker there is permanent — the wrapper's own follow-up `refreshQueue()` cannot correct it, since by its fingerprint nothing changed. Every other refusal keeps the row: that message really is still queued. ⑥ The same-epoch re-attach applies `_applyLiveMeta(msg)`, which is carries-the-key guarded throughout, so it can only replace a fact the server just stated.

**INVARIANTS.** A value computed from records the computer may never have seen must state whether it is a FACT or a GUESS — "how fresh" is the wrong question when the failure mode is "did we ever know", and the two answers are the same bytes. The process that OWNS a fact must be able to re-state it ON DEMAND, including the EMPTY answer, and the ask is gated on that process's own advert, never on a backend id. An answer that must be repeatable cannot ride a deduping publisher (`force`, or the re-statement is swallowed exactly when it is needed); a latch that owns a publisher OWES the asks it swallowed. Absence reported by the owner of a thing is a FACT — remove the row, do not mark it; a marker for a row that should not exist is state that outlives its subject. A guard that suppresses an INFERENCE must not also suppress the DATA (the rows still apply; only the chip reconciliation is held). And a branch written for one field of a snapshot silently drops the other eight — an attach path either applies the payload's live half through the ONE shared applier or it is a whitelist waiting to drift (sixth strike in this family).

**GATES.** `scripts/test-queue-steer.mjs` ⑬ (mechanism + client, with pre-fix controls patched out of the real modules) and ⑭ (END TO END: worktree server, the REAL wrapper under dtach against a stub app-server, the ring rolled by a real long reply and measured on the buffer file, SIGKILL + reboot, a negative-control boot with the ask gated off against the SAME wrappers that corrects nothing, then the fixed boot correcting both directions, and a headless browser where the ghost strip ends empty, a real pending row comes back, and a real ✕ click's `gone` removes its row without ever painting it red — the same wire frame then replayed into a pre-fix client copy, which paints it). `scripts/test-harness-contract.mjs` holds the conformance row (ask + answer + advert, per harness) and `scripts/test-codex-p2-wrapper.mjs` ⑩ the wrapper verb against a real wrapper (including the control that a `thread/queue/changed` over an unchanged queue publishes NOTHING — the reason waiting can never fix this).

**r2 — THE MIRROR HALF: THE PAYLOAD ARRIVED BEFORE THE ANSWER IT ASKED FOR, AND WAS APPLIED AFTER IT** (round-1 verifier, reproduced first). The round-1 fix was measured on windows OPENED AFTER the reboot, where the payload is applied the instant it lands and therefore always wins. The reported shape is the other one — a window that STAYED OPEN across the restart — and it never reaches the same-epoch branch at all: a restart always changes `normEpoch` (`session._normEpoch = Date.now()`, boot-restore), so `_reattach` takes the epoch-CHANGED branch, which DEFERS the whole payload behind `Math.random() * 500` ms (2.338.0's render stagger, so N windows do not re-render their tails in one tick). The resync that this same attach asked for is answered by the wrapper in ~10 ms. So the guess landed LAST and wiped the answer it provoked: measured 12/12 in a focused harness at a 10 ms answer, and 7/10 in the verifier's own end-to-end run (it is an ordering race, right whenever the timer happened to fire first). **The damage is PERMANENT for that window, which is what makes it the ghost's mirror rather than a blink: the ask is SELF-LIMITING, so the server — which by then holds the row and hands it to the NEXT attach as a fact — never asks again.** A message that will really run then sits on the wire, in the server, and nowhere on screen: no row to steer, remove or edit.

**THE FIX IS RECENCY, and deliberately not "known beats unknown".** `_setQueue` gains `at` — WHEN THIS STATEMENT ARRIVED — and refuses any statement older than the newest one already applied (`_queueStatedAt`); `_reattach` stamps the frame with `msg.__rxTick` (`performance.now()`, monotonic, because the only question ever asked of these two numbers is which arrived first) BEFORE the branch hands it to a timer, and `_applyLiveMeta` reads it. Absent ⇒ now, which is the truth for every caller that applies a frame the moment it lands. A "known wins" rule would have been wrong: the rows the placeholder has to clear are themselves a KNOWN list — the pre-restart client's — and clearing them is the whole of round 1. Recency is the ONLY property that separates the two cases, and both directions are measured. The same comparison closes a second instance nobody reported: a steer that empties the queue INSIDE the stagger window used to be undone by the deferred payload's older KNOWN rows.

**r2 INVARIANTS.** A DEFERRED application is judged by when its INPUT ARRIVED, never by when it ran — a snapshot handed to a timer must carry its own instant, or "last executed wins" quietly makes the newest fact the loser (and every jitter/stagger/debounce in the codebase is a place where that sentence is false by default). That instant rides ON THE FRAME (the payload IS the meta) — a second out-of-band channel beside it is the whitelist-drift class. When a fix's two directions are "clear THIS list" and "keep THAT one", the flag that NAMES the two cases is usually not the property that separates them; find the one that does. A SELF-LIMITING ask turns a transient race into a PERMANENT loss — once the answer flips the flag nothing asks again, so a race inside the window that provoked the answer is not a blink and must not be reasoned about as one. And two test-methodology invariants, both of which cost a wrong-green first: a browser leg that opens FRESH windows after the restart is structurally incapable of seeing this, so the surviving-window case must keep a window OPEN across the SIGKILL; and an end-to-end leg's PREMISE must be re-established rather than inherited — the first cut of the ⑭b arms ran after several resyncs had written fresh `queue_changed` records back into that same ring, so the reboot rebuilt a normalizer that DID know, the placeholder never happened, and both arms "passed" for the wrong reason.

**r2 GATES.** `scripts/test-queue-steer.mjs` ⑬d (deterministic, no browser: the REAL `_reattach` with the stagger PINNED — an ordering race must be pinned, not sampled, or the control is a coin toss — both orderings, a pre-fix module with ONLY the guard line removed, the ghost direction still cleared in BOTH copies so the measurement is of the guard and not of a placeholder this round disabled, the rule's two directions on the ONE writer, and the known-vs-known twin) and ⑭b (END TO END on the SAME live sessions and real dtach wrappers as ⑭: exactly one window per session — a second view would answer the self-limiting ask first — the ring re-rolled by a real long reply and measured on the buffer file, the stagger pinned in the page, `_setQueue` instrumented as a pass-through recorder, and the two arms built from the same file differing by ONE line, each into its own bundle, with WHICH CODE THE PAGE IS RUNNING read off the loaded prototype rather than hoped about. Both arms record the SAME two calls in the SAME order — the wrapper's answer, then the stale placeholder — and only the outcome differs: PRE-FIX the strip ends EMPTY, FIXED the row is still there). 828 asserts. `scripts/test-auto-resume.mjs`'s carries-the-key sweep reads every `meta.<key>` in `_applyLiveMeta`, which is why `__rxTick` is spelled with its own `in meta` test even though its absent case resets nothing — and the drift pin that had bounded two lines by a BYTE BUDGET (red twice now, once per added comment) is restated as ORDER + SHAPE.

**r3 — THE SAME RACE ON THE LINE ABOVE, AND THE PRE-FIX COPY THAT DIRTIES THE TREE** (round-2 verifier, both reproduced first).

**① THE ADVERT WAS NEVER GIVEN THE RULE.** r2 handed recency to the queue ROWS and left the line one above them — the ADVERT (`queueSupported` / `queueVerbs`) — judged by "whatever ran last". That is not a lesser fact: `_queueCaps()` collapses to `NO_QUEUE_CAPS` when the flag is false, and `ChatInput._renderQueue` then does `const items = this._queueCaps.queueOps ? this._queue : []` and HIDES the strip. So a stale `queueSupported:false` reproduces r2's own outcome exactly — a real pending message the wrapper holds, present in `_queue`, rendered NOWHERE — with r2's guard intact and green beside it. Round 1 filed this as an admitted open issue with "no reproduction", on the reasoning that "the advert comes from the running wrapper's sidecar and does not change under a restart, and `_setQueueSupported` early-returns when nothing changed". That reasoning is true only for a LOCAL session with a READABLE sidecar. ws-handler names the other case where it asks (`or REMOTE — its sidecar lives on ITS machine`), and the 2.339.2 sidecar-resolution-failure class is a third; there the advert falls back to the IN-BAND publication, which a restart RESETS. Reproduced to this branch's own evidentiary standard — the REAL `wrapperCaps` answering `{inputQueue:false, queueResync:false, reason:'no-sidecar'}`, the REAL `queueAdvert` block LIFTED out of ws-handler and RUN over a REBUILT normalizer (returning `{queueSupported:false, queueVerbs:null, queueKnown:true}`), and that exact payload driven through the REAL `ChatView.prototype._reattach` on the epoch-CHANGED branch with the stagger PINNED at ~495 ms against the wrapper's own publication at 10 ms: `{ids:['qReal'], supported:false, queueOps:false, stripCalls:[{n:1,ops:true},{n:1,ops:false}]}`. The reversed order is correct in both copies, so it is an ORDERING race of exactly the family r2 closed.

**THE FIX IS THE SAME RULE FROM THE SAME STAMP, WITH ITS OWN CLOCK.** `rxAt` is computed ONCE in `_applyLiveMeta`, hoisted ABOVE the advert, and handed to both writers. `_setQueueSupported(next, verbs, { at })` keeps its OWN `_queueAdvertStatedAt` — deliberately not `_queueStatedAt`, because `_dropQueueRow` stamps the ROWS at `now` from a purely local inference, and one shared clock would let a statement about the rows censor a statement about the advert for no reason at all. **And the guard sits ABOVE the no-change early return**, which is load-bearing rather than tidy: the wrapper's answer is usually a no-CHANGE (same process, so the pre-restart view already holds `supported:true` with the same verbs), so a guard below that return never records the answer's instant and the stale payload still wins — measured `{supported:false, queueOps:false}` below vs `{supported:true, queueOps:true}` above.

**② THE BRANCH'S OWN PRE-FIX COPY COULD BLOCK THE RELEASE GATE.** ⑬d writes `src/lib/.chat-view.stale-prefix-<pid>.js` into `src/lib/` (it must be a SIBLING or its relative imports do not resolve) with NEITHER of the two protections this repo has already written down for exactly this idiom — no gitignore entry and no start-of-run PID sweep, only a `finally` unlink, which a SIGKILL does not run. Measured: `git status --porcelain` prints `?? src/lib/.chat-view.stale-prefix-99999.js`, `node scripts/ci.mjs --heavy` answers `REFUSED — this tree is DIRTY, so a marker for <sha> would not describe what ran` (exit 2) and the fast tier likewise writes no green marker; the stranded copy also sits inside the directory test-turn-truth-ui's leg ⓪ walks. Both precedents (`src/server/vs-wake-mut-*.js`, `src/server/vs-aredge-mut-*.js`) carry the explanation verbatim in .gitignore, so this one is written the same way, and ⑬d sweeps by PID LIVENESS at the top — only PIDs that are GONE, because this suite can legitimately run twice in one worktree and deleting a live run's module mid-import is worse than the litter. The sweep reports its own verdict (`removed` / `spared`) and the assert reads THAT rather than inventing a second rule.

**r3 INVARIANTS.** A fix's scope is the OUTCOME it repairs, not the field it first looked at — every fact that decides whether the same thing is rendered owes the same rule ("does this strip have controls" and "which rows does this strip have" are two halves of one user-visible outcome). Two different facts need two stamps: one clock lets either censor the other, and the writer that makes a purely local inference will do it routinely. The position of an early return is load-bearing — a recency guard placed below "return if nothing changed" fails precisely when the answer is a no-change, which is the common case, so the position itself must be MEASURED and independently mutation-red. An open issue filed as "no reproduction" must name the premise its reasoning rests on; this one was true for local sessions and false for remote, which is why it was filed instead of fixed. The two protections for writing patched copies into the source tree are a PAIR (gitignore for a SIGKILL's litter, the PID sweep against accumulation) and an assert that cannot fail is not an assert — both halves are mutation-red here. And one methodology note: a control that could not be BUILT deserves one loud red, never an `import(undefined)` — while mutation-testing this very arm, a missing needle took every remaining assert in the file with it (4 clean reds after the fix).

**r3 GATES.** `scripts/test-queue-steer.mjs` 842 — ⑬d ⑥ (the REAL `wrapperCaps` + the REAL `queueAdvert` + the REAL `_reattach`, a pre-fix copy with ONLY the advert guard removed, the reversed-order negative control, the rule's two directions, and the separate-stamps assert) plus the sweep's positive/negative controls and the gitignore assert; the two drift pins that named the old call text and the old signature are restated. Mutation-tested against the product source: removing the advert guard ⇒ 4 red (suite still completes); moving it BELOW the early return ⇒ 2 red; removing the .gitignore stanza ⇒ 1 red. Also green: test-auto-resume 221 (its carries-the-key sweep reads the rewritten `_applyLiveMeta` body), test-harness-contract 316, test-codex-p2-wrapper 401, test-attach-rebuild 55, test-acp-harness 135, test-chat-trim-guard 112, test-worktree-userchan-ui 85.
## A RESTORED SESSION WENT DARK UNTIL SOMEBODY TYPED (2026-09-09, production journal)

**THE SHAPE.** systemd stopped vibespace at 05:20:14Z for an in-app update to 2.369.77. The new server said `Found 12 existing session(s), reconnecting...` at 05:20:22Z and `Ready.` at 05:20:53Z (with a `[ws] heartbeat round tainted (tick 4s late, longest loop gap 30s)` across that boot). At 05:20:56Z it spawned the local daemon; the singleton that answered was the OLD one, so `[agentd] daemon 2.369.75 != 2.369.77 - upgrading (attempt 1/3)` streamed a new bundle into it, the daemon re-exec'd, and `device-daemon: device session routing ENABLED` landed at 05:21:02Z. The owner's claude session then showed `3 Bash - running...` for about **eight minutes** while its wrapper kept appending records to `data/session-buffers/<id>.buf` the whole time (05:20:17 ... 05:23:44 ...). The moment the owner typed, ws-handler's detector fired `Broken pty stdin detected - re-attaching dtach` (05:29:05Z; a second claude session the same way at 05:26:21Z on ITS first input) and the local re-attach flushed everything at once. A codex session that received a queue-op at 05:25:51Z did NOT log the detector - its wrapper acked - so at least some daemon channels worked and at least two did not.

**THE MECHANISM.** server.js constructs `deviceMgr` and calls `restoreSessions()` on the very next lines ("bring it up BEFORE restore so re-adopted sessions attach through it too"), so every restored session's `attachToDtach` took the daemon path *while the link was still being established* - and that path FIRE-AND-FORGOT the open. `deviceMgr.openSession()` resolves as soon as the `open-session` control frame is WRITTEN; the daemon's answer lives on `handle.ready` (`session-open` / `session-error` / the 2.271.0 T1-6 pre-ready link death), which nothing awaited, and `setupSessionPty` ran on the shim regardless. An open the daemon never completed is then a **silently dead pty**: no journal line, the session still LIVE in the sidebar, and nothing to re-attach because the shim's `onExit` never fires either. The one thing that could heal it was a detector that only runs on user INPUT - which is the exact opposite of the restart-survival promise, whose whole content is that you do not have to touch anything.

**HONEST LIMIT.** The journal carries neither a `session-error` nor an `[agentd] connection lost` in that window, so WHICH of the two silent shapes (a reply that never came, or one nobody was listening for) hit those sessions is NOT established. Both are the same dead bridge, both are covered, and the suite drives three fault shapes (refuses / never answers / answers then relays nothing) rather than claiming the one we cannot prove.

**THE FIX.** (1) **Observe the open**: race `handle.ready` against a bounded budget (5 s, against a measured 2-11 ms) and fall back to the local pty on rejection OR timeout, naming the session and the reason - never silently. (2) **Boot ordering**: take the daemon path only when the link is ALREADY live. Awaiting it would put the whole ~40 s spawn + version handshake + bundle upload + re-exec in front of every session's restore and stall the boot outright if the daemon never came up; migrating a working local pty onto the daemon afterwards is a teardown + re-attach on a bridge that is already relaying. (3) **A liveness PROBE on every attach**: `dtach -a` emits its redraw preamble to the attaching client within milliseconds even when the program inside is silent, so a healthy attach always produces a byte and a dead bridge never does - a READ, no stdin write, no wrapper cooperation, works for chat and terminal. A probe LINE was rejected on measurement: chat-wrapper acks any stdin line and then forwards an unrecognised JSON line to claude's stdin / wraps a non-JSON line as a user message, i.e. **a billed turn**, and the sessions this exists for are by construction running the previous release's wrapper. (4) **One healer**: the probe and ws-handler's input detector both call `reattachLocalPty`; the detector's "did anything come back" half now reads the same liveness STAMP instead of `session.buffer.length` (a strict subset - the terminal branch swallows the preamble outright).

**INVARIANTS.**
- **A promise nobody awaits is a failure mode nobody can see.** `openSession()` answering "the frame is written" is not the daemon answering "the session is open"; a bridge whose open was never observed has no error, no exit and no journal line - it just goes quiet, which is indistinguishable from a quiet session.
- **A guard that only runs on user input cannot guard a promise about NOT needing the user.** Restart survival means output flows with zero input; the detector that healed this was structurally unable to fire until the promise had already been broken for eight minutes.
- **Probe by READING something the transport always produces, not by WRITING something the peer must interpret.** The write had a real price (a billed turn on a wrapper too old to know the verb); the read - dtach's own redraw kickoff - costs nothing, needs no cooperation, and is the same evidence for every mode.
- **A timing verdict taken during a known event-loop stall must be confirmed after the loop breathes.** Timers run before poll, so a 30 s boot gap fires "no bytes arrived" ahead of bytes that arrived long ago; the confirm stage is one extra timer, and it is the difference between a probe and a coin flip.
- **The tie in a millisecond comparison belongs to whichever side is cheaper to be wrong about.** Healing RE-SENDS the user's keystroke, so a byte stamped in the same millisecond as `since` reads ALIVE (`>=`): a missed heal gets another chance 4.6 s later, a duplicated keystroke does not get taken back. The predicate this replaced compared a monotone byte COUNT and had no tie to lose.
- **A duck that stands in for node-pty must hold a listener SET.** `daemonPtyShim` kept ONE `onData` slot, so registering the liveness stamp first would have silently REPLACED the whole stdout consumer - the fix's own instrumentation deleting the feature it instruments.
- **A channel's bytes can legally precede its "open" reply, and a promise settles a microtask too late to catch them.** The mux drains every frame in one read synchronously; buffer at the seam and replay when the listeners exist. Measured 0/8 same-read coalescing on this box, so the regression drives the shape directly through a fake handle and says so, instead of hoping for a race it cannot cause.
- **A control that cannot fail proves nothing.** The e2e leg's "the restored session streams NOTHING" is worthless unless the fixture is still printing, so an INDEPENDENT reader of the same dtach socket asserts that first - and the fixture command itself is a handshake with a retry, because typing `while :; do ...` into an interactive login shell at a fixed offset was measured landing mid-startup, parsed as a fragment, and answered `parse error near \`do'` (which is what made every zero below it vacuous on the first run).

**Gates.** scripts/test-restore-liveness.mjs (59: the probe's premise measured on both transports; the fault matrix - healthy / refused / never answered / answered-then-silent - each with a PRE-FIX control that is a patched copy of the real module; the early-byte seam with its own single-mechanism control; the boot-ordering gate with master's behaviour in the same window; an end-to-end restore over a real self-upgrading daemon with ZERO input; the one-healer wiring pins) + test-restore-smoke / test-local-device / test-agentd-session / test-session-schema / test-architecture.
## THE CEILING ON EVERY TURN NOBODY TYPED (2026-09-08 — docs/design-account-hardening.md §4.4c / P9, owner decisions D2 / D3 / D6 / D8)

This one has **no incident report**, which is the point: §1.4 of the design lists it under *"money that leaks with no incident attached"*, so it had to be MEASURED rather than argued.

**THE SHAPE.** Producers in this tree can start a BILLED turn with no per-occurrence owner action. Each carried a local floor — auto-resume's loop breaker (3 immediate fires per session per hour), the Stop nudge's `s._lastStopNudge`, the jobs engine's 30 s per-conversation flood floor. Not one of them is a bound on MONEY: they pace ONE producer, they are per SESSION while the bill belongs to the SUBSCRIPTION (nine conversations can be parked on one), and they live in memory on an instance that restarts several times a day.

**MEASURED**, on this instance's whole transcript corpus (8,087 files). A `find ~/.claude/projects -mtime -3` sample is a TRAP and this work fell into it first: `-mtime` selects **files**, and a long-lived conversation's file carries records from two months back, so the same grep reports "550 nudges in 3 days" when the truth is 550 over two months. Corrected:

```
603 Stop-nudge mini-turns · 72 conversations · 2026-07-10 → 2026-09-09
999 forced assistant records · 536,353,861 cache-read + 513,458 output tokens
peaks: 93 in one day · 184 in a rolling 72h · 21 on ONE conversation in ONE hour
```

(this instance runs `stopNudgeStaleMinutes=0` **and** `stopNudgeCooldownMinutes=0` — every-stop mode — which is exactly why the cooldown was never the bound). Alongside it, auto-resume's 130 billed continues in one night (its own essay below).

**THE FIX.** ONE authorizer: PURE `src/spend-authorizer.js` (the closed reason set, rolling hour/day windows, two-phase authorize/note, the 80 % notice, and `overageState` — the ONE reader of a record captured since 2.289.0 that had ZERO consumers) + ORCH `src/server/spend-guard.js` (persisted `data/spend-budget.json`, one journal line per (reason, identity, why) per 5 min, one "For you" item per (identity, why) per 6 h, telemetry). It is **constructed inside the pool engine**, because every input it needs is resolved there and its identity must be the engine's OWN `fireIdentityFor` — not a fifth derivation of session→account (§2 of the design: four rival derivations produced five incidents in three days). Wired at every producer: auto-resume (before the pre-fire gate AND again after it, on the identity the continue actually lands on — the gate can re-point the link), the delivery ladder for Background Work notifications and agent messages, the Stop nudge, and the codex reset credit.

Plus the three things §1.4 named and nobody read:

- **Paid overage** now refuses unattended spend (D3b). With overage on, `utilization` stays under 1 while every token is billed pay-per-use, so `accountRemaining()` ranked the account spending real money as the one with the MOST headroom. It is also rendered in BOTH quota panels (the taskbar popup and Manage Agents, where the owner picks a switch target), from the same PURE verdict. On this instance the record is present on 6 of 7 subscriptions and reads `inUse:false` (`org_level_disabled`) — dormant here by measurement, not by hope.
- **The EDF reserve floor** (D2, 15 % weekly by default): below it a member stops being a VOLUNTARY switch/placement target while a HARD-DEAD current member may still escape onto it, and the escape SAYS which bar it landed on. `poolBlockedNotice` learned the shape too — when a bar is the only thing that emptied the candidate list it says *"every other member is held back by your spending limits"* instead of "out of quota", which prescribes waiting for a reset that has nothing to do with it.
- **Both money gates fail CLOSED**: `beforeAutoResumeFire`'s `catch { return true; }` and the identical shape at the wiring site in server.js. Two sites, so fixing either one alone would have stayed green.

**INVARIANTS.** A floor on one producer's PACING is not a bound on MONEY · the unit of a spend ceiling is the CREDENTIAL SLOT the turn will bill, never the session · a ceiling that does not survive a restart is a scheduling detail (the Stop nudge's cooldown was an unregistered in-memory field the session-schema detector could not even SEE, because it matched only `session._x =` while the live session is passed around as `s`) · TWO PHASE, because an authorization that never became a turn must not consume budget · ignorance never blocks (an unreadable credential, an absent overage record) but an UNNAMEABLE IDENTITY does, since a ceiling nobody can be charged against is not a ceiling · a refusal must LOSE NOTHING (the delivery ladder stashes and the same words ride the conversation's next turn) and must always reach the user (journal + inbox, never a second in-chat card class) · every declared reason has a producer and every producer's reason is declared, both directions asserted, because a spare slot is what the next producer slides into without anyone deciding anything · and **measure the ceiling against the real history before claiming it fixes anything**.

**HONEST BOUNDARY (measured, not asserted).** Replayed through the real guard against those 603 nudges, the shipped D6 numbers refuse **20** (seven credential slots) to **55** (all on one slot) of them: this is a BACKSTOP against bursts and loops, not a routine throttle. The nudge exit condition saves nothing on that history either — 63 of the 72 nudged conversations received exactly one nudge each — it bounds the pathological long-lived non-reporting session, and its population is an upper bound because `data/session-status.json` keeps only the live sessions' keys.

**SIDE FIX, same class.** `scripts/test-session-schema.mjs` now reads ANY receiver (with a non-session allowlist whose rows fail when they stop matching), and the nine fields it could never see are registered with owners: `_lastStopNudge`, `_childPid`, `_restoreAgentTasks`, `_preambleSeen`, `_toolsIntroSeen`, `_mgrIntroSeen`, `_groupSeenAt`, `_ctxSig`, `_groupSnap`.

**r2 — FIVE REAL DEFECTS FOUND BY AN ADVERSARIAL VERIFIER (plus a sixth this branch found while verifying the fourth), EACH REPRODUCED BEFORE IT WAS FIXED (2026-09-09).** Full text in kb-file-structure under `spend-authorizer.js / server/spend-guard.js`. In one line each:

① **The ceiling did not exist for one of its five declared producers.** The codex reset credit's gate was `if (authorizeSpend)`, and `server.js` never passed `authorizeSpend`/`noteSpend` to `usage-pool-engine.create()` — so in production it was dead code. It was invisible from two directions at once: the census's `GATED_RE` matched `spendGuard` anywhere, and the file it matched is the file that CONSTRUCTS the guard; and the leg that "proved" the gate sent a payload matching **no branch at all**, so its assertion was true for an unrelated reason. Reproduced: with the per-identity hourly ceiling at ZERO the credit is still spent. Fix = the deps are deleted and the producer asks the module-local guard directly; `GATED_RE` matches a CALL, so construction alone no longer counts as asking. *A gate that depends on a dep the one real caller does not pass is a gate nobody has; a census that reads "mentions the guard" is blind to a producer inside the file that builds it; and a leg that never reaches the path proves nothing — it needs a positive control.*

② **An oat-only subscription was refused every unattended turn, permanently, with a factually false reason.** The guard asked `memberLoginState` — the SLOT reader, deliberately oat-blind because re-pointing a symlink cannot hand a token to a running CLI — about an ACCOUNT. For B-211a `oatOnly` (spawnable, serving turns, **no credential file at all**) that answers "missing/unusable", so auto-resume, the Stop nudge, job notifications, peer messages and the reset credit were all refused `identity-cannot-serve` — and unlike the hour/day caps, that refusal never expires. Fix = `accountCredentialState` (the ACCOUNT reader `accountLoginState`, which the panels and the repair already use); the slot reader keeps its own callers. *The credential FILE is not the ACCOUNT, and here the wrong answer is permanent.*

③ **Every budget above 800 was unenforceable.** `MAX_STAMPS = 4 * 200` truncated the ledger while the settings UI offered 2000 and 10000. Measured: 1500 charged spends against a 1000/day cap counted 800 and authorized the 1501st. Fix = retention derived from the live limits (still bounded) AND the clamp lowered to the schema's own `max`, which the suite now re-derives FROM the schema. *The offered range and the enforceable range are one set.*

④ **The ladder charged a full turn for a delivery that opens no turn.** A `notification` steered into a turn already running is folded into it and bills nothing, yet it consumed the identity's hourly budget — so ~12 mid-turn Background Work notifications exhausted the ceiling on zero turns and the next auto-resume continue was refused `hour-cap`. The verifier's proposed fix ("charge only when idle") is recorded as WRONG: claude's cli-inbox and codex's `thread/queue/add` both defer a mid-turn delivery into its own billed turn. Only `notificationDelivery(caps) === 'steer'` + `kind:'notification'` + a turn in flight is free — read off the caps row, never a backend id — and because that is a PREDICTION the charge is WITHHELD, then settled by the wrapper's own `peer_message_result{mode}`. *Over-charging a money ceiling is not the conservative direction: it spends the bound that protects the expensive events on events that cost nothing.*

⑤ **The paid-overage refusal had no staleness bound.** `asOf` was captured and never consulted, so a record that stopped being refreshed refused every unattended turn indefinitely — including the continue for a session that is by definition idle and therefore produces nothing to refresh it with — and `retryAfter` printed an instant in the past. Measured here: seven live records, `asOf` ages up to **33 h on a cache refreshed 4 minutes ago**, and **0 of 7 carrying a `resetsAt`** (so `resetsAt` alone would be inert and a few-hour window would let real money through). Fix = the blocking claim expires on its own passed `resetsAt` or after 7 days, an undated record cannot claim the present, `stated`/`evidence` keep what the record said and which rung expired it, and the refusal detail carries the age. Dormant here by measurement (all seven read `inUse:false`). *Only the claim that BLOCKS needs a date — and it needs one.*

⑥ **(round 2, found while verifying ④) The settlement had no correlation rule, so a Stop made a billed turn free.** The wrapper's `peer_message_result` has six emitters and TWO of the three `ok:false` ones are about a DIFFERENT, earlier message (a queued peer item dropped by Stop, or removed from the queue, handed back so the ladder can re-stash). Round 1 consumed a pending entry on any answer, so a Stop landing between our write and the wrapper's reply shifted the entry, and the frame's REAL answer — `mode:'queued'`, a billed `thread/queue/add` — then found an empty queue and charged nothing. Reproduced end to end against the real ladder + real guard. The split is exact and is now an ASSERT over the wrapper's own emitters (every `ok:true` carries a `mode`; every `ok:false` carries `text` and none), so a mode-less answer settles nothing and the third `ok:false` (the wrapper's own delivery failure) simply strands its frame, charged nothing. Two more halves came out of the same reading: the settle queue tracks EVERY frame written on that rung, charged or not — the wrapper answers in write order, so a queue holding only the predicted-free frames hands a charged peer message's answer to the notification's entry — and a frame stranded past 120 s (>4× the wrapper's own 30 s `thread/queue/add` budget) is DROPPED rather than left to absorb the next message's answer. *A correlation-free settlement must derive its match rule from the PRODUCER's own emitters — and assert that derivation, because it is a claim about someone else's code — and it must track every frame it wrote, or the ordering it relies on is not the ordering it has.*

Each of the six was mutation-tested against the PRODUCT source: reverting a fix turns 1–9 asserts red, restoring it returns ALL PASS. Round 2 also added the controls that would have caught its own blind spots — a permanent negative control for a file that constructs the guard but never asks it (with the retired regex beside it, proving it would have passed), a "the census can read the schema" assert so the schema⇄clamp check cannot go vacuous, and three positive controls on the steer exception so it can never widen silently.

**r3 — THREE MORE (2026-09-09, each reproduced first).** ⑦ **The `Spending` settings category was never rendered, so every ceiling this work added was unreachable.** `SETTINGS_CATEGORIES` is not an ordering hint — it IS `SettingsUI._renderContent`'s render loop (group by `schema.category`, then render by iterating THAT array), so an unlisted category is grouped and dropped. Replaying that loop over the real schema: **118 settings, 108 rendered, 10 NEVER RENDERED** — the seven `spend.*`/`pool.*` rows (every money ceiling, the overage consent, the EDF reserve floor) plus three `opencode.*` rows invisible since they shipped; a search for `unattended` matched 5 rows and rendered 0 sections ⇒ "No settings match your search." There is no second write path (`serverSetting()` reads the sparse `data/settings.json`, which only this UI writes — measured live: 30 keys, none `spend.*`). Live consequences: `pool.reserveFloorPct` defaulted to 15 and changed every switch/placement decision with no way to set it back to 0, and the guard's inbox item, the pool's blocked notice and the overage chip all pointed at "Settings → Spending", a place that could not be opened. Fix = two array entries + **`test-architecture` §44**, a build-time census asserting the CONSEQUENCE (every row reaches a section), pinning the UI's own coupling, failing an EMPTY listed category too, with both controls. ⑧ **The pre-fire money gate still failed OPEN at auto-resume's own layer.** P8 says "at EVERY layer"; §1.4 named two and r2 fixed those two — but `src/server/auto-resume.js` has a third (`catch { gate = true; }` + `.catch(() => deliver())`), and r2 verified the fix by GREPPING the other two files. Driven against the real module: gate `false` ⇒ 0 continues, `true` ⇒ 1 (both controls), **THROWS ⇒ 1 billed continue, REJECTED PROMISE ⇒ 1**. Masked in production today, but the mask lives in two other files and one natural refactor (making the wiring lambda `async`) removes both halves. Fix = one named `gateFailedClosed()` (journal + `spend-gate-error` telemetry), `gate = false`, and a rejection handler that never delivers — as `then`'s SECOND argument, because master's trailing `.catch(() => deliver())` also caught a throw from `deliver()` itself (`save()` on a full disk) and answered it by delivering AGAIN; the delivery's own failure now gets its own line and no retry. The arm survives, exactly as for a `false` verdict. ⑨ **The producer census was FILE-granular**, so a new producer in any already-gated file was invisible — ①'s class one level up, in the four files where the next producer is most likely to land. Reproduced by replaying the shipped body over a scratch file with one gated and one ungated producer: verdict `GATED`, unwired 0. Fix = a per-SITE verdict where "gated" means a gate call is still IN SCOPE ABOVE the site, scope decided by INDENTATION rather than a byte window (measured: the ladder's own gate sits 6,285 chars above its last rung, so any window wide enough for the real code would call the scratch file gated too and the control would be theatre), with whole-line comments blanked (auto-resume's JSDoc `@param deps.authorizeSpend (…)` satisfied the call-shaped regex — r2's rule was true of the regex and false of its input), calls to locally gated helpers counted as anchors, and `ALLOW` keyed by (file, primitive). r3 invariants=**an ordered list a renderer ITERATES is a switch, not documentation**; **a defence-in-depth rule must be verified AT the layer it names — a source regex on two other files proves nothing about the third**; **a census answers per SITE or it answers about the file that happens to contain the site, and its scope rule must be measured against the real code before the control it enables is worth anything.**


**r4 — TWO MORE (2026-09-09, each reproduced first), plus one this branch found by mutation-testing its OWN new leg.** ⑩ **The delivery ladder AUTHORIZED an unattended turn against one credential slot and DEBITED a different one**, so the per-identity ceiling could be bypassed indefinitely on the authorized account while an account nobody asked started refusing its own legitimate unattended turns. `deliverToConversation` resolves `identity` as `session ? null : {…}` — null on the local-session branch by design — and handed that null to the charge, so `spend-guard.note()` ran `identityOf(session)` a SECOND time, at charge time. The rpc rung makes that window wide on purpose: it WITHHOLDS the charge and defers it to `settleRpcDelivery` up to 120 s later, while a codex session's slot follows the pool DEFAULT (the engine's per-session pass skips codex) which the 30 s pool timer re-decides. Measured on the real guard + real ladder with a 1/hour cap and `identityOf` flipping A→B inside that window: **three deliveries authorized against a cap of one**, ledger `{B:3}`, A debited 0 — its ceiling never bound, and B now refuses its own auto-resume continue. The PURE verdict already carried the resolved slot; throwing it away was the whole defect. Fixed at all four pairs (the other three escaped only because of how their code happens to be arranged today, not because the question has one answer), and the guard now COUNTS the shape in production (`chargesUnhinted`, journalled + `spend-charge-unhinted` telemetry) so the rule has a measurement rather than a grep. ⑪ **The design's §1.4 row was marked CLOSED while one of the three fields it names still had zero readers** — `grep -rn spendControlReached src/` returned only its writer. Not a money leak (codex REJECTS those requests rather than billing them), but the leak the row itself describes is real for it: nothing marks a window spent, so both panels drew friendly donuts and `accountRemaining()` ranked an account that cannot serve a single request as the one with the most headroom. The record was the defect, so both halves were fixed: PURE `spendControlState` beside `overageState` (three-state, dated on the SAME `OVERAGE_STALE_MS`, refusing under its own name in the `identity-cannot-serve` class — `spend.allowOverageTurns` deliberately does not unlock it, because that setting consents to SPENDING, not to being refused by the vendor), both panels rendering it from that same verdict, and a census that walks the fields the row CLAIMS and requires the row to name each one's reader. ⑫ **(found by mutation-testing r4's own new leg) An assert that could not fail**: §5c's peer-message leg put its awaiting transport behind a `findPeer` that answered null, so the ladder ran to its charge synchronously and the leg stayed GREEN with the product fix reverted while the four legs around it went red; an order probe printed `authorize → CHARGE → (the driver re-points)` with the transport never called. The rung is now reached, with a control that says so before the money is read.

**r4 invariants**: **CHARGE WHAT YOU AUTHORIZED** — the authorizer is entitled to resolve the slot and its verdict CARRIES it so the charge can name the same one; a charge that re-derives a mutable identity asks the question twice, and two-phase accounting makes that window as wide as the settlement. **A rule that holds "because nothing awaits in between" is a property of an arrangement of code, not of a design** — state it at every pair and give it a production counter, not a grep. **A record that says a thing is finished must name every field it claims**, because that record is what the next round reads. **Only the claim that BLOCKS needs a date**, and a second staleness window for one physical fact is a twin. **An assert that cannot fail is not an assert — including the ones this round wrote**; mutation-test your own new legs, not just the product code they were written for.

**r5 — TWO MORE (2026-09-09, each reproduced first).** ⑬ **The persisted ledger was PRUNED AT BOOT with the DEFAULT limits, so every restart forgave spend an owner-raised cap had counted.** Retention is a function of the caps (r2's ③) — but at the one place it is applied to the ledger on disk, the caps are NOT KNOWABLE: the guard is constructed inside `usage-pool-engine.create()` at `server.js:434`, and `serverSetting` reads `persistenceRouter.readSettings`, which `setupPersistence()` only assigns **318 lines later** (`server.js:752` → `src/routes/persistence.js:600`). Measured against the real router and the real PURE module at that instant: `router.readSettings = undefined`, `serverSetting('spend.unattendedPerIdentityDay') = undefined`, `budgetLimits() = {12, 60, 200, 80}` — the DEFAULTS — so `stampCap` was **264** rather than the live one. Reproduced end to end on the real guard: a ledger holding **500** same-day stamps for one slot with the owner's day cap at 500 (the schema offers 2000, and 10000 for the instance row) came back holding **264**, and the guard then authorized **236 more unattended turns today instead of 0**, on every boot — and this instance restarts several times a day. Pruning can only DROP, so the later `note()` calls (which do use live limits) can never restore them; this is r2's ③ verbatim — *a ceiling the settings UI offers and the ledger cannot count is not a ceiling* — re-opened through the boot path, and the suite missed it because §1 and §3 both build the guard with a WORKING `serverSetting`. Fix = **prune wide at load**: a named `A.LOAD_RETENTION` (`CAP_MAX` + headroom = 10064 stamps, the widest any offerable cap can count), because dropping a stamp is irreversible and forgives money while keeping one costs bounded memory, and the first `note()` re-prunes with the live limits. ⑭ **The delivery ladder authorized SYNCHRONOUSLY and charged only after an awaited transport, so the per-identity hourly ceiling was bypassed by the in-flight concurrency count.** Nothing between the two re-read the ledger and nothing reserved, so N deliveries dispatched in one pass all read the same pre-charge counts. Reachable in production: `src/jobs.js:210` fires `deliverToConversation` fire-and-forget from loops over `this.jobs.values()` (run-completion at :511 and the boot catch-up pass at :593, which can notify several missed jobs in one tick), and `/api/agent/msg/send` is a concurrent HTTP route into the same ladder — while the 30 s `_notifyRate` floor is keyed PER CONVERSATION and therefore does not serialise across them. Measured with the real guard + the real ladder, cap 2/hour, five DISTINCT conversations resolving to ONE credential slot (the design's own "nine conversations can sit on one subscription") dispatched in a single synchronous pass: **5 delivered, 0 refused, 5 turns charged against a cap of 2**; sequentially on an identical world: 2. `chargesUnhinted` stays 0 throughout, so r4's counter is structurally blind to this shape. The other three producers are NOT affected — each runs authorize → act → note with no await between — which is exactly why the fix is in the AUTHORIZER rather than in the ladder. Fix = **an authorization HOLDS what it authorized**: `authorize()` returns `v.hold`, an in-memory reservation that `pendingCounts` adds to the caps exactly like a charge, until `note()` converts it, `release()` gives it back, or it times out at `RESERVE_TTL_MS` (3 min > the ladder's own 120 s settle window, ≪ the shortest window the caps count). A hold is deliberately NOT a stamp: the stash rung — the ladder's normal fallback for an unreachable conversation — opens no turn, and the ladder gives its hold back at **ONE** release point (a `finally`, because enumerating the nine exits plus two throwing catches is the fragility that `finally` removes), while a withheld rpc frame is released when the wrapper CONFIRMS the steer and charged when it says the frame became a turn. Holds are not persisted, on purpose: a restart kills the in-flight deliveries they stand for. Two shapes fell out of the fix and are pinned: **a PROBE must not hold** (auto-resume asks the ceiling twice per fire — before its pre-fire gate and after it, on the slot the continue lands on — and with the first call reserving, the second refused its own request and NOTHING ever fired at cap 1/hour), and **the ladder's rungs must stay in the same function as its gate** (round 1 of this fix split them into a helper, and §2's per-site census went red on all four rungs — correctly: a helper the gate merely calls is exactly the shape r3's census exists to catch).

**r5 invariants**: **do not make an irreversible decision with limits nobody can answer yet** — a module constructed before the settings reader exists must prune to the WIDEST retention its own clamp allows, because the two error directions are not symmetric (over-retention is bounded memory, under-retention is money). **Two-phase accounting is only as strong as the window between the phases**: an authorization that has not yet become a charge must still bind the ceiling, or the bound is per-caller-scheduling rather than per-identity. **A hold is not a charge** — it is released by the exits that opened no turn, and there must be exactly ONE of those. **A "may I?" probe must not consume the thing it is asking about.** **A counter that measures one shape (`chargesUnhinted`) is blind to the next one** — the new shape got its own (`holdsExpired`/`holdsOpen`, asserted 0 across every producer). **When the census goes red at a refactor, read it before working around it**: it was describing the invariant, not the diff.

**GATE**: `scripts/test-spend-paths.mjs` (261, fast tier) + `test-architecture` §44 (inside `npm run build`) — §2 is a grep-derived census over `git ls-files -- src server.js data/bin` minus `src/lib/`, PER SITE since r3, it PRINTS every site with the anchor that gated it, its (file, primitive) allowlist rows die when they stop matching, and its controls are a synthetic producer in a scratch tree (the same producer, wired, is the positive one), a file that constructs the guard without asking it, a mixed file holding one gated and one ungated producer, and a gate that exists only in a JSDoc block. The rest drives the REAL modules — including, since r3, all four arms of auto-resume's own pre-fire gate against a patched copy of the real module. Since r4 it also drives §5c (the charge names the slot the authorization resolved, on all four producers, with a patched copy of the real ladder carrying r3's shipped line — byte-compared against `4ceba626` — as the negative control) and §7b (a census over the fields the design's §1.4 row CLAIMS, with r3's own verdict text as its control). Since r5: §1's reservation rules, §3d (the production boot ordering pinned on the real files, then the ledger driven through it), §5d (five concurrent deliveries on one slot, with the sequential run as the positive control) and §5e (the probe, the withheld steer and the hold census) — each with a negative control that is a PATCHED COPY of the real module, and all six mutations (boot prune / no hold / no release / probing holds / no give-back at settle / no give-back when stranded) verified red before restoring.

## GATE SUITES CLAIMED MACHINE-GLOBAL NAMES (2.369.76, heavy tier RED on 40ad936d)

**THE SHAPE.** The heavy tier for 2.369.75's commit went red on test-chat-paging: first `npx esbuild` (unminified rebuild in the suite's worktree) failed with no stderr, then the retry's `git worktree add --detach /tmp/vs-chatpage-smoke HEAD` failed with status 128. Neither is a product failure. The suite's worktree path, chrome profile dir (`/tmp/vs-chatpage-chrome`) and ports (3990/9340) were fixed strings; a verifier agent — told, as every verifier is, to run the gate suites from its own checkout — ran the same suite in the same minute. Its `git worktree remove --force` deleted the heavy run's tree mid-build, and its `worktree add` recreated the path before the heavy run's retry got there. The heavy tier's machine lock (2026-09-07 round 2) serialises heavy RUNS; it cannot cover a process that never takes it.

**THE CENSUS.** 28 suites carried the shapes: 23 with quoted `/tmp/vs-<name>` literals, 17 with `const PORT = NNNN`, three suites sharing 3989/9339, four sharing 3991/9341, five using `3951 + (process.pid % 20)` (a 1-in-20 collision, and two of them share one range). 2.369.46 had fixed exactly one (test-chat-e2e :3995) inline — the third strike is where an idiom becomes shared: `scripts/scratch.mjs` (`scratch(name)` = `/tmp/vs-<name>-<pid>`; `freePorts(n)` holds every listener open until all are chosen, because closing each before the next `listen(0)` can hand the same port back). It is deliberately not a `test-*.mjs` — the tier census would demand a tier for it.

**THE GUARD.** test-ci-gate §6 already had `machineGlobalFixtures` (the 2026-09-07 detector: a literal that IS a port, a `/tmp` literal that reaches the filesystem) but scanned FAST-tier suites only — the tier that collided was the other one. It now scans both tiers, and pins that the scanned set contains the heavy tier and the suite that collided. The detector's own fixtures (scripts/fixtures/machine-global-shapes/) are the controls; test-ci-gate's `/tmp/vs-ack-smoke` assertion is a control READING a fixture, not a claim, and the rewrite reverted its accidental hit.

**INVARIANTS.** A lock covers the processes that take it and nothing else — when the colliders are other agents, the fix is a name nobody shares, not a bigger lock. A suite's scratch names are part of its correctness on a shared box. The second inline fix of a class is the signal to make the idiom shared and the detector tier-wide.

## A MEMBER BECAME USABLE AND NOBODY NOTICED (2026-09-08 02:00–02:31, from this instance's own journal)

**THE SHAPE.** Pool "全部" hit 0 % on every member. The owner added a subscription in the middle of it. Eight fable conversations were parked on the newcomer **while it had no usage reading at all**, re-armed for a reset **eight hours away**, and released only when the owner typed a prompt into one of them seven minutes later.

**THE JOURNAL, VERBATIM** (`journalctl --user -u vibespace`, account ids resolved from `data/accounts.json`; the newcomer `sub-080ce98c7dca` was named "39AI Max" that night and is "UCI Max" now):

```
02:00:02 [notice] Pool "全部": no member can serve it — spent: 5h 0%
                  (still available: 7d 62%, Fable 34%)…
02:03:29 data/accounts.json: the record is created
02:18:27 data/subs/<new>/.vibespace-login-status.json: {"state":"success"}
02:18:33 [pool] per-session switch <pool>/<sid>: <old> → <new> (fam=fable, from 0%)   ×8
02:18:33 [notice] Pool "全部": conversation "<name>" moved to 39AI Max
                  (its fable quota was at 0%)                                          ×8
02:18:37 [auto-resume] sess-5-…: armed for 2026-09-08T10:20:00.000Z
           (re-armed at fire: 39AI Max: no usage data | Fish Max: 5h 0% < 10% |
            ProblemFactory Max: Fable 0% < 5% | Personal Max: 5h 0% < 10% |
            B-Stack Max: 7d 1% < 5% | PandyMax: 7d 0% < 5%)
02:18:40 [auto-cli] quota refresh <new>: failed (drift 0pt)
02:22:10 [auto-resume] <opus sid>: pool switched to UCI Max — continued automatically
02:25:01 [auto-resume] sess-5-…: disarmed (user sent a prompt)
02:31:43 [auto-cli] quota refresh <new>: ok (drift 4pt)
```

The opus conversations at 02:22:10 are **not** the feature working: one of their turns ended, and a turn end is the one event that already re-evaluates the pool. The fable ones had nothing to end.

**THE POOL NEVER GOT TO DECIDE.** It re-evaluates on turn ends, on streamed usage records, and on a 30 s timer for auto pools. A conversation that is ARMED on exhaustion produces neither of the first two, and the timer only re-ranks the pool — it never re-examines who is waiting. So "a member became usable" reached nothing:

* **(A) A LOGIN SUCCEEDING IS ALSO A QUOTA EVENT, and nothing treated it as one.** Nothing read the newcomer's usage when its login landed. The auto-cli loop is the only other reader, and its one attempt at 02:18:40 failed, which doubled its backoff from 5 to 10 minutes — the next read was 02:31:43, thirteen minutes later, exactly as `5*60e3 * 2^1` predicts.
* **(B) A MANUAL REFRESH THAT REVEALS A USABLE MEMBER TOLD THE POOL NOTHING.** `/api/usage/refresh` writes the reading and answers `{success:true}`. It re-runs no pool decision and does not touch the conversations armed on exhaustion. The owner clicked it, watched the number change, and the conversations kept waiting for 10:20Z.
* …and at 02:31:43, when the auto-cli read finally succeeded, **that changed nothing either** — a cache write is not an event anybody subscribes to.

**THE FIX IS ONE EDGE, because a third producer of a fresh reading would otherwise forget it too** — and in this incident the third producer *had* forgotten it. `engine.onMemberReadingFresh(memberId, why)` (src/server/usage-pool-engine.js) does exactly two things:

1. **RE-DECIDE the pools this member belongs to** — `maybePoolAutoSwitchForPool(id, {force:true})`. `force` drops that pool's 10 s event-KICK gate for this one call: the gate throttles per-RECORD kicks, and a member's first reading is not a kick, it is the fact the whole decision was missing. The 180 s dwell belt is untouched, so this can no more oscillate than any other evaluation.
2. **RE-EXAMINE the conversations that are WAITING** — every armed session on this member, or on one of its pools, goes through auto-resume's own `fireNow`.

**Each half is load-bearing for a DIFFERENT shape, and finding that out took mutation testing.** The first spelling of the regression armed the conversations in both legs — and half ① then looked decorative, because deleting it changed nothing: the PRE-FIRE GATE (`beforeAutoResumeFire`) runs `maybePoolAutoSwitch` itself, so half ② was doing half ①'s work as a side effect of the spending machinery. The honest split is:

* **half ② is the incident's own shape**: the conversations were ALREADY parked on the newcomer since 02:18:33, so the pool re-decide moves nobody — the per-session pass `continue`s long before it reaches its own `fireNow`, and a switch-driven nudge is structurally unreachable.
* **half ① is the shape where nobody is armed**: two live conversations on a spent member that have not hit the wall yet. Nothing fires, so the gate never runs, and only half ① can move them.

`engine.onMemberLoginSuccess(memberId, why)` is the login half: ONE read through the EXISTING caps-routed rung (`probeQuotaForKey` → claude's `refreshViaCliPanel`, i.e. `claude -p /usage` with the official binary making the fetch; codex's app-server twin), then the same edge. **No new vendor surface**, and it is the human's own login action that triggers it — the same class as the ⟳ button, not a timer.

**IT DOES NOT SPEND BY ITSELF.** Every release goes through `fireNow → attemptFire`, i.e. the 2026-09-07 loop breaker (same-identity quarantine, 3/hour cap, backoff) AND the pre-fire gate that re-verdicts the target and vetoes the turn if it is still blocked. There is no bypass, by construction: this module only says "look again".

**THE PRODUCERS, all three.** `src/server/account-usage-routes.js` hangs the login half on the SAME transition detection the expiry sweep already uses — `loginFingerprint` read off THE ANSWER THE ROUTE ALREADY HOLDS, never a second credential reader — because all three finalize routes are polled every 3 s for up to 5 minutes and a side effect hung on the raw answer would spawn ~100 CLI panels per login. It gets its OWN latch (`lastWakeFingerprint`), deliberately not the sweep's: two of those routes call `sweepLoginExpiry` UNCONDITIONALLY for a documented reason, so sharing one map would couple "we already swept" to "we already read the usage" and disabling either would silently disable the other. All five login-success exits wake **the record the login LANDED in** (`'moved'` ⇒ `movedTo`; the auto-merge exit ⇒ the SURVIVOR, since waking the throwaway spawns a panel read for a record about to stop existing and leaves the one the pool holds unread). The call is fire-and-forget: a login must never wait on, or fail because of, a quota read. `src/usage-routes.js` takes the edge on all three LOCAL rungs of `/api/usage/refresh` (session / cli-panel / bare token); the `host` branch takes none of them — a remote machine's pool is not ours to decide for. And the auto-cli loop in server.js takes it on a successful refresh, which is what makes "a third producer cannot forget it" a true sentence rather than an aspiration. The edge is deliberately NOT buried inside `refreshViaCliPanel`: the pre-fire SPEND GATE calls that function too, and a gate that releases conversations as a side effect of asking a question is not a gate.

**COMPOSITION, NOT A TWIN.** `readingForeignForWake()` asks the window-fingerprint work that landed in 2.369.73 (inc-mts8a8mr-ulmm) whether the stored snapshot is really this member's. That work ships TWO rules and this asks the SECOND on purpose: ① the LAG SHADOW is about a reading ARRIVING on a session after that session's link moved — it needs a session, a re-point and an age, and the producers already applied it before the snapshot reached disk; ② the WINDOW IDENTITY GUARD (`decideReadingTarget`) is the question a wake actually has. It is reached through the same `establishedWindows()` + `windowGroupOf` the write-time guard uses, so there is ONE predicate — and it is deliberately not called "shadowed", because that word already names rule ①. READ-ONLY: `guardReadingTarget` is the WRITE path and it archives; a wake may only decline to act. A brand-new member has no established window for anything to contradict, so the incident's own case still acts.

**"NO READING" IS A REFUSAL TO ACT, NEVER A VERDICT OF 0 %.** The edge names its refusals: `no-reading`, `stale-reading` (older than 10 min), `foreign-reading`, `member-not-usable`, `wake-floor`. It is idempotent and rate-floored per member (20 s), and **the floor is stamped only when the wake ACTED** — a declined wake costs one file read, and stamping it would let a producer that called a millisecond before its own cache write eat the wake the real one needed.

**NOT READY IS NOT FAILED.** The auto-cli loop skipped on the roster's `loggedIn`, which is `parseAuth`'s boolean: TRUE the moment an accessToken *string* exists, whatever its expiry. A credential file whose access AND refresh tokens have both expired therefore reads as "logged in", the panel spawn cannot possibly answer, and every failure doubles a backoff that never had a chance — the satisfiable gap `src/login-state.js` exists to close. The loop now asks `autoCliReady`, which is `memberLoginState`, the credential-FILE reader the pool's slot validation already uses. The FILE reader is the right one and not the account-level `accountLoginState`: this rung is `refreshViaCliPanel`, which DELETES `CLAUDE_CODE_OAUTH_TOKEN` from the child's env and points `CLAUDE_SECURESTORAGE_CONFIG_DIR` at the account's own dir, so a long-lived token cannot serve it. Finally, the login-edge read counts as one of the loop's OWN attempts (`lastMemberReadAt`), because both schedulers spawn the SAME panel through the SAME rung and two attempt clocks race.

**HONEST BOUNDARIES — what this fix is NOT claimed to explain.**
* The brief for this work said the newcomer's `.credentials.json` was written at 02:24, six minutes AFTER the auto-cli failure. **That is not verifiable today and the available evidence points the other way.** The file's mtime is now 12:19 (the pool re-mints that symlink and utimes-bumps the target on every re-point — which is exactly why `credsTokenSig` exists instead of an mtime check), and the loop's own `!a.loggedIn` gate means an accessToken string must already have been on disk at 02:18:40 or no attempt would have been made at all. The sibling files agree: `.claude.json` and `.vibespace-login-status.json` are both stamped 02:18:27.
* So `autoCliReady` is **not** claimed to be what would have prevented that particular failure count. It closes an adjacent, reachable hole (the doubly-expired file), and the "member has no credentials yet" half was already covered — `parseAuth` answers `loggedIn:false` with no file, and the journal shows no auto-cli attempt for the newcomer before its login landed. Both halves are measured in the suite so the distinction cannot rot into a claim.
* What IS reproducible, and what the regression actually replays, is the load-bearing half: **after the reading existed, nothing re-drove the pool and nothing looked at the conversations that were waiting.**

**INVARIANTS.**
* **A member becoming usable is an EVENT, not a state someone will eventually poll.** The pool re-evaluates on turn ends and streamed records; a conversation that is armed produces neither, so it can wait out a reset that stopped being relevant hours ago.
* **Every producer of a fresh reading takes the SAME edge** — and "a third producer cannot forget it" is only true if you go and wire the third producer that already exists.
* **A wake has two halves and each needs its own control, in the shape where only it can answer.** If a control survives deleting the mechanism it names, the mechanism was being done by something else and the assertion was decorative.
* **A polled route may not carry an unbounded side effect** — gate on the credential fingerprint of the answer the route already has, never on a second read of the file; and give a second side effect its own latch, or disabling one silently disables the other.
* **Two belts get two controls.** A leg that leaves the second belt standing proves nothing about the first (the fingerprint gate looked protected until the engine's own 5-minute floor was wound back).
* **When two schedulers spawn the same child through the same rung they must share ONE attempt clock.**
* **A boolean that is TRUE for a state it was never asked about is a gate that spends money** (`parseAuth.loggedIn` vs `loginState.usable`).
* **"No reading" is a refusal to act, never a verdict of 0 %** — and a refusal names itself.
* **A rate floor is stamped when you ACT, not when you are asked.**
* **Do not reuse an established word for a different rule.** Rule ① is the shadow; rule ② is the guard. One name for two things is how this subsystem grows twins.

**r2 — THE WAKE COULD SPEND ON A MEMBER IT HAD JUST PROVEN SPENT (adversarial verifier; all three reproduced before they were fixed).**

**(a) HALF ② FIRED CONVERSATIONS THE POOL COULD NOT MOVE.** Round 1's half ② filtered on POOL MEMBERSHIP and fired. That is right only in the shape it was written for. In every shape where the pool structurally CANNOT move a conversation onto the newcomer, the continue went out anyway — billed to the member the conversation was already stuck on — and the in-chat card named THAT member as recovered, which is this batch's own invariant ("a card explaining a billed turn must name the thing that caused it") inverted. Three real production states reach it:

* a **MANUAL pool** (`auto:false`): `maybePoolAutoSwitchForPool` returns at the top, so half ① is a no-op and the link never moves;
* a **REMOTE conversation** (`s.host`): the per-session pass skips it BY DESIGN, same outcome;
* a **COLD pool** (`hot:false`) where half ① did move the link: the pool broadcast `pool-auto-switched` so the CLIENT restarts the conversation, and master's ONLY other `fireNow` call site guards that same spend with `if (a.hot)` and says why. Round 1 had no `hot` gate at all.

MEASURED on the real engine + real AccountManager pool + real per-session symlinks + real auto-resume, armed by the REAL rejection producer (`recordRateLimitEvent` + `noteTurnEnd`): with a manual pool, master's own mechanisms (`maybePoolAutoSwitchForPool` + three auto-resume ticks) deliver **0** continues and the link stays on the spent member; one call to `onMemberReadingFresh` delivers **1**, onto a member whose cache reads `utilization: 1, status: "limited"`, with the card `账号 Fish Max 已恢复可用，已自动继续这个任务。` The remote shape is identical. **The pre-fire gate cannot catch any of them**: `quotaVerdictFor` answers a POOLED scope with "any member usable" — true, and beside the point.

**THE FIX ASKS THE FACT THIS MODULE ALREADY NAMES.** `fireIdentityFor` is "the identity a CONTINUE fired into this session would land on" — it is auto-resume's own `fireIdentity` dep, so `attemptFire` re-resolves the very same fact. Half ② asks it TWICE: once BEFORE half ① is allowed to move anything, once after. A session is fired only when the continue lands on `memberId`, would have landed there before half ① ran too, and (if its scope is a pool) that pool is HOT. Read only after the re-decide, "the wake moved this session onto the new member" is indistinguishable from "it was already parked there" — and the first of those is a spend half ① has already accounted for: it fires a hot pool itself and restarts a cold one through the client, so half ② adding a second continue there is a double spend, not a rescue. Half ②'s contract collapses to one sentence: **it serves only the conversations nothing could move** — which is exactly the shape round 1's own essay says it exists for.

**WHAT THE COLD RULE COSTS, DELIBERATELY.** A cold pool's conversation is released by the TIMED path at its armed reset rather than early. That is latency; firing anyway is money, spent on whichever member the CLI still holds — the restart is a CLIENT action, so with no browser connected nothing restarts, the link says one member and the running CLI still holds another's credentials, and nothing in this process knows whether a client obeyed a broadcast.

**(b) THE CARD'S ORDERING WAS NOT BYTE-IDENTICAL, CONTRARY TO THE ROUND-1 COMMIT.** Round 1 hoisted the `/^account usable again/` arm ABOVE `kind === 'now'` to make room for `cause`. That silently changed a PRE-EXISTING pair which the engine itself writes both halves of: the near-arm at `scheduleWallProbe` (`armIfEnabled(id, session, Date.now() + 45000, 'account usable again')`) followed by a real pool switch firing with `kind:'now'` (the per-session pass and the pool pass). Measured against `master:src/server/auto-resume.js`: master says `账号池已切换到 X`, round 1 said `账号 X 已恢复可用` — a real pool switch described as a recovery. No suite covered the pair, so the claim was untested as well as untrue. `cause` may only REFINE master's precedence: **`moved` still outranks it** (if the pre-fire gate re-pointed us, the continue lands on a member the wake never spoke about, so "the pool switched to X" is the true sentence), and below `cause` master's order is restored verbatim. All eight reachable pre-existing shapes are now byte-compared against master's own copy, loaded through `git show` under the sanitized git environment. The pair itself is pinned where the other `continueNoticeFor` pairs live — test-auto-resume-loop §3, beside the DRIFT guard that already names both engine strings (that suite is now 191, and the round-1 ordering makes it red on its own).

**HONEST BOUNDARY, IN THE SOURCE AND IN THE SUITE.** Deleting the before/after snapshot changes no outcome TODAY — auto-resume refuses the second fire on its own (`session._arFiring` is raised for the whole gate, and a delivered continue deletes the arm); the mutant delivers the same one continue with the same card. It stays because half ②'s money-safety must not be OWED to another module's private in-flight flag, and because the skip is what makes half ②'s contract testable rather than a sentence in a comment. §8f drives that interlock directly so the claim is not decorative.

**r2 INVARIANTS.**

* **Pool membership is not a spend authorization.** A wake that RELEASES a conversation must prove the continue lands ON the member that became usable — the pooled verdict ("any member usable") is structurally unable to answer that question.
* **A guard master states with a reason at its own spend site is owed at every new spend site.** `if (a.hot)` existed, with its reason written next to it, at the only other `fireNow` call site.
* **Ask the fact the module already NAMES for that question, at both instants that matter.** "Where would a continue land right now" already had a function; the bug was asking a different question (membership) and asking it once.
* **A claim of byte-identity is a MEASUREMENT, not a reading of the diff.** The pair round 1 broke is written by the engine 900 lines away from the function it changed.
* **A redundant guard must say it is redundant, and hand the suite the mechanism that really protects.** Otherwise the next reader either deletes it as dead or trusts it for a safety it does not provide.
* **A declared-but-never-passed test control is an unmeasured shape.** `mkWorld`'s `auto` parameter was documented as a real control in round 1 and no leg ever passed it; all three shapes above live behind exactly such controls.

**r2 GATE.** §8 of the same suite, on the same real engine/pool/symlinks/auto-resume: (a) manual pool, (b) remote conversation, (c) cold pool — each asserting 0 continues, the NAMED skip reason, and no "recovered" card; (d) the incident itself still releases both conversations; (e) the login shape where half ① moves the link and fires it ITSELF, asserting exactly ONE continue worded as the switch it was; (f) the `_arFiring` interlock driven directly, both ways; (g) the card's precedence table plus the eight-shape byte comparison with master. **The negative controls are PATCHED COPIES of the real engine** written beside it (they must be siblings or their relative requires do not resolve), each replacement's hit count asserted, unlinked on exit, swept by PID liveness at start, and gitignored so a SIGKILL can never leave the tree dirty for the release gate. Each of the three guards, removed on its own, goes red; so does round 1's `continueNoticeFor` ordering.

**r3 (adversarial verifier, round 3 — both reproduced on the real engine/routes before anything was changed).**

**(a) HOTNESS IS A CAPABILITY VERDICT, AND HALF ② WAS READING THE USER'S CHECKBOX.** r2's cold gate asked `!pa.hot`. The engine does not: the pool-level path computes `const hot = !!a.hot && poolCaps.hotSwitch === 'verified'` and spends only `if (hot)`, and `capsOf('codex').hotSwitch` is `'impossible'` (the 2026-08-24 experiment — CODEX_HOME is canonicalized at startup and the tokens live in process memory), so a codex pool ALWAYS cold-restarts. So a codex pool carrying `hot:true` got, from the capability gate, exactly the billed continue the pool-level path had just refused — r2's own failure mode arriving through a door r2 did not check. MEASURED on the real engine + real AccountManager codex pool + real auto-resume, armed by the real codex rejection producer (`recordCodexQuotaSignal({type:'task_failed', codexErrorInfo:'usage_limit_reached'})`), in the incident's shape: `wake = {acted:true, fired:["sess-9-…"], skipped:[]}`, the continue delivered, the card `账号 ChatGPT New 已恢复可用，已自动继续这个任务。`, and `cold-restart broadcasts: 1` — the same conversation was simultaneously told to restart through the client, while the continue lands in a CLI that cannot re-read credentials and therefore bills the member it was spawned on: the exhausted one. **REACHABILITY IS PERSISTED STATE, NOT A HYPOTHESIS**: until 2026-09-05 manage-agents rendered the "Hot switch (no restart)" row inside a bare `if (a?.pooled && !selectedHost)` with no backend gate, and `PATCH /api/accounts/pool/:id` still writes `hot: req.body?.hot` with no caps gate. The flag is inert on every other path (the engine gates on caps; the client now hides the row), so a user who set it saw nothing happen and had no reason to unset it. Note the per-session pass's own `if (a.hot)` is unreachable for codex (it `break`s for non-planC backends), so the caps-gated pool-level guard is the ONLY `fireNow` guard a codex pool can reach — and that is the one half ② disagreed with. Fix: ask the same predicate the engine asks, `!(pa.hot && capsOf(pa.backend).hotSwitch === 'verified')`.

**(b) THE AUTO-MERGE EXIT SPAWNED A PANEL FOR THE RECORD IT WAS ABOUT TO DELETE.** `/api/accounts/subscription/:id/finalize` woke `req.params.id` unconditionally, above the merge branch — and `onMemberLoginSuccess` → `probeQuotaForKey` → `refreshViaCliPanel` runs its roster check AND its `execFile` synchronously inside the route (the first real await is inside the `new Promise` executor), so the throwaway is still in the roster at spawn time; `mergeSubscription` then deletes it. MEASURED through the real routes factory + real AccountManager: `CLI PANEL SPAWNS: 2 ["sub-<throwaway>","sub-<survivor>"]`, `merged=true`, `throwaway still exists: false`, and `usage-cache/<deleted-id>.json` left on disk. Nothing in accounts.js or the routes removes usage-cache entries when an account goes away (zero grep hits for `usage-cache` in either file) and `establishedWindows()` reads every `.json` in that directory with no roster filter, so the litter is not inert. The code contradicted **its own comment three lines away** ("THE SURVIVOR is the record the pool holds … the throwaway above is about to stop existing") — a comment where a gate was needed. Fix: hoist the `dup` lookup (a pure roster read, and the only thing that knows whether this record will still exist in a moment) ABOVE the wake and gate the pre-merge wake on `!dupOf`; the merge branch already wakes the SURVIVOR, and the catch — above BOTH the rethrow and the `merge-account-live` answer — wakes this record, because **a deferred wake must not become a dropped one**: the fold failing is a reachable, documented exit where the fresh record is still the one holding the login. BELT: `refreshViaCliPanel` now asks the roster again AT THE WRITE and returns false, so a record removed mid-probe (the merge, or an ordinary "remove this account") can never leave a cache file or a window sidecar behind.

**WHY (b) WENT UNMEASURED.** §6 EXIT 4 asserted only `w.probes.includes(w.PANDY)` — never that the throwaway was NOT probed. Worse, it reached for an email SETTER this `AccountManager` does not have and then took its own SKIP branch, so the entire auto-merge exit was unmeasured. The route's dup lookup reads `x.email || (a name containing @)`, so `am.rename` is all the leg needed; it now asserts the merge really happened before asserting anything about it.

**r3 INVARIANTS.**

* **A capability is a VERDICT; a settings flag is a WISH.** When the module already computes the verdict for its own spend, every other spend site must ask the same expression — reading the raw flag turns a capability gate into a bypass of the guard beside it. A flag that is inert everywhere else is *more* dangerous, not less: nobody has a reason to turn it off.
* **A side effect that spends money must be ordered after the decision that says who will exist.** The merge lookup was a pure read available at any point; the wake was placed before it only because that is where the sweep already was.
* **A comment that states the invariant three lines from the violation is a bug report.** The code said "the throwaway is about to stop existing" and then read its usage.
* **A gate added to skip work must name every exit that still needs the work.** Gating on `dup` without the catch-side wake would have dropped the login edge on the blocked-merge exit entirely.
* **A guard on a window between a check and a write belongs at the WRITE.** Ordering fixes the one caller you know about; re-asking the roster before writing fixes the class (account removal is an ordinary user action too).
* **An assert that only says who WAS woken cannot see who else was.** Positive-only assertions about a set are how a second, wrong element hides.
* **A leg that SKIPs on a capability it guessed at is an unmeasured shape** — the same lesson as r2's declared-but-never-passed control, one layer down: this one silently skipped because it probed for an API instead of using the one the code under test actually uses.

**r3 GATE.** §8c′ drives a REAL codex pool (real `createCodexSubscription` + `createPool({backend:'codex'})` + `updatePool({hot:true})`), armed by the real codex rejection producer, with the session ANSWERING the `rpc-rate-limits` probe through the wrapper's own error reply so the 20 s waiter settles instead of racing the wake (without that, the wall probe re-arms mid-gate and the continue is dropped for an UNRELATED reason — a leg that would go green on the wrong fact); it asserts 0 continues, the named `cold pool` skip, no second restart broadcast and no "recovered" card, with a first assert that the two backends really disagree about hotness so the leg cannot be vacuous. Its negative control is a patched engine copy carrying ONLY the raw-flag spelling — it delivers the continue and writes the card. §6 EXIT 4 now takes the merge for real and asserts the survivor is woken, the throwaway is NOT (`probes.length === 1`), and no usage-cache entry is keyed to the deleted record; its negative control is a patched copy of the ROUTES module with the pre-fix ordering, which produces two spawns and the phantom cache file. §6 EXIT 4b drives the blocked-merge exit through a real `merge-account-live` refusal and asserts the edge is still taken, on the record that kept the login. §9 drives `refreshViaCliPanel` through the real `setupUsage` factory with a FAKE `claude` on `CLAUDE_CMD` (zero vendor cost, real `execFile` → real parse → real writes): a live account still gets its cache file AND its window sidecar (the belt is a refusal, not an off-switch), an account removed while the panel runs gets neither, and the negative control — a patched copy of `src/usage-routes.js` with the roster re-check removed — leaves both.


**GATE.** `scripts/test-new-member-wake.mjs` (115, fast tier) — the incident replayed on the REAL engine + REAL AccountManager pool with real per-session symlinks + REAL auto-resume + the real rejection producer, and the REAL `account-usage-routes` factory for the login exits. §1a pins the shape (parked on an unread member, so nothing can switch); §1b is master reproduced (three ticks pass, still waiting for the far reset — then one call to the edge continues both); §1c switches each half off in the shape where only it can answer; §2 refuses no/stale/foreign readings and a member that is itself spent; §3 pins both floors and the polled-route fingerprint gate *with the engine floor wound back*; §4 measures the `loggedIn` gap against the real `parseAuth`; §5 proves the quarantine and the 3/hour cap still hold against readings that keep arriving; §6 drives all five login exits through the real route factory plus the auto-cli producer; §7 pins §ban-safety. Sixteen mutations of the product source were run against it — every one goes red, and the three that survived the first draft are why §1c and §3 are written the way they are. §8 is r2 (above), §8c′ + §6 EXIT 4/4b + §9 are r3 (above). Suite total 115.

## A LOW-USAGE ACCOUNT SUDDENLY JUMPED TO 93 % (inc-mts8a8mr-ulmm, 2026-09-08 05:27Z — the reading was one request older than the link)

**THE REPORT.** Owner: *"a low-usage account suddenly jumped to 93% 7d"*. The account (Personal Max) had been at 11–13 % all day, on its own `/usage` panel readings, before and after.

**WHAT ACTUALLY HAPPENED**, reconstructed entirely from this instance's own stores and journal:

| time (UTC) | event | evidence |
|---|---|---|
| 05:27:00 | the pool moves 9 sessions PandyMax → Personal Max | `slot-transitions.jsonl`: 9 `per-session-switch` rows + 1 `pool-switch` row; journal `[pool] auto-switch … (exhausted, from 2% left)` |
| 05:27:16 | a response to a request made **39 s earlier with PandyMax's token** arrives; `readingSlotFor` had resolved the turn's slot at its first reading = Personal | Personal Max's own anchor stream 05:27:39: `sevenDay {u:0.93, resetsAt:1789030800}` — **PandyMax's window** — beside a `fiveHour` and a `scopedWeekly` that are Personal's own |
| 05:28:38 | the pool reads Personal as "5 % left" and moves everything to **Fish Max** | journal `[pool] auto-switch … (exhausted, from 5% left)`; 10 more ledger rows |
| 05:29:39 | Personal's own `/usage` panel restores it to 13 % | its own `on-demand` anchor |
| 05:31:40 | the pool moves everything back to Personal | 10 more ledger rows |

**Two false pool switches inside 98 seconds, caused by our own write.** 2.369.68's slot rule is right and stays — a reading belongs to the credential slot the process was handed, pinned for the turn — but it cannot see that the slot moves while requests are IN FLIGHT. A re-point reaches the CLI on its NEXT request, never the response already on the wire; the pin is set by the turn's FIRST reading, and the first reading of that turn was a straggler.

**THE EVIDENCE THE RESPONSE CARRIES ABOUT ITSELF.** A rate-limit payload names the WINDOW its numbers are counted in, and a weekly window is an ACCOUNT FINGERPRINT: measured over the whole anchor corpus of this instance (7 live subscriptions, 30 days, 6 634 anchors carrying a weekly reset), **every identity has exactly ONE weekly reset phase** (`resetsAt mod 604800`), stable across the entire period, and **the seven are all distinct**. A roll adds exactly one week so the phase survives it; the ±60 s wobble between the `/usage` panel's and the event stream's spelling of the SAME window is normal. So the record can be asked which credentials produced it, and that outranks the slot, the turn pin and the OTel observation — all of which describe what we BELIEVE the process is reading.

**THE FIX** — one PURE rule (`src/reading-lag.js`, imports nothing), two clauses, wired at every server-side value producer through `readingSlotFor` / `guardReadingTarget`, and mirrored VERBATIM into the shipped statusline tool:
- ① **the LAG SHADOW**: after a re-point at T, a reading whose window matches the PREVIOUS slot's established window and NOT the new one is filed on the PREVIOUS slot; the first reading that matches the NEW slot ends the shadow and it stays ended; when neither window decides (two members genuinely sharing a phase, or no established window yet) it falls through to the r3 statusline clause — *the link moved but the numbers did not*; BOUNDED at 10 min so a session that never speaks again cannot pin a slot forever. It is applied BEFORE the turn pin is set, so a pin created by a shadowed reading pins the PREVIOUS slot.
- ② **the WINDOW IDENTITY GUARD**: a reading whose window contradicts the target member's own established window is NEVER written there — re-filed when EXACTLY ONE account's window matches (identity groups collapsed), else ARCHIVED to `data/archive/readings-window-mismatch.ndjson` with the reason, the target's own window and its age, + telemetry + one journal line per session per verdict transition. **No evidence ⇒ no refusal.**
- **the established window is STAMPED AT THE WRITE** (`cache.ownWindow`) by the ONE producer whose key and whose credential dir are the same decision — `refreshViaCliPanel` — and is never read back out of `sevenDay.resetsAt`, which is precisely the field a mis-filed reading overwrites; `captureRateLimitEvent` and `markLimitBanner` preserve it explicitly, because both base their write on the identity group's FRESHEST sibling.

**THE SECOND BRANCH, and it is two different things.** Fish Max's own anchor stream carries `rate_limit_event` anchors 04:11:03Z–05:03:04Z with PandyMax's window and 7d 0.89–0.91, while the pool had moved the sessions to PandyMax at 04:10:02Z. Version telemetry dates that window to **2.369.67 — before the readings-by-slot fix (2.369.68) shipped**, so those rows are residue of the refuted `orgVerifiedKey` routing (the sessions' OTel-observed org was Fish Max; the journal logged `session … observed on Fish Max while linked to PandyMax` every 10 min through it). The repair that shipped with that fix could not reach them: its only evidence is *the member's own credential file DATED its death*, which covered ONE member here (`[migrate] readings-by-slot: {"members":1,…}`) — everything mis-filed **between two logged-in accounts** survived it, which is exactly the "silent half" its own header names. **But the same shape recurs under the CURRENT code**: 06:11:39Z and 06:12:39Z put PandyMax's 0.95 on Fish Max again, at 2.369.69, with `fetchedAt 06:10:46Z` — 20 s AFTER the link had moved to PandyMax, i.e. the numbers were freshly and correctly PandyMax's. That producer was a session whose TURN PIN had been set before three re-points and never cleared. The shadow cannot speak there (no recent re-point explains it); the **window guard** is what catches it, and it re-points the turn pin to the proven account so the rest of the turn follows the evidence.

**THE DATA REPAIR** (`repairByWindow`, migration `2026-09-refile-readings-by-window`) uses the evidence the earlier one lacked. Measured on a COPY of this instance's stores: 7 identities established from their own panel readings, **464 anchors re-filed, 0 archived, 39 pairs voided, 7 caches seeded with `ownWindow`, 0 foreign caches**, `rates.json` archived + dropped, 7046 records in and 7046 out (archive-never-destroy, verified by line count), idempotent, 51 ms. **460 of the 464 predate 2.369.68**; the 4 that postdate it are the incident's own two rows plus the 06:11/06:12 pair. PandyMax's own 95 % is REAL (EDF put the sessions on it; its own panel readings climbed 0.60→0.95 over 12 h) and is untouched — the repair moves a row only when its window names exactly one live account, and archives with a reason otherwise.

**INVARIANTS.**
- **A reading is evidence about itself.** When a record names the window its numbers are counted in, that outranks every piece of bookkeeping we keep about which credentials a process holds: bookkeeping describes intent, the window describes what answered.
- **A rolling identity must be compared by the part that does not roll.** The weekly PHASE, not the absolute `resetsAt` — otherwise every legitimate window roll reads as a foreign account.
- **A fingerprint must name the thing it claims did not change.** The r3 clause says *the numbers did not move*; fingerprinting only the window would have shadowed nearly every reading following a re-point between two members sharing a phase. Caught by this branch's own shared-phase leg, in this branch's code, before it shipped.
- **The fact a guard reads may never be the fact a bad write produces.** Deriving the established window from `sevenDay.resetsAt` would let one mis-filed reading redefine the account and immunise every later one.
- **No evidence ⇒ no refusal**, and every refusal is archived with the reason, the target's own window and its age — so "the account's window genuinely moved" stays distinguishable from "this reading was mis-filed".
- **A write that moves money is not silent.** The shadow and the guard each speak once per session per verdict transition; the incident's whole diagnosis was "which account did this number go to, and why", and the journal had nothing to say about the write that caused it.
- **A repair whose only evidence is "this member's login died" can only ever see the visible half.** The silent half — two logged-in accounts — needs evidence carried by the data itself.

**R2 — TWO REAL DEFECTS IN THE FIX ITSELF (adversarial verifier; both reproduced before being believed).** They are the same species: a fact was trusted further than the measurement supports it.

**(a) ONE STATUSLINE RENDER DISARMED THE WHOLE FIX.** `ownWindow` was a FIELD of the usage-cache snapshot — and that object is REBUILT WHOLESALE by every reading producer. The highest-frequency one is the SHIPPED statusline hook (`data/bin/vibespace-usage`, once per 8 s per account, our own write, entirely legitimate): its `out` literal preserves `scopedWeekly`, `scopedFetchedAt`, the org identity and `spend` **one field at a time** — the 2.70.0 "Fable-vanishing" class its own comment names — and simply never listed the window. The two writers that WERE taught to preserve it (`captureRateLimitEvent`, `markLimitBanner`) are the low-frequency ones; the highest-frequency one clobbered. Reproduced twice: a real run of the shipped file over a cache carrying the window (`BEFORE …sevenDay:1789142400 → AFTER undefined`, keys `fiveHour,sevenDay,scopedWeekly,orgUuid,orgName,spend,overallStatus,fetchedAt,source`), and then end to end against this branch's OWN §14 harness (real engine + real AccountManager pool + real per-session symlinks): one legitimate render per member with each member's own numbers and own window took the established windows `[true,true,true] → [false,false,false]`, `establishedWindows()` went empty, `decideReadingTarget` degraded to *"no established window to contradict"*, the lagging response landed on the low-usage account again, and **the SECOND FALSE SWITCH happened** — byte-for-byte this branch's own `stampWindows:false` negative control. Every gate stayed green because no leg ran a statusline render before §14. On this instance 32 anchors already carry `source:'passive'`, so the path is live; the same clobber shape exists at the bare-token ⟳ (`JSON.stringify(u)`) and at the codex snapshot writer.
*The fix is not a fifth entry in a hand-written preserve list* — that list is what failed, five times, on this one file. **The window moved OUT of the object every reading producer rewrites**, into `data/usage-cache/.window-<key>` (`windowSidecarName`, no `.json` so every usage-cache scanner keeps ignoring it — the `.slot-<session_id>` sidecar beside it already relies on that rule). `refreshViaCliPanel` writes it; `establishedWindows()` reads it, KEYED OFF THE CACHE FILES so a sidecar left by a removed account can never become a re-file target; the two preserve blocks are **deleted**, and the ⟳ and codex writers need no change at all — which is the sign the class is closed rather than patched.

**(b) THE FIVE-HOUR RUNG WAS A NEW MISATTRIBUTION OF EXACTLY THE CLASS THE GUARD EXISTS TO PREVENT.** `compareWindows` let `fiveHour` alone produce an identity verdict, so every `five_hour` `rate_limit_event` (which carries NO weekly component) reached that rung. The corpus refutes the premise — measured read-only over `data/usage-anchors`:
- a SAME-account 5 h reset sits >120 s from that account's own still-future panel-stamped value on **7.2 % (33/456)** of Fish Max's readings and **7.4 % (47/639)** of Personal Max's — a `'differ'` about its rightful owner (real pair: stamped `1788780540`, read `1788786600`, Δ 6060 s);
- DIFFERENT identities carry IDENTICAL 5 h resets constantly — **47 distinct colliding values**, because they snap to the clock (`resetsAt mod 1800` peaks at 0/600/1140/1200/1740) — an `'agree'` with a stranger;
- scanning the real 30-day corpus for the exact guard state (target's stamped 5 h still future, reading >120 s away, EXACTLY ONE other account matching) yields **50 moments** where a legitimate reading would have been re-filed onto a stranger.

Proven at the pure rule with those numbers (`refile → bstack`, and `archive` when no sibling lines up) and END TO END through the real producer: PandyMax kept its stale 0.2 while B-Stack Max received 0.55 under a confident journal line. **A five-hour window identifies a TIME, not an account** — so the weekly half is now the ONLY identity evidence, the rung is deleted from both spellings, and a reading with no weekly component short-circuits `decideReadingTarget` with the honest reason `reading states no weekly window` (the target usually HAS an established window, so *"no established window to contradict"* would be a false sentence in the journal and the archive). Identity is therefore decided WITHOUT a clock — `nowSec` and `absNear` are gone, because comparing weekly PHASES survives a roll by construction. The suite had pinned the refuted premise as correct (§13 asserted a 5 h mismatch is `'differ'`) and no leg drove the guard with a `five_hour` event, which is why this shipped green.

**R2 INVARIANTS.**
- **A fact only ONE producer may state does not belong in the object all the others rewrite.** Preserving it is a promise every current AND future writer has to keep; moving it is a property. The list had already lost `scopedWeekly`, the org identity, `spend` and `corroborated` — the window was the fifth, and it was the one the money decisions read.
- **When a class recurs, close it structurally instead of extending the list.** The measure of the fix is that two of the three clobbering writers needed no change.
- **Ask what a field IDENTIFIES, and measure it before believing it.** "The window names the account" is true of the weekly half and false of the five-hour half; the same sentence covering both turned a guard into a source of the harm it was written to stop.
- **A guard with no evidence must write, not move.** `'unknown'` is the answer for a reading that names no account, and it must never be spelled like `'differ'` (which re-files) or like a match (which steals).
- **A reason string is an assertion about the system.** It reaches the journal AND the archive record, and it is the whole diagnosis a human gets.
- **Mutation-check each mechanism separately, and anchor the fixture to the condition the retired code needed.** The first version of these legs used the corpus's raw seconds — now in the PAST — so the restored rung (which only fires while the target's stamped 5 h is still FUTURE) could not redden them: a leg that cannot fail is not a leg.

**R2 REGRESSIONS.** §13 states the measured facts (5 h drift + collisions) instead of the refuted premise, with the retired rung as a permanent negative control; the two mechanisms (the deleted rung, and the no-weekly short-circuit) get one leg each because the short-circuit fires BEFORE `compareWindows` is consulted, so neither alone reddens the engine-level leg. §15 gains the CLOBBER leg — the incident replayed AFTER one real statusline render per member, driven through the SHIPPED FILE'S OWN BYTES, with a negative control that is a PATCHED COPY of that same shipped file with only the storage decision reverted (the patch is asserted to hit, and it reproduces the whole incident including the second false switch) — and the FIVE-HOUR leg through the real producer, with a positive control proving the WEEKLY guard is still armed in the same world. Mutation-checked: restoring the 5 h rung reddens 2, widening the short-circuit reddens 2, both together (the true pre-fix rule) redden 7 including the engine-level leg, and reverting the storage decision reddens 19. Re-measured on a COPY of this instance's stores: 464 anchors re-filed / 0 archived / 39 pairs voided / **7 window sidecars seeded, 0 caches carrying an in-snapshot window**, 7059 records in and 7059 out, idempotent, 52 ms. test-readings-attribution 283.


**Suite**: test-readings-attribution 259 — §13 the pure rule (weekly phase, roll invariance, jitter, three-state compare, the shadow on the incident's own timing, the bound, the r3 fallback, the guard's write/refile/archive/ambiguous outcomes, identity-group collapsing) + the byte-identical mirror in `data/bin/vibespace-usage` driven functionally through the SHIPPED bytes; §14 the incident replayed against the REAL engine + REAL pool + real symlinks (the lagging response lands on the burned member, the "low-usage account" keeps its own number, the anchor follows, **ZERO second switch** from a fresh engine's own pool evaluation) with the PRE-FIX negative control that reproduces the second switch end to end, the shared-phase leg that only the shadow can answer (+ its own negative control), and the STALE TURN PIN branch; §15 the guard in the live producers (archive with the reason, inert without evidence, rejections deliberately un-guarded, `ownWindow` surviving both session-attributed writers, source pins); §16 the repair on a fixture shaped like this instance's stores. Mutation-checked: disabling the shadow reddens 2 legs (its journal line and the shared-phase case), disabling the guard reddens 7 (the stale-pin branch and the archive branch).

**ROUND 3 (adversarial verifier — the fix's own clock was wrong in two places; both reproduced with the real engine + the real pool + real symlinks + the SHIPPED tool's own bytes, three-arm matrix).** ① `decideLagShadow` short-circuited on `shadow-expired` BEFORE consulting either window, and the shipped statusline had no re-point instant to give it — it passed the age of its last OBSERVATION (`.slot-<id>`'s `at`, i.e. the last render under the OLD slot, which necessarily precedes the re-point that ended it and therefore systematically OVERSTATES). One ordinary render of an IDLE pooled TERMINAL after the pool moved LINK→SPARE — 30 min since its redraw, link re-pointed one second earlier — answered `shadow-expired` and filed LINK's 0.96 (carrying LINK's own weekly window) onto SPARE with `source:'passive'`, and the pool made **the SECOND FALSE SWITCH** (→ FISH, "exhausted, from 4 % left"). Observation age 5 min ⇒ untouched, so the mechanism is isolated to the age bound; and MASTER's tool (9d6e6180, whose r3 rule has no clock at all) ⇒ untouched, so this was a **REGRESSION vs master**, unconditional once the tool has written an `at` once — also reproduced with NO window sidecars (a fresh instance): master keeps 0.11, the branch filed 0.93/0.96. **Fix**: the clock bound moved BELOW both window rungs in BOTH spellings (a clock is a PROXY for "were those credentials still in play"; the window is the ANSWER, and a proxy may never overrule what it stands in for — the bound now only decides where nothing else can, which is the only place it was ever load-bearing), `repointAgeMs == null` is UNKNOWN and does not expire, and the statusline dates the RE-POINT from the link the pool re-MINTS (`repointPoolSymlink` = symlink-to-temp + rename ⇒ `lstat(link).mtimeMs` is that instant; measured 7200 s untouched / 0.001 s after), keeping `st.at` only as forensics. The server side is unchanged in reachable states: `lagShadowFor` already BREAKS its ledger scan at `SHADOW_MS`. ② **The 8×/minute producer was the one value producer with no window guard at all** — the shadow only speaks when a re-point explains the reading, so every other path wrote whatever `accountKey()` resolved; `decideReadingTarget` moved INSIDE the mirror sentinels and the statusline now re-files onto the one matching account or REFUSES, recording the reason in `.window-refused.ndjson` beside the cache (no `.json`, per-day shard on overflow). A shipped single file has no identity-group table, so an org-merged login reads as TWO matches and is refused rather than re-filed — the conservative direction. **INVARIANTS**: a clock is a proxy and the evidence outranks the proxy, so a time bound belongs BELOW every rung that can actually answer; **unknown must never be spelled like expired**; an "honest upper bound" is only honest if the test it feeds is safe in that direction (the comment correctly said `st.at` is never earlier than the re-point — which is exactly why it must not be compared against an expiry); ask what a value DATES, and prefer the first-class record we already own (the symlink we mint ourselves) over a proxy; **a rule shared by two spellings must be shared WHOLE** ("no shipped caller has needed it yet" is not a tier boundary); and **two barriers against one harm need one control each, read where each barrier speaks** — with the guard in place, reverting only the ordering moves no money, so §15 reads the ordering control at the `.slot-<id>` state file and judges the money with a PRE-FIX copy that reverts both. Mutation-checked: the clock-first ordering in both spellings reddens 5, the observation age reddens 3, removing the statusline guard reddens 4. Migration re-measured on a COPY of this instance's stores (live data): 7 identities / 7 receivers, **475 anchors re-filed, 0 archived, 39 pairs voided, 7 window sidecars seeded, 0 caches carrying an in-snapshot window, 2 foreign cache snapshots archived and rebuilt**, 7082 records in and 7082 out, idempotent, 51 ms. **test-readings-attribution 310.**

**ROUND 4 (adversarial verifier — one HIGH defect, measured on a COPY of this instance's stores before it was believed): THE REPAIR WAS MANUFACTURING THE MISATTRIBUTION IT EXISTS TO CLEAN UP.** The by-window migration established each account's window as `{sevenDay, fiveHour:null, scoped:{}}` — it threw the model-scoped half of the evidence away — so it decided every anchor on its 7-day bucket alone. That is a blind check, not a smaller one, because **an anchor is a snapshot of a usage-cache FILE and that file has two writers**: the statusline rewrites `{5h, 7d}` from its own payload and PRESERVES whatever `scopedWeekly` the file already held (it has no scoped buckets of its own — the 2.70.0 Fable-vanishing rule its own comment names). A reading that landed on the wrong key before the window guard existed therefore leaves a record that is ITSELF A MIX: another account's 7d on top of **this stream's own** Fable bucket. Measured here: **444** such records against **32** whose halves are both foreign and **4466** clean ones — so **443 of the 476** re-files moved complete with a Fable bucket whose weekly phase the destination contradicts, and `repairCachesByWindow` then rebuilt a cache from one of them (`best` = the newest record with an agreeing 7d, `refiledFrom` naming the other member) and copied its value, its `resetsAt` and its `scopedFetchedAt` over the target's own. `src/account-pool-auto.js` reads `cache.scopedWeekly` in `accountRemaining` / `weeklyDeadline` / `bucketRems`, so that is a misattributed reading that can flip a pool switch — the 2.305.0 scoped-bucket class, created by the repair.

**The fix moves PER BUCKET, never wholesale.** ① each identity now establishes its **scoped** phases from its OWN on-demand rows under the same two thresholds as the 7d (measurably the same phase as its 7d on all seven identities here, every cluster inside the ±120 s tolerance, all seven distinct); ② `repairAnchorsByWindow` asks `decideReadingTarget` one bucket at a time — the PRIMARY half is `{5h, 7d}` (one payload, one producer, one credential, so the 5h travels with the 7d and never alone), the record follows its primary half and carries ONLY the buckets that agree with where it lands, every other bucket is archived with its own reason and the archive line keeps the ORIGINAL WHOLE RECORD; a record with no agreeing bucket is archived, not moved; ③ `repairCachesByWindow` picks `best` only among records ALL of whose judgeable weekly buckets agree, `_cacheFromAnchor` filters on the account's window as a second independent barrier, and — because a per-bucket move can leave the newest own-window record with no scoped bucket — it now preserve-merges the previous snapshot's scoped bucket (through the same filter) instead of replacing it, exactly as all three live writers do; ④ a cache whose 7d agrees but whose scoped bucket does not is stripped in place, a shape the rebuild branch structurally cannot reach. **The live writer path needs no change and the reason is pinned**: every `guardReadingTarget` call is handed a producer PAYLOAD, a `rate_limit_event` names exactly one bucket, and a `get_usage`/panel/codex snapshot is one credential's single response — the mix is manufactured downstream by the per-key preserve-merge, and each preserve reads the TARGET's own previous list after the guard has moved `key`.

**R4 INVARIANTS.**
- **A record assembled by more than one writer must be judged by more than one verdict** — ask what wrote each field before deciding the record as a unit.
- **Establishing "the account's window" from a subset of the evidence is not a weaker check, it is a blind one**, and a repair that cannot see half of a record will move that half onto a stranger.
- **A reason string answers the question its line is about.** `decideReadingTarget`'s reason answers "does this bucket belong to the stream it sits in"; quoting it on a drop produced 443 archive lines reading *"window matches the target"* about a bucket that was dropped.
- **"No evidence ⇒ no refusal" governs REFUSING TO WRITE, not MOVING.** An unjudgeable bucket may stay where it is; carrying it to another account is a positive claim (705 scoped entries here state no `resetsAt`, and `bucketRemaining` still reads their utilization).
- **A rebuild is a WRITE and inherits the preserve rule every other writer has** — replacing the snapshot from an anchor that states no scoped bucket DELETES the model cap the pool decides on.
- **Two barriers against one harm need one control each, read where each barrier speaks**: after a full pass the anchor half has already cleaned every stream, so the `best` picker is driven ALONE over an uncleaned store rather than left as a guard nobody can delete and redden.

**R4 MEASUREMENT (both repairs over ONE frozen copy of this instance's stores).** r3: 476 re-filed whole, **443 rows land carrying a Fable bucket the destination's own phase contradicts**. r4: 476 re-filed (32 whole, 444 partial), 444 buckets archived with their own reason, 0 records archived, 41 pairs voided, 7 identities established and all 7 able to receive, 7 sidecars seeded WITH their scoped phases, 1 foreign cache archived + rebuilt keeping its own Fable bucket, rates dropped, 7096 records in and 7096 out, **census: 0 contradicting weekly buckets in any stream and 0 in any cache**, idempotent, 52 ms. Suite: **test-readings-attribution 340** (§16b, whose negative control is a PATCHED COPY of the real module with exactly this round's five decisions reverted — each replacement asserted to hit — and whose money assertion is the real `decidePoolSwitch` moving off the account under the fix and staying on it under r3). Mutation-checked, each mechanism alone: 8 / 12 / 3 / 4 / 2 / 3 red; restored, green.

## THE "HISTORY IS HIDDEN" ROW NEVER SHOWED ON THE ONE LIST THAT NEEDED IT (2.369.70, caught by the Actions mirror)

The sidebar's `_render()` early-returns "No sessions" when the filtered list is empty (and no hosts / no search / not the tasks tab). The OpenCode "stopped conversations are hidden — its background service is off" row is rendered by the workbench builder, i.e. AFTER that return, so an instance whose only conversations are the hidden OpenCode ones — the exact user the row exists for, whose list is empty BECAUSE the history is hidden — never saw the way back. The plugin suite's leg 4 was green on the dev machine for the wrong reason (unrelated claude sessions keep the list non-empty) and red on the GitHub Actions runner (no sessions at all) for two pushes.

Fix: the empty branch calls `_renderServiceHintRows(sessions)` before returning (nothing wipes `listEl` after it, which is why the "workbench-only" pin now allows exactly that one extra call, immediately before the `return`). Regression: test-opencode-plugin leg 4b forces `_allSessions = []` + no hosts and asserts "No sessions" + the row + its Enable action; A/B with the fix reverted goes 3 red.

Invariants: **an early-return branch is a render path** — every row that is "gated on broadcast state" must be reachable from every branch that can paint the list, or the branch that happens on the poorest instance is the one that lies; **a leg that passes because of the developer's own data is environment-dependent** — the Actions mirror is the instance with nothing, read it after every push (memory: push后要看CI镜像).

## QUOTA READINGS WERE KEYED BY THE ORG THE CLI STARTED ON (2026-09-07, owner: "为啥不是从根源上解决假数据而是加 guardrail")

The visible symptom was absurd: a member whose Claude login had been **wiped on 2026-09-02 22:55 PDT** kept receiving limit-banners and Fable-bucket readings for five more days. The first instinct — "make logged-out members reject readings" — was rejected by the owner as a guardrail over a lie, and he was right: the logged-out member is only the case where the lie becomes *visible*.

**The mechanism.** Every VALUE reading — statusline ingest, `rate_limit_event` capture, limit-banner marks, the auto-cli panel result's attribution, the anchors they feed, and the OTel "corrections" — was keyed by `orgVerifiedKey()` = the OTel-observed org. That org is the identity the CLI cached in its config dir **at SPAWN**. The credentials a *running* CLI reads can be re-pointed later — 2.1.257's `rpe()` re-reads `.credentials.json` on an mtime change, which is exactly what the pool's hot switch does and exactly why the re-point bumps that mtime. So after any hot switch a session's readings were filed under the account it was SPAWNED on, for the rest of its life. 2.369.66 had already fixed the BLOCKING half with this same correction (a rejection is a fact about the credential SLOT); this is the VALUE half, and it had been left standing on the refuted premise the previous day.

**The silent half is the dangerous one.** Between two logged-in members nothing looks wrong at all: a Fish-billed session spawned under B-Stack files Fish's numbers under B-Stack. Both panels move, both anchor streams learn, and the estimator's rates are trained on the mixture. Dry-run over a COPY of this instance's stores: **3 foreign cache snapshots, 248 foreign anchors across 3 identity streams (145/102/1), 44 foreign attribution entries** — and a second member wiped at 12:39 that same day received a reading at 19:24, i.e. the defect was still producing damage while the fix was being written.

**The fix is that there is now ONE resolver.** `readingSlotFor(session, at)` — the validated credential slot, PINNED FOR THE TURN and cleared by `noteTurnEnd` — is the twin of `rejectionSlotFor` and deliberately shares `wallSlotFor`'s body, because a reading and a rejection are facts about the same thing. `resolveUsageKey` resolves `sessionBillingMember` too, and **`sessionReadingMember` and `orgVerifiedKey` are DELETED**: keeping two functions for one question was the bug's home, and the one that was wrong owned every value on the instance. The observation survives as `corroborateReading()` — logged once per session per 10min, telemetered, and stamped on the write as `corroborated:true|false`, a label that moves nothing.

**Why the pin is a TURN.** Every producer calls `maybePoolAutoSwitch` the moment it writes, so one turn's ~20 readings would otherwise be split across the members *our own re-points* moved to while they were arriving — numbers credited to an account that served none of that turn. A re-point reaches the CLI on its NEXT request, so the turn boundary is the first moment a switch can be true.

**The statusline needed its own answer.** `data/bin/vibespace-usage` was keyed by `VIBESPACE_ACCOUNT_KEY`, fixed at SPAWN, under a comment claiming "a hot re-point is a known attribution seam handled by the ledger's time-based records" — wrong on both halves (the CLI does re-read, and the statusline cache is not the ledger; nothing ever re-attributed it). ws-create now also ships `VIBESPACE_ACCOUNT_LINK` = the credential SYMLINK itself, and the tool `readlink`s it per write: the slot resolved by the process that produced the reading. The REMOTE branch deliberately has no twin, with the reason written down — a host's credentials are tarred at spawn and the engine's re-points skip host sessions, so there is no later switch to be wrong about.

**A symlink has no history — so give it one.** `data/slot-transitions.jsonl` records every re-point `{sessionId, poolId, from, to, at, why}` (sessionId null = the pool default moved). accounts.js is the SINGLE WRITER — `ensureSessionPoolLink` / `setPoolTarget` are the only two functions that move a link, so spawn, engine, manual route, signed-out self-heal and account removal are covered by construction. `slotAt(sid, ts)` answers by TIME; **no record means UNKNOWN, never agreement**.

**The repair is root-cause too.** A one-shot migration (`2026-09-reattribute-readings-by-slot`) walks the four stores using evidence in strength order: the transition ledger, then each member's own last-known-good marker (a wiped credential file's mtime — decisive and inference-free), then an OPTIONAL operator-provided frozen journal whose `[pool] per-session switch` lines backfill into the ledger (a line we cannot place in TIME is COUNTED, never guessed). A foreign cache snapshot is archived with a reason and **rebuilt from the newest anchor predating the wipe**; a foreign anchor is archived and the record after it re-chained with `costSince:null` (the cost is real, the Δu is not); `rates.json` is dropped so the estimator re-learns; an attribution entry is re-attributed when the ledger can place it and archived otherwise, which makes the by-time walk fall back to that session's previous un-refuted entry. Idempotent, restart-safe, archive-never-destroy.

**And the panel stopped lying.** "Updated 3min ago" is the sentence the incident could not survive. Every panel now names the SOURCE of its latest reading, adds the corroboration verdict when one exists, and — for a member that cannot produce readings at all — shows its last REAL reading with "signed out since <stamp>", plus an explicit warning when the newest cached reading POSTDATES the death.

**Invariants.**
- TOP-PROCS LIST WENT EMPTY ON A BUSY BOX (release gate, 2026-09-08): `ps aux` output is unbounded by process count AND argv length — 2463 processes plus one `claude -p <29 KB prompt>` made 4.4 MB, past sysinfo's 4 MiB execFile maxBuffer, so topProcs answered [] and the sysinfo op shipped no processes (test-sysinfo-op red on the release gate; the panel would show nothing). Fix: one PS_MAX_BUFFER = 32 MiB on every ps call (width flags are GNU-only and would break the BSD rung); test-sysinfo-op's fat-ps leg drives a 5.5 MB fake `ps` through the real read() with a 4 MiB negative control. Invariant: a child's stdout bound must be sized from the WORST real table (count × longest argv), never a round number.
- **A reading is keyed by the credentials the producing process was handed** — the symlink we control, never a telemetry attribute about it. Where a producer has no session (the auto-cli panel), that is its spawn's config dir, and the key and the config dir must be ONE decision.
- **A telemetry attribute that names an identity is not proof of which token authorized a request.** `organization.id` was measured wrong twice (2.361.0 and B-2c9b) before the owner's post-mortem; the "≥25min stale token" forensic was itself made *with* the channel it was meant to validate — it measured how long the CLI keeps REPORTING its spawn identity, not how long it keeps BILLING it. **A channel may not be its own control.**
- **Two functions answering one question means one of them is wrong**, and it will be the one nobody re-derives. `sessionReadingMember`/`sessionBillingMember` diverged for exactly one release — the former is DELETED since 2026-09-07 — and the wrong one owned every value on the instance.
- **A mutable fact that our own handler changes must be resolved once per EVENT, not per record** (the 2.369.66 rejection lesson, now applied to readings — the scope is the turn, because that is what defines it).
- **State with no history cannot answer a late question**; if late consumers exist, record the transitions rather than letting each one invent a proxy.
- **"Unsatisfiable guard" is a claim to re-measure, not inherit.** 2.369.66 deleted a credentials leg from `validateBillingSlot` as unsatisfiable; `parseAuth` reports `loggedIn:true` for a login whose access AND refresh tokens have both expired, so it was satisfiable and it was the gap that let dead-account readings look authorised — and the same gap made such a member a legal SWITCH TARGET (`ensureSessionPoolLink` checks the same boolean). `healthyPoolMembers` excludes it with **no fallback**: moving a conversation onto credentials that cannot authorize a request is not routing around a dead account, it IS the dead account, and an all-unusable pool must reach the loud `no-members` state rather than pick one anyway. The auth-FAILURE mark keeps its escape hatch because that one is a live inference; a credential state is a fact.
- **Memoize a health answer on the FACT, not on a clock.** The credential state gates both switch targets and a user-facing warning, so a 30s time box would keep answering "signed out" for 30 seconds after a re-login. Keying the memo on the file's mtime+size costs one stat (the cheap half) and makes a change visible on the very next call.
- **An archive that can lose what it exists to preserve is not an archive** — the transition ledger's rotation named shards `Date.now()`, so two trims inside one millisecond overwrote each other; it appends to a daily shard now.
- **A panel that reports only freshness is reporting the wrong thing** when attribution is the failure mode; name the producer, and let a source nobody recognises print itself verbatim rather than be absorbed into a familiar bucket.

**ROUND 2 (adversarial verifier — six defects, every one reproduced first; each fix carries a regression assert AND a mutation-tested negative control).** ① **MAJOR: a REMOTE CODEX reading was routed into the host's CLAUDE bucket.** `usageCacheKeyFor`'s first rule ("a remote session with no account → `host-<id>`") is a CLAUDE fact — usage-routes seeds `_hostUsage` from those files, the remote statusline harvest writes the host's `__global__` into them, the on-demand ⟳ overwrites them with an OAuth panel and the Agents machine rows render them as that machine's own quota. Codex remote chat is supported (ws-create: "codex remote chat rides the SAME keeper") and `recordCodexQuotaSignal` has no host gate, so the moment readings and rejections shared ONE resolver a remote codex session's `rate_limits_updated` snapshot began OVERWRITING the host's claude numbers (reproduced: `{"limitId":"codex",…}` replacing the host's `sevenDay 0.11`, printed "via unknown" because the codex snapshot carries no `source`) AND disappearing from codex's own panel, whose disk seed only matches `cxs-*`/`__global_codex__` — while `noteWallSignal` on the same session still keyed `codexQuotaKeyFor` = `__global_codex__`, so a reading and its wall twin landed on different identities. The rule is now a TRANSFORM OF THE ANSWER rather than a backend test — whatever machine identity `resolveUsageKey` names, only `__global__` (the claude machine login) has a per-host form; an account-billed session never resolves to it, so "no account" is implied, and every other machine identity (codex's today, a future harness's tomorrow) passes through untouched. A `backend === 'claude'` spelling would have silently moved remote OpenCode/ACP sessions OFF the host bucket they already used, so that spelling is pinned as a mutation. ② **The daemon's sealed-orders reflex wrote no ledger row.** `agentd._execute` re-points a pool credential link while this server is DOWN (calling `repointPoolSymlink` directly — that machine has no AccountManager), so `slotAt()` answered with the last ORCHESTRATOR row: a confident WRONG answer, not the "unknown" the ledger promises, in exactly the window where a late consumer has nothing else to go on (and `repairAttribution` would then re-attribute an entry to a member the session had already left, stamped `repairedBy`). The device already reports every field the ledger needs, so the reconcile callback appends through `accounts.noteDeviceRepoint()` — idempotent under a replayed log (the daemon clears it only on ack, and the in-memory dedup dies with the process), `sessionId` derived from WHICH link moved (a plan-C link lives under `poolLinksDir`, anything else is the default), an unresolvable `from` left null rather than guessed — which keeps "accounts.js is the SINGLE writer" physical instead of a grep that hides a hole. ③ **A conversation whose LAST attribution entry was archived kept the refuted account in the ledger forever.** UsageHistory's re-bake only recomputes `if (e.sid && attrib[e.sid])` — correct when the baked value came from the WALK ("for sids without any, the baked value is the only record we have"), false the moment this repair empties a sid, and such sids exist by construction (the old OTel corrective record fired whenever the observation disagreed with `attribAt`, which answers `acct:null` for a sid with no entries, so a conversation's FIRST and only entry could be a corrective one). Clearing `.attrib-rebake-v1` could never reach them. The repair now NAMES the emptied conversations (`data/usage-history/.attrib-emptied.json` — appended and kept, because it stays true) and the re-bake falls through to the session-meta account for them. ④ **`deathMarkers` equated "the local credential dir is wiped" with "this account cannot produce readings"** — but a wiped dir plus a valid LONG-LIVED TOKEN (B-211a) is a supported, spawnable, reading-PRODUCING configuration (`resolveForSpawn` → `{oatOnly, CLAUDE_CODE_OAUTH_TOKEN}`; the session still carries `_accountId = sub-X` and `VIBESPACE_ACCOUNT_KEY = sub-X`). Measured on a fixture: the panel rewound from 42%/61% to a 6-day-old 10%/20%, the fresh anchor dropped, the instance-wide `rates.json` deleted, the attribution row archived — and `/api/usage` would have rendered "signed out since <5 days ago>" plus the amber warning for an account that works. `src/login-state.js` now answers TWO questions: `loginState` (the FILE) and `accountLoginState` (the ACCOUNT — file OR token, dying at the LATER instant, and an oat even DATES an account whose dir never existed). The callers choose deliberately, in writing: the panels and the repair ask the ACCOUNT question; `validateBillingSlot`/`memberLoginState` keep the FILE one, because a token stored in accounts.json cannot be delivered by re-pointing a symlink to a RUNNING CLI, and making the slot account-aware would turn a wiped member into a valid switch TARGET. `AccountManager.OAT_TTL_MS` now READS the constant from login-state (one definition, suite-pinned). ⑤ **`corroborated` survived the preserve-merge**: it is a verdict about ONE write, but `writeUsageCacheForKey`, `refreshViaCliPanel` and the remote-statusline harvest inherited the previous producer's, rendering "via own /usage panel · not corroborated" about a reading the account made about itself (a session-less producer has nothing to corroborate WITH). All three now apply `captureRateLimitEvent`'s own rule — `undefined ⇒ delete`, a verdict the caller DID supply is honoured (an unconditional delete would be accept-and-ignore) — and a drift guard over EXECUTABLE lines requires every usage-cache merge to DECIDE the label for its own write (the first version of that guard matched a comment and passed while the fix was reverted). ⑥ the kb's own stale sentences — the WALL essay a reader reaches FIRST still advertised `sessionReadingMember` as live and the credentials leg as "an unsatisfiable guard", both of which this change deletes and reverses — marked REFUTED in place, guarded PER MENTION (these essays are single giant paragraphs, so a line-level rule passes as soon as any other sentence on the line says REFUTED — measured). **R2 invariants**: a key rule that holds for ONE backend must SAY so (a resolver that absorbs another's job inherits every unstated assumption of the code it replaced); "X is the SINGLE writer" is an invariant only while every writer routes through X — a grep that passes because the second writer lives on another machine is the hole, not the proof; a repair that changes what a store KNOWS must tell every derived store what it changed (the re-bake's "leave it" rule was true only of the world before the repair ran); the credential FILE is not the ACCOUNT — ask the question the consumer actually has, and write down which consumers must NOT inherit the wider answer; provenance belongs to the write that made it (a merge preserves IDENTITY, never a VERDICT); and a guard that scans source must scan EXECUTABLE source at the granularity of the thing it guards. 

Gate: **scripts/test-readings-attribution.mjs (139)** — every producer × {before switch, after hot switch, after logout} against the REAL engine and a REAL pool with real per-session symlinks (driven through the producers, never by injecting a key), the pre-fix negative control (orgVerifiedKey's rule applied to the same state files the reading on the spawn-time org), the turn pin, the ledger, login-state incl. the satisfiable gap, the migration on a fixture shaped like this instance's stores, and a headless-chrome 375×667 leg of the real panel with a live-member negative control. `test-usage-anchors` and `test-auto-resume-loop` — both shipping OUTSIDE the release gate — were added to it in the same change.
## THE PROCESS LIST WENT EMPTY ON THE BUSIEST MACHINES, SILENTLY (2026-09-07, found by the release gate going red)

`test-sysinfo-op`'s oldest assert — "top procs with pid+rss" — went red on the dev box mid-session, then again on a re-run, having passed an hour earlier. The suite was innocent and so was the daemon: `sysinfo.read().procs` really was `[]` on a machine running 2,404 processes.

**The measurement.** `topProcs()` ran `ps aux --sort=-rss` through `execFile` with `maxBuffer: 4 * 1024 * 1024`. Command lines on a box hosting ~60 agent worktrees are long, so `ps aux` output is ~1.8 KB per process: measured **4,258,412 bytes at 2,404 processes** — past the cap. `execFile` kills the child and errors; the BSD fallback leg (`ps aux`, same 4 MiB) errors identically; and the code then did `resolve([])`. No log, no error field, no way to tell "this machine has no processes" from "we threw the answer away". The full-table twin `listProcs()` had the same shape with 8 MiB — `ps axo <cols>` measured 4,244,406 bytes, i.e. already at half its cap, and its cap is applied AFTER parsing, so the WHOLE table has to fit.

**Why it matters beyond a red suite.** Those rows feed the System panel's process manager and, worse, the memory-pressure alert's `top: …` line — which is printed *because* the machine is under pressure. The one artefact that says "here is what is eating the box" disappears exactly when the box is being eaten. This is the degrade-gracefully class again (2.276.0 writer sweep, 2.340.2 lost binding): **a catch broader than the failure it was written for hides its own bug, and only a path that logs the message VERBATIM ever gets found.**

**Fix.** One named constant `PS_MAX_BUFFER = 64 MiB` (~36k processes) shared by both readers, the `topProcs` timeout raised 5s→8s to match its twin, and BOTH final failure paths now `console.warn` the verbatim `ps` error saying the list will be empty. Nothing about the parse, the cap or the sampling changed.

**Invariants.** ① A buffer sized for a laptop is a silent truncation on a server — size it from a measurement and name the number in the comment. ② A degrade path that returns an EMPTY collection must say so out loud; an empty list and a discarded answer are different facts and callers cannot tell them apart. ③ A reader whose whole job is "what is this machine doing" must not be the thing that stops working when the machine is busy.

**Test.** scripts/test-sysinfo-op.mjs — a fake `ps` on `PATH` (in a child process, so `PATH` never leaks into the rest of the suite) emitting ~6 MiB of plausible output: the shipped module still returns rows, and the NEGATIVE CONTROL — the same table against a copy of `src/sysinfo.js` with the pre-fix 4 MiB constant spliced back in — returns the empty list this fixes. (An assert that cannot fail is not an assert.)

## STEER-ALL PUT 25 MESSAGES IN THE TURN AND SHOWED ONE (2026-09-07, owner "我刚才在那个codex session里全给插入了，但是我只能看到我最后插入的一条消息")

In a long-running codex session the owner pressed **Steer all** on a 25-item queue. Every steer succeeded — the model demonstrably got all 25 — and the chat window showed exactly ONE new bubble: the message typed after the last resume.

**Where the messages came from.** The app-server's input queue belongs to the THREAD, not to the wrapper. The live buffer of the wrapper that came up at 07:06:43 UTC carries `queue_changed n=25` two seconds after boot, and every one of those 25 `clientUserMessageId`s is a webui msgId minted **~10 hours earlier**, by the wrapper this one replaced. So the queue was INHERITED across a Terminate+Resume while `queueMeta` — and every bubble the wrapper had ever written — started empty (all 25 `queue_op_result` frames carry `msg_id: ""`; the single message with a real msgId is the one typed at 07:38:03, the one bubble the owner saw).

**The hole.** A user bubble comes from the record the wrapper writes as the text passes THROUGH it (`handleInput`). For a submission it never typed, the only live notice is the app-server's own carrier — `item/completed {item:{type:'userMessage', id, clientId, content}}` (0.153.4 `UserMessageThreadItem`; the rollout persists the same thing as `item_completed` / `UserMessage` / `client_id`, 28 of them at 07:38:44-45, each immediately after its `response_item`, with **no `user_message` event anywhere**: the queued path's carrier does not exist on the steered path). That kind fell off the end of `_handleItemCompletedInner` with **no branch and no breadcrumb**, so twelve-plus messages entered the turn in silence. Reproduced against the shipped wrapper in-suite: 12 steers OK, 1 bubble.

**A second defect the same session data exposed:** every message the user TYPES renders twice after a reload. One message is written by BOTH producers — us (as it is sent, carrying `webui_msg_id`) and codex (when the app-server commits it, carrying its own `msg_…` id and nothing of ours) — and `recordFingerprint` keys the first by ID and the second by CONTENT, so `mergeCodexRecords` dedupped neither. Measured through the REAL reader over the owner's real 66MB rollout + live buffer: 3/3 typed messages doubled.

**FIX, at three layers:**
1. **Wrapper (`data/bin/codex-chat-wrapper.js`).** `item/completed` routes `userMessage`: `recordInboundUserMessage(clientId, userInputToContent(content))` writes the bubble for any submission ENTERING the turn whose id we have not already recorded. A landed `turn/steer` records it AT ONCE, in queue order (the commit twins arrived 42s later on the owner's session — a message that went in must be visible when it went in; a REFUSED steer records nothing, that message is still queued). `recordedUserCids` remembers every id whose bubble already exists (chat-input msgId, the generated `queued-…` id, the peer cid), and an item with **no** clientId is ours by construction — only `turn/start` carries none. The record's `webui_queue_id` marker is out-of-band on both readers; the queue ROW advertises the same clientUserMessageId as its `msgId` so the strip row and the bubble's `Steered` chip join.
2. **Normalizer (`src/codex-message-manager.js`).** `webui_queue_id` is a SECOND-CLASS identity: stamped into `webuiMsgId` (`_stampUserIdentity`) so the chip joins, but it does not suppress peer detection (most inherited items were agent-to-agent messages and must keep their labelled card) and `recordKey` strips it like `webui_peer`, so both copies of one message mint the same id.
3. **Store (`src/codex-session-store.js`).** The inherited bubble's marker is stripped from `recordFingerprint` (both copies are written inside the turn that commits the message, so their turn-scoped content keys agree), and the ID-KEYED twin gets a **claim**: an id-carrying copy of ours claims its content key and the next codex-side copy of that content consumes it and is dropped — n copies in, n bubbles out. Ours survives, so the chip join and the live message id are preserved.

**INVARIANTS.** (a) **Every kind an upstream stream can emit is either routed or NAMED** — the wrapper is an allowlist too, and it had no `else`; a kind that is neither gets logged once verbatim and counted in `meta.unhandledItems` (`noteUnhandledItem` / `NO_RENDER_COMPLETED_ITEMS`), because the absence of that breadcrumb is exactly how `userMessage` stayed lost. (b) **State that outlives the process is not ours to assume**: the app-server's queue survives a wrapper restart, so "we have a bubble for everything in it" was only ever true for the wrapper that filled it — anything entering the turn under an id we do not know needs a record. (c) **A message that enters the turn must be visible when it enters**, not when the server gets round to committing it. (d) **The twin rule is FORWARD ONLY**: a claim covers copies that come after it (ours is always written first), because letting a late claim swallow an EARLIER codex record would delete an old message from history the moment its text was typed again after the buffer had rotated — a millisecond tie renders twice instead, since a duplicate is a nuisance and a deletion is data loss. (e) **A falsy id is not an identity** — a `webui_msg_id: ''` copy keys on content like codex's, or it is a stranger to its own twin.

**Gates.** scripts/test-codex-p2-wrapper.mjs ⑥ (the REAL wrapper against a stub that hands it an inherited queue: steer-all → 12 bubbles in order, the late commit twins add nothing, a DRAINED item gets its bubble from the twin alone, our own message is never re-recorded, the unhandled-kind breadcrumb — plus a PRE-FIX control run verbatim from `git show master:data/bin/codex-chat-wrapper.js`, which reproduces "12 steers, 1 bubble"). scripts/test-queue-steer.mjs ⑨ (the redacted rollout-shape fixture scripts/fixtures/codex-steer-all-rollout.jsonl → 12 bubbles from the rollout alone and 12 from the 24-record rebuild, with negative controls at 24 and 2; the drained cross-turn twin; two identical texts staying two; the forward-only guard; the peer-card guards; and a headless-chrome leg that renders the real normalizer's messages with the REAL renderer and counts twelve bubbles, ten of them chipped `Steered`).
## A CODEX CONVERSATION SAT IDLE FOR 32 HOURS WITH QUOTA AVAILABLE (2026-09-08; owner ruling: auto-resume is generic)

**Symptom.** codex thread `01a0733f…` (webui `sess-13-1788764799305`, backend codex, `accountId: null` = the machine's codex login, no pool) hit its usage limit and never came back on its own. `data/auto-resume.json` was `{"armed":{},"fires":{}}` and the journal held **zero** `[auto-resume]` lines for that session, ever.

**Timeline, from this instance's own stores (read-only).**
- `2026-09-07 13:16:40Z` — the app-server refuses the turn. **99** records in that thread's own rollout (last at `13:19:28.411Z`, re-counted 2026-09-08 17:5x PDT), all `codex_error_info: usage_limit_exceeded`, message *"You've hit your usage limit … or try again at Sep 13th, 2026 8:36 PM."* — `resetsAt: null`, `rateLimits: null`. The live-buffer count of the camelCase twin is **no longer reproducible**: that buffer was replaced when the owner resumed the thread (it now starts 2026-09-08 22:43Z), which is itself the reason a wire-shape claim has to be re-measured rather than inherited.
- `13:20:19Z` — the last reading: `limitId:'codex'`, sevenDay utilization **1.0**, `resetsAt 1789356983` (= Sun Sep 13 2026 20:36:23 PDT — the prose, to the second).
- 32.6 h of silence (`codex-rate-limits` telemetry gap 13:20:19.976Z → 21:55:00.608Z, 1955 min).
- `2026-09-08 21:55:00Z` — a FRESH window on the SAME lane: `limitId:'codex'`, sevenDay **0 %**, a NEW `resetsAt 1789509303`. Quota available. Nothing woke it.

**Four independent breaks, each sufficient on its own, all measured** (the fourth was found by re-measuring the wire while verifying the first three).
1. **The harness classifier looked for a word the CLI never says.** `EXHAUSTION_RE` matched `usage_limit_reached` and five workspace variants; the wire says `usage_limit_exceeded` (codex-core's rollout, snake_case) and `usageLimitExceeded` (the app-server, camelCase — its own 0.153.4 schema documents the translation: *"This translation layer make sure that we expose codex error code in camel case"*). Census over the whole local rollout corpus (re-run 2026-09-08): `codex_error_info` takes exactly three values — `usage_limit_exceeded` **102**, `cyber_policy` **6**, `unauthorized` **1** — and the regex matches **none** of them. `task_failed` never carried `codex_error_info` at all in any spelling the regex would take — so codex auto-resume was structurally dead from the day it shipped (2.368.20) and no test could see it, because every fixture spelled the enum the way the regex did.
2. **The wrapper read the error off the wrong object.** `turn/completed` with `status:'failed'` carries its `TurnError` on `params.turn.error` (0.153.4: `TurnCompletedNotification {threadId, turn}`, `Turn.error` — *"Only populated when the Turn's status is failed"*). The wrapper read `params.error`, which does not exist, and emitted `task_failed {error: '', /* no enum */}`.
3. **Nothing anywhere could act on "the window reopened EARLY".** The reset was SIX DAYS out, so `armIfEnabled`'s 26 h ceiling was RIGHT to refuse a timed wait — and then returned null, leaving the conversation with nothing watching. The only thing that could ever have continued it is the 21:55Z record, and a quota reading's ONLY effect on a waiting session was to DISARM it.
4. **And the enum's SHAPE was assumed, not measured** — the r4 lesson of the B3 batch, landing on the very field this release turns on. `CodexErrorInfo` is a serde **externally-tagged** enum, so a variant that carries data arrives as a ONE-KEY OBJECT. Verbatim from this instance's own live buffer (the incident thread's, `data/session-buffers/sess-13-1788764799305.buf`): `"codexErrorInfo":{"responseStreamDisconnected":{"httpStatusCode":null}}`. Every fixture in the repo — and the first draft of this release's own reader — assumed a bare string, and `String({…})` is `"[object Object]"`, which is **truthy**, so the object form did not even fall through to the message ladder: it was read as an unknown enum and dropped, silently, exactly like break ①. Whether `usageLimitExceeded` is itself a unit or a data variant in 0.153.4 could NOT be measured here (no such record survives, and `codex app-server generate-json-schema` is a REJECTED oracle — 7 INET connects incl. `chatgpt.com:443` even logged out, so asking it is a §ban-safety violation), so BOTH shapes are read rather than betting on one.

**The ruling.** *"auto resume 应该是通用的, 只要支持 hook/注入的 harness 都支持, 形式可以不一样 — 有些是发消息, 有些是 start turn 之类的固有指令."* The timer, the loop breaker, the notices, the tri-state gate and restart survival are VibeSpace's own and stay harness-neutral; exactly two facts are harness-specific and both move onto the DESCRIPTOR — the LIMIT SIGNAL (`quota.signalFromStream`, which already existed) and the RESUME VERB (`descriptor.resume {form, deliver}`: `message` / `turn-start` / `prompt` / null). `capsOf(backend).autoResume {signal, resume, supported}` is DERIVED from those two by the registry, and every surface that offers or acts on the feature reads that row.

**The fix, per break.** ① the enum is a closed SET seeded from the retired regex and extended with what the wire sends, compared case- and separator-insensitively (one list, not two), plus a LAST-RESORT parse of the CLI's own sentence for the reset the record omits (rounded UP a minute — late costs a tick, early costs a billed turn into a wall). ② the wrapper forwards `params.turn.error`'s message + typed enum, so ONE classifier reads both the `error` notification and a failed `turn/completed`. ③ a far reset is now a **WATCH**: still no timed continue (the ceiling is intact and costs nothing), but the fresh-window edge can keep the promise — and a quota reading on an ARMED session is JUDGED instead of disarming it (PURE `src/auto-resume-signal.js`). ④ `codexErrorEnum()` reads the enum as a string, as an externally-tagged one-key object, or as an internally-tagged `{type}` — the variant's own payload comes back with the name, because the http variants prove a variant can carry fields and a reset would ride exactly there; `classifyAuthFailure` reads the same field through it, so the two cannot disagree.

**Invariants.**
- **A capability that only one harness implements is a branch waiting to happen.** The two harness-specific halves are descriptor members and the caps row is DERIVED from what the descriptor really implements, so a new harness cannot inherit a control nothing serves, and `supported` cannot be hand-set on the surface that reads it.
- **An enum's SHAPE is the emitter's decision, not the schema's** — measure the bytes, and when the bytes are gone (buffers rotate) read BOTH plausible shapes rather than betting on one; `String(x)` on a wire value that can be an object yields a truthy `"[object Object]"` that no vocabulary matches and no degrade path notices. A vocabulary fix that only fixes the SPELLING re-ships the same silence.
- **An enum you match on is a MEASUREMENT, not a memory.** The retired names had been in the file since the feature shipped and nothing ever asked the corpus whether they occur; the answer was zero, twice over, in two different casings. Census the wire before believing a vocabulary — and keep the retired spelling as the seed of the new set so the diff shows what changed.
- **"Refuse to sit forever" and "watch for free" are different decisions.** Refusing a six-day TIMER was right; refusing to WATCH cost the user 32 hours. A ceiling on a promise about TIME must not silently also cancel the promise about EVIDENCE.
- **A reading is evidence about the WALL, not about the session.** Disarming on one is only harmless where readings cannot reach an idle session; codex proves that is a per-harness accident, not a rule. Judge the reading, and keep the promise when it says the wall is gone.
- **What a reading is ABOUT is three facts, not one.** Lane, bucket, and no sibling bucket spent — each of them a real way to fire a billed turn straight back into the wall (the measured `codex_bengalfox` lane read 0 % through the entire stall).
- **A re-verdict must not downgrade a wait it does not understand.** Two callers re-arm the same session from a verdict with no signals in hand; before they inherited the wall's lane/bucket they replaced a wall-aware wait with a lane-blind one that no reading could ever open — this release's own defect, one function later, caught by its own conformance leg.
- **The negative control is the pre-fix classifier, through the harness's public seam** (`signalFromStream`), so the 32-hour stall is reproduced with everything else — the real engine, the real pool, the real module, the real records — unchanged.

**r2 — THE FIX'S OWN LOOP, three findings from the adversarial verifier, all reproduced against the real modules before anything was changed.**

**① The fresh-window edge was a SUSTAINED burn, not a one-shot.** Round 1's `noteQuotaReading` put no per-arm limit on itself: every reading whose armed bucket read healthy re-entered `fireNow`, whose only ceilings are the loop breaker's — `FIRE_MAX_IMMEDIATE = 3` per **rolling hour** and a **ten-minute** quarantine. A WATCH stands until its far reset passes (six days in the incident), so the shape is a standing burn. Reproduced through the module's own public seam with a clock we own, against the REAL `src/server/auto-resume.js` as committed in round 1 (the incident's world: a watch six days out, the plan lane reading healthy every 30 s, the CLI answering every continue with another limit rejection): **[3,3,3,3] = 12 billed continues over four simulated hours, 492 "continuing now" journal lines for 480 readings**, arm still standing — ~72/day for the life of the wait. The engine's own comment had already claimed the opposite bound ("a healthy `codex` reading fires once, the CLI rejects, and the loop breaker quarantines the identity"): false in both halves, and the reason the loop was invisible. **The premise is reachable here today**: the exhaustion record carries `rateLimits: null` so the engine SYNTHESISES `limitId:'codex'`, and if the true wall is the model lane (`codex_bengalfox`, measured interleaving on this account minute by minute) the plan lane's very next push reads healthy about a wall it does not describe.

**Fix** = the edge is SINGLE-SHOT per WALL, and the shot is spent **at the delivery** (`edgeSpent` on the persisted breaker record, keyed `lane|bucket|scopedName`), cleared only by PROOF OF WORK. Stamping it at the REJECTION REPORT — the first draft, and the reviewer's own wording — is indistinguishable while every caller reports the outcome and is round 1's loop again the moment one does not: measured through the seam (arm → reading → fire → **no outcome reported** → re-arm) it gives [3,3,3,3] against 1. Today's engine does report it (`onWalledTurn` calls `noteFireOutcome(id,false)` first and is the only arm site carrying a wall), so the delivery-time stamp changes no production count as wired — it is there so the bound is a property of the module that SPENDS rather than an emergent property of five call sites plus a classifier, and this feature's own incident is a classifier that matched nothing for eight months. The key is deliberately NOT the reset instant, and both reasons were checked: on this instance's codex anchor stream (1799 readings / 30 days) the sevenDay reset carries **316 distinct values and changes between consecutive readings 414 times**, one wall being spelled two ways (`1789356983` ×417 beside `1789356984` ×3 — the ±wobble that made reading-lag.js compare weekly PHASE); and it would not cover this finding's own premise anyway, since the mis-attributed sibling lane carries a different reset BY CONSTRUCTION. A monthly SPEND CONTROL now opens nothing (`spendControlReached`, asked BEFORE the lane because it is a fact about the ACCOUNT — the codex twin of 2.361.2; unverified on the wire and it says so, which is safe because the rung can only REFUSE), while `credits.hasCredits === false` is the rung we did NOT write: measured, `usage-cache/__global_codex__.json` — the incident's own login — carries `hasCredits:false` while serving turns off plan quota, so reading it as spent would make the edge permanently inert here, i.e. the original incident re-introduced as a guard.

**② The branch's own leg for this was vacuous, which is why it shipped green.** Round 1's leg drove 8 readings and asserted `opened >= 1` (the WALL verdict — true whether or not a turn was billed) and `r.n > 0 || r.last` (true for n = 1 and for n = 999); `w.fired.length`, the only thing that costs money, was never read, and no hour boundary was crossed. Mutation-tested: with `FIRE_MAX_IMMEDIATE = 999`, `FIRE_QUARANTINE_MS = 0` and `FIRE_BACKOFF_MS = [0,0,0]` — the loop breaker's caps removed outright — the suite still reported ALL PASS. It is now an upper bound on billed fires across four simulated hours with three patched-copy controls (round 1's un-guarded edge; round 1's optimistic log; this fix's own draft), the constant is pinned to a literal a human agreed to as well as read back from the module, and **the suite that OWNS the breaker drives the new producer** (test-auto-resume-loop §4g: the real `recordRateLimitEvent`, the real engine, the real pool, its own clock — because the breaker's backoff is a wall-clock rule and 40 readings inside one real second are bounded by it whatever the edge does, which would have made the control read 1 against 1 and prove nothing). Under the same mutation both suites now go red.

**③ The journal announced continues that mostly never happened.** The line was written once per READING, before the attempt — 492 lines for 12 continues (41:1) — burying the exact channel this incident was diagnosed from ("ZERO [auto-resume] lines for that session"), while the branch had already added a per-verdict throttle for the negative side citing that very rule. It now speaks AFTER the attempt and says what happened: `continued now` only for a delivered continue, a breaker refusal once per `REFUSE_LOG_MS`, and a refusal by this rung reported to the caller by NAME (`already-refuted`, with `wallOpen:true` so it can never be printed as "the wall is still up").

**r2 invariants** = **a reading is a CLAIM and the CLI's answer outranks it** (a producer pushing every few seconds must not be able to re-buy a verdict it has already been refuted on); **a money bound belongs to the module that SPENDS, not to a caller's classification** (stamp at the spend, not at the report about it — and say so where the caller's comment claims otherwise); **a comment that credits a mechanism is an assertion — measure it or delete it** (the quarantine is ten minutes; the hourly cap it really rested on resets every hour, forever); **an assert that counts VERDICTS does not bound SPEND** (`open` is true whether or not a turn was billed, and `n > 0` is true for 999); **the suite that owns a budget must DRIVE every new producer that spends it**, not grep for it; and **a fixture with no clock cannot measure a rule made of clocks** — the world has to be able to reach the hour boundary the loop lives on.

Gates: test-auto-resume (200), test-harness-contract (306, the conformance run + the backend-id census), test-quota-source (112 — the shape legs drive the REAL object-form record from the incident buffer as a negative control, with the pre-fix `String(enum)` reader as the mutation), test-codex-p2-wrapper (382), test-auto-resume-loop (200), test-new-member-wake (119, master's own wake suite — its `attemptFire` wiring pin now names the ROUTE rather than the exact argument list, since a reading-driven fire carries its wall).


## AUTO-RESUME FIRED 130 TIMES INTO A WALL (2026-09-07, owner's frozen journal + screenshot; owner decision ut-1c6c15a2db items ①④ — items ② ledger backfill and ③ projection-driven re-reads stay OPEN)

**Symptom.** One conversation was auto-continued **130 times** between 23:32 and 04:03 (a second one 32 times, up to two per SECOND). Every continue was a billed turn the CLI answered with "You've hit your session limit · resets 6am". About 150 of the conversation's last 150 messages (2295–2445 of 2445) were the same three junk artefacts repeating: the CLI's error line, VibeSpace's injected "自动续跑" prompt, and a "账号池已切换到 PandyMax，已自动继续这个任务。" card.

**What the evidence said.** Every cycle in the journal is byte-identical apart from timestamps:

```
[auto-resume] <id>: armed for <now+45s> (switched to a usable account)
[pool] per-session switch <pool>/<id>: <observed> (observed; linked <link>) → <link> (re-point, same target)
[auto-resume] <id>: 账号池已切换到 <link> — continued immediately
```

740 of those `(re-point, same target)` lines — the pool "switching" a session to the member it was already linked to. The final arm at 04:03:48, once a real reading finally landed, names every member dead **including the one the engine had spent four and a half hours calling usable**, and ends `— session observed on Fish Max while linked to B-Stack Max`.

**Root cause (two independent defects; both reproduced before either was touched, scripts/test-auto-resume-loop.mjs).**

① **Attribution.** B-2c9b (plan B) made the OTel-observed org override the link for BLOCKING decisions, on the premise that "a hot-switched CLI keeps its old token, so the observation names what it is burning". The owner's post-mortem corrected the premise: `organization.id` on an `api_request` is the identity the CLI cached in its **config dir at SPAWN**, not the token that authorized the request — the credential file itself IS re-read, which is exactly what the re-point's mtime bump exists for. So every rejection was filed against the org the session had STARTED on, the linked member — whose credentials the process actually reads — never received a wall verdict and stayed "healthy" in the cache forever, `quotaVerdictFor` answered "usable via \<link>" on every cycle, the per-session pass decided FROM the (dead) observed member and therefore "switched" to the link it was already on, and the hot-switch path called `fireNow()`.

② **No memory.** Neither fire path remembered that the previous continue onto that exact identity had been rejected seconds earlier, and `fireNow` skipped the pre-fire gate (`beforeFire`) that the timed path runs — so the one component that re-probes and re-verdicts before spending was bypassed by the path that fired 130 times.

**The fix.**

- **A rejection is a fact about the credential SLOT.** (Since round 3 a rejection RECORD reads the TURN-PINNED form `rejectionSlotFor` — the raw resolver below is only for "where would a fire land right now"; see Round 3 ①.) `wallSlotFor(session)` resolves the session's own per-session symlink (or the pool's default link) and VALIDATES it — a real member of this pool (`validateBillingSlot`; named failures `no-slot` / `slot-not-a-member` / `slot-unreadable` — ~~`poolMembers()` already filters by loggedIn, so a separate credentials leg would be an unsatisfiable guard~~ — **REFUTED 2026-09-07**: `parseAuth` answers `loggedIn:true` for a doubly-expired login, so the leg is satisfiable and `validateBillingSlot` now carries it (`slot-wiped`/`slot-expired`/…); the suite still pins that every named reason is reachable). Its `slotOk` verdict travels **on the wall signal**, because it is only true at the moment the rejection ARRIVES: by the time the turn ends the pool has usually re-pointed the link off the account that just refused us (measured — the first version validated at turn end and reported "not this session's credential slot" about the very account that had rejected it).
- **One function no longer answers two questions.** `sessionBillingMember` (link + slot validation) answers every BLOCKING question: the demotion target, the verdict order and `viaId`, the per-session switch's `currentId`, both quota-probe targets, `_wallScope`, and the loop breaker's fire identity. ~~`sessionReadingMember` (observed over link — the old body verbatim) answers only `resolveUsageKey`~~ — **REFUTED AND DELETED the next day (2026-09-07 readings-by-slot)**: a utilization **number** does describe whatever token produced it, but the observation never NAMED that token (it names the identity cached at SPAWN), so the values half was wrong in exactly the way the rejections half had just been fixed. `readingSlotFor` — the same validated, turn-pinned credential slot — now answers for readings too, and `resolveUsageKey` resolves `sessionBillingMember`.
- **The verification ladder gains a top rung.** A wall on the session's own validated slot is ground truth by itself — we know what the symlink points at, which is strictly better evidence than a telemetry attribute. Every other account still needs the 2.368.34 corroboration (≥2 walled turns in 120s, or it IS the observed org), and the journal names which rung authorised it: `(1 walls / credential slot)`, `(1 walls / observed-org)`, `(2 walls / 2-walls)`.
- **A per-SESSION memory.** `noteSessionWall(sid, member)` records "this member rejected THIS conversation" for 10 minutes — written even when the demotion is HELD, because "it rejected me" is true either way — and is unioned with the breaker's `recentFireFailures`. `quotaVerdictFor` cannot answer `usable` through such a member, and `decidePoolSwitch({exclude})` cannot pick one. `exclude` is a NAMED pure input reported as `all-rejected`, never a silently empty candidate list; `pessimism` was the wrong tool because "it just said no to this conversation" is not a percentage, and docking it would let a big enough gap re-select the member on the very next tick.
- **The loop breaker** (auto-resume.js). A fire the session answers with another limit rejection is a FAILED fire; the record `{identityKey, at, count}` is per session and is PERSISTED with the armed waits, because a deploy must not hand the loop a fresh budget. Four named refusals: `fire-pending` (never two continues in flight), `same-identity` (10min quarantine), `backoff` (immediate fires 0 / 60s / 5min), `hourly-cap` (3 immediate fires per session per hour, after which only the TIMED path — the one anchored to a real reset — may fire). Both paths became ONE `attemptFire`, so the immediate path runs the same `beforeFire` gate; `_arFiring` is raised BEFORE the gate is called, because the real gate runs `maybePoolAutoSwitch` → `fireNow` and everything before its first `await` is synchronous re-entry (measured 1992 levels deep with the flag one line late). `onWalledTurn` reports the failure FIRST, before anything re-arms or re-switches — that ordering is the off-switch — and `noteRecovered` clears the breaker BEFORE its armed-record early return, since a fire has already deleted the armed record (round 4: …and only when the caller's evidence is WORK — see the classification table below).
- **One honest line.** When the breaker trips the session is told once per window: "账号池已切换到 X，但它同样被用量上限拒绝… 将在 T 重置后自动继续" or "…且暂时没有可用的成员 — 可以添加成员、把这个会话切到别的账号，或等待配额重置". The success line ("已切换到 X，已自动继续") is likewise once per distinct target per session per window; the incident wrote it ~150 times. A refused TIMED fire stays journal-only — it self-heals inside 10 minutes, so saying it would be noise.

**Invariants.**
- A fire answered by a limit rejection is a FAILED fire, and that fact must survive a restart.
- "Where do I fire" and "who rejected me" must be the SAME fact, or the breaker quarantines an account that never rejected anything.
- A judgement about "which account is this session on" is only valid at the instant the evidence arrives — by the end of the turn our own pool switch has rewritten it. Carry the verdict on the signal.
- A telemetry attribute is not a credential. Values belong to the token that produced them; a REJECTION belongs to the credential slot that was read. We OWN the symlink — that is the stronger evidence.
- Every "no way out" outcome must speak (one operator line, one user line), but an outcome that self-heals in minutes must stay quiet.
- The negative control for a two-defect incident is a MATRIX, not a snapshot: re-create each pre-fix behaviour through a public seam and show that removing either one alone stops the failure (both off ⇒ ≥10 unbounded fires here).

**Round 2 (adversarial verifier; four defects, all reproduced through the real engine before anything was touched).**

- **① The "one honest line" was one line for four different facts (medium).** `logRefusal` computed the named check `chk` and then dropped it: `breakerNotice` hard-coded the exhaustion narrative for EVERY reason. Reproduced at module level and through the real engine + real pool: the first continue goes out onto Account A, the pool re-points onto healthy Account B, the second attempt is refused `fire-pending`, and the conversation is told *"账号池已切换到 Account B，但它同样被用量上限拒绝，且暂时没有可用的成员。已停止反复重试…"* — **B had never been fired at, B and the rest of the pool were healthy, and the session was still ARMED** (it continues ~15 s later on the tick). Worse, that card SPENT the once-per-window budget, so the genuine "nothing can serve you" line was then suppressed for the rest of the hour (measured: the real same-identity refusal that followed said nothing at all). The harsh variant is the one users hit, because it triggers when `resetsAt <= now + 5min` and the near-arm is always `now+45s`. Fix: the text is a PURE function of the refusal reason (`refusalNoticeFor`), and each class has its OWN budget. `same-identity` may say "it refused us too"; the additional clause "and there is nowhere else to go" requires a SECOND fact — the pool's own no-target verdict, handed over by `noteNoPoolTarget` from the `all-rejected`/`no-members`/`stuck` branch of the per-session pass — never an inference from a refusal reason. `hourly-cap` says exactly what it knows (N tries this window, immediate retries paused). `backoff` and `fire-pending` are JOURNAL-ONLY and spend nothing: they are sub-minute pacers on a promise that is still being kept, and no-silent-failures is about a BROKEN promise. A card that says "stopped retrying" seconds before retrying is a lie the user has to un-learn.
- **② The fire was recorded against the account we LEFT (medium).** `attemptFire` resolved the identity BEFORE the pre-fire gate — and the gate is `beforeAutoResumeFire`, which runs `maybePoolAutoSwitch` and can re-point the credential link. Reproduced with a real pool {DeadOne (5h at 1), HealthyOne (5%)}: the tick's gate moved the link, the continue was delivered while linked to HealthyOne, and `_fires.last.key` said DeadOne — so a rejection would quarantine the account we had already left, leave the REAL rejector fireable, journal the wrong name, and dedup the card under the wrong key. This is precisely the invariant round 1 wrote above `identityFor()` ("the fire onto X failed" and "X rejected this session" must name the same X), broken in the one case that comment warns about. Fix: `deliver()` re-resolves the identity after the gate, re-runs `canFire` on it (a gate that moves us onto an identity we already burned this window ABORTS instead of spending), and keys `noteFired`/`announce`/the journal to where the continue actually landed.
- **③ The card claimed the quota had reset when the pool had swapped accounts (low).** The timed path announced "用量上限已重置" unconditionally — and after round 1 that is the DOMINANT recovery path (see ④), so the common case now told the user something false. Fix: the wording comes from what actually unblocked us — `kind === 'now'` (always a pool switch), a near-arm reason (`switched to a usable account` / `account usable again`), or the identity having MOVED during the gate; only with none of the three is "the limit reset" the reason. A drift guard pins the two arm-reason strings against the engine that writes them.
- **④ The suite certified an ordering production does not take (low).** §2's leg "a hot per-session switch still continues the armed session immediately" INJECTED the wall signal, which skips the early `maybePoolAutoSwitch` that BOTH real producers run. Measured on one world, changing only the producer: **injected `noteWallSignal` ⇒ 1 immediate continue; real `recordRateLimitEvent` ⇒ 0; real `markLimitBanner` ⇒ 0.** The link is re-pointed to the healthy member BEFORE `onWalledTurn` arms the session, so the `finally` pass has nothing left to switch and `fireNow` is never reached; the session is armed at +45 s and continued by the TICK — **45 s arm + 15 s grace on a 30 s tick ⇒ ~60–90 s, not "immediately"**. §2 now drives both real producers and pins that latency, keeps the injected leg only as a labelled artificial control, and adds the case the immediate path actually exists for (c1206711: a switch that lands LATER onto an already-armed session — driven through `maybePoolAutoSwitchForPool`, the engine's own seam, with the wall-clock eval gate and dwell belt wound back instead of slept through). §4b's "the cache is untouched" had the same shape — it only held because the signal was injected; a real rejection writes its own reading (`source: rate-limit-event`, utilization 1) through `captureRateLimitEvent` before the ladder runs. It now asserts the LADDER's output (no `wall`-sourced write, the named hold/`not-a-member` verdict) and a companion leg drives the real producer to show both writers.

**Round-2 invariants.**
- A refusal may only claim what its OWN reason knows. Four named reasons are not one message with four spellings, and the clause "there is nowhere else to go" belongs to whoever computes it (the pool), not to whoever noticed a retry was too soon.
- A notice budget is per CLAIM, not per module: one shared stamp lets a frequency notice eat the only line that carries an actionable fact.
- No-silent-failures covers a BROKEN promise. A pacing refusal on a session that is still armed and does continue is journal-only — narrating it is noise that the next event contradicts.
- Any state a GATE can change must be re-read after the gate. Resolving an identity, then running a gate whose whole job is to re-point that identity, then recording the pre-gate value, mis-keys everything downstream — and the re-read must re-run the admission check, not just relabel.
- The text that explains a billed turn must name the fact that caused it. "The limit reset" and "the pool switched" are different events; deriving both from the armed record (plus "did the gate move us") keeps one source for the card and the journal line.
- **A test that INJECTS a signal has chosen an ordering.** When two producers exist, the suite must drive the real ones and state the measured latency; the injected leg survives only as a labelled control, next to the measurement that shows why it differs.

**Round 3 (adversarial verifier; both defects reproduced on the real engine + a real pool before anything was touched).**

- **① The slot was resolved per RECORD, and the first record moves the link (high).** ONE rejection reaches us as SEVERAL records — the CLI's `rate_limit_event`, the assistant limit banner, and a banner inside a `task_notification` (three producers, src/server/stdout/claude-stream-json.js :323/:328/:563; `noteWallSignal`'s own comment already states that a banner and its rejected event are ONE wall). Every producer calls `maybePoolAutoSwitch` the moment it marks the cache, so the FIRST record re-points the link by itself — and round 2 asked `wallSlotFor` again for the SECOND one. Reproduced in both producer orders and in the two-rejected-events shape: `recordRateLimitEvent` keys correctly to LINK and its switch moves the link to SPARE; `markLimitBanner` then resolves the slot to **SPARE**, marks SPARE's cache, and its signal makes `demoteWalledAccount` demote SPARE `(1 walls / credential slot)` and `noteSessionWall(SPARE)`. The healthy member the pool had just rescued us with was thereby marked exhausted **with the branch's own top-rung authority** and excluded from this session for 10 minutes — the exact misattribution the branch exists to remove, now upgraded from "held" (base HELD it: `single wall on SPARE (session not observed there)`) to "authoritative". Harm measured against a FRESH ground-truth reading showing SPARE 95% healthy: verdict `usable:false, blockedUntil:+2h` instead of `usable via SPARE` — a multi-hour wait with a healthy member sitting in the pool, where the pinned code near-arms and continues in ~60 s. (In the 7d variant the healthy member's weekly bucket is stamped exhausted with a reset THREE DAYS out.) Fix: `rejectionSlotFor(session)` — `wallSlotFor` PINNED FOR THE TURN. The first keyed signal of `_turnWallSigs` owns the key and its `slotOk` for the whole turn; both rejection producers call it; `wallSlotFor` stays the fresh reading for `wallKeyFor`/`fireIdentityFor` ("where would a CONTINUE land right now" is a different question) and for the pin's own no-signal fallback. The pin is the TURN, not a time window, because that is what defines it: a pool re-point does not reach a RUNNING CLI (2.361.0 — a process kept billing its original org for 35 minutes across four switches), so every rejection inside one turn came from the credentials the slot named when that turn's first rejection arrived. `noteTurnEnd` clears the signals, so the pin is exactly one turn wide, and a turn whose end record never arrives degrades the way the wall machine already does (the next turn's real work classifies it NORMAL and clears them).
- **② A near-arm guard that could never be true, pinned as if it were the protection (low).** Round 2 refused to near-arm when `v.viaId === demoted.key`. That branch is unreachable: `demoteWalledAccount` calls `noteSessionWall(member)` on EVERY path that resolves a member (held demotions included) and `quotaVerdictFor` forces `usable:false` for that set, so `viaId` can never be the rejector; the only `demoted.key` returned without a session wall is `not-a-member`, whose key by construction matches no member id either. The suite pinned it by source grep, so no test ever executed it — the repo's 恒假守卫 lesson, with the added cost that the pin read stronger than the code was. Fix: the branch now re-reads the SAME store the verdict read (`nearArmVeto(v.viaId, sessionWalledMembers(id))`, PURE and exported), logs `INVARIANT VIOLATED` via `console.warn` when the two disagree, and says in its own comment that it is an ASSERTION and names the mechanism that actually protects. The suite executes the assertion's truth table (walled ⇒ violation, not-walled ⇒ null as the negative control, missing viaId/set ⇒ null, array form identical) AND executes the real protection functionally: a member that walled this session is never the verdict's way out even with its cache reading healthy and the demotion HELD — with "nothing walled ⇒ it IS the way out" as the control.

**Round-3 invariants.**
- **ONE event, SEVERAL records: resolve the mutable fact ONCE per event, not once per record** — especially when handling the first record CHANGES that fact. The `slot` flag already carried "this was true when the rejection arrived"; the KEY needed the same treatment.
- Pin by what DEFINES the scope (the turn — one CLI process, one token) rather than by a time window; a window admits every coincidence that falls inside it and excludes everyone slower.
- A guard that cannot fire must not be pinned as protection. Either delete it, or keep it as a loud invariant assertion that SAYS so and let the suite execute the mechanism that really protects.
- A negative control for a per-record/per-event fix re-creates the un-pinned resolution through the same public seam, and the fix's own legs must go red without it (mutation-tested: pin off ⇒ 19 asserts red; the near-arm wiring reverted ⇒ its pin red; the session wall dropped from the demote path ⇒ 5 red, from the held path ⇒ 6 red).

**Round 4 (adversarial verifier; reproduced on the real engine + a real pool + both real producers before anything was touched).**

- **The breaker's whole memory was cleared by a PASSIVE READING (medium).** `noteRecovered(id, why)` ran `noteFireOutcome(id, true, why)` unconditionally, and `noteFireOutcome(_, true)` **deletes the record**: the failed-fire quarantine, the 3-per-hour immediate counter, both once-per-window notice budgets, and the persisted copy on disk. But `noteRecovered` is not one signal — its loudest caller is the engine's non-rejected `rate_limit_event` (usage-pool-engine, "fresh non-rejected reading"), the record the CLI emits *whenever quota info changes*, which this instance sees roughly **20× per rejection**, plus its codex twin (`rate_limits_updated` with nothing exhausted). A reading says a bucket has room; it says nothing about whether the conversation produced a token. Reproduced end-to-end: a real continue delivered onto B-Stack Max, the CLI answers with another `rate_limit_event status:"rejected"`, the breaker records `fails:[{key:SPARE}]` and spends its exhaustion notice — then ONE `status:"allowed"` reading through `recordRateLimitEvent` and the record is gone from memory *and* from `data/auto-resume.json` (`fires: {}`), `canFire(SPARE,'now')` answers `ok:true`, and the identity that had just rejected our continue is immediately fireable again. So neither budget the round-1 spec requires was ever enforced in production — the loop the breaker exists to break, one reading later. Fix: `noteRecovered(id, why, { worked = true } = {})`, and `noteFireOutcome(id, true, why)` runs **only when `worked`**. The DISARM stays unconditional for every caller (a fire onto a session that is no longer waiting is the wasted billed turn this call has always existed to prevent) — only the breaker reads the classification.

**Who may clear the loop breaker — every `noteRecovered` caller, classified.** The table is executable: test-auto-resume-loop §5 DERIVES the call sites (walk `src/`, keep whatever mentions `noteRecovered`, paren-match each call, read its `why` string and whether it passes `worked:false`) and fails on any site missing from the table *and* on any dead row — a hand-written inventory is the tool this codebase has watched fail four times (B-3185 r7).

| call site | `why` | worked | because |
|---|---|---|---|
| usage-pool-engine `noteTurnEnd` | `turn completed normally` | **yes** | the turn ended with real output. This is also how a *successful continue* clears the breaker: its own turn completes and reaches this line |
| ws-handler chat-input | `user sent a prompt` | **yes** | a human took the conversation over — exactly who the budget was protecting |
| usage-pool-engine `recordRateLimitEvent` | `fresh non-rejected reading` | no | a bucket has room; nothing about this conversation. The ~20×-per-rejection event that caused this defect |
| usage-pool-engine `recordCodexQuotaSignal` | `fresh non-limited codex reading` | no | the codex twin of the same reading |
| usage-pool-engine `reset_credit_result` | `codex reset credit consumed` | no | the LIMIT moved on that identity; the conversation still produced nothing. Nothing on this path fires (the disarm above already removed the wait), so the classification only decides whether a live quarantine survives — and it self-expires in 10min, the same floor `tryResetCredit` itself paces on |
| auto-resume `tick` | `disabled` | no | the feature was switched off under a live arm; a toggle off/on must not hand the loop a fresh budget any more than a deploy may |

**Round-4 invariants.**
- **A "the session is fine now" signal is not one signal.** Before letting a call clear state that BOUNDS SPENDING, ask what its cheapest caller actually knows — the passive readings outnumber the rejections ~20:1 here, so the rare caller's semantics were being written by the common one.
- **Disarm and de-quarantine are different decisions with different evidence bars.** Dropping a wait is cheap and self-correcting; forgetting that an identity rejected our last continue re-opens a billed loop. One function may serve both, but only if each half reads its own evidence.
- **The classification must be enforced where it is DECLARED, not trusted.** `worked` defaults to true (so the two proof-of-work callers stay unchanged), which means a new caller that says nothing gets the dangerous answer — acceptable only because the suite derives every call site and fails on an unclassified one before it can reach production.
- Negative control = the pre-fix CALL, replayed through a public seam: the engine reaches auto-resume through `getAutoResume()`, so handing it a wrapper that drops the third argument re-creates round 3 exactly, with both real producers untouched. With one reading per cycle it re-fires **25/25** cycles (≥10 = unbounded); with the classification honoured the same world stops at ≤3 and the quarantine holds. Mutation-tested three ways: the `if (worked)` guard removed ⇒ 13 asserts red (incl. `fires=25`); the claude call site's `{worked:false}` removed ⇒ 12 red (the codex legs stay green, so each call site is separately load-bearing, and the audit pin names the unlisted site); and the guard-removal is also run from a MUTATED COPY of the product source inside the suite so the guard itself has a leg that dies with it.

**Gates.** scripts/test-auto-resume-loop.mjs (186; each round-2/round-3/round-4 fix mutation-tested — re-applying the earlier behaviour turns 1–19 asserts red, including the exact round-1 card text) + scripts/test-auto-resume.mjs (128; §11 was rewritten to RECORD the refuted plan-B rule rather than delete it) + test-pool-auto (67) + test-account-pool (33) + test-account-verdicts (45) + test-otel-truth (43) + test-rate-limit-capture (42) + test-codex-pool (61).

## THE INSTANCE DEFAULT ATE THE CONVERSATION'S OWN VALUE ON EVERY RESUME (B-6b6d, owner ruling 2026-09-07 — option ①)

**What it was.** 2.369.62 fixed the record that STATED a turn's effort but disclosed the reason the wrong value was there in the first place: `src/lib/session-lifecycle.js` resolved `sessionEffort = effort !== undefined ? effort : defaults.effort` (and the identical `sessionModel` line) on EVERY create — including the ones that carry a `resumeId`. So a resume that supplied nothing (the chat resume bar, resume-all, a card with no ⚙ override, `restartConversationInPlace`, a fork) shipped `codex.defaultEffort` on the wire, ws-create's B-21e4 continuity fallback was gated on `if (!sessionSpec.env.CODEX_WEBUI_EFFORT)` — i.e. on the client having sent nothing — and therefore could not fire exactly when it was needed. A conversation the owner had set to `ultra` resumed at `xhigh`, and the wrapper then synthesized a `turn_context` saying so.

**The ruling.** The conversation's OWN value wins on resume/fork/restart. The instance default (`<prefix>.defaultModel` / `.defaultEffort`) is a NEW-session default. An explicit per-session pick (card ⚙, Session Properties, the New Session dialog's pickers) still beats both.

**The shape.** One PURE module, `src/resume-continuity.js` — `resumeSpawnPick({explicit, conversation, instanceDefault, resume, hasSource})` → `{value, origin}` with `origin ∈ chosen | conversation | instance | harness` — and TWO callers:
- **the client** (session-lifecycle's `createSession`) calls it with `hasSource:false`, which is the honest client-side fact: only the server can read a conversation's own records. On a continuation it resolves to `''` and the client sends NOTHING. `permission`/`extraArgs` deliberately keep master's behaviour (the ruling covers model+effort; a permission mode IS recoverable from claude's init record, so changing it is its own product decision ⇒ owner gate).
- **the server** (ws-create, immediately BEFORE `buildSessionArgs`) calls it with the conversation's value read through the harness descriptor's `store.lastTurn<Knob>` hook, and writes the resolved values back onto `data.model`/`data.effort` so every downstream reader (pool chooser's declared model, `_spawnModel`, the model lock's implicit target, `session._effort`) sees the value the session really starts with.

**`hasSource` is not a second declaration.** It is derived from the descriptor hook's PRESENCE, so a harness either ships the reader or it does not — there is no `resumeContinuity` caps boolean to drift out of sync with it. codex ships both (`lastTurnModel`/`lastTurnEffort` → the rollout's last `turn_context`, the B-21e4 readers moved onto the descriptor); **claude declares NEITHER** (round 2 — round 1 shipped a model reader and it is removed, see below); **opencode declares the model** (`serve.facts().sessionModel` — OpenCode's own session record names it, v1 list only so a resume adds no route the sidebar poll does not already use; `''` when the opt-in background service is off, which falls to the instance default and is logged). A generic ACP harness declares neither, and `src/harnesses/acp.js` says why in place rather than leaving it to be re-derived.

**THE ASYMMETRY, stated because it looks like an omission.** On a knob with NO conversation source, a resume sends **nothing** rather than the instance default — nothing claude writes records the effort a turn ran at, so an unconditional fallback there would mean "the instance default applies to every claude resume", which is the defect itself wearing a fallback's clothes. With a source that came up EMPTY (a thread resumed before its first turn) the instance default IS honest, and the ladder's caller logs the decision (`continuityLogLine`) so a resume that quietly took the default is readable afterwards.

**The origin is STATED, never derived.** A conversation's own value and the instance default are frequently the same string, and only the server ever read the conversation's records — so the panel cannot work out where a value came from. On a NEW session the client resolves the instance default itself, and the wire cannot tell that apart from a pick, so it also sends `spawnOriginHint` and the server reconciles through `applyOriginHint` (round 3 ②). The server records `_modelOrigin`/`_effortOrigin` on the session, persists them in session-meta (all three boot-restore paths — and every writer that RE-AUTHORS one persists it too, round 3 ①), and ships them on the `created` reply (the creator never gets an `attached` — 2.368.4), the attach payload, and the active-sessions payload. `spawnValueOrigin(stated, live, picked)` in agent-meta only decides how to SHOW it: a pick saved AFTER the spawn still flips the row to `spawn`, so the origin can never contradict the "(saved: X — applies on the next resume)" note beside it, and a session that predates the field falls back to `responseStyleOrigin`'s comparison rather than claiming a fact we do not have.

**Two twins fixed on the way past.** (a) `outputStyle` was added to the active-sessions payload in 2.369.58 but never to the sidebar MERGE, so Session Properties' "live" response-style value was silently always empty and every session read as `saved`; the B-6b6d rows land in both merge branches WITH it. (b) On the `created` reply the effort now rides along with the origin, because on a resume the client deliberately sends none — without it the chip on a resumed window would stay empty.

**Boundaries, stated rather than discovered later.** ① A REMOTE resume reads the conversation's records out of the local transcript CACHE (both locators already scan `data/remote-jsonl`), so its answer is as fresh as the last fetch — still the conversation's own value, just possibly an older one; the alternative (an unrelated instance default) is strictly worse, and this is the behaviour the codex twin has had since 2.369.32. ② The read costs one directory scan + one bounded tail read per resume, and only when the client sent NO pick — an explicit pick short-circuits it, because the ladder would discard the answer anyway. ③ `permission` and `extraArgs` still take the instance default on a resume: the ruling covers model+effort, and a permission mode IS recoverable (claude's init record), so moving it is its own product decision.

**Invariants.** ① An instance default is a NEW-object default; on a continuation, the object's own recorded value wins, and an explicit per-object choice wins over both. ② A fallback gated on "the caller sent nothing" is dead code for as long as some other layer always sends something — when you add such a gate, name the layer that must stay silent and pin it. ③ Do not declare the same capability twice: derive "can this harness answer?" from the presence of the reader, never from a parallel boolean. ④ Where a value came from is a FACT the resolver holds and no one downstream can reconstruct — state it on the record instead of guessing from the value. ⑤ A knob with no recoverable history must send nothing rather than a default, or the default becomes the answer for every continuation. ⑥ A row that states two facts must not let the ellipsis eat the second one (375×667: the origin is the last thing on the line — measured, then wrapped).

**ROUND 2 (adversarial verifier: 1 major + 4 medium/low, every one reproduced on real data first).**

**① THE CLAUDE MODEL SOURCE IS GONE, NOT REPAIRED (major + medium, one root cause).** Round 1's reader answered a different question from the one the ladder asks. The ladder asks *what should this resume COMMAND*; `message.model` says *which model SERVED that turn*, and it says it in a form that cannot express the conversation's context-window variant. Measured over the whole local corpus — 2650 transcripts, 3.5 GB under `~/.claude/projects` — **zero** `"model":"…"` values carry a `[…]` suffix, while the CLI's own `system/model_refusal_fallback` records prove conversations running `claude-fable-5[1m]` (5 sightings). So `--model claude-fable-5` on the resume of a `claude-fable-5[1m]` conversation silently turns 1M of context into 200k — on a path where master commanded nothing at all, since the client had just stopped sending `claude.defaultModel` there. It was also wrong about the model itself: a classifier reroute is recorded as an ASSISTANT record whose `message.model` is ALREADY the fallback target and which carries a `{type:'fallback',from,to}` content block, and the `system/model_refusal_fallback` record round 1's guard looked for is written **2–10 lines AFTER it** — so the ≤20-line BACKWARD look-back matched **1 of 139** reroutes in the owner's transcript, while **13,694 of 81,707** main-thread assistant records (16.8%, median run 87, max 1647) sit inside such a reroute run, i.e. ~1 in 6 interruption points would have resumed pinned to the fallback (and `_spawnModel`/`familyOfModel` would then have sent the plan-C pool chooser after an Opus-quota account for a Fable session). Round 1's own suite stayed green because its fixture wrote the system record BEFORE the assistant record it describes. **Fixing the guard would have left the variant loss**, and a rung that can never produce a commandable answer must not ship as the capability (the 2.369.66 rule), so the SOURCE goes: `lastClaudeTurnModel` and the claude descriptor's `store.lastTurnModel` are deleted, a claude resume commands NEITHER knob, and the CLI's own session record — which is variant-exact — decides. The asymmetry stops being an odd exception and becomes one sentence: **claude records neither knob in a form a spawn can command.** The reason lives in `src/session-store.js` where the reader used to be, so the next person to want one reads it first.

**② AN EXPLICIT "Auto (model default)" MUST SURVIVE (medium).** The New Session dialog always sends a defined string and its FIRST option is `''` (“Auto (model default)” / “Default”). Round 1 collapsed `''` into “no pick” in both tiers, so choosing Auto resolved to the instance default — measured: `{model:'', effort:''}` with `codex.defaultEffort=xhigh` reached `buildSessionArgs` as `effort:'xhigh'`, where master spawned with no flag at all. `''` is now a STATED choice on a NEW session (`{value:'', origin:'chosen'}`) and the client sends the resolved value VERBATIM there so the empty string survives the wire; ws-create forwards `explicit` untouched instead of normalising it. On a CONTINUATION the distinction genuinely is not available (the create message carries `model: sessionModel || undefined` on that path) and `''` still means the same as silence — documented in the module rather than left to be rediscovered.

**③ OPENCODE CAN ANSWER, SO IT DOES (medium).** With no `lastTurnModel`, an OpenCode resume stopped applying `opencode.defaultModel` and — unlike the claude-effort asymmetry, which is stated in three places — nothing took its place and nothing said so. OpenCode's own session record names the model, so the store now ships the reader (`sessionModel`, built on the same cached v1 `listNow` the sidebar `discover` uses: no new route, no per-directory instance — the 2.369.50 rule). The background service is opt-in and default OFF, so `''` is the common answer, which takes the instance default and logs which rung it used. The setting's own description says all of this.

**④ AN ORIGIN WE DO NOT HAVE IS NOT SHOWN (low).** `spawnValueOrigin(null, live, undefined)` fell through to `responseStyleOrigin`, which returns `'instance'` for any live value with no saved pick — so every session restored from an older session-meta asserted “(instance default)” as fact, including ones whose model came from the New Session dialog, and the helper's own comment said it declined to claim one. With no stated origin the helper may now only claim what the PICK proves (`spawn` / `saved` / `chosen`); otherwise it answers `'unknown'` and Session Properties renders the value with **no parenthetical**.

**Round-2 invariants.** ⑧ A reader that cannot express the thing it is read back INTO is not a source — measure the field's value space before wiring it to a spawn, and delete the source rather than stacking a second guard on it. ⑨ A guard whose window is derived from an assumed record ORDER must be built from records in the order the producer really writes them; a fixture that encodes the assumption will keep it green forever. ⑩ `''` and absent are the same thing only where the transport cannot tell them apart — say where that is, and do not collapse them anywhere else. ⑪ “No source ⇒ send nothing” is right, but it silently changes behaviour for every harness that acquires it, so each one either gets a real reader or gets the sentence written down where its own descriptor lives. ⑫ A helper that declines to claim a fact must not delegate to one that will.

**ROUND 3 (adversarial verifier: 2 medium + 1 low, each reproduced against the real module before it was touched).**

**① THE RE-AUTHORED ORIGIN WAS NEVER PERSISTED (medium).** `src/server/stdout/codex-events.js` re-authors `session._effortOrigin` when the wrapper reports an effort we did not command (`'conversation'` — the thread's own) or one that moved away from a value we did (`'chosen'` — a `/effort` typed inside the session), but its `writeSessionMeta` listed `effort` and not `effortOrigin`. Reproduced on the real consumer through `src/server/session-stdout.js` with the spawn state ws-create really persists for a codex resume whose rollout said `ultra` (`{effort:'ultra', effortOrigin:'conversation'}`, verified on disk): after `/effort high` memory says `high / chosen` and the DISK said `high / conversation`, and `boot-restore.js` reads `meta.effortOrigin` — so after a restart Session Properties rendered `high (this conversation's own value)` for a value the user had just changed by hand. That is verbatim the contradiction the origin exists to remove; the field's own schema row claimed `persisted:'meta'`, which was only half true. The write now lists `effortOrigin`. It deliberately does **not** list `modelOrigin`: nothing in this consumer authors that key, the object spread above already carries it through, and re-listing a key a writer does not own would stamp `null` over a real disk value for any session object that lacks the field — pinned as a negative control rather than assumed.

**② A NEW SESSION COULD ONLY EVER RECORD 'chosen' (medium).** For a NEW create the CLIENT resolves `<prefix>.default*` (it needs the value for its own chip, and it owns two facts the server does not: the legacy `session.defaultEffort` key, honoured only while `<prefix>.defaultEffort` is UNMODIFIED, and `settings.isModified` itself) and — since round 2 — sends it VERBATIM so a stated `''` survives. The server therefore saw a bare string it could only read as an explicit pick: `_modelOrigin`/`_effortOrigin` were **always** `'chosen'` on a new session, `'instance'` and `'harness'` were unreachable there, and Session Properties labelled `<prefix>.defaultModel` “your choice for this session” on every create path that shows no picker at all — `openShellTerminal`, the toolbar, `setup-flows`, `manage-agents` (the branch's own browser leg printed the evidence: `wire-none {model:'gpt-5.6-sol', effort:'xhigh'}` for a create that supplied nothing). The fix is not a guess: the client already computed the origin and threw it away, so it now rides along as `spawnOriginHint` and `applyOriginHint(origin, hint)` (PURE, in the same module) reconciles. It may only **downgrade** our own `'chosen'` to `'instance'`/`'harness'`; every other rung was decided by the server from facts the client never had, the value is untouched, a hint of `'conversation'`/`'chosen'`/garbage is ignored, and a client that sends none keeps master's answer.

**③ A CLAUDE RESUME LOST ITS POOL PLACEMENT (low).** Round 2 deleted the claude model source, so `data.model` is undefined on a claude resume — and the pooled-spawn chooser (`poolChooserForModel`) short-circuits on `familyOfModel(null)` **before** `decidePoolSwitch`, placing the session on the pool's current target with no evaluation at all. Reproduced on the REAL engine with a REAL `AccountManager` pool and real caches in the 2.305.0 shape (5h/7d fine, the current target's Opus weekly cap spent): master's input placed the session on the healthy member, the branch stranded it on the spent one — and a claude pool switch does not reach a running CLI for ~25 min (2.361.0), so the spawn-time placement is the one that counts. **Placement is not commanding**: the chooser only wants a model FAMILY (`'fable'`/`'opus'`/`'sonnet'` — coarse by construction, no point release and no `[1m]` variant, which is exactly why the variant argument that killed the reader does not apply here). `data._placementModelHint = data.model || <prefix>.defaultModel || null` restores the input master had and reaches the chooser call site and nothing else — never `data.model`, never the spawn spec, never the argv (pinned). With the setting unset the hint is `null` and behaviour is master's. **`session._spawnModel` is deliberately left null on that path** and must stay so: it records what the spawn COMMANDED, and the spawn commanded nothing — writing the hint there would put a value we never sent into the attach payload, session-meta, the restart chip and `sessionModelFor`'s floor. The running session gets its real answer from `_servedModel` at the first assistant record; the gap the hint closes is the one moment before that, which is exactly the placement decision.

**Round-3 invariants.** ⑬ When one tier resolves a value the other tier must classify, the wire has to be able to carry *which fact it is* — a bare value is indistinguishable from a choice, and the reconciliation belongs in the same pure rule as the resolution (a hint may only weaken the receiver's own verdict, never re-point a fact only the receiver holds). ⑭ A writer persists the keys it AUTHORS and leaves the rest to the spread; listing a key you do not own is a silent `null` over somebody else's fact, and the difference is a negative control, not a comment. ⑮ Deleting a source removes it from every consumer, including the ones that only needed a WEAKER projection of it — before removing a reader, enumerate the readers of its output and ask what each of them actually needs (family vs exact id). ⑯ An ordering assert must first assert that both sites exist: `indexOf` answers `-1` for absent text and `-1` is less than everything, so the pin goes green exactly when the code it orders has been deleted (measured — this assert survived its own mutation run before it was fixed).

**Gates.** scripts/test-codex-effort-meta.mjs 245 (leg ⑪ = the PURE truth table incl. the stated-empty rule, the end-to-end rollout→descriptor→ladder→adapter-env→REAL wrapper run whose FIRST synthesized `turn_context` already says `ultra`, the pre-fix client replayed as the negative control, the entry-point audit, and the origin plumbing; leg ⑪e = round 2 — the claude reroute/variant DATA pins built from the real record order with round 1's fixture order as the negative control, the claude “neither knob” behaviour, the stated-empty create, the opencode reader against a fake serve, and the `'unknown'` origin row; round 3 = leg ⑦'s second bench (the real stdout consumer with the real spawn state on disk, the restored-vs-live claim compared through the real label rule, and `modelOrigin` surviving the write untouched), leg ⑫ (the `applyOriginHint` truth table + the client→JSON-wire→server composition, with the hint removed as the negative control that reproduces “everything is 'chosen'”), leg ⑫c (both halves pinned against source, incl. the placement hint never becoming a command) and leg ⑬ (the REAL pool engine: the fix places on the healthy member, the pre-fix chooser argument strands on the spent one, master's input agrees with the fix, and an unset setting is master's behaviour)) + test-auto-resume (129, the 2.368.4 `created`-payload law) + test-codex-0153 (93) + test-owner-batch-2369-32 (28) + test-architecture (82, the new PURE module) + test-pool-auto (67) / test-account-pool (33) / test-account-verdicts (45) / test-codex-pool (61) for round 3's placement leg + test-session-schema/test-ws-contract/test-harness-contract (132)/test-stdout-registry (58)/test-wrapper-files. Mobile: scripts/dbg-session-props-mobile.mjs (375×667, real server + headless chrome) — round 2 adds the pre-upgrade session (Model/Effort rows carry the value and ZERO dim spans, 18px vs the stated row's 32px, no overflow) and a WIRE leg that drives the real `createSession` in the page with `ws.send` swallowed (nothing reaches the server, no agent is spawned): a NEW create carries the stated empty verbatim, a create that supplied nothing carries the instance defaults, a continuation sends neither key. Round 3 adds the `spawnOriginHint` each of those four creates emits (`instance` / `chosen` / `harness` / `harness`) and the row the fix makes reachable at all — `'instance'` was unreachable for a new session, so “(instance default)” had never been rendered on a phone: both twins 18px, unclipped, `contentOverflowX`/`docOverflowX` = 0.

## THE MERGE ITSELF WAS THE DEFECT (integration r2, 2026-09-07: readings-by-slot + B-6b6d + login-expiry landing together)

Three findings from an adversarial review of the INTEGRATED tree. None of them exists on either chain alone — each is a property of the merge, and each was reproduced before it was fixed.

**① MAJOR — pre-filtering the evidence out of the decision degraded the notice (usage-pool-engine).** Chain A hardened `healthyPoolMembers` to drop any member `src/login-state.js` (the FILE reader: "can a request be made right now") calls unusable. Master 2.369.67 had, the same day, shipped the pool's login attribution — and it BUILDS that attribution from the members the decision was SHOWN: `decidePoolSwitch` pushes each login-blocked member into `loginBlocked` and only then may claim `all-logins-expired`. A member removed before the call can never be named. Reproduced on the doubly-expired shape (`accessToken` present, both `expiresAt` and `refreshTokenExpiresAt` in the past — the satisfiable gap `login-state.js`'s own header documents, and the one shape `parseAuth` still reports `loggedIn:true` for, so `poolMembers()` keeps it):

- master, unfiltered: `Pool "MyPool": no member can take it — Bravo (login expired), Charlie (login expired). Re-login those accounts in Manage Agents.`
- merged, pre-filtered: `Pool "MyPool": no member can serve it — spent: 5h 1% (still available: 7d 50%). Conversations on it will hit a limit until a window resets, you add a member, or you move them off the pool.`

i.e. the one wall a user clears in 30 seconds, reported as a spent quota bucket with the quota remedy — verbatim the defect 2.369.67 exists to fix. Both suites stayed green because **neither crosses the seam**: every `all-logins-expired` assert in test-login-expiry calls `decidePoolSwitch` DIRECTLY with a hand-built member list, and test-readings-attribution pinned only that `healthyPoolMembers` returns `[]` for an all-unusable pool, never what the notice then says.

**Fix — fold, do not pre-filter.** `poolReadLogin()` (the memoized reader every gate already consults) now asks BOTH modules and takes the worse answer: `accounts.loginStateOf` (src/login-expiry.js — the refresh-token DEADLINE) and `memberLoginState` (src/login-state.js — the FILE). It is only ever a DOWNGRADE, and it keeps the file reader's own numbers (`since` is the instant it stopped working, so `msLeft` stays a coherent negative number instead of an 'expired' state carrying "30 days left"). Decision sites take a new `switchCandidates(poolId)` — the 2.335.0 auth-fail escape, no login pre-filter. ACT sites (`notePoolAuthFailure`'s `alive`, `poolChooserForModel`) keep `healthyPoolMembers`' hard exclusion, because there the list is a set of re-point targets. **Exclusions are byte-identical; the attribution came back.** The fold is load-bearing, not cosmetic: the shape only the FILE reader can see (access token expired, refresh token GONE) reads 'unknown' = no claim to the deadline reader, so without it the naming would have cost the exclusion.

**② MEDIUM — a sub-line resurrection in the auto-loaded LAW index (CLAUDE.md).** The hand-merge resolved the Pool/billing routing cell by keeping "both sides verbatim", which took master's copy — still carrying base text chain A's round 3 had deliberately deleted. The cell then stated the refuted rule (`quota VALUES keep the observed-org routing`) twenty characters before its own refutation, while the shipped tree routes every value through `readingSlotFor` and leaves `setTruthLookup` unwired on purpose. The routing row is ONE 3.3 KB line, so the revert is SUB-LINE: a line-granularity lost/resurrected check reports zero. Fixed by deleting the superseded paragraph (the following one subsumes it and is strictly more correct); the historical kb essay keeps the rule in the PAST tense ("kept", not "keep") — that tense is the whole difference between a record and a rule.

**③ MEDIUM — a lying gate: the version-marker leg cleared a same-topic PREDECESSOR (test-codex-effort-meta ⑩).** The leg carried `MARK = '2.369.62'` as "this change's marker" and cleared any claimant matching `/effort|ultra|xhigh|turn_context/i`. Master had ALREADY RELEASED 2.369.62 — for this chain's own predecessor ("codex effort a TURN ran at … test-codex-effort-meta 108; B-6b6d filed for the resume-default half") — which matches that regex BY CONSTRUCTION, because the sites cite it precisely for being on that topic. So the assert went green while asserting something false. **A topic regex cannot separate a predecessor release from its own follow-up.**

The remedy was NOT a renumber: master's 2.369.62 is an ancestor of HEAD and every 2.369.62 in the tree is a correct BACK-REFERENCE to it, so rewriting them to an unreleased number would MANUFACTURE the dangling cross-reference the leg exists to prevent. The number was playing two roles the leg conflated, so it now asks two questions: ① the back-reference must RESOLVE (the release exists on the integration branch and is about the work the sites cite it for); ② this branch is unreleased and stamps no number of its own, so the leg names the first free number (the suite's `NEXT_FREE` constant — the only place it may be written, since a doc that hardcodes an unassigned number is the very cross-reference this leg forbids), PROVES it is unclaimed via git, and runs a census asserting every `2.369.x` named in the cited files is a release that really exists. `describesThisChange` was tightened to markers only this change carries (`resumeSpawnPick` / `resume-continuity` / `resume ladder` / `spawnOriginHint` / `readingSlotFor` / `readings-by-slot`) — deliberately NOT `B-6b6d`, which the predecessor's own subject already names.

**Invariants.**
- **Evidence must survive to the step that judges on it.** Removing a member before `decidePoolSwitch` removes what the refusal is able to SAY; fold a second verdict into the reader the gate already consults instead, and keep the hard exclusion only where the list is acted on.
- **Two suites that each pin one half of a seam are not a gate for the seam.** The crossing leg asserts the SERVER NOTICE STRING end to end (`healthyPoolMembers → decidePoolSwitch → poolBlockedNotice`), because that is the artifact the user reads.
- **The auto-loaded index is a source pin.** It gates the NEXT change, so a superseded paragraph there outranks any essay that contradicts it — and in a one-line table cell a resurrection is sub-line, invisible to line-granularity checks. Pin the row against the CODE that refutes it.
- **A version marker is a CROSS-REFERENCE: it either resolves on the integration branch or it is a defect.** One number cannot be both "a back-reference" and "this change's stamp" — one of the two halves will be false.
- **"Same topic" is not "same change."** A predecessor and its follow-up share every topic word; distinguish them with a marker only the follow-up carries.
- **An assertion that can pass vacuously is not an assertion.** Even "this number is free" has to be proven against git, or it degrades into a comment.

**Gates.** scripts/test-readings-attribution.mjs 194 (§5b = the crossing leg + three negative controls: an all-quota-dead pool must keep the quota sentence and never say re-login; the file-only-dead shape must be excluded AND named; the two lists must stay two different questions — plus a wiring pin, the 2.355.0 unstaged-wiring class. §9b = the LAW-index pin with the pre-fix row as its negative control, and an explicit allowance for the historical essay). scripts/test-codex-effort-meta.mjs 245 (⑩ rewritten: predecessor resolves / next number proven free / census over the cited files / three negative controls — the same-topic predecessor passing the old predicate and failing the new one is the permanent proof the blindness is gone).

## THE EFFORT A TURN RAN AT — OUR SYNTHESIZED TWIN OUTLIVED THE TRUTH (2.369.62, owner "我刚才把那个van的对话调成了 ultra, 但我看它回复怎么都 metadata 显示的是 xhigh?")

**Symptom.** A conversation the owner had set to `ultra` reported `xhigh` in the message-metadata popup on every single reply, for the whole session.

**What the evidence said.** codex's own rollout carries `turn_context.effort = ultra` on every turn since 2026-09-06T20:33 (21/21, plus its own `thread_settings_applied` records), the usage ledger says `ultra`, and `data/session-meta` said `ultra`. But the LIVE buffer of that session — 1137 records — contained exactly ONE `turn_context`: the wrapper's synthesized copy at `07:06:44.566Z`, effort `xhigh`. codex's own copy of the SAME turn says `ultra` at `07:06:46.484Z`.

**Root cause (three faults stacked).** ① The wrapper synthesizes the `turn_context` every reader takes a turn's effort from, at `turn/started` — and 0.153.4's `TurnStartedNotification` is `{threadId, turn:{id,status,…}}` with **no effort and no model**, so the value comes from our own bookkeeping. It printed the mutable `effort` var = the SPAWN ENV, which on this resume was the instance default `codex.defaultEffort = xhigh`. ② The turn was started by the APP-SERVER (an auto-continue on resume: `goal_cleared`, `turn/started`, then the `thread/resume` reply — all inside 07:06:44.565-.566Z), so we never sent `turn/start` and that value had never reached codex at all; the reply carrying `reasoningEffort:'ultra'` landed 1.9ms LATER and only assigned `meta.effort`, leaving the emitted record and the `effort` var alone (the adoption at updateMetaFromThread is gated on `!effort`). ③ That single record was the only `turn_context` in the buffer — the app-server steered and drained everything inside that one turn — so `CodexMessageManager._status.effort` stayed `xhigh` and every `token_count` baked `xhigh` onto every message. A fourth fault kept it from ever healing: `mergeCodexRecords` fingerprints a turn_context on its turn id and kept the FIRST copy, and ours is always the earlier of the twins — so codex's authoritative copy was suppressed on every rebuild, forever.

**Fix.** Three named facts replace one var in the wrapper (`effort` = commanded-next, `threadEffort` = the app-server's own word, `turnEffort` = what the ACTIVE turn runs at); a turn WE start uses what we put on `turn/start` (which also re-points the thread, per TurnStartParams' own doc, reverted on refusal), a turn the app-server starts uses the THREAD's; `noteTurnEffort()` RESTATES the same turn id when the resume reply lands late; `wrapper_meta` carries the `{effort, effortNext}` pair (mid-turn attach, no rollout re-read); `set-effort` now also sends `thread/settings/update {effort}` so a turn the app-server starts on its own runs at the value the UI is claiming; the merge folds a repeated turn_context into the first copy but takes the better copy's VALUES (codex's own always wins); and the normalizer splits `_status.effort` (this turn) from `_status.effortNext` (pending) with one writer that also pushes a live `meta/effort` op.

**Invariants.** ① A record that states what a turn IS RUNNING AT may only carry a value that reached the agent — the pending/commanded value belongs in its own field, and a pick made mid-turn never relabels messages the running turn already produced. ② When we synthesize a twin of a record the agent also writes, the agent's copy MUST win the fold — ours is structurally the earlier one, so first-wins silently makes our guess permanent. ③ A knob the UI presents as "applies from now on" must be told to the party that will act on it (`turn/start` alone is not enough when the app-server starts turns by itself). ④ A liveness/state check must read the process's own immediate statement, not a debounced sidecar — the 200ms `scheduleMeta` lag is enough to queue a message into the turn you just stopped (caught by this fix's own test bench). ⑤ 'ultra' is a MODE, not a level: state both facts from the catalog or state only the mode; never hardcode the level.

**Known residue — CLOSED by B-6b6d (owner ruling 2026-09-07, essay below). The paragraph is kept because it is the diagnosis the fix was built from.** `codex.defaultEffort` is documented as applying to "new **or resumed** Codex sessions", and the client sends it as `data.effort` at `src/lib/session-lifecycle.js` (`sessionEffort = effort !== undefined ? effort : defaults.effort`), which suppresses ws-create's B-21e4 EFFORT-CONTINUITY fallback (`lastCodexTurnEffort`) — whose whole purpose is that a resume "can never flip a conversation that was running at high/ultra". **Precise scope** (r2 review corrected the first wording, which read as if every resume were affected): the card's gear popover writes a per-session override and `resumeSession` forwards it, so the bite is limited to resume paths that supply nothing — the **chat resume bar**, **resume-all**, and any card with no saved override — where `savedCfg.effort` is `undefined` and the instance default takes its place. The **MODEL twin is the identical line** (`sessionModel` / `lastCodexTurnModel`, ws-create.js:394) and is unchanged for the same reason. Flipping the priority ("only apply the instance default when there is no `resumeId`") is a product-default decision ⇒ owner gate, and it must move effort and model together (STANDING SWEEP). What this release does instead is make it VISIBLE and stop it corrupting the record: the session's saved effort is now honest (the wrapper's own value reaches `session._effort` + session-meta, so a `/effort` typed into the chat no longer leaves a stale value behind for the next resume), and the status-bar tooltip says which value the turn in flight is really running at. Leg ⑪ pins both twins plus the mechanism itself (the adapter really does leave `CODEX_WEBUI_EFFORT` empty when nothing is supplied, and really does fill it when the default is), so a half-fix fails red.

**Round 2 (adversarial verifier; four defects, all reproduced before fixing).**
- **① The version marker named someone else's release (major).** The branch stamped `2.369.61` into 36 places across 17 files while master had ALREADY SHIPPED `2.369.61` (Alt+Enter = steer) — 16 seconds before this branch's own commit. Same class as the 2.369.58 r2 renumber off `2.369.54`. Renumbered to **2.369.62** in one pass. The belt that existed (test-harness-honesty's CHANGELOG rule) could not see it: a branch that is not rebased has no CHANGELOG entry for EITHER number, so the check passes vacuously. **The number is claimed on the INTEGRATION BRANCH, so the check has to ask git** — leg ⑩ scans `origin/master`'s release-commit subjects AND its CHANGELOG headings for the marker and fails unless the claimant IS this change, with the old number `2.369.61` as a standing negative control (it must still read as claimed-by-someone-else, or the check is blind). No ref ⇒ explicit SKIP, never a silent pass.
- **② `effortNext: null` is a statement, not a gap (minor).** The new session-meta writer read `payload.effortNext || payload.effort` — re-conflating the two facts the release exists to separate. Picking "Auto (model default)" makes the wrapper clear the thread's effort and publish `{effort:'<the last turn ran at>', effortNext:null}`; the fallback then wrote the last turn's LEVEL into `session._effort`, session-meta, the attach payload's `chatStatus.effortNext`, the chip after a restart and the next resume spawn — overwriting the `null` ws-handler had just written on the same click. Now `effortNext` is the only field read, and only when present (`undefined` = an old wrapper says nothing ⇒ master's behaviour, the 2.361.1/2.364.1 skew rule).
- **③ A record in codex's spelling that codex refused (minor).** `set-effort` emitted `thread_settings_applied` — codex's OWN rollout shape — unconditionally, including after `thread/settings/update` was rejected, and unmarked. Our payload carries 5 `thread_settings` keys where codex's real one carries 9 (`model_provider_id`, `approvals_reviewer`, `collaboration_mode`, `permission_profile`…), so the two can never share a fingerprint and a rebuilt history kept both forever. Now: recorded only when the RPC was ACCEPTED (a refusal leaves the per-turn `turn/start` fallback, and the pending pick still reaches every client through `wrapper_meta.effortNext` — a statement about US, which a refusal leaves true), and both wrapper-authored copies carry `wrapper: true` like the synthesized `turn_context`. `mergeCodexRecords` folds the twin the same way (codex's copy wins), keyed on the stated VALUES and scoped to ADJACENCY — high → ultra → high is a real sequence, and folding the third into the first would leave a rebuilt status reporting `ultra`.
- **④ The disclosed resume gap needed an owner, not a paragraph (minor).** Filed as B-6b6d with the MODEL twin; scope corrected above.

**Round-2 invariants.** ⑥ A version marker is a CROSS-REFERENCE: it must resolve on the branch it will be integrated into, and the only source that knows is git — a CHANGELOG check on an un-rebased branch is vacuous by construction. ⑦ `null` from a producer that always sends the field is a VALUE ("nothing pending"); only `undefined` is absence. Never `a || b` across two fields that mean different things — that is the exact conflation the incident is about, re-introduced one layer down. ⑧ Never write a record in another party's spelling for something that party did not do; if you must synthesize one, MARK it, and let the real author's copy win the fold.

Gate: scripts/test-codex-effort-meta.mjs (108) — the race reproduced against a stub app-server with the REAL wrapper, plus per-defect regression legs with negative controls (master's own record shapes; a REFUSING app-server; `effortNext:null` vs an old wrapper's absent field; adjacent-vs-sequential settings twins; the squatted version number).
## B-7638 — THE SEVEN REVIEW RESIDUALS (the 2026-09-06 review-fixes-r2 verify; every one reproduced before it was fixed)

Three fixes shipped in that batch each left something behind that the verify pass named and the repro confirmed. They are unrelated in code and identical in shape: **a rule was written for the state the incident happened to be in, and the state it drifts into afterwards was never asked about.**

**① The over-cap adoption trusted a 4 KB window (src/hosts.js).** The remote-transcript cache heals a pre-provenance slot by refetching it whole, with ONE exception: a remote already past `maxBytes` cannot be refetched at all, so a byte-verified cache adopts the provenance instead of failing "too large". "Byte-verified" read the last 4 KB — sound at the moment of a splice, because an append lands its foreign bytes at the TAIL. But the pre-fix delta path kept appending AFTER it spliced, so on any long-lived slot the marker is buried under later appends and a tail read calls the hybrid verified — forever, because a stopped thread never changes again. The whole-file scan (1 MiB chunks, 3-byte carry so a zstd magic split across a boundary is still seen) is the evidence such a slot owes. **Round 2 corrected WHICH slots owe it.** The first cut gated the deep scan on "the meta carries no `remotePath`" — but that is exactly the provenance the *shipped* tail-only adoption used to stamp: it adopted a buried hybrid and the delta path then wrote `remotePath` onto that very slot, so `sameRemote` is TRUE for the corruption the scan exists to find, and every slot that passed through the ⑥ window would have been served on a tail check forever. The gate is now a meta SCHEMA MARKER (`v: 2`) written by every meta writer in `_fetchRemoteByFind`: any meta from before this fix pays ONE whole-file scan the first time its bytes are trusted, and is re-stamped so the cost is paid once per slot, never per poll. A refused slot is left unstamped.

**① b The cap error's remediation could not work (src/hosts.js).** The same batch appended "delete it to re-sync" to the "too large" error when the cache did not verify. The suffix is reachable ONLY when the remote is past the cap — where a whole refetch is impossible by construction — so following the advice destroyed the last local copy and failed identically (verified: delete cache → same error; delete cache AND meta → same error). It now states the true situation: no prefix to grow from, the remote is past the *N* MB cap, and deleting the cache would not help. Advice in an error message is a claim about the system; it needs the same evidence as any other claim.

**② The ssh rung could not grow an over-cap slot (src/hosts.js).** Both early-return branches handed the verified cache back and left the meta unstamped, so the whole-file scan repeated on every poll; `stampVerified` (remotePath + compressed + `adopted` + the schema marker) on both rungs ends that — **and that is all the stamp does**. The round-2 verify caught the first cut claiming more: the *harm* here was that the legacy ssh rung hard-failed "remote transcript too large" on the very next byte of growth, and stamping provenance changes nothing on that path (the short-circuit needs `meta.size === size`, so any growth falls straight into the cap throw, with no fallback to the verified cache — a >64 MB transcript that opened yesterday was an error today). The real fault was a TWIN GAP: the slab rung has grown an over-cap slot by an append-only delta since 2.187.0 and the ssh rung only ever whole-`cat`ted, so the same conversation behaved differently depending on which transport happened to serve it. The ssh rung now carries the same delta (`tail -c +N`, 1-based on GNU and BSD), under the same legality test (same remote file, uncompressed on both sides, cached prefix no longer than the remote) and the same never-stamp-bytes-we-did-not-get rule — with one refinement the last rung needs: a read that OVERTAKES the stat (a live transcript grew mid-poll) keeps the extra bytes, because an append-only file's extra tail bytes are genuinely its next bytes, and stamps what the cache actually holds; only a SHORT read throws.

**② b ROUND 3 — the three residuals of the round-2 delta itself (src/hosts.js).** The verify that passed round 2 named them, and each was reproduced before it was fixed.
  **(i) The compression-legality clause had no negative control on either rung.** `canDelta`/`canDeltaSsh` both carry `!isZstPath(remotePath) && !cacheIsCompressed()`, and DELETING it left test-codex-zst ALL PASS — the suite only ever exercised the twin-SWITCH (plain ⇄ .zst), never a growth with BOTH sides compressed. That case is the innocent-looking one and the worst: a host re-compresses a finished rollout, so the remote keeps its path, the cache holds .zst bytes that verify (for a compressed slot the magic IS the evidence), and the cached archive is shorter than the remote — every other delta precondition holds. The "delta" would weld a foreign frame tail onto the old frames; `zstdDecompressFrames` then stops at the earlier frames and the slot serves the OLD transcript, with the growth silently gone. Both rungs now carry a both-compressed growth leg (⑥d) whose asserts go red when the clause is deleted from EITHER copy — the two legality tests are twins, and a guard only one copy carries is the drift this batch keeps paying for.
  **(ii) The delta REPLACED the last rung's whole `cat`.** Round 2 added `tail -c +N` to the ssh rung as an `if/else` against the whole-`cat` branch, so a `tail` that FAILS — the remote truncated/rotated between the stat and the read, or a host whose `tail` has no `-c +N` — now hard-fails a fetch the pre-r2 code completed, and this is the LAST rung: nothing under it to fall through to. A delta failure now falls back to the whole `cat` when `size <= maxBytes` (logging the failure VERBATIM, per the degrade-path rule), and the hard error is kept ONLY over the cap — where a whole re-pull is impossible by construction and the delta really was the only road, so the fallback can never become a cap bypass. The over-cap short-read hard failure from round 2 is unchanged.
  **(iii) The meta writers rebuilt provenance instead of carrying it.** Both rungs stamped a FRESH object at the end of a fetch, so the very next delta erased `adopted` (the marker meaning "these bytes were VERIFIED, never fetched") and the `slab` lane marker — the exact opposite of the durability ① claims, and it landed on the one slot class that can never re-earn it (an over-cap slot cannot be refetched to prove itself again). One shared `nextMeta(fields, {whole})` now spreads the prior meta on BOTH rungs; only a WHOLE refetch clears those two markers, because they describe bytes that are no longer in the slot.

**② c ROUND 4 — TWO POLLS, ONE SLOT, ONE OFFSET (src/hosts.js).** Both rungs are now read-then-append against a size measured BEFORE the remote read, and nothing serialized the SLOT. Two overlapping fetches of the same conversation — the 5s session poll and a user opening the window, two clients, goal-sync and an attach — each read `fs.statSync(cachePath).size`, each asked the host for `[localSize, size)`, and each appended it: the cache grew a DUPLICATED region. That much self-heals (a doubled file no longer matches its own meta ⇒ whole refetch), which is why three review passes walked past it; the permanent damage is one poll later. Once the remote grows past the doubled size the legality test holds again, the next tail is appended at the doubled offset, and the file's size now EQUALS the remote's — so the stamp says COMPLETE and the short-circuit serves it forever, with a region duplicated and the region behind it MISSING. Nothing catches it downstream either: duplicated JSON records carry neither a NUL byte nor a zstd magic, so the byte checks of ① are silent, and a stopped conversation never changes again. Two independent legs, because the failure has two independent sources. **(i) The slot is single-flighted**: `_fetchRemoteByFind` is now a thin wrapper over the core (`_fetchRemoteByFindOnce`) holding an in-flight promise per cache path — a second caller AWAITS the first's verdict instead of racing it (one remote read, one append; every caller resolves its cap from the same harness descriptor, so a coalesced caller cannot lose a cap it needed, and the next poll re-decides with its own). **(ii) The append refuses a stale offset**: `_appendDeltaAt(cachePath, offset, buf)` opens `r+`, re-reads the size THROUGH the fd it is about to write with, and writes POSITIONALLY at `offset` — at a matching offset the write is idempotent, at a mismatching one it does not happen at all. `false` means "the slot moved": under the cap that is a whole refetch on both rungs (the ssh rung reuses the round-3 fallback), over the cap — where a whole re-pull is impossible by construction — it is a loud failure that leaves the slot exactly as it was found. Pins: test-codex-zst ⑥f, with pre-fix repros for both legs (the doubling, then the poll that seals it and the short-circuit that then serves it forever) and mutation-tested one leg at a time: deleting the single-flight reddens the two coalescing asserts on both rungs, deleting the offset check reddens the four moved-slot asserts.

**② d ROUND 5 — THE REFUSAL THAT DEGRADED (src/hosts.js).** Round 4's moved-slot refusal was correct and unreachable on the rung that needed it most. The over-cap `throw` sits INSIDE the data-plane `try`, and that catch means "this TRANSPORT did not work, try the other one" — so on a dial host (or any host with the data plane on) with a reachable ssh fallback the refusal landed in `catch (e2) { /* legacy fallback below */ }`, silently, and the ssh rung then measured `fs.statSync(cachePath).size` on the MOVED file, computed a fresh tail offset from it and appended: the remote's bytes for the moved region were never fetched, the file's size nevertheless EQUALS the remote's, the meta stamps COMPLETE, and a stopped conversation serves the hole forever. Reproduced verbatim (dial transport, foreign 203-byte append during `fsReadRange`, remote past the cap): no error, no log line, `meta.size == remote size`, remote bytes [P, P+203) missing — and the next poll short-circuited on it. Three legs, each mutation-tested alone. **(i) The verdict is typed.** `slotMovedError` carries `code:'ESLOTMOVED'` and the catch re-throws it (`_isTerminalFetchError`): a statement about the CACHE is true on every rung; only TRANSPORT failures may degrade. **(ii) What a rung learned outlives it.** `observed.moved` (`_slotMovedEarlier`) denies the ssh rung a delta even when the slab rung reached the fallback for an unrelated reason — it can observe the movement, refetch whole UNDER the cap, and then have that whole read die on the wire. **(iii) The durable half.** One poll later the movement is invisible to `_appendDeltaAt`: the foreign bytes are simply in the file, the offset it re-reads MATCHES them, and the append lands right behind them — so the fix would only have delayed the corruption by one poll. `_prefixIsOurs(localSize, meta)` asks the meta instead of the clock: a delta may extend only bytes this cache's own writers stamped (the file may not hold MORE than `meta.size`). A SHORTER file stays growable on purpose — a positional append only ever writes at the end, so a short file is still a genuine prefix (a crash between the append and the meta write, a truncation), and refusing it would strand every over-cap slot that ever crashed mid-write. Over the cap every one of these is a loud failure that leaves cache AND meta byte-identical, and the cap error names WHICH fault it is ("could not be verified" vs "holds N bytes where the last fetch stamped M"). Two consequences fell out: the whole `cat` now stamps `buf.length` instead of the stat it was promised (a live transcript hands back more bytes than the probe said — the tail branch of the same rung has always stamped the real length, and an under-claiming meta would make (iii) read our own fetch as foreign bytes), and the data-plane catch now LOGS every fault it swallows VERBATIM — it logged nothing at all, which is how a degrade path hides a lost binding for three days (2.340.2).

**② e ROUND 6 — THE SHAPE ROUND 5 CONDEMNED BY MISTAKE (src/hosts.js).** Round 5's "grow only what we stamped" is right about foreign appends and wrong about one INNOCENT shape it cannot tell apart from them. Until round 5 the whole `cat` rung stamped the PROBE's stat while `cat` returned everything the live transcript had grown to since, so a perfectly healthy fetch left `meta.size` SHORTER than the file it wrote. Under the cap that costs one whole re-pull and heals; OVER the cap there is no whole re-pull, so `_prefixIsOurs` turned every such slot — a remote stopped thread that opened yesterday — into a permanent hard failure ("holds N bytes where the last fetch stamped M"), forever, because nothing on either side can ever move again. The heal cannot be a local scan: the corruption round 5 exists for appends ORDINARY TEXT (⑥g/E2), which no NUL/magic/record-boundary check can distinguish from our own missing bytes. So the only witness that can tell them apart is asked — THE REMOTE. When a slot is over the cap AND its meta predates the `sizeExact` marker AND the file is longer than that meta claims, the gap `[stamp, file)` is read back (a BOUNDED read of just the gap: `fsReadRange` on the slab rung, `tail -c +N | head -c LEN` on the ssh one) and compared byte-for-byte with what the cache holds there. Equal ⇒ genuine prefix, the stamp was merely short ⇒ re-stamp `meta.size = file size` with the marker, ONCE, and the delta rides again. Not equal ⇒ round 5's refusal stands, unchanged. Four bounds keep it from becoming a way around round 5 or a per-poll cost: only over the cap (under it a whole refetch is the remedy — adopting bytes you can simply re-fetch is trusting what you did not have to trust), only for marker-less metas (a round-5+ writer stamps what the file holds, so a longer file THERE is genuinely foreign), never on a slot a rung watched move in this window (`_slotMovedEarlier` — we already know where those bytes came from), and free local evidence FIRST (the round-3 whole-file splice scan, skipped when this poll's `cacheUsable` already paid for it). The verification is written to disk the moment it is PROVEN, not when the fetch that triggered it finishes — otherwise a delta that dies on the wire throws the proof away and the next poll pays again, on exactly the slots where the fetch is least likely to complete. A refusal, by contrast, is memoized IN MEMORY only (per slot, keyed by the size it was made on, 10 min): stamping a slot we would not serve freezes the corruption in, and the memo also carries the VERDICT so the replayed refusal gives the same diagnosis. Four verdicts, four different sentences appended to the cap error — a checked byte mismatch, a splice marker found locally, a slot seen moving, and "the remote could not be read (retryable)" — because a refusal that claims the check it skipped is "an error string is not a diagnosis" with the sign flipped. Second half, the round-5 MINOR's own cost: the degrade line round 5 added is now rate-limited per HOST (≤1/60s, `_warnDegradeOnce`, carrying `(+N similar suppressed…)` onto the next window) — the thing it degrades on is usually persistent, and every attach/poll/goal-sync tick comes through there, so a line per fetch is a journal that says one sentence.

**② f ROUND 7 — THE HEAL'S OWN COSTS, AND A GUARD THAT NEVER ASKED WHAT IT WAS COMPARING (src/hosts.js).** The round-6 verify named three, each reproduced before it was fixed. **(i) "Paid once per slot" was paid twice per poll, every poll.** The whole-file splice scan is a SYNCHRONOUS read of the entire cache file on the event loop, and round 6's own comment claimed it was "skipped when this poll's `cacheUsable` already paid for it" — true only within ONE rung. `cacheUsable` runs on the slab rung, the slab rung's "too large" is degradable, so the ssh rung runs it again: two whole reads of a 64 MB slot per poll. And "per poll" is the worse half — the slots that owe the scan are exactly the ones that REFUSE, and a refused poll leaves the meta byte-identical BY DESIGN (round 6's disk invariant), so nothing ever retires the legacy marker: every session poll, window attach and goal-sync tick re-read the whole file, forever. The verdict is a statement about BYTES, so it is now memoized against the bytes it was made on — the cache file's identity (inode, size, mtime), 10 min TTL, dropped explicitly by every writer in the fetch that touches the slot. That makes it free across rungs AND across polls, so the `deepDone` flag is gone: the heal asks the question outright instead of inferring from a flag whether some earlier caller happened to have answered it. The scan itself now answers in THREE states — clean / a marker is in there / the file could not be read — because only a VERDICT may be remembered: the caller refuses the poll either way, but memoizing an unreadable file would let a transient EMFILE freeze a healthy slot for the whole TTL (the same line round 6 drew between a refusal and 'unverified'). Invariant: **a "paid once" claim is about a CACHE, and a cache needs a key that names what it is about — the flag that says "someone already did this" is only true inside the scope that set it.** **(ii) A rate limiter that suppressed the wrong thing.** Round 6's `_warnDegradeOnce` keyed the 60 s window on the HOST id alone, so the first fault held the whole minute and a DIFFERENT fault on that host inside it — the heal's gap probe dying while the link flaps, a free identifier an extraction left behind (the 2.340.2 class this line exists to expose) — was dropped into a "+N similar" tally that names nothing: the rate limiter became the silent catch it replaced. "Similar" has to MEAN similar, so the window is keyed on the host AND the message's CLASS (`_degradeClassKey`: the same sentence with its offsets/sizes/ids blanked). Each distinct fault speaks once a minute; only genuine repeats are counted. **(iii) The gap comparison never asked whether it was still the file it measured.** The heal compares the cache's `[stamp, EOF)` with the remote's own bytes, and the two sides are taken at DIFFERENT times: `localSize` from a stat before the remote round trip, the local bytes after it. A slot that shrinks in between (a truncation, a non-atomic writer, an operator repairing it) hands back a short region, and the pre-fix code read that mismatch as the strongest verdict it has — 'foreign' ("the bytes past that point were CHECKED against the remote: they are not its own") — and MEMOIZED it for ten minutes, so one transient shrink condemned a perfectly healthy over-cap slot and the memo replayed the mis-diagnosis on every later poll with no probe left to correct it. `_healGapIsStillOurs` (neuterable, like every other leg here) re-stats before comparing: a slot that changed under the probe is the MOVED class — terminal for this poll, remembered by nobody, re-judged next poll. Invariant: **a comparison between two reads taken at different times must prove they are about the same object before it may report the DIFFERENCE as evidence — and a fact about a race may never be memoized as a fact about the bytes.** Pins: test-codex-zst (M)/(N)/(O) — the pre-fix scan count (3, then +2 per poll) against one scan for the slot over three polls and two rungs, plus memo-cleared and bytes-changed negative controls; both key shapes A/B'd on the same two faults; the shrink race reproduced as a memoized 'foreign' that survives the file's recovery, then as a shifted verdict whose very next poll heals the slot.

**③ A transcript shorter than the magic could never be verified (src/hosts.js).** `head.length < 4 ⇒ refetch` meant an empty or one-record transcript was fully re-pulled on EVERY poll, forever. A cache under 4 bytes cannot BE a zstd frame and cannot hide a splice: it verifies (still subject to "a .jsonl cache starts with a record"), while a sub-magic cache under a COMPRESSED remote is still refused.

**④ A home-dir dot glob widened to every user (src/plugin-manifest.js).** 2.369.44 taught the validator that node reads a declared fs path only up to the first `*` and grants by STRING PREFIX; 2.369.43 taught it to collapse `.` segments the way node does. At `~/.*` the two rules collide: node keeps `.*` as an ordinary segment (the grant is the prefix `<home>/.` — dotfiles), but collapsing the dot HERE emitted `<home>*` — the whole home directory plus every sibling path starting with those characters (`/home/u*` reaches `/home/u2/…`). The manifest said one thing and node was handed another, invisibly. A `.` segment immediately before `*` is now refused, naming the prefix it would have produced.

**⑤–⑦ The ACP bookkeeping nudge is OURS, not the user's (data/bin/acp-wrapper.js).** Three consequences of one confusion:
  ⑤ The 'queue-cleared' notice ("send it again") subtracted dropped NUDGE entries but not dropped PEER ones — and a peer message is not the user's to re-send either: it goes back to the delivery ladder on `peer_result ok:false` and arrives again on its own, so the advice was both wrong and impossible to follow. Nudges AND peers are subtracted now; the log line keeps the true total.
  ⑥ The nudge queued with a visible `queued_input`, painting a "Queued" chip on a bubble that does not exist. It queues with `silentQueue` (as peer messages already did) and stays visible in `queue_changed` as kind 'system', so the strip is still honest about what is waiting.
  ⑦ **A Stop landing inside the `/api/agent/stop-check` round trip still billed a turn.** The nudge is fetched by `endPrompt` and dispatched when the answer lands — by then the interrupt has finished and `activePrompt` is null, so the fetch resolved straight into `runPrompt` and started a fresh turn the user had just asked to stop; the only trace was a reminder appearing out of nowhere. The interrupt now bumps `stopEpoch` and aborts the request in flight, and an answer whose epoch is gone is discarded with a logged reason. The latch is never set on that path, so the NEXT completed turn nudges normally.

**INVARIANTS.** (a) A verification window sized for the moment of corruption expires the moment anything is appended after it — a decision that ADOPTS unverified bytes must pay for evidence over the whole artifact, and record that it did so, so the cost is paid once. (a2) That record must be a marker THIS code writes (a schema version), never a field the buggy code also wrote: gating a heal on "the old code left this blank" misses every slot the old code filled in on its way past the bug — and a fix's own release window is where those slots come from. (a3) Name the harm the fix removes, then prove the fix removes THAT: a change that is separately correct ("record the adoption") can be shipped with a false causal claim attached ("…so growth syncs"), and a test that quietly relaxes the condition under test (dropping `maxBytes` on the growth poll) will agree with it. (a4) Advice inside an error message is a claim about the system — if the branch that emits it can only be reached where the advice cannot work, it is a false claim, not a courtesy. (a5) A guard nobody can delete-and-fail is not a guard: every legality clause owes a leg that goes RED when that clause alone is removed — and when the clause exists in twin form on two rungs, it owes one on EACH, or the copies drift the moment one is edited. (a6) A fast path added under an existing one must leave the slow path REACHABLE: replacing the only road with an optimisation turns every failure mode of the optimisation into a failure of the whole feature, and on the LAST rung there is nothing below to catch it — fall back where the slow path is still possible, and hard-fail only where it provably is not. (a7) A meta writer overwrites what it just LEARNED, never what it inherited: durable provenance and a fresh object literal are contradictory designs, and the field that gets erased is always the one that cannot be re-earned. (a8) A read-then-write against a size measured before the read is a lost update waiting for a second caller: serialize the RESOURCE (the cache slot), and make the write itself refuse the offset it was computed from once that offset is no longer the end of the file — a positional write at a re-checked offset is idempotent where a blind append is a duplicated region. Corollary for the repro: a duplication whose records are IDENTICAL is byte-identical to the correct file, so a fixture built from repeated records will pass the negative control while the bug is fully present. (a9) **A refusal is only a refusal on the rung that can be reached.** A guard thrown inside a try whose catch degrades to another implementation is not a guard — it is a slower path to the same corruption, because the fallback re-derives its inputs from the very state the guard refused to trust. Classify the throw: a TRANSPORT failure may degrade, a verdict about the shared RESOURCE must be terminal on every rung and must travel as a CODE the fallback tests, never as a message nobody reads. (a10) **A guard that only sees the race window has to be paired with one that reads the evidence the race left behind.** An in-window check (re-read the offset through the fd you are about to write with) expires the moment the poll ends: next time round, the foreign bytes are simply part of the file and every in-window question answers "fine". The durable form asks a record the code wrote itself — grow only what we stamped — which also means every writer must stamp what the file ACTUALLY holds, or the invariant starts reading our own fetches as foreign. Corollary: when a fix's negative control needs three separate neuterings to reproduce the original bug, that is not redundancy to trim — it is three independent legs, and the suite should be able to remove each one alone and go red. (a11) **A rule written for a corruption will also condemn every innocent shape that wears the same signature — find them before shipping it, because the rule's own remedy may not exist for them.** "The file holds more than we stamped" is foreign bytes AND our own short stamp, and the branch where a whole refetch is impossible is exactly where the innocent one dies permanently. Two consequences: gate the strict rule on a marker THIS code writes so it is absolute only where it is provably right (a10's corollary — a writer that stamps the real length cannot over-stamp), and when local evidence cannot separate the two shapes, ASK THE SOURCE OF TRUTH for the disputed bytes instead of loosening the rule (a bounded read that compares them is positive evidence; a splice scan is only absence of one kind of evidence). The heal that follows must be bounded to the case with no other remedy, paid once, durable the moment it is proven, and its refusals must be remembered in MEMORY — a refusal stamped on disk freezes the corruption in. (a12) A degrade-path log obeys the "say what you swallowed" law AND the "do not say it 500 times" one: rate-limit per host and carry the suppressed count forward — a suppressed fault is not a forgotten fault. (b) Any rewrite a validator performs on a declared path must be a rewrite the consumer performs too; where they differ, refuse rather than emit the wider form (a normalization that widens a grant is a silent privilege escalation). (c) The rule "count only what the USER did" has to enumerate everything that is not the user — one exception (our nudge) is a hint that there are others (a peer message). (d) An in-flight request belongs to the turn that started it: when that turn is cancelled, record the cancellation and DISCARD the answer — never let a round trip that outlived its turn start a billed one.

**Pins.** scripts/test-codex-zst.mjs ⑥c (a buried splice; a magic straddling the 1 MiB chunk boundary; a hybrid that ALREADY carries provenance — refused, because `sameRemote` is not evidence; the one-time cost proven by counting the scan's own reads across two polls; a >1 MiB clean over-cap slot that must still adopt; the ssh rung's stamp AND its delta growth under the cap it was adopted with, plus the live-overtake, short-read and unverifiable-cache negative controls; the honest cap error with a "do exactly what the old text said" control; tiny-transcript short-circuit with two negative controls — every one red with its own fix reverted); ⑥d/⑥e (round 3: a re-compressed .zst remote over a .zst cache refetched WHOLE on BOTH rungs, with the guard-less legality predicate asserted to call that growth legal and the spliced bytes shown to decompress to the OLD transcript — both legs red when the compression clause is deleted from their own rung; a `tail` the host cannot run and a short tail both falling back to the whole cat under the cap, with an over-cap negative control proving the fallback is not a cap bypass; `adopted`/`slab` surviving a delta on both rungs and cleared by a whole refetch); ⑥f/⑥g (rounds 4-5: the two overlapping fetches and the moved offset; then the verifier's dial-transport scenario — slab rung first, ssh rung reachable, remote past the cap, a foreign 203-byte append during `fsReadRange` — refused on BOTH rungs with cache+meta byte-identical, refused AGAIN on the next poll by the stamped-size leg alone, each leg isolated (only the typed verdict removed still refuses, but the caller loses the diagnosis), the pre-fix shape restored by neutering all three legs and shown to seal the corrupt slot and serve it forever, the degrade line asserted verbatim with a healthy-fetch control, and the overtaking whole `cat` stamping what it fetched with a pre-fix-meta control); ⑥h (round 6: an over-cap slot seeded with the old stat-sized meta and a CLEAN longer cache — the pre-fix permanent failure reproduced first (refused twice, nothing can ever move), then healed by one bounded probe of exactly [stamp, file) and grown by the ordinary delta, on the ssh rung AND its slab twin; the heal proven durable across a delta that dies on the wire; a DIRTY longer cache of the same length and with no splice marker refused with the checked-mismatch diagnosis, cache+meta byte-identical, its verdict memoized in memory and re-probed once the memo is cleared; a buried splice marker refused with NO remote read and without claiming the check it skipped; the same shape under the cap refetched whole with no probe at all; a marker-carrying meta refused without asking anyone; a probe that dies on the wire refusing as RETRYABLE and healing on the next poll; and the degrade line rate-limited per host with a second host and a window-expiry leg — ten mutations, each reddening the suite alone); scripts/test-plugin-security.mjs (`~/.*` / `/srv/a/.*` refused, the pre-fix outputs `/home/u*` / `/srv/a*` asserted never produced, `~/.config/*` and `~/./*` unaffected); scripts/test-acp-harness.mjs (the peer entry excluded from the notice, the nudge's silent queue entry, and a Stop held inside a deliberately delayed stop-check with a same-server control that proves the delay itself still nudges).
## THE WRITER SWEEP KILLED READERS, AND ITS TWIN NEVER RAN AT ALL (B-3185, src/writer-sweep.js + src/cli-identity.js + src/server/boot-restore.js + src/hosts.js + src/routes/sessions.js)

Two defects in the same scan, one of them destructive and one of them silently dead.

**(1) A substring decided who dies.** The pre-resume sweep finds processes holding a conversation's transcript open and SIGTERMs the ones that are the agent CLI. "Is it the CLI" was `case "$(ps -p $pid -o args=)" in *claude*)` — a substring over the WHOLE command line — so ANY process whose argv merely NAMED a path under `~/.claude` qualified: `tail -f ~/.claude/projects/<id>.jsonl`, an editor with the transcript open, and a test or agent process running from a git worktree under `~/.claude/worktrees/`, which is where the worktree-only-smokes law puts every agent. The suite hit this first: holding the transcript fd in its own process, it matched its own guard and SIGTERMed itself — exit 143, `npm run ci` red, on code that was fine — and the response at the time was a WORKAROUND IN THE TEST (spawn a holder with a neutral argv) rather than a fix, so the live hazard stayed. Same bug in the codex legs via `*codex*`. Same-origin A/B, identical fixtures, only the script swapped: OLD kills `tail -f` and the worktree process (both `survived=false`); NEW leaves both alive and still kills the real writer.

FIX: `vs_is_cli <pid> <claude|codex>` decides by the EXECUTABLE, in the three rungs that cover the three shapes the CLIs ship in — argv[0]'s basename (native install / vendor binary), the interpreter's FIRST NON-FLAG operand when argv[0] is node/bun/deno (npm shebang shim, `…/claude-code/cli.js`, `…/codex/bin/codex.js`), and `/proc/<pid>/exe` (a native claude execs `~/.local/share/claude/versions/<ver>`). argv comes NUL-split from `/proc/<pid>/cmdline` where it exists, `ps -o args=` elsewhere. A later ARGUMENT never identifies a process. The fd evidence stays the PRIMARY signal — vs_is_cli only answers writer-or-reader — and the codex ARGV leg (`codex resume <tid>` / `CODEX_WEBUI_RESUME_ID=<tid>`) is deliberately exempt because it is self-evidencing and the shape it catches (an orphaned wrapper / its dtach master) is not the codex binary.

**(2) The scan forked per process, and its twin forked per FD.** Measured on the dev box (32 cores, loadavg 3.4–4.8, 3678–3713 processes, 405,735 open fds): the sweep's `ls`+`grep` PAIR per process = 7356 forks and 9.9–12.2 s wall against a 20 s budget — over half of it spent on fork/exec, and past the budget entirely on a busier or bigger machine. boot-restore's "which conversations does a live claude still hold?" probe was the same scan written a second time as a `readlink` fork PER FD — ~6.4 minutes here — against a 6 s timeout, so it ALWAYS timed out, and a bare `catch {}` turned that into "nobody is live": the boot socket dedup silently lost its liveness signal and fell back to createdAt. (Its `/proc/[0-9]*/fd/*` glob was NOT an ARG_MAX case, which is what the first fix's comment claimed and the r2 review corrected: a shell `for` loop expands in the shell's own memory and never reaches execve. ARG_MAX bounds `vs_fd_chunk`, which EXECS `ls` — that is why THAT is chunked.) FIX: ONE shared `fdScanShellFns()` in writer-sweep.js — one `ls -l` per 400 fd directories + one awk, ~20 forks, 3.0–3.5 s — embedded verbatim by both backend scripts and by boot-restore, whose failures now LOG. A/B on the same fixture: old probe `FAILED after 6007ms ETIMEDOUT`, new probe `ok in 3160ms` with the holder named.

**WHAT THE FIELD MEASUREMENT ALSO SAID (do not overclaim this fix).** The installed claude (2.1.226, native) does NOT hold its transcript open — it appends and closes; a machine-wide scan found ZERO holders of any `~/.claude/projects/**.jsonl` with 16 CLIs running. So (i) the claude FD leg mostly finds READERS, not writers — which is exactly why a substring identity test there was dangerous rather than merely sloppy — and the leg that actually reaches a live claude is its own `~/.claude/sessions/<pid>.json` lock file (unchanged, now behind the same executable test); (ii) boot-restore's probe is fixed as a MECHANISM (no stall, no silent catch) but still answers "nobody" for current claude, so `claudeAlive` remains effectively false locally. Giving it the CLI registry keyed with `procStart` is the real fix and is deliberately NOT in this commit: retiring is destructive (SIGTERM the owner + unlink the dtach socket), and changing which socket boot destroys is its own decision, not a rider on a performance fix. Read-only field validation of the new predicate against ~3700 live processes: of 70 whose argv merely contains 'claude', exactly the 16 real CLIs answer yes; every dtach master and chat-wrapper that carries the whole `…/claude --resume …` line as ARGUMENTS answers no — the old rule said yes to all of them, and SIGTERMing a dtach master destroys the session (`data/sockets/cw-*` is the danger constant).

**INVARIANTS: (a) Identify a process by its EXECUTABLE — argv[0], the interpreter's first non-flag operand, /proc/<pid>/exe — never by a substring of the whole command line: the transcript path the process is READING is in its argv too, so a substring test cannot separate the writer from every reader of the same file. (b) The evidence that selects a candidate and the test that decides its fate are different questions: keep the fd evidence primary and let the identity test only classify. (c) A guard that kills must be provable with a NEGATIVE control that is not vacuous — assert the reader really has the file open before asserting it survived. (d) When a suite works around a product guard to stay green, the guard is the bug: the workaround is a report, not a fix. (e) A per-item fork inside a /proc walk is a budget bomb — batch it, and size the budget from a MEASUREMENT on a real process table, not from an intuition. (f) A degrade path that can never succeed is indistinguishable from a feature that was never written: a probe that always times out into a bare catch must at minimum SAY SO. (g) When two places run the same scan, they are one implementation with two call sites — the second copy is where the fix does not land.** **R2 REVIEW ROUND — six defects in the fix itself, each now carrying a negative control (the invariants they add): (h) A DRIFT PIN THAT CANNOT MATCH THE DRIFT IS NOT A PIN. The §1 guard against a reintroduced whole-argv identity test was an unsatisfiable regex — it matched neither the verbatim pre-fix line nor any realistic reintroduction, while the kb advertised it. Assert a pin against the EXACT retired shape, then prove it by putting that shape back and watching it go red. (i) A LOOP THAT CARRIES STATE ACROSS ITEMS MUST RESET IT ON EVERY ITEM, NOT ONLY ON THE ONES IT RECOGNISES: the awk pid attribution cleared `p` only on a header matching `/proc/<pid>/fd:`, and `/proc/self/fd` — appended to every chunk on purpose, and the fd table of the `ls` process, which INHERITS the sweeping shell's fds — is exactly the header it does not recognise, so an inherited matching fd was attributed to the chunk's last numeric pid and that innocent process was the SIGTERM target. (j) A COMMENT THAT NAMES THE WRONG MECHANISM IS HOW THE NEXT PERSON REMOVES THE RIGHT GUARD: the chunking was justified as ARG_MAX on a glob that a SHELL for-loop consumed and never execve'd. (k) A NARROWING FIX MUST NOT WIDEN ANYTHING: rung 3 accepted every process whose `/proc/<pid>/exe` lives under the CLI's `versions/` install dir — and the CLI re-execs that same image as its bundled helpers (measured: 2–3 live `ugrep` processes with argv[0] a bare `ugrep` and exe `…/versions/2.1.238`), so a helper holding an inherited transcript fd became a target inside the very fix that stopped killing readers. Rung 3 now also demands the PRESENTATION (argv[0] names the CLI, or argv[0] renamed nothing at all). (l) REPRODUCE THE SHAPE, DO NOT DESCRIBE IT: the suite labelled its own regression leg from `process.argv[1]`, which node absolutises, while the guard reads the command line AS TYPED — it claimed to be running "the real regression shape" for an argv with no `.claude` in it. A copy of the suite now runs from a real `.claude/worktrees/` checkout both absolutely and relatively, with both argvs read out of /proc and asserted. (m) THE STANDING SWEEP IS THE POINT OF (g): hosts.js's remote-discovery CO leg still carried ALL THREE retired shapes (per-process `tr`+`grep` fork pair, per-FD `readlink`, whole-argv identity — 5.02 s / 8068 forks at 4034 processes vs 3.52 s / ~20 forks ported) three commits after the sweep was fixed. Porting it surfaced a live hazard of its own: the leg is the LAST command of the ssh discovery script, `_ssh` REJECTS a non-zero exit, and a `while read` loop exits with its last iteration's status — so a rollout held by a non-CLI process made the whole host's discovery fail into the serve-stale-cache catch. NEVER LET A SCAN'S FINDINGS DECIDE A SCRIPT'S EXIT STATUS.** **R3 REVIEW ROUND — three defects, one of them the twin r2 chose to record instead of kill: (n) A RECORDED TWIN IS STILL A TWIN. r2 left `src/discovery-facts.js` identifying processes by command line (`pidLooksClaude` = `cmdline.includes('claude')`, `isCodexCommandLine` = a whole-argv regex) and wrote the reason down: it labels a card RUNNING, it never SIGTERMs anything. The blast radius really is different — and it was still the wrong call, because a rule with two spellings has two behaviours the moment either is touched AND because the loose spelling was doing its own damage: measured on the dev box, it answered YES for **106 of 4277 processes** against **16 real claude CLIs** (dtach masters, chat-wrappers, the generated `code` editor helper, a zsh shell-snapshot; codex: **30 vs 4**), so a lock file whose pid had been RECYCLED by any of them produced exactly the phantom "running" session the PID-reuse guard exists to prevent. The rule now lives ONCE in `src/cli-identity.js` — `isCliProcess(pid, name)` beside `cliIdentityShellFns()`, dependency-free so the daemon bundle still carries it — and BOTH suites drive the two spellings over the same live pids in the same instant (a one-sided edit is red in test-writer-sweep §6 and in test-discovery-interpret §8). Field check before shipping the narrowing: over 4277 live processes it never answers YES where the old rule said NO, and every real CLI still answers YES. (o) `(deleted)` IS A NORMAL STATE, NOT AN EDGE CASE. The kernel appends ` (deleted)` to `/proc/<pid>/exe` once the image is replaced on disk — which is precisely what `claude` auto-update and `npm i -g @openai/codex` do to LIVE sessions (two of the four agent CLIs running on this box carry the suffix right now). Unstripped, it defeated BOTH executable rungs — basename `claude (deleted)`, and the r2 "nothing renamed" disjunct comparing `2.1.258` against `2.1.258 (deleted)` — so the sweep skipped the ONE process it exists to stop, a mid-update CLI still appending to the transcript, at the moment a double-writer is most likely. **A guard that reads kernel-formatted state must handle every form the kernel emits.** (p) A CONTROL THAT CANNOT REACH THE CODE IT GUARDS IS NOT A CONTROL. The `vs_argv`-silencing leg drove a pid past `pid_max` — for which `[ -r /proc/<pid>/cmdline ]` is FALSE, so it took the `ps` fallback and never reached the redirect the fix silences; it passed identically on the unfixed code. The failure needs `[ -r ]` to PASS and the OPEN to FAIL, which in production is an unschedulable TOCTOU race, so the SHIPPED text now runs with exactly one literal substituted (the /proc root) against a unix SOCKET — access(2) says readable, open(2) says ENXIO, the shell prints the same `cannot open …` — with the verbatim pre-fix spelling as the control. (A directory is the tempting fixture and the wrong one: `O_RDONLY` on a directory SUCCEEDS, so `tr` fails at READ time and its own `2>/dev/null` swallows it either way — a control that passes on both sides, which is the defect being fixed.) (q) THE SHELL THAT RUNS THE TEXT IS NOT `sh`. The identity text is embedded in scripts that reach a machine three ways, and only ONE of them is `sh -c`: the device rung runs `sh -c <script>`, but BOTH ssh rungs — the sweep's fallback (`sweepWriters`) and the discovery CO leg (`hosts.js _ssh`) — hand the script to `ssh host -- <script>`, which the REMOTE USER'S LOGIN SHELL interprets. "POSIX sh" is therefore the floor, not the target. It bit the (o) fix on arrival: in zsh `(…)` is a glob GROUP, so the unquoted `${vs_c_e% (deleted)}` matched " deleted" and stripped NOTHING — the auto-update fix was dead on exactly the hosts whose login shell is zsh (a very common default, this dev box included), and invisible to every `sh -c` test in the suite, all of which stayed green. Quoting the pattern (`${vs_c_e%' (deleted)'}`) makes it literal in every shell (POSIX: quoted characters in the word are not pattern characters; verified dash/bash/busybox/zsh/ksh). **Quote any pattern whose literal characters are special SOMEWHERE, and test embedded shell text under every shell that can interpret it — not just the one your test harness picks.** The gate is functional: the suite drives the SHIPPED text through every login shell present on the box and demands identical verdicts on the `(deleted)` fixtures, with the unquoted spelling as the negative control — it parses and is INDISTINGUISHABLE under `sh`, and diverges only under zsh, which is precisely why the defect survived a suite that only ever ran `sh`.** (r) A BRANCH NO TEST CAN REACH IS A BRANCH THAT DRIFTS. Every consumer of the identity rule has a SECOND body for machines with no /proc (macOS/BSD ssh hosts), reached by `lsof`, and Linux-only suites had never executed either of them — so both quietly kept the rule the rest of B-3185 retired: `src/discovery-facts.js`'s rung gated on `/codex/.test(cmd)` and the `hosts.js` CO leg's `else` on `awk … c ~ /codex/`, i.e. **lsof's COMMAND field**, which is `comm` (the exec'd file's name, truncated, prctl-settable by the process itself) matched as a SUBSTRING — `codex-keeper`, `codexd`, a dtach master renamed after the thread. The shell sweep never had this: it takes `lsof -t` and runs `vs_is_cli` per pid. Worse, r1/r2 emitted the shared shell functions INSIDE the CO leg's `then` block, so on a mac host `vs_is_cli` did not EXIST in the branch that would have called it. And the JS rung carried a second, independent defect that only running it could reveal: `execFileSync` THROWS on a non-zero exit, and `lsof +D <dir>` returns 1 whenever any file under the tree has no open instance — i.e. essentially always (measured on the real `~/.codex/sessions` and on a single-holder fixture: correct `p`/`n` output, EMPTY stderr, status 1), so the catch produced `[]` = "no codex thread is running" and the entire macOS/BSD codex liveness fact was a degradation path that could only degrade — (h)'s always-fails-so-never-written, again, in the one place no test looked. Fixes: emit `-Fpn` and credit the `p` pid through the shared predicate on BOTH sides; hoist the shell function definitions above the `if`; read lsof's STDOUT and treat only a SPAWN-level failure (no lsof, timeout, buffer overflow) as "cannot tell", because a shell has always ignored lsof's status. **An exit code is an error signal only if the program says it is.** The gates make the branch reachable rather than trusting it: the JS rung is NAMED and exported (`listOpenRolloutPathsViaLsof`) and driven against live fixtures with ONE negative control PER defect, and the CO leg's else-branch is driven by substituting ONE literal — the `[ -d /proc/self ]` probe — leaving the loop, the identity test and the trailing `:` exactly as shipped. **THE STANDING SWEEP, RE-MEASURED (r3 — and the answer is not "zero"):** a FOURTH place asks the same question — `src/session-store.js` `isProcessClaude`/`isProcessClaudeAsync`. **r3 RECORDED THIS ONE'S BLAST RADIUS WRONG, and r4 corrected it (see the r4 round below): the pair is not "the LOCAL sweep's PID-reuse fallback behind B-2104's `procStart` match" — that describes only the ASYNC half. The SYNC `isProcessClaude` had exactly one caller, `/api/kill-pid`'s local branch, i.e. it GATED A SIGTERM, which is precisely the class of caller this whole essay is about; the async half is the one behind `procStart`, reached from `isLockClaude` alone, and it only decides whether a card reads RUNNING.** The rule reads `ps -o comm=` (the name of the file that was EXEC'd), i.e. it looks like an EXECUTABLE test rather than the argv substring B-3185 retired; measured over 4277 live processes it and `isCliProcess` answer YES to **the same 16**, with **zero** divergence either way — **but that measurement was of THIS box's process table, not of the rule: `comm` is 15 bytes the process may set for itself (node renames its main thread to `MainThread`, so an npm-installed `node …/claude-code/cli.js` answers NO) and `.includes('claude')` accepts `claude-keeper`; the local install here is native, which is the only reason the two agreed.** Porting the ASYNC half would be a behaviour change to the hot local sweep whose fallback rung is macOS-only (where `isCliProcess` is SYNCHRONOUS — a per-lock blocking fork is the 2.242.0 stall this async family exists to avoid), and the alternative — teaching the shared predicate a `comm` rung so both sides trivially match — WIDENS a kill path (comm is settable by the process itself via prctl) inside a narrowing fix, which is exactly (k). What it gets instead of prose is a live PARITY GATE: scripts/test-local-discovery-device.mjs already asserts "device-facts sweep === local sweep", so the two rungs are compared on every run — and its fixture was corrected from an ARTIFACT (a `#!/bin/sh` script named `claude`: the kernel gives that process argv[0] `/bin/sh` while `comm` stays `claude`, so it passed one rung and failed the other, pinning a disagreement no real install produces — and a wrapper script's lock file names the CHILD's pid anyway) to a real binary named `claude`. **A twin you keep must be a twin a test compares.** **R4 REVIEW ROUND — three defects, and the first one says the standing sweep had been asking too narrow a question: (s) THE RETIRED RULE WAS STILL LIVE ON A KILL PATH NOBODY HAD ENUMERATED. r1–r3 swept "who decides identity?" through the sweep and through discovery, and never asked "who else can SIGTERM a pid?". `hosts.js killRemotePid` — the sidebar's Terminate for a REMOTE discovered session — still ran `case "$(ps -p N -o args=)" in *claude*|*codex*) kill -TERM N`, the verbatim whole-argv substring B-3185 exists to retire, on a path that KILLS, on a machine the user cannot look at: a remote `tail -f ~/.claude/projects/<id>.jsonl`, an editor with the transcript open, a wrapper whose ARGUMENTS name `…/bin/codex`, or a dtach master carrying the whole `claude --resume …` command line (SIGTERMing which DESTROYS the session) all matched — and the route then answered `{success:true}`, so the user was told it worked. Its LOCAL twin in the same route was the other spelling (`ps -o comm=` + `.includes('claude')`) and wrong in the mirror direction: it could not kill an npm-installed CLI at all (comm is `MainThread` — node renames its own main thread; measured live) while accepting anything calling itself `claude-…`. Both now ask THE identity — the remote through a named `killPidShell(pid)` that embeds `cliIdentityShellFns()` verbatim (a host has no checkout) and tests BOTH CLI names, the local through `isCliProcess(pid,'claude'|'codex')` — so `hostId` is a parameter here too, and the sync `isProcessClaude` is DELETED rather than left lying about. The three outcomes (`VS_GONE`/`VS_OK`/`VS_NOTAGENT`) and the `ps`-based existence test are unchanged (`kill -0` would report another user's process as gone). **Enumerate kill paths as kill paths: the sweep is not the only thing in a product that sends a signal.** The rest of the sweep, with reasons, is short and now written down: the codex argv leg (`*codex*resume*<RID>*` / `CODEX_WEBUI_RESUME_ID=<RID>`) is an OWNERSHIP test, not an identity test — it names the exact conversation, which is self-evidencing, and the shape it catches is deliberately not the CLI binary; every other kill in the tree selects by something WE own (our dtach socket path, our own child pid, a plugin's own install root), never by what a command line says. (t) THE SURVIVING TWIN'S RECORD WAS WRONG — corrected above; a twin you keep must be recorded with the radius it actually has, or the next reviewer accepts a kill-path rule on the strength of a discovery-path justification. It is now pinned as discovery-only: one caller (`isLockClaude`, itself reached from ONE place — the lock probe in the discovery scan), asserted by name, and its sync twin asserted GONE. The honest edge: a lock this rung credits WRONGLY still puts that pid on a card, and the sidebar's Terminate hands that pid to `/api/kill-pid` — but the ROUTE re-asks THE identity and refuses, which is precisely why the gate belongs where the SIGTERM is and not on the label that led there. (u) THE TWO SPELLINGS DISAGREED ON A NEWLINE INSIDE AN argv WORD. argv is a list of NUL-separated words; the JS twin reads that list, but the shell turned NULs into newlines and took the Nth LINE — the FIRST LINE of the Nth word. Verified on live processes, in both directions and both harmful: argv[0] `/usr/bin/claude<LF>/usr/bin/tail` is basename `tail` to JS and `claude` to the old shell (the sweep kills a reader), and `<dir>/dir<LF>name/claude` is the reverse (a real writer survives the sweep, and the double-writer corruption happens). The shell now parks real newlines on `\001` across the line select and restores them after, so "the Nth line" IS "the Nth NUL-record" in both spellings, and the `ps` fallback flattens its blob the way the JS twin's `/\s+/` split always did. **AND THAT WAS ONLY HALF OF IT — the round-4 fix was itself measured, and the value still had to survive `$(…)`, which strips EVERY trailing newline.** So a word (or a path) that ENDS in one still read `…/claude` in the shell and `…/claude<LF>` in JS, and the shell's answer is the PERMISSIVE one, on the paths that kill: verified on live processes for all three captures — argv[0] `…/claude<LF>` (rung 1), argv[0] `…/claude<0x01>` (the park byte itself comes back out as a newline, so it was eaten too — the code comment claiming that residue "can only make a word look LESS like the CLI" was false for a TRAILING one), and `/proc/<pid>/exe` `…/claude<LF>` under a launcher argv[0] (rung 3, which no argv fixture can reach). One helper `vs_cap` now captures all three: a sentinel byte appended INSIDE the subshell protects the tail, then exactly ONE terminator — the one the producer itself printed — comes off. **A fix for "the two spellings read this structure differently" is not done until the value has been carried all the way to the comparison: the transport (`$(…)`) mangles it too, and a partial fix leaves the harmful direction alive.** **When one spelling of a rule can read a structure the other cannot, the difference is not academic — it is a different answer on the input that carries the structure.** **R5 REVIEW ROUND — three defects, and two of them are the same shape: a rung the harness cannot reach, and a name in a comment that no longer exists. (v) THE EXISTENCE PROBE IN FRONT OF THE KILL WAS BUSYBOX-BLIND. r4 fixed WHO `killRemotePid` may SIGTERM and left the question BEFORE it — does this pid exist? — spelled `C=$(ps -p N -o args=)`, with the reason written down that `kill -0` would report another user's live process as gone. busybox `ps` has no `-p` at all (measured here, busybox 1.37.0: `ps: invalid option -- 'p'` plus a usage block on stderr, exit 1, stdout EMPTY), and the capture's whole point is `2>/dev/null` — so on a host whose login shell lives in busybox (Alpine images, embedded boxes, any `ash` rootfs, and remember BOTH ssh rungs hand this text to the REMOTE USER'S shell) EVERY Terminate answered `VS_GONE` and the route returned `{success:true, gone:true}` while NOTHING had been signalled: the user was told the process was already gone, the card flipped, and the CLI kept running and kept writing. **A capability the probe does not have must never read as a FACT about the subject.** The fix is a ladder in which every rung is POSITIVE evidence: `kill -0` (POSIX, a shell builtin everywhere — its SUCCESS is proof; r4's objection was only ever about its FAILURE, where EPERM and ESRCH share one exit status, so a failure is handed to the next rung instead of believed), then `[ -d /proc/N ]` (Linux incl. every busybox host, world-visible for a process we may not signal), then `ps -p N` (the no-/proc rung — BSD/macOS `ps` does have `-p`). And a second, quieter lie went with it: once the pid is known to exist, `vs_is_cli` answering NO has two very different causes — we READ its argv/exe and it is a `tail` (a finding), or we could read NEITHER (no /proc AND a `ps` that cannot answer, i.e. exactly the busybox combination). `vs_known` separates them, so the outcomes are FOUR: `VS_GONE` / `VS_OK` / `VS_NOTAGENT` / `VS_UNKNOWN` (alive, no evidence readable), and `killRemotePid` THROWS with an explanation on the last two instead of manufacturing a success. The enumerated sibling `src/server/sysinfo-wiring.js signalProc` keeps its `ps -p`, with the reason re-derived rather than inherited: there it sits on the FAILURE branch of a kill that was already ATTEMPTED, so it can mislabel EPERM as ESRCH but can never report a kill that did not happen as a success — pinned by asserting the `kill` precedes any `ps -p` in its script. (w) THE `ps` FALLBACK RUNG OF THE r4 FIX HAD NEVER BEEN RUN. `vs_argv`'s `else` branch only executes where there is no /proc, r4 changed it (`tr '\n' ' '` before awk) and justified it with a sentence about the JS twin — but every fixture on a Linux box takes the /proc rung, so the claim shipped as prose. It is now driven with NO edit to the text under test, by two environment substitutions that together spell "this machine has no /proc, and its `ps` is not procps": the `/proc/$1/` root is re-rooted (three literals, asserted — re-rooting only the cmdline read leaves rung 3 answering from `/proc/<pid>/exe`, and the `ps` rung never decides anything), and a stand-in `ps` goes first on PATH. The stand-in is asserted to be NECESSARY, not assumed: measured here, procps `ps -o args=` renders an argv word's embedded newline as a SPACE, so this box's own `ps` cannot produce the multi-line blob the flattening exists for — and the branch only ever runs on a platform whose `ps` this box does not have. The JS side is DRIVEN too, not mirrored: a child node process whose two /proc reads throw runs the SHIPPED `procArgv`/`isCliProcess`. Result: the two spellings return the identical word for argv[0..2] and the identical verdict on three live fixtures, and the verbatim pre-r4 line (no flattening — awk then prints field i of EVERY line) answers the OPPOSITE of the JS twin on BOTH newline fixtures while remaining indistinguishable on a plain one. **A rung whose test can only take the other rung is a rung with no test.** (x) A COMMENT ABOUT IDENTITY NAMED A FUNCTION THAT NO LONGER EXISTS. scripts/test-local-discovery-device.mjs — the page that ARGUES why a second spelling of "is this pid claude?" is allowed to survive — still credited the local rung to `isProcessClaude`, the sync twin r4 DELETED, and still glossed `comm` as "the name of the file that was EXEC'd", which r4 had just measured to be false as an identity claim. On that page, of all pages, a stale name is an invitation to re-add the retired rule. Corrected to `isProcessClaudeAsync` reached from `isLockClaude`'s no-`procStart` fallback (which is why that fixture's lock deliberately carries no numeric `procStart`), and the note now carries a DRIFT GUARD: every identity function it names must be DEFINED in one of the three modules it attributes them to, and the deleted sync twin must be nameable nowhere — not in the note, not in session-store's code — with the verbatim pre-r5 sentence as the negative control. **Documentation that justifies keeping a twin is load-bearing: guard its names the way the code's names are guarded.** **R6 REVIEW ROUND — one defect, and it is r5's own escape hatch: (y) THE ENUMERATED SIBLING WAS NOT SAFE, AND THE REASON THAT SAID SO ONLY DESCRIBED HALF OF IT. r5 fixed `hosts.js killPidShell`, ENUMERATED `src/server/sysinfo-wiring.js signalProc` as the other kill path, and let it keep its `ps -p` on a hand-written reason: 'there it sits on the FAILURE branch of a kill that was already ATTEMPTED, so it can mislabel EPERM as ESRCH but can never report a kill that did not happen as a success' — pinned by asserting the `kill` precedes any `ps -p`. That script has TWO `ps -p` calls and the sentence is true of the SECOND only. The FIRST is the post-signal aliveness check on the SUCCESS branch, and there a busybox-blind probe does not mislabel an outcome, it MANUFACTURES one: measured here (busybox 1.37.0; fixture `trap \"\" TERM; sleep 60`) the pre-r6 script answers `OK-GONE` for a process that is still running, so `signalProc` returns `{ok:true, gone:true}` — the process-manager row flips to gone, the user believes the Terminate worked, and the CLI keeps writing. The failure branch's mislabel is real too and also measured: a live pid this uid may not signal answered `ESRCH` = 'no such process (already gone)' rather than EPERM. **A twin kept alive by a comment is a twin, and the comment is only as good as its author's count of the call sites** — the ordering pin was satisfiable and true and still let the defect through, because it asked about ORDER when the question was CAPABILITY. The fix is therefore not a second, better reason: `vs_alive` moved into src/cli-identity.js beside the identity text (same three transports, same remote-login-shell floor, dependency-free so the daemon still bundles it) and BOTH scripts embed it, so no per-site judgement remains to be got wrong; `signalVerdictScript(pid, sig)` is module-level and EXPORTED so the suite drives the shipped bytes and validates pid/signal AT the builder. And the class gets a STANDING SWEEP rather than a memory: no `ps -p` may serve as an EXISTENCE test anywhere on a kill/signal path (cli-identity, writer-sweep, hosts, sysinfo-wiring, session-store, routes/sessions, the agentd installer, every tracked data/bin twin), every surviving occurrence is listed WITH its reason — all of them ARGV/environ READS whose existence question was already settled by `kill -0`, plus the one no-/proc rung inside `vs_alive` itself — and a DEAD allowlist entry fails the suite too, because an exemption nobody prunes is exactly how this one survived a round of review. Honest edge, pinned: a ZOMBIE reads ALIVE to the ladder, to procps `ps -p` and to the local branch's `process.kill(pid,0)` alike — unchanged, kernel-level, and the reason §17's fixtures are double-forked orphans.** **R7 REVIEW ROUND — one defect, and it is the instrument rather than the rule: THE STANDING SWEEP WAS GATED ON A HAND-WRITTEN LIST.** r6 replaced "a twin kept alive by a comment" with a sweep, and then handed that sweep a literal array of seven files (plus `git ls-files data/bin`). A hand list is the SAME instrument this bug has already defeated three times — r4 found the retired rule alive on a kill path nobody had counted, r5 counted the sibling and let it go, r6 found that r5's reason for letting it go described only the second of that script's two `ps -p` calls — so "no `ps -p` existence test on ANY kill/signal path" was enforced exactly where its author had already looked. Measured: the r6 list missed **47** files that really do send or probe a signal, among them `server.js` (it wires signalProc), `src/jobs.js` (verified handle-kills), `src/server/boot-restore.js` (dtach retire = SIGTERM + unlink), `src/plugins.js` / `src/vnc.js` / `src/port-forward.js` / `src/opencode-serve.js` (each stops a daemon it started), `src/agentd/agentd.js` (device-side session pipes) and `src/ws-handler.js` / `src/ws-create.js` (the kill + teardown cases) — every one a place the next probe could land without reddening a thing, and the sweep would still have printed ALL PASS. The fix is that the file set is DERIVED by grep from the question the rule is about: every non-test file under `src/`, `data/bin/` and `scripts/`, plus the repo-ROOT scripts where `server.js` lives, whose text sends or probes a signal (`kill -TERM/-9/-0/…`, `SIG*`, `pkill`, `process.kill(`, `killRemotePid`, `signalProc`, `vs_alive`); extension-less files enter by SHEBANG, which is also how a GENERATED agent CLI (`vibespace-status` is deliberately untracked) is reached at all — `git ls-files data/bin` could never see it. The derived list is PRINTED by the suite, so the reviewer reads what was swept instead of a comment's count of it, and the grep is deliberately OVER-inclusive (the i18n dictionaries match the word `SIGSTOP`, the adapters match `SIGINT`): a false positive only widens where the rule is enforced, a false negative is the whole defect. Two structural pins keep the instrument honest — the derivation must lose nothing the r6 hand list covered (this reddens if the walk roots are narrowed) and it must still cover the ten named permanent kill paths the hand list missed (this reddens if anyone re-narrows to a hand list). And because a sweep only ever run against a clean tree is a sweep proven to say nothing, the SHIPPED sweep is parameterised by ROOT and run a second time against a temp tree built to contain each shape: the retired `ps -p N >/dev/null 2>&1` probe on a brand-new kill path IS caught; an ALLOWLISTED line copied into another file is still stray (the exemption is per file, it does not travel with the text); a kill path with no `ps -p` is swept and stays green; a `ps -p` in a file that signals nothing is out of scope, stated rather than assumed; the suites are excluded, which is the only reason this file may write the retired shape down as a control; and a shebang tool under `data/bin` is caught. A last leg closes the one hole a source scan structurally cannot see: every exported shell BUILDER (`pidAliveShellFn`, `cliIdentityShellFns`, `fdScanShellFns`, `writerSweepScript` claude+codex, `killPidShell`, `codexOpenRolloutsShell`, `signalVerdictScript`) is swept on the text it EMITS, with the verbatim pre-r6 script as that scanner's negative control.  A rebuilt tree caught a real hole in the first cut of this leg: `data/bin/vibespace-agentd.js` is the esbuild bundle of `src/agentd/**`, so it carries cli-identity's and writer-sweep's `ps -p` lines VERBATIM under a filename no per-file allowlist can ever hold — the sweep went red on a CLEAN tree the moment anyone ran `npm run build` (measured). GENERATED files (gitignored = not part of the shipped source) are therefore a THIRD bucket rather than a skip or an allowlist row, and their rule is stricter and needs no author: every `ps -p` line in them must appear VERBATIM in an AUTHORED file the sweep already walked, so a build step cannot introduce a probe and the reason still lives with the author (controls both ways: a bundle that only COPIES an authored probe passes; the same bundle with one line its sources never contained is caught). **The invariant: a standing sweep is only as good as the set it walks — derive the set from the property, never enumerate it, and print what was walked.** Suite: scripts/test-writer-sweep.mjs (239) — writers swept / readers survived incl. the suite's own process under a SIGTERM trap and two copies of it run from a `.claude/worktrees/` checkout, the rung-3 identity matrix (one binary, three argv[0]s) with r1's rung reverted against the live helper as the control, the awk attribution probe with the sticky-`p` revert as the control, the discovery-twin legs (structural + functional + exit status), the two `(deleted)`-image writers (one per exe rung) with the strip reverted as the control, the cross-login-shell verdict parity (dash/bash/busybox/zsh/ksh) with the UNQUOTED `(deleted)` pattern as the control, the JS⇄shell identity parity matrix and the retired JS rule as ITS control, the `vs_argv` silencing control on a socket, the two newline-in-argv[0] fixtures (one per direction) with the pre-r4 `vs_argv` reader as the control, the three TRAILING-byte fixtures (argv[0] in LF, argv[0] in `\001`, an exe PATH in LF) with the three pre-`vs_cap` `$(…)` captures as the control — reverted in the product, five asserts go red including the 46-pair live parity matrix, §14's KILL-PATH legs — the real `killPidShell` text run against real processes (a `tail -f` on a claude transcript and on a codex rollout REFUSED and alive, a real `claude` and a real `codex` terminated, `VS_GONE`, a non-integer pid rejected at the builder) with the verbatim pre-r4 remote line KILLING its own victim as the control, `killRemotePid` ITSELF with the device link stubbed to run the script locally (a builder referenced as a free identifier throws only when the METHOD runs — the lost-binding class no structural pin can see), plus `/api/kill-pid` driven through the real express router (the reader refused 400 and alive; the npm-shape CLI the comm rule could never see terminated) with the retired comm rule as ITS control — the structural pins (one /proc walk, no per-process ls, no whole-argv test, every `kill -TERM` line carries `SWEPT:`, killRemotePid builds from killPidShell, the surviving comm twin has exactly one caller and no sync twin) proven by reintroducing each retired shape, the boot-restore wiring pin, and a self-checking pin on this very number — plus §15's no-/proc `ps`-rung parity (re-rooted /proc + a stand-in `ps`, the JS twin driven into its own fallback in a child, the pre-r4 line as the control) and §16's busybox kill-path legs (the pre-r5 script answering VS_GONE for a live reader AND a live `claude` under busybox as the control, the shipped script refusing/terminating/reporting-gone correctly there, VS_UNKNOWN reached by re-rooting /proc, the caller's four outcomes, and the enumerated `signalProc` sibling, whose pin r6 replaced with the shared-text one) — plus §17's SIGNAL-verdict legs: the measured busybox blindness of the quiet `ps -p N >/dev/null` form, the verbatim pre-r6 script answering OK-GONE for a LIVE `trap "" TERM` process as the control, the shipped builder answering OK-ALIVE on that same process and OK-GONE on one that really dies (fully reaped, asserted via /proc, which is why the fixtures are double-forked orphans), ESRCH for a pid that was never there, a REAL EPERM leg on a live pid this uid may not signal with the pre-r6 ESRCH as its control (skipped-with-a-reason where the environment cannot produce one), signalProc's own four-verdict mapping through a stubbed device link, the builder's pid/signal validation, and the STANDING SWEEP over every kill/signal-path file with its reasoned allowlist. Revert-verified: putting the pre-r6 probe back in the product turns 5 asserts red (naming both offending lines by file:line), a brand-new `ps -p` existence test elsewhere turns the sweep red on its own, and a STALE allowlist entry turns two red. r7 revert-verified the same way: reverting the derivation to the r6 hand list turns the regression pin red (it names the 47 files that fall out), and each of the six temp-root controls is a live measurement of the shipped sweep rather than a description of it. test-local-discovery-device is 9 (the three new asserts are the note's drift guard); test-sysinfo-op is 29 (its two "probe existence via ps -p" pins became three: no bare `kill -0` split, no bare `ps -p <pid>`, and the shared ladder embedded verbatim).

**ROUND 2 — three defects an adversarial verifier reproduced against that fix, and the deeper one the reproduction exposed.**

1. **The MAJOR (its own scenario).** Round 1 keyed the inherited bubble on its CONTENT so codex's copy would collapse onto it — so two INHERITED items with the SAME text steered into one turn were two bubbles live and ONE after a reload. A steered message deleted, live and reload disagreeing, in the exact case the fix exists for ('continue', 'go on', identical agent-to-agent notices — a 25-item queue makes that ordinary).
2. **Measuring it turned up the pre-existing twin.** Codex's OWN user records were content-keyed too. On the OWNER'S rollout — the session in the report — a reload rendered **68 of its 95 user messages: 27 deleted**; across the local corpus, **133 deleted over 89 rollouts**. Every single collision carried a different `msg_…` id AND a different `create_time` (0 same-id, 0 same-create_time): distinct submissions, minutes apart, not one genuine duplicate among them.
3. **A phantom spinner (minor).** The inherited bubble deliberately skips `_finalizeStreaming` (it lands mid-turn), but it was still a hard `break` in that function's backward scan — so a reply left open when the turn ended stayed `status:'streaming'` forever on every rebuild: a spinner on a dead, read-only conversation. The same mechanism the peer-card exemption on that line was written for.
4. **A claim that leaks deletes (minor).** A twin claim short-circuited by the fingerprint's duplicate check was never retired, and the leaked claim later ate a legitimate codex-only record — the deletion the forward-only rule exists to prevent, arriving through the back door.

**THE ROUND-2 RULE: a user submission's identity is the ID its producer minted, never its text.** Ours is keyed by `userRecordIdentity` (webui_msg_id / client_msg_id / **webui_queue_id** — the cid a previous wrapper minted names the same submission, so one namespace), codex's by `codexRecordIdentity` (`msg_…`, its own namespace). Records with no id on either side (pre-0.15x rollouts: 144/572 in the corpus, all ≤2026-05, no file mixed) keep the content key — but ours and codex's stay in SEPARATE content namespaces, so the duplicate check can only ever collapse ONE producer's own record, which is what makes "a record dropped as a duplicate already had its twin effect applied" true and the leak in (4) structurally impossible. The ours↔codex pair is retired by the CLAIM alone, and now EVERY copy of ours claims (round 1 claimed only the id-keyed ones because a content-keyed copy of ours collided with codex's on the fingerprint; that collision is gone).

**AND THE ASYMMETRY THE CLAIM CANNOT COVER.** A forward claim assumes ours is written first. Two producers of ours are written LAST: the `item/completed` twin for an item the app-server DRAINED (it emits that only after persisting its own record) and the IDLE peer path (`turn/start` commits before `recordPeerMessage` runs — test-peer-msg-card had pinned exactly that order since 2.362.2 and caught the first, provenance-blind attempt at this rule). Such a record marks itself `webui_after_commit` and YIELDS to an unconsumed codex copy of the same content **in the same turn** — it drops ITSELF, never deletes anything — while an early copy never yields, so a same-text neighbour can never swallow one. `webui_queue_via` ('steered'|'drained') stays as forensics; the READER reads the commit-order fact only, because reading the queue label would have made the rule queue-only.

**ROUND-2 INVARIANTS.** (f) **Identity before content**: keying a record by what it SAYS collapses distinct facts — the corpus number (133 + 27 real deletions) is what that costs. (g) **A duplicate is only a duplicate within one producer**: two producers' records must never share a key namespace, or the dedupe silently means two different things. (h) **A message that does not CLOSE a turn's open streams must not HIDE them either** — the exemption is the same one, on both sides of that scan. (i) **Every claim must be retired on every path that drops the record standing for it**, including the duplicate short-circuit; an unretired claim is a deletion waiting for the same text to appear again. (j) **A producer that writes after the commit must SAY so**: the wrapper's three writers of a user record are now a census the suite counts, and a fourth answers "had the app-server committed it yet?" before it ships. (k) A negative control is a PATCHED COPY of the real module (asserted to have matched), never a hand-written imitation of what I believe the old code did.

**Round-2 gates.** scripts/test-queue-steer.mjs ⑨(j) — the two same-text steers (live + rebuild + a control at ONE bubble), two distinct codex submissions of one text (rollout alone + a control that deletes the third), a fork replay still collapsing, the drain twin (+ a control that doubles it when the record is marked early), the leaked-claim scenario, the legacy retirement (+ its control), the stranded stream (+ a control that leaves it streaming), and the producer census. scripts/test-codex-p2-wrapper.mjs ⑥ — the REAL wrapper's own records rebuilt against synthesized codex copies (14 messages × 2 producers → 14 bubbles, in order), and each inherited record naming its producer. scripts/test-peer-msg-card.mjs — both peer orders, with the idle path declaring the commit order the wrapper now writes.

**ROUND 3 — two more the next adversarial verifier reproduced, and the third producer the measurement found.**

5. **The twin that could never collapse: an ATTACHMENT (minor, but it hit every image message).** Round 2's own comment claimed our copy was "BYTE-IDENTICAL to codex's rollout copy … for text and data-URL images". It was true of `userInputToContent` and false of its sibling three hundred lines below: `handleInput` hand-rolled `content: [...attachments.map(input_image), ...(text ? [input_text])]` — **attachments first** — while codex persists what `encodeUserInput` SENT, i.e. the text first. Measured on the whole local corpus (97 plain rollout files, **5489 user records**): **0** begin with an `input_image`, every `input_text` block is exactly `{type,text}`, and **34 of the 40** `input_image` blocks carry a third key `detail` that only codex writes. Two different keys ⇒ no claim made, no claim consumed, **two bubbles for one message** — reproduced, with the text-only control on the identical scaffold at one.
6. **A claim whose twin NEVER ARRIVES (minor) — round-2 finding ④ through a second back door.** Round 2 retired a claim when a *duplicate* codex record was short-circuited by `seen`; it did nothing for a claim whose twin never existed at all. Our copy is written BEFORE the submission reaches the app-server, so there are whole classes of send that never become a user message: every wrapper-served slash command (`/compact`, `/review`, `/model`, `/effort` — answered by the wrapper, `record()` runs before `applySlashCommand`), every send whose RPC throws, and every queued item Stop or the user removes before it runs. Reproduced against the shipped module: ours `continue` in turn A + a codex-only `continue` in turn B ⇒ ONE bubble, **the turn-B message deleted**, at any turn distance. Codex's copy carries no client id (all 389 user records in the 2026-09 corpus have only `internal_chat_message_metadata_passthrough`), so the pair genuinely cannot be joined by id — the claim must be **withheld** instead.
7. **THE THIRD PRODUCER, found by measuring (5) end to end.** `CodexAdapter._buildUserPreview` builds the same hand-rolled attachments-first array on the SERVER and ws-handler appends it to `session.buffer`, so a typed message has **two copies of ours** in the buffer, both carrying the same `webui_msg_id`. The preview is written first, so it WINS the fingerprint and **its** content key is the one that claims — a fix that only straightened the wrapper's spelling would have changed nothing in production. Measured: preview + wrapper + codex ⇒ 2 bubbles before, 1 after.

**THE ROUND-3 RULES.** (l) **One message, one spelling, across every producer of our own record.** Our copy is `userInputToContent(encodeUserInput(text, attachments))` in the wrapper and the same order in the server preview; a field only ONE producer writes (codex's `detail`) is normalised out of the twin key, exactly as `TWIN_VOLATILE_FIELDS` does at the record level — inside the content array now. (m) **A submission that never reaches the app-server claims nothing.** Every such case says so out of line — the `webui_user_retracted` event, which names the record by IDENTITY (round 3 also declared the known-in-advance case on the record with `webui_no_commit`; **round 4 deleted that half, see below**) (a data URL can be megabytes, and re-deriving the reader's content key inside the wrapper would be a second copy of that algorithm). The merge maps identity → the content key that claimed under it and withdraws exactly one claim; two different retractions of one record never go negative, an identical one from two sources is folded by the fingerprint, and a retraction naming an id we never emitted is a no-op. **The BUBBLE always stays** — the user really sent that text, `task_failed` is what reports the failure, only the claim goes; and an item the app-server had already DRAINED (`{deleted:false}` = it RAN) is never retracted. (n) **The queued PEER copy finally has an identity**: the app-server cid it was queued under, carried as the same second-class `webui_queue_id` an inherited bubble uses (never `webui_msg_id`, which suppresses the peer card) — so Stop can retract it by name, and two peer messages with the SAME text inside one turn stop collapsing into one, which was round-1's MAJOR still open for the one producer that minted no id.

**ROUND 4 — THE HALF OF (m) THAT COULD NEVER RUN, AND A FILE NO SEARCH COULD READ.** Two findings, both reproduced first.

8. **The write-time declaration was INERT in production (minor, but it silenced its own defect).** Round 3's own finding (7) had already named the third producer: `CodexAdapter._buildUserPreview` writes the SERVER's copy of a typed submission into `session.buffer` (ws-handler) under the SAME `webui_msg_id`, it lands FIRST, and **it is therefore the copy that wins the fingerprint and makes the claim**. That copy carries no marker and cannot: only the wrapper knows which texts are its own slash commands (`SLASH_COMMANDS` is advertised through `wrapper_meta`), and a second regex in the adapter is precisely the drift round 3 wrote its comment block to prevent. So `webui_no_commit` was written onto the copy nobody reads — measured on the production scaffold (preview + wrapper + a later codex-only `/compact`): **1 bubble, the turn-9 message deleted**, exactly the defect (m) was written against. The r3 regression leg was green only because its scaffold omitted the preview — the very producer the same round had identified. **The fix uses the mechanism that already survives the preview**: the wrapper retracts by ID (`retractUserRecord(msg.msgId, 'served by the wrapper as a slash command')`), and the id is a fact BOTH copies carry. It is withdrawn **before** `applySlashCommand` runs, not after: `/compact` takes 1–2 minutes and a wrapper killed inside that window would otherwise leave a claim standing over a message it never sent. One gate now decides both (`isWrapperSlashCommand`), so the two cannot disagree. The marker is gone from every producer; it survives on `WEBUI_USER_MARKERS` as a **compat strip only** (wrappers outlive an update inside dtach — 2.361.1 — and a record from an r3-era wrapper must still key like the same message, or those buffers double every typed message on reload).
9. **Two raw NUL bytes made a module invisible to search (minor).** `src/codex-session-store.js` used `\x00` — the actual byte — as the separator inside the per-turn Map key, and echoed it in the comment above. `file(1)` reported "data"; `grep -n USER_RETRACTION_EVENT` **on the very file that defines that constant** printed nothing and exited 1; ripgrep answered "binary file matches" with no line. Every search-driven read of that module — a review, an incident, a twin sweep — skipped it silently. Fixed by spelling it `\u0000` (byte-identical at runtime, verified), and the same class was found in `src/incident.js` (the `startsWith` guard against transcript-service's `DEVICE_HANDLE` sentinel, on master since B-8ebb; the raw byte made that file binary too and the guard read like a typo, so it is now spelled as an escape WITH the comment naming the convention — behaviour untouched).

**THE ROUND-4 RULES.** (o) **A declaration is only worth what the reader can see.** When two producers write the same fact, a marker on one of them is not a rule — it is a coin flip decided by write order. State it about the SUBMISSION (an id both copies carry), not about one copy of it. (p) **A guard that must be withdrawn later should be withdrawn at the moment of decision**, not after the long operation it guards — a process killed in between leaves the wrong state permanent. (q) **A regression leg must be built from the producers PRODUCTION has**, not from the ones the fix happens to touch: the r3 leg passed on a scaffold missing the producer its own commit message named. (r) **Source files are text.** A control byte in source removes the whole file from every text tool the team uses; a separator that cannot collide with the data is a fine idea, spelled as an escape.

**Round-4 gates.** scripts/test-codex-p2-wrapper.mjs ⑦ — the REAL wrapper now retracts the slash command (asserted BEFORE `compact_started` in the event stream, and its record asserted to declare nothing), the retraction census is SIX, and the rebuild scaffold carries **one server preview per chat-input, built by the real `CodexAdapter._buildUserPreview` from the same frame the client sent** — 12 bubbles, with two controls: strip all retractions (5 messages deleted) and strip ONLY the slash command's (the turn-9 `/compact` deleted, 11 of 12). scripts/test-queue-steer.mjs ⑨(j9) — the production scaffold end to end, plus **round 3's own reader rebuilt by patch**: green on r3's scaffold (wrapper record only, 2 bubbles) and red on the production one (preview first, 1 bubble) — the finding pinned as a control; a census asserting no producer writes the marker; the legacy marker still stripped from the fingerprint, the twin content key and the normalizer's record key. scripts/test-architecture.mjs #42 — no file under `src/`, `data/bin/` or `scripts/` carries a NUL byte (binary fixtures excluded by extension), with a temp-directory control that plants one and confirms neither the `\u0000` escape nor a `.zst` fixture is a false positive; verified RED against the pre-fix bytes (**that WALK is superseded by round 5 below** — it read the working tree, which is where the product installs its own 64 MB `rclone`).

**ROUND 5 — THE CENSUS THAT READ THE WORKING TREE INSTEAD OF THE SOURCE.** One finding, MAJOR, reproduced first.

10. **The NUL census failed `npm run build` on every instance that had ever used a storage mount (major).** Round 4's #42 walked `src/`, `data/bin/` and `scripts/` with `readdirSync` — but **`data/bin` is where the product installs its own runtime artifacts**: `installRclone()` writes a 64 MB arch-specific `rclone` there (gitignored since 2.368.9, boot self-heals it when missing), next to `rclone-dl.zip` and the boot-generated `vibespace-status` / `vibespace-agentd.js`. Byte 7 of that download is a NUL, so the assert read `✗ … (data/bin/rclone (byte 7, line 1))` and the build exited 1 — reproduced by copying the production instance's own 63,668,408-byte `rclone` into a clean worktree. The blast radius is what makes it major: `npm run build` runs the suite AFTER esbuild, and that build is the pre-push **release gate** and the in-app **"Update VibeSpace…"** step (`scripts/update.sh`), so an affected instance could neither push nor self-update — the service simply never restarted. The extension blocklist could not save it (the artifact has no extension, and the *next* 64 MB download would evade a new entry too), and neither could a path exclusion: the census must still read the tracked CLIs that live in that very directory. **The fix changes what "source" means**: the file list comes from `git ls-files -z -- src data/bin scripts` (432 files; ~3 ms to list, ~25 ms to read the 9.3 MB — where the walk it replaced read that 64 MB binary in full just to look at byte 7), so every gitignored or otherwise untracked runtime artifact is out of scope BY CONSTRUCTION. Tracked-but-deleted files are skipped; the binary-fixture extension exclusion stays for tracked `.zst`/`.png`/… fixtures. When git cannot answer for the tree — no git binary on PATH, or no metadata at all (tarball / `git-archive` / `npm pack`) — the source list is unknowable and the census **SKIPS with a reason** instead of failing: verified both ways (a `git archive` export of this branch, and the suite run under `env -i PATH=/tmp/empty-path-dir`, both green with the 64 MB binary present).

**THE ROUND-5 RULES.** (s) **A census over "the code" must read the SOURCE LIST, not the working tree** — the working tree also holds what the product itself writes there, and a gate that a normal user action turns red is a gate that gets bypassed. `git ls-files` is the structural answer; an extension/name blocklist is a guess that the next artifact invalidates. (t) **A check that cannot establish its own scope must SKIP with a reason, never fail** — failing a build over files it was never meant to read is a false positive with the blast radius of the release gate. (u) **An assertion that can pass vacuously is not an assertion**: the census now also pins that its listing really covers `src/` + `scripts/` + `data/bin/` (naming the incident's own module, this suite, and a tracked extension-less `data/bin` CLI), so a mis-scoped or empty listing is loud rather than green.

**Round-5 gates.** scripts/test-architecture.mjs #42, rebuilt: the census itself (432 tracked files) + the **scope pin** (a vacuous listing cannot pass) + three controls in a throwaway `git init`/`git add` repo — (i) a tree with **no git metadata SKIPS**, (ii) a **tracked** NUL is found while the `\u0000` escape and a `.zst` fixture are not, (iii) **planted-but-untracked is invisible**: an extension-less gitignored `data/bin/rclone` and an untracked `src/untracked.js` full of NULs never reach the census — *that is the point of the round*, so it is asserted rather than assumed. Verified RED for a real regression (a NUL file `git add`ed into `src/` fails the suite; the identical file unstaged passes), and `npm run build` verified GREEN end to end with the 64 MB binary still planted under `data/bin`. NOTE: `origin/master`'s `scripts/test-writer-sweep.mjs` carries a raw NUL at line 2016 (confirmed by reading `origin/master:` — byte 157114), so the census will be red on the integration target until that literal is spelled as an escape; that file is not on this branch.

**ROUND 6 — THE CENSUS'S OWN GIT RAN IN SOMEONE ELSE'S ENVIRONMENT.** Two findings, both reproduced in throwaway `/tmp` repos before a line was changed.

11. **Round 5's controls ran `git init` / `git add -f` with the inherited process environment, so they could rewrite a REAL repository (major).** Git's entire repository-location layer lives in environment variables — `GIT_DIR`, `GIT_WORK_TREE`, `GIT_INDEX_FILE`, `GIT_COMMON_DIR`, `GIT_OBJECT_DIRECTORY`, `GIT_ALTERNATE_OBJECT_DIRECTORIES`, `GIT_CEILING_DIRECTORIES`, `GIT_PREFIX`, `GIT_NAMESPACE`, and the config channels `GIT_CONFIG_PARAMETERS` / `GIT_CONFIG_COUNT`+`GIT_CONFIG_KEY_n`/`VALUE_n` (which reach the same place indirectly, via `core.bare` / `core.worktree` / `safe.directory`). A `git -C <tmpdir> …` obeys them all. And this suite does not choose its own process: it runs inside `npm run build`, which runs inside the pre-push **release gate** and the in-app **"Update VibeSpace…"** — i.e. inside hook processes that export exactly those names (measured on git 2.51: a `pre-commit` hook is handed `GIT_INDEX_FILE` and `GIT_PREFIX`; `GIT_DIR` is normal for any tool driving a worktree). Three reproductions: **(a)** `GIT_DIR` at a linked worktree's gitdir ⇒ the control's `git init` REINITIALISED that repository and flipped `core.bare` `false`→`true` **in the shared config**, after which `git status` in the main checkout dies `fatal: this operation must be run in a work tree` — which breaks `scripts/update.sh`'s `git pull --ff-only`, `ci.mjs`'s green marker, and (finding 12) makes the census itself SKIP green forever; *this happened to the production checkout and was repaired by hand*. **(b)** `GIT_INDEX_FILE` at another repo's index ⇒ the REPO census read the FOREIGN listing (432 files → 1), so the suite went **RED describing a tree that is not ours**, while `git add -f` staged the raw-NUL fixture into that repository and replaced its `.gitignore` index entry. **(c)** plain repo + `GIT_DIR` ⇒ the fixture is staged there too. **The fix is ONE sanitized environment**, built above `trackedSource` and passed to every git the block spawns, read side and write side: 25 redirecting names deleted (plus the numbered `GIT_CONFIG_KEY_n`/`VALUE_n` pairs), while `PATH`, `GIT_EXEC_PATH` and the strictly-more-isolating `GIT_CONFIG_NOSYSTEM` / `GIT_ATTR_NOSYSTEM` are deliberately kept (they only *remove* ambient system files). The write side additionally names its target on the command line (positional directory for `init`, `--git-dir`/`--work-tree` for `add`/`config`, `cwd` = the tmp repo) — but that is belt-and-braces only, because **it is not sufficient**: measured, `git --git-dir=A --work-tree=A add` under `GIT_INDEX_FILE=B/.git/index` still writes B's index. The environment is the load-bearing layer.

12. **The SKIP branch counted as a passing assert and asserted a cause it never verified (minor).** It printed "no readable git index (export/tarball)" without checking anything of the sort. A checkout whose `core.bare` was flipped TRUE — exactly what finding 11(a) leaves behind — prints the *same green line*, even though `git ls-files` there answers perfectly; only `rev-parse --show-toplevel` dies, because bare means "no work tree". So the accident both disabled the census and told everyone the tree was a tarball. Now `--show-toplevel` failing falls back to `git -C <base> rev-parse --git-dir`, with ownership = `<base>/.git` is itself a repository entry — so a tmpdir that merely *sits inside* another repo still cannot borrow that repo's index, bare or not — and every skip carries git's own failing command plus its trimmed stderr. Re-verified that the census stays SKIP-with-reason (never red) when git is absent from `PATH`: `git -C <base> rev-parse --show-toplevel: spawn failed: ENOENT; --git-dir: spawn failed: ENOENT`.

**THE ROUND-6 RULES.** (v) **A test that has to run inside someone else's process must NAME the environment it runs its own tools in** — inheriting it hands the caller the choice of which repository/index/config your tool writes, and the release gate is exactly such a caller. (w) **An explicit target on the command line is not isolation**: `--git-dir`/`--work-tree` do not override `GIT_INDEX_FILE`; sanitize the environment *and* name the target, and know which of the two is actually protecting you. (x) **A SKIP must quote the failure, not guess the cause** — a green line that invents its own reason disguises "this gate was switched off" as "there was nothing here to check", and it survives every later reading of the log.

**Round-6 gates.** scripts/test-architecture.mjs #42, controls rebuilt: the **sanitizer pin** (all 25 redirectors plus `GIT_CONFIG_KEY_0`/`VALUE_0` gone from a deliberately hostile environment, `PATH` and `GIT_CONFIG_NOSYSTEM` kept); the **finding's own negative control** — two decoy repos, one hostile `GIT_DIR`+`GIT_WORK_TREE`+`GIT_INDEX_FILE`+`GIT_CONFIG_*`: through the sanitizer the decoy's `config`/`index`/`HEAD` are **byte-identical** afterwards *and* the controls are asserted to have really done their work in the throwaway repo (so the assert is not vacuous), while the same write run **raw** is asserted to reach the other decoy (so "unchanged" is the sanitizer, not a control that never ran); the **`core.bare=true` control** (`--show-toplevel` asserted to fail there, and the census asserted to RUN and find the tracked NUL anyway); the **ownership pin** that the `--git-dir` fallback did not weaken (a directory inside another repo borrows nothing, bare or not); and every SKIP asserted to quote `rev-parse`. Verified end to end: `npm run build` green, and green again under `GIT_INDEX_FILE=<throwaway>/.git/index` and under `GIT_DIR=<throwaway worktree gitdir>` with the throwaway repos' whole `.git` trees (64 files) byte-identical before and after and the REPO census still reading **our** 432 files; SKIP-with-reason confirmed with git absent from `PATH`. test-queue-steer 265 + test-codex-p2-wrapper 250 unchanged.

**ROUND 7 — THE NEGATIVE CONTROL WAS THE LAST THING STILL RUNNING IN SOMEONE ELSE'S ENVIRONMENT.** Two findings, both reproduced in throwaway `/tmp` repos first.

13. **Round 6 sanitized every git except the one whose whole job is to be unsanitized (major).** The RAW leg — the negative control that proves the sanitizer matters — built its `git add -f` environment as `{ ...process.env, GIT_DIR, GIT_WORK_TREE, GIT_INDEX_FILE }`: **three** of the 25 names `GIT_REDIRECTORS` enumerates pinned, the other **22** still ambient. A control may differ from the guarded run in exactly the ONE way it is testing; this one differed in 23. Measured on git 2.51, each in its own throwaway repo: with `GIT_OBJECT_DIRECTORY` — or `GIT_COMMON_DIR` — exported at a **third** repository, that write puts its blobs there (`objects/e1/5da8…`, `objects/fa/7947…`) while `npm run build` stays **green**; and when the same name points at a nonexistent directory, a `0500` directory, or a plain non-repo, `npm run build` goes **RED** on the line *"the guarded assert above is the sanitizer working, not the controls failing to run"* — a message that accuses the sanitizer of a failure caused by a variable no line in the file ever names (`fatal: not a git repository: '<exposed>/.git'` / `insufficient permission for adding an object to repository database`). Round 6's `repoStamp` could not have caught the silent half either: it read `config`/`index`/`HEAD` only, and the object channel touches none of the three. Fix: the raw leg's base is `GIT_ENV` plus exactly the three decoy redirectors it is testing, and `repoStamp` hashes **every file under `.git`** (recursive readdir + sha256 per file).

14. **The census sentence overclaimed its scope (minor).** "no source file carries a NUL byte" was measured over `git ls-files -- src data/bin scripts` — **432 of the repository's 556 tracked files**. The 124 outside included `server.js` (the bootstrap this same suite ratchets to ≤2100 lines), `install.sh`, `run.sh`, `editor-helper.sh`, `deploy/docker/*.sh`, `docs/screenshot-helper.js` and `docs/examples/hello-plugin/server.js` — every one a file a human greps, none of them protected. The roots were never the load-bearing part (**"tracked"** is what puts the 64 MB rclone out of scope), so the pathspec is gone: the census reads the whole index (measured 556 files; 24 skipped by extension — 4 `.png` + 20 `.woff2`, the only genuinely binary tracked files — and **0** offenders), and the scope pin now names `server.js` and `install.sh` beside the three root witnesses. A new binary fixture ⇒ extend `BINARY_EXT` by extension with a comment naming the file; never a directory exclusion (the tracked CLIs in `data/bin` must stay in scope).

**THE ROUND-7 RULES.** (y) **A negative control is a control**: it may differ from the guarded run in exactly the ONE named way it is testing — `{ ...process.env, <3 names> }` is not "the raw environment", it is 22 uncontrolled variables wearing a label, and the harm runs both ways (a silent write into a stranger's repository, or a RED release gate whose message blames the wrong thing). (z) **A tamper-detector must cover the channel the tamper uses**: `config`/`index`/`HEAD` cannot see an object write, so the stamp guarding against `GIT_OBJECT_DIRECTORY` has to hash the object store too — hash the whole artifact, not the three files you happened to think of. (aa) **A scoped measurement must state its scope in the sentence that reports it**, and when the scope turns out to be an unexamined sample (three directories out of a whole index), widen it rather than reword it.

**Round-7 gates.** scripts/test-architecture.mjs #42, controls rebuilt again: the census is the **whole index** (556 files) and the **scope pin** names `server.js` + `install.sh` beside the three root witnesses; `repoStamp` hashes every `.git` file; the raw leg starts from `GIT_ENV`. Two new asserts, two halves of one control: `GIT_OBJECT_DIRECTORY` **and** `GIT_COMMON_DIR` are exported into this process at an `ambient-third` repository for the **whole** census block and it must be byte-identical at the end (with the environment asserted to still be hostile, so the assert cannot pass vacuously), while a **sacrificial fourth repo** proves the PRE-FIX base still reaches one — if that leg ever goes green-by-nothing, the ambient environment is not hostile and the first assert means nothing. Verified: `npm run build` green in five legs — plain, and under ambient `GIT_OBJECT_DIRECTORY` / `GIT_COMMON_DIR` / `GIT_INDEX_FILE` / `GIT_DIR`+`GIT_WORK_TREE` pointed at throwaway repos — with every decoy's whole `.git` tree (21 files) byte-identical before and after and the REPO census still reading **our** 556 files; SKIP-with-reason re-confirmed with git absent from `PATH` (71 asserts, exit 0) and on a `.git`-less export copy. The fix's own negative controls are **patched copies of the real suite** with the anchor hit asserted: reverting the raw base alone turns the whole-block assert RED, and additionally narrowing `repoStamp` back to `config`/`index`/`HEAD` turns the round-7 negative control RED (the leak becomes invisible to both detectors at once). test-architecture 80; test-queue-steer 265 + test-codex-p2-wrapper 250 unchanged.


**Round-3 gates.** scripts/test-codex-p2-wrapper.mjs ⑦ — the REAL wrapper: a refused `turn/start`, a refused `thread/queue/add`, an explicit remove and a Stop sweep each producing exactly one retraction (and none for the message that landed nor for the slash command, which declares itself on the record); the image message's content compared to the `turn/start` params it was sent as; **the server preview and the wrapper's own record compared BYTE FOR BYTE through one client frame** (+ a control where the old preview spelling splits the pair); and a rebuild of the real records against a rollout that also carries the same texts later — 12 bubbles, with a control that strips the declarations and **deletes five real messages**. scripts/test-queue-steer.mjs ⑨(j8)-(j10) — the image twin (+ a code control with the block normalisation patched out, + a data control in the old order, + two different images staying two), the no-commit and retraction scenarios (each with a patched-module control that reproduces the deletion), the not-a-blanket-off-switch cases, `userTwinKeys`/`retractionIdOf` truth tables, the marker stripped from both keys, the peer-collision pair (+ its control), and wiring pins on all three producers.

## DESKTOP-RESUME PAGED A PINNED WINDOW INTO HISTORY (inc-mtq5bpjt-0o0n, after 2.369.42, owner "切换桌面后，新桌面的窗口内容跳到历史消息了")

Three chat windows on the target desktop — all PINNED at the live tail when the desktop was hidden — came back at `scrollTop 0` in old history with the tail trimmed away. The capture is unambiguous about intent: `wheelAgo` 1.8M–4.7M ms on every window, no touch/pointer, no re-attach. Per window the shape was identical: the resume re-measure bounced scrollTop (`1967→0→1976→3297→1950` in 240 ms, `pin=1` throughout), then ~2.9 s later `trimBottom removed 48`, `extendTop:done … anchored:false st 0`, `scroll 1950→0 pin 0`.
  (round-5 verifier note) The gutter hit-test is the MINIMAP-OFF path only: `.chat-minimap-active` sets scrollbar-width:none, so on every conversation with ≥3 turns the native gutter is 0 px and a reader positions via the minimap, whose callback stamps `_noteUserNav('minimap')` — that is the production carrier of "a positioning act during the resume settle"; the gutter branch covers short conversations and minimap-off windows.

The mechanism was NOT a missing suspend: 2.369.1 suspends a desktop-hidden ChatView and 2.369.3 gives it `content-visibility:hidden`, and both held. The hole was the **resume TRANSITION** plus the one upward-paging entry point that the whole 2.301→2.339 gate campaign never touched. The scroll handler's branch needs `!pinned && userRecentUp`; the wheel branch needs a wheel; neither fired. The gap sentinel's `IntersectionObserver` (`_observeHistoryGap`, `rootMargin: '300px 0px 0px 0px'`) did: it is a PURELY GEOMETRIC trigger, and `_loadEarlierGap`'s tail-mode branch — `if (!this._teleported && this._windowStart > 0) { await this._extendTop(); return; }` — had **no intent gate, no pin gate, no settle gate and no suspend gate at all**. `_showWin` cleared content-visibility, the subtree re-measured, scrollTop read 0 while still pinned, the sentinel entered the 300px margin, and 50 messages were paged in with zero user intent. `_extendTop`'s completion then did the damage: `trimBottom` dropped the live tail (`windowEnd < total`), the element anchor — measured under transitional geometry — failed (`anchored:false`) and the delta fallback clamped scrollTop to 0. `setSuspended(false)`'s rAF re-tail had already run, BEFORE the fetch resolved, so it could not undo any of it. Only sessions >34 MB are affected, because the sentinel only exists when the server reports a gap (all three transcripts were 68–868 MB).

FIX, five parts, all structural:
1. **ONE gate predicate, `ChatView._autoPagingBlocked()`**, returning a REASON (`disposed / suspended / resume-settle / pinned / settling / no-input / input-resize`) — exactly the laws the scroll handler already obeyed, in one place instead of re-invented per entry point. `_loadEarlierGap` applies it for every AUTOMATIC caller (the observer, and the scroll/wheel-driven `_maybeSeekEarlier`) BEFORE the tail-mode branch, and traces `gapSkip {why, via}` so the tracer shows the refusal. An explicit retry CLICK passes `{ auto: false }` and bypasses it — a click is intent.
2. **A resume settle**, `_resumeSettleUntil` (1200 ms, sized from the capture's +366…+602 ms bounce), armed by `setSuspended(false)`: the scroll handler no-ops (BEFORE it touches the pin — transitional geometry must not unpin either), the observer path no-ops, the loadHistory auto-fill DEFERS (bounded re-arms) instead of deciding, and the pinned re-tail is re-asserted once when the settle expires. Real user input (wheel/touch/pointer/key) clears the window immediately — the settle suppresses input-LESS displacement only, never a reader.
3. **INVARIANT: a pinned view never loses its tail.** `_extendTop` does not `trimBottom` while `_pinned` (that trim is what converts an accidental page-up into permanent damage) and re-asserts the bottom after the prepend; both are traced (`trimSkipPinned` / `pinnedRetail`). The DOM stays bounded because the live-append path trims the TOP while pinned and the next genuine (unpinned) page-up trims normally.
4. **The settle covers the re-tail, and the re-tail asserts off a SNAPSHOT** (the verifier's minor on this fix, reproduced before fixing). As first written the settle expired at `RESUME_SETTLE_MS` while the pinned re-tail ran 40 ms later, and the re-tail refused once the live `_pinned` flag was gone — so an input-LESS displacement landing in that 40 ms gap (repro: `scrollTop = 0` injected at resume+1210 ms) reached the scroll handler, unpinned the window, and the re-tail then declined to bring it back: `fromBottom 1536`, permanently. Two changes close it: `_resumeSettleUntil = now + RESUME_SETTLE_MS + RESUME_RETAIL_SLACK_MS` (the window now ENDS at the re-tail, so nothing can decide in between), and `setSuspended(true)` snapshots the pin into `_pinnedAtSuspend` — the last honest reading, taken before the geometry starts lying — which the re-tail asserts on (`!_pinned && !_pinnedAtSuspend`) and then clears. A window that was pinned when it was hidden returns to the tail regardless of any transitional unpin on the way back, while real input runs `_endResumeSettle()` (settle AND snapshot), so a reader who scrolls away during the settle is never yanked to the bottom.
5. Every pre-existing gate stays byte-identical on the non-resume paths (2.307/2.308/2.338/2.339 are pinned by test-chat-trim-guard).

**INVARIANTS: (a) every AUTOMATIC paging trigger — including observer callbacks, which are pure geometry and fire with no event to inspect — goes through the same intent gate; an entry point that pages without asking "was this the user?" is the bug, whatever the mechanism displaced scrollTop. (b) When a state has a "hidden" guard, the TRANSITION out of it is a separate, unguarded state — guard the re-measure window explicitly, and let real input, not a timer alone, end it. (c) A pinned view is AT the live tail by definition: nothing automatic may page it up, and nothing may trim its tail. (d) The guard belongs on the path that is still alive in the failure state — here `_loadEarlierGap` itself, not its call sites. (e) A settle window and the repair it protects must not have a gap between them, and the repair asserts on state captured BEFORE the corrupting transition (a snapshot), never on the live flag the transition is free to corrupt.** **ROUND 2 (two more, both reproduced in headless chrome on the same 36 MB window): (f) A repair that runs on a TIMER must ask whether the user has since acted — only the four message-list listeners ended the settle, so a reader who navigated through a surface that is NOT the list (the minimap lives on the container, the floating run bar on `this._container`, `jumpToIndex` and a search reveal touch neither) was YANKED back to the live tail 2.2 s after their jump; every off-list positioning surface now stamps `_noteUserNav` and the repair compares nav-vs-resume (the chat-view-seek `userScrolled` idiom generalised: `_lastNavAt`, `_lastUserScrollAt`, `_lastJumpAt`, `_search._lastRevealAt`). (g) A time-boxed guard is a CLIFF, and the failure walks right off it — an input-less displacement at resume+1400 ms unpinned and stranded the window 3/3 (the +1240/+1280 probes had only survived because `_forceScrollToBottom`'s 10-frame chain happened to cover them). The fix is the same shape as the paging gates: gate the STATE CHANGE (the unpin) on positive evidence for the whole horizon, not just the transition on a timer — plus a bounded re-assert series. Corollary (h): a no-op guard must sit below the pure READOUTS it does not own — the settle `return` above `_updateRunBar` froze the floating run bar for 1.24 s of every desktop switch.** **ROUND 3 (reproduced with TRUSTED CDP input — `Input.dispatchMouseEvent`, not a synthetic `dispatchEvent`): (i) A CLICK IS NOT A POSITIONING ACT. Rounds 1–2 gave every message-list listener the same effect — `_endResumeSettle()`, which zeroes the settle window AND drops `_pinnedAtSuspend` AND clears the bounded re-tail series — so a plain left-click or tap anywhere in the message list during the 1.24 s settle disarmed the entire repair, and the resume's OWN input-less displacement, arriving a beat later behind the click, reproduced the incident (measured: click at +300 ms, `scrollTop = 0` at +1400 ms → unpinned, `fromBottom 1883`, stranded). The effects are now split by GRADE. **INPUT** (`_noteUserInput`: a pointerdown, a non-navigation key) stamps `_lastUserScrollAt` — the evidence the paging gates' `no-input` rule wants — and may end the settle WINDOW, nothing more. **POSITIONING** (`_notePositioning`: wheel, touchmove, the navigation keys PageUp/PageDown/Home/End/Arrow*/Space, plus `_noteUserNav`'s six off-list surfaces) is the only grade that drops the pin snapshot and cancels the re-tail series, and it is the only stamp `_navigatedSince` reads (`_lastPositionAt`, never `_lastUserScrollAt`). THE SCROLLBAR DRAG is the one positioning act with no event of its own, so it is recognised by a SIGNATURE (`_pointerDragScroll`, stamped in the scroll handler ABOVE the settle return so the drag can end the very settle it starts inside) — round 3 wrote that signature in TIME (a scroll within 400 ms of a pointerdown that moved the view), which round 4 below replaces with the PLACE the press landed. Generalised: **a guard's exit condition is the act that makes the guard WRONG, not any evidence that a human is present** — the resume repair is wrong only once the reader has MOVED the view, so only moving may cancel it. KNOWN RESIDUAL (round-3 MINOR, backlog B-8e5e, deliberately not fixed here): a real wheel-up at resume+1400 ms pages as it should but is re-PINNED about 1/3 of the time, because under collapsed geometry the scroll handler makes no boundary decision at all (`collapsedGeomSkip`) and `_extendTop`'s own pinned-tail invariant re-asserts the bottom — that final pin state belongs to the 2.301.0 guard, not to this fix, and asserting it here would pin someone else's mechanism.** **ROUND 4 (the round-3 SIGNATURE's own two MAJORs, both reproduced with trusted CDP input): (j) A SIGNATURE MADE OF TIME CANNOT TELL THE READER FROM THE ARTIFACT. "A scroll within 400 ms of a pointerdown that moved the view" is satisfied by the very displacement this fix exists to suppress — the incident's re-measure bounces run +366…+602 ms after the switch — so a plain CONTENT click landing anywhere near one was promoted to `scrollbar-drag`, ran `_endResumeSettle()` (window + snapshot + series) and stranded the window BEHIND the click: round 3's own repair, undone by round 3's own signature (its leg passed only because it put the click at +300 ms and the displacement at +1400 ms, outside the window; at +450 ms the fixed build ends pinned at the tail and the restored time-window control traces `userPos/scrollbar-drag → unpin → extendTop:done`, `fromBottom 1883`). (k) …and the same window LOST real readers: a drag whose first move comes more than 400 ms after the press — press, look, then drag, a scrollbar reader's normal tempo — was never positioning at all, so the bounded re-tail series pulled them back to the live tail: a NEW harm versus master, measured (`holdDrag` ends at `fromBottom 1440`; with the hit-test neutered the same drag ends `fromBottom 0`, re-pinned). THE FIX — key the drag on WHERE THE PRESS LANDED, not on when a scroll follows: `_pointerOnScrollbar(e)` hit-tests the press against the list's scrollbar GUTTER (`offsetWidth - clientWidth` minus the borders — on the right in LTR, on the LEFT in RTL, plus the bottom strip for a horizontal scrollbar; coordinates converted viewport→LAYOUT px first, because the uiScale body `zoom` scales `getBoundingClientRect`/`clientX` but not `clientWidth` — the 2.369.5 VNC-pointer class), the pointerdown stores the verdict in `_pointerDownOnScrollbar`, WINDOW-scoped `pointerup`/`pointercancel` listeners bound to `winInfo._listenerCtl.signal` clear it (a drag routinely releases outside the list; `dispose()` also removes them for views whose window outlives them), and `_pointerDragScroll` is simply "that flag is set AND the view moved more than `POINTER_DRAG_PX`". `POINTER_DRAG_MS` is deleted. A click in the content area therefore never becomes positioning however close in time a displacement lands, and a press-and-hold-then-drag is positioning for the whole press. With the semantic minimap on, the native scrollbar has zero width (`scrollbar-width: none`) so nothing can land in the gutter — correct, because there the reader drags the MINIMAP, which is already `_noteUserNav('minimap')`. Generalised: **identify an act by what DEFINES it (a scrollbar drag is a press on the scrollbar), never by a temporal correlation with its effects — a time window admits every artifact that happens to land inside it and excludes every human slower than it.** Pins: scripts/test-chat-trim-guard.mjs (112, incl. a DOM-free unit over the extracted decision table, a FUNCTIONAL one that runs `setSuspended` hide→show against fake geometry (a transitional unpin still re-tails, a real reader is left alone), and round 4's units over the shipped `_pointerOnScrollbar` — content click false / gutter press true / no-scrollbar list false / RTL gutter on the left / border ≠ scrollbar / a uiScale-zoomed rect that the un-converted comparison would call a gutter press) + scripts/test-desktop-resume-paging.mjs (35) — a headless-chrome A/B on a real >34 MB transcript across a real desktop switch, whose NEGATIVE CONTROL rebuilds the bundle with the gates patched out and must show `extendTop:done` on the resume, through an input-less `scrollTop→0` sentinel probe, and must strand the window on the resume+1210 ms gap injection that the fixed build ends pinned at the tail; round 2 adds a nav-during-settle leg (a jump at +400 ms must STAY where the reader put it), a 1240/1400/1900/2400 ms input-less displacement sweep that must all end pinned at the tail, a real wheel-up at +1400 ms that must still page, and a PER-MECHANISM negative control for each — neutered on the instance in the same run, because the source-level control rebuilds with `RESUME_SETTLE_MS = 0` and cannot host a timing-dependent repro. Round 3 adds three TRUSTED-input legs at resume+300 ms on the same window — a real left-click at the list centre (the +1400 ms displacement behind it must still end pinned at the tail; control: give the click the old full `_endResumeSettle()` and it strands again), a real `mouseWheel` up (it must unpin IMMEDIATELY and page, and `unpinSkipResume` must never fire against a reader), and a real native-scrollbar drag (`mousePressed` on the 15 px scrollbar + `mouseMoved`, with `.chat-minimap-active` removed first because the semantic minimap hides the native scrollbar — it must trace `userPos/scrollbar-drag` and leave the reader where they dragged; control: neuter `_pointerDragScroll` and the same drag is pulled back to the tail, st 0 → 624). Round 4 adds two more on the same window, each with its own instance-level control: a click at +300 ms with the displacement at **+450 ms** (fixed: pinned at the tail; control: restore the pre-fix time-window signature on the instance — it strands the window), and a gutter press HELD 900 ms before the first move (fixed: `userPos/scrollbar-drag`, the reader stays at `fromBottom 1440`; control: make `_pointerOnScrollbar` answer false — the same drag is yanked back to `fromBottom 0`), plus trusted-input asserts that a content press is not a gutter press and that the flag is cleared on release. **ROUND 5 (B-9702, the residue the round-2 leg left behind — and the first round MEASURED before it was explained): (l) OUR OWN SCROLL CHAIN IS AN AUTOMATIC WRITER TOO.** Round 2 taught the repair to cancel its re-tail TIMERS the moment the reader positions the view, and left `_forceScrollToBottom`'s 10-frame chain running — the other automatic writer, and the very one round 2's own note blamed for the +1240/+1280 probes surviving by accident. Measured, not assumed: the round-2 leg "a REAL wheel-up at resume+1400 ms still pages" was run 20× on that build and the reader was dragged back to the live tail **5 times (25 %)**, with one trace shape every time — `userPos/wheel` at +1399 ms, then `repin st:1760 sh:2468 ch:708 structAge:1404 fsb:-1 posAgo:3` at +1402 ms, then `trimSkipPinned` → `pinnedRetail` → `extendTop:done`. Read: the +1240 ms rung's chain wrote `scrollTop = scrollHeight` **3 ms AFTER the reader's own scroll**, the pin re-engaged off OUR write, and the pinned-tail invariant (part 3 above) then faithfully re-asserted the bottom — the reader's page-up silently undone. The backlog's hypothesis, "the collapsed-geometry window re-pins", was REFUTED by the same capture: `sh - ch` was 2.5 viewports and `collapsedGeomSkip` never fired there. **FIX: (l) the chain carries an EPOCH and `_cancelForcedScroll(via)` bumps it** from `_notePositioning` / `_noteUserNav` (never from `_noteUserInput` — a click still moves nothing), so every frame already queued finds itself orphaned instead of writing once more; a later `_forceScrollToBottom` starts a NEW epoch, so a cancel can never kill auto-follow (its own unit control). **(m) A MUTE MUST OUTLIVE THE WRITE IT MUTES**: `_programmaticScroll` was cleared in the same frame as the chain's final write, and a scroll event is delivered AFTER the callback that wrote it — which is exactly why the capture reads `fsb:-1` at the re-pin, i.e. the guard whose whole job is to reject our own displacement had already stood down for it. The release now runs one frame later, guarded on `_fsbActive` so a chain restarted in that gap keeps its mute. Same 20-run measurement after the fix: **0/20**, `repin` never appears, and 9 of 20 legs show `fsbCancel … frames:9` — the chain caught on its last frame, which is precisely how narrow the race was. **(n) An assert that declines to name the USER-VISIBLE outcome is where a residue hides**: the round-2 leg checked only "something paged" and "the unpin gate didn't fire", explicitly reasoning that the final pin state "is not this gate's to decide" — so it passed on every run where the reader lost their page-up. It now asserts the reader's outcome on a settled snapshot (unpinned, away from the tail, `windowStart` moved up, no `repin`), plus a DETERMINISTIC form of the race — a chain armed by hand immediately before the wheel — whose per-mechanism control neuters `_cancelForcedScroll` on the instance and must drag the reader back to `fromBottom <= 8`. Generalised: **every automatic writer of scrollTop must lose to a positioning act (a timer was only one of them), and a guard against our own writes must outlive the last of those writes.**

## A MEMO KEY THAT NAMED NEITHER THE PRICES NOR THE LEDGER (2.369.43, review batch — the 2.369.36 interval memo's two holes)
The inc-mtox23xw fix memoized `costBetweenMulti` on `ids|from|to|events-at-or-before-to`. Both remaining holes were reproduced before fixing. ① PRICING: the key names the LEDGER but not the PRICE TABLE, and `_cost` reads `this._pricing` live — so after a pricing edit (a tier rate or a per-account discount, POST /api/usage-stats/pricing or a config import through persistence.js) every already-memoized historical interval kept returning pre-edit dollars forever, poisoning the estimator's learned rates and every anchor's `costSince` while the Usage window (which does not go through the memo) showed the new numbers. Repro: a 10× opus tier edit and a 50% account discount each left the total frozen at $3.50 against a truth of $35.00 / $17.50. FIX: `UsageHistory.pricingToken()` = a sha1 of {tiers, accounts}, recomputed only when `_pricing` is REPLACED (the same object-identity check `_tier` uses), appended to the key; a ledger object that cannot version its prices is NOT memoized at all. ② EVICTION: `learnRates` walks one identity's anchor pairs in the SAME order on every recompute — a cyclic reference string, the one access pattern where FIFO (and LRU) go off a CLIFF instead of degrading. Measured on the shipped code: a 20000-key working set hits 100%, 21000 hits **0.0%** — a 5% overshoot destroys 100% of the memo and the per-pair ledger walk of inc-mtox23xw comes straight back. Real anchor files are append-only forever (production: 5124 qualifying pairs over 28 days across 8 identities, ~180 new/day, biggest identity 1126), so the 20000 cap is reached in months, not never. FIX: a RANDOM victim (O(1) via a parallel key array) — the same input now holds 53% at 1.3× the cap. Also: the memo moved from a module global to a WeakMap per UsageHistory, so two ledgers can never read each other's costs through a key that names neither. **INVARIANTS: a memo over computed values keys on every INPUT of the computation, not just the data it walks (a rate table, a discount, a rounding rule are inputs); if an input cannot be versioned, do not memoize; an eviction policy is chosen against the ACCESS PATTERN — recency policies are worst-case for repeated full scans, and "cap exceeded" must degrade, not cliff; a cache keyed by values that name no instance belongs to the instance (WeakMap), never to the module.** Pins: scripts/test-usage-ledger-perf.mjs (21) — price-edit/discount/clear parity vs brute force, hit-path parity, cross-ledger isolation, and a hit-rate floor at 1.3× cap (both negative-controlled: dropping the token reddens 4, true FIFO reddens the hit-rate assert at exactly 0.0%).

## THE SHORT-VIEW RESCUE THAT COULD NEVER FIRE (2.369.43, review batch — dead since 2.369.36)
`loadHistory`'s post-attach auto-fill exists because a view whose content does not fill the viewport has NO scrollable range: the scroll handler never runs, and on touch there is no wheel-up path either, so a window sits on two run headers with thousands of unreachable messages above. The 2.369.36 hardening (a re-attach rebuild read sh<=ch on a TALL pinned window while content-visibility heights were unresolved, fired `_extendTop`, and a late slab + trimBottom cost the live tail) added `rendered < 30` to the gate. That predicate is UNSATISFIABLE: every attach path ships `tail(50)` (ws-handler's `_normalizer.tail(50)`, `transcripts.page`), `_windowStart > 0` holds EXACTLY when those 50 arrived, and the semantic fold HIDES members (`.chat-run-collapsed`, display:none) instead of removing them — so `rendered` is ~50 whenever the gate is armed. Measured over 33 real production transcripts with total>50: 50 of 50 tail messages render a `.chat-msg` child, min 50, none below 30. The rescue was dead code for everyone, and the fold-dominated case (inc-mtajy6wr's cousin: 50 cards collapsing to ~240px in a 700px viewport) had no way out on touch. RE-DERIVED from the intent instead of the symptom: the indeterminate geometry is TRANSIENT — the same premise `collapsedGeomSkip` runs on ("heights resolve within ~1.5s") — so CORROBORATE the reading: `_shortViewNeedsFill` is a named predicate over the injected list geometry, and `_scheduleAttachFill` measures at 700ms and again 900ms later, extending only if both readings agree AND `_lastStructuralAt` did not move in between (any paging/trim/desktop-resume hands the decision back to the machinery that owns it). The card count survives only as the HARM bound (`rendered + 50 <= 150`): the rescue adds ONE page and never at a size where a trim could eat the live tail. Since inc-mtq5bpjt-0o0n the whole schedule also DEFERS past the desktop-resume settle (bounded re-arms, and the second reading re-checks it) — a resume is the same class of artifact, and the resume path owns the tail while it is running. **INVARIANTS: a guard added under incident pressure must be shown to be SATISFIABLE on the path it guards — a condition that is always false is indistinguishable from deleting the feature, and nothing reports it; when the honest signal is transient noise, corroborate it across the settle window rather than proxying it with an unrelated quantity; DOM-free client predicates get functional tests (chat-view.js imports in plain node) — the pre-fix gate looked plausible in review and was arithmetically impossible in production.** Pins: scripts/test-chat-trim-guard.mjs (61) — the fold-dominated case fires, the tall/no-history/suspended/disposed/teleported/over-cap cases do not, and the two-reading corroboration is driven end to end (artifact ⇒ no extend, genuine ⇒ exactly one).
## CODEX .ZST / DISCOVERY / REMOTE-FETCH BATCH (2.369.x, review findings — four ways a codex thread went missing or came back wrong)

**① A RATIO GUESS DECIDED WHETHER A THREAD EXISTED.** `readHeadText` (discovery-facts) read a bounded COMPRESSED prefix and one-shot decompressed it under `maxOutputLength: max(plainBytes*4, 1MiB)`; anything compressing better than ~4× threw EZSTBIG, the `catch { return ''; }` turned that into an EMPTY head, `extractCodexThreadMeta` produced `threadId: ''`, and the thread VANISHED from /api/sessions — then the empty meta was cached by mtime, so for a stopped thread it stayed gone until a restart. Measured on the dev box: compressing the 40 real `~/.codex` rollouts at the DEFAULT zstd level, 7 reproduced it (ratios 4.0–6.0, e.g. plain 1,115,325 → comp 195,087). Fix: `zstdDecompressHead` BISECTS over compressed prefix sizes under a fixed output cap (a truncated prefix decompresses to the plain text it covers — verified on this runtime) until ≥ plainBytes of plain text exist; an unreadable head now THROWS a coded error (`EZSTUNSUPPORTED`/`EZSTHEAD`; zero plain bytes out of non-empty compressed input is a failure, not emptiness) so the existing `extractFailed` rule refuses to cache it, and `_noteExtractFailure` logs the reason verbatim once per file per 10 min. **Invariants: a bounded reader must be bounded by CONSTRUCTION (bisect/stream), never by a guessed compression ratio; "unreadable" and "empty" are different answers and only the second may be cached; a degrade path that hides a thread must say so in the log.**

**② A CACHE GUARD MEASURED THE WRONG CLOCK.** The codex rollout walk's per-directory listing cache (`codex-session-store._listDirCached`) skipped the readdir when the dir mtime was unchanged AND `now - mtime > 2s` — the coarse-timestamp guard evaluated at LOOKUP time. Settledness is a property of the CAPTURE: a listing taken while the mtime tick was still open can miss a sibling created in the same second, and that sibling never bumps the mtime again, so once the clock passed mtime+2s the stale listing was trusted forever (the poll is 5s — the window is always over by the next lookup) and the second rollout never appeared in the session list. Fix: record `settled` at fill time; an unsettled capture is re-read on the next lookup until it is taken settled (the readdir-per-poll win is untouched for settled captures). **Invariant: a freshness/settle predicate is evaluated and STORED when the data is captured — evaluating it at read time answers a question about the reader, not about the data.**

**③ "PLAIN WINS" WAS A COMMENT, NOT A RULE.** `interpretDiscoveryLines` de-duplicated a thread's `.jsonl` and `.jsonl.zst` twins by keeping the FIRST entry, commented "plain wins — first in mtime order". Both producers sort newest-first (hosts.js `sort -rn`, agentd.js `b.mtimeMs - a.mtimeMs`) and compression happens AFTER the last write, so the compressed twin was always first — and the ssh scanner can only read a compressed head where the host has `zstd(1)`, so the remote card silently lost the cwd/name its plain twin's HC/NC lines carried. Fix: group by thread id, prefer the plain twin's facts EXPLICITLY, MERGE what each twin has (either fills a gap), mtime = the newer of the pair, CO on either twin marks the one thread running. **Invariant: when two files describe one entity, the preference is expressed in the predicate, never inherited from an upstream sort; a dedup that drops facts must merge them instead.**

**④ ONE CACHE SLOT, MANY REMOTE FILES.** `hosts._fetchRemoteByFind` keys `data/remote-jsonl/<host>/codex/<tid>.jsonl` by conversation id, but codex's `remoteFind` predicate matches the plain rollout OR its `.zst` twin, so `find … | head -1` could resolve a DIFFERENT file across polls (the host compresses a finished rollout). The meta recorded only {size,mtime,fetchedAt,slab}, so the append-only delta path concatenated the new file's bytes onto the other file's cached prefix and stamped the result complete — and a stopped thread never changes again, so the corrupt transcript was served forever. Fix: the meta records `remotePath` + `compressed`; a changed path invalidates the slot (full refetch, no cache-valid short-circuit); a compressed remote — or an already-compressed cache file, detected by magic bytes so pre-fix metas are covered — is never delta-appended; the locate pipeline is `find … | sort | head -1` so the pick is deterministic and prefers the plain twin. **Invariants: an incremental/delta sync must verify the SOURCE identity, not just its size and mtime; a cache slot shared by several possible sources records which source filled it; a locate that may match several files sorts before it picks.** **④b THE FIX DID NOT HEAL WHAT WAS ALREADY BROKEN (same batch, review follow-up).** Provenance protects the slots the FIXED code writes — but every slot in the field already carried a meta without `remotePath`, and the first cut read that as “same file” (`!meta.remotePath || …` is vacuously TRUE), so a cache the old delta path had ALREADY spliced (plain prefix + the twin’s bytes, stamped complete) kept passing the size/mtime short-circuit — forever, since a stopped thread never changes again and nothing else invalidates the slot. A migration whose trigger is “the remote moved” never runs for the exact population it was written for. Fix: a meta WITHOUT provenance is NOT valid — refetch whole ONCE and rewrite it (and it may not delta either) — plus the cache FILE now testifies about itself: its head magic must agree with the resolved remote (zstd bytes under a plain remote, or the reverse, = the other twin’s bytes in the slot) and a plain cache whose last 4KB hold a NUL byte or a zstd frame header is spliced (an append always lands its foreign bytes at the TAIL). The ONE exception to “heal by whole refetch” is a remote already past `maxBytes` — a whole refetch is impossible there (the delta path is how such a slot grew), and failing “too large” on a transcript that worked yesterday is worse than trusting the byte check, so a verified cache adopts the provenance we just resolved. **Invariants: when a fix adds a field the correctness check depends on, records written BEFORE it are not “unknown, assume fine” — they are the corrupted population, and the missing field must read as INVALID; a self-heal must not be gated on the broken input changing; and a cache’s own bytes are stronger evidence than its metadata about those bytes.** Pins: scripts/test-codex-zst.mjs ①b/③/④b/⑥/⑥b (real-shaped >4× fixture at default level, twin order both ways, utimesSync same-tick repro, stub device switching remote files under one slot, and a pre-written hybrid cache + old-shape meta that heals with the remote standing still — each with the pre-fix predicate asserted as the negative control).
## ACP WRAPPER: STOP DIDN'T STOP, DENY RAN THE TOOL, AN EXIT-0 AGENT DIED SILENTLY (review batch over data/bin/acp-wrapper.js, after 2.369.31)
## ACP WRAPPER: STOP DIDN'T STOP, DENY RAN THE TOOL, AN EXIT-0 AGENT DIED SILENTLY — AND THE STOP THAT FIXED #2 SILENCED THE BOOKKEEPING NUDGE (review batch over data/bin/acp-wrapper.js, after 2.369.31)

Four defects in the generic ACP v1 client plus the round-2 follow-up ②b that the ② fix introduced, all reproduced against the REAL wrapper over pipes (scripts/dev/mock-acp-agent.mjs), all pinned in scripts/test-acp-harness.mjs (91).

**① Stop lost a race with the prompt-context fetch.** `runPrompt` claims the turn SYNCHRONOUSLY (so a second message queues) and only then awaits `fetchContextPrefix()` — a real HTTP round trip to /api/agent/prompt-context. A Stop landing inside that await saw `activePrompt`, flipped `cancelled` and sent `session/cancel`; the await then returned and dispatched the prompt anyway, so the wire read `initialize → session/new → session/cancel → session/prompt` and the agent — which had no turn to cancel — ran the whole turn the user had just stopped (and, like the mock, an agent may LATCH that cancel and kill the NEXT turn instead). Fix: `runPrompt` holds its own turn handle (`mine`) and re-checks `mine.cancelled` after the await, before dispatch: a cancelled prompt emits `prompt_end` with stopReason `cancelled` (shared `endPrompt` path, so streaming/meta/queue-drain stay identical) and is never sent. The interrupt verb also only emits `session/cancel` for a turn it actually dispatched (`mine.dispatched`) — an undeliverable cancel is worse than no cancel.

**② The interrupt verb kept the queue.** ACP has no queue verb, so the wrapper holds queued prompts locally; the old handler cleared them only in the `else if` branch that runs when NOTHING is active. Stop during a turn therefore cancelled it and the cancelled turn's own `drainPromptQueue()` immediately dispatched the queued prompt — the agent kept working, with a second permission request, right after Stop. Fix: interrupt ALWAYS splices the queue and says how many it dropped (`queue-cleared` notice). A queued PEER message was already reported delivered (`peer_result` ok/queued), so dropping it silently would lose a promised message: each dropped peer entry re-emits `peer_result {ok:false,text,fromName}` (the queue entry carries `opts.peerText/peerFrom` for exactly this) and the acp-events consumer re-stashes it on the delivery ladder.

**②b The dropped queue entry kept its latch (review round 2).** The stop-time bookkeeping nudge is FETCHED by `endPrompt` (a `/api/agent/stop-check` round trip) and dispatched only when the answer lands — by then `drainPromptQueue()` has usually started the next turn, so the nudge itself lands in the local queue. `nudgeTurnActive` (the guard that stops a second nudge from being queued behind the first) is cleared by `endPrompt` for the nudge's OWN turn, and a queue entry that never runs never reaches an endPrompt: the ② fix's unconditional queue drop therefore STRANDED the flag at true, and every later `end_turn` took the `!nudgeTurnActive` branch — one Stop at the wrong moment silently disabled the bookkeeping reminder for the rest of the session (no stop-check call, no notice, nothing in the journal to see). Fix: the interrupt loop resets `nudgeTurnActive` for every dropped nudge entry, and the `queue-cleared` notice counts only what the USER queued (telling someone to "send it again" for an internal nudge they never sent is a lie; the log line still carries the true total). **Invariant: per-turn state must die with the turn it belongs to — when a lifecycle hook (endPrompt) is the only place that clears a flag, EVERY path that can destroy that turn without running the hook has to clear it too.** Pinned by two asserts that go red on the old wrapper (`stopChecks` frozen at 1 after a later end_turn) plus a control that runs the same setup WITHOUT the Stop — green in both states, so it proves the harness sees nudges at all and that the fix is not just the latch deleted.

**③ A Deny selected an ALLOW option.** `resolvePermission`'s last-resort fallback was `msg.approved ? options[0] : options[options.length - 1]` — positional, not kind-checked. An ACP agent is free to offer only `[allow_once, allow_always]` (the option list is the agent's), and then the LAST option is `allow_always`: a rejection executed the tool AND stood the permission down for the rest of the session (reproduced: `{approved:false}` → selected `always` → tool_call_update completed). Fix: FAIL CLOSED — a rejection with no reject-kind option answers `{outcome:'cancelled'}` (ACP's own no-answer outcome; the tool does not run) and emits a `deny-cancelled` notice saying what the Deny became, because cancelled may end the turn rather than just refuse the tool; approvals keep the positional fallback. Today's desktop client only sends `approved:false` without an optionId from the option-less permission card, so this was a latent contract hole rather than a live UI path — the wrapper's stdin verbs are a documented contract and a Deny must never be answerable with an allow.

**④ An agent exiting 0 before `initialize` produced ZERO records.** The child-exit handler gated its boot-failure notice on `code !== 0`, and `finalizeExit()` calls `process.exit()` synchronously — so `boot()`'s own catch (the `boot-failed` notice) never runs from that path either. An agent that quits 0 before answering initialize (a CLI printing usage because the acp subcommand changed, an unauthenticated agent giving up quietly) left an empty journal and an empty buffer file: a blank chat window that never said why, the ACP twin of the "dead session = blank shell" class. Fix: the notice fires whenever the agent exits before the session is ready, whatever the code (text names `exit code 0` / `signal X`), and the journal IS the history so the buffer file carries it across a restart.

**INVARIANTS: ① a flag captured before an await is re-checked after EVERY await before the side effect it guards (a synchronously-claimed turn plus an async prefix is a race by construction) — and never send a control message for work the peer does not know about; ② "Stop" is total: local queues are part of the stopped state, and anything dropped that was already reported delivered goes back to the delivery ladder, never quietly discarded; ②b dropping a queued item also drops ITS per-turn state — a latch cleared only by the lifecycle hook of the turn that never ran outlives the session it was meant to guard, and a user-facing count never includes work the user did not queue; ③ a permission fallback fails CLOSED — a rejection may never resolve to an allow-kind option, and a rejection expressed differently than the user asked must SAY so; ④ exit code 0 is not success when the protocol handshake never completed — report the failure on the record channel the window rebuilds from, not only on a non-zero exit.**
## PLUGIN SECURITY BATCH — MANIFEST TEXT BECAME CODE, AND CONSENT COVERED THE WRONG THINGS (2.369.43, plugin-system review; all reproduced before the fix)
## PLUGIN SECURITY BATCH — MANIFEST TEXT BECAME CODE, AND CONSENT COVERED THE WRONG THINGS (2.369.43 + round 2 in 2.369.44, plugin-system review; all reproduced before the fix)
Seven findings in the Ph2/Ph4 plugin system, one theme: **a plugin manifest is UNTRUSTED INPUT, and consent is about a package, not an id.** ① *Generated shims interpolated free text into code*: `syncShims` wrote `// ${t.description.replace(/\n/g,' ')}` — only `\n` was stripped, so a lone **CR** (or U+2028/U+2029, which end a line for a JS parser) closed the `//` comment and the rest of the description became statements in `data/bin/vibespace-tool-<id>-<name>` — a file that runs OUTSIDE the plugin's `node --permission` sandbox, with `VIBESPACE_SESSION_TOKEN` in its env, and that SHIPS to every ssh host and dial device with the core agent tools (repro: `--help` alone wrote the attacker's file). Fix: `jsLiteral()` (JSON string + `U+2028`/`U+2029` escaped — the shims run under whatever node a remote host has) is the ONLY way manifest text enters generated code; description and args live in `const DESCRIPTION` / `const ARGS_SCHEMA`, the comment header carries only the regex-validated id/tool slugs; and `cleanText()` in the PURE validator strips control chars + line separators from EVERY free-text field (description, label, window title, tool description, setting/theme labels) at the one choke point. ② *Consent did not cover agent tools*: `server:true` + `contributes.agentTools` with no `capabilities` block was `needsConsent === false`, so those PATH-wide programs installed themselves silently — `needsConsent` now includes contributed tools and the summary item says they run outside the sandbox (the shipped hello-plugin example now needs consent too). ③ *Declared fs paths were prefix-tested unnormalized* while node's permission model COLLAPSES them: `//home`, `/home/u//vibespace/data`, `~//vibespace/data`, `/home/u/./vibespace/data` and `/./` all passed the forbidden-root check — and `--allow-fs-read=/.` grants the entire filesystem (verified against real `node --permission`). `normalizeFsPath` now expands `~`, collapses the path itself (PURE, no `require('path')` — the validator has ZERO requires by design) and refuses anything that still contains `//` or `/./`. ④ *Reinstall inherited trust*: `registry.enabled`/`registry.trust` are keyed by plugin id, so a DIFFERENT package declaring an already-trusted id came back auto-enabled and trusted (its client module served same-origin, its consented capabilities intact — a copied capability surface keeps `capabilitiesHash` identical, so the drift check never fires). The installer now fingerprints the staged package (`contentHash`, sha256 over `relpath\0size\0filehash`), and `install()` keeps the fast path ONLY for a byte-identical package from the same recorded source; anything else resets `enabled` + `trust` with a notice, and the install dialog says so. `update()` (the owner pulling a new version from the source they already chose) keeps consent and records the new fingerprint. ⑤ *Proxied route replies were documents on the app origin*: `/api/plugins/<id>/x/*` echoed the child's `contentType`/body with no `nosniff` and no CSP, so a consent-free plugin could serve HTML/JS **same-origin as VibeSpace**. Every proxied reply now carries `X-Content-Type-Options: nosniff` + `Content-Security-Policy: sandbox; default-src 'none'`, and a document content-type (html/xhtml/svg/xml) from an untrusted plugin is served as `text/plain` with one journal line. ⑥ *The intentional-stop mark was per RECORD*: `rec._stopping` was set by `stopChild` and cleared only when the OLD child exited (≤3s: SIGTERM→SIGKILL), so a freshly started child that crashed inside that window was filed as an intentional stop — no crash count, no `lastError`, no restart, panel says "stopped". The mark is now a Symbol on the CHILD. ⑦ *The uploaded `.vsp` leaked*: the `source` whitelist threw ABOVE the try/finally that removes the multipart temp file. Also fixed under the same review: `capabilitySummary` said "write files under X" while `permissionArgs` adds `--allow-fs-read` for every write path (now "write AND read"), and `stageFromPath`'s recursive audit + `cpSync` of ≤200 MB ran SYNC on the main thread against an owner-supplied path — now `fs/promises` with a bounded (depth-capped) walk, honoring the "never block the event loop" law. **INVARIANTS: ① free text from a manifest/registry/peer NEVER enters generated code except through a JSON literal with the JS line terminators escaped — and it is sanitized once, in the validator, not at each use site; ② a path grant is normalized the way the ENFORCER normalizes it before any prefix/containment test (the same class as any allowlist compared against an unnormalized string); ③ anything that installs a program on a session's PATH is a capability and needs consent, whatever tier declared it; ④ consent/trust is recorded for a package (content + source), never for a name — replacing the code behind a trusted id re-opens the dialog; ⑤ a proxied third-party response on the app origin is DATA: nosniff + sandbox CSP, never a live document unless the plugin is trusted; ⑥ a "we did this on purpose" flag belongs to the OBJECT being acted on, never to a record that can hold two of them.** Pins: scripts/test-plugin-security.mjs (48, one reproduction per finding: injected shim + real spawn, the exact path list, install-A/install-B trust reset, proxy headers, a hang-on-SIGTERM child racing a crashing successor, upload cleanup), plus the consent-gate updates in test-plugin-loader / test-plugin-trust.

**ROUND 2 (2.369.44) — the verifier's two survivors, both "the model was right, the MODEL of the enforcer was wrong":** ⑧ *The `*` wildcard was not modelled.* ③ taught `normalizeFsPath` to collapse a path the way node's permission model collapses it — but node also accepts a WILDCARD, and it truncates the pattern: it reads a declared path only up to the FIRST `*` and grants every path whose STRING starts with what came before it. Probed against real `node --permission` (v24): `--allow-fs-read=/t/a/*` AND `--allow-fs-read=/t/a/*/never-used` both read `/t/a/b/c/f.txt`; `--allow-fs-read=/t/a/z*` reads `/t/a/zz.txt` but not `/t/a/b/…` (a character prefix, not a path-segment one); `--allow-fs-read=/*` reads `/etc/hostname`. Because the forbidden-root test compared the pattern as if it were a plain path, `capabilities.server.fs.read: ["/*"]` and `["<parent-of-the-install-dir>/*"]` PASSED validation and handed a plugin process VibeSpace's own install and data dirs — the exact grant ③ existed to refuse, through a grammar nobody had modelled. Fix: the wildcard is split off before collapsing (`/dir/*` = a subtree, `/dir*` = a string prefix that also covers `/dirty` — collapsing the trailing slash away would have silently widened one into the other), at most ONE `*` and only as the last character (a mid-pattern `*` is refused LOUDLY instead of accepted as something narrower than it is), containment runs on the literal prefix (`r.startsWith(prefix)` — so a wildcard over a parent of a forbidden root, and a partial-segment `…/dat*` catching `…/data`, are both refused), `/` and `/*` are refused with or without roots, and an accepted pattern is stored VERBATIM so the string the owner consented to is the string node gets. ⑨ *The reinstall trust reset was gated on the FOLDER.* ④'s rule is "consent survives only for a byte-identical package from the same recorded source", but the implementation read `r.replaced && !(sameSource && sameContent) && …` — and `r.replaced` only says a directory was overwritten. `enabled` and `trust` live in `data/plugin-registry.json` keyed by id and OUTLIVE the directory: with a hand-deleted plugin folder (or a prior install that failed after the registry write), there was nothing to replace, `replaced` was false, and a DIFFERENT package installed under that id came up pre-enabled and pre-trusted — its `client: module` served SAME-ORIGIN, no dialog anywhere. Fix: the gate is the registry record alone (`!(sameSource && sameContent) && (enabled[id] || trust[id])`); the notice is worded for both shapes ("a different package under the same id"). **INVARIANTS (added to the six above): ⑦ an allowlist entry is validated against the ENFORCER'S GRAMMAR, not just its normalization — if the enforcer accepts wildcards/globs/prefixes, the containment test models them, and a pattern whose effective grant is wider than it reads is refused loudly rather than accepted; ⑧ a security decision keyed on persistent STATE is gated on that state, never on a side effect that usually accompanies it (`replaced` is a fact about a directory; consent is a fact about the registry — the record outlives the folder).** Pins: scripts/test-plugin-security.mjs (62 — the wildcard refusal list + a legitimate wildcard as the negative control + the real installer refusing a parent-of-install-dir wildcard; hand-delete the folder, reinstall a different package → disabled/untrusted/module 404, then the same package from the same source → consent kept). Both were verified RED against the pre-fix code before the fix landed.
## OPENCODE SERVE INDEXED THE WHOLE MACHINE (2.369.50, owner's production instance on 2.369.42)

Two hours after the S9 serve keeper lazily started `opencode serve` (cwd = the server process's own cwd, `/home/<user>`), that serve sat at **157-169 % CPU (209 CPU-minutes), VmRSS 5.0 GB, 33 threads, /proc `rchar` +14.3 GB in 4 s** (3.6 GB/s page-cache reads). Hottest thread `fff-watcher-own` (442 412 ticks) beside `notify-rs inotify`, **30 021 inotify watches**, all on one device — `/tmp`. The db was 244 KB and `/global/health` answered in 20 ms: it was not hung, it was **burning**.

**What actually caused it (measured with /proc against a real 1.18.29 serve, not inferred):** OpenCode boots an "instance" per DIRECTORY, and each instance recursively indexes + inotify-watches that whole tree. Which of OUR calls boots one is not obvious, so it was measured route by route:
* `GET /session`, `GET /session/:id`, `GET /session/:id/message`, `GET /project`, `GET /session?scope=project&directory=/` — boot **NOTHING** (0 fff threads, 0 watches, 60 s idle after each).
* **every** `GET /api/session/{id}/…` route (message, history, context, the session itself) boots an instance for that session's `location.directory`: +19 `fff-*`/`notify-rs` threads, +5 process threads, +180 MB RSS, and a recursive watch of the tree.

2.369.42's NAMING lookup (`firstUserMessage`) used the v2 `/api/session/:id/message?limit=3&order=asc` because it returns messages oldest-first. The owner's store held exactly one session whose `directory` was `/tmp` — a shared dev box's `/tmp`. The session had no user message, so the 60 s name-retry poked it forever. **The incident report's own hypothesis (our `directory=/` project query keeping the instance hot) was REFUTED by measurement — that query costs nothing; the naming route was the whole story.** The `cwd=$HOME` start is a real second weakness (it makes the serve's default project the `'/'` catch-all) but it indexed nothing on its own.

**Fix, four layers.** ① **Naming moved to the v1 list** (`GET /session/:id/message`, the WHOLE conversation oldest-first — v1 `limit` is the NEWEST N so it cannot page from the front), byte-capped at 1 MiB via a new streaming `maxBytes` in the client (six unbounded whole-conversation fetches into the server process is an OOM waiting to happen; a conversation past the cap has long since earned a real OpenCode title to fall back on). ② **The serve runs from its own empty directory**, `data/opencode-serve/cwd`, made into a throwaway git repo — measured: OpenCode's project resolution walks UP for a git worktree, so a plain directory inside the VibeSpace checkout resolves the WHOLE checkout (node_modules and all) and a *fake* `.git` directory does not stop the walk; with no repo above it the project is the `'/'` catch-all. ③ **`listAllSessions` takes the directory-less listing first** (verified: a bare `GET /session` is store-wide regardless of the serve's cwd; the v2 `GET /api/session` is the same set with cursor pagination and also needs no directory) and only issues `scope=project&directory=` for worktrees that pass `bootstrappableWorktree` — never `'/'`, `$HOME`, the tmp dir, nor the vcs-less `'global'` catch-all row (a real project row carries `vcs`, which is the API's own "is a real project" signal — no fs call, so the never-block-the-event-loop law holds). `forkSession`, the one call that hands a directory to a mutating endpoint, now calls `POST /instance/dispose?directory=` afterwards (verified: the forked session stays readable). ④ **A resource guard in the keeper**: the child's own `/proc` is sampled every 60 s; >150 % CPU sustained ≥5 min, or RSS >2 GB on any single sample, STOPS the serve, parks the locator `parked:runaway`, logs the numbers + what to turn off, fires telemetry `opencode-serve-runaway`, pushes `harness-store-updated` (BACKEND_META.storeReason + a toast) and refuses to respawn for an hour. Plus **boot self-heal**: a recorded serve whose `GET /project/current` worktree is `'/'` or `$HOME` (a 2.369.42 leftover, and its record has no `cwd`) is SIGTERMed and replaced from the isolated cwd — the owner's instance heals on update with no manual step. New setting `agents.opencodeServeAutostart` (default ON = the shipped behaviour) is read through a function, so `VIBESPACE_OPENCODE_SERVE=0/1` still wins, `VIBESPACE_SKIP_AGENT_HOOKS=1` still forces off, and a live flip needs no restart. **SUPERSEDED 2026-09-07 (owner decision, the real closure of this incident): a background third-party daemon must be OPT-IN, so the setting and the smoke belt are both DELETED and the switch is the built-in `opencode-serve` PLUGIN, DEFAULT OFF** — `decideAutostart({env, pluginWantsUp})` in the shared module is the one decision (the `VIBESPACE_OPENCODE_SERVE` ops override still wins), an explicit disable STOPS the process (including one merely adopted from the record), and the first time a user touches OpenCode the product offers the service once (per instance, broadcast) with the sidebar admitting that stopped conversations are hidden until they say yes. Invariant added: **the default answer to "may we run a daemon on the user's machine?" is NO — a resource guard makes a runaway survivable, it does not make an unrequested daemon acceptable.** **Two follow-ups from the adversarial review of that change (2026-09-07), both invariants in their own right:** ① **flipping a default flips what every derived condition MEANS.** `/api/home` reported the store as FAILED on `parked || (!ready && autostart === false)`; the second half was the ops kill switch alone while autostart defaulted ON, and became the NORMAL state the moment the service turned opt-in — so the client's "a park must never be silent" red toast fired on every single page load (fresh instance, after "Not now", and on instances that never had OpenCode). The predicate is now ONE function shared with the live push (`storeFailureReason`, src/server/cli-env.js) and fires only on a PARK; deliberately-off travels as `row.service` and is shown passively. Corollary: when you flip a default, re-read every condition that mentions the old one — and when two code paths answer the same question (a boot fetch and a live push), make them literally the same function, because the loud one will be the one that drifts. ② **a kill switch must cover ADOPTION, not just starting.** The reuse rung runs before the autostart gate, so `VIBESPACE_OPENCODE_SERVE=0` stopped us spawning a serve while happily adopting the one that outlived the last restart — and the panel, correctly locked because the env forces it off, then offered ZERO controls for a running third-party daemon. "Off" means the process is gone: under the switch a recorded serve is SIGTERMed + forgotten (the existing replace path), and `install()` runs that ladder once at boot so an instance nobody polls does not inherit one either.

**Same-source A/B on the owner's real store** (one serve, one session at `directory=/tmp`, the only variable being which route the naming lookup uses), 60 s each:

| | new loop (v1) | 2.369.42 route (v2), SAME serve |
|---|---|---|
| CPU | 1.9 % | 10.2 % and climbing |
| VmRSS | 458→334 MB | 334→**515 MB** |
| threads | 13 | **32** |
| `fff`/`notify-rs` indexer threads | **0** | **19** |
| inotify watches | **0** | **27 336** |

**INVARIANTS.** ① A third-party daemon we start is a RESOURCE we own: sample its `/proc`, bound it, stop it, and SAY SO — "healthy" answers to `/health` say nothing about what it is doing to the machine, and a background process that burns for two hours in silence is the same class of defect as a silent failed user action. ② Never hand a long-lived agent daemon the server's own cwd or `$HOME`: what it resolves from that cwd is what it will index. ③ When a daemon's cost depends on WHICH route you call, measure the routes — the API shape (v1 vs v2, "the small one must be cheaper") is not evidence, `/proc` is; the plausible hypothesis in the incident report was wrong and only measurement caught it. ④ A directory-less listing beats a per-directory one whenever both exist; a directory you pass to a foreign daemon must be a REAL project (its own metadata says so), never `'/'`, `$HOME` or a tmp dir. ⑤ A whole-response read into the server process gets a byte cap. Pins: scripts/test-opencode-serve.mjs (131; instance-boot modelled in the mock, spawn-cwd spy, self-heal, runaway with a fake /proc reader, dispose, the byte cap) + the manual measurement mode `node scripts/dev/opencode-serve-live.mjs --measure [seconds]`.

**Two bystanders found while getting the gate green** (both pre-existing on master, both position/path-coupled fixtures): test-quota-source pinned `src/backend-caps.js` as the LAST entry of test-architecture's PURE set, which went red the moment 2.369.43 appended `search-card.js` — now a membership match; and test-writer-sweep held the transcript fd in ITS OWN process while the sweep script's guard is `ps -o args=` matching `*claude*`, so under an agent worktree (`…/.claude/worktrees/…`) or any checkout below a directory named `…claude…` **the suite SIGTERMed itself** (status null, no stderr, stops mid-run) — the holder is now a child with a fixed, claude-free command line in `os.tmpdir()`. **Invariant: a fixture must not depend on where the repo lives, and a test that can kill itself will, on someone's machine.**

## CODEX EXCLUDED FROM THE FRAME-FILE BYPASS BY BACKEND ID (design-harness-plugins §1 P1, after 2.369.17)
The 2.360.0 frame-file bypass (big chat frames ride data/chat-frames/, stdin carries a `_frame_file` pointer — the fix for the 79928a2b 38MB pty-shredding poisoning) carried `session.backend !== 'codex'` in its condition, because codex-chat-wrapper had no `_frame_file` verb. So for codex the pre-2.360.0 world never ended: a multi-image paste (the client sends the same claude-shaped `{type:'user'}` frame with base64 image blocks to every backend; CodexAdapter.formatChatInput wraps it into one `chat-input` line) rode raw pty stdin at any size, and when the channel shredded it the wrapper's stdin loop hit `if (!msg) continue;` — the message was lost and NOBODY was told (no log line, no event, no toast); the capability gate's honest ">1MB refused, old wrapper" path never ran either, because the backend-id branch short-circuited before it. FIX (three touches, one contract): ① codex-chat-wrapper resolves `_frame_file` on its stdin path — read, unlink ALWAYS (never an orphan), must parse as ONE object, nested pointers refused, then dispatched through `handleInput` exactly as a stdin line (so the user record + turn/start are the normal chat-input path); it advertises `caps: { peerMessage, frameFile }` in the sidecar; every failure (unreadable/shredded/nested frame, unparseable raw line) logs AND emits a `task_failed` event, which CodexMessageManager renders as a system error card telling the user to resend; it also emits `_stdin_ack` on stdout the moment a stdin line arrives (the design doc's P2 "stdin ack" row, pulled in here because the bypass depends on it: ws-handler's broken-pty detector re-sends the line after 5s without an ack or buffer growth, and a re-sent pointer finds its file already consumed — a spurious error on top of the pre-existing duplicate-turn symptom; the server's codex-events stdout branch already consumed the ack, only the wrapper never sent it). ② ws-handler's bypass condition is size + local transport only (`>64KB && !session.host && session.socketPath`); the wrapper's advertised capability decides pointer / raw ≤1MB / evidenced refusal — the SAME three-way outcome for every backend, so an old codex wrapper now gets the honest "Terminate + Resume" refusal instead of a silent loss. ③ test-chat-frame-guard §2b drives the REAL codex wrapper against a stub app-server (pointer → intact 200KB image in turn/start input; shredded/missing/nested files and an unparseable raw line → four loud task_failed events, wrapper alive, next normal line still works) and §4 pins the gate shape (negative pin on the backend-id branch). INVARIANTS: ① a transport-safety feature (bypass, guard, refusal) is gated on the RUNNING wrapper's advertised capability, never on a backend id — a backend-id exclusion is a permanent "this backend is still broken" branch that also hides the gate's honest refusal; the registry law (backend-caps.js, peerDelivery) applies to wrapper stdin verbs too; ② a wrapper's stdin loop never drops a line silently — an unparseable line is a lost user message and must reach the user through the same error channel as any other stdin-handler failure; ③ a new stdin verb is added to BOTH wrappers (or declared unsupported in caps) in the same change — the twin-file class ("twin-sets = 0 is a metric") includes data/bin/chat-wrapper.js ⇄ codex-chat-wrapper.js.

## CODEX SANDBOX CLOSED LOOPBACK → AGENT TOOLS DEAD (2.369.17, design-harness-plugins §1 P0)
codex's `workspaceWrite`/`readOnly` sandbox policies default to `networkAccess:false` and the seccomp filter enforces it on 127.0.0.1 (measured: EPERM, `CODEX_SANDBOX_NETWORK_DISABLED=1`; AF_UNIX blocked too — a unix-socket agent API would NOT have helped). Every vibespace-* tool POSTs to VIBESPACE_API on loopback ⇒ in every non-yolo codex chat the agent was taught tools that could not run and nudged each Stop for bookkeeping it could not do. Fix: wrapper policies carry `networkAccess:true` when VIBESPACE_API is set; terminal path `-c sandbox_workspace_write.network_access=true` (BARE boolean — `pushCodexConfigOverride` JSON-quotes and would make it a TOML string); boot probe = `codex sandbox -- true` (functional, async, assume-supported until answered) instead of a PATH lookup that "failed" on npm installs and ran terminal 'default' unsandboxed while chat was sandboxed. **Invariants: a capability the product depends on (network to our own API) is verified by running it, not by looking for a file; the same permission mode must resolve to the same sandbox on every spawn path (chat vs terminal); `-c` overrides are TOML-typed.** Forensic tell found while pinning it: a child spawned by Node inside the network-disabled sandbox prints NOTHING (exit 0) — Node's stdio "pipes" are AF_UNIX socketpairs and the seccomp filter swallows writes to them, while the same command under a real shell pipe prints ERR-EPERM; any probe of sandboxed processes must report through a regular file. Pins: scripts/test-codex-sandbox-net.mjs.

## ATTACH STORM STALL → HEARTBEAT KILLED THE CLIENT → KILLS LOST (2.369.16, userW inc-mtndq0vb "无法打开也没法 terminate，卡死了")
The session was never stuck: dtach/wrapper/claude alive, socket connectable. The server had self-updated 3 min earlier; after a restart every chat session's FIRST attach ran a SYNC `convertHistory` over its whole transcript on the main thread (58 sessions, 1.35GB, 10–56MB each) and a desktop switch re-attached 19 windows → ~4 min of event-loop stalls (attach-acks in 32s bursts, `attached` 4 min late). The 30s heartbeat then judged a pong it could not have read during its own stall, terminated the live client, and the frames queued unread on that socket (second attach, both kills) died with it; the client reconnected into another round. Fix: ① `convertHistoryAsync` (both normalizers) + `normalizers.rebuildHistory` (single-flight, `_rebuildQueue` holds live records and replays them in order, flag after success) + `feedLive` as THE live-feed gate; ② `src/server/ws-heartbeat.js` — a tick >5s late terminates nobody (re-ping, judge next tick); ③ kill acknowledged to the requester (`killed {ok}`) and re-sent by the client across reconnects (`app.killSession` = `ws.request` resend:true; zero bare kill sends remain). Review-hardened the same release: rebuilds are FIFO (round-robin slicing lands every window at the total time), the rebuild window re-acks every 10s with progress and the client ladder extends on fresh acks; attach re-checks liveness after the await; peer cards + auto-resume writes go through `feedPeerCard`/`feedLive`; failed rebuilds drain the held queue; view-only/subagent/HTTP converts are async and `transcript-service.view` joins an in-flight rebuild; the heartbeat has a 1s loop-gap pulse detector (tick lateness alone missed a 30–35s block right after a ping) and a 6-round bound; the kill ack carries the REQUESTED id (`resolvedId` = remapped) and the client disarms its watchdog and sends the backendSessionId fallback as a one-shot (a resent fallback frame would kill the resumed successor). **Invariants: no synchronous whole-transcript work on the main thread anywhere (ws, HTTP, view-only); a liveness verdict must exclude our own stall from the measurement window — measured by the loop, not by the tick; every writer into a session normalizer goes through feedLive/feedPeerCard; a user command written to a socket is re-sent until acknowledged, the ack names the id the client sent, and the resent form must be idempotent against successors; after any await in a handler, re-check the session is still the one you started with.** Forensic tell: attach-ack (synchronous) arriving tens of seconds after send = loop stall, not network; `attached` for other sessions still arriving = socket alive. Pins: scripts/test-attach-rebuild.mjs (26).

## STRANDED WRITES UNDER A DISCONNECTED MOUNT POINT (2.369.15, owner's OneDrive "is not empty, use --allow-non-empty")
Not a stale fuse endpoint — the mount point held REAL files: the task store's generated `.vibespace/TASK.md` recreated `<mp>/…/AIContext/.vibespace/` on the bare local directory while OneDrive was disconnected (the constructor's boot regeneration runs BEFORE mounts exist, and every task change regenerates). Those files "vanish" the moment the storage reconnects (shadowed under the mount) and rclone refuses the reconnect; `--allow-non-empty` would only have hidden them. Fix, three layers: ① connect (`_ensureMountpointDir(mp,{quarantine:true})`, rclone AND cephfs paths — a kernel mount shadows silently) moves the whole directory aside to a sibling `<mp>.stranded-<ts>/`, recreates the mount point empty, broadcasts a level-2 server-notice naming path + items — never delete, never merge; the mount-point EDIT path never quarantines (a user may point a record at a populated directory on purpose). ② `mounts.shadowedBy(p)` = the registered storage whose mount point contains p but is NOT mounted (gmail folders + credential-only records excluded); files.js refuses every op under it with 503 "“X” is not connected — this folder is its mount point". ③ task-groups `_syncTaskMd` consults the `isPathShadowed` dep (lazy TDZ-safe getter, same shape as getPortForwards) and skips; `syncAllContextMd()` re-runs once after mounts exist and on every `mounts-updated` broadcast (the wiring tees it), so the skipped file lands the moment the storage is back. **Invariants: any server-side writer into a user path asks `shadowedBy` first; a non-empty mount point is quarantined, never deleted; a dead fuse endpoint (ls fails/times out) is NOT quarantine material — it falls through to the stale-daemon kill.** Pins: scripts/test-mount-stranded.mjs (26).

## THE FOURTH READER SITE — EVERY STOPPED OPENCODE CONVERSATION OPENED EMPTY (2026-09-07, B-eac2)
"View History" on a stopped OpenCode conversation showed **"No messages in this session's transcript yet."** while `opencode serve` had the whole conversation. S9's store-backed reader (`OpencodeServeSessionMessages`) has no bytes until `prepare()` has run — the essay even says so — and `transcript-service` awaits it at its **three** reader sites. The ws `attach {viewOnly:true}` branch is a **fourth** site, and it called `sm.raw()` without preparing. The failure mode is exactly the one the design chose on purpose ("a caller that does not await prepare gets an EMPTY reader, never a wrong one"), which is why it was silent: no error, no log, no telemetry — just a conversation that looked empty. It shipped with S9 and survived a 135-assert suite because every node-level test called `readConversation`/`transcript-service` directly; nothing opened the window a user opens.
**Fix**: `if (typeof sm.prepare === 'function') await sm.prepare();` before `sm.raw()` in that branch, with the failure logged verbatim.
**Invariants**: ① a lifecycle hook that "every reader site awaits" needs the SITES enumerated in the gate, not the count asserted in prose — grep every `createSessionMessages(` call site when adding one; ② "empty, never wrong" is a safe DEGRADATION and therefore an invisible BUG — a degrade path that cannot be distinguished from a legitimate empty result must be tested through the surface the user touches; ③ the thing that found it was a **browser leg driving the real feature end to end** (open the conversation, right-click a message, confirm the dialog, read the toast) — the node suites were all green.

## OUR OWN CLICK REPORTED AS SOMEONE ELSE'S TURN — and two more from the S9-remainder round 3 (2026-09-07, B-eac2)

**① THE "external" LABEL ACCUSED THE USER OF BEING A STRANGER.** Piece (f) marks a conversation `external` when its `time.updated` moves "while we were not the ones moving it" — and "we" was implemented as *a live session of ours holds this id*. Every mutation we make through our OWN serve routes on a **stopped** conversation (roll back, restore, answer/reject an ask) moves that same field, so clicking "Roll back to before this message" made the row claim another process was driving it for 90s. Reproduced 3/3 through the real product routes against a real 1.18.29 serve (`stopped` → `external` within 6s; the one run that missed had never listed the row before, and production lists it every 5s). User-visible, all confirmed against the rendered UI: the card dims to opacity .7 and loses its click-to-focus cursor, its title reads "Running in unsupported terminal (PID ?)", a yellow EXTERNAL badge appears, `session-card.js`'s `when:` HIDES the Fork… row, and a sidebar whose `defaultStatusFilter` excludes 'external' loses the conversation entirely. The branch's own suite had PINNED the bug as correct behaviour — it asserted `status === 'external'` immediately after its own `facts.revertTo(...)`, labelled "a real change we did not drive".
**Fix**: `noteOwnWrite(id, session)` + `ownWriteVerdict` in the facts. Recognition is **by VALUE** wherever the action returns one — MEASURED: `revert`/`unrevert` hand back the row's FINAL `time.updated` and 1.18.29 never bumps it again (0ms delta at +250ms…+8s) — so the listing that reports exactly what we wrote is ours however late it lands, an older value (a listing already in flight while we wrote) keeps the entry, and the first value PAST ours is somebody else's and says so on that very tick. Only the actions that return no Session (an answered ask) fall back to a 10s clock window, and they now re-read `/question` even for positional answers because the question is the only thing that names the conversation they are about to move. Fork writes nothing down, and that too is measured (forking does not move the SOURCE row).
**Invariants**: ① "someone else changed it" must be defined as *not us*, and **us includes every mutation our own product routes make** — a live-session check only covers the conversations that have a process here, which is precisely the set this feature exists to serve; ② prefer recognising your own write **by its value** over a clock window: the response already carries the new stamp, and a window is a blind spot in exactly the direction ("a real TUI wrote 2s after my click") the feature must not lose; ③ measure whether the server keeps moving the row after answering you before choosing the window at all; ④ a green assertion that says the buggy behaviour is correct is worse than no test — when an assert's label ("a real change we did not drive") contradicts the two lines above it, the label is the bug report.

**② KICK() DID NOT WAKE THE SLEEP IT WAS SUPPOSED TO INTERRUPT.** The live lane's reconnect loop backs off up to 30s; `kick()` (fired on the locator's ready edge — "the plugin was just enabled", "the serve respawned on a new port") reset `attempts` and aborted the fetch controller. During a backoff there is no fetch to abort, and nothing cleared the `setTimeout` the loop was sitting in, so the already-scheduled wait ran to completion and only the NEXT one was short. The service ships OFF, so this is the FIRST-USE path: measured 25.1s from "serve ready" to "lane healthy" end to end (11.0s to the next connect attempt in the unit shape), and while the lane is unhealthy the 10s list timer this whole piece replaced is the only source of freshness.
**Fix**: the sleep keeps its resolver; `kick()` and `stop()` clear the timer and resolve it before aborting. 51ms after the fix.
**Invariant**: an "interrupt the wait" primitive must end EVERY state the waiter can be in — a socket AND a timer. A regression that drives a fake lane (`kick(){kicks++}`) proves the call happened, never that it worked; the leg must reach a real ≥3s backoff so a sub-second reconnect cannot be an accident of a short timer.

**③ A PTY THE SERVE NO LONGER HAS IS AN EXIT, NOT A DROPPED SOCKET.** The serve terminal stayed open ~12s after the shell exited, logging five bogus warnings: the bridge treats every socket close as transient and burned all five reconnect rungs (800+1600+2400+3200+4000 ms) against an upgrade that answers a permanent HTTP 404. Measured on the wire: after `exit`, `GET /pty/<id>` is `PtyNotFoundError` and the ws upgrade is a plain 404, which the `ws` client reports as `error: Unexpected server response: 404` then `close`.
**Fix**: PURE `ptyGone(message)` (404/410 terminal; 502/503, ECONNREFUSED, ECONNRESET, "socket hang up" transient) sets a flag in the error handler that short-circuits `close` into one honest log line + `onExit`. The mock serve now answers 404 for an upgrade to a pty it no longer has, so the end-to-end leg asserts ONE attempt and an exit under a second.
**Invariant**: a bounded-retry policy needs a TERMINAL class — retrying a verdict only postpones the truth and buries the log; and the fixture must model the real refusal (the mock used to accept every upgrade, which is why the retry storm was invisible).


## A SERVE TERMINAL INDEXED THE USER'S REPO — and two more from the S9-remainder round 4 (2026-09-07, B-eac2)

**① "OPEN A TERMINAL IN THIS SESSION" RE-INTRODUCED THE 2.369.42 RUNAWAY, ON THE ONE NEW PATH THAT TAKES A DIRECTORY FROM THE USER.** `openPty` passed `directory=<the conversation's cwd>` to `POST /pty`, and `?directory=` is precisely what makes the serve boot an OpenCode *instance* for that tree — a recursive index plus an inotify watch of every directory in it. `closePty` only sends `DELETE /pty/{id}`; nothing in the pty family releases an instance (`forkSession`, 110 lines earlier, calls `client.disposeInstance(dir)` for exactly this reason). Same-origin A/B on a real 1.18.29 serve — fresh serve per arm, isolated HOME, target = a 200-directory / 4046-file git repo, /proc `Threads` + `grep -c '^inotify wd' /proc/<pid>/fdinfo/*` + `VmRSS`:

| arm | base [thr, wd] | after create | after `DELETE /pty/{id}` |
|---|---|---|---|
| WITH `?directory=` (what shipped) | [16, 0] | [44, **204**] | [41, **204**] |
| without it (body `cwd` only) | [16, 0] | [45, **4**] | [42, **4**] |

Three open/close cycles on the same directory never dropped below 204; only `POST /instance/dispose?directory=…` released it (threads 45→40, watches gone). And `readlink /proc/<shell-pid>/cwd` was the target directory in **both** arms — the query buys the SHELL nothing, the `cwd` **body** field is what places it; the query only decides which instance owns the registry entry. Watches ≈ directory count, so one terminal on a `node_modules`-bearing repo reproduces the 30 021-watch shape of the original incident, and the only backstop is the runaway guard, whose remedy is to SIGTERM the whole serve and park it for an hour (every stopped OpenCode conversation becomes unlistable).
**Fix**: the whole pty family (`ptyList`/`ptyCreate`/`ptyGet`/`ptyResize`/`ptyRemove`/`ptyTicket`/`ptyConnectUrl`) sends no `directory`; they all land on the DEFAULT instance, which is our own empty throwaway repo (the 4 watches above). **They had to change TOGETHER**: the pty registry is per instance — measured, `PUT /pty/{id}?directory=X` on a pty created without the query answers 404 `PtyNotFoundError`, and vice versa — so a half-migrated family is a terminal that opens and can then never be resized, closed or reaped. The mock serve now models both facts (an instance is booted by the QUERY only, never by the body `cwd`; the registry — including the ws upgrade — is per instance) and the real-binary leg measures the /proc watch delta with the old shape as its control.
**Invariants**: ① every path that hands a USER directory to a long-lived third-party daemon is the 2.369.50 class until measured — API shape tells you nothing, `/proc` does; ② a resource an API lets you CREATE but not release through the same family is not "cleaned up by close", and the right move is usually to never create it rather than to add a dispose; ③ when a resource id is scoped by a query, the whole family sharing that id is one atomic change — verify the cross case (the other query 404s) so a half-migration cannot be green.

**② A SERVE-OWNED PTY OUTLIVES US, AND NOTHING REAPED IT.** `session._opencodePtyId` was written in `ws-create` and read **nowhere**; `OpencodeServeClient.ptyList()` was never called. A serve pty is deliberately not dtach-restorable (`socketPath` is null, so boot-restore never sees it), and the locator's `process.once('exit')` only SIGTERMs a serve *we* spawned (`if (ch) ch.kill(...)`). So on a SIGKILL/OOM restart — or on **any** restart while the serve was ADOPTED from `data/opencode-serve.json` (`state.child === null`) — the serve survives and every pty it owns survives with it, with nothing left in the product able to reach or kill it. Independently confirmed on the real serve: a pty stays `running` and re-connectable after its websocket is closed, and only 404s once its own shell exits.
**Fix**: `facts.reapPtys()` — the adopt-or-reap ladder the dtach/job paths already use — on the READY EDGE of each serve **process** (`${pid}:${port}:${startedAt}`, idempotent: a routine notify must not re-sweep and race a terminal being opened). It asks the directory-less `GET /pty` and keeps `live.ptys` (every pty THIS process opened, registered *before* the ticket call so the open→session-register window is never a kill window) ∪ `heldPtyIds()` (server.js reads `session._opencodePtyId` → cli-env → the facts: the field's first consumer). A failed listing does not mark the serve swept, so it retries on the next ready edge.
**Invariants**: ① a registry field with no reader is a fix that was never wired — `grep` the field, not the intent; ② anything we create on a daemon that outlives us needs an adopt-or-reap decision at the moment the daemon becomes reachable, which is the only instant "who still holds this?" is answerable; ③ the keep set must include what THIS process just made, not only what the persisted model records, or the sweep races its own product.

**③ THE LIVE LANE COULD LIE ABOUT ITSELF, TWO WAYS.** `laneHealthy()` switches the list-refresh fallback OFF when the SSE lane is connected AND the store watch is `active` — and `active` was true for any directory that merely *existed*, while `armWatch()` ran at `start()` (before any serve could exist) and **latched** (`watchTried` was reset only when the watch attached nowhere), so the documented "the dirs are resolved LAZILY, once the serve answers `GET /path`" never ran on any machine where `$HOME/.local/share/opencode` already exists — i.e. every machine that has ever run opencode. Reproduced with the real product wiring against a real 1.18.29 serve: `state().liveLane.watch` named the SERVER process's store, `liveLaneHealthy` was true, and a session created by a SECOND opencode process on the serve's REAL store never appeared (8s of 100 ms-spaced `discover()` calls, **zero** `GET /session` issued because the lane claimed health); the same probe pointed at the serve's actual store saw it in 493 ms. In the default deployment both resolve to the same directory (`agentEnv()` keeps HOME and XDG_DATA_HOME), so this was a latent gap plus a false health signal rather than a shipped outage — but the false `active` is the worse half, because it is the flag that turns the fallback off.
**Fix**: `createStoreWatch` refuses a directory with no `opencode.db` (recorded in `failed`; `active` stays false ⇒ "unknown" ⇒ the timer fallback stays on, and the next reconnect re-arms because the serve creates the db at boot). `armWatch(why)` RE-RESOLVES on every `connected` against the serve's own `GET /path` home and re-arms only when the dir set actually DIFFERS (`watchedDirs`, plus a `rearms` counter in `state()`), so a reconnect storm cannot churn the watchers.
**Invariants**: ① a health flag that other machinery *disables a fallback* on must be positive evidence of the thing it names — "the directory exists" is not "this is the store"; ② "resolved lazily, once X answers" is a comment, not a mechanism, whenever the eager path can succeed: check that the lazy branch is REACHABLE on a normal machine; ③ blind is worse than slow — when a signal cannot be validated, degrade to unknown and keep the dumb timer.

## A DEAD ASK STAYED ANSWERABLE, AND AN OFF SERVICE STILL WATCHED THE STORE — S9-remainder round 5 (2026-09-07, B-eac2)

**① THE WARM QUESTION MAP OUTLIVED THE SERVE THAT MINTED IT.** `readConversation` joins an OPEN `question` tool part in the transcript to the live `que_…` request id, so a pending ask survives a page reload as an answerable card; it re-reads the authoritative `GET /question` only when the warm map is **empty**. But `/question` is answered out of ONE serve process's own memory — measured on the real 1.18.29 binary: a fresh serve answers `[]` regardless of what the previous one held — and the lane's reconnect handler refreshed the *busy* map (`statusMap()`) and not the questions. So after a serve restart (a keeper respawn on a new port, an OOM, an adopted instance replaced) the map was warm and entirely dead, every open ask was joined to a `que_…` nobody holds, and the card rendered with a Submit that could only 404: the exact dead Submit the `stale` marker exists to prevent. Reproduced both ways — against two mock serves with the product's own adoption path (card stayed `requestId: que_live1`, `stale: false`), and against the real binary (SIGTERM the child, keeper respawns, `state().pendingQuestions` still 1 while the new process's `/question` is `[]`).
**Fix**: the map carries the serve PROCESS it was filled under (`live.questionsFor = ${pid}:${port}:${startedAt}`, the same key the pty reaper uses). A map stamped for a different process is not "known", it is **unknown**: `pendingQuestionsFor` answers `[]`, so `readConversation` re-reads the authoritative list and marks the card `stale`; `answerQuestion`/`rejectQuestion` take their cold-map re-read path; `state().pendingQuestions` stops claiming asks that belong to a process that is gone. And every lane `connected` now DROPS the map before re-reading it (`refreshQuestions`) — drop first, so a failed read leaves "unknown" (⇒ stale, honest) rather than "known" from a serve that no longer exists.
**Invariants**: ① a cache of a REMOTE process's in-memory state must be keyed by that process's identity, not by the endpoint's URL — a respawn on a new port is a different fact source wearing the same name; ② whatever a reconnect re-reads because "we may have missed events", it must re-read **all** of it: a handler that refreshes one derived map and not its sibling is a silent half-recovery; ③ when a re-read fails, the honest state is UNKNOWN — a stale "known" is what turns a disabled button into a broken one.

**② WITH THE BACKGROUND SERVICE OFF, THE LIVE LANE STILL WATCHED THE USER'S OPENCODE STORE.** `install()` started the lane unconditionally, and `lane.start()` arms an `fs.watch` on the store dirs resolved from the env THIS server runs under (`armWatch('boot')`) plus an SSE reconnect loop with its own timers. Since 2.369.59 the OpenCode background service ships OFF and the rule is that nothing of ours runs or watches until the user turns it on — yet a fresh instance whose owner never enabled OpenCode held an inotify watch on `~/.local/share/opencode`, so an unrelated `opencode` process's writes woke our server for a feature that was off. Measured on a REAL boot (fresh `data/`, plugin at its default, isolated `HOME`): the unfixed server holds **1** inotify wd on the store dir (`/proc/<pid>/fdinfo`), while its `sse.lastError` reads "the OpenCode background service is off".
**Fix**: the lane follows ONE predicate — the same decision the keeper spawns on (`opts.autostart` = `decideAutostart`: the ops override, else the plugin record) **or** a serve that is actually READY, which covers the one case that decision does not: a RECORDED instance we merely ADOPT (allowed with the plugin off; once we are talking to it, following it costs nothing new). Every locator state change re-evaluates it, so enabling the plugin brings the lane up without a restart and disabling tears it down — SSE socket, backoff timer and store watch. Pinned by a real boot with the plugin off (0 watch handles, no record, not even the throwaway `data/opencode-serve/cwd`), whose POSITIVE CONTROL is the same instance with the plugin turned on through the real route (watch appears, then is released again on disable).
**Invariants**: ① "default OFF" means no timers, no sockets and no fs handles — a feature that only *refuses to act* while still subscribing is not off; ② the thing that decides whether a background client runs is the same predicate that decides whether its server runs, read through a function so a plugin toggle needs no restart; ③ a "we hold nothing" claim needs a positive control in the SAME process and the SAME measurement, or zero is indistinguishable from a broken probe.

## A SERVICE THAT WAS TURNED OFF KEPT THE WATCH IT WAS STILL ARMING — S9-remainder round 6 (2026-09-07, B-eac2)

Round 5 made the live lane follow the service: `install()` arms it only while the OpenCode background service is on, and a disable calls `lane.stop()`. That closed "the lane was never allowed to start". It did **not** close "the lane was already starting". `lane.start()` fires `armWatch()` and the SSE loop WITHOUT awaiting them, and both are `async` before they own anything they could release:

* `armWatch()` awaits `resolveDirs()` — which asks the locator for the serve's own `GET /path` home on a 2s budget, and that budget is spent **in full** exactly on the machine where the serve is parked or cannot boot, i.e. the state a user who is turning the service OFF is most likely in. `stop()` could only drop what was already attached, so the continuation then ran `watch = createStoreWatch(...)` on a lane nobody holds: an `fs.watch` on the user's real OpenCode store that no `stop()`, and no `uninstall()`, can ever reach again.
* `connectOnce()` awaits `locator.client({budgetMs: 8000})` before it creates its `AbortController`, so a `stop()` landing there aborted a controller that was still `null` (or the *previous* connection's) and the continuation opened a brand-new `/global/event` subscription. Its ~10s heartbeats keep it fed, so it never even trips the 45s idle abort.

**Measured, unfixed, three ways.** ① Through the real product wiring (`install()` → `locator.start()` → `locator.stop()`) with a serve that never answers: disabling at 0 ms and at 1200 ms both left **1** inotify wd on the store (`/proc/self/fdinfo`, matched by the store dir's inode) — and the leaked watch was **live**: a later write to `opencode.db-wal` fired `onExternal`, which in the product is a `markDirty` → an `opencode-updated` broadcast → every open client re-running the `/api/sessions` sweep, for a feature the user had turned off. ② Same result with the keeper genuinely PARKED after its five crashes. ③ On a real mock serve, a stop inside `locator.client()` left the serve holding an **open** SSE response, while `lane.state().sse` reported `stopped: true` **and** `connected: true` at the same time — a lane contradicting itself about whether it exists.

**Fix — `stop()` is TERMINAL, not merely "no new work"**. The lane gets `laneStopped`, set FIRST in `stop()`, and every async arm re-checks it at the point where it would take ownership: `armWatch()` returns early on entry (a queued `connected` frame must not re-arm a dead lane) and, after the await, hands the built watch to the ONE attach point — `attachWatch()` closes it and returns null instead of storing it. `resolveDirs()` skips its second round trip (`c.paths()`) when the lane died during the first: an in-flight call cannot be cancelled, but a stopped lane owes the serve no further requests. `connectOnce()` checks `state.stopped` after `locator.client()` (before creating the controller) and again after the fetch settles (an abort no longer cancels a body that has already arrived). The lane is single-use by construction — `stopLive()` drops it and `armLive()` builds a fresh one per enable — so "stopped" is safely a one-way door, and `kick()` is a no-op on it.

**Invariants**: ① a stop is terminal for work already IN FLIGHT, not just for work not yet started — every `await` between "we decided to acquire" and "we own it" is a window in which the owner can disappear, so the check belongs at the ACQUISITION point, not only at the entry; ② give a resource exactly one attach point, because that is the only place that can refuse to attach it to something dead; ③ an object whose lifecycle is "created per enable, dropped per disable" should say so and make stopped irreversible, rather than leaving a half-dead instance that can still act; ④ "we released it" is provable only with a NEGATIVE control that leaks — this branch's suite runs every leg twice, once against `src/opencode-events.js` and once against a copy of that same file with exactly these guards removed (asserted to match once each, so a control that stops neutering anything FAILS instead of quietly turning the A/B into two green arms).


## A SERVICE THAT WAS TURNED OFF STILL ADOPTED A SERVE, AND BUILT A FRESH WATCH FROM IT — S9-remainder round 7 (2026-09-07, B-eac2)

Round 6 made the live lane's `stop()` terminal for an arm already in flight. It fixed the lane. It did not fix **who starts the lane**: nothing the user clicks does — `install()` starts it from what the KEEPER publishes (`laneWanted = st.ready || wantsAutostart()`), and the keeper's `locate()` tests `state.stopping` **only at ENTRY**. Every rung below that entry reaches `adopt()` — the one place `state.client` is ever assigned — after an `await` that `stop()` cannot cancel:

* the reuse `/global/health` probe against a RECORDED serve that outlived a VibeSpace restart (1.18.29 answers in ~1.2s cold; a busy one takes hundreds of ms), then `unsafeReuseReason`'s `GET /project/current` on top of it;
* `ensureServeCwd`'s `git init` for the isolated serve cwd;
* the ≤20s boot wait after a spawn — which is exactly the "starting…" window a user gives up in and clicks Disable.

**Measured, unfixed, through the real wiring** (`serve.install()` → `locator.start()` → `locator.stop({killRecorded:true})` — the `_ocStart`/`_ocStop` pair in src/plugins.js — with a real `createLiveLane` over a real store dir): a Disable landing inside the reuse probe left the locator reporting `ready: true` **and** `stopped: true` at once, `source: 'reused'`, the runaway-guard interval `stop()` had just cleared RE-ARMED, and a BRAND-NEW live lane built — whose own `laneStopped` is `false` by construction, so round 6's guard could not help. That lane attached **1** inotify wd on the user's real `~/.local/share/opencode` and it FIRED: a later write to `opencode.db-wal` produced an `onExternal` ⇒ `markDirty` ⇒ `opencode-updated` broadcast ⇒ a fleet-wide `/api/sessions` sweep, for a feature the user had just turned off — while the published client pointed at a process `stop({killRecorded})` had already SIGTERMed. The same shape reproduced with the disable landing inside the boot probe (`source: 'spawned'`, same leak). Two neighbours in the same family: a stop landing inside `git init` still walked on and SPAWNED a brand-new third-party daemon after "off" (it was refused in production only by luck — `_ocStop` writes the plugin record before calling `stop()`, so the re-read `autostartOn()` happened to be false; a stop that is authoritative must not depend on a SECOND fact), and the boot loop's `return null` walked away from a child it had just spawned, leaving a live `opencode serve` plus a record the NEXT boot would ADOPT.

**Fix — the check belongs at the ACQUISITION point.** `adopt()` returns null when `state.stopping`: one guard over every rung, present and future, because it is the only place a client is published. `locate()` additionally re-checks after the isolated-cwd await, after the reuse health probe (so a disable never even evaluates the reuse verdict, nor signals a recorded pid on behalf of a service already being torn down) and after `freePort()` (never START a daemon for a service that was turned off mid-ladder). The boot loop bails through `abandon()`, which SIGTERMs the child and clears the record only when it names that port; its `why` is set only when WE decided, so a child that crashed on its own keeps `onChildExit`'s honest `lastError`.

**Invariants**: ① round 6's rule generalises — the guard goes where OWNERSHIP is taken, and for a keeper that is the single publisher (`adopt`), not the entry of the function that walks toward it; ② a state a client reads as "we are running" (`ready`) must never be published by a code path that started before the user said stop — `stopped:true` and `ready:true` cannot both be answers to the same question; ③ a stop that is only correct because some OTHER fact (a plugin record written first) happened to be flipped first is not a stop — make the primitive itself authoritative; ④ never walk away from a process you spawned: a bail path that leaves the child AND its record turns "off" into "adopted again on the next boot"; ⑤ the negative control is the whole proof — every leg here runs twice, once against `src/opencode-serve.js` and once against a copy with exactly these 6 checks removed, and that copy must publish `ready`, arm a lane, leak an inotify wd and fire; its replacements are asserted to match their expected counts so a stale control fails loudly instead of turning the A/B into two green arms.


## A LEVEL IS NOT A CANCELLATION: A DISABLE→ENABLE PAIR WAS SERVED BY THE LADDER THE DISABLE KILLED — S9-remainder round 8 (2026-09-07, B-eac2)

Round 7 moved the guard to the ACQUISITION point (`adopt`, the only place `state.client` is ever assigned) and re-checked after each await that `stop()` cannot cancel. It guarded on `state.stopping` — and that is a **LEVEL, not a cancellation**: `stop()` raises it and the very next `start()` lowers it again. So the guard is not terminal across a **Disable→Enable pair** — the panel's own Stop/Start, or a user who turns it off and changes their mind. `start()` clears `state.stopping` while `ensure()` still holds the in-flight `locate()`, so the Enable does not begin a ladder: it JOINS the cancelled one, which then sails past every re-check and publishes evidence it gathered **before** the stop.

**Measured, unfixed, through the real wiring** (`install()` → `locator.start()` → `locator.stop({killRecorded:true})` → `locator.start()` — the `_ocStop`/`_ocStart` pair in src/plugins.js — against a RECORDED serve that outlived a restart and answers in 700 ms, with a real `createLiveLane` over a real store dir): Disable at T, Enable at T+50 ms, both inside the reuse `/global/health` probe ⇒ **two** adoptions. The Enable's own ladder spawned and adopted a serve, and then the stale ladder woke up and adopted `source:'reused'` **on the exact port `stop({killRecorded})` had just SIGTERMed and whose record it had deleted** — overwriting the live one. The published client pointed at a dead process; the spawned child it displaced was still running with nothing tracking it; `state.child` had been destroyed before either. And with the epoch alone, the mirror failure: the Enable's `start()` resolves the CANCELLED attempt's `null`, so the route reports a click that did nothing.

**Fix — per-attempt cancellation, the shape round 6 used for the lane.** `state.stopEpoch` is bumped by `stop()` **and** by every new `locate()`; `locate()` captures `const epoch = ++state.stopEpoch` at its top and passes it into `adopt(port, pid, source, epoch)` and every post-await check, all of which now ask `cancelled(epoch) = state.stopping || epoch !== state.stopEpoch`. A cancelled attempt can never become valid again, and only the NEWEST attempt may take ownership. `stop()` also DETACHES the in-flight attempt (`ensuring = null`) so the Enable's `ensure()` starts its OWN ladder, and `ensure()`'s settle handler only clears the slot it still owns (`if (ensuring === p)`) — otherwise a late-finishing cancelled ladder drops the live one that replaced it. `abandon()`'s `why` is now set only while `state.stopping` holds: a run cancelled by a NEWER ladder must not write a complaint that ladder is about to contradict.

**Invariants**: ① a flag that the next user action clears is a LEVEL and cannot cancel work already in flight — cancellation needs a token the in-flight work captured (an epoch), and "newer attempt wins" must be part of it, not just "a stop happened"; ② the retry after a cancel must be a NEW attempt: a single-flight cache that hands the next caller the promise of the run just cancelled turns Enable into a silent no-op, so the canceller detaches the slot and the settle handler only clears the slot it still owns; ③ a bail path may only write `lastError` while its own stop is still the current story — a cancelled run that narrates over the run that replaced it publishes a stale complaint on a healthy service; ④ `abandon()` SIGTERMs the child it holds in its CLOSURE, never the one in `state.child` (`stop()` nulled that first, and after an Enable that slot belongs to a newer ladder) — the conditional null is a no-op on every path today and stays conditional for exactly that reason, pinned by a leg whose mock child records its kills by phase; ⑤ the negative controls are one per mechanism, 2 of them, each neutering exactly ONE check so the two failures stay distinguishable (drop the epoch ⇒ two adoptions, the last one a SIGTERMed process; drop the detach ⇒ the Enable's own `start()` resolves null), plus a third that removes `abandon()`'s SIGTERM so the kill assert can fail. Gate: scripts/test-opencode-s9.mjs ROUND 8.


## A RECORD YOU DO NOT OWN: A CANCELLED LADDER DELETED THE LIVE ONE'S RECORD AND ORPHANED THE SERVE — S9-remainder round 9 (2026-09-07, B-eac2)

Round 8's `ensuring = null` is what makes an Enable start its OWN ladder — and it is also the first time **two ladders can be alive at once**. Every post-await site in `locate()` got the epoch check except the two in the **reuse rung**: `const bad = await unsafeReuseReason(probe, rec)` is an await, and its bad branch then ran `killPid(rec.pid,'SIGTERM'); clearRecord();` with no check at all, while `else if (!pidAlive(rec.pid)) clearRecord();` ran after a `DEFAULT_TIMEOUT_MS` health probe with none either. `abandon()` (the boot-loop bail) even states the rule the reuse rung was breaking: *"never clear a record that names a DIFFERENT serve"*.

**Measured, unfixed, through the real wiring** (`install()` → `locator.start()` → `locator.stop({killRecorded:true})` → `locator.start()` — the `_ocStop`/`_ocStart` pair in src/plugins.js — with a recorded serve that answers in 700 ms): Disable at T+1000 ms lands inside the VERDICT probe (past round 7's post-health check), Enable at T+1050 ms starts its own ladder, spawns a serve and writes its record; the stale ladder wakes at ~T+1400 ms, decides the serve it probed must be replaced, and **deletes the record the live ladder had just written**. Result: `ready:true`, `source:'spawned'`, a running `opencode serve` on a port nothing on disk names — the next boot can neither adopt it nor kill it, which is the orphaned-third-party-daemon class the record exists to prevent. Same outcome through the dead-pid arm (a recorded serve that never answers + a pid that is not running). The stale ladder also fired a SIGTERM of its own on behalf of a service the newer ladder now owns.

**Fix — every record write and clear is OWNED, and every post-await action in the reuse rung is behind `cancelled(epoch)`.** `clearRecord(owns)` takes the `{port, pid}` pair it believes is recorded and REFUSES when the record on disk names a different serve; `abandon()`, `onChildExit()` (the port travels from the spawn site — `state.port` is still null while a child dies during its boot wait) and `parkRunaway()` all name theirs. Port alone is not identity: `freePort()` hands out a port that is free RIGHT NOW, so the port an abandoned child is giving up is re-bindable by the very ladder that cancelled it, and abandon's old `r.port === port` test would then have deleted the NEW ladder's record — the pid discriminates. `stop()` keeps its UNCONDITIONAL clear deliberately: it is synchronous, it bumps the epoch first, and "the user turned the service off" outranks anything on disk.

**Invariants**: ① a record naming a live process is a promise to the NEXT boot — deleting one you do not own is how "off" becomes "a daemon nobody can find", so a clear must NAME what it believes is there and refuse a mismatch; ② once cancellation exists, "the file on disk is mine" stops being true by construction — every post-await ACTION (kill, clear, publish), not just the publish, needs the token; ③ port equality is not identity for anything that can be recycled, and the pair the writer wrote is the claim; ④ the safe unconditional clears are exactly the synchronous authority points that bump the epoch first, and they say so in a comment; ⑤ the fix is deliberately TWO layers, so the control that reproduces the original loss is a PRE-FIX copy removing all 3 replacements, while one copy per mechanism proves each layer is load-bearing alone (drop the verdict check ⇒ the cancelled ladder still SIGTERMs, two kills, and only the ownership layer saves the record; drop the ownership test ⇒ abandon() deletes a recycled-port record naming the live serve) — plus a positive control that a genuinely dead recorded pid is STILL cleared by an uncancelled ladder, because a guard that refuses every clear would be the same bug wearing the other hat. Gate: scripts/test-opencode-s9.mjs ROUND 9.

## THE RECORD WE WERE ABOUT TO OVERWRITE: A RECORDED SERVE THAT WAS ALIVE AND SILENT BECAME AN ORPHAN ON EVERY RESTART — S9-remainder round 10 (2026-09-07, B-eac2)

Rounds 7-9 hardened the reuse rung against a **concurrent** ladder. This one needs no concurrency at all, and it is older than all of them: the rung owned exactly ONE arm of an unanswered `/global/health` — a DEAD recorded pid (`} else if (!cancelled(epoch) && !pidAlive(rec.pid)) clearRecord(owned);`). An ALIVE one was neither killed, nor cleared, nor logged; control fell THROUGH to the spawn twenty lines below, whose `writeRecord` overwrote data/opencode-serve.json with the new child.

**Measured, unfixed, through the real wiring** (`install()` + `locator.start()`, a socket that accepts and never answers at the recorded port, a REAL live process at the recorded pid): `ready:true`, `source:'spawned'`, the record naming the new port — and the recorded process still running, with nothing on disk naming it. The runaway guard cannot see it (it samples OUR child's /proc), `stop({killRecorded})` cannot reach it (it reads the record), and the next boot cannot adopt it. Meanwhile OpenCode keeps indexing and inotify-watching whatever instance it had booted: the 2.369.42 resource shape, arriving through a different door, once per restart of a wedged serve, in silence.

**Fix — "we are about to overwrite this record" is an OWNED DECISION** (`settleRecordedServe`), with outcomes that are named rather than implied: `'clear'` (the recorded serve is provably gone), `'keep'` (we are not going to spawn, so nothing overwrites it and nothing may act on it — it still NAMES the process `stop({killRecorded})` would signal), `'answered'` (a longer confirm probe reached it after all ⇒ adopt it), `'blocked'`, `'cancelled'` (round 9's rule: a superseded ladder touches nothing). What a live pid IS gets its own PURE verdict, `classifyRecordedPid`, over injected procfs reads: `'ours'` = running `serve` with the RECORDED `--port` under our own uid (the port is the discriminator, not argv[0] — the `opencode` launcher may re-exec its runtime); `'other'` = a different uid, THIS server process, or a different command line, i.e. the serve is gone and the record is stale bookkeeping we may delete but whose pid we must never signal; `'unknown'` = procfs said nothing. Only `'ours'` may be stopped, and only after ONE longer confirm probe (`RECORD_CONFIRM_TIMEOUT_MS`, 4s: a cold 1.18.29 needs ~1.2s and a busy one more — **slow is not wedged**, and if it answers we adopt it and stop nothing), a warn naming pid/port/budget, and a BOUNDED wait for the exit. An unverifiable live pid, or one that ignores the SIGTERM, is `'blocked'`: no kill, no spawn, and a PARK whose `lastError` names the pid, the port, the record file and both ways out — parking because `storeFailureReason` (the /api/home reason + the `harness-store-updated` push, 2026-09-07) speaks ONLY for a park, so a park is what makes the user see it instead of a silent orphan. It re-tries on its own cooldown (`BLOCKED_RETRY_MS`, through the generalized `state.retryAfter` gate that now also carries the runaway's), because the state can heal without anyone: the serve may answer again, or the process may exit.

**Invariants**: ① `pidAlive` answers *"something runs under that number"*, never *"that is my serve"* — pids are recycled and a record outlives its process, so the two things a live pid licenses (a SIGNAL, and being OVERWRITTEN) each need positive identification, and they need DIFFERENT ones: kill only what you proved is yours, overwrite only what you proved is gone; ② a fall-through is not a decision — the arm nobody wrote is the arm nobody tested, and here the untested arm was the destructive one (a rung that owns a record must enumerate its outcomes, including "leave it alone"); ③ a probe budget is a statement about US, not about the process — before acting irreversibly on "it did not answer", ask once more with a budget that fits the slowest healthy case; ④ when you cannot prove either fact, refuse BOTH actions and say so where the user already looks (the park channel), because a silent orphan is the one outcome nobody can debug; ⑤ a new park kind must answer "how does it end?" — `retryAfter` is now the single gate, and every surface that renders a park (⚙ card, store reason) needs its own row or it will describe the new state as the old one. Gate: scripts/test-opencode-s9.mjs ROUND 10 (a PRE-FIX control whose 2 replacements restore the fall-through, one leg per verdict against REAL processes and REAL procfs, and four positive controls — healthy / slow / service-off / dead-pid — because a guard that refuses every clear would be the same bug wearing the other hat).

## A GUARD THAT CAN NEVER ANSWER IS AN OUTAGE, AND THE ONE BUTTON A PARK LEAVES ENABLED — S9-remainder round 11 (2026-09-07, B-eac2)

Round 10 made "we are about to overwrite this record" a decision — and hung two of its five outcomes on a `/proc` read. **macOS is "full support" in the README and has no `/proc` at all.** There `readProcCmdline` answers `null` for EVERY pid, so `classifyRecordedPid` returns `'unknown'` for every live one, and the `'unknown'` arm PARKS: a stale record whose number has since been recycled onto an unrelated program takes the store dark forever, fires the red toast on every page load, and tells the user to go stop a process that has nothing to do with us. That is strictly worse than the pre-round-10 fall-through it replaced. A verdict that can never be reached is not a guard; a verdict that is ALWAYS reached is not a guard either.

Three fixes, each reproduced first (the no-`/proc` host simulated by stubbing the readers, the remote script driven against a fake record and a real silent socket):

**① A PORTABLE IDENTITY RUNG, and "this HOST cannot answer" as its own verdict.** `readProcCmdline`/`readProcUid` fall back to `ps -p <pid> -o uid=,args=` (execFile, 2s, briefly memoised) — procfs first, `ps` where there is none, the ladder `src/discovery-facts.js` already uses for `pidLooksClaude`, and a VALUE read, never an existence probe (that stays `pidAlive`'s `kill -0`). Then the distinction the fix turns on: `hostCanIdentify()` asks the INJECTED reader about our OWN pid, once. A reader that cannot describe the process it is running inside has no procfs and no usable `ps`, so a live pid there carries no information — verdict `'blind'`, and the caller behaves like the pre-round-10 path: clear the stale record, signal NOTHING, spawn. `'unknown'` keeps its meaning and its park: *the reader normally answers and about THIS pid it did not*.

**② THE SHIPPED SSH SCRIPT WAS THE UNFIXED TWIN.** `data/bin/vibespace-opencode-op`'s `locate()` still probed the recorded port and, on any non-answer, spawned a second serve and overwrote the record — on SOMEONE ELSE'S machine, where there is no keeper, no runaway guard, no park and no ⚙ card, so the orphan can never be reached again. The settlement is ported verbatim in behaviour (dead pid ⇒ clear+spawn · provably not that serve ⇒ clear, never signalled · verified ours + still silent after the longer confirm probe ⇒ SIGTERM + bounded wait · unidentifiable or still running ⇒ `fail()` with the pid, the port, the record path and both ways out, instead of spawning over it), and `scripts/test-opencode-remote.mjs` now drives BOTH classifiers over one table (16 rows, verdict-for-verdict) plus the three budgets, then runs the REAL script through each verdict against a real silent socket and real processes — with a PRE-FIX copy of the script as the negative control (it starts the second serve and orphans the first).

**③ `stop({killRecorded})` SIGTERMed THE VERY PID THE PARK HAD REFUSED TO TOUCH.** In a blocked park every OpenCode control is dark except Disable — and Disable was `killPid(state.pid || rec.pid)` behind nothing but a self-pid guard. It now routes through the same verdict (`decideRecordedKill`): the pid of a serve we are TALKING to (the client is the identity proof), or a recorded pid whose verdict is `'ours'`; anything else is left alone, the record is still cleared (the user's "off" outranks the file), and `lastError` SAYS what was not signalled and why.

**Invariants**: ① a guard added under incident pressure must be **reachable AND escapable on every platform the product claims** — "no evidence obtainable here" is a fact about the MACHINE and must be told apart from "no evidence about this pid", by probing the one pid whose answer we already know (our own); ② a fallback rung for a missing kernel interface is a VALUE read (`ps -o …=`), never an existence probe — liveness stays `kill -0`; ③ every control a park leaves enabled must obey the park's own verdict, or the park is only advice; ④ the CS-separation law applies to DECISIONS, not just to data walks: when the local settlement changes, the shipped single-file rung changes in the same commit with a parity assert that runs both classifiers over the same inputs (and a table row per decision boundary — an unpinned `serve`/`--port` half let a one-sided edit through in the first draft of that very assert); ⑤ a negative control whose deadline is 1 ms in the future is a coin toss, not a control — the cooldown a control depends on must actually elapse.


### VNC POINTER OFFSET UNDER DPI ZOOM (2.369.5, inc-mtdrm922 — userW + owner repro at DPI 90%)

Right-click on the in-container desktop highlighted the remote menu row ABOVE the cursor. Under the body `zoom: var(--ui-scale)`, noVNC mixes viewport px (clientX/gBCR) with layout px (clientWidth) → remote pointer ≈ actual × zoom, error growing with distance. **Invariant: the VNC canvas must live at NET zoom 1** — desktop-window's container carries `zoom: calc(1 / var(--ui-scale, 1))` (var-reactive, calc-in-zoom Chromium-verified). Any future embedded-canvas surface (xterm is internally consistent today) that mixes coordinate spaces gets the same counter-zoom treatment, never per-event math patches.

## RECONNECT RE-ATTACH REBUILD STORM (2.369.2, inc-mtd2pg6x "刚刚又卡死了")

A ws reconnect re-attaches every session; each attach rebuilt the window's whole DOM even when NOTHING changed — N windows × hundreds of messages in one task (5.5s stall co-timed with `state-resync` in the capture). **Invariant: loadHistory must no-op on an IDENTICAL slab** (same normEpoch + total + head/tail message ids + tail-anchored + not teleported) — meta/chatStatus/taskState/live state/typing indicator still apply, the DOM rebuild is skipped (`loadHistory:identical-skip` trace tag). Any future attach-path change must keep the skip reachable (it sits BEFORE the epoch write — writing the epoch first would defeat the comparison). Pins: test-chat-trim-guard.

## DIALOG EATEN BY SELECT-DRAG + DESKTOP-SWITCH PAGING STORM (2.369.1, inc-mtd1c2sd + inc-mtd1d0ft)

① The legacy shared dialog overlay (app.js `hideDialogs` wiring) closed on CLICK with `target === overlay`. A text-selection drag from inside an input releasing outside fires the click on the common ancestor (= the overlay) → the dialog vanished mid-form ("我选中个东西结果对话框没了"). **Invariant: backdrop dismissal must key on where the interaction STARTED (mousedown), never on the click target alone.** createModalShell already did; any new dialog shell must too.

② Desktop switches froze 30-60s (compositor stalls in the capture): visibility-flipping 4-6 chat windows re-measures all their content-visibility state; geometry transits through sh≈ch and every window's paging machinery reacted at once (fill-loops, extendTop storms, pin restores). **Invariant: a desktop-hidden ChatView is SUSPENDED — no paging/pin/boundary decisions off meaningless geometry** (`setSuspended`, wired in desktop-manager `_hideWin`/`_showWin` + stage-manager's direct `_hiddenByDesktop` writers; resume arms `_lastStructuralAt` so the existing settle guard covers the re-measure, pinned views re-tail in ONE hop; **2.369.9: re-tail means the LIVE tail — a behind-the-tail window (mobile touch paging trims the tail, windowEnd<total) takes the full jumpToBottom, else the old-position landing of inc-mtfi6034**). Any new code path that hides/shows windows wholesale must flow the suspend flag. **2.369.3 third leg (inc-mtd54h45): desktop-hidden CHAT windows also get `content-visibility: hidden` — bare visibility:hidden still styles/lays out the tree每帧 and DISCARDS the auto items' rendering state (the switch re-measure source); 'hidden' skips the subtree AND preserves the cache, so a switch repaints instead of re-measuring. Chat only — terminals carry WebGL canvases.** Pins: scripts/test-chat-trim-guard.mjs (14).

## FOLD-DOMINATED TRIM WHITE-SCREEN (2.368.29, inc-mtajy6wr — owner "上翻的时候出现大量白屏")

Semantic collapse (2.368.22-24) folds whole tool/agent runs; the c1206711-class transcripts (thousands of consecutive tool ops) render a 150-message window as a couple of run headers — SHORTER than the viewport, so scrollHeight clamps to clientHeight (captured 787=787 on every landing). `_trimBottom`'s fixed cap then removed the only VISIBLE content on each upward page and every wheel-tick teleported the window 50 messages deeper (ws 4572→4036 in ~6s) over blank space. The 2.301-2.307 collapsed-geometry guards all fired (`collapsedGeomSkip` on every landing) but they gate SCROLL decisions — the trim runs inside `_extendTop` regardless.

**Invariant: a rendered window shorter than ~2 viewports must GROW, not slide.** Both trims (`_trimBottom`/`_trimTop` — symmetric, downward paging through folds is the mirror image) raise the cap to 600 while `scrollHeight < clientHeight * 2`; 600 is the absolute DOM bound (fold members are hidden, cheap). Known residual: a single fold run longer than 600 messages re-enters the slide regime — acceptable until seen in the field. Pins: scripts/test-chat-trim-guard.mjs (6). The chatTraces ring (extendTop:done/trimBottom/collapsedGeomSkip tags) is the capture channel that caught this — keep it.

## Bug Fixes Applied
_Recent invariant-bearing entries only. The ancient one-liner chronicle (2026-03→07 era, ~150 fixes) moved verbatim to docs/history-archive.md — check there before re-diagnosing an old-sounding symptom._
- A CAPABILITY THAT ARRIVES AFTER THE PIXELS (2026-09-06, queue/steer round-3 review, caught before shipping): the `Queued` chip on a user bubble is built by `renderUserMsg`, which asks the view `_canSteerQueue()` and passes `onSteer = null` when the answer is no — and `applyQueueChip` renders a null-handler chip as `disabled`. But `_queueSupported` starts FALSE by construction (the running-wrapper gate of the round-2 fix), and BOTH of its sources land AFTER the bubbles: `loadHistory` renders every message and only then calls `_applyLiveMeta` with the attach payload's `queueSupported`, and a live session's baseline `queue_changed` arrives some frames after the first bubbles. So after ANY history load the chip rendered permanently dead — visible, wrong, and silent on click — while every grep-level pin and the round-2 functional assert (which set `_queueSupported: true` on the stub BEFORE rendering) stayed green. FIX: `_setQueueSupported` is the ONE writer of the flag; a change in EITHER direction schedules `_refreshQueueChips()` (rAF-coalesced, one pass over `_elements`, re-running `ChatRenderers.applyQueueChip` for every rendered message that carries a `queueState`). false→true makes the chips live, true→false makes them inert — a control that cannot work must not look like it can. INVARIANTS: ① when a capability decides how a widget RENDERS, the capability's setter owns re-rendering the widgets already on screen — a render-time-only read is correct exactly once, at a moment nobody controls; ② a functional test whose fixture starts in the FINAL state tests nothing about ordering — the assert must render BEFORE the capability arrives and then deliver it (both messengers: the attach payload and the wrapper's own meta op); ③ the strip escaped this bug only because it re-renders from `_setQueue` — sibling surfaces fed by the same flag must each be checked for who rebuilds them. Gate: test-queue-steer ⑦ (both orderings + the reverse flip + the one-writer pin), verified with a two-sided negative control (breaking either writer reddens only that path's asserts).
- THE CREATOR NEVER GETS AN 'attached' — LIVE STATE MUST RIDE 'created' (2.368.4, owner after updating for 2.368.3: "切换+resume还是停留在默认"; the style was verifiably ACTIVE — display only): a resume's creating client loads history over HTTP with NO meta and never receives an `attached` payload, so per-session live state (output style, auto-resume) that only rode the attach payload never reached the chip on the PRIMARY flow the feature exists for — pre-2.368.3 that surfaced as the eternally-pending hourglass (the live value never arrived, so the saved pick always looked pending), post-2.368.3 as a flat "默认". Second half: `attachSession`'s attached handler copied the payload into meta KEY BY KEY and the hand list lacked `outputStyle`/`autoResume`/`streamingKind` — the whitelist-drift class, FIFTH strike. FIX: `created` always carries `outputStyle`/`autoResume` (null = CLI default; the client's carries-the-key guard needs the key PRESENT), a shared `ChatView._applyLiveMeta` applies it everywhere, and both attach-shaped call sites pass the server payload WHOLESALE — the payload IS the meta, never a re-copied subset. INVARIANTS: ① adding a field to one reply payload ⇒ enumerate EVERY path a window is born through (create, attach, view, restart-reset) and prove each one delivers it — the creator/attacher split is invisible until a field only rides one of them; ② never hand-copy a payload into a meta object key by key (that list is a whitelist and whitelists drift — five strikes now); pinned in test-auto-resume (58).
- THE SERVER KNEW THE ADDRESS, THE UI STILL GUESSED IT (2.367.3, owner mapped an frp URL and every surface kept showing the LAN hostname): 2.367.0 gave the server ONE correct resolver for "this instance's URL", but four client surfaces each glued a path onto `location.origin` (design popover, publish dialog, chat `/p/<id>` linkify, Ports path mount) and the design row even preferred the relative `path` over the server's already-absolute `url`. Resolving centrally on the server is worthless while every consumer re-derives the same value locally. FIX: `utils.absUrl()` is the single joiner (instance public address → browser origin), the address rides `/api/home` at boot AND the `instance-url` broadcast live, and each surface lost its own join — pinned NEGATIVELY per file. INVARIANT: when a value gets an authoritative resolver, GREP FOR ITS RE-DERIVATIONS in the other tier and route them through it — a "single source of truth" that only one tier consults is two sources of truth (same class as the standing twin-set sweep).
- SILENT DROP MADE A QUIET TRUTH CHANNEL UNDIAGNOSABLE (2.367.2, the 2.367.1 follow-up): with arrival counters in place the CI failure named itself in one run — `posts=1 kept=2 events={api_request:2}` — so the CLI exported, the parser understood, and the records died at `if (!rec.rid || !rec.orgUuid) continue`: the CI identity (a personal setup-token) emits `api_request` with NO `organization.id`. The drop was correct; being SILENT was not — in production the same line turns a working exporter into an empty stash with no clue. Ingest now counts `noRid`/`noOrg`/`stashed`, and the E2E asserts the reachable truth (env→exporter→receiver→parser) instead of an account property. INVARIANT: a `continue` that discards otherwise-valid input must COUNT its reason — the difference between "nothing arrived" and "we dropped it, here is why" is the whole diagnosis, and one counter turned a 20-push mystery into a one-run answer.
- CI WAS RED FOR 20+ PUSHES AND ONLY THE OWNER SAW IT (2.367.1, owner: "我怎么老收到ci失败的邮件提醒?"): from 2.361.0 (the release that ADDED it) to 2.367.0, every GitHub Actions push failed on one assertion — the chat E2E's OTel truth check — while the local pre-push gate was green every time. Two separate faults. ① The assertion demanded a property of the ENVIRONMENT (does this runner's CLI export OpenTelemetry logs) as if it were a property of our code, and could not distinguish "the CLI exported nothing here" from "our parser dropped what it sent", so it failed instead of skipping. ② Nobody looked: `npm run ci` passing locally says nothing about the Actions mirror, and the only feedback channel was an email to the OWNER — 20+ of them. FIX: the receiver counts arrivals (posts/rejected/kept/seen) and exposes `GET /api/otel-stats`; the E2E SKIPs loudly with those counters when zero posts arrived and still hard-fails when posts arrived without a usable api_request. INVARIANTS: ① an assertion about a capability the environment may not have must classify the miss (skip with evidence) — never fail blind; ② after a push, CHECK the mirror (`gh run list --limit 2`) — a green local gate is not evidence about CI, the same class as "loopback is not the user's address" (2.366.1); ③ when the only signal for a failure goes to a human's inbox, it will be ignored for weeks — route it back to the agent that can act on it.
- CONFIG-WHITELIST STRIKE FOUR (2.368.1, owner-caught within hours of 2.368.0: picked an output style, resumed, chip still "默认"): `sidebar-state.setSessionConfig` whitelists persistable keys, and BOTH 2.368.0 writers (`outputStyle`, `autoResume`) were missing from it — the exact failure its own NOTE documents for `account` (2.43.0) and `groupManager` (2.132.0). The pick was accepted by the UI, silently dropped at persist, and the only symptom was an inert chip. Worse, the truthy filter (`if (config?.[k])`) would have erased an explicit `autoResume:false` even if whitelisted — the tri-state's per-session OFF (its entire reason to exist) cannot ride a truthy check. FIX: whitelist + an explicit `=== true || === false` branch for autoResume; the session records the EFFECTIVE spawn style (default-sourced included); a pick renders as PENDING on the chip (`Concise ⏳`) at pick time and at attach. INVARIANTS: ① adding a per-session config key = same commit touches the whitelist, and test-auto-resume pins it (a comment asking future authors to remember has now failed four times — only a test stops a fifth); ② booleans that mean "explicit OFF" never pass through truthy filters; ③ a saved-but-not-yet-effective choice must be VISIBLE — the silent drop was only diagnosable because the owner noticed nothing changed.
- HOSTED PAGE NEEDED A SECURE CONTEXT (2.366.1, owner: "打开后无法加载" then "你用的是你发给我的链接访问的吗？"): a published design canvas rendered its chrome and then hung EVERY artboard on "Loading artboard…" forever, for the owner and for anyone not on loopback. Root cause: **`crypto.randomUUID` is gated on a SECURE CONTEXT**; VibeSpace is reached over plain http on a hostname / LAN IP / Tailscale address, none of which are potentially-trustworthy, so `randomUUID` is `undefined` and the canvas editor (which mints ids with it) never finishes mounting an artboard. It worked over `127.0.0.1` because Chrome treats loopback as trustworthy — so every test I ran on loopback was meaningless, and I asserted "renders perfectly" three times before the owner asked whether I had used the link I gave them (I had not). Two decoys cost most of the diagnosis: the owner's console was full of extension scripts dying on `Invalid target origin 'null'` and of `localStorage` SecurityErrors — BOTH real consequences of the sandbox CSP's opaque origin, NEITHER the thing that hung the page. FIX: a serve-time COMPAT PRELUDE on `/p/` content (polyfill `randomUUID` from `getRandomValues` — a real v4, never Math.random; in-memory `localStorage`/`sessionStorage` when the opaque origin makes the real ones throw; each installed ONLY when genuinely missing, so https/loopback keep the browser's own) + a SHELL/RAW split: `/p/<id>` is our own trivial HTML on the real origin framing `/p/<id>/raw`, where the content still runs under the same sandbox CSP (never `allow-same-origin`) — isolation identical, top-level origin real, extensions no longer crash. INVARIANTS: ① **verify a hosted artifact at the address the USER will use** — loopback is a secure context and a LAN hostname is not, so `crypto.*`, storage and every secure-context-gated API behave differently there; a loopback pass proves nothing; ② content we host but did not write may depend on capabilities the deployment lacks — polyfill at the hosting layer, never edit the artifact; ③ when two error storms appear, check whether either can actually STOP the page before adopting one as the cause (both of these were noise); ④ every browser measurement must carry a NONCE (a query string echoed back from `location.search`) — three of my measurements were stale-tab artifacts that "confirmed" two different wrong root causes; ⑤ the fix must be proven by an A/B on ONE origin with the capability toggled (without polyfill 5/5 artboards stuck, with it 0/5), not by "it looks fine now".
- SHARE URL GUESSED FROM SOMEONE ELSE'S BROWSER (2.366.1, same session; owner: "你怎么知道我用啥地址能访问你？你怎么知道是不是存在反代？"): 2.366.0 remembered "the origin a browser last used" and returned it as the share URL — including to an AGENT, which then wrote `http://<this box's hostname>:3456/p/…` into a chat reply. A stored origin is a guess about a device that is not asking; behind a reverse proxy or a tunnel it is wrong by construction. FIX: an absolute URL comes only from `agentd.publicUrl` or from the Host of the request that is ASKING (browser routes only, because that browser will open it); agents get the RELATIVE path, the CLI prints it with an explicit "do not prefix a hostname", and the chat renderer linkifies `/p/<id>` against `location.origin`. INVARIANT: the server does not know how it is reached — only the asking browser does; never persist an origin to hand to a third party.
- GUARD ON THE PATH THAT MUST RUN (2.366.1, owner: chip said "1" but the popover only offered to create a new design): `_refillDesignList()` returns early on a detached node — correct for broadcasts arriving with no popover open — but the popover's INITIAL fill called it before appending the list, so it never populated. INVARIANT: a staleness guard belongs on the async/late path; the constructing path must run after the node is connected (the 2.306.0 "guard the live path" class again).
- CAPABILITY GATE READ THE WRONG FILE (2.364.1, owner: "一直在提示我wrapper太老让我terminate并resume，三次，我都照做了，中间还更新了一次vibespace"): the 2.361.1 frame-file capability gate decided whether a chat-wrapper understands `_frame_file` pointer lines by reading `caps.frameFile` from `readSessionMeta(sockName)` = `data/session-meta/<sock>.json` — the SERVER's session record (webuiSessionId/claudeSessionId/account…), which never carries caps. The wrapper writes its caps into its SIDECAR `data/session-buffers/<id>.json` (what boot-restore, the stuck-thinking heal and wrapper-files.resolveWrapperFiles all read). Consequences: every wrapper tested "old" regardless of age → the bypass was DEAD for two releases (data/chat-frames has zero files after the 2.361.1 commit time) → every >1MB paste was refused → the refusal text ("long-running wrapper started before the update — Terminate + Resume") sent the owner through three restarts and one update that could not help, then into terminal mode. It was invisible to diagnosis because (a) the refusal left NO server-side log line (the journal for the whole window is empty) and (b) the guard suite pinned the gate by TEXT (`/caps\?\.frameFile/` regex on ws-handler) — a pin that matched the wrong-file read perfectly. The first diagnosis in this very incident repeated the mistake: it read the refusal's own claim ("wrapper predates the update") as the explanation, found an old wrapper that fit, and stopped — the owner's "I resumed three times" was the falsifier. FIX: `wrapperCaps(BUFFERS_DIR, id, sockPath)` in src/server/wrapper-files.js is the ONE capability reader (sidecar via the collision-aware resolver, stateless, returns {frameFile, reason ok|no-caps|no-sidecar, startedAt, pid}); the gate caches only a POSITIVE verdict (`!== true` ⇒ re-read; a wrapper still booting a 789MB resume has no sidecar yet and must not be locked out by its first read); the refusal carries EVIDENCE (size, the wrapper's start time, or "still starting — wait a moment") and logs `chat-input REFUSED` to the journal. INVARIANTS: ① a capability a process advertises is read from the file THAT process writes, through its canonical resolver — never from a neighbouring record sharing the id (session-meta ≠ sidecar; two files, two writers); ② negative capability verdicts are never cached (absence is temporal, presence is permanent for a process lifetime); ③ a refusal names its evidence (which wrapper, started when, why) — a refusal that only restates its own hypothesis misdirects the user into "fixing" the wrong thing; ④ gate tests run the reader against the file the REAL writer produced (test-chat-frame-guard §2 spawns data/bin/chat-wrapper.js, then calls wrapperCaps on its output) plus a negative control with the wrong-file shape present — text pins on the reader prove nothing about which file it reads; ⑤ when an incident report contradicts the explanation ("did that three times, still happens"), the explanation is wrong — re-derive from the code path, not from the error text.
- PROXIED POST EATEN BY THE BODY PARSER (2.363.2, inc-mt2bpw2f, userW: "用Proxy打开这个网页，里面的交互按钮点不动"): app.use(express.json) sat ABOVE app.use(unblocker), so a proxied JSON POST's body stream was consumed by the parser before unblocker forwarded it — the target received headers with a Content-Length and waited for a body that never came, until the page's fetch aborted. GETs bypass body parsing entirely, so proxied pages RENDERED perfectly while every interactive action hung — a half-working page that reads as "the app is broken", not "the transport is broken". Repro'd deterministically (isolated express+unblocker harness + headless chrome clicking a POST button: AbortError with the parser first, post-ok without). FIX: the json parser skips /proxy/ paths (`req.path.startsWith('/proxy/') ? next() : jsonBody(...)`); unblocker deliberately stays mounted AFTER auth.middleware — moving it up would have been the obvious fix and an open-proxy/SSRF hole. INVARIANTS: streaming middleware (proxies, tunnels, upload relays) must see the RAW request — global body parsers and any pass-through route are mutually exclusive, and a new global parser must audit every streaming consumer below it; "renders but won't interact" through any relay ⇒ suspect the method split (GET path vs body-carrying path) before suspecting the page. Gate: scripts/test-proxy-post.mjs (6, real unblocker module).
- SEND REFUSAL READ AS ATTACH FAILURE (2.363.1, inc-mt2arppw, userW: "发消息就会直接中断，然后就得resume"): chat-view's per-session error handler treated EVERY {type:'error', sessionId} as an attach failure → view-only rescue → the LIVE window flipped into the Resume bar; AND the server's chat-input refusals (poison guard / too-large-for-old-wrapper) put their actionable text in `error:` while the handler read `msg.message` — so a CORRECT refusal ("…NOT sent. Terminate + Resume…") displayed as a dead-looking window with no reason, and the incident ring recorded msg:"" (it too read only .message), hiding the story from forensics. The session was never broken — attach always succeeded, the loop was user-visible-only. FIX: refusal sites send code:'input-rejected' + text in BOTH fields; ChatView renders coded refusals in-chat (✗ card) BEFORE the rescue branch; ring captures .error too. INVARIANTS: one ws error TYPE serving multiple semantics needs a CODE the client branches on — "error while attached" ≠ "error attaching", and conflating them turns every refusal into a fake crash; error payloads use ONE field name end-to-end (message), or every reader carries the belt; a diagnostic ring that drops the payload field is a forensic hole (this hunt cost an hour to a msg:"" entry). Gate: test-chat-frame-guard §5 pins (24). **ROUND 2 (2026-09-06, caught in review before shipping): the fix's own generalization was a REGRESSION.** Adding the queue-op refusal, the rule was widened to "any per-session `error` carrying a `code` is a scoped refusal" — which silently swallowed `ended-during-attach` (ws-handler raises it when the CLI dies WHILE its history rebuilds): a coded error whose session really is gone, which must keep taking the view-only rescue + Resume bar, or a dead session renders as a live-looking empty window. The rule is now an EXPLICIT allow-list (`SCOPED_REFUSAL_CODES` = input-rejected / not-codex-chat / queue-op-unsupported) plus an opt-in marker the SERVER can set (`scope:'action'`), and `_onSessionError` is a prototype method so both meanings are driven functionally (test-queue-steer ⑦). INVARIANT: "carries a code" is a fact about the WIRE, not about severity — a discriminator you generalize must be one whose values you enumerate; when the safe default is the destructive branch, opt IN to the gentle one.
- PEER MESSAGE INVISIBLE ON THE LIVE STREAM (2.362.2, inc-mt27t0bg, userW: 收端会话"没有一个 highlight 展示…来自谁谁谁的消息和消息本身的内容没有显示"): when the CLI drains a cross-session inbox delivery (harness SendMessage / vibespace-msg / job notify), the stream-json STDOUT stream NEVER carries the user record with the sender's words — verified against real 2.1.233 AND 2.1.235 buffers: stdout gets command_lifecycle started/completed + an init re-emit + the turn's assistant/tool records + a terminal `result` whose `origin` field carries the full envelope {kind:'peer', msg_id, name, fromMode, body}; the user record itself is JSONL-only (written ~13ms after enqueue). So the 2.349.0/2.361.6 peer card only ever rendered on JSONL rebuilds (restart/resume) — a LIVE-attached window showed the agent replying to nothing, and frequent dev restarts on the owner instance masked the gap for weeks. FIX: `_processResult` mines `result.origin.kind==='peer'` (body non-empty — real body-less origins exist: {kind:'peer',from:'unknown'}) and synthesizes the peer card at turn end (late but visible live; true order on any rebuild); dedup = `_peerMsgIds` Set noted at ALL THREE peer sites (user record, queued_command attachment, result rung); msg_id is AUTHORITATIVE when present — the last-12-messages text-containment fallback applies ONLY to msg_id-less legacy records (review-caught pre-ship: AND-ing it unconditionally suppressed every repeat fire of a same-body recurring notify and any short body ⊂ recent typed text — the very symptom re-introduced); mining precedes the is_error branch so interrupted turns still surface the message. INVARIANTS: the live stdout stream and the JSONL are DIFFERENT record sets — "renders after refresh/restart" vs "renders live" must both be tested for any record class (this is the 2.88.0 queued_command lesson in the opposite direction); when a stream omits a record, its terminal `result.origin` may be the only carrier — mine it, dedup'd by msg_id, never by assuming the primary record arrived. Gate: scripts/test-peer-msg-card.mjs (19, incl. same-body/distinct-msg_id and short-body⊂typed negative controls — a dedup test whose cases vary EVERY key at once proves nothing). SECOND LAYER (2.363.0, owner report the day after): SERVER-posted deliveries (Background Work notify / vibespace-msg — peer-messaging.js posting as an UNREGISTERED peer) get a CLI origin of {kind:'peer',from:'unknown'} with NO name/msg_id/body — the result.origin is body-less, so mining has nothing and even 2.362.2 stayed blank for this class. The server IS the poster ⇒ the delivery ladder renders the card itself on every ok lane (conversation-deliver emitPeerCard → normalizer injectPeerCard, deliberately containment-free), stash drains emit the same card at injection (4 sites), and peerDisplayName parses the server frames for rebuild parity. INVARIANT: a message channel's VISIBILITY must be owned by the party that HAS the information — when the transport strips attribution (unregistered poster), the poster renders, never hope the receiver can. THIRD LAYER (codex, design-harness-plugins §1 P1): CodexMessageManager had no injectPeerCard at all, so `feedPeerCard` returned false for EVERY codex session — auto-resume notices, Background Work drains and vibespace-msg cards were dropped without a trace, and the rpc-queue lane's wrapper-recorded user message rendered as an anonymous "You" bubble. Fix: codex `injectPeerCard` (same card shape as claude's); the rpc frame carries fromName+cardText and the wrapper writes them as a `webui_peer:{name,body}` marker into ITS OWN buffer record (the party holding the information) — `recordKey` + `mergeCodexRecords` strip the marker so the buffer copy keeps colliding with codex's rollout copy of the same user message (pinned in BOTH timestamp orders: the idle path writes the rollout copy first, the queued path the wrapper copy first), and a rollout-only rebuild recognises the ANCHORED server frame through the shared `peerDisplayName`. Rejected alternative: "the deliver lane calls feedPeerCard for codex too" — the wrapper record + an in-memory card double-render live, and the card would fire before the wrapper confirms delivery (ok:false re-stashes and re-renders at drain ⇒ a phantom card). INVARIANTS: a marker one transport adds to its copy of a twin record must be stripped by EVERY fingerprint that dedups the twins, and the test must vary which twin survives; a mid-turn peer record must never finalize the active turn's streams, and a peer card is never a turn boundary; typed records (webui_msg_id) are never peer cards whatever their text looks like. Gate: test-peer-msg-card (52) + test-peer-delivery (21).
- NOTIFY-CRON NEVER WOKE ITS OWNER AGENT (2.361.5, the 设备运维大师 relay's final layer): the notify ACTION called only d.notifyUser (user inbox/toast) + a passive group event — _notifyOwner/_deliverTo were never invoked for action-type notify, so an agent's self-scheduled reminder (the dated-obligation pattern the bookkeeping rules themselves prescribe) reached the human's inbox and the group's passive context line but NEVER the owning conversation. Diagnosed from the delivery topology, not the knobs (urgency=low / notify=inherit were innocent — the lane simply wasn't wired). FIX: notify fires ride the standard owner-delivery stack (notifyEffective toggles → 30s rate floor → live peer message → stash) in addition to the user lane; plus a per-job `notifyLog` ring (every ATTEMPT: lane/ok/to/reason, cap 12, panel-rendered as "Delivery log") so delivery is OBSERVABLE — the debugging here consisted entirely of reconstructing invisible outcomes, which the journal now makes a 5-second read. INVARIANTS: every notification-shaped feature must state its DELIVERY TOPOLOGY explicitly (who gets woken, who gets a passive line, who gets an inbox item) — "notify" without a recipient model breeds phantom-channel hunts; delivery attempts leave a journal (telemetry having it ≠ the owner can see it — the no-silent-failures law applied to the notification system itself).
- RUN+ECHO REMINDER = SILENT NO-OP + MISLEADING CREATE RESPONSE (2.361.4, the other half of 79928a2b's reminder hunt, owner-directed): scheduling a bare echo IS the natural agent instinct for a reminder, but the quiet-success law (notifyOk=false) made that shape a permanent no-op — worse, the creation response's "auto-notify: ON — messaged on completion/failure" reads as per-fire notification (it means terminal/park/ask events). FIX: scheduled pure echo/printf (regex-conservative: no pipes/chains/substitution) defaults notifyOk=true with an explanatory line; every scheduled task creation now states quiet-success semantics explicitly. INVARIANT (pairs with the resolved-time echo of 2.361.3): a creation response must state the semantics the CREATOR most plausibly assumed — "what will I be told, and when" for anything scheduled; when a common instinct-shape maps to a silently-useless configuration, either make the instinct work or refuse loudly, never accept-and-ignore.
- AGENT-TIME IS UTC, BARE DATETIMES ARE SERVER-LOCAL (2.361.3, 79928a2b's cron-notification hunt): agents' entire visible world is UTC (transcript timestamps, task logs), so when one computes "now + 2 minutes" it writes a UTC wall time — and `new Date("YYYY-MM-DD HH:MM")` parses bare strings in the server's LOCAL timezone, scheduling the job hours out. The agent then observes "notification never arrived" and diagnoses a broken delivery channel (its actual earlier misconfig — run+echo with notifyOk=false success-silence — it had already found itself; the cron ENGINE was firing on time throughout, runs accumulate in the reused "<name> run" child job). FIX: --at accepts relative "+2m"/"+1h30m" (the form agents should always prefer), creation echoes `next fire: <ISO> (in Xm)` so a tz mis-parse is caught at create time, help/manual teach the trap. INVARIANT: any agent-facing time input must either be relative, carry an explicit zone, or ECHO its resolved absolute time back — silent local-tz interpretation of an agent-supplied wall time is a footgun by construction.
- MONTHLY-SPEND-CAP REJECT UNMARKED (2.361.2, live same-night): once an org has extra usage, the CLI enforces the weekly lane on OVERAGE-INCLUDED accounting — rate_limit_event carries `seven_day_overage_included`, which rate-limit-capture mapped to 'other' (surfaced, never marked). A member with weekly spent + org monthly spend cap exhausted (`overageDisabledReason: org_level_disabled_until`, banner "You've hit your monthly spend limit") stayed at 7d=0.53 in cache and the pool re-picked it after the banner's 5h mark rolled. FIX: map the type to sevenDay (rejected → dead until the EVENT resetsAt; warnings → enforced-lane utilization reading; overage fields land in cache.overage). INVARIANTS: a bucket-type allowlist in a capture path is a silent-drop trap — every newly observed rateLimitType must either map or FAIL LOUDLY in a gate test (the api_retry lesson, third strike); "monthly limit" is NOT a new quota bucket — it is the extra-usage monthly credit budget (org spend cap), an ACCOUNT-level dead-until state; the pool's liveness model still lacks the spend-cap dimension as a first-class signal (parked: consume cache.overage.disabledReason as account-dead-until in verdicts).
- WRAPPER VERSION SKEW ATE IMAGE PASTES (2.361.1, c1206711, same-day catch; **NOTE: the gate's reader pointed at the wrong file until 2.364.1 — see CAPABILITY GATE READ THE WRONG FILE above**): 2.360.0's frame-file bypass assumed every session's chat-wrapper understands `_frame_file` pointer lines — but wrappers are LONG-LIVED processes (dtach survives updates), so every session spawned before the update forwarded the pointer VERBATIM to claude, which silently dropped the unknown type: the user's image+text message vanished from the UI on refresh (never reached the transcript — zero queue-operation records) while the full content sat orphaned in data/chat-frames/. FIX: wrapper advertises `caps: { frameFile: true }` in its boot meta; ws-handler consults it (session._wrapperFrameFile, schema-registered) and only capability-advertising wrappers get pointer lines; capability-less wrappers keep raw stdin ≤1MB (the historically-safe single-screenshot range) and a VISIBLE refusal error above it; orphans swept at 48h. Note the graduation window: sessions spawned by 2.360.0/2.361.0 servers run frame-file-capable wrappers WITHOUT the marker — they are treated conservatively (raw/refuse) until recreated, which is correct-direction degradation. INVARIANT (capability-gate law extended to wrappers): any NEW stdin protocol the server speaks to a session must be gated on a capability the session's RUNNING wrapper advertised — mirror of the device-op rule "old daemons are never asked — unknown ops HANG", and the second member of the "long-lived process keeps its loaded code" class alongside the CLI stale-token/stale-binary essays. Recovery for a hit session needs no server update: Terminate → Resume respawns the wrapper from current data/bin code.
- HOT-SWITCH STALE-TOKEN MISATTRIBUTION → OTel TRUTH CHANNEL (2.361.0, B-345b 终案): the dead-reckoning ≥10pt burst layer under-estimated -30~-49% and would NOT converge after 2.340.0. Mechanisms tested and REFUTED before the real one (archive — do not re-investigate): training censoring (73 big pairs WERE in the learning set, 42% of learned Δu), output-token share (3-4% both strata), model-family mix (fable ~90% both), cw5/cw1 TTL mix (30/70 vs 20/80), per-request overhead ($/req equal), unresolved-host exclusion ($0), walker gaps (raw-transcript recount diff = $0 — the ledger fully covers local spend), reading lag (storm-envelope residual doesn't cancel), cross-instance spend (owner ground truth: none exists). ROOT CAUSE: pool hot-switch does not reach a RUNNING CLI — repointPoolSymlink's utimes/mtime trick only affects new processes or token refresh; forensic case 79928a2b was auto-switched across 4 orgs in 35min while its process kept billing the ORIGINAL org to 95% and rate-limited from it (the rate_limit_event came from that session). 558 mid-session switches / $7.9k of ≤30min-post-switch spend in 12 days meant link-intent attribution scrambled storm-time cost in both directions → per-org odometers wrong AND learned rates attenuated. FIX = consume the only channel that NAMES the billing org per request: the CLI's OTel api_request export → loopback receiver → rid-exact bake override + corrective attribution + stash (kb-file-structure: otel-truth.js / server/otel-ingest.js). INVARIANTS: link-intent attribution is NEVER billing truth while hot-switching exists — per-request identity comes from the truth channel; transcripts/statusline/rate_limit_event are confirmed identity-free (like /status, the 2026-08-05 essay); calibration audits must stratify by movement size AND reconcile at storm level (slice-level residuals hide inside storms).
- SCHEMA-TWIN FALSY-0 REVERT (2.360.1, inc-mt0mozsp): the owner set both stop-nudge numbers to 0 ("every stop" mode, a first-class semantic since 2.210.0 — schema min: 0, server clamp0 passes explicit 0) and watched them jump back to 10/30 on Save. Root cause was TWO stacked defects in the Manage Agents → Agent instructions tab (manage-agents.js), which is a SECOND editing surface for the same settings the Settings window handles correctly: ① a hardcoded pre-2.210.0 bounds/default map `{staleMinutes:[1,240,10], cooldownMinutes:[2,720,30]}` — a schema TWIN that never learned min moved to 0, so even a surviving 0 would clamp up to 1/2; ② `Number(inp.value) || dflt` — the falsy-0 coalescing erased the 0 before the clamp even ran. Forensics: the incident bundle's action ring showed the user in `agents-cond-num` inputs + the agents-adv Save button (NOT the Settings window), three settings-updated echoes, and data/settings.json missing both overrides afterward. FIX: the dialog reads `SETTINGS_SCHEMA[key].min/max/default` (twin deleted), and Save distinguishes "empty/non-numeric → default" from "explicit 0 → 0". INVARIANTS (arch asserts #41-42): UI code never re-declares a schema row's numbers inline — bounds/defaults have ONE home (settings-schema.js); number-input handling never uses `|| default` (explicit 0 is a value — the same trap settings-ui.js already avoided with Number.isFinite). The class to grep for when adding any second editing surface: every constant it embeds is a twin that will drift.
- 38MB TRANSCRIPT POISONING VIA PTY-SHREDDED IMAGE PASTE (2.360.0, owner's 设备运维大师 79928a2b): a 5-image chat paste builds ONE multi-MB stream-json frame; that single line does NOT survive the node-pty→dtach→wrapper stdin channel — mid-line bytes AND the newline get dropped, the shredded frame glues onto the next send, JSON.parse fails in formatChatInput, and the else branch WRAPS THE SHREDS AS TEXT into the conversation → a 38MB user record = every later API call rejected (session reads as dead) + the huge-session 32MB tail window lands MID-LINE (history renders blank; the user thinks it's lost — it never was). THREE-LAYER FIX, all pinned in scripts/test-chat-frame-guard.mjs (ci gate): ① frame-file bypass — ws-handler writes frames >64KB to data/chat-frames/ and stdin carries only a `_frame_file` pointer line; the wrapper reads, validates it as ONE frame (a shredded file is DROPPED, never wrapped), forwards, unlinks; local claude chat only (remote wrappers can't see this fs — their large pastes still rely on ②); ② poison guard — formatChatInput's wrap-as-text branch THROWS on >512KB blobs that look like frames; ws-handler surfaces it as a per-session `{type:'error'}` toast; ③ RESCUE (productized surgery, owner request): transcripts.rescue / POST /api/session-rescue / session-card "Rescue transcript…" (stopped local sessions) — streams the file (never blocks the loop), stubs every >1MB record's message content IN PLACE (same uuid/parent = chain+order+count untouched), byte-preserved .pre-rescue backup, only rewritten lines re-validated, atomic swap; refuses while any writer holds the file (fuser). Invariant: the wrap-as-text fallback is a POISON PATH for anything huge — any new stdin channel work must keep the guard in front of it.
- VERBATIM-COPY CASE TYPO IN A GENERATED SCRIPT (2.359.1, owner's Ctrl+G outage — broken 35 releases since the 2.325.0 拆分): the decomp commit 9e42417 moved the editor-helper template "verbatim" but turned bash `\${PORT}` into `\${port}` — inside a JS template literal where escaped `\${…}` is bash and unescaped `${…}` is JS, the case slip is invisible to every static gate (acorn no-undef sees no JS identifier; bash -n sees a legal script). The generated `code` helper curl'd an empty port and waited on its signal file forever. Invariants: ① generated scripts need CONTENT-level gates — restore-smoke now checks the worktree-boot-generated files: every `${lowercase}` bash ref must have an in-script definition + the editor URL must ride `${PORT}`; ② templates that mix JS interpolation with escaped shell vars are a "verbatim copy" trap — when extracting one, diff the GENERATED OUTPUT before/after, not the source text; ③ a helper that waits on a signal file must have a timeout-with-message (still open — parked).
- UNSTAGED WIRING — A GREEN UNIT TEST OVER A DEAD FIX (2.355.0, userW's inc-msy27q2e repeating his own 2.331.0 report): commit 52eb46c shipped `scanStoppedInDesktopStates` (the resume-all-covers-every-desktop fix) + its unit suite, but the `loadAutoSave` CALL SITE was never staged — the pure function sat exported-but-uncalled for 24 releases while `test-resume-all-desktops` stayed green, because a pure function's test proves the LOGIC, not the WIRING. Invariants: ① every "pure fn + test" fix needs a WIRING PIN in the same suite (grep: the defining module must CALL the function ≥1 time beyond its definition — now in test-resume-all-desktops, which also joined the ci gate; it was manual-only); ② the 验证过的build≠提交的内容 rule extends to NEW code, not just edits — after committing a fix, grep the COMMIT DIFF for both the definition AND a call site (`git show --stat` touching only the defining file + tests = red flag).
- MANUAL POOL HOT-SWITCH DEMOTED BY PLAN C (2.355.0, userW's inc-msz495u6 "热切换死了"): per-session links (2.315.0) take precedence in poolCurrentFor, but setPoolTarget only moved the pool DEFAULT symlink — a manual target change silently became new-sessions-only (verified live on the reporting instance: default → new member, 16 live session links → old member; his pool auto:false hot:true, so nothing would ever move them). Fix: the USER route passes {sweepSessionLinks} (gated on pool.hot) and setPoolTarget repoints every live link + logs the count; the ENGINE never passes it (its per-session moves are the model-family projections). Invariant: when a NEW indirection layer (per-session links) takes precedence over an OLD control surface (the manual switch), every writer of the old surface must be re-audited as a consumer of the new one — the layer demoted the control silently and no test caught it because both tests lived on their own side of the layer.
- FREE-IDENTIFIER SWEEP ×8 + SERVER NO-UNDEF GATE (2.343.2, owner-requested "再检查下还有没有类似的"): a 3-agent audit + deterministic scans found EIGHT more latent breaks — classifyCliDeath/https/os/adapterRegistry/ClaudeCodeAdapter free identifiers, agentdDeps ghost binding ×2 (mounts-plugins-wiring AND server.js ssh→dial auto-graduation, which had silently never fired), persistenceRouter free in incident-wiring (bundles lost layouts), and the allow-exit hosts-updated broadcast deleted as collateral (stale/inverting toggle). Root pattern: the server had NO no-undef gate (the client has had one since 2.330.1). Invariants: ①scripts/test-server-globals.mjs (acorn scope analysis, all server-tier files, in npm run ci) makes the whole class build-impossible — it caught the 8th find on its own first run; ②degrade catches around these were mostly SILENT (bare catch{}) — the 2.284 verbatim-log rule remains the difference between weeks-latent and found-same-day.
- DEAD-CODE SWEEP DELETED A LIVE METHOD (2.343.1, publish layer 2): the dc37220 audit batch removed PluginManager._notify() as "dead" while 8 in-file call sites remained — plugin broadcasts + publish threw for weeks, unseen (no battery exercises plugin mutation paths). Invariants: ①deleting "dead" code requires grepping the SAME file for call sites, not just cross-file references; ②test-architecture #9 now statically requires every server-side `this._x()` call to have an in-file definition (src/lib mixins excluded — prototype methods live across files there).
- PROXY-SWALLOWED ASSIGNMENT (2.343.0, 7th of the decomposition class: owner's publish report): lazy.js mk() Proxy had get-only traps — `portForwards.plugins = plugins` in mounts-plugins-wiring wrote to the dummy target, the real singleton never got the handle, publish dead fleet-wide for 17 releases with a misleading "not available on this instance" error. No throw anywhere = invisible to every battery. Invariants: ①mk() carries a forwarding set trap (scripts/test-lazy.mjs pins reads/binds/ASSIGNMENTS/re-created-singleton writes); ②extraction checklist: any `x.prop = value` where x crosses a module seam must verify x is the real instance, not a bridge; ③a capability-missing error message must distinguish "handle never wired" from "feature unconfigured" — the misleading error cost the diagnosis.
- RELATIVE-PATH LOST BINDING (2.341.1, 6th of the class, first RELATIVE-PATH: userW's dial-mount outage): extraction #13 (拆分) carried `require('./package.json')` from server.js into src/server/dial-pairing.js verbatim — the spec is syntactically fine, resolves against the NEW directory, and throws "Cannot find module" only when the dial path RUNS (`deviceForDial` = every fresh-DeviceManager dial op incl. machine mounts; `ensureAgentdOnHost` = ssh agentd install). No free identifier, so the static free-variable check was blind; call-time, so boot smokes and the route battery were blind; the error DID surface to the user (no-silent-failures held). Invariants: ①when moving code between directories, every relative require/import moves its ANCHOR — re-resolve them as part of the extraction checklist; ②test-architecture #8 (permanent, in `npm run build`) statically resolves every relative require/import under server.js+src/ (.js/.json/.mjs/index.js candidates) and fails on a dangling one — negative-controlled against this exact file; the class is now build-impossible.
- LATENT LOST BINDING (2.340.2, 5th of the class, first LATENT): parseRateLimitEvent/captureRateLimitEvent were free identifiers in usage-pool-engine.js since extraction #5 — recordRateLimitEvent's try/catch swallowed the ReferenceError into `[usage] rate_limit_event capture failed: …` (48 log lines over 3 days, found only while diagnosing an unrelated stuck session). Passive rate_limit capture (limit→pool reflex + rate_limit anchors) was dead the whole time and every gate stayed green — the boot/route batteries can't see a runtime throw that a catch eats. Invariants: ①a degrade-path catch must log the message VERBATIM (2.284 rule — sole reason this was found) AND such logs must be GREPPED after every decomposition (`journalctl | grep -iE 'failed:.*is not defined'`); ②the restore-smoke could assert zero '*is not defined' lines in its boot output — worth adding when touching extraction seams.
- STUCK-THINKING AFTER RESTART / COUNTER-COLLISION FILE MISMATCH (2.339.2, 设备运维大师 real report): boot-restore derived the wrapper sidecar/buffer paths from the SOCKET name, but 2.302.0-era counter-collision sessions carry a different id in the wrapper argv — every restart read nonexistent files into catch{} and silently lost streaming/goal/todos/agent-watchers/buffer for those sessions. Invariants: ①any read of `<id>.{buf,json}` for a LIVE session must go through src/server/wrapper-files.js resolveWrapperFiles (socket-process argv is the authority when the expected pair is missing); ②the wrapper sidecar's `streaming` field is the turn-state authority across restarts — the ws attach reconciles a server-believed mid-turn against a settled (>3s) sidecar saying false and heals (missed-result class can never show thinking forever); ③counter-collision residue is PERMANENT in long-lived fleets — never assume socket name == wrapper id.
- TYPING-DRIVEN PAGING SELF-FEED (2.338.0, Windows freeze audit; the round-5 disease through the uncovered door): chat-input autosize per-keystroke write→read→write + real wraps shrinking the scroller → content-visibility re-resolution + scroll anchoring drifted scrollTop with ZERO user input → `_extendTop` (which unlike extendBottom had NO positive-evidence gate) paged 50 messages per bounce until history exhausted — a finite seconds-to-30s freeze that self-recovers, NOT a loop. Fixes carry invariants: ①every extend branch (top AND bottom) requires recent real user input — displacement is never intent, and new entry points (input resize, cv resolution, anchoring) must be assumed until proven absent; ②chat-input autosize stays rAF-coalesced with a no-op skip and stamps `__vsInputResizeAt` (250ms displacement guard both extends read); ③`_forceScrollToBottom` is a SINGLE refreshed-countdown chain — never one chain per op (overlapping chains = N forced layouts/frame); ④the tail-append runs pass is rAF-coalesced (rAF runs pre-paint so the 2.227.9 no-flash guarantee holds); ⑤streaming full-text re-parse throttled ≥150ms; ⑥code-block DOM capped 3000 lines + plain output skips the span-carry walk; ⑦reconnect epoch resets and catch-ups stagger 0-500ms per window; ⑧boot restore runs under wm._restoring (one bookkeeping pass at end); ⑨WebGL contexts capped at 12 (Windows/ANGLE context-burst = GPU-process stall = whole-page freeze); ⑩the 45s wake detector votes suspend-vs-block by SUM of longtasks (freeze shape is many medium tasks).
- **Three-tier closure marathon (2.297.0–2.300.0, owner directive "一次性完全做完"; invariants, do not regress)**: ① **scripts/test-vendor-whitelist.mjs is the structural §ban-safety law** — exactly two files may construct vendor requests (pinned counts + gate markers), the device protocol's vendor surface is EXACTLY the quota-refresh op (humanGated hard-refusal + throttle stamped ONLY before a real vendor call (2.271.0 rule) + read-only peek), shipped usage tools import no network primitives; a new vendor call anywhere fails the suite until deliberately allowlisted WITH its gates. ② **Offline-bias defenses**: usageHistory.sourceWatermarks() (per-machine newest-event ts, free on the shard walk) + hosts.linkState() (never probes) → darkSources()/darkTaintedAccounts() (server.js, 30s memo) → decidePoolSwitch `pessimism` docks a tainted account's remaining on BOTH sides (current trips exhaustion earlier AND a tainted candidate fails the settle bar); anchors record `dark:[hosts]` and extractPairs VOIDS pairs touching a dark interval (Δu real, cost missing ⇒ falsely hot rate). ③ **costBetweenMulti counts RESOLVED remote events** (only atype:'host' stays out) and **host-login remote sessions skip the noteLive ring** — the pre-2.297.0 pair made remote spend rise-then-un-count in the estimate. ④ **place-secret is THE secret channel on the device link** (0600 AT OPEN, atomic, $HOME-confined — the fsWrite-then-chmod pair left a world-readable window); old daemons keep the legacy pair as fallback. ⑤ **quota-refresh executes ON the login-holding machine** (usage-routes host branches prefer the op; server never sees the token; foreign-IP signal gone). ⑥ **sealed-orders**: the daemon executes a LOCAL pool fallback switch ONLY under the double condition (limit banner in live pipe stdout AND authedServers===0); banner parser = the bundled adapter's parseLimitBanner, re-point = account-material — ONE implementation each; executions ride the NEXT pool-orders REPLY (pushing at hello RACES the client's control-routing installation — observed live); server re-pushes orders at boot + every eval, hash-throttled. ⑦ **bufferOwner explicit in every session meta** ('server' today; attach routes by it when R6 session.open lands). ⑧ Session-brain steps 2–4 + R5's local-discovery leg are the SOAK-GATED remainder per the design's own sequencing rule — recorded in docs/design-three-tier.md §session-brain status, not silent debt. ⑨ Push-stream client additions need THREE touches or they silently no-op: the op in the id-keyed routing set, the unsolicited-push branch BEFORE prevControl, and the reconnect re-arm (the usage-events routing line was lost to an aborted batch edit and cost two debug cycles — the daemon-side failure LOG, added per the 2.284.2 no-silent-catch lesson, is what located it).
- **PAGING INTENT GATE (2.307.0, inc-msorcsrl — the mechanism two earlier fixes missed)**: after an upward page, content-visibility resolves the fresh batch and NATIVE SCROLL ANCHORING raises scrollTop to keep the view stable (captured: 85 → 1466 in 26 ms — no input device can do that). The `near the end ⇒ _extendBottom` rule read that displacement as the user scrolling DOWN, trimmed the top and walked the window back to the live tail. **scrollTop change ≠ user intent** — content edits, native anchoring and our own anchor restores all move it. Boundary EXTENSIONS now require agreement with the user's real wheel direction (`_wheelDir`/`_wheelDirAt`, 1.2 s expiry so keyboard/programmatic scrolling still pages); content growth has no direction and can no longer masquerade as intent. Diagnostic rule this cost three captures to learn: when a guard is demonstrably FIRING and the symptom is unchanged, the ASSUMPTION is wrong — go back to the data for the unexplained number, do not harden the existing explanation.
- **PAGING BOUNCE / COLLAPSED GEOMETRY (2.301.0→2.306.0, inc-mso818ry + inc-msor3oax)**: while content-visibility leaves a freshly paged batch unresolved the list collapses to `scrollHeight === clientHeight`, making "at top" and "at bottom" the SAME position. 2.301.0 guarded the SCROLL handler — and the recurrence capture shows that guard FIRING while the bounce continued, because **a collapsed list emits no scroll events at all**: the WHEEL handler is the only live path there, and its bottom-edge branch was unguarded (one `deltaY>0` — the momentum tail of an UPWARD gesture — hit `_extendBottom` and yanked the window to the live tail, ~1/s). The bottom branch now requires a REAL scrollable range (>50px); the scroll guard is "scrollable range < one viewport AND within 1.5s of a structural mutation" (`_lastStructuralAt`, stamped at extendTop/extendBottom/trimTop/trimBottom) — a fixed 200px bar missed the field landings (782/923/997 against a 755px list) and simply widening it would make a genuinely short partial window skip decisions FOREVER. RULE: before guarding an event, verify that event still fires in the failure state, and cover every entry point that computes the same boundary (scroll/wheel/touch/observer). Test: scripts/test-paging-collapse-guard.mjs (26, field numbers).
- **SCOPED-BUCKET MERGE (2.305.0, inc-msof8i22 — the pool refused to leave an account whose Opus was spent)**: model-scoped weekly caps arrive EITHER as `model_scoped[]`/`limits[]` entries OR as named `seven_day_opus`-style fields, and REAL payloads carry BOTH with different members. Both parsers (`ClaudeCodeAdapter.parseGetUsageResponse` + usage-routes `_parseUsage`) treated the named fields as a fallback for an EMPTY array, so one Fable entry hid the Opus cap on every path — ten usage-cache files, zero Opus buckets, across ⟳/statusline/get_usage alike, and the pool's exhaustion test therefore never fired. They MERGE now (dedupe by model name, array entry wins). Regression must pin the BOTH-PRESENT shape; array-only and named-only both pass while the bug is live.
- **COUNTER CAPTURE (2.302.0, proven in fleet production data): a monotonic counter's value is CAPTURED ONCE and reused — any re-read after an `await` is a concurrency race.** ws-handler minted `id` with `++sessionCounterRef.value` but built `sockName` from a bare re-read ~100 lines and several awaits later (cwd preflight, resume host inference, findKeeperFor), so a burst of concurrent creates all took the LATEST value: 15 live sessions on a fleet instance, 7 with mismatched counters, four sharing `cw-36` **1 ms apart** — same-millisecond arrivals share a socket path AND a session-meta filename, so one session's metadata (name, claudeSessionId, account, taskId) silently overwrites another's. One `seq` now feeds id + sockName + default name (server.js adopt path too); scripts/test-session-id-race.mjs forbids bare re-reads structurally and reproduces the symptom as a negative control. Sibling fix: the client's `_migrateStateMap` silently DROPPED one of two values mapped onto the same session key (how a session ends up wearing another's custom name while the rightful owner loses its own) — now a `state-key-collision` breadcrumb + telemetry event.
- **B-b87b batch (2.267.0 — all 16 confirmed findings from the 31-agent global review; do not regress)**: ①dial-session-bridge pipe onExit = TRANSPORT death (a real child exit rides the `_remote_exit` sentinel) — it must tear the loopback down so the attach client respawns, never no-op (dial chat froze forever on any link drop). ②SettingsManager._save nulls `_saveTimer` in finally (guard covers the in-flight POST; a fired-timer id left armed made applyRemote/refetch skip every future remote settings change until reload). ③user-state pushes are DIFF-PATCHES against the last server-applied snapshot (`_serverStateJson`; new merge-only `PATCH /api/user-state`) — a stale tab can only send keys it changed; same-key races stay last-write-wins (documented residual). ④explicit account pick VETOES keeper/agentd/dial adoption (adopted children keep their original billing — the pick must respawn); adoption probes gate on `!data.accountId`. ⑤`resumeSpawn`/`sawFirstId` persist in session meta (implicit-fork adoption survives restarts). ⑥pooled account as DEFAULT on a remote create falls back to the host login; explicit pool pick on remote refuses (pools are local-only). ⑦remote spawns prepend `AMBIENT_OAT_UNSET` (all 5 command builders) — a host profile's CLAUDE_CODE_OAUTH_TOKEN silently re-billed every remote session. ⑧ssh Terminate of agentd-ADOPTED sessions keys the state file by `session.keeperSid` (webui-id key = remote claude survived every terminate). ⑨pty-wrapper retry: `totalAttempts` monotonic cap + ladder reset only when the child LIVED ≥30s (ssh stderr is pty data — resetting on bytes made a dead host retry at 1s forever). ⑩chat-view _reattach: the sync handler stays armed for the WHOLE retry ladder (input re-enable is the only 30s concern); terminal ladder paths remove it. ⑪`parseArchiveListing` = the ONE listing parser (remote-fs exports, files.js imports) + RemoteFs.archiveExtractEntry/_streamCmdToFile/fetchToLocal (binary-safe both transports); ⑫/api/file/excel|csv|docx remote branches via `remoteToLocalTmp` (size+TTL-validated temp cache — the CSV pager re-reads ranges). ⑬dial ctx sync = `syncDialGroupCtx` (per-file newer-wins over the device link, CONTENT-HASHED both directions so fsWrite's mtime loss can't ping-pong; traversal guard on remote rel paths). ⑭client `_sessionFolderMatch` strips the display host label (board membership now agrees with server groupsForSession for remote sessions). ⑮fake `code` → data/bin/editor/ (see File Structure). ⑯billing-switcher rows render dead-reckoned estimates via estDisplayPair (dash-underlined) instead of stale raw readings.
- **Display cwd must never reach a spawn (2.225.2, userL h200 real incident)**: the sidebar merge composes remote webui sessions' cwd as the folder-grouping DISPLAY string `"<host name>: /path"` (a load-bearing grouping/archivedFolders KEY — do not change the composer); it leaked into persisted openSpecs and a resume `cd`'d into the literal label → claude ran from $HOME → "No conversation found" → 10min unresumable block. `stripCwdHostLabel` (utils.js — strips iff the remainder is an absolute/~ path) guards the client boundary (createSession/attachSession/viewSession) + a server-side twin at ws-handler create intake heals already-persisted specs and stale-bundle clients. Any NEW consumer that sends a merged-record cwd to the server must strip.
- **Restart-survival audit batch (2.218.0-2.219.0, user directive '整体review所有不抗重启处'; full 54-finding report: SharedContext/vibespace-restart-audit-20260721.json)**: ①resume host inference — a host-less claude resume with no local transcript but a unique data/remote-jsonl cache hit resumes ON that host; `hosts.findKeeperFor` first probes for a LIVE keeper child and ATTACHES (keeperSid) instead of second-writing. ②unreachable-host memo (60s) — transcript fetch serves stale cache instantly instead of a 15s ssh timeout per window. ③CRITICAL dial-bridge restore read meta under a cw-stripped name that NEVER matched → no dial session survived any server restart (fixed + bridge keyed by webui id). ④IMPLICIT FORK adoption — `claude --resume` of a conversation locked by another live claude silently forks to a new id; the 2.156.1 hijack guard vetoed it, so live stream showed turns that every rebuilt history lost ('compact recap里有,窗口不展示'); a first-id-line change on a RESUME spawn (`session._resumeSpawn` && !`_sawFirstId`) is now adopted. ⑤failed-resume rescue — resume closes the old window first, so a failed create's spec-less shell EVAPORATED on refresh (silent window loss); it now flips into view-only + real viewSession openSpec. ⑥`agentdSession`/`forkRequested` persisted+restored; ssh chat terminate is mechanism-AGNOSTIC (tries agentd state-file kill AND keeper stop — the restored-flag miss silently orphaned remote claudes). ⑦dead-socket cleanup preserves REMOTE sessions' metas as `.orphan` files (B-1525 first half). ⑧writeSessionMeta atomic; uncaughtException runs the full flush belt. ⑨reconnect refetches maintenance+settings; reattach safety timer 30s; unknown-prior-epoch reattach = full view reload; read-only windows skip reattach. ⑩(2.219.1) session cards surface `remoteState` — a LIVE card whose host is unreachable carries an amber 'host unreachable' chip (the local wrapper shell IS alive and queues input by design; the card no longer pretends the machine is fine); rides active-sessions, restored from wrapper meta.
- **Dead-session replay = blank window shell (2.217.0, userL ×12 real report; user directive: restart后必须能看read-only view+手动resume)**: attachSession builds its ChatView only INSIDE the one-time 'attached' handler — an initial-attach error (stale serverId replayed after the server lost its sessions: OOM kill, pod/docker recreation) had NO ChatView for the error path to flip read-only, so the handler dropped and the window stayed a bare shell. Fix: the error branch drives `_viewIntoWindow` (shared with viewSession) into the SAME window — view-only history + Resume bar; works host-less (the transcript finder scans data/remote-jsonl) and with the host machine DOWN (fetchSessionJsonl serves the stale cache instead of throwing). ChatView-side `_tryViewOnlyRescue` covers the reconnect-time variants. Root enabler fixed too: syncSessionIdentity read `match.hostId` but live entries carry `host` — remote sessions' specs NEVER got their host backfilled. Smoke: scripts/test-attach-rescue.mjs (worktree+CDP, 8 asserts).
- **Resume guard (2.179.0, userW's duplicate-session incident)**: a plain claude `--resume` REUSES the conversation id — resuming an id that is ALREADY live on the same machine put TWO claude processes on ONE JSONL (double-writer class) + duplicate sidebar cards (real incident: billing-switch kill no-op'd on a stale webui id, the respawn then double-resumed). ws-handler 'create' refuses (`code:'resume-already-live'` + existing session info; client attaches the live session instead); forks exempt (new id). **CODEX WAS NEVER EXEMPT (P1 codex double-writer, 2026-09): the guard's "codex resume forks a new thread id" clause was FALSE for the current wrapper — `thread/resume` REUSES the thread id (only `thread/fork` mints one), so resuming a thread still held by a live app-server put two app-servers on one rollout with no refusal, no sweep and no `_resumeWarning`. Now the guard matches codex on `backendSessionId` (same host semantics), the writer sweep has codex legs (see writer-sweep.js), and the client re-sends codex resumes on reconnect under the same guard-turns-duplicate-into-attach reasoning as claude.** The 'kill' case falls back to resolving by backendSessionId when the webui id is unknown, and the billing switcher passes it.
- **Username migration must also re-encode claude's project dirs (2.236.1, userW's real incident)**: the 3.5.0 image's vibe→<name> rename + `/home/vibe` symlink covers recorded ABSOLUTE paths, but claude encodes transcript dirs from the RESOLVED cwd — pre-migration transcripts under `projects/-home-vibe-*` were invisible to every resume ("No conversation found with session ID"). `migrateLegacyHomeProjects()` (server.js boot, before restoreSessions) renames each to the new encoding + symlinks the old name (VibeSpace's own cwdToProjectDir over recorded /home/vibe cwds resolves through it); idempotent, collisions skipped.
- **Changing the container USER ripples to EVERY `kubectl exec` (2.162.7 regression)**: the 3.5.0 personalized-username image runs the container as root (boot-root.sh renames vibe→instance-name, drops via runuser); but `kubectl exec` bypasses the entrypoint's runuser and runs as ROOT — so the admin's exec-based git update hit "dubious ownership" on the uid-1000 PVC repo and every update failed. Fix pattern: `git config --system --add safe.directory '*'` at boot (root) + `safe.directory $(pwd)` in update.sh. Any root-USER container change must audit all kubectl-exec-driven ops (update/restart/probes).
- **Dial-device (paired) sessions — FULLY FIXED + E2E-verified (B-0d70, 2.163.0–2.164.1; scripts/dbg-dial-session-e2e.mjs, real dialed daemon, 22 asserts)**: the failure stack, all fixed: (1) stale cached DeviceManager bound to a dead ws stream after re-dial — online=true but blank (`_dialStream` tracking, rebuild on change, cache invalidation on dial-in, 2.162.3); (2) daemon child spawns inherit launchd/systemd's minimal PATH → no node/claude (spawnEnv prepends node dir + tool bins, 2.162.4); (3) chat-wrapper only substituted `__VS_OFFSET__` when VIBESPACE_REMOTE_SID was set — dial's agentd-attach got a literal placeholder → offset=NaN → zero relay → blank (OFFSET_MODE gates on the placeholder's presence, 2.162.6); (4) **THE #1 blank-chat root cause (2.163.0)**: the server shipped ITS OWN os.homedir() (`/home/<user>` on the pod) as the device spawn cwd — on a Mac that path doesn't exist, and `child_process.spawn` with a nonexistent cwd emits an async `'error'` event that, WITH NO LISTENER, **crashed the whole device daemon → every session blank**. Fix: the daemon's pipe-session resolves cwd to a real dir (fallback $HOME) + attaches a spawn 'error' listener (a bad cwd/binary can NEVER take the daemon down); ws-handler defaults a remote/dial session's empty cwd to the DEVICE home (`hosts.homeDir`, dial-aware); RemoteFs `info`/`home`/`stat` + `_run` gained a device fast path so `/api/file/info?host=<dial>` works (was ssh-only → 400 "has no ssh" → New Session preflight reported every existing dir nonexistent = the "/Users/<user> 不存在" report). (5) **terminal-on-dial IMPLEMENTED (2.164.0)**: device runs claude/codex in a node-pty via the daemon's `open-session`, proxied through DialSessionBridge (pty mode: open-session/resize/kill + live bytes); local side = dtach → pty-wrapper → vibespace-agentd-attach in a new RAW-TTY mode (`stty raw` on its controlling tty — the exact `ssh -t` shape). node-pty best-effort installed by the pairing installer (chat/files/mounts never need it). **Terminal-on-dial invariants (2.164.1 review, do not regress)**: (a) the bridge KILLS the device pty on attach `onDead` + `close(sid)` — a pty is LIVE (not the keeper model), and pty-wrapper's REMOTE_RETRY respawns the attach on every link flap, so without the kill each reconnect orphaned another claude on the device; (b) attach-cli pty mode exits 0 on `session-exit`/permanent errors (auth/proto/session-error printed once) so pty-wrapper FINALIZES instead of looping 120× on a nonzero device-child exit — only a genuine transport death reconnects; (c) SIGWINCH reads `process.stdout.columns` in a `setImmediate` (a sync read races Node's own size refresh → forwards the STALE size); (d) terminal-dial billing mirrors the chat guard (reject an explicit subscription §ban-safety; fail the create on API-key placement failure — no silent wrong-billing). Codex CHAT on dial fails fast (JSON-RPC wrapper can't ride the byte pipe, B-0588); codex TERMINAL works. Diagnosis toolkit: pod-internal WS probe + read data/session-buffers/<id>.buf bytes + /proc/PID/cmdline for literal __VS_OFFSET__ + hosts/:id/test tools probe + a temp shared-file tracer for the SIGWINCH chain (server→pty-wrapper→attach-cli→bridge→daemon).
- **#workspace must be overflow:clip, NEVER a scroller (2.161.2, three-iteration live-tracer diagnosis)**: overflow:hidden containers are still PROGRAMMATICALLY scrollable — browser focus-scrolling scrolled the whole workspace whenever focus landed inside a window extending past its bottom (freeform windows legally can), with no scrollbar to undo it; every window then appeared shifted by exactly scrollTop ("整个工作区被往下推", stuck at 239px live). Correlated with bottom-edge right-clicks, masqueraded as a context-menu bug. clip + a scroll-snapback listener in WindowManager (belt). Diagnosis pattern: telemetry ring-buffer tracer on the user's machine; beware self-inflicted diff artifacts (windows keyed by a constant '?' fabricated the 'windows moved' evidence — key diffs by REAL ids).
- Permission/AskUserQuestion resolution must survive restarts WITHOUT the control_response (2.109.5, real report: an answered questionnaire resurrected as awaiting-input): the control_response only exists in server MEMORY — it goes to claude's stdin and the wrapper's .buf tees stdout only, so a restart-rebuilt history is always request-without-response. FOUR cooperating guards (keep all): (1) MessageManager's tool_result merge auto-resolves an unresolved `permission` (result ⇒ answered) and emits `permission` in the edit fields ONLY when newly resolved there (unconditional re-emit re-appended the "✓ Allowed" chip renderToolMsg deliberately omits on live completions + clobbered client selectedAnswers); (2) _processControlRequest's error→pending restore applies ONLY to interrupt-FLUSHED tools (content still tool_call) — a message with a REAL tool_result is settled and auto-resolves instead (else the [tool_use, tool_result, control_request] end-append replay order resurrected denied/errored cards), together covering BOTH replay orders (the buffer-merge anchor can place control_request before OR after the tool_result depending on rotation); (3) **is_error alone is NOT a denial** — `_resolutionFromResult` treats only the CLI's canned user-rejection text as denied (an APPROVED tool that then fails is is_error too — hundreds per transcript — and must not render "✗ Denied"); (4) `activePendingPermissions()` (session-store) scans ALL records for resolving tool_results, never a tail window (the answer scrolls out of any fixed window while the session keeps working — same class as the chatStatus 200→2000 scan-depth bug); plus the client attach injection skips tools already complete/error. Run-collapse side (same release): a card with an UNRESOLVED permission returns null from `_updateRuns`' kindOf (never folds — hidden Allow/Deny stalls the turn unnoticed) and the permission-edit path calls `_updateRuns()` directly (the overlay mutates the card in place; the childList MutationObserver never fires for it). **Paging compensation is ELEMENT-ANCHORED, never scrollHeight-delta (2.111.5, tracer-diagnosed root fix; COMPLETED 2.229.1 — the paging-jump recurrence had three residual holes, all forensics-confirmed via a scrollTop-write interceptor in scripts/test-chat-paging.mjs: ① _extendBottom's per-message _onCreateMessage path ran the PINNED live-trim storm per batch (now gated on !_loadingHistory — batch callers trim once); ② _trimTop still used relative `scrollTop -=` which DOUBLE-COMPENSATED against the browser's native scroll anchoring reacting to the same removals (captured ±4000px adjacent-frame oscillation; now absolute element-anchored, delta = anchorless fallback only — RULE: any scroll compensation must be an ABSOLUTE anchor restore, a relative ± collides with native anchoring); ③ _withViewportAnchor returned failure when its anchor got FOLDED by a run-collapse pass inside fn (likely since 2.213.0 widened foldable kinds) → callers hit estimate-skewed delta; now restores on the nearest visible sibling like _updateRuns)**: under content-visibility:auto a fresh insert measures at its ~80px ESTIMATE while trimmed elements had REAL heights — the delta can go NEGATIVE (captured live: insert-50/trim-50 shrank scrollHeight 312px → scrollTop clamped to 0 → top sentinel load-looped at the clamp). `_withViewportAnchor(fn)` (chat-view.js) wraps every paging mutation (_extendTop with the fold inside, _loadEarlierGap, _trimGapDom top-side); delta math survives only as the no-anchor fallback. Diagnosis pattern: chat scroll tracer (ring buffer of every scroll-affecting op + Ctrl+Shift+J manual dump → /api/telemetry kind:'trace' name:'chat-scroll-jump'; the wheel-recent exemption HID the repro — jumps happen mid-scroll; content-DISPLACEMENT jumps produce NO scroll event, so a 300ms viewport-top-drift watcher covers those). Run-collapse × virtual-scroll invariants (2.109.5, real report "往上翻阅跳动+翻不回来"): `_updateRuns` VIEWPORT-ANCHORS every pass (captures the topmost visible non-header child + offset before mutating, restores after — collapsing above-viewport content must never move what the user reads; skipped when pinned; a folded anchor falls forward to its next visible sibling); `_extendTop`/`_extendBottom` call `_updateRuns()` synchronously right after their own scroll compensation (the 180ms-debounced observer pass used to fold freshly loaded cards AFTER compensation → viewport yank → sentinel re-trigger → load loop); run open/closed memory (`_runExpanded`) is keyed by ANY member, not the first; while PINNED only the LAST run keeps an opened state (2.216.0 — an accidental/watch expand inherited into a growing run stayed open forever; unpinned reading never auto-collapses) (pagination prepends older members onto a run, changing its first element — a first-member key re-collapsed the run being read on every _extendTop); `_updateRuns` ends with `_runsObserver.takeRecords()` — the `_runsMutating` flag alone CANNOT suppress observer self-triggering (MutationObserver callbacks deliver at a microtask checkpoint after the finally resets the flag), so without the drain every pass scheduled another identical pass in a permanent 180ms rebuild loop.
- SILENT TRANSCRIPT LOSS (2026-07-03, CRITICAL): sessions spawned by a server that was itself started from INSIDE a Claude Code session never wrote lock files or project JSONLs — `CLAUDE_CODE_CHILD_SESSION=1` (inherited through dtach→pty-wrapper→claude) alone puts the CLI into child-session mode (verified by A/B env test on 2.1.199; `CLAUDECODE`/`CLAUDE_CODE_ENTRYPOINT`/`CLAUDE_CODE_SESSION_ID` individually are harmless). Conversations looked fine live but vanished on terminate+resume (WebUI and CLI both read the missing transcript). server.js now strips `CLAUDECODE`, `CLAUDE_CODE_*`, `CLAUDE_EFFORT`, and stale `CLAUDE_WEBUI_*` from process.env at startup when it detects it was launched inside a session, and restoreSessions warns (Linux /proc scan) about surviving sessions still running poisoned. NOTE for agents working on this repo: restarting the server from your own Bash is SAFE ONLY because of this sanitizer — do not remove it.
- Chat status bar empty until first reply after resume/attach: model + contextWindow came only from `result.modelUsage` / system-`init` records, which are stream-json **stdout-only and never written to the JSONL** — any pure-JSONL restore got `model:null, contextWindow:0`. Fix in `SessionMessages.chatStatus()`: fall back to `assistant.message.model` (skip `<synthetic>`), infer contextWindow from observed usage (>190k tokens ⇒ 1M beta else 200k); permission mode isn't in the JSONL at all → `session._permissionMode` (remembered from launch args) merged into chatStatus by both WS attach and `/api/session-messages?withStatus=1`. Codex needs none of this (rollout JSONL carries turn_context model + token_count + approval policy natively).
- Clipboard image paste failures: (1) `paste` event never fires — xterm.js v5 uses Clipboard API directly, bypassing paste event. Fix: `attachCustomKeyEventHandler` intercepts Ctrl+V, redirects focus to hidden contenteditable div. (2) `navigator.clipboard.read()` unavailable over HTTP. Fix: contenteditable div receives real paste event with clipboardData.items. (3) `xclip -i file` unreliable. Fix: `cat file | xclip` pipe. (4) Bracketed paste `\e[200~...\e[201~` inserts text, doesn't trigger image check. Fix: send raw Ctrl+V (`\x16`) which triggers Claude Code's Ink framework clipboard check. (5) Server polls `xclip -t TARGETS -o` to confirm clipboard ready before responding. (6) DISPLAY can NOT be trusted from the inherited env — servers (re)started from shells with a stale `DISPLAY=:99` broke every xclip call, and under XWayland even the right display fails without the compositor's XAUTHORITY cookie (`/run/user/<uid>/.mutter-Xwaylandauth.*` — note "Xwaylandauth" does NOT contain the substring "xauth", match /auth/i). `detectXDisplay()` (server.js) probes DISPLAY candidates (env + /tmp/.X11-unix/X*) × XAUTHORITY candidates (env, /run/user/<uid>/*auth*, ~/.Xauthority) with `xset q` at startup; the winning pair is used for the server's xclip calls (app.locals.xEnv) AND injected into session spawn env (the CLI reads the clipboard itself on Ctrl+V). Sessions spawned before the fix keep their dead DISPLAY until restarted. **COOKIE ROTATION (2.67.1, real incident):** a compositor/Xwayland restart mints a NEW `.mutter-Xwaylandauth.XXXXXX` file — every running session keeps the OLD path → "Invalid MIT-MAGIC-COOKIE-1", paste dead in all of them at once. Fix: `stabilizeXAuth` merges the working cookie into `~/.Xauthority` and spawns sessions with THAT stable path (processes re-open the auth file per X request, so a later `refreshXEnv()` merge heals everything without respawn); the paste route retries once through `app.locals.refreshXEnv` on cycle failure. One-time manual heal for pre-existing sessions: `xauth -f <oldfile> merge <newfile>`.
- Spontaneous terminal shrink + disconnect (mid-use, no user action): `resizeSessionToMin` min'd over ALL `session.clients`, but entries get injected at a hardcoded 120×30 placeholder on `attach` and on subagent View-Log attach (the viewer registers into the PARENT session's clients map). A reconnecting/ghost client at the placeholder, or an open subagent viewer, won the min and shrank the PTY for everyone (broadcast `effective-size` → `terminal.resize`). Compounded by NO WS heartbeat — a half-open connection (network blip, sleep/wake, OOM-induced unresponsiveness from heavy local jobs) lingered in the clients map for the full TCP keepalive window. Fix: (1) client entries carry `real:true` only after a genuine terminal `fit()` resize; `resizeSessionToMin` considers only real clients (falls back to non-viewer placeholders when none, for chat PTYs); subagent viewers flagged `viewer:true` and never participate. (2) WS heartbeat (ping every 30s, terminate on two missed pongs) evicts ghosts promptly. (3) Terminal re-`fit()`s on reconnect so the placeholder is replaced by a real size before the heartbeat could shrink to it.
- Dead settings in schema: removed 9 settings defined in `settings-schema.js` but never read by any code (enableAutoGrouping, enableStarredDrawer, showNewSessionCard, showRefitButton, showOverlapIndicator, flatTimeSort, suppressWaitingOnRestore, layoutBindings, colorOverrides). Removed empty Hotkeys, Themes, File Explorer categories.
- Tab merge hit-test matched occluded icons: dragging A's titlebar onto A's own position would merge with a stacked window B underneath (because `getBoundingClientRect` ignores occlusion). Fix: use `elementFromPoint` after temporarily disabling `pointer-events` on the dragged element; match the `.window` ancestor. Extracted into shared `_detectTabMergeTarget` helper on tab-group mixin, used by all three drag paths (titleBar, icon drag, tab drag-out). Side benefit: window.js was checking for `.tab-bar` (wrong class name — actual is `.tab-bar-tabs`), so tab-bar merge drops never worked; now they do.
- `/compact` leaving chat stuck on 'thinking': stream-json emits `user` messages (compact summary) but no `result` after `/compact`. `_processUser` never called `_finalizeStreaming`, so stale streaming assistant messages persisted indefinitely. Fix: `_processUser` now calls `_finalizeStreaming` at start of new user message. Wrapper also treats `compact_boundary` system message as end-of-stream (`meta.streaming = false`).
- isStreaming derived from stale normalizer messages: old heuristic scanned normalizer messages for `status==='streaming'`, but `processLive` could create streaming entries before `convertHistory` finalized them (race on server restart). Fix: replaced with explicit `session._isStreaming` flag tracked from protocol signals (Claude: `result`/`compact_boundary`/`user`; Codex: `task_started`/`task_complete`/`turn_aborted`/`task_failed`). Also set immediately on `chat-input` send (no round-trip delay). Initialized from wrapper metadata on restore, cleared on exit. Single source of truth — no more heuristic.
- Huge sessions (>512MB JSONL) — tail-only load + continuous seek scroll (no seam marker): `readFileSync(fp,'utf-8')` THROWS past Node's ~512MB max string length (swallowed error → blank chat). `readJsonlBounded` (codex adapter, shared with Claude session-store) with `{tailOnly:true}` loads a line-aligned last-32MB TAIL only — NO head, NO elision seam marker (the old head+2MB+seam+tail model is gone). The whole earlier history is one continuous virtual scroll: `ChatView._installSeekSentinel` puts an invisible 1px `.chat-gap-sentinel` at the top of the tail; an IntersectionObserver + scroll-driven `_maybeSeekEarlier` call `_loadEarlierGap`, which seek-reads one ~2000-record slab older each time (`/api/session-history-gap?endLine=N&count=C`), walking the cursor from `tailStartLine` down to line 0 (`_finishSeek` removes the sentinel at 0). Gap messages carry `.chat-gap-msg`, are excluded from virtual-scroll trim + `_windowStart/_windowEnd` accounting, render read-only (orphan tool results where the call is outside the slab — acceptable). Seek primitives: `getJsonlLineIndex` caches a line→byte-offset table (streaming Buffer scan, no >512MB string; EXTENDS incrementally — append-only growth scans only the new tail bytes, so seeks stay ~15ms even while a live session keeps writing, instead of re-streaming 100s of MB per jump); `jsonlGapInfo` gives `{tailStartLine, totalLines}`; `readJsonlLineRange` seek-reads any raw line range (records tagged `__line`). `?info=1` returns the gap; `?endLine`/`?startLine=N&count=C` seek-normalize a slab (subagent-filtered to match the display path); `&whole=1` unclamps the read to `[0,totalLines)` (default clamps to `[0,tailStartLine)` so continuous scroll-up never re-reads the registered tail). JUMPS (search + minimap) TELEPORT by ABSOLUTE FILE LINE — immune to normalized-index drift AND to the live tail sliding forward as the file grows. `ChatView._seekTeleport(line)` replaces the whole view with a `whole=1` slab centered on the line (±300 lines), keeps+resets the sentinel (scroll-up continues by line), sets `_teleported` + `_gapCursorDown`, and shows the scroll-to-bottom button as 'return to latest' (click → `jumpToBottom` restores the live registered tail, `_resetGapAfterJump` clears `_teleported`). Teleport browsing is BIDIRECTIONAL: scrolling down seek-loads newer slabs (`_maybeSeekLater`/`_loadLaterGap`, cursor `_gapCursorDown`, 3s idle backoff at file end) and the DOM is capped by `_trimGapDom(side, cap=3400, keep=2400)` which drops far-side elements and REWINDS the corresponding cursor so scrolling back re-loads them — the cap MUST exceed teleport slab + one full 2000-line slab (~1200 msgs) per direction or up/down loads thrash each other's content. content-visibility is force-disabled only while the jump LANDS (stable heights → sync scroll hits), then `_setStableHeights(false)` re-enables it ~1.6s later. The collapse of never-cv-rendered elements is ASYNC (relevancy decided at paint) — scrollHeight shrinks hugely + scrollTop clamps, so scroll-delta compensation FIGHTS native scroll anchoring (observed scrollTop yanked to 0; view relocated ~1.5s after landing). Correct approach: on restore, REPLAY the landing — `_scrollElStable(this._lastJumpTargetEl)` or the search's recorded `_lastRevealRun` (exact range inside a tall message), skipped if the user scrolled since the jump (`_lastUserScrollAt`). A match already RENDERED in the live view (|element ts − match ts| < 2s) scrolls directly — no teleport (replacing the live view for something on screen was jarring); same fast path in `_jumpToFileTime` via `_scrollToNearestTs(ts, 2000)`. The pos indicator hides while teleported (its window-index numbers are stale). The `.chat-search-flash` overlay parents inside the chat container (absolute, host-relative coords) NOT document.body — a body-level fixed overlay draws over other windows stacked above. One jump primitive: `jumpToFileMatch` (search) and `_jumpToFileTime` (minimap) both teleport, so there is NO index-drift path left. Landing is content-visibility-proof: `_scrollElStable` / ChatSearch `_revealNearest` re-center over ~12 rAF frames + timed re-centers at 180/400/750ms (heights keep computing for ~1s after a big slab loads); `_revealNearest`/`_revealInEl` route through `_scrollToRange`, which handles FOUR reveal blockers: (a) most tool cards are COLLAPSED `<details>` — `_expandRangeAncestors` opens the match's whole ancestor chain first (+ forces content-visibility on the message); (b) expanding fires the card's DEFERRED REHIGHLIGHT (`data-highlight-deferred`, toggle listener) which REPLACES the card DOM and detaches the Range — every settle step re-acquires a live range from the same (stable) `.chat-msg` element when `startContainer.isConnected` goes false; (c) `_scrollNestedIntoView` scrolls nested overflow ancestors on BOTH axes — vertical (max-height cards) AND horizontal (nowrap code lines; only when the match is outside the h-viewport, so wrapped text doesn't jiggle); (d) centers the outer list on the range nearest the anchor (raw-line→message isn't 1:1). It pulses a `.chat-search-flash` overlay glued to the range for ~1.2s (takes a GETTER so it follows a re-acquired range; hidden while clipped by the list or any inner scroll container); the current highlight is a solid high-contrast `::highlight(chat-search-current)` vs the dim `chat-search` and `applyHighlightLayer` re-asserts the current highlight from a stored node+offset `_currentAnchor` (scroll-driven re-applies would otherwise wipe it). Search covers the WHOLE file and STREAMS progressively (less-style live count): `searchJsonlFullStream` (codex adapter) reads the JSONL ASYNC (yields between 16MB chunks so the response flushes and other requests aren't blocked; raw-substring pre-filter → parse candidates → match extracted text; 500-match cap; 240MB ≈ 0.6s) via `?search=q&stream=1`, writing NDJSON: a `{tailStartLine,totalLines}` preamble, then one `{line, ts, role, preview}` per hit as found, then `{done,total,truncated}`. ChatSearch (`_streamFullFileSearch`) reads the stream, jumps to the FIRST hit immediately, updates the status `${idx}/${n}… searching` live, finalizes to `${idx}/${n}${truncated?'+':''}` on done, and aborts the prior stream (AbortController) on a new query. The `Cache-Control: no-transform` header makes the compression middleware skip buffering so writes flush in real time. (Non-streaming `?search=q` + sync `searchJsonlFull` kept as a fallback.) Matches carry `{line}`; all matches seek-load/teleport by absolute file `line` (no normalized index needed — the teleport model is immune to index drift). ChatSearch auto-switches to full-file streaming mode when the gap minimap is active. The MINIMAP covers the whole conversation: `scanJsonlUserTurns` (mtime-cached) streams the file once (substring pre-filter `"role":"user"` → parse ~2k candidate lines, ~1.2s for 541MB); `?fullturnmap=1` returns every user turn in TIME coordinates + file line; `ChatMinimap.renderFullExtent` positions markers by `(ts-firstTs)/(lastTs-firstTs)`, thumb tracks the visible ts span (`setVisibleTsRange`←`_reportVisibleTsRange`, which uses getBoundingClientRect for real visibility + min/max ts, and in teleport mode ignores ts past the conversation's `lastTs` — the normalizer stamps `Date.now()` on slab records lacking a timestamp, which would otherwise stretch the thumb to the recent end; live messages are also deferred while teleported so they don't leak into the history view); clicking a marker `_jumpToFileTime(ts, line)` teleports. Messages carry `srcLine` (from each record's `__line`) + DOM `data-ts`/`data-line`. NOTE: a live-growing session's tail slides forward, so tail-relative indices drift for the active session — teleport-by-line sidesteps this entirely.
- Codex thread vanished from session list permanently: a transient IO error in `extractCodexThreadMeta` was swallowed and the empty meta (threadId='') was cached keyed by the file's mtime — if the session never wrote again (terminated), the entry never invalidated. Fix: failed extractions are never cached.
- Stale openSpec replay → blank chat window with outdated title: a window whose dtach session died and was resumed under a NEW server id kept `serverId`/`name` from creation time in its `_openSpec`. `replayOpenSpec` replayed them verbatim (attach to nonexistent session → blank; stale first-user-message name instead of custom rename), `captureState` dropped `backendSessionId` when the stale serverId no longer matched `_allSessions` (self-perpetuating), and the server's attach-not-found error had no `sessionId` so the ChatView couldn't correlate it. Fix: replayOpenSpec re-resolves serverId/name against the live session list by backend+backendSessionId (+ `getCustomName`), falls back to `viewSession` when dead; captureState/restoreState/loadNamed fall back to `openSpec.backendSessionId`; error carries `sessionId` and ChatView goes read-only with the Resume bar.
- Chat drag-upload overlay permanently visible ("won't disappear"): **this project has NO global `.hidden {display:none}` — every component defines its own scoped rule**. The new `.chat-drop-overlay`/`.chat-upload-toast` had a base `display:flex` and toggled a `.hidden` class with no matching CSS, so they were never hidden. Fix: added `.chat-drop-overlay.hidden`/`.chat-upload-toast.hidden { display:none }`. Verification gotcha: assert `getComputedStyle(el).display`, NOT `classList.contains('hidden')` — the class toggled correctly the whole time while the element stayed `display:flex`. (Same root cause as the earlier minimap-label-not-hiding bug.) The overlay show/hide itself uses a dragover-idle timer (no `dragleave`/`relatedTarget` — unreliable in Safari — and no `dragend`, which OS/Finder file drags never fire).
- Claude fork behaved identically to resume in chat: the WebUI **never captured Claude's stream-json `session_id`** (zero refs anywhere — for new sessions the id was filled in lazily by lock-first discovery). `--fork-session` makes claude mint a NEW session id and write a separate JSONL (verified empirically: plain resume reuses the id, fork returns a fresh one), but the server kept tracking the PARENT id passed at creation — so a forked window shadowed the original (same name/history/resume target) and the fork's transcript was orphaned. Fix: the **claude** stdout parser (`setupSessionPty`'s chat branch is `if (backend==='codex'){…} else {…claude…}` — the adoption MUST go in the `else`; a first attempt in the codex branch silently never ran) adopts `msg.session_id` when it differs from the tracked id, records the parent in `forkedFrom` (informational for Claude — discovery never hides on it, unlike Codex), persists `claudeSessionId` in session meta, and `broadcastActiveSessions()`. Guarded by a one-shot `session._forkRequested` flag (set in ws-handler only when `data.fork` + Claude) so a normal resume — whose id the parser also sees on every line — can never be hijacked. **FIRST capture is UNCONDITIONAL since 2.156.1 (userL real incident): the fork guard used to veto null→id adoption — the ONLY parser-side capture — so a brand-new session could never learn its id from the stream; local sessions were silently rescued by lock-first discovery, REMOTE keeper sessions were not (meta null forever → attach transcript-prefetch path error → window permanently blank while the remote claude ran fine). With no tracked id there is nothing to hijack; a CHANGED id still requires the fork flag.** **Timing gotcha (why it looks like resume at first):** under `--input-format stream-json` claude emits NOTHING until the first input message — the fork id rides in the `system/hook_started`/init line that only appears AFTER the user sends their first message. So a freshly-forked window is genuinely indistinguishable from a resume until you continue it; on that first turn claude forks, the parser adopts the new id, and the window becomes its own session (separate JSONL, distinct sidebar entry, resumes as the fork). The client propagates the adopted id to the window `_openSpec` via `syncSessionIdentity` on the next `active-sessions` broadcast (same path Codex fork/resume uses). Needs a server restart to take effect (dtach sessions survive it). Verified live: fork + one message → `backendSessionId` 649e5dbe… → 5c480868…. **UX follow-up:** because the divergence needs a first message, clicking Fork (chat mode) now opens `#dialog-fork` (a "first message" popup); submit forks AND sends it (`createSession({initialMessage})` → `_sendChatMessage` → `chat-input`, sent AFTER history renders so `loadHistory` doesn't wipe the echoed message) so the session diverges immediately instead of mimicking a resume. Terminal-mode forks skip the popup. The popup also has an editable **Title** field (`#fork-title`, pre-filled with the default `<base> (forked)` via `_defaultForkName`) so you fork straight into the title you want; the title is passed as the session name (window title is right immediately) and — once the fork's NEW id is adopted (`syncSessionIdentity`, guarded by a remembered `parentId` so the parent isn't renamed before divergence) — persisted as a custom name (`sidebar.setCustomName`) keyed by the fork's session key. The window-title resolver prefers `getCustomName`, so a forked window keeps its real title instead of falling back to the first-message-derived name after it stops or the page reloads. **Fork-from-message (point-in-time fork):** each assistant message in chat shows a GitHub repo-forked icon (`UI_ICONS.forkBranch`) next to the open-in-editor button (`addForkBtn`, claude-only, assistant-only, not subagent views, requires `msg.uuid`). It branches a session truncated at that message via `claude --resume <id> --resume-session-at <message.uuid> --fork-session`. **Two CLI gotchas (found by disassembling the binary; the help text misleads on both):** (1) `--resume-session-at`'s help says "(use with --resume **in print mode**)" but it works fine in our stream-json interactive mode — verified live, no `--print` needed (which we must never use → program-use billing). (2) help calls the arg `<message id>` but it's looked up by **`message.uuid`** (the JSONL record uuid), NOT `message.id` (`msg_…`) — passing the latter errors `No message found with message.uuid of …`. So the normalizer now threads the raw `uuid` onto every message (next to `srcLine`). The forked window's displayed history is truncated to match via `/api/session-messages?untilUuid=<uuid>` (returns the convo up to that message, last 50 of that range, `total=upto` so scroll-up still paginates). Related discovered-but-unused: `--rewind-files <user-message-id>` restores **file** state to a message point (the rewind/checkpoint feature — separate from conversation fork). Verified live end-to-end: fork-at + message → new id adopted (2a1e29c6…).
- Minimap/search accuracy (huge sessions, reconnect, gap slabs) — five related fixes + the 2.286.1 sixth, keep ALL of them: **(6, incident-diagnosed via the auto-shipped scroll tracer) INDEX-mode jumpToIndex needs the SAME landing machinery as the teleport path** — it kept a single-rAF scrollIntoView, so on tool-heavy windows the 180ms-debounced run-collapse pass + cv height resolution yanked the landed view to scrollTop 0 (+ a spurious extendBottom on collapsed heights) ~200ms after landing; now folds runs synchronously pre-measure + lands via _scrollElStable. CDP smoke: scripts/test-minimap-jump.mjs (9 asserts, distinct-target-proven). (1) index→element mapping must use `'.chat-msg:not(.chat-gap-msg)'` — gap-loaded elements are in the DOM but OUTSIDE the window index space, so a bare `.chat-msg` NodeList misaligns `relIdx` onto the wrong message (search expanded/scrolled the wrong card once any gap slab was loaded). (2) `_reattach` catch-up must advance `_windowEnd` after appending missed messages (`_fetchMessages` silently updates `_total` via `_fetchMessagePage`, so the two diverge otherwise — post-reconnect minimap thumb/pos-indicator/jumps were off by the missed count); it also feeds missed user turns to the minimap and re-applies the search highlight. (3) The search highlight layer must be re-applied after EVERY window mutation: `_extendTop` AND `_extendBottom`/`jumpToIndex`/`jumpToBottom` (only the first did → highlights vanished scrolling down). (4) Full-extent (time-coordinate) minimap: live messages have ts beyond the init-time `lastTs` — `updateThumb` clamps against `max(lastTs, visibleBotTs)` (stale span pinned the thumb to top:100%, overflowing the track) and new live user turns call `appendFullTurn`. (5) Index-mode markers are `startIdx/total` — `setViewport` repositions them when total changes; edit re-renders must preserve `dataset.ts` (create path sets it, the edit path lost it).
- CodeMirror active-line highlight hid the selection on the first/last selected line: `highlightActiveLine` (from `basicSetup`) decorates the line under the selection HEAD even during a range selection, and that background paints on `.cm-line` ABOVE the selection layer (`z-index:-1`), so the opaque light-theme active-line (`#f1f5f9`) covered the selection rect on that one line. Fix: tag the editor `.cm-has-selection` when any range is non-empty + a theme rule `&.cm-has-selection .cm-activeLine{background:transparent}` (outranks light + oneDark `.cm-activeLine`). **Focus gotcha:** first attempt toggled the class directly on `view.dom` via a ViewPlugin — but CodeMirror rebuilds `view.dom.className` from scratch on every focus change (`updateAttrs` → `"cm-editor" + (cm-focused) + themeClasses`), wiping the class, so the highlight reappeared once the editor lost focus. Correct way: contribute the class through the **`EditorView.editorAttributes` facet** (a function value) — `attrsFromFacet` re-evaluates it on every update (incl. selection + focus, `updateAttrs` runs at the end of each view update) and `combineAttrs` CONCATENATES its class into the className CM builds, so it survives blur. Both editors share `activeLineSelectionPatch` (exported from code-editor.js). Lesson: never mutate `view.dom.classList` on a CodeMirror editor — it's clobbered on focus/theme changes; use the editorAttributes facet.

- **r6 integration (2.369.74, the r6 verifier's last finding):** `setupUsage()` runs at module top level and loads `data/usage-cache.json` into `_rateLimitCache` BEFORE `runLocalMigrations()` (inside `server.listen`'s callback), so a repair that unlinks the poisoned sibling changes nothing for that process — `usage-routes` now exports `reloadRateLimitCache()` (re-read + re-merge, idempotent) and server.js calls it right after the runner; test-readings-attribution §16c(h) drives the production order through the real route with the no-reload arm as the negative control. The migration ships under the id `2026-09-refile-readings-by-window-v2` because 2.369.73 marked the un-suffixed id applied as a DEFERRAL.

- **2026-09-08 (2.369.75 gate): `listOpenRolloutPathsViaLsof` budget 4 s → 20 s and a timeout is UNKNOWN, never `[]`** — lsof enumerates every process's fds before `+D` filters, and on a 3,900-process box under a full fast tier that passed 4 s; the timeout came back as "no codex thread holds a rollout" (the B-3185 always-failing-degrade shape). The array now carries a non-enumerable `unknown` reason and the degrade is logged (rate-limited); test-discovery-interpret's lsof legs SKIP with that evidence instead of failing parity.
