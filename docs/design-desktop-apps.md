# Design: generic desktop-app windows (X11 rendered in the browser)

> Owner 2026-09-13: "while at it, support generic desktop apps (browser-based X11 rendering and the like); keep the code structure maintainable, split components sensibly."
> This is the IMPLEMENTATION SPEC for docs/design-agent-browser-v2.md §4.7 (native client windows) and §4.9 (window targets), generalised from "one native window" to "any local desktop application". It replaces the original P8 scope; P9/P10 (agents operating windows) build on it. Chinese twin: docs/design-desktop-apps.zh.md.

## 0. In one paragraph

Any local desktop application (`exec` + args + cwd) can be opened in VibeSpace as a **window type `desktop-app`**: the server gives it its **own** X display and picture server, the browser renders it through one shared **picture-view component** and forwards input. The picture server is chosen by a **capability ladder**: `xpra` (per-window seamless, adaptive encoding, used when installed) → `vnc-display` (one Xvnc per app, or Xvfb + x11vnc, whole display) → the existing singleton whole desktop (D10's fallback, never re-invented). Each rung is **one capability row**, not an if-chain; an app's record stores only which rung it chose and why. Everything goes through **one keeper** (count cap + runaway guard + boot adoption, the browser keeper's discipline) and **one ws bridge** (`/api/desktop/:id/stream`, cookie-authed, server-side, raw ports never exposed).

## 1. What exists today (read-only measurement, 2026-09-13)

- This box: Ubuntu 25.10, Wayland session (`DISPLAY=:1` is Xwayland); `Xvfb`, `x11vnc`, `websockify`, `xdotool`, `wmctrl`, `xdpyinfo` present; `xpra` NOT installed but apt offers 6.5.3; `Xtigervnc`/`Xvnc` absent. Two Xvfb instances already running (agent-browser's and a 1600x1000 one).
- Fleet image (deploy/docker/Dockerfile, Debian bookworm): `tigervnc-standalone-server` + XFCE present; bookworm's repo has `xpra 3.1.3`.
- Already in the product: `src/vnc.js` (the singleton whole desktop: Xvnc `:7` + XFCE, `POST /api/vnc/start`, detached + adopted on boot), `src/lib/desktop-window.js` (noVNC view, singleton window, DPI counter-zoom `zoom: calc(1 / var(--ui-scale))`, resizeSession/scaleViewport), the `/api/vnc` upgrade bridge in `server.js`. These three are the **precedents** this design REUSES rather than copies.

## 2. Module split (every maintainability requirement lives here)

| Tier | Module | Owns | Never does |
|---|---|---|---|
| PURE | `src/desktop-apps.js` | App registry rows `{id, label, exec, args, cwd, env, category, backendPrefs}`; the **display-backend capability table** `DISPLAY_BACKENDS` (`xpra` / `vnc-display` / `desktop-singleton`, each row: `perWindow`, `adaptive`, `needs:[bins]`, `stream:'rfb'|'xpra'`); the `resolveBackend(hostFacts, prefs)` ladder (every fallback with its reason); the app-session state machine `launching → ready → exited|failed`; the cap + runaway numbers (the SAME constants as browser §3.5) | imports nothing; touches no filesystem |
| SHARED | `src/desktop-display.js` | **Machine facts**: which binaries exist, X display allocation (`-displayfd`), X auth cookies, window enumeration on a display (`xdotool`/`wmctrl`, reused by P9), `xpra` version probe; `hostId` is a parameter (v1 local only; the daemon bundles it later) | makes no decision; writes no record |
| ORCH | `src/server/desktop-app-keeper.js` | Lifecycle: spawn (setsid, detached) X display → app → picture server; `data/desktop-apps.json` atomic write + `desktop-apps-updated` broadcast; boot **adoption** (pid + starttime + port); count cap, runaway guard (>150 % CPU for 5 min or an RSS cap ⇒ stop + park + notice, the opencode-serve guard's shape); per-app idle timeout; stop/kill; exit ⇒ broadcast | knows no specific app; touches no ws |
| ORCH | `src/server/desktop-stream.js` | **One** ws bridge: `GET /api/desktop/:id/stream` (cookie auth, backpressure, closes on disconnect; `rfb` proxied to the 127.0.0.1 port, `xpra` proxied to xpra's ws); the `/api/vnc` bridge now CALLS it (the singleton desktop = one fixed id), retiring the twin | starts no process |
| ORCH | `src/routes/desktop-apps.js` | `GET /api/desktop/apps` (registry + live sessions + per-rung availability with reasons), `POST /api/desktop/apps` (`{appId}` or `{exec,args,cwd}`), `POST /api/desktop/apps/:id/stop`, `GET /api/desktop/apps/:id`; signatures carry `host` | no business decisions |
| CLIENT | `src/lib/vnc-view.js` | The **shared picture-view component**: the noVNC loader (`loadRFB`) extracted from `desktop-window.js`, DPI counter-zoom, resize/scale policy, focus + input forwarding, reconnect + status chip; used by `desktop-window.js` AND the new window | knows no window type |
| CLIENT | `src/lib/desktop-app-window.js` | Window type `desktop-app` (`registerWindowType`, `openSpec {openDesktopApp:{id}}`, one window per app session, tab-group capable, layout-restorable, several clients may watch one display); title = the escaped app label; status bar: backend rung + reason, CPU/RSS, Stop | never fetches process facts directly (reads the broadcast) |
| CLIENT | `src/lib/desktop-app-launcher.js` | ⚙ menu "Desktop apps…" + a toolbar item (registered through contributions.js); the launch dialog is **catalog-first** (2026-09-14, owner item A): an intro line in plain words (what this does, what to click; zh+ja) → the backend chip in the ladder's words → Running (Open/Stop + the slot count, shown above the catalog only when non-empty) → the **Applications catalog** = a grid of cards (label + one-line exec/reason + an icons.js SVG picked by the registry `category`), one click launches, a launching card spins and stays disabled until the record answers, an app not on PATH is **dimmed with its reason, never hidden**, an empty catalog says so in plain words and opens the command form by itself → "Advanced: run any command" under a disclosure (exec, args, cwd with autocomplete + Launch + Recent; collapsed by default, its open state persisted in user state `desktopAppAdvancedOpen`, merge-only PATCH); cards and the disclosure are buttons (aria-expanded); ≤768 px single column; a failure always reaches a toast. **The width lives on `.dialog.desktop-launch`, never on the body** (measured 2026-09-14: a body min-width overflowed the fixed 440 px `.dialog`, and an overflow:hidden box is still a scroll container, so focusing the Command input scrolled the dialog itself by 308 px — title off-left, ✕ mid-header, the Applications column a 64 px sliver); every programmatic focus is `{preventScroll:true}`, the columns are `minmax(0,1fr)` | holds no session state |

Rule: **a new display backend = one row in `DISPLAY_BACKENDS` + its probe in `desktop-display.js` + its proxy in `desktop-stream.js`** and no other file. `server.js` gets wiring only (size ratchet 2100 lines — measure first, extract an existing stanza if over).

## 3. The backend ladder (each rung is one capability row)

| Rung | Composition | Per-window | Adaptive on bad links | Needs | Verdict |
|---|---|---|---|---|---|
| `xpra` | `xpra start :N --start=<exec> --html=on --bind-tcp=127.0.0.1:<port>` (seamless), the HTML5 client library or the hosted upstream client (D21 (c)) | yes | yes | `xpra` | first choice when installed; `apt install xpra` here, one line in the fleet image |
| `vnc-display` | one `Xvnc :N -localhost -rfbport <port>` per app (the image has it) or `Xvfb :N` + `x11vnc -display :N -localhost -rfbport <port>` (this box today) + a light WM (`xfwm4`/`openbox` if present, bare otherwise) | no (whole display, but the display holds only this app) | no | `Xvnc`, or `Xvfb`+`x11vnc` | the rung both machines can run today; P8-1 lands here first; **x4 (2026-09-22): whether the display can follow the VibeSpace window is this row's `fit` column by `via` — `Xvnc` **follows** (TigerVNC accepts the client's SetDesktopSize; the argv spells `-AcceptSetDesktopSize`; measured here: 1280x800→900x600 answered status 0 in 43 ms, the framebuffer the new size at 50 ms; `=0` is the one lever that refuses), `Xvfb+x11vnc` is **fixed** (x11vnc 0.9.17 has no SetDesktopSize; the browser scales; the window chip names it: `fixed 1280x800 (Xvfb) — install tigervnc for a window that follows`); under BOTH spellings the keeper does the **app-fit** (xdotool moves the app's top-level to 0,0 and resizes it to the framebuffer, no WM needed; a second top-level keeps its size and is nudged inside) |
| `desktop-singleton` | the existing `src/vnc.js` `:7` whole desktop | no | no | as above | D10's fallback: the app starts inside the shared desktop and the window is just Desktop |

`resolveBackend` takes the first rung whose `needs` are all present; every fallback logs `[desktop] backend fallback: xpra→vnc-display (xpra not on PATH)` and writes `backend` + `fallbackWhy` into the record, which the window's status bar shows — the user must know whether they are looking at a window or a whole display.

## 4. Data model (`data/desktop-apps.json`, atomic writes)

```
{ "apps": { "<id>": { "id", "label", "exec", "args", "cwd", "env"?, "source": "registry"|"adhoc",
            "backend": "xpra"|"vnc-display"|"desktop-singleton", "fallbackWhy": null|string,
            "display": ":N", "port": <127.0.0.1 port>, "pids": { "x": n, "app": n, "server": n },
            "startedAt", "state": "launching"|"ready"|"exited"|"failed", "exitCode"?, "lastError"?,
            "idleTimeoutMs", "lastInputAt" } } }
```
The record stores **facts** only (pids / port / display / chosen rung), never derived values. Boot adoption: alive only when pid AND starttime match; otherwise marked `exited` with `lastError` kept.

## 5. Security

- Picture ports bind 127.0.0.1 only; the browser only ever reaches the cookie-authed ws bridge (the `/api/vnc` discipline).
- `exec` comes from the registry or from the user's dialog — **never from an agent**; the agent side (P9) receives window-target handles, not exec.
- Apps inherit the sanitized `agentEnv()`-style env (not `process.env`); DISPLAY/XAUTHORITY are injected by the keeper; secrets never in argv.
- Per-app idle timeout (default 30 min without input ⇒ stop, countdown visible in the status bar; "keep running" is an explicit action).

## 6. Test gates

| Suite | Tier | Content |
|---|---|---|
| `test-desktop-apps` | fast | PURE: registry-row validation, the full `resolveBackend` matrix (one row per missing-binary combination + the fallback text), the state machine, cap/runaway decisions |
| `test-desktop-display` | fast (SKIP with evidence when the binaries are absent) | display numbers never collide, auth cookies, window enumeration (a real Xvfb + one of `xterm`/`xlogo`/`xmessage`) |
| `test-desktop-app-keeper` | heavy | real Xvfb + x11vnc (or Xvnc): launch → port listening → RFB handshake (`net`) → record on disk → **SIGKILL the keeper process and rebuild ⇒ adoption** → clean stop (no orphan X / server; `/proc` counts equal before and after); the runaway guard with a CPU-burning fake app |
| `test-desktop-app-window` | heavy | headless chrome: launch dialog → window appears → canvas not all-black → a second client watches the same display → SIGKILL the server + reboot ⇒ the window restores and reconnects → Stop ⇒ the window shows exited; **geometry pins since 2026-09-14** (rects the page computes): a fresh open at 1000×800 / 777×800 / 480×640 — `.dialog` never scrolls sideways (scrollLeft 0, scrollWidth ≤ clientWidth), the title is hit-testable at its centre, ✕ sits inside the dialog's right 48 px, the first card label lies inside its card, both Advanced columns keep ≥ 38 % when two are shown, three nowrap Recent entries squeeze nothing; the intro rendered in zh; a real card click launches with its launching state; the disclosure closed by default and persisted open across a reload |
| `test-vnc-view` | fast | the shared view component's DPI counter-zoom and coordinates (the inc-mtdrm922 measurement); `desktop-window.js` passes the byte-identical assert set after switching to the component |

## 7. Slices

| Slice | Content | Rounds |
|---|---|---|
| **P8-1** | every module in §2 (the `xpra` rung probe + record only, proxy seam left), the `vnc-display` rung end to end, `vnc-view.js` extracted and `desktop-window.js` moved onto it, the `/api/vnc` bridge folded into `desktop-stream.js`, the launcher, the window, the five suites, kb/CLAUDE.md, the Dockerfile gains `xpra` (package only, not enabled) | 6–9 |
| **P8-2** | the `xpra` rung end to end: D21 (c) — host the upstream HTML5 client first as the verification slice (the 200 ms / 1 Mbps measurement written into the kb), then draw with `xpra-html5-client` inside `desktop-app-window`; per-window titles/icons. **Chunk x1 landed (2026-09-21): the server half + the validation slice** — the `xpra-seamless` recipe (one `xpra start --daemon=no --displayfd=3 --use-display=no --html=on --bind-tcp=127.0.0.1:<port> --bind=none --resize-display=yes --clipboard-direction=both` per session, everything else off by name; xpra starts its own Xvfb and is the WM, the app is still the keeper's own child; the four measured traps and every flag's reason are in kb-file-structure.md under desktop-display.js), the keeper record `{backend:'xpra', stream:'xpra', probe:'http'}` sharing every limit / guard / idle / adoption (DA1: an old session is never migrated) + `windows(id)`, the bridge's `xpra` relay (ws↔ws, same backpressure / keepalive / named close), the hosted upstream client at `/api/desktop/:id/xpra-ui/*` (cookie auth, nosniff, no listing), the instance order `desktop.backendPrefs`; the window showed the hosted client in an iframe until x2. **Chunk x2 landed (2026-09-22): the client half, D21 (c) (b)** — the npm `xpra-html5-client` was vetted and refused (its README: MPL-2.0, derived from the upstream client, vs Apache-2.0 in package.json; frozen 2022-05-25, xpra 4.x era; 10.7 MB unpacked / 2.37 MB ESM with node-forge + lodash-es; a whole client with its own window model — the window would not be ours); the transport is the upstream html5 v21 `Protocol.js` WORKER served unmodified from the installed package through the x1 route (`/api/desktop/:id/xpra-ui/js/`, MPL-2.0, docs/third-party-notices.md), and everything above the packets is ours: `src/lib/xpra-proto.js` (PURE words), `xpra-client.js` (the DOM-free session), `xpra-view.js` (the pane) on the picture shell extracted from vnc-view.js (`picture-shell.js`). The app window fills the pane and follows it, dialogs are nudged inside, the title bar carries the app's own title + icon, the clipboard works both ways (the plain-http chip measured on a hostname page), IME/Latin-1 text types through a NATIVE keymap (§7.2). Gates: test-xpra-client (fast, 114) + test-desktop-xpra-window (heavy, real xpra + xterm). **Chunk x3 landed (2026-09-22): the rung CLOSED** — a new heavy gate test-desktop-xpra (a real worktree server with password auth + the real xpra 6.5.3 + its own Xvfb + xterm, driven from node through the product's routes and the ONE ws bridge, no chrome, ~50 s): bring-up, the binding check (xpra bound to 127.0.0.1 only, a raw TCP connect to this box's non-loopback address refused), relay auth (no cookie 401 / foreign id 404 / a real rencodeplus hello), resize-follows and the clipboard both ways at the protocol level, keep-alive vs idle stop, adoption across a SIGKILL + reboot (xpra AND vnc-display records adopted as born, never migrated), the cap = keeper-limits' number (§7.3); test-desktop-apps §1 gained the guard-numbers imported-not-copied census; the fleet image's xpra 3.1.3 is recorded as OPEN (§9-1 + the Dockerfile comment). **Chunk x4 landed (2026-09-22; the owner saw a vnc-display session as a small Calculator in the top-left of a 1280x800 black root: "even VNC can't be like this — no auto-fit, no size matching?"): the vnc-display rung FITS the window too** — (a) the Xvnc server side follows the client's SetDesktopSize (`X_SERVER_ARGS.Xvnc` spells `-AcceptSetDesktopSize`; measured in §7.4; `-extension RANDR` only blinds xrandr on the X side, the client resize still lands), (b) the keeper's **APP-FIT step** (PURE `appFitPlan` + `desktop-display.applyWindowPlan`, ONE chained xdotool: after ready every 500 ms until the app maps a top-level, after every SetDesktopSize the bridge reports (debounced 250 ms), and every tick as the belt; the framebuffer size is READ from xdpyinfo — the truth — never the client's ask), (c) Xvfb+x11vnc keeps its fixed geometry, is still fitted, the browser scales, the chip names the limit, (d) the heavy gate test-desktop-vnc-fit (real Xvnc + real xterm + headless Chrome: the VibeSpace window resized twice, the framebuffer following within 2 s, the app rect = the framebuffer, zero pixels outside the app on the canvas); the fleet image gains `xdotool wmctrl` (§9-8: with a WM the fit asks for a maximise through wmctrl — no WM here, **not measured**). Gates: test-desktop-apps §9 (206) + test-desktop-display §6 (144, the measurement as a gate: Xvnc following + the `=0` control refusing) + test-desktop-app-keeper §13 (217) + test-desktop-vnc-fit (heavy). **x5 SHIPPED (2026-09-22, the owner's ruling): several clients = ONE active viewer** — among the human viewers of an app window exactly one is ACTIVE (its pane drives the app's geometry, only its input / clipboard is relayed), every other one is BLOCKED (an overlay: the app's title, "Active on another client", "Resume here" — no picture); while an agent drives, everybody is Watch (the picture scaled to fit the pane, no geometry sent). Rule, table and measurements in §7.5; §9-11, the r8 leftover, closes with it. | 4–7 |
| **P9** | see browser design §4.9: `src/window-targets.js` reuses `desktop-display.js`'s enumeration, the `vibespace-window` CLI | as scheduled |

### 7.1 The D21 (c) (a) validation slice, measured (2026-09-21, this box: xpra v6.5.3 + xpra-html5 21, a worktree server + headless Chrome, a relay-level netem)

| Item | netem off (loopback) | rtt 200 ms / 1 Mbps |
|---|---|---|
| Bring-up: displayfd / HTTP 200 / first window mapped | 175 ms–1.2 s / 1.3–2.3 s / ~2.5 s | same (server side) |
| First window canvas in the hosted client (after navigate) | 0.7 s | 0.9 s |
| The picture | ONE seamless window = the app's own canvas (370x616 calculator / 484x316 xterm), black share 0.00, no root | same |
| Keystroke → glyph on the canvas (xterm; a no-key control stayed still) | 34 ms | 265 ms (≈ RTT + 65 ms) |
| Viewport 1000x700 → 640x480 | the virtual screen follows (xdpyinfo 640x480); the app window keeps its own size (seamless — fitting it to the pane is x2's `configure-window`) | same |
| Clipboard | loopback is a secure context: the client has the async Clipboard API + polling; on a plain-http hostname the API is absent and the upstream client falls back to its in-page pasteboard (x2 measures that path with its own chip) | same |
| Session RSS with a client connected | xterm 203 MB / calculator 460 MB (xpra 86 + Xvfb 98 @ 4096x2304 + the app; the GTK4 damage stream grows xpra) | 202 / 463 MB |
| CPU with a client connected (5 s) | xterm 0–0.2 % / calculator 10–16 % (its own animation) | same |
| One `xpra info` | 115–135 ms + a full connection (a `windows.<xid>.*` family) ⇒ not for a poll; the live title/icon ride the protocol | — |


### 7.2 Chunk x2, measured (2026-09-22, this box: xpra v6.5.3 + xpra-html5 21, a worktree server + headless Chrome, a real xterm running `cat > file`)

| Item | Measured |
|---|---|
| First window drawn after the window opens | 0.76 s (0.75–0.76 over three runs) |
| The app window follows the pane (xterm, cell-snapped) | 898×550 of an 898×553 pane; 634×407 of 638×413 — at 0,0, the X server's own geometry within one cell of the pane, our canvas within 3 px of X |
| Black pixels outside the app window (a screenshot of the pane, decoded in the page) | 0 of 5,456 (638×413) and 0 of 0 (898×553 — the window covers the pane) |
| The title bar | the app window's own title, equal to what the X server reports (`GET …/windows`) |
| Typed text | `hi xpra 42` through trusted key events, read back from the app's file |
| IME / non-layout text | `é中文` through CDP insertText and `é` as a key event — ONLY through the native keymap: xpra 6.5.3 translates a keycodes-only client onto its `us` keymap (`set_keycode_translation`) and never invents a keysym; with `x11_keycodes` + `query_struct` it programs the X keymap from the client's map (`set_all_keycodes`), so the client publishes a row per new character (`keyboard-config`) before pressing it |
| Clipboard browser → app | a `paste` event's text is what `xclip -o -selection clipboard` reads on the app's display |
| Clipboard app → browser, secure page (loopback) | `xclip -i` on the display lands in `navigator.clipboard` |
| Clipboard app → browser, plain http (a hostname page, `isSecureContext === false`) | the "Copied in the app — click to copy" chip; a trusted click copies (read back from the secure page) |
| Which viewer gets the app's copies | ONE — the first clipboard-enabled client, then whoever last sent input (xpra's ui driver); a second viewer must touch the app first |
| A second top-level (xterm's Ctrl+Button1 menu, override-redirect) | drawn at the pointer inside the pane; its 446 px hang 149 px below a 553 px root because Xt positions against the screen size cached at connect — X's own clipping, not ours |
| Clipboard browser → app, plain http with REAL pastes (CDP's native paste command — the browser's own clipboard, no async API) | Ctrl+V on the pane and the Paste chip's PASTE BOX + Send (the shell's, on EVERY rung since the follow-through) both read back by `xclip -o`; the box names HTTPS as its reason and hands the focus back to the app |
| Under the UI scale (`vibespace.uiScale` 125, body zoom 1.25), a THIRD size (560×460 layout px) | pane 698×500 on screen = the session's screen; canvas 694×498 px drawn on 694×498 screen px (net zoom 1); X's window 694×498; 0 black px of 3,388 outside it; pointer moves at pane (137, 91) and (558, 350) land on exactly those X coordinates (`xdotool getmouselocation`) |
| Stop | "Stopped", every recorded pid gone |

### 7.3 Chunk x3, measured (2026-09-22, this box: xpra v6.5.3, a real worktree server with password auth on + a real xterm, driven from node through the routes and the bridge; one test-desktop-xpra run = 48 s)

| Item | Measured |
|---|---|
| `POST /api/desktop/apps` to ready | 1.53 s (display :4; ONE xpra pid is both x and server, the app its own pid, no WM of ours, probe http) |
| xpra's session files | `data/desktop-apps/<id>/xpra/` (`4`, `server.pid`); `--bind=none` ⇒ no unix socket anywhere; `~/.xpra` and `~/.Xauthority` absent |
| The binding check | the xpra port has ONE `/proc/net/tcp` LISTEN row, `0100007F` (no tcp6 row); a raw TCP connect to this box's non-loopback address is ECONNREFUSED while 127.0.0.1 answers; control: the product's own port is bound `00000000` and the non-loopback connect answers |
| Relay auth (the real server) | no cookie ⇒ 401 at the upgrade; cookie + a foreign id ⇒ 404; cookie + the id ⇒ 101 |
| A real hello through the relay (the installed client's rencode.js loaded into the suite's realm) | answered in 512–515 ms: xpra 6.5, 98 packet-types (incl. display-configure, keyboard-config, configure-window, clipboard-token) |
| new-window | 484×316 at 0,0, title "vs-x3-title", class ["xterm","XTerm"] |
| Resize-follows (display-configure + configure-window) | 640×480 ⇒ X's own geometry 640×472 in 412 ms and ONE `window-move-resize 640×472` back (xterm's cell snap — X corrected the request); 900×620 ⇒ X 898×615 in 1.03 s and NO confirmation — the rule read from `x11/server/seamless.py`: `do_process_window_configure` notifies every OTHER client, `size_notify_clients` fires only when X's final geometry differs from the server's own clamp (client-geometry); hence the x2 client applies its fit optimistically and takes corrections |
| Clipboard browser → app (protocol level) | after a `clipboard-token` through the relay, `xclip -o -selection clipboard` on the display reads the same text |
| Clipboard app → browser (protocol level) | `xclip -i` on the display ⇒ a greedy `clipboard-token` on the relay carrying the text; the token counts as INPUT for the idle clock (lastInputAt moved) |
| Idle stop / keep-alive | `PATCH /api/settings {desktop.idleTimeoutMin: 0.05}` applies to NEW launches at once: the silent session exits 5.0–6.0 s after ready (3 s timeout + the 5 s tick), stoppedBy idle, xpra + Xvfb + app gone; the kept-alive one beside it (idleTimeoutMs 0) lives; the first session keeps the timeout stamped at launch |
| Adoption across SIGKILL + reboot | three live sessions (two xpra + one vnc-display pinned through `desktop.backendPrefs`) all outlive the server; 3/3 adopted 1.8–2.0 s after the reboot with the backend each was born with, the same pid + starttime, the same port; the vnc-display one never migrated; the ladder still picks xpra; a launch after the reboot takes xpra; the relay to an adopted xpra session answers a hello |
| The cap | `GET /api/desktop/apps` `cap.cap` === keeper-limits.CONCURRENT_CAP (6) |
| Stop | four sessions stopped through the product ⇒ exited by user; all 15 recorded pids gone; the marker census EMPTY (no process carries this run's `VIBESPACE_DESKTOP_APP=<id>` or XAUTHORITY path) |

### 7.4 Chunk x4, measured (2026-09-22, this box: Xtigervnc 1.15.0 + xdotool 3.2016 + a real xterm / gnome-calculator; the keeper's argv verbatim; a worktree server + headless Chrome)

| Item | Value |
|---|---|
| A client's SetDesktopSize 1280x800→900x600 (the keeper's argv) | an ExtendedDesktopSize rect with status 0 in **43 ms**, xdpyinfo/xrandr reading 900x600 at 50 ms; growing to 1600x1000 (past `-geometry`) and back to 1280x800 both succeed |
| Control `-AcceptSetDesktopSize=0` | status 1, the framebuffer stays 1280x800 ⇒ that flag is the one lever |
| Control `-extension RANDR` | xrandr says "RandR extension missing", yet the client's SetDesktopSize still moves the framebuffer to 900x600 (reading the size on the X side needs no RANDR) |
| `xdotool windowmove 0 0 windowsize 1280 800` on bare X (no WM) | xterm 484x316 → 1280x800+0+0 in **3 ms**; gnome-calculator (GTK3) 370x616 → 1280x800+0+0 in **3 ms** (the server applies the client's ConfigureWindow directly) |
| The real keeper (§13): ready → first fit | 101 ms (xterm maps at once; a GTK app takes 1–2 s and is awaited in 500 ms steps) |
| A real RFB client through the real bridge: SetDesktopSize 900x600 / 1024x700 | framebuffer AND the app's window follow within **305 / 306 ms** (250 ms debounce + ~50 ms server + a 2 ms fit); the idle clock untouched |
| The belt: the window hand-moved to 300x200+40+40 | back at 1024x700+0+0 on the next tick (782 ms at a 1 s tick) |
| Headless Chrome (test-desktop-vnc-fit): after connect | window 900x620 → pane 898x553 = framebuffer = canvas = app window 898x553+0+0; 0 pixels outside the app; black share of the canvas 1.7 % (an xterm on white); settled 336–340 ms after Connected |
| Resized to 900x640 / 1180x760 | the framebuffer changed within 2 s; pane 898x573 / 1178x693 = framebuffer = canvas = app window; 0 pixels outside; settled 22–24 ms after the change was seen (fit 4–5 ms, why `client asked …`) |
| X places a second top-level at +2000+1500 | the belt nudges it inside: 244x134 → +934+559 (inside 1178x693), size kept, the main window untouched |
| The Xvfb+x11vnc spelling (Xvnc removed from PATH) | fitMode fixed, the chip reads exactly `fixed 1280x800 (Xvfb) — install tigervnc for a window that follows`, the app fitted to 1280x800, the 1280x800 canvas scaled by noVNC, a VibeSpace window resize changes nothing on the display |
| No xdotool | refused by name once (`xdotool not on PATH`), the framebuffer still measured, the record still ready, asked again after a minute |
| Under UI scale 125 (body zoom 1.25) at a third size: window 640x500 layout px | the framebuffer became the ON-SCREEN pane 798x550 within 314 ms (not the layout px), canvas 798x550 drawn on 798x550 screen px (net zoom 1), the app window 798x550+0+0 with 0 pixels outside it; a pointer at canvas (137,91) / (638,385) lands on exactly (137,91) / (638,385) in X (xdotool getmouselocation) |
| The title bar (x4 close-out) | shows the APP WINDOW's own title (the name the keeper's fit enumeration already read — the `appTitle` fact, zero extra spawns, refreshed every tick): `vs-fit "q" <b>x</b>` verbatim as text, zero elements in the title bar; the launch label when no name is readable |
| How xwininfo 1.1.6 prints a name (measured) | UNESCAPED: a UTF-8 `_NET_WM_NAME` with its quotes verbatim `"计算器 "x" \ é"`; xterm's non-Latin-1 title is COMPOUND_TEXT and prints as `(name in unsupported encoding COMPOUND_TEXT)`; a UTF-8 name under the C locale prints as `" (failure in conversion from UTF8_STRING to ANSI_X3.4-1968)"`. The old parser regex DROPPED the first two lines whole — the app's window vanished from the fit plan and from windows(id); the line is now parsed from the right (geometry, class group, name), an unreadable name is null with the row kept, and xwininfo runs under `LC_ALL=C.UTF-8` |

### 7.5 x5 — several clients = ONE active viewer (2026-09-22, the owner's ruling; this box: xpra v6.5.3 + a real xterm, a worktree server + two headless Chrome pages)

The owner, verbatim: "直接block掉非active客户端的app界面，因为多客户端同时操作鼠标感觉也会有问题" (block the app pane of every non-active client outright — several clients moving one mouse is trouble anyway) + "仿照terminal…可以手动take over" (like the terminal — a manual take over). The terminal's size-override (server.js `resizeSessionToMin` + `_sizeOwnerWs`, terminal.js's "Resume here" overlay) made the default: exactly ONE of a window's HUMAN viewers is active; Resume here takes over (the taker becomes active, the previous one blocked); when the active one leaves, the remaining viewer that was active MOST RECENTLY takes over by itself (never active ⇒ the most recently joined) — since 2.369.156 after a **5 s grace** (end of this section). With the agent lease (P9b): an agent driving (lease input `agent`) makes every human Watch; a human takeover of the agent's window (lease input `user`) makes that client the active one and the others blocked.

| State | Picture | Input / clipboard | Geometry (display size, configure-window) |
|---|---|---|---|
| active | yes | relayed | relayed — the app follows ITS pane |
| blocked | **none** (the overlay; the bridge opens NO upstream for it) | nothing relayed | nothing relayed (its hello and the held kinds are kept and go first when it becomes active) |
| watch | yes (the whole stage scaled to fit the pane: contain, never cropped; since 2.369.156 **never upscaled** — capped at 1, centred) | cut | cut (keymap / display size / **configure-window** held and replayed at a takeover; unmap-window cut) |

Where it lives: PURE `src/desktop-viewers.js` (`viewerState` / `nextActive` / `activeAfterJoin` / `viewersView` / the client's `paneState`; imports nothing, bundled into the browser too); the keeper's election (`viewerJoined/Left/takeoverViewer/activeViewer/onViewers` — viewer ids only, no WebSocket, never persisted) + the `desktop-app-viewers {id, active, viewers:[{id, label, since}]}` broadcast (panes by their PUBLIC key, NEVER a socket's secret id); the bridge's `viewerSeats` (window-live-wiring composes the keeper's election with the engine's lease through `viewerState`; every keeper change and every `window-leases-updated` calls `refresh`) — a blocked socket opens no upstream and keeps only its hello + the held kinds; active/watch → blocked CUTS it (the upstream closed, the socket closed 4001 `blocked`, the pane reconnects at once as a blocked viewer); blocked → active opens the upstream and replays what it held first; the rfb rung takes the same policy (blocked = no TCP); `POST /api/desktop/apps/:id/viewers/takeover` (Resume here; with an agent holding the window it is the engine's human takeover, and a hold by another human MOVES with `holderAlive:false` — never handed back, nothing announced, nothing billed) + `GET …/viewers`; the client's overlay writes the title as textContent and Resume here flips the pane LOCALLY before the request (never waiting for the broadcast echo).

| Item (test-desktop-xpra-window §5) | Measured |
|---|---|
| A first ⇒ active | A's pane 698×433 ⇒ X 694×433 (xterm's cell snap); A types into the app |
| B arrives (another size) | the overlay "Active on another client" + the app's name + Resume here; B's view holds 0 windows, 0 canvases |
| B resizes to 818×493 and types while blocked | 2.5 s later X is still 694×433 (A's pane); "zz" never reached the app |
| B clicks Resume here | **648 ms** later X = 814×485 (B's pane 818×493); A shows the overlay with no picture; B types into the app |
| A closes / B closes, A reopens | B stays active and connected / A is active and connected again |
| an agent attaches (a real fake-claude session's vsst_ token) | both pages Watch: D's pane 518×313, X 694×433 ⇒ stage scale 0.723 (= the contain fit), the canvas 502×313 wholly inside the pane; D's small pane and A resized to 900×620 change nothing in X; typing refused on both |
| CONTROL: the pre-x5 server (a worktree copy whose wiring hands the bridge no seats) | the second page is NOT blocked, and its resize to 818×493 turns X into 814×485 within 3 s — the "B's pane changes nothing" leg fails there |
| the bridge over fake upstreams (test-desktop-viewers) | blocked: 0 upstream connections, 0 packets either way; the takeover replays hello → keyboard-config → display-configure → configure-window in 11 ms; CONTROL (a patched copy whose seat is always `free`): the blocked B's display-configure / configure-window / key-action all reach the upstream |

**2.369.156 close-out** — the round-3 adversarial verify's (MERGEABLE) four LOWs closed, its two INFO items made the **product's defaults** (the owner can object):

- **The grace (default, `VIEWER_GRACE_MS` = 5 s):** when the active pane's socket closes while other viewers remain, the seat is HELD for its pane for 5 s — the others stay blocked and the broadcast's `active` still names that pane (`viewersView`'s `activePane`; `active: null` would make every client read itself active while the bridge still blocks it); the same pane reconnecting (a network blip), or a new window naming it as its predecessor (a page reload: the tab's sessionStorage remembers the previous pane key and the url carries `?prev=` — a hint only, it never takes a seat that is not being held), takes the seat back; Resume here inside it wins at once; only when it runs out is the most recently active viewer elected. A page reload or a network blip no longer hands the app to another device.
- **Watch never upscales (default):** the xpra rung's Watch stage scale is capped at 1 and centred (whole-pixel offsets) — a pane larger than the app shows it crisp at 1:1, a smaller one still scales down to fit. The vnc rung's picture stays noVNC's `scaleViewport` (unchanged).
- **LOW-1:** every join logged a false `reconcile failed — c.reconcile is not a function` warn (the join's own broadcast refreshed before the socket was wired) ⇒ `refresh()` skips a socket not wired yet (it applies its state itself right after).
- **LOW-2:** on the xpra rung the blocked overlay named the launch LABEL, not the app's own title (a blocked pane has no protocol session) ⇒ the keeper reads the main window's title from X through `windows(id)` (PURE `appMainWindow({seamless})`) into the record's `appTitle` when a blocked seat appears, at a takeover, and on the tick while one exists (≤ every 10 s); in the window a blocked pane names the app by the record's title first, an active / watching one by its protocol title first.
- **LOW-3:** an ANONYMOUS socket (no `?viewer=` — the hosted upstream page `/api/desktop/:id/xpra-ui/` is exactly that) was seated as `anon-N` and could take the ACTIVE seat ⇒ only named panes join the election; an anonymous socket on a governed stream is a read-only WATCH seat (the picture, input / geometry cut), never elected, never blocked, never broadcast. The hosted page therefore only watches (the upstream page strips `?`/`=`/`&` from its `path`, so it cannot carry a viewer id); the product's window is the driving surface.
- **LOW-4:** a silent drop of the active pane's socket blocked that pane's own reconnect for up to two ping rounds (29.8 s measured) ⇒ a join through a NEWER socket of the pane that holds the seat moves the seat to it at once (a pane is one client) and the old socket is cut; a cut socket leaves the election at once (not after its close handshake, up to 30 s).

| Item | Measured |
|---|---|
| LOW-4 through the bridge (test-desktop-viewers §5) | the old socket silently dropped, a new one joins ⇒ ACTIVE in **11 ms**, its hello + keys reach the upstream, the old upstream closed; CONTROL (a keeper copy without the move): still blocked, 1 upstream connection |
| the grace through the bridge | A closes ⇒ B has 0 upstream connections during the grace; A's pane reconnects ⇒ active, no upstream ever opened for B |
| LOW-3 | the anonymous socket's hello + map-window reach the upstream, its key-action / configure-window / display-configure cut; the first named pane is active; CONTROL (the old `anon-<n>` seating): the anonymous socket takes the active seat, its keys + geometry reach the upstream |
| LOW-1 | 0 warn lines over the scenario's 5 joins; CONTROL (the skip removed): 5 |
| LOW-2 (test-desktop-xpra-window §5, real xpra + xterm, the hostile title `vs-x5 <b>"t"</b> & <i>x</i>`) | B blocked: the overlay AND the title bar show that title verbatim (0 child elements); A's overlay after the cut the same; CONTROL (a keeper copy without the join's read): no title on the record, only the label |
| Watch never upscales (test-desktop-xpra-window §5) | A's pane 1078×633, larger than the app's 698×433 window ⇒ stage scale 1 (the uncapped fit: 1.462), the canvas 698×433 = the window's own size, gaps left/right 190/190, top/bottom 100/100 (a BACKGROUND tab runs no rendering steps, so the ResizeObserver's re-fit waits for the tab to be shown — the first read, taken in the background, was the un-centred 0/380) |

## 8. Decisions (owner-approved: as recommended)

| # | Decision | Recommendation |
|---|---|---|
| DA1 | Backend order | `xpra` > `vnc-display` > `desktop-singleton`; installing xpra upgrades NEW sessions, running ones are not migrated |
| DA2 | One display per app or a shared one | **One per app** (isolation, independent idle, independent stop); the shared desktop is only the fallback |
| DA3 | Idle default | stop after 30 min without input; setting `desktop.idleTimeoutMin`, 0 = never |
| DA4 | Who may launch | humans only (the dialog); agents operate already-open windows through P9 |
| DA5 | xpra client | D21 (c) as written |

## 9. Unverified

1. HTML5-protocol differences between xpra 6.5 (this box) and 3.1 (the bookworm image) — x1 measured 6.5.3 only (on 6.5.3 `--mmap=no` makes every connection refused, `--commands=no` disables `--start*` too, a MISSING XAUTHORITY file makes it write the real ~/.Xauthority, setproctitle blanks its own environ — whether 3.1 does the same is unmeasured); the image side stays OPEN and is never measured against the fleet from here. **x3 record (2026-09-22): deploy/docker/Dockerfile's comment names 3.1.3 vs 6.5.3 and points back here; every rule x1–x3 measured on 6.5.3 (the packet names `display-configure` / `keyboard-config` and their legacy fallbacks, the native `x11_keycodes` path, the greedy clipboard token, `size_notify_clients`' correction rule, the four traps) is an unknown on 3.1.**
2. Wayland-only apps (no Xwayland fallback) will not start under Xvfb — registry rows carry a `needsWayland` flag and refuse honestly.
3. The `vnc-display` rung at 200 ms / 1 Mbps is inferred from §4.7's ranking, not measured.
4. (x2) The fleet image's xpra 3.1.3 + its bundled html5 client: our window loads that machine's own `Protocol.js` (matched to its server by the package manager), but the hello and packet forms are 6.5.3's (`display-configure` / `keyboard-config` fall back by the server's packet-types list; the native `x11_keycodes` path and the clipboard forms are UNMEASURED on 3.1) — OPEN, never measured against the fleet from here.
5. (x2) Images on the clipboard: text only (a `clipboard-token` carrying `image/png` is ignored both ways).
6. (x2) An override-redirect popup an app places beyond the pane is clipped like X clips it at the root (Xt caches the screen size at connect) — no client can move it; a larger pane is the only fix.
7. (x2) HiDPI: the pane's CSS px are the X px (net zoom 1 under the UI scale); a 2× display sees the app upscaled 1:2 rather than rendered at device resolution.
8. (x4; MEASURED r5, 2026-09-22) The fit path under a window manager (the fleet image has xfwm4): measured with the fleet image's OWN xfwm4 4.18, run from the image against a scratch X on this box (and through the real keeper, test-desktop-app-keeper §13 (g) via `VIBESPACE_TEST_WM_DIR`). The x4 plan was wrong there: a reparenting WM puts the client one level down inside a nameless frame and keeps a MAPPED 5x5 "Xfwm4" helper at -1000,-1000 as a classed depth-1 window, which the depth-1 rule picked as the main. Now the plan fits the CLIENT (`appWindows`), `wmctrl -i -r <client> -b add,maximized_*` makes the frame exactly the framebuffer (xterm 1280x776+0+24 under a 24 px title), the WM keeps it so through the app's own resizes and a root resize (1000x700 and back), and `appTitle` is the client's name. Still never tested against the fleet itself from here; bookworm's xfwm4 4.18 is the image's own binary.
9. (x4) Override-redirect popups (menus) and dialogs cannot be told apart on bare X (both are root's direct children): the fit step only nudges a window that OVERFLOWS (size untouched) — a menu normally lies inside the framebuffer and is unaffected; a menu open while overflowing would be moved, no side effect observed but not measured on purpose.
10. (x4 close-out) The window ICON on the vnc-display rung: the xpra rung's icon rides its protocol (x2); the vnc-display rung shows the title only (`appTitle`, from the enumeration that already runs). Reading `_NET_WM_ICON` needs one more xprop spawn per tick plus a server-side PNG encode (a spawn's fork tax grows with RSS) — not done, OPEN. A COMPOUND_TEXT title (an xterm with a non-Latin-1 title and no `_NET_WM_NAME`) is unreadable, so the window shows the label.
11. (r8, MEASURED 2026-09-22; **CLOSED by x5** — `configure-window` and `unmap-window` left the watch list, the former held and replayed at a takeover; the view decision by the owner's ruling: Watch scales to fit, every non-active human viewer is blocked outright, §7.5) A Watch viewer's `configure-window` used to resize the holder's APP WINDOW inside the shared display: the bridge fences a refused viewer's keymap (r7) and display-size packets (r8, held and replayed at its takeover), but `configure-window` stays a watch type — through the real bridge a watcher's 480x360 took the holder's xterm from 898x589 to 478x355, and it stayed after the watcher left (xpra's seamless.py `do_process_window_configure` applies any non-readonly client's geometry). Fencing it needs a VIEW decision first: the xpra window draws at net zoom 1, so a Watch pane smaller than the holder's window must either scale the picture or crop it (the shipped client fits optimistically and would otherwise paint a larger window into a smaller canvas). The rfb rung has no per-window geometry (noVNC scales the whole framebuffer).
