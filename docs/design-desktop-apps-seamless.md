# Design: desktop apps round 3 — seamless windows, clipboard, DPI derivation, remote hosts (a design for the owner)

> The owner's feedback after testing 2.369.156 + .158 (docs/design-desktop-apps.zh.md, P8-2), verbatim (Chinese) and answered point by point:
> "我简单测试了下app功能，为啥应用里复制之后要手动点击才能复制到本机？这个不能seamless吗？以及我关闭内部窗口之后外部窗口还要额外关闭一次，以及实际上如果内部窗口具有完整的窗口控制能力（关闭按钮啥的），外部窗口就不应该显示任何东西，来达到seamless的效果（当然有没有可能有什么feature必须要在外部窗口展示东西，所以无法seamless的？你也要考虑。以及内部app的dpi也应该是可调的，最好是能从vibespace自身的dpi自动推导，保证最佳的窗口渲染效果，当然还是有必要在界面配置里加入一个remote app的dpi调整的。以及你要思考下整个app功能对于remote host的适配程度，自动seamless setup能力等等。"
> — (1) why does a copy inside the app need a click to reach my clipboard, can that be seamless; (2) closing the inner window leaves the outer one to close again; (3) when the inner window has full controls (a close button etc.) the outer window should show nothing — and is there anything that MUST be shown outside, making seamless impossible; (4) the app's DPI should be adjustable, ideally derived from VibeSpace's own DPI, with a setting for remote apps too; (5) how well does the whole feature fit remote hosts, and "auto seamless setup".
>
> Design only (no code, no version bump). Every point gets a row in the IMPACT TABLE first, then ONE trimmed set to build, then the decisions only the owner can make (D1…D9, each with a recommended default). Every "measured" value is from 2026-09-23 on this box (xpra v6.5.3, gnome-calculator 50 / GTK 4.22, xterm, headless Chrome 153, a real worktree server); scripts and raw results in `/tmp/vs-design-measure/` (not committed). Chinese twin: docs/design-desktop-apps-seamless.zh.md.

## 0. In one paragraph

Four of the five are **small client-side changes one lane can close** (clipboard: replace "click to copy" with the 5-second gesture window the user's OWN Ctrl+C opens — measured to hold on plain http; close: the app exiting closes our window; seamless: detect the app's own header bar and hide our title bar, and dragging its header bar drags our window — xpra already sends us that drag as a packet we currently ignore; DPI: derive the app scale from VibeSpace's own effective scale `devicePixelRatio × UI scale`, per window, relaunch to apply). The fifth (remote hosts) is **one separate large lane** in the exact shape of browser-serve / browser-access (run the same code where the app lives, relay xpra's ws over the agentd data plane) and needs D5–D8 first.

## 1. The impact table

| # | What the owner saw | Why it is so today (file:line) | What is technically possible | The minimal change | Cost | Risk | Recommended? |
|---|---|---|---|---|---|---|---|
| 1 | A copy inside the app needs a chip click to reach the local clipboard | The page is `http://<hostname>`, `isSecureContext === false` (xpra-view.js:119), `navigator.clipboard` **does not exist** (measured, M1); `deliverCopy` (picture-shell.js:201–207) can only take the chip on a non-secure page, and the chip's click copies through `execCommand('copy')` (picture-shell.js:43–54 / 195–199). A browser rule: an insecure origin has no async clipboard API | **Measured M1**: `execCommand('copy')` succeeds when called **asynchronously within 5 s of a trusted key/pointer event** (0/50/150/300/1000/3000/4800 ms all succeed, 5200 ms on fails); our view's `preventDefault` on keydown does not matter (M1b); the app's clipboard token arrives 23–300 ms after the user's Ctrl+C (M3c's lost-window at 23 ms is the same link's order of magnitude; §7.2 says 50–300 ms) — **inside the window**. So: the user presses Ctrl+C in the app (or clicks Copy in its menu) ⇒ the token arrives ⇒ we write the local clipboard, **no click**. What cannot work: a copy the user did not make on THIS page (an agent's copy through P9, an app copying on a timer, a copy from another client) — outside the window, the chip stays (the browser's rule, not ours) | `deliverCopy`'s non-secure branch: try `copyViaSelection(text)` first; true ⇒ toast, no chip; false ⇒ the chip (≈5 lines); the chip gains a one-time hint "for seamless copy every time, enable HTTPS (three routes, §3.1)", remembered per device, dismissable | S (half a lane-day incl. tests) | Low: the gate is the browser's own return value; negative control = no gesture ⇒ false ⇒ chip (measured). Firefox has the same 5 s transient activation (unmeasured); Safari may require a synchronous handler (unmeasured) ⇒ falls back to the chip there | **Yes** |
| 2 | Closing the app's own window leaves the VibeSpace window to close again | The keeper only turns the record `exited` (`onPartExit` → `fail(rec,'app-exit')` → teardown → commit, desktop-app-keeper.js:704–719); the client's `render()` merely **disconnects the picture and writes one status sentence** on a terminal record (desktop-app-window.js:362–365); the view says "The application closed its window" (xpra-view.js:238); **no code closes the window**. Measured M3c: the calculator's own ✕ ⇒ `lost-window` at 23 ms ⇒ status at 59 ms ⇒ record `exited` at 61 ms ⇒ the window stays | Terminal record + a view with zero windows ⇒ `app.wm.closeWindow` + a toast "Calculator exited"; every client closes its own pane from the same `desktop-apps-updated` broadcast (layout sync cannot reopen it: an openSpec replay that meets an `exited` record ⇒ toast, no window). The reverse: the outer ✕ today does **not** stop the app (`onClose` only disposes the view, desktop-app-window.js:406; the app lives until the idle timeout) ⇒ make the outer ✕ send the app `close-window` (xpra-proto.js:375 already exists = WM_DELETE_WINDOW; the app may ask "save?"), a second ✕ within 5 s = Stop (the build's `OUTER_CLOSE_AGAIN_MS`; the first draft said 10 s) | The terminal branch of `render()` + a dead-record gate in `openDesktopApp` + `onClose` sending `close-window` (D2b optional); a dialog / popup closing is **not** the app exiting: the rule reads the **record** (process gone) AND **zero windows in the view**, both | S–M | Low; a launcher that forks and exits (process gone, windows remain) is not mis-closed (the zero-windows condition); a `failed` record **stays** (the error must be visible — the no-silent-failures law) | **Yes** |
| 3 | When the inner window has full controls, the outer window should show nothing | window.js:85–95 builds a title bar for every window type; desktop-app-window.js:135 calls `createWindow` like any window; the xpra seamless rung has no server-side decorations (recipe desktop-display.js:471–484: xpra is the WM and draws no frame), so GTK's header bar (CSD) is painted inside the pane and our title bar sits on top of it | **Detection (measured M2/M3a)**: in xpra's `new-window` metadata the calculator carries `decorations: 0` (from `_MOTIF_WM_HINTS` with flags∋DECORATIONS and decorations=0, xpra x11/models/window.py:844–849); xterm has **no** `decorations` key at all (no motif hints ⇒ xpra's default -1, not sent) ⇒ the rule `csd = meta.decorations === 0`; `_GTK_FRAME_EXTENTS` is absent without a compositor, `GTK_CSD` is unset, window-type is NORMAL for both — none is a signal. **Dragging (measured M3b)**: dragging the header bar ⇒ GTK sends `_NET_WM_MOVERESIZE` ⇒ xpra (the WM) sends us `initiate-moveresize [wid, x_root, y_root, direction, button, source]` (seamless.py:750–765 / source/window.py:297–302; direction 0–7 = the eight resize edges/corners, 8 = MOVE, 11 = CANCEL) ⇒ we **ignore it** today (xpra-client.js:331) and nothing moves. So the seamless "follow" = wire that packet into a drag/resize of the OUTER window (the inner X window stays fitted at 0,0; the belt is untouched). The app's own maximize/minimize buttons ⇒ `_NET_WM_STATE` ⇒ `window-metadata {maximized|iconic}` (both declared, xpra-proto.js:54) ⇒ map to `toggleMaximize/minimize` (whether GTK4 draws those two buttons under xpra is **unmeasured** — only ✕ was) | §3.3: PURE `seamlessVerdict` + the client's `on.moveresize` + WindowManager's `beginDragFromPointer/beginResizeFromPointer` + the title bar and status strip folded into a 0-height hot zone (top-edge hover reveals for 1.5 s) + a per-window toggle | M (one lane, 1–2 days) | Medium: the things that must stay on top of the app have an honest list (§3.3 table), of which the **agent lease** and the **tab chain** SUSPEND seamless instead of stacking on it; a kiosk-style app that sets decorations 0 without its own close button ends up with no ✕ — the escape is the taskbar's right-click menu (taskbar.js:239) + the top-edge hot zone, always there | **Yes**: auto-seamless when CSD is detected; a per-window toggle in the ⋯ menu |
| 4 | The app's DPI should be adjustable, ideally derived from VibeSpace's own DPI | .158 already: the launch carries `devicePixelRatio` (launcher:186), `appScaleFor('auto', dpr)` = 2 from DPR 1.5 up else 1 (desktop-apps.js:378–385), `scaleKnobs`: integer GDK_SCALE + Xft.dpi for the fraction (386–392; GTK has no fractional GDK_SCALE on X11, 1.5 scales text only), the setting `desktop.appScale` auto/1/1.5/2 (settings-schema.js:133–141), fixed at launch, a relaunch to change (the description says so), the `2×` chip. **The UI scale is not in it**: `vibespace.uiScale` is body zoom + `--ui-scale` (utils.js:875–889) and the pane is counter-zoomed to net zoom 1 (COUNTER_ZOOM utils.js:848) ⇒ at UI scale 1.25 the chrome is 1.25× and the app 1.0×. Measured M4: DPR 2 + uiScale 125 ⇒ record scale 2 / dpi 96, pane 1123×700 CSS ⇒ X 2246×1400, body zoom 1.25 unused | VibeSpace's own effective scale = `devicePixelRatio × uiScale` (the screen's × what the user declared for this device). Derivation: the integer part by r2's nearest-in-ratio rule (geometric midpoint √2: < 1.414 ⇒ 1, else 2; ≥ 2.83 ⇒ 3), the remainder goes into dpi **upward only** (`dpi = max(96, 96 × eff / gdk)`): 1.00→(1,96); 1.25→(1,120); 1.5→(2,96); 2.0→(2,96); 2.5→(2,120); 3.0→(3,96). GTK widgets follow the integer, text follows dpi (the §7.6 measured rule); Qt derives its fraction from Xft.dpi (unmeasured); xterm faceSize × gdk; Electron/Chromium apps need `--force-device-scale-factor=<fraction>` (unmeasured; a registry row could declare it, later) | `appScaleFor` gains a `uiScale` argument + `scaleKnobs` accepts any effective value in 1..3 (the record already stores `scale` + `dpi`; the chip prints `2.5×`); the window's ⋯ menu "Scale ▸ auto / 1× / 1.5× / 2× / 2.5× / 3×" ⇒ `POST /api/desktop/apps/:id/relaunch {scale}` (a new record with the same exec/args/cwd, the same window swapping ids); the global `desktop.appScale`'s auto = the derived value; a remote app carries the same knobs in its launch op (derived on the hub, applied on the device) | S–M | Low; the honest limits stay: xpra is a pixel stream, a scale change needs a relaunch (GDK_SCALE is read at startup), the client's `dpi` must equal the record's (§7.6 — else xpra rewrites Xft.dpi and doubles the text) | **Yes** |
| 5 | How the whole feature fits remote hosts; auto seamless setup | `hostFacts` refuses a non-local host by name (desktop-display.js:111–117 / 160–161), the keeper too (:268), the routes answer `unsupported-host` (routes/desktop-apps.js:70–74); the bridge connects to `ws://127.0.0.1:<port>/` (desktop-stream.js:837) and `net.connect(port,'127.0.0.1')` (:729). v1 says "the daemon op is not written yet" | **The shape exists**: browser-serve (a SHARED op table + runner the daemon bundles) + browser-access (`call(hostId, op)`: local in-process / a paired device through an agentd op / an ssh host without a daemon REFUSED by name `host_needs_daemon`) + `forwardCdp` (a hub-side loopback listener → `dm.tcpForward(remotePort)`, the port-forward primitive). desktop-display.js is already a `hostId`-parameterised machine-facts module (fs/child_process only); the agentd's esbuild `--bundle` (package.json:10) packs it statically like browser-serve. The xpra protocol is end to end and the bridge relays bytes ⇒ clipboard/input/geometry/x5 seats need **zero changes**. Impossible: macOS/Windows devices (no X11 — refused by name `no_x11`); ssh hosts without a daemon (an xpra session started over one ssh is an orphan on the first disconnect, with no keeper process — refused, no single-file rung) | §3.5: SHARED `src/desktop-serve.js` (the keeper's "done on the machine" half extracted: facts/launch/stop/status/list/windows/fit/keep-alive) + ORCH `src/server/desktop-access.js` (browser-access verbatim) + the agentd `desktop-serve` op (three-touch rule + capability gate) + the bridge's endpoint through `forwardPort` + `hostId` on records (a one-shot migration) + a machine picker in the launch dialog + "Install xpra on <machine>…" (owner-clicked, plan shown first, passwordless sudo; the fleet = the image) | L (two lanes) | Medium-high: the keeper's 1181 lines inline lifecycle and policy — extracting the SHARED half is the bulk of the lane; the fleet image's xpra 3.1.3 protocol remains OPEN (§9-1/§9-4) — recommend bumping the image to xpra.org's 6.x bookworm packages (D7) rather than measuring 3.1; a rebooted device loses its apps (the hub's records turn `exited` at the next `list`, honestly) | **Yes, but decide D5–D8 first; its own lane** |

## 2. Measurements (2026-09-23, this box)

### 2.1 M1 — clipboard, headless Chrome 153, page `http://<hostname>:<port>` (a hostname, not loopback)

| Item | Result |
|---|---|
| `isSecureContext` / `typeof navigator.clipboard` | `false` / `undefined` (the loopback page: `true` — loopback proves nothing) |
| `document.execCommand('copy')` with no gesture at all | `false`, clipboard unchanged |
| `execCommand('copy')` **synchronously** inside the keydown handler (trusted Ctrl+C) | `true`, clipboard = the written value |
| `execCommand('copy')` **asynchronously** (setTimeout) after a trusted Ctrl+C | 0 / 50 / 150 / 300 / 1000 / 3000 / **4800 ms all `true`**, `navigator.userActivation.isActive === true`; **5200 / 7000 ms `false`** (Chrome's 5 s transient-activation window) |
| Same, with `e.preventDefault()` in keydown (our view's shape) | 150 / 300 / 4800 ms still `true` (activation is independent of preventDefault) |
| A `copy` event listener's `clipboardData.setData` (with a selection) | works — but with **an empty focused textarea and no selection** (our IME textarea's shape) Chrome **dispatches no copy event** (0) ⇒ the "second Ctrl+C rewrites" route is **rejected** |
| `navigator.clipboard.writeText` on http under activation | the API does not exist (`hasApi:false`) |
| Control: Ctrl+V into a textarea on http | the paste event works (today's browser→app path is unaffected) |

### 2.2 M2 — the CSD X properties on a bare Xvfb (no WM — the seamless rung's situation)

| App | `_MOTIF_WM_HINTS` | `_GTK_FRAME_EXTENTS` | `_NET_WM_WINDOW_TYPE` | Other |
|---|---|---|---|---|
| gnome-calculator (370×616, min 370×616) | `0x3, 0x1, 0x0, 0x0, 0x0` (flags = FUNCTIONS\|DECORATIONS, functions ALL, **decorations 0**) | none (no compositor ⇒ no shadow margins) | NORMAL | `_GTK_APPLICATION_ID` org.gnome.Calculator; env `GTK_CSD` unset |
| xterm (484×316, inc 6×13, base 4×4) | **absent** | none | absent | needs server decorations — on the seamless rung it **has no close button at all** |

### 2.3 M3 — on the real product (worktree server + headless Chrome, xpra rung)

| Item | Result |
|---|---|
| M3a calculator `new-window` metadata (the client's `client.windows`) | `decorations: 0`, window-type [NORMAL], class [gnome-calculator], size-constraints min [360,616] |
| M3a xterm | **no `decorations` key**, size-constraints {base [4,4], inc [6,13], min [10,17]} |
| M3b a trusted 120×64 px drag on the header bar inside the pane | **2 `initiate-moveresize` packets** received (everything else draw/ping/pointer); the VibeSpace window's `left/top` unchanged (70,45); the client's main window (0,0) unchanged; X reports `[0,0,898,616]` unchanged — today the drag **does nothing** |
| M3c clicking the calculator's own ✕ | **23 ms** `lost-window` packet → **59 ms** view status "The application closed its window" → **61 ms** record `exited` (`exitCode 0`, log `exited: application exited (code 0)`) → the VibeSpace window **stays** (`exists:true`, view `connected`, 0 windows) |
| M4 a DPR-2 page + `vibespace.uiScale` 125 (body zoom 1.25, `--ui-scale` 1.25) | record `scale 2, dpi 96` (DPR 2 sent); pane **1123×700 CSS** ⇒ X main window **2246×1400** device px, canvas backing 2246×1400 in a 1123×700 CSS box (ratio 2); the app's minimum 720×1232 device px. The UI scale **is not part of it**: chrome 1.25×, app 1.0×. Derived: 2 × 1.25 = 2.5 ⇒ GDK_SCALE 2 + dpi 120 |

## 3. The design, point by point

### 3.1 Clipboard: the gesture window + a one-time HTTPS hint

Rule (PURE, `clipboardDelivery` grows to three states `api | gesture | chip`): a secure page with the API ⇒ `api` (today); else try `copyViaSelection(text)` first — it succeeds within 5 s of the user's last trusted input, and the token lands in that window (the Ctrl+C / Copy click is a trusted event OUR page received before forwarding it to the app); success ⇒ toast "Copied to your clipboard", no chip; failure ⇒ the chip (a copy from outside the window: an agent, a timer, another client). No `copy` event needed (M1b rejected it).

Which routes are seamless and which cannot be, plainly:
- **Seamless**: https pages (already today); a copy the **user triggers** on an http page (this design).
- **Not seamless**: a copy on an http page that no gesture on this page caused — an insecure origin cannot write the clipboard without a gesture, a browser rule; the chip stays.
- **Three routes from http to https for this owner (exact steps)**: ① zero infrastructure: Chrome `chrome://flags/#unsafely-treat-insecure-origin-as-secure`, enter `http://<hostname>:3456`, restart the browser — this browser only; `navigator.clipboard` then exists and today's code is seamless (**unmeasured**, documented Chrome behaviour); ② `tailscale serve --bg 3456` (Tailscale mints the certificate; the address becomes `https://<machine>.<tailnet>.ts.net`); ③ a reverse proxy (Caddy, two lines: `<hostname> { reverse_proxy 127.0.0.1:3456 }`, its local CA trusted once in the browser). The product does no TLS itself (server.js:159 is `http.createServer`, no TLS switch) — **not recommended to add**: a proxy / Tailscale is the usual way, and a cert-path setting would be one more TLS surface to maintain. The hint is one-time per device (`desktopCopyHintShown`) and links to a new section in docs/getting-started.md.

Gate: test-xpra-client §3's fake DOM gains two legs — activation present (`copyViaSelection` injected true) ⇒ no chip + toast; absent ⇒ chip; test-desktop-xpra-window's plain-http leg becomes: a trusted Ctrl+C into the app followed by `xclip -i` within 200 ms ⇒ the secure page reads the app's clipboard, **zero clicks**; negative control = `xclip -i` with no key ⇒ the chip appears.

### 3.2 Close: the app exiting closes our window; the outer ✕ is the app's own close

- Client `render()`: `rec.state === 'exited'` and `windows().length === 0` ⇒ `showToast(t('{app} exited', …))` + `app.wm.closeWindow(winInfo.id)`; `failed` ⇒ the window stays with the red sentence (today). `openDesktopApp(app, id)`'s first GET meeting `exited` ⇒ toast, no window (a dead record replayed by layout sync no longer becomes an empty window). `stoppedBy === 'user'` (Stop pressed) closes too (D2d).
- Both conditions are required: **only the process gone** (a forking launcher) ⇒ no close — the keeper's marker census already sees the real app process (`evidenceCensus`); recording it as `pids.app` is the keeper's follow-up (not this round); **only the windows gone** (the app closed its main window and keeps running, tray-style) ⇒ today's sentence "The application closed its window" + a Stop button, no auto-close.
- The outer ✕ (seamless or not): first `client.send(P.closeWindow(mainWid))` (xpra-proto.js:375 = WM_DELETE_WINDOW; the app may ask "save?" — nothing closes then); a second ✕ within 5 s (`OUTER_CLOSE_AGAIN_MS`, the lane brief's number; the first draft said 10 s) ⇒ `POST …/stop`; the app's exit takes the path above. Measured (A r1, 2026-09-24): xterm, which has no client-side decorations, honours WM_DELETE_WINDOW by hanging up its shell and exits with it — an interactive bash ends on the first ✕; a shell that survives SIGHUP (zsh's first-run `zsh-newuser-install` menu — a HOME with no .zshrc) keeps xterm running, and the second ✕ (the toast names it) is the Stop. Multi-client: each client closes its own pane from the broadcast; the x5 seat is released as today (the socket closing = leave). An agent lease (P9b): the app exiting ⇒ the engine's `holderAlive` fails ⇒ the lease goes away through today's `window-leases-updated` path and the agent's next verb gets `window_gone` (an existing refusal).
- Gate: test-desktop-xpra-window gains a leg — the CSD ✕ ⇒ within 2 s the window is gone from `app.wm.windows` and the toast text matches; a second page closes at the same time; control = a copy with the closing line removed.

### 3.3 Seamless chrome

**The verdict (PURE, `src/lib/desktop-seamless.js`, imports nothing, bundled)**:

```
seamlessVerdict({ csd, setting, userToggle, lease, chain, phone, connected })
  → { seamless: bool, why: 'csd' | 'user' | 'setting-off' | 'ssd' | 'lease' | 'chain' | 'phone' | 'disconnected' }
csd = meta.decorations === 0 (the main window); setting = desktop.seamless 'auto' | 'off'; userToggle = per window 'auto' | 'on' | 'off' (user state desktopAppFrame[appId])
seamless ⇔ connected ∧ ¬lease ∧ ¬chain ∧ ¬phone ∧ (userToggle==='on' ∨ (userToggle==='auto' ∧ setting==='auto' ∧ csd))
```

**The window's form**: seamless ⇒ `.window.seamless` — the title bar and the status strip fold into a **0-height hot zone** (a class of its own, not `display:none`; the hot zone is the top 6 px); hovering the top edge for 250 ms or holding Alt slides both out for 1.5 s (leaving folds them); the `.window-active` 1 px border stays (focus must remain visible); the resize handles stay (eight empty divs, invisible anyway). **Not seamless** (SSD apps like xterm, or any suspension) = today's window.

**The interaction map**:

| The user does in the app | xpra packet | We |
|---|---|---|
| drags the header bar | `initiate-moveresize` direction 8 (MOVE) | `wm.beginDragFromPointer(winId)`: enter `_setupDrag`'s processMove from the current pointer (the same machinery: grid snap, shake bypass, tab-merge hit test, desktop-preview drop); release pointer capture; the X main window stays at 0,0 (the belt untouched) |
| drags a CSD edge / corner | direction 0–7 | `wm.beginResizeFromPointer(winId, dir)`: `_setupResize`'s handle path (the minimum = the app's minimum, already); pane changes ⇒ the existing refit |
| keyboard move/resize (Alt+F7/F8 style) | 9 / 10 | as above, the mouse takes over |
| CANCEL | 11 | end the drag, restore |
| clicks the app's maximize / minimize | `window-metadata {maximized}` / `{iconic}` | `toggleMaximize` / `minimize` (whether GTK4 draws those buttons under xpra is **unmeasured**; without them, the hot zone / the menu) |
| clicks the app's ✕ | the app exits | §3.2 |
| double-clicks the header bar | the app itself (GTK maximizes = the row above) | — |
| right-clicks the title bar (today's window menu) | — | **the taskbar item's right-click** (taskbar.js:239 `showWindowContextMenu`, the full menu already) + a ⋯ in the hot zone |

**What MUST still be shown on top of the app (the honest list) and where it lives**:

| Thing | Today | In seamless mode | Justifies keeping a minimal bar? |
|---|---|---|---|
| the x5 blocked overlay "Active on another client / Resume here" | a whole-pane overlay | unchanged (it covers the app anyway) | no |
| the "Scaled to fit — the app needs at least w×h" badge | a pane badge (xpra-view.js:110) | unchanged (transient) | no |
| the plain-http copy chip | the status strip (picture-shell.js:103) | a floating chip at the pane's top-right, auto-hides after 10 s; after §3.1 only for copies from outside the window | no |
| the disconnected status + Reconnect | the strip | a centred overlay while NOT connected (the picture is gone anyway); hidden once connected | no |
| the `2×` scale chip, backend chip, CPU/RSS, idle countdown, Keep running, Stop | the strip (desktop-app-window.js:286–304) | visible while the hot zone is revealed; the taskbar's right-click menu "Desktop app ▸ Keep running / Stop / Scale ▸" | no |
| the idle-stop < 1 min warning | a chip | a toast | no |
| **the agent-held marker** (P9b: the mode badge / Take over / Hand back, desktop-app-window.js:302–316) | the strip | **seamless SUSPENDED** (the bar shows) — the user must see at a glance that an agent drives; the alternative (a 4 px coloured border + a badge in the hot zone) is cheaper and less visible | **yes** (the one real reason) |
| the tab chain's tab bar (it lives in the title bar), icon-drag merge, P7's owner/auth badges | the title bar | **seamless SUSPENDED** (entering a chain shows the bar) | yes (structurally) |
| the focus / active state | the title bar's active background | a 1 px border (CSS) | no |

Conclusion: only the **agent lease** and the **tab chain** deserve a bar, and both are handled by suspending seamless; everything else fits the hot zone / the menu / a toast.

**Default**: auto-seamless when CSD is detected; a per-window ⋯ (in the hot zone) "Window frame ▸ auto / on / off" remembered per app id in user state; a global `desktop.seamless` auto|off (`off` = one switch back to today). A phone (≤768 px) is never seamless (the window is the screen and the title bar is the only way back).

**Gate**: fast test-desktop-seamless (the verdict's full matrix + the suspensions + the toggle's precedence); test-xpra-client §2 gains `initiate-moveresize` ⇒ `on.moveresize` (fake worker); heavy test-desktop-xpra-window gains two legs: the calculator ⇒ `.window.seamless`, title bar `offsetHeight 0`, a 120×64 header-bar drag ⇒ window `left/top` +120/+64 (±2) and the X main window still at (0,0); xterm ⇒ not seamless, the title bar visible; control = M3b's behaviour today (nothing moves).

**SHIPPED (lane B, 2026-09-25, 2.369.177)** — built as above; what differs from the plan and what was measured:
- **The verdict is §3.3's formula verbatim** (`src/lib/desktop-seamless.js`); `why` answers the WANTED fact first (user / setting-off / ssd / csd) and only then the pause, so the menu can say "shown while an agent drives this app" instead of "no title bar". A blocked second client has no picture to learn the app's frame from, so it answers `ssd` (bars shown above the "Resume here" overlay) rather than `disconnected` — the same outcome.
- **The toggle's two words.** The menu is the FRAME ("Show window frame ▸ Auto / On / Off", user state `desktopAppFrame[<app id or exec:basename>]` holds the frame word); the verdict's `userToggle` is the SEAMLESS word — frame On ⇒ userToggle off. One PURE function crosses them.
- **The reveal timings:** 250 ms of hover in the top 6 px (a geometry test, so the resize handles keep the edge), or Alt held on the active window; the bars OVERLAY the app (the pane never resizes on a hover — no app re-fit); after the pointer goes back into the app or Alt is released they stay 1.5 s ("slide out 1.5 s" read as the linger), and leaving the window folds them at once.
- **The drag enters the SAME machinery:** `WindowManager.beginDragFromPointer` starts the title bar's own drag at the gesture's press point (converted from xpra's x_root/y_root), fed by POINTER events on a per-drag controller — the pane cancels its pointerdown, so no compatibility mouse events come. The view hands the pointer over: capture released, no motion to X, ONE button release to X on the document pointerup. MEASURED: a 120×64 header-bar drag moves the window 120/64; X and the client keep the main at 0,0; M3b's "2 packets" are `[8 MOVE, button 1]` then, on the release, `[11 CANCEL, 0, 0, button 0]` — GTK4 cancels on release, after our drop (a no-op).
- **Menus are one submenu deep** (showContextMenu): "Desktop app ▸ Keep running / Stop / Scale ▸" became four top-level rows of the window/taskbar menu — Show window frame ▸, Scale ▸, Keep running, Stop app — the same rows under the revealed ⋯.
- **The honest list, as built:** the blocked overlay and the fit badge unchanged (in the pane); the copy chip floats at the pane's BOTTOM-right while the strip is folded and hides after 10 s (r1: the planned top-right sits exactly over a CSD header bar's ─ □ ✕ — measured on GNOME Calculator); the idle stop's last minute is a toast; disconnected = a pause (the strip with Reconnect shows) rather than a separate overlay; the lease and the tab chain pause; focus = the 1 px `--border-active` frame.
- **Found on the way:** a click into an app's picture never activated its VibeSpace window (the cancelled pointerdown suppresses the mousedown the window's focus listener waits for) — fixed with a capture-phase pointerdown on the window.
- Gates: test-desktop-seamless (fast), test-xpra-client §2d/§2e, test-desktop-xpra-window §12 (heavy, CONTROL = a copy with the verdict forced false).
- **Verifier r1 (2026-09-25), three differences from the first build, each reproduced red on the real rung first:** (a) a drag off a MAXIMIZED seamless window (the app's header bar or our title bar) left X's `maximized: true` — the app kept its restore glyph and its first click was dead; window.js's drag un-maximize now calls `onResize` like every other un-maximize (and a cancel that re-maximizes too), which is what sends `setAppState({maximized:false})`; (b) the floating copy chip moved to the pane's BOTTOM-right (the top-right covered the app's ─ □ ✕ for its 10 s); (c) the revealed title bar and strip were z-index 25 / 24, over the resize handles (10), so after the 250 ms reveal a press on the top edge's 3 px handle band became a title-bar DRAG — they are 9 / 8 now, and while revealed the top edge is still a resize (§12 (4) hit test, live control with 25 put back). The hot zone stays a geometry test.

**Lane D (a) item C (2026-09-25) — CHROME UNDER SEAMLESS, MEASURED** (Google Chrome 153.0.8010.47 under xpra 6.5.3, the keeper's `chromium` row on a fresh profile; the owner's second screenshot: two bars stacked, "Chrome的顶部标题栏似乎不会自动隐藏？"):
- **What Chrome creates:** 0x400004 = the browser window (the only one xpra forwards, reparented into `Xpra-CorralWindow-0x400004`); unmapped helpers 0x600001 "google-chrome" 10×10 (the WM_CLIENT_LEADER), 0x400000 "Chromium clipboard" (override-redirect at -100,-100), 0x400006 1×1 override-redirect; menus and tooltips arrive as `new-override-redirect` (popups). The first-main pick is right — no evidence picker needed.
- **What it reports by default:** `new-window` metadata `has-alpha: true`, `title "New Tab - Google Chrome"`, `class-instance ["google-chrome (<profile dir>)", "Google-chrome"]`, `window-type ["NORMAL"]`, `size-constraints {gravity 10, minimum-size [500,87]}`, **`decorations: 1`**; X: `_MOTIF_WM_HINTS 0x2,0x0,0x1,0x0,0x0` (flags DECORATIONS, decorations 1 = "window manager, draw my frame"), `_NET_WM_WINDOW_TYPE_NORMAL`, `WM_WINDOW_ROLE "browser"`, no `_GTK_FRAME_EXTENTS`, `_NET_FRAME_EXTENTS 0,0,0,0`.
- **Why the bars did not fold:** Chrome does not recognise the window manager (`_NET_SUPPORTING_WM_CHECK` names "Xpra"), so its "Use system title bar and borders" — the profile pref `browser.custom_chrome_frame` = false — is its default: it draws its tab strip and toolbar with NO ─ □ ✕ and asks the WM for a frame ⇒ not CSD ⇒ `{seamless:false, why:'ssd'}`. The bar the owner read as Chrome's title bar was OURS (it carries Chrome's X title and icon); the strip under it too. Nothing in the verdict path excludes a browser row, and the strip has no rule of its own (it folds with the title bar under `.window.seamless`). Chrome 153 has NO command-line flag for it (the binary's strings name only the pref).
- **With `browser.custom_chrome_frame: true`:** `_MOTIF_WM_HINTS 0x2,0x0,0x0,0x0,0x0` ⇒ **`decorations: 0`**, `_GTK_FRAME_EXTENTS 5,5,5,5`, minimum [510,97] (maximized [500,87]), `_GTK_HIDE_TITLEBAR_WHEN_MAXIMIZED 1`, `_NET_WM_OPAQUE_REGION` = the window minus its two 15 px top corners (rounded). Chrome draws its tabs AND its own ─ □ ✕ ⇒ `why: 'csd'` ⇒ seamless. Chrome's own Settings → Appearance toggle flips `decorations` LIVE on the same X window (the verdict follows it).
- **Its buttons through our client** (trusted clicks, a 900×620 window at DPR 1): □ ⇒ `{maximized:true}` ⇒ ours maximized; its restore ⇒ ours restored; ─ ⇒ `{iconic:true}` ⇒ ours minimized; our restore ⇒ `setAppState({iconified:false})` ⇒ Chrome repaints; ✕ ⇒ Chrome exits 0 with no window left ⇒ our window closes (A2). A drag of the tab strip ⇒ `initiate-moveresize` direction 8 in BOTH frame modes — with x_root/y_root **+10,+5 off** the real press (GNOME Calculator's are exact): the window moved 110/59 for a 120/64 gesture. The view now starts the drag at the pane's OWN press when one is in flight (the app's root point only for a keyboard move): 120/64 ⇒ 120/64.
- **Built:** the keeper seeds `<profile>/Default/Preferences` with `browser.custom_chrome_frame: true` ONCE per profile (a marker `.vibespace-frame-seeded` beside it — Chrome may drop a default-valued key, and a user who turns the system title bar back on inside Chrome must keep it), only when the key is ABSENT (an existing `false` is the user's), in `bringUp` BEFORE the browser starts — a fresh scaffold, a kept profile and one carried to a Scale ▸ successor alike (at `launch` the successor's empty scaffold would no longer be empty and the carry's `rmdir` would fail). Forcing "Show window frame ▸ Off" alone was NOT the fix: in its default layout Chrome has no ─ □ ✕, so forcing seamless would leave only the drag. Measured through a Scale ▸ relaunch 1× → 1.5× (DPR 1): the carried profile keeps the pref and the marker, the successor is seamless again (decorations 0, minimum [765,146] = 1.5 × [510,97]), and the window follows the scale like any app (798×558 → 1197×837 — a browser row's widgets are its whole scale).
- **Measured residual (open):** not maximized, Chrome draws a 5 px resize band inside our 1 px frame (1 px grey at the edge, 3 px white, a 1 px grey line) and rounds its two top corners (transparent pixels); maximized, both are gone. xpra 6.5.3 does not forward `_GTK_FRAME_EXTENTS`, Chrome's opaque region INCLUDES the band, and Chrome 153 has no `_GTK_EDGE_CONSTRAINTS` (tiled) support — the client has no fact to crop it by; the keeper reading `_GTK_FRAME_EXTENTS` from X would be the way. Firefox is unmeasured (a snap on this box, refused `snap-profile-unreachable`); its likely switch (`browser.tabs.inTitlebar`) is not seeded. The keeper's Stop can leave Chrome's `profile.exit_type: "Crashed"` (2 of 3 runs, reader 2 — a Scale ▸ successor then shows "Restore pages?"): cause not isolated, open.
- **Found on the way (fixed):** a scroll packet's moves were applied one after another on the canvas they read, so a later move read rows an earlier one had overwritten (593 overlaps in 66 packets on chrome://settings — ghost rows until a full repaint); every move of one packet now reads ONE snapshot (upstream's `do_paint`). A `has-alpha` window is cleared under each draw.
- Gates: test-desktop-seamless §6 (the measured metadata ⇒ the verdict; the seeding ⇒ Chrome's frame ⇒ seamless; CONTROL: no seeding ⇒ `ssd`), test-desktop-apps §14 (the pref table), test-xpra-client §7 (the press, the scroll snapshot, the alpha clear), heavy test-desktop-xpra-window §16 (the real binary) with its CONTROL copy.

### 3.4 DPI: derived from VibeSpace's own scale, adjustable per window

- **The derivation (PURE, `appScaleFor(setting, dpr, uiScale)`)**: `eff = clamp(dpr × uiScale, 1, 3)`; `gdk = eff < √2 ? 1 : eff < 2√2 ? 2 : 3` (r2's nearest-in-ratio rule extended to 3); `dpi = max(96, round(96 × eff / gdk))` (the remainder goes upward into dpi only, text is never shrunk below 96). `scaleKnobs(eff)` accepts any effective value and answers `{scale: eff, gdkScale, dpi, env, xresources}` (GDK_SCALE, QT_SCALE_FACTOR = gdk, XTerm faceSize = 8 × gdk × dpi/96). The table:

| eff | gdk | dpi | GTK widgets | text |
|---|---|---|---|---|
| 1.00 | 1 | 96 | 1× | 1× |
| 1.25 | 1 | 120 | 1× | 1.25× |
| 1.50 | 2 | 96 | 2× (1.33× relative to the chrome) | 2× |
| 2.00 | 2 | 96 | 2× | 2× |
| 2.50 | 2 | 120 | 2× | 2.5× |
| 3.00 | 3 | 96 | 3× | 3× |

- **Where uiScale is read**: the launch request carries `uiScale` beside `dpr` (`uiScale()` utils.js:841); the route validates 1..2 (`UI_SCALE_MIN/MAX`). A remote app carries the same pair in its `launch` op; the device only applies env + xrdb (derived on the hub, like `dpr` today).
- **Per-window control**: the hot zone / taskbar menu "Scale ▸ auto (2.5×) / 1× / 1.5× / 2× / 3×" ⇒ `POST /api/desktop/apps/:id/relaunch {scale}`: the keeper starts a new record with the same exec/args/cwd/host, the window's `_desktopAppId` and openSpec swap to the new id (the same VibeSpace window, the picture reconnects), the old record is stopped. The description says "changing the scale relaunches the app (unsaved work is lost)", the menu item confirms (createModalShell). The global `desktop.appScale`'s `auto` = the derivation; the explicit 1/1.5/2 stay, 3 is added.
- **What .158 already did and this leaves alone**: device pixels end to end, `size-constraints` clamping the window, the Watch stage scaling, the client's `dpi` = the record's dpi.
- **Honest limits**: a change needs a relaunch (GDK_SCALE is read at startup); a fraction only affects text in GTK (an X11 limit); Qt/Electron fractional knobs unmeasured; the vnc-display rung does not scale (whole display).
- Gate: test-desktop-apps §10 gains the table (six rows + the √2 / 2√2 boundaries + a regression control: uiScale absent = 1 reproduces .158 row by row); heavy §7 gains a leg: uiScale 125 + DPR 2 ⇒ record `scale 2.5, dpi 120`, the calculator's minimum around `[900,1540]` (asserted only as ≥ the 2× values and a text line taller than 2×).
- **2026-09-25 note — lane D (b): the clickable chip + a per-app default scale** (the owner: "可以改成那个1.5x 已选的提示直接可以点快速切换" + "最好加入可以在app启动前那个app选择界面调整每个app默认dpi的能力"). (1) The status strip's scale chip is a CONTROL whenever the window can relaunch (`relaunchVerdict` null — a running xpra app): role=button, Enter / Space, a click opens the SAME Scale ▸ rows (one function) anchored at the chip; otherwise plain text with the reason in its tooltip. (2) Each catalog card in the launch dialog (on a machine whose rung is xpra) carries a sibling control whose menu — Auto (Settings → Desktop app scale) / 1× / 1.5× / 2× / 2.5× / 3× — stores the APP's default in user state `desktopAppScale[<key>]` (the `desktopAppFrame` key: the registry id, else `exec:<basename>`; Auto removes it; synced to every client) without launching. Every launch sends the app's stored default as `scaleChoice`; `scalePick` precedence = a relaunch's choice > the app default (origin `app`, chip "1.5× · App default") > an explicit `desktop.appScale` > auto. The route refuses a bad value by name before a paired machine is asked; a paired machine gets it inside the `launch` op's `body` (the op is otherwise unchanged; an older agent drops the field and its record says what it ran at). The window's Scale ▸ marks the app default's row, and adds "{n}× is this app's default" / "Make {n}× the default for this app" (no relaunch) / "Forget this app's default". (3) The explicit set is now 1 / 1.5 / 2 / 2.5 / 3 (`EXPLICIT_SCALES` — each spelled exactly by `scaleKnobs`; as built on lane D (b) that was the floor rule (2.5 = GDK_SCALE 2 + 120 dpi, labelled "GTK apps: widgets 2×, text 2.5×"), and at the lane-D merge lane D (a)'s ceil rule below replaced it: 2.5 = drawn at GDK_SCALE 3 and shown at 0.8333, every row a plain "n×"; 3 = GDK_SCALE 3) for the window menu and the card; the Settings enum stays 1 / 1.5 / 2. Decision flagged for the owner: with an explicit Settings value, a per-app "Auto" means "follow Settings" (a per-app "always derive from the screen" would need a stored 'auto' row — not built). Gates: test-desktop-app-scale (fast), test-desktop-xpra-window §14 (real rung, two clients, a copy without the field and the control), test-desktop-remote §7 (a real daemon).

**Lane D (a) (2026-09-25) — A FRACTION IS A REAL SCALE; THE WINDOW FOLLOWS A RELAUNCH** (the table above is how A3 spelled a fraction — superseded). The owner, GNOME Calculator relaunched from 2× to 1.5× through Scale ▸: "从2x切换到1.5x之后出现大量白边，你窗口尺寸计算不对". MEASURED first (xpra 6.5.3 + gnome-calculator 50, headless Chrome at DPR 2 / 1.5 / 1, the real Scale ▸ path): our geometry was exact at every transition — the X main window = the pane in device px, margins 0/0/0/0, 0.0 % of the pane uncovered. The white was the calculator's OWN background: the floor rule drew 1.5× at GDK_SCALE 1 + 144 dpi (widgets 1×, text only) and libadwaita clamps its content column (~676 logical px), so the relaunch halved every widget and the column (676 → 338 CSS px at DPR 2) in a window that kept its size — the app's own background went from 25 % to 62 % of the width.
- **The rule now** (`scaleKnobs(s)`, rule `ceil`): GDK_SCALE = ⌈s⌉ at 96 dpi, the view shows the picture at `pictureScale` = s ÷ ⌈s⌉ — widgets AND text at s, resampled by the browser; an integer scale is still drawn 1:1.

| s | GDK_SCALE | dpi | picture | widgets + text |
|---|---|---|---|---|
| 1 | 1 | 96 | 1 | 1× |
| 1.25 | 2 | 96 | 0.625 | 1.25× |
| 1.5 | 2 | 96 | 0.75 | 1.5× |
| 2 | 2 | 96 | 1 | 2× |
| 2.5 | 3 | 96 | 0.8333 | 2.5× |
| 3 | 3 | 96 | 1 | 3× |

  The same rule for auto's fractions (a 125 % UI on DPR 1 draws at 2 and shows at 0.625 — the app matches the chrome at ~2.56× the pixels of a 1:1 picture; keeping auto integer-only would bring back M4's 1.0× app beside 1.25× chrome). The browser rows keep the floor rule (`rule: 'dpi'`, `scaleRuleOf`): Chrome scales WHOLE from the font dpi (measured [750,131] at 1 + 144 dpi = 1.5 × [500,87]) and stays crisp at 1:1. The record stores `gdkScale` + `pictureScale`; the client reads the RECORD (`renderOf`) — a record without them (older, or a paired machine on older code) was the floor rule and is shown 1:1.
- **The window follows a relaunch** (PURE `relaunchPaneCss`): new pane = the app's logical size × the new widget scale ÷ dpr, logical = the pane's X px (the main's, when the view scaled it to fit) ÷ the old GDK_SCALE; the asking client resizes the window (`WindowManager.resizeWindowTo`, top-left kept, capped at the workspace; never maximized / a tab chain / the phone) and the layout sync carries it. DPR 2: 898×678 → 1× 449×339 → 1.5× 673.5×508.5 → 2× 898×678 → auto 898×678 → 1× 449×339 — exact round trips; the app's own background 24.7 % at every step.
- **Three places OUR margins could appear, each measured and fixed:** (1) F3 — every CSD connect mapped the app at the pane with the bars still up (the fold waited for `main`, which came after the fit) and re-fitted ~160 ms later: the client now names the main BEFORE it fits it and the view resizes inside that call, so the map-window is the final size; (2) a pane below the app's minimum (DPR 1 at 2×: 720×1232 in a workspace-capped 898×913 pane) was scaled to fit with 116 CSS px of pane background either side (25.8 %): the fit keeps the pane's SHAPE at the fit scale (1212×1232); (3) GTK snaps to its own size ~650 ms after the map and the belt's same-task re-fit cleared the rows in between for ~110 ms: a shrinking window keeps its canvas backing for 1 s.
- **Measured after** (test-desktop-xpra-window §15, DPR 2 and DPR 1, the window's own Scale ▸ rows auto → 1 → 1.5 → 2 → auto → 1, then a resize to 1200×800 and back): every edge ≤ 1 CSS px and 0.00 % of the pane's own background in the screenshot at every step; the calculator's column per 1× constant (DPR 2: 338.0 CSS at 2×, 1×, 1.5×; DPR 1: 676 / 673); every map-window = the settled main. CONTROL (a copy with the floor knobs and no relaunch fit): 2× → 1.5× column ratio 0.500 in a window that kept its 898 px — the owner's screenshot.
- **Honest limit:** a fractional picture is RESAMPLED (a little softer than a whole scale — the price of real widgets); the 1:1 identity legs (test-desktop-xpra-window §6/§7) stay at integer scales.

**Lane D verify fixes (2026-09-25, the adversarial verifier's one finding above low, reproduced red first).** THE PER-APP MAP TRAVELLED WHOLE FROM A PAGE THAT MISSED A BROADCAST: `src/lib/desktop-app-prefs.js` read user state once per page and never again, and a save PATCHed the WHOLE map (`{desktopAppScale: <this page's copy>}`), while the route merges only top-level keys. Measured on a real server with two headless-Chrome pages (the launch dialog open on both): B's socket dropped (a lid / a network blip — WsManager reconnects by itself), A stored xterm 2× ⇒ `{"xterm":2}`; B reconnected and its xterm card still read "Auto"; B stored the calculator 1.5× ⇒ `{"gnome-calculator":1.5}` — A's choice gone with no word, and the same with B saving while still down; the Show window frame ▸ map rode the same loader. FIX: the loader re-reads on EVERY reconnect (`ws.onStateChange`, the rule desktop-app-window.js already keeps for its record) and a save is an EDIT of ONE app's entry — `saveAppPrefs(key, (map) => setScaleChoice(map, appKey, choice))` — shown locally first, then applied to the map the server holds NOW (a fresh GET) and PATCHed as that one key; saves from one page run one at a time; a refused save rolls the view back to the server's map (the low finding "applied locally, never rolled back" fixed with it) and toasts the reason. Measured after, same recipe: B's card read "2×" after the reopen and the server kept `{"xterm":2,"gnome-calculator":1.5}`; B saving while still down kept `{"xterm":3,"gnome-calculator":2}`. Residual (named): two pages editing the SAME map inside one GET→PATCH window (tens of ms) are last-write-wins, as a same-key race on user state already is. Gate: test-desktop-app-scale §8 (two pages over a fake server that merges exactly as the route does and broadcasts only to connected sockets, both maps; three patched-copy controls: no reconnect re-read, the old local-map PATCH, a kept refused edit) + the §7 census (every `saveAppPrefs` call passes an edit) + heavy test-desktop-xpra-window §14 (1b) (the same recipe on two real pages). The same suite's §12 (8) read user state ONCE right after the local-first view flipped (Show window frame ▸ On, then Auto) — a judge racing the write, which the save's extra GET made lose every time (red on both attempts of the first heavy run: `us` null, then the key still there); both reads now wait (≤ 4 s) for the server's copy, the assertion unchanged.

**Lane D verify lows (not fixed, 2026-09-25)** — recorded as the verifier wrote them:
- **Fast tier RED on test-exit-forensics under load**: `pty/SIGHUP` baseline saw 0 signals while real xpra / Calculator / Chrome legs ran beside the fast tier; 285/285 alone. No file of this lane is involved (`data/bin/pty-wrapper.js`, `chat-wrapper.js`, `src/exit-facts.js`, the suite: no diff). The integrator runs the fast tier once on a quiet machine before the push; the suite's baseline leg may deserve a load-tolerant wait (outside this lane).
- **Two "Auto"s mean different things**: the window's Scale ▸ "Auto (n×)" re-derives from the screen and ignores both Settings → Desktop app scale and the app's default; the card's "Auto" means "follow Settings". With `desktop.appScale` 2 and a 1.5× app default, the window's Auto on a DPR-1 page relaunches at 1× · auto. An owner decision (§3.4 note): at minimum word the window row by what it does ("Auto — this screen (n×)") and/or offer "Settings (2×)" / "App default (1.5×)" as rows of their own.
- **A launch in the first 3 s of a page can drop the app default silently**: the launcher waits ≤ 3 s for the first user-state read, then launches without `scaleChoice` (the record runs at the instance default, "1× · auto") and says nothing. Fix when touched: await the read (the card already shows a launching state) or toast that the app's default could not be read.
- **The per-app maps are never pruned**: keys for apps the registry no longer names (a removed row, a command typed once) stay in `desktopAppScale` / `desktopAppFrame` forever; only a person's Auto removes one. Growth is bounded by the distinct apps a person ever chose. Prune on the dialog's render, or keep documenting the shape as intentionally unpruned (as the frame map is).
- (The verifier's fifth low — "a save applied locally is never rolled back when the server refuses" — is not in this list: the minor's fix above rolls it back.)

**2026-09-26 note — inc-muhmqvzf-jodk: a snapped window never clips its picture** (the owner, a Mac at 1920×963 / DPR 2 / UI 100 %, the sidebar open, a 1×3 grid: "chrome吸附在右侧，右侧有截断"). Every lane-D candidate inside the picture was MEASURED and ruled out by the reproducer (202 states on 2.369.177 and this lane): Chrome's (20,20) / (10,10) / (25,25) new-window origin is mapped at 0,0 in the same tick and X agrees (the origin IS read, never assumed — no change needed); the canvas backing = the X size after every snap; Chrome accepts every fit down to its minimum; the pane is the window minus its 1 px borders and covers the picture to ≤ 1 CSS px; every map-window is already the final size (no F3 race). The cut was the WINDOW: a window whose own minimum (Chrome 1020 device px ⇒ 512 CSS with its own frame; the calculator 618 CSS tall at 2×) is larger than its zone grew right / down from the zone's corner and #workspace (`overflow: clip`) cut what hung past it — 58 device px of Chrome in the owner's right third, 360 of the calculator's keypad on the bottom half, 9–11 at UI 125 % for a new window's default cascade. "The fit keeps the pane's shape when the minimum is larger" is still true and was never the cause: that rule scales a picture the WORKSPACE cannot hold (the minimum capped at the workspace); here the workspace could hold the window, the placement put it in the wrong place. FIX (window manager, not the view): ONE placement, `WindowManager._placeWindow` over PURE window-min-size.js `zoneBox`, used by every snap / grid / range / stored-bounds / restore-from-maximize path, plus `_defaultPlacement` for createWindow — the window overlaps its neighbour and slides back inside the workspace (less the 4 px gutter), never past its left / top edge; a zone that already ended past the edge keeps its own edge. After: the owner's case 58 → 0, the whole reproducer matrix (Chrome + Calculator × DPR 1/2 × UI 100/125 % × sidebar 0/426/470) 0 px hidden by the workspace. Gates: test-window-minsize §4 (fast) + heavy test-desktop-app-snap (the matrix on the real rung, judged on screenshot pixels, with a pre-fix control copy). r2 (the adversarial verify): the first fix covered the sidebar opening only under a VISIBLE window and the un-maximize only on an unchanged workspace — minimize / maximize → the sidebar opens → restore / un-maximize still hung 422 CSS px past and the capture cascaded; now a minimized window is placed by the reflow while hidden and the un-maximize rescales its stored box (kb-bugfix-invariants, the inc-muhmqvzf-jodk entry's r2 bullet; gates test-window-minsize §4b + the snap gate's flow lines). Named, not changed: at a FRACTIONAL ratio (2.5× on DPR 2 = X px ÷ 2.4, GTK sizing in steps of 3 X px) lane D (a)'s cover rounding still leaves ≤ 1 CSS px of the picture under the pane edge — the heavy judge allows ≤ 1 CSS px there and demands exactly 0 in the owner's case.

### 3.5 Remote hosts: run the same code where the app lives

**Layers (the CS-separation law + the browser-serve precedent)**:

| Layer | Module | Content |
|---|---|---|
| SHARED | `src/desktop-serve.js` (new) | the op table `DESKTOP_SERVE_OPS = ['facts','launch','stop','status','list','windows','fit','keep-alive','relaunch']` + `install({env, dataDir, homeDir, log})` + `runDesktopServeOp(ds, op, params)` — the keeper's **done-on-the-machine half** extracted: X/xpra/app bring-up (today's `bringUp`), part identity (pid+starttime, the marker census), teardown, the fit belt, window enumeration, the machine's own record file (`<dataDir>/desktop-apps.json`; on a device `~/.vibespace/desktop-apps.json`). Every op answers `{ok, code, error}`, never a throw across the wire |
| ORCH (hub) | `src/server/desktop-app-keeper.js` (slimmed) | registry + policy: hub records (`data/desktop-apps.json` + `hostId`), the cap, the idle policy, runaway thresholds (samples come from the device's `status`), the x5 election, broadcasts, boot adoption by asking every machine `list` |
| ORCH (hub) | `src/server/desktop-access.js` (new, browser-access verbatim) | `call(hostId, op, params)`: local ⇒ the in-process runner; a paired device ⇒ `dm.desktopServe(op, params)`; an ssh host without a daemon ⇒ `host_needs_daemon` (refused by name, never a silent local fallback — **as shipped: an ssh host gets the daemon installed and is served; `host_needs_daemon` = an agent that predates the op; deviation ⑦**); `forwardPort(hostId, port)` = `forwardCdp` verbatim (a hub loopback listener → `dm.tcpForward`, reference-counted, the device resolved per connection) |
| daemon | `src/agentd/agentd.js` + `client.js` | the `desktop-serve` op: the handler `require('./../desktop-serve.js')` (bundled) and replies `mux.control({op:'desktop-serve-result', id, result})`; client.js `desktopServe(op, params)` gated on `capabilities.includes('desktop-serve')` (an old daemon is never asked — an unknown op hangs); `'desktop-serve-result'` joins the id-keyed routing set at client.js:253 (three-touch rule, touch 1; no unsolicited pushes ⇒ touch 2 n/a; no watches ⇒ touch 3 n/a) |
| bridge | `src/server/desktop-stream.js` | the ONE change: the upstream endpoint comes from `streamEndpointFor(rec)` — local `127.0.0.1:<port>`, remote `127.0.0.1:<forwardPort>`; the same for the xpra ws and the rfb tcp; x5 seats, backpressure, keepalive, named closes, the clipboard/input policy are **unchanged** (the protocol is end to end) |
| routes / UI | routes + launcher + window | `host` is no longer refused: the launch dialog gains a machine picker (this machine + paired devices with the `desktop-serve` capability; ssh-only hosts greyed "needs the agent"); each machine's catalog greyed by ITS facts (never hidden); the window title carries the host label (a display string, never into a spawn) |

**Remote clipboard / input**: xpra's `clipboard-token`, `key-action`, `configure-window` all ride the same ws, forwarded from the hub's loopback to the device's loopback into xpra — nothing parses them in between; §3.1's gesture window is browser-side and machine-agnostic. Latency: + the device link's RTT (the CDP forward precedent; §7.1: 265 ms per keystroke at 200 ms RTT).

**Auto seamless setup**:
- **Detectable**: the `facts` op (`binOnPath` xpra/Xvfb/xauth/xdotool/xwininfo/xdpyinfo + `xpra --version` + whether each registry row's exec is on PATH) — the dialog shows each machine's ladder verdict and reason (today's chip words).
- **Installable, never silently**: ① fleet pods ⇒ the image only (Dockerfile:23–38 already carries xpra + xterm + xdotool + wmctrl; D7 decides the version); ② a paired Linux box ⇒ the owner clicks "Install xpra on <machine>…" (the rclone one-click shape, mounts.js:310): `facts` first for the distro and codename, **the plan shown first** (`apt install xpra xterm xdotool xauth` when the distro's xpra is ≥ 5, else add xpra.org's repository `https://xpra.org/<dist>/<codename>` + key, pinned to 6.x), passwordless sudo required (the `_sudoAvailable` probe, plugins.js:295) — else refused by name with the commands handed to the owner to paste; the run streams its log into the dialog through `runCmd`; ③ this machine ⇒ as ②.
- **Impossible**: macOS / Windows devices — no X server, the seamless rung does not exist (XQuartz + xpra is a manual install we do not drive); the dialog says `no_x11` by name. ssh hosts without a daemon — `host_needs_daemon`; the one-click "install the agent on this host" is the existing path.

**Record migration**: every record in `data/desktop-apps.json` gains `hostId: 'local'` (the one-shot migration `2026-09-desktop-apps-host-key` in src/server/migrations.js through the shared runner — ledger-keyed, retried next boot on failure); device-side records are written by the device's own op into `~/.vibespace/desktop-apps.json` (the daemon's DEVICE_MIGRATIONS table owns that shape later); a hub record = the device's record + hub-only fields (viewers, idle policy, pane facts). Boot: the hub asks the in-process `list` for this machine and `list` on every online device, adopts by `hostId:id`; an offline device's records keep an `unknown-host-offline` state and are asked again when it returns.

**Tests**: fast `test-desktop-serve` (the op-table census: every op the hub calls is in the SHARED table, the daemon handler covers each by name, the hello-ack capability string; every op's failure is `{ok:false, code}`); **real-daemon** `test-desktop-remote` (the test-sysinfo-op template: an agentd started from the build output as a fake paired device in a scratch HOME ⇒ `facts` ⇒ `launch xterm` ⇒ `forwardPort` ⇒ a real hello through the bridge ⇒ a keystroke into a file ⇒ `stop`; **the capability gate**: a daemon copy with the capability removed ⇒ `host_needs_daemon` within 1 s, never a hang); heavy = the existing test-desktop-xpra-window run once more with `host=<the fake device>` on its main legs; test-migrations gains row 43.

**SHIPPED (lane C1, 2026-09-25, 2.369.178; D5–D8 decided by the owner as recommended).** Landed: SHARED `src/desktop-serve.js` (the keeper's on-the-machine half moved VERBATIM — X/xpra/app bring-up, part identity, the marker census, teardown, the fit belt, window enumeration, the resource SAMPLE, the machine-local record file — plus the nine-op `DESKTOP_SERVE_OPS` and `runDesktopServeOp`, every op `{ok, code, error}`, nothing thrown across the wire); `src/server/desktop-app-keeper.js` slimmed to registry + policy (x5 election, the idle verdict, the resource REPORT — report only, never a stop, the 2.369.171 law — the relaunch seat carry, broadcasts); ORCH `src/server/desktop-access.js` (browser-access's shape verbatim: `call(hostId, op, params)` local in-process / a paired device's (or an ssh host's, its daemon installed over ssh) agentd op / an agent predating the op refused `host_needs_daemon` by name (deviation ⑦); `forwardPort` = `forwardCdp` verbatim, ref-counted, the device resolved per connection); the agentd `desktop-serve` op (three-touch rule touch 1: `desktop-serve-result` in client.js's id-keyed routing set; the hello-ack capability; ONE machine keeper per daemon PROCESS, its record `~/.vibespace/desktop-apps.json`, re-adopted at daemon boot when a live session is on record); migration `2026-09-desktop-apps-host-key` (every hub record `hostId: 'local'`; new records carry it from `M.newRecord`). Gates: test-desktop-serve (fast, new), test-desktop-remote (heavy, new, a real daemon), test-migrations' new row; test-desktop-app-keeper / test-desktop-xpra and the rest green with ZERO assertion changes (the local byte-identity proof).

**Deviations from this section (each named):** ① the cap (CONCURRENT_CAP) is judged by the MACHINE over its own sessions (it bounds that machine's resources), not summed by the hub across hosts — the table put "capacity" on the hub. ② For this machine the policy reaches the in-process machine keeper as HOOKS (`view`/`onCommit`/`idle`/`readyTick`/`onSample`/`onTeardown`, each called exactly where the old tick ran that code) and there is ONE store (hub record = machine record); REMOTE records in the hub registry, the hub judging a device's samples (a device keeps its latest sample for the `status` op) and remote idle are lane C2. ③ The table has a `relaunch` op (one machine act: successor + `replacedBy` + the old stop); since the C1 fix round (2026-09-25) the in-process hub keeper calls the SAME machine relaunch with an `onSuccessor` hook (its seat carry sits between the successor's record and the old stop), so there is ONE relaunch — and it carries 2.369.176 (a browser relaunch MOVES its profile onto the successor, deferred bring-up), which landed on master while the lane was open and was re-ported into the machine half at the rebase; test-desktop-app-keeper §18 (c3) + its no-carry control run against it, and the two relaunch wiring pins (test-desktop-apps §13, test-desktop-viewers' seat-carry pin, now over both arms) name that shape (the only assertions reworded). ④ Three heavy suites' HARNESS changed (never an assertion): test-desktop-app-keeper's `mutant()` applies each anchor to whichever half holds it (exactly one hit across the pair) and re-binds the keeper copy's `require('../desktop-serve')` to a patched machine-half copy; the `stops-on-over` anchor lost one nesting level (the report is `reportSample`); test-desktop-apps' keeper pins read both halves; test-desktop-xpra-window's three keeper levers name src/desktop-serve.js. ⑤ Re-running test-desktop-xpra-window's main legs with `host=<the fake device>` needs the bridge's port resolution — C2. ⑥ **The real-device leg ran over the ssh stdio transport** (a standing daemon under a scratch HOME on the paired test box, dialed by the test process's DeviceManager); the **dial pairing to a scratch hub through `ssh -R` was NOT done** — it belongs with the machine choice and the install rung in C2; the op path and the data plane (the same Mux) are identical. ⑦ **An ssh host is not refused — it is served by a daemon** (measured on the real HostManager class, test-desktop-serve §5): `hosts.device()` installs the bundled daemon over ssh and drives it over ssh stdio, so D5's point (a daemon keeps the app; never a one-shot ssh xpra) holds by construction; a bootstrap that fails (no node there, ssh down) is `host_unavailable` naming the reason, at the latest after the 8 s connect deadline. `host_needs_daemon` on a real handle is the client's capability gate (an agent that predates desktop-serve, < 1 s, test-desktop-remote §6); desktop-access's "no `desktopServe` method" arm is a belt for stand-ins. browser-access (the precedent) behaves the same and its kb entry now says so.

**§8-7 measured (2026-09-25).** The paired test box (Ubuntu 24.04 noble, node 20; apt's candidate xpra 3.1.5 < 5 ⇒ D6's xpra.org noble repository, 6.5.3 + xterm + xvfb installed with its passwordless sudo): ssh connect + hello 1738 ms, `facts` 191 ms, the launch op 13 ms, launch → ready 2176 ms, the ws through the hub's loopback forward opened in 19 ms, xpra's hello 535 ms (531 ms on the loopback fake device — xpra's own hello work, not the link), first draw 126 ms, **xpra ping → ping_echo through the forward 7 ms (7 ms on all five)** — the input path's round trip; "abc" (three key presses) landed in the file the app writes on the device (≤ 599 ms upper bound, including one ssh file read per poll of the harness); `windows` answered the app's own row, stop ⇒ exited, nothing on the device still carries the session marker. On the loopback fake device (test-desktop-remote): keystrokes → file 11 ms. As §7.1 estimated: the forward adds the link's RTT and nothing else — bytes are relayed, never parsed.

**SHIPPED (lane C2, 2026-09-25, 2.369.178).** Landed: ① the bridge: `src/server/desktop-stream.js`'s ONLY change is `streamEndpointFor(target)` — a target on this machine answers `127.0.0.1:<port>` synchronously (the pre-C2 path, byte-identical), a target carrying `hostId` answers the hub forward's loopback port (`forwardPort` = desktop-access.forwardPort, one reference per socket, released once when it closes; no forward wired ⇒ 502 by name); the x5 seats, backpressure, heartbeat, named closes and the clipboard / input policy are untouched. ② the hub registry (`src/server/desktop-app-keeper.js`): a paired machine's record is the device's (D8); the hub keeps what it last saw in `data/desktop-apps-hosts.json` — NEVER inside `data/desktop-apps.json` (the local machine keeper owns that file whole and would judge a remote record by this machine's pids and reap it); launch / stop / keep-alive / fit / windows / relaunch all go through the op (the hub's settings carried); a launch is followed every 400 ms until it settles; the policy stays the hub's: the idle verdict from the input the bridge reported (a hub-side `lastInputAt`, persisted), the resource REPORT of the device's own sample (judged once per sample, report only), the x5 seat. ③ routes / UI: `host` is no longer refused (`hostParam`: '' / local ⇒ the exact pre-C2 calls); `GET /api/desktop/machines` (a row per machine drawn WITHOUT a connect ladder: ready / connect / offline / host_needs_daemon / no_x11 — greyed, never hidden); the launch dialog's "Run on" row, each machine's own catalog / ladder / cap; the window title "{app} — on {machine}" + a host chip (the host name is a display string, never a spawn input). ④ auto seamless setup: `facts {install:true}` answers the install facts (distro, the codename with `UBUNTU_CODENAME` first, apt's candidate, `sudo -n`, the installed xpra); PURE `xpraInstallPlan` gives the plan first (apt ≥ 5 ⇒ the machine's own sources; else xpra.org's repository + key, pinned 6.x, with xpra-x11 / xpra-html5 named — they are RECOMMENDS of xpra-server there); it runs through the agent's existing `run-stream` op (`sudo -n sh -c <plan>`, stderr folded in, the log streamed into the dialog as NDJSON); no passwordless sudo ⇒ `no_sudo` by name with the commands to copy; macOS / Windows ⇒ `no_x11`. The fleet: the image only — deploy/docker/Dockerfile now installs xpra.org's bookworm 6.x, pinned (D7; no image was built by this lane). ⑤ boot: this machine adopts first, then IN THE BACKGROUND every machine the registry remembers and every dialed-in one is asked `list` and adopted by host + id; a machine that does not answer keeps its records as `unknown-host-offline` (a view state; the window stays open), re-asked every 30 s by the tick; a machine no longer on the instance (unsupported-host) has its records dropped. Gates: test-desktop-serve §6–§10 (fast), test-desktop-remote §7 (real daemon: the hub keeper + the real bridge + a real xpra hello + keys into the file + offline 404), test-desktop-xpra-window §13 (real chrome: a scratch daemon paired to the worktree server as a person pairs one — the machine picker, the device's ladder, the launch there, the window through the hub forward, the pane covered, the title naming the machine, keys landing in the file on the device, Stop closing the window).

**Deviations from this section (C2, named one by one):** ⑧ the xpra.org repository uses the MEASURED deb822 shape (`URIs: https://xpra.org` + `Suites: <codename>` — this box's own /etc/apt/sources.list.d/xpra.sources), not the design's `https://xpra.org/<dist>/<codename>`. ⑨ the install is not a new desktop-serve op (the table stays nine): `facts` takes an `install` parameter and the run rides the agent's long-standing `run-stream` — an in-process child on this machine, the same argv on both transports. ⑩ ssh-only hosts are not greyed "needs the agent": C1's deviation ⑦ proved an ssh host gets the daemon installed over ssh and is served by it, so the picker shows an unconnected ssh machine as "connects when chosen" (`connect`); what is greyed is an agent predating the op (`host_needs_daemon`), an offline dial device, macOS / Windows. ⑪ capacity is still each machine's over its own sessions (C1 deviation ①); the hub's `liveRecords()` counts this machine only (the browser keeper's ONE ceiling and the window-targets engine live here). ⑫ a remote app is NOT an agent window target (the engine gets `keeper.local`: xdotool / AT-SPI act on this machine). ⑬ a hub without xpra serves the hosted html5 client from the DEVICE, read through its agent (`fsReadRange`, a closed set of file kinds). ⑭ an anonymous socket (no `?viewer=`) is still a read-only watch seat — the real-device leg's test client had to name itself to type (the existing x5 rule, not a defect).

**§8-7 measured · C2 (2026-09-25, the paired test box, dial pairing to a scratch hub).** The scratch hub = the lane's worktree server on this box (loopback only, scratch HOME + data/); the device = the lane's built agentd bundle under a scratch HOME / root on the box, paired as a person pairs one (`POST /api/device/dial-pair`; the daemon dials `/api/device-dial` through `ssh -R <rport>:127.0.0.1:<hubPort>`). Paired + dialed in 0.8 s; the install plan 0.36 s (noble; apt's candidate 6.5.3-r0-1 — the xpra.org source C1 added — xpra 6.5.3 present, `sudo -n` present ⇒ plan `source: apt, already`, only xterm / xdotool / xauth / xvfb); the install rung really ran through `sudo -n`, 27 log lines streamed, 3.2 s; the device's ladder = xpra 6.5.3; the launch op 14 ms, launch → ready 1.84 s; the ws through the hub bridge + forward opened in 4 ms, xpra's hello 552–622 ms, first draw 62–98 ms; **xpra ping → ping_echo over the whole path 7–8 ms on a link whose TCP round trip is 6.4–8.9 ms**; **a key press to the frame drawing the app's echo 11–18 ms**; "abcde" landed in the file on the device; `windows` answered the app's own row; Stop ⇒ exited, nothing on the device carries the session marker; cleanup: the scratch daemon stopped, the scratch dirs removed, the hub's processes swept by evidence (xpra left installed). **One trap measured on the way:** the same path through a `ssh -N -R` tunnel (no pty) round-trips a ping in 53–56 ms — the extra ~47 ms is Nagle + delayed ACK: OpenSSH sets TCP_NODELAY only on an interactive (pty) session; adding `setNoDelay` to the daemon's ws client (ws-min) changed nothing in this topology (measured), a `-tt` tunnel with a remote command brought it back to 7–8 ms. That is the harness's tunnel, not a product path; the product's only `ssh -R` use is a remote agent reaching the hub API (out of this lane, unmeasured). A device dialing directly over the internet uses ws-min's own socket without `setNoDelay` — whether that adds latency is UNMEASURED.

**C2 verify fixes (2026-09-25, five findings, each reproduced first).** ① a per-click op (relaunch / stop / keep-alive / windows) on a paired machine whose link dropped MID-request waited out desktop-serve's own 60 s timeout before its 503 (measured 60 004 ms on a real daemon killed 103 ms into a relaunch, while the device re-dialed in ~110 ms) — the agent client now fails every in-flight request the instant its link dies (`mux.onDead` rejects `conn.pending`, code `link_lost`): 503 `host_unavailable` in milliseconds. ② a viewer that half-closed during the forward's setup leaked a forward reference (ws drops such a handshake without its callback) — the reference rides the socket's own close too. ③ + ④ the install rung: a remote run past 2 minutes was reported "exited 1" (run-stream's fixed 120 s belt) while apt kept running, and the per-machine lock was released — a second click could start a second apt. The belt is now per op (the install passes `installMs + holdMs`), a deadline is `install_timeout` by name, a lost link `install_link_lost`, and the child is **never killed** — deviation ⑮: the finding suggested killing it; it is apt/dpkg as root under `sudo -n` (this user cannot signal it; `sudo` relays SIGTERM to the shell only, apt runs on) and a dpkg interrupted mid-unpack needs `dpkg --configure -a`, so the install is WAITED OUT instead: the machine's install slot (owned by desktop-access now, not the route) stays held until the child is gone (the local close / the device's stream-exit), at most 60 min; a second install meanwhile is `busy`. ⑤ the `sudo -n true` probe itself is now gated (a fake sudo on a scratch PATH, a no-probe copy as the control). Gates: test-desktop-serve §6/§8/§9/§10, test-desktop-remote §8 (real daemon: SIGSTOP with the op pending, SIGKILL ⇒ answered in ms; the pre-fix client copy waits out the timeout).

**C2 verify r2 fixes (2026-09-25, five findings, each reproduced red first).** F1 (critical) a viewer whose connection RESET while the paired-machine forward opened took the whole hub down: the socket waiting on the forward had no error listener (node's http server drops its own at 'upgrade'; ws adds one only when it takes the socket), so the reset was an `uncaughtException` ⇒ `process.exit(1)` (measured: 0 listeners, uncaught ECONNRESET; the gate's legs were FINs). The bridge listens before the park and hands the listener to ws at the upgrade; the CDP mediator had the same shape (and left the upstream open when its client left first) — fixed too. F2 a run-stream child whose HUB link died stayed paused forever (a 4 MB producer frozen at ~270 of 1000 lines, still frozen after the re-dial): the daemon resumes every orphaned child into `~/.vibespace/run-stream/<pid>.log` (capped at 8 MB) and it runs to its end. F3 + F4 the install rung — see deviations ⑯ ⑰. F5 the paired-device suites found the capability by the list's LAST entry; they find it by content now, and test-architecture §56 keeps every suite that way. Gates: test-desktop-serve §8 / §9 / §10, test-desktop-remote §0 / §6 / §8, test-browser-mediation ③b, test-architecture §56 — each fix with a mutant-copy control.

**Deviations (verify r2, named):** ⑯ the xpra install is no longer a CHILD of whoever started it (the hub process here, the daemon's run-stream on a device — a hub restart / Update / OOM or the daemon's re-exec closed its pipe and killed the install's shell and dpkg at their next line: measured rc 141): it runs DETACHED on the machine — its own session (`setsid`), stdin /dev/null, its output appended to `xpra-install.log` in the machine keeper's data dir, a pidfile `xpra-install.pid` ("<pid> <starttime>") and an exit file beside it — and the hub only FOLLOWS the log (`tail --pid`). The dialog's streamed log is that follow. ⑰ "one install per machine" is the machine's PIDFILE, not the hub's memory: the facts op reports `installing: {pid, since}` while the recorded pid + starttime lives, so a restarted hub (or a re-created access layer) asked to install RE-ATTACHES — "Still installing on <machine> — re-attached", its log from the start — instead of starting a second apt; a hub that stopped following an install (install_timeout, install_link_lost) keeps its slot (`busy`) until the machine's facts say the install is gone (at most 60 min; the line that frees it names what it saw — never "ended" on a lost link alone). ⑱ F2's orphaned run-stream output goes to a per-pid log on the machine; the verifier's other option — a NEW connection re-attaching to a running stream by id — is not built (it would be a new op with its capability; the one stream that needs following, the install, is detached and re-attached by ⑰).

**Verify r2 lows (not fixed, recorded 2026-09-25):** L1 the hub registry: a status/list answer whose snapshot predates a launch/relaunch answer prunes the NEW record for one tick (stub-proven; not reachable on one FIFO link) — fix when needed: a per-host request epoch (`sentAt` vs `hub[id].ingestedAt`). L2 a late op answer resurrects a removed machine's row in data/desktop-apps-hosts.json until the next boot — fix: `ingestOne` / `ingestHost` refuse a host `hostKnown` no longer knows. L3 the plan that RUNS is recomputed from fresh facts, not the object the dialog showed (integrity only: the codename is CODENAME_RE-gated, the route cookie-only, no device text through innerHTML) — fix: echo the executed `plan.script` sha256 in the NDJSON done line and let the dialog compare. L4 two hubs driving one machine: a hub's idle verdict never sees the other hub's input (code-derived) — fix: mirror `lastInputAt` to the device through a note-input op.

**C2 verify r3 fixes (2026-09-25, four minors + three lows, each reproduced red first).** M1 the install launcher, after a pid wrap, could follow the OLD pidfile's pid (another live process) when the new install ended before its first poll — the hub then waited out its 15-min deadline for an install that had finished in milliseconds (measured: a launcher hung past 6 s beside an exit file saying 0); it now reads only the new pidfile and follows only a pid whose starttime it just verified. M2 an orphaned run-stream child paused at the cut stayed paused when its log could not be written (a Writable that errors never emits 'drain': /dev/full ⇒ frozen at line 117 of 4000); a pause now lifts on drain OR error OR close, and an errored log is a pure drain. M3 a CDP lease revoked (or its browser restarted) while an agent's client was parked on the upstream open ended up a live connection in no grant's set — revoke, repoint and shutdown could not reach it; the park is the grant's now and is re-checked at the open (503 by name). M4 two hubs starting the install inside the 5–20 ms before the runner's pidfile both started a runner (2 of 2, 5 of 5); the slot is claimed atomically. Lows: L5 a machine without `setsid` is refused at once by name (was 10.5 s, cause lost); L6 the orphan log's 8 MiB cap is per file (a pid wrap appended another 8 MiB: 15,106,486 bytes); L8 an unreadable /proc is refused by launcher and runner (never a "<pid> " pidfile nothing can verify); **L7 (shutdown with a client parked ⇒ an uncaught TypeError on the nulled wss) is covered by M3's check** (`closed` ⇒ 503, and shutdown terminates the parked upstream).

**Deviations (verify r3, named):** ⑲ the machine's install slot is a LOCK DIRECTORY `xpra-install.lock` beside the pidfile (r2's ⑰ read the pidfile, which the RUNNER wrote after the launcher's check): claimed by the launcher with `mkdir` before it spawns, its `owner` file "<pid> <starttime>" (the launcher's, then the runner's), removed by the runner after the exit file; a lock whose owner is PROVEN gone (no /proc entry, or a readable starttime that differs — an empty probe proves nothing) is broken by the next claimer (once per dead owner, under a keyed `mkdir` mutex), an ownerless one after 2 s; a `start` that loses the claim FOLLOWS the winner's install and answers its recorded exit. ⑳ an orphaned run-stream's log that cannot be written is a pure drain — the output is dropped, the child is never paused on it — and the 8 MiB cap counts the file's existing bytes (a full file from a previous process with the same pid takes only marker lines). ㉑ a CDP client parked while its grant moved is REFUSED (503: Lease Gone / Browser Restarting / Browser Stopped / Server Shutting Down), not re-dialed to the new upstream — the same url reaches the current browser on the client's retry.

**C2 verify r4 fix (2026-09-25, one major, reproduced red first).** Two concurrent `start`s could still run two xpra installs: ⑲'s break rule removed a lock that was ownerless only because another claimer was between its `mkdir` and its owner rename (measured on the r3 launcher: 1–4 in 150 starts over an empty dir, 12/20 and 17/20 trials multi-runner with that rename slowed 30 / 50 ms). The slot's lock is now one the kernel holds — deviation ㉒. Gates: test-desktop-serve §10 (40 natural + 20 widened trials of five concurrent starts ⇒ one runner each; a killed install's lock released at once; a killed launcher's runner still holding it; CONTROL `flock -n 9` → `true` ⇒ 5 runners), test-desktop-remote §8.

**Deviation (verify r4, named):** ㉒ the install's mutual exclusion is `flock` on a lock FILE the running install holds, replacing ⑲'s lock directory, owner file and break rule: the launcher opens `<T>/vibespace-xpra-install-<first 12 hex of sha1(realpath of the state dir)>.lock` as fd 9 (`T` = `/run/user/<uid>` when it is a writable directory this user owns, else `/tmp/vibespace-<uid>` created 0700 — named by the uid, never the environment since verify r5) and takes `flock -n` before it spawns; the detached runner shell and apt-get inherit the descriptor (apt-get closes inherited descriptors for the children it forks — APT::Keep-Fds — so dpkg never holds it; a dpkg outliving both is fenced by dpkg's own lock), the launcher closes its own copy, so the lock lives exactly as long as the install and the kernel releases it when the last holder exits or is killed: nothing is ever broken by hand, nothing goes stale, no owner identity. It is on LOCAL storage on purpose — the state dir (a device's ~/.vibespace, the hub's data/) may sit on NFS or a FUSE mount, where flock is emulated or absent; /tmp and the runtime dir are local and cleared by a reboot, exactly the slot's lifetime (the machine's). A start that finds it held follows the running install (the pidfile, as before); a lock that cannot be taken at all is refused by name (exit 125), never answered with the racy scheme. util-linux `flock` joins `setsid` as a requirement (checked first, 125 naming util-linux). The pidfile and the exit file record the lock's path, and the facts op's `installState` reports it (`installing.lock`, `lastInstall.lock`).

**C2 verify r5 fixes (2026-09-25, four lows — the lane's last round, each with its pin and control).** L1 a start in the SAME second as an earlier install's exit, while a child that install left behind still held the lock, answered that stale exit instead of running (the verifier: 6 of 6) — the exit file's instant is nanoseconds now and a start answers it only when it is strictly newer than itself (CONTROL: the whole-second `-ge` launcher answers the stale 100). L2 (wording) the lock is held by the runner shell and apt-get, never dpkg (㉒ above). L3 the lock's directory was `$XDG_RUNTIME_DIR` or `/tmp` — two spawners with different environments named two locks for one state dir, and the `/tmp` name was predictable; it is `/run/user/<uid>`, else `/tmp/vibespace-<uid>` (0700), refused by name when that is not a directory this user owns (CONTROL: the r4 line names two locks for one state dir across three environments). L4 a runner started without the lock (fd 9) was reported only after the launcher's full 10 s pidfile wait; the wait is bounded by the runner's own life (125 in ~0.2 s, naming the lock; CONTROL: still waiting at 3 s). Gates: test-desktop-serve §10, test-desktop-remote §8.


### 3.6 Lane E (2026-09-25): a window is SHARED with agents, never assumed — reach, tree or pixel mode, the request

**The owner (2026-09-25):** "浏览器窗口不是也应该能给agent操作吗？为啥你说不让？另外可能默认不要让agent能看到所有窗口，而是创建前和创建后能选择把窗口暴露给哪些agent或者group，或者直接从一个窗口能发起让某个特定agent控制的request。话说我们的设计下多个窗口是可以同时被多个agent操作的吧？" — and at 19:05: "我理解任何窗口发送给agent的时候应该都要有个选项可以切换无障碍还是像素模式。像素模式是最接近用户本人操作的方案作为兜底。"

**The decisions (defaults named to the owner, built as named):**
- **D1 hidden by default.** A desktop-app window is invisible to every agent; `vibespace-window list` shows nothing the user did not share; every verb on it is refused `not_exposed` by name. ONE exception: a window an agent opened itself (`vibespace-window open`) is shared with that agent's session only.
- **D2 who, and when.** A share names live agent SESSIONS (by their durable conversation key — `<backend>:<conversation id>`, `webui:<id>` before one exists, server.js `sessionStatusKey`'s spelling, so it survives a restart / resume of that conversation) or whole TASK GROUPS (every session in the group, now or later — membership is asked at every verb). Chosen BEFORE launch (the Desktop apps dialog's "Share with agents" row, remembered per app in user state `desktopAppReach` under the desktopAppFrame key) or AFTER (the window's ⋯ and its title-bar / taskbar menu → "Share with agent…"). Revocable any time; a revoke while an agent holds the window ends its lease at once — its next verb answers `not_exposed`.
- **D3 the request.** From a window: "Ask <agent> to take control" — VibeSpace sends that agent a message naming the window (label, handle, the mode, the road, the user's optional line quoted as a note). FREE by default: the delivery ladder's stash, i.e. the agent's next turn. "Wake it now" (off by default) = a billed turn through the ladder's gated `deliverToConversation` under the declared spend reason `window-share-request` (a refusal — the unattended-turn budget, fail closed — or no live lane falls back to the next turn and says why; a UX floor of one wake per conversation per 30 s). The request GRANTS the window to that agent first. **The open point, as built:** when ANOTHER agent holds the window the dialog says so and offers "End <name>'s hold on this window" (checked) — the user's intent is "this agent controls it".
- **D4 browsers are windows too.** A Chrome / Firefox the USER launched from Desktop apps is a target like any app once shared (marked "[the user's browser]"; an agent's own web work stays `vibespace-browser`); `open` of a browser row by an agent (and `url` / `keepProfile`) stays `browser_is_human`. The browser row starts WITH its accessibility switch (chromium `--force-renderer-accessibility`; firefox `GNOME_ACCESSIBILITY=1` in its env) — always, because a share after launch needs it and turning a11y on at runtime would flip org.a11y.Status for the user's whole session.
- **D5** the real-desktop consent switch (`window.realDesktopTargets`, tier 3) is unchanged and orthogonal — that class never reads a share.
- **D6** one holder per window; any number of windows held by different agents at the same time (each app has its own display — injection on one never reaches another). Stated in the manual and pinned.
- **D7 the mode.** Every share carries `auto` (default) | `tree` | `pixels`, chosen in the picker and switchable any time (a switch takes effect at the holder's next verb, audited `mode-changed`); the window's chip names it ("Shared with 2 · Pixels"). `pixels` = the closest thing to the user by hand: `screenshot` (the window's own image + its size + origin + scale) and `click --at` (a pixel of THAT image), `type` (keys), `key`, `scroll` (new); the tree verbs (`snapshot`, `click @ref`, `type @ref`) are refused `mode_pixels` with the owner's sentence. `tree` = everything (the pixel verbs are the agent's own fallback). `auto` resolves at attach (one probe; a young app re-asked for up to 3 s) and at every snapshot: tree when a node the agent can act on answers, else pixels, and `list` / `attach` say which and why ("no accessibility tree — pixel mode").

**The model.** PURE src/window-reach.js (the reach record per window `{windowId, mode, rows:[{principal, grantedAt, by: user|request|self-open}]}`, one row per principal, revoke drops only its own row, widen-only, `reachFor` = MAX with the deciding row named; the mode table `verbGate`; the pixel plan / visibility / point mapping; the request text; the picker; the launch memory). The window-targets engine holds the store (data/window-reach.json, atomic 0600, carried to a Scale ▸ successor, pruned when the window ends; broadcast `window-reach-updated`), the reach gate on every verb, the mode resolution, the pixel road. The user routes (cookie-authed, human-only — an agent token is `agent_forbidden`): `GET/POST/DELETE /api/desktop/apps/:id/reach`, `PUT …/reach/mode`, `POST …/reach/request`, `share` on the launch. A paired machine's window is no agent target (`share_local_only`).

**Measured (the lane-E reader + the builder, 2026-09-25, xpra 6.5.3, at-spi 2.60.4, Chrome 153, GNOME Calculator on GTK4, a private session bus):**
- **Chrome WITH `--force-renderer-accessibility`:** a snapshot reaches the PAGE inside the default 600-node budget — 235–237 nodes in ~50 ms; the page appeared ~0.9 s after the record was ready (attach's auto probe resolved `tree` in 825 ms); the button "Press me" = role `button`, actions `[press, showContextMenu]`, `click @ref` ⇒ the page reads "pressed 1" with NO viewer; a page text field = role `entry`, state `editable` but NO EditableText (census `editableText 0`) ⇒ focused through the tree (`Component.grab_focus`), then typed as keys ⇒ "typed xyz" (needs a viewer); `scroll down --by 5` ⇒ scrolled ~600 px (120 px a notch). **WITHOUT the flag (the control):** 4 nodes — the application + 3 frames, children unreadable — and no page in 30 s; auto resolves pixels "its accessibility tree is closed".
- **GNOME Calculator (GTK4):** 107 nodes in ~0.5 s, every button with `click`; `click @ref` works with no viewer. GTK4 reports every node's SCREEN extents as 0,0 (WINDOW-relative extents are right: "7" at 24,428 64×44 logical) — tree bounds are never click coordinates.
- **xterm:** not on the accessibility bus at all ⇒ auto resolves pixels "no accessibility tree".
- **The xpra rung without a client:** the main window is `IsUnviewable` — a root grab AND a window grab are black, `click --at` answered ok but did nothing, `key` was lost, AT-SPI `grab_focus` returned True with no effect. With a client attached the window's OWN X window has real pixels (its alpha bytes 0 — forced opaque), while the ROOT grab (the pre-lane-E screenshot) stays black (xpra composites offscreen). The first client resized the root from 4096×2304 to its own 1920×1200 (a 2264-px-tall Chrome window then extends below it ⇒ `outside_window` by name). A headless stand-in viewer costs ~171 MB PSS (Xvfb 56 + client 115).
- **Coordinates:** `click --at` is a pixel of the window's own image (device px at the app's scale): the calculator at scale 2 — "7" at (86,876) / (112,900), at scale 1 (43,438); the main window at (0,0) for the calculator, (20,20) for Chrome — the origin is read at act time, never assumed.
- **Defects fixed on the way:** `xdotool mousemove --sync` to the pointer's current position hung 7 s (a second click / key on the same spot failed `inject_failed`) — the moves are unsynced now; a `--button` 4..7 was coerced to a LEFT click — refused by name, the wheel is `scroll`; typed keys with the pointer outside xterm's window were lost — the pointer rests inside the main window before keys / typed text.
- **D6:** two agents on two windows at once (a tree click on the calculator + typing into xterm) — both landed; a cross act ⇒ `not_attached`.

**Gates:** test-window-reach (fast — the 30-cell reach table, the 72-cell mode table, the pixel plan on the measured geometry, three patched-copy controls), test-window-targets §4–§5 (fast — hidden by default, the browser legs `not_exposed` until shared, the request over a stub ladder, the human routes, the STATUS census), test-window-target §4 (heavy — the real xpra rung under a private session bus: every D1–D7 leg with the calculator, xterm and Chrome, the real ladder with the real spend guard refusing the wake at an owner cap of 0), test-desktop-app-window §E (heavy — the chrome UI).

**Honest limits:** Firefox's `GNOME_ACCESSIBILITY=1` is unmeasured (the box's firefox is a snap, which cannot open a profile in a scratch dir); the CPU cost of Chromium's renderer accessibility on heavy pages is unmeasured; the mode resolution is in memory (re-probed after a restart); a window on a paired machine cannot be shared (lane C's engine addresses this machine's windows only).

**Lane E verify (2026-09-25) — the adversarial verifier's two findings above low, fixed (each reproduced red first):**
- **MAJOR — the launcher's row stated the opposite of the click.** Untouched, the "Share with agents" row read "Hidden from agents (each app remembers its last choice)" while the launch applied the app's REMEMBERED share (the lane's own heavy suite pinned exactly that). D2 says the next launch PROPOSES the same share — a proposal is seen before the act. Now the words are derived from the rule the click uses (PURE `launchSummary` over `launchShare`): untouched, the row reads "Each app launches as you last shared it" whenever any app remembers a share; each catalog card whose app remembers one carries a chip ("Shared with Ops · Pixels"); a launch that applied a remembered share is toasted with where to change it. Gates: test-window-reach §7b (the 50-cell table + a control that says "hidden" when untouched), test-desktop-app-window §E (the chip + the row's words before the second launch, the toast after it).
- **MINOR (a reach leak) — a revoke did not stop a verb already in flight.** Reach was checked once, before the verb's awaits (an `auto` probe of up to 3 s, the helper's snapshot, the input probe, the plan, the grab); a revoke landing there still injected (`click --at`, `click @ref`), handed the tree over (`snapshot`) or answered "attached" with the lease gone (`attachWithMode`) — reproduced with a 400 ms fake helper and a revoke 100 ms in. Now `stillHeld` re-asks the whole decision (window live, switch on, reach, the lease, no takeover) immediately before every act and every hand-over; a verb is linearized before or after a withdrawal, never across it. The same re-check covers a Task Group left, a takeover and the real-desktop switch going off mid-verb. Gate: test-window-targets §5 (seven race legs + a patched engine copy without the re-check). Incident essay: docs/kb-bugfix-invariants.md.

**Lane E verify lows (not fixed, 2026-09-25)** — recorded for a later round, each with the verifier's proposed fix:
1. **The pixel origin follows the LARGEST window.** `pixelPlan` picks the main window by area, so a popup / dialog LARGER than the main window moves the coordinate origin between a screenshot and a click (a 100 px-off click with no refusal). Measured on Chrome 153 its popup (320×274) stayed smaller than the main window (2033×2284), so it needs an oversized dialog to bite. Proposed: remember the main window id per handle while it stays mapped (re-pick only when it is gone), and refuse `outside_window` with a note when the origin changed since the last screenshot.
2. **The 30 s wake pacer is in memory only.** A server restart between two "Wake it now" clicks allows a second billed wake inside the floor (the spend guard's per-identity hour cap is the only ceiling then). Proposed: stamp the last wake on the reach record, or read the spend journal's last `window-share-request` row for that conversation at create().
3. **"End <agent>'s hold" ends whoever holds the window at SEND time.** The dialog captures the holder when it opens; if another agent took the lease before Send, the request ends THAT agent's hold. Proposed: send `endHoldOf` naming the holder the dialog showed; end only when it still holds, else answer `holderChanged` and say so.
4. **Human-only share routes gate agents by the Authorization header only.** With password auth OFF, a local caller that omits its token is not stopped (the existing reset-credit / inbox-reply pattern — with auth on, the cookie gate refuses it). Proposed: a positive human witness (a per-page nonce) on every human-only route, reset-credit and inbox-reply included, in one change.
5. **Unreproduced: a URL typed into Chrome's omnibox after `key ctrl+t` did not navigate** in one real run (the tab strip showed a New Tab; neither page's heading in the tree; the page-field `type @ref` leg passes). Suspicion: `focusedNode` picked a node other than the omnibox. Proposed if it reproduces: prefer the omnibox entry after ctrl+t when no ref is given, or teach `type @<omnibox ref>` in the manual.
- **An observed gate flake, not a verifier finding (2026-09-25, recorded for the next diagnosis):** test-window-targets §5's pixel-road leg on the real GTK fixture threw `windowShot needs a window plan` (an uncaught throw — the whole suite exits) in 5 of 11 runs between 15:14 and 15:23 PDT — 4 of 7 with these fixes, 1 of 4 on the unfixed lane-E base commit, so it predates them — while another lane's FULL heavy tier ran four parallel lanes on the box; at the throw the suite's own display answered `xwininfo -root -tree` with NO windows (the fixture gone, or the display number re-served by another X server). 0 failures in the 10 runs after, including 6 run three at a time. Cause not found; if it recurs, capture the fixture's and Xvfb's exit (code + signal) first.

**Lane E verify r2 (2026-09-25) — the second adversarial round: no major, two minor + five low; four fixed (each reproduced red first), three recorded below.**
- **M1 (minor) — the re-check was pinned per FUNCTION, not per SITE.** Four single-site mutants — no re-check before the scroll, none in the typed-keys path, none before the app-window screenshot's answer, none before the tier-3 screenshot's answer — passed every behavioural leg of test-window-targets (the verifier: 244/0; reproduced here on a copy of the tree) (the product was right at all four; the suite could not tell). Now the race legs are DETERMINISTIC (the user's act fires at the start of a named await) and a PER-SITE CENSUS grep-derives every `held()` / `heldOrUnlink(` / `stillHeld(` call in the engine, replaces each alone with the unchecked lease in a patched copy and requires ≥ 1 leg red (14 sites; click @ref's is annotated `// no-await-before` — redundant by construction, its claim checked in the code). New legs: the scroll, pixel-mode type, type into a field editable by state, the window-plan screenshot, the tier-3 screenshot with the switch going off.
- **M2 (minor) — the launcher named a stale share.** The card chip and the launch toast named principals straight from the per-app memory ("Shared with alpha, Ops (old name) · Auto" for a session that had ended and a group that was deleted — the launch writing rows nobody matches; the memory is rewritten only when the row is touched). Now the words read the picker's roster: "alpha (not running now)", "Ops (not in the list now)" (the picker's own words), a renamed group by its current title; the launch's audit counts the unmatched rows. Gates: test-window-reach §7b (roster leg + control), test-desktop-app-window §E (the remembered group deleted before a third launch).
- **L4 (low, fixed — fail-closed direction) — a Task Group store that THROWS read as "not a member".** The verb answered `not_exposed`, the lease was dropped and audited "exposure revoked by the user"; a revoke of an unrelated principal during the fault dropped a group-held lease too. Now `reach_unreadable` (a new closed code, 503): the verb is refused by name, the lease is kept, audited by:store; `list` counts the undecidable windows; `detach` always works.
- **L5 (low, fixed) — a takeover did not cancel the act already running.** `type` (12 ms a character) kept injecting up to 20 s after a takeover or a revoke, interleaving with the user's keys; `TYPE_MAX` 4000 could not finish inside the fixed 20 s. Now every injection is the LEASE's cancellable child (killed by its own handle on a takeover / a dropped lease) and the verb answers `window_paused` / `not_exposed` with `did.partial`; `TYPE_MAX` = 1500 and the timeout scales with the text. **Measured on the way:** a SIGKILL between a key's press and its release leaves the key HELD and the server autorepeats it (37 presses in 1.5 s, "qqqq…" into the entry) — so every killed injection (cancel or timeout) first RELEASES what it may hold through XTEST (unmapped keysyms skipped for free; a release of a key not down is dropped). Incident essay: docs/kb-bugfix-invariants.md.

**Lane E verify r2 lows (not fixed, 2026-09-25)** — recorded for a later round, each with the verifier's proposed fix:
- **L3 — the untouched "Share with agents" popover shows an EMPTY picker** while the row says each app launches as it was last shared (the picker starts from the dialog's own blank choice, not from any app's memory). Proposed: a first line in the popover explaining that an untouched row keeps each app's remembered share and that a choice here overrides it for this launch.
- **L6 — an Advanced (exec) or Recent launch applies a remembered share with no chip before the click** (the chips decorate the catalog cards only). Proposed: decorate the Recent rows the same way, and caption the Advanced Run button with the share the command's key remembers.
- **L7 — the card chip clips the MODE behind a long zh / ja group name** (a 40-character Task Group title fills the chip's two lines; the tooltip still has it). Proposed: put the mode first ("Pixels · Shared with …"), or show one name + "+N".
- **An observed gate flake, not a verifier finding (2026-09-25 17:2x PDT, verify r2 fix run):** test-desktop-app-window's LAST leg — the singleton Desktop window's status chip settling within 15 s ("no VNC server installed" on this worktree server) — answered `null` once in three runs (the first, directly after test-window-target on a box at load ≈ 3.5); both re-runs green end to end. The leg and its code are untouched by lane E; recorded for the next diagnosis (capture the chip's text at the deadline first).

**Lane E verify r3 (2026-09-26) — the third adversarial round: one minor + four low, ALL fixed (each reproduced red first); nothing moves to "not fixed".**
- **F1 (minor) — the r2 cancel was keyed on a lease OBJECT a re-attach replaced.** `injecting` registered on the lease `requireLease` returned at the top of `act()`; a detach + re-attach by the same session (or the user's revoke + re-share + the agent's re-attach) during the verb's probe put a NEW object in the lease map — the re-check passed on it, the takeover's cancel looked at it and found nothing, and the type ran to its end through the takeover (`ok` after 403 ms, nothing killed). Now running acts are kept per WINDOW HANDLE; a takeover or a dropped lease cancels every act on that window. Gate: test-window-targets §5 (two re-attach legs; control = the r2 object-keyed act). Incident essay: docs/kb-bugfix-invariants.md.
- **F2 (low) — a TIMEOUT-killed injection's `partial` never reached the caller.** The primitive said `partial: true` + `released`, the engine threw without `did`. Now `did` rides every unfinished injection's refusal and audit line (route 502 + `did`, the CLI's "STOPPED PART-WAY"). Gate: §5 forced-timeout leg through the real primitive + route + CLI + control.
- **F3 (low) — non-ASCII pixel `type` needed a UTF-8 locale the display env did not guarantee.** Measured on this box's Xvfb with no LANG: `type "ab中文é"` typed "ab", then xdotool failed "Invalid multi-byte sequence encountered" with no `partial`. Now a non-ASCII text runs under the UTF-8 rule desktop-display.js already applied to xwininfo (`LC_ALL=C.UTF-8`, else the env's own UTF-8 locale, verified by `locale charmap`); where none loads, `no_utf8_locale` (503) names the first such character before anything is typed. A type xdotool gives up on after it started answers `partial` with its own message (never the command line, which holds the text). Gate: §2b over fake `locale` answers, §3 on the real Xvfb with LANG stripped + a no-rule control.
- **F4 (low) — `list` handed over a revoked window's row one await late.** It now re-asks reach after its last await (`stillListed`, the per-site census's 15th site). Gate: a §5 race leg.
- **F5 (low) — the launch audit counted a Task Group the store could not read as `unmatched`.** A group row that finds nobody while a membership read threw is `undecided` (PURE `reachedState` / `launchCounts`; the share view marks the row `undecided`). Gates: test-window-reach §2c + control (g), test-window-targets §5 + control.

## 4. The trimmed set (build these)

| Build | Content | Decide first |
|---|---|---|
| **A** (one lane, S+S+S–M, needs no decision) | §3.1 the gesture window + the one-time hint; §3.2 app exit ⇒ window closes, dead records open nothing, outer ✕ ⇒ `close-window` (twice = Stop); §3.4 the `DPR × uiScale` derivation + per-window Scale ▸ relaunch + `desktop.appScale` auto = derived | none (D1/D2/D4 at their defaults; the owner objects, we change) |
| **B** (one lane, M) | §3.3 seamless: the PURE verdict + `initiate-moveresize` wired into the outer drag/resize + the hot zone + the suspensions + the per-window toggle + the global off | D3 |
| **C** (two lanes, L) | §3.5 remote: the desktop-serve extraction + desktop-access + the daemon op + the bridge's endpoint + the record migration + the machine picker + the install rung + three suites | D5–D8 |
| Not this round | in-product TLS; Electron/Qt fractional-scale knobs (D9); adopting a forking launcher's real pid; a GTK Broadway rung | — |

## 5. The decisions only the owner can make

| # | Decision | Recommended default |
|---|---|---|
| D1 | Write the clipboard on plain http through the gesture window (within 5 s of the user's own Ctrl+C / click), the chip outside it; a one-time HTTPS hint and which route it points to | **Yes**; one hint per device, dismissable; document three routes (Chrome flag / Tailscale serve / Caddy), no TLS in the product |
| D2 | (a) app exit ⇒ the window closes + a toast; (b) the outer ✕ ⇒ `close-window` to the app first, a second ✕ within 5 s = Stop; (c) `failed` stays; (d) Stop closes the window too | all **yes** |
| D3 | Seamless default: auto on detected CSD; suspensions = agent lease / tab chain / phone; the reveal = top-edge hover 250 ms or Alt; the per-window toggle remembered per app id; the global `desktop.seamless` auto/off | as recommended |
| D4 | The derivation = `DPR × uiScale`, the integer nearest in ratio, the remainder upward into dpi only; 1.5× stays an explicit choice; per-window Scale ▸ relaunch with a confirm | as recommended | **2.369.176:** the browser Scale ▸ rows work too — the keeper stops the old browser, moves its profile dir onto the successor and starts it at the new scale (logins/tabs kept); a live DPI change does not exist on X11 (GDK_SCALE / Xft.dpi are read once at start).
| D5 | Remote scope: paired devices with the daemon only; ssh hosts without a daemon refused by name (no single-file rung) | **Yes** |
| D6 | The install rung: owner-clicked on a paired Linux box, the plan shown first, passwordless sudo required (the distro's apt when ≥ 5, else xpra.org's repo, pinned 6.x); the fleet through the image only | **Yes** |
| D7 | The fleet image's xpra: 3.1.3 (bookworm) is unmeasured — either measure 3.1 in a bookworm container, or switch the image to xpra.org's 6.x bookworm packages so the whole fleet speaks one protocol version | **Switch the image to 6.x** (skips a measurement nobody wants; closes §9-1/§9-4) |
| D8 | Where remote records live: the device holds `~/.vibespace/desktop-apps.json` + the hub's registry (apps survive a hub death and are adopted when it returns) vs hub-only | **Device-held + hub registry** |
| D9 | Electron/Qt fractional-scale knobs (a registry row's `scaleArg`) | **Not this round** |

## 6. What needs the owner

1. Answers to D1–D9 (the defaults count as "agreed").
2. HTTPS: pick one of the three routes (§3.1), or say "the chip is fine, no HTTPS" — lane A does not block on it.
3. Remote: name one Linux device **paired with the daemon** for lane C's real-machine leg (an ssh-only host is not enough — it needs the agent installed first), and whether it has passwordless sudo (decides whether the install rung can be measured there).
4. The fleet: approve bumping the image to xpra.org 6.x (D7), or ask for the 3.1 measurement first.
5. Whether GTK4 shows minimize/maximize in the header bar under xpra — measurable here in a 30 s script, just not measured this round; if wanted, lane B measures it and pins the mapping in the suite.

## 7. Proposed build order

| Order | Lane | Content | Gate |
|---|---|---|---|
| 1 | A | §3.1 + §3.2 + §3.4 (no decision needed; each item's own fast leg + new legs in heavy test-desktop-xpra-window) | test-xpra-client, test-desktop-apps §10, test-window-minsize, test-desktop-xpra-window |
| 2 | B | §3.3 (after D3; after merging with A it runs its heavy suites on the merged tree itself — the 2.369.134 lesson) | test-desktop-seamless (new, fast), test-xpra-client §2, test-desktop-xpra-window |
| 3 | C1 | the desktop-serve extraction + desktop-access + the daemon op + the record migration (after D5/D8; the local behaviour stays **byte-identical** — test-desktop-app-keeper / test-desktop-xpra all green is the extraction's gate) | test-desktop-serve (new), test-migrations, test-architecture's SHARED census |
| 4 | C2 | the bridge's endpoint + the machine picker + the install rung + the real-daemon suite (after D6/D7) | test-desktop-remote (new, real daemon), test-desktop-xpra-window's `host=` leg |

## 8. Unverified / honest limits

1. The gesture window in Firefox / Safari: Firefox has the same 5 s transient activation (per spec), Safari may require a synchronous handler — both unmeasured; the fallback is the chip, never worse than today.
2. Chrome's `unsafely-treat-insecure-origin-as-secure` flag making `navigator.clipboard` appear — documented behaviour, unmeasured.
3. ~~Whether GTK4 draws minimize/maximize under xpra~~ — MEASURED 2026-09-25 (lane B): GTK 4.22 / GNOME Calculator 50 under xpra 6.5.3 paints minimize, maximize AND close; clicking them sends `window-metadata {maximized:true|false}` / `{iconic:true}`, mapped to our maximize / restore / minimize (set semantics), and our restore sends `{iconified:false}` back (xpra answers `{iconic:false}`, the app draws again). Pinned in test-desktop-xpra-window §12 (3).
4. `initiate-moveresize` directions 0–7 from a CSD edge/corner — MEASURED 2026-09-25: GNOME Calculator under xpra (no compositor ⇒ no shadow ⇒ no CSD resize border) sends NONE from its edges or corners; resizing stays our eight handles (kept in seamless). Directions 0–7 / 9 / 10 are still wired (the handles' own resize) and pinned on the fake worker (test-xpra-client §2d).
5. Qt's fractional `QT_SCALE_FACTOR`, Electron's `--force-device-scale-factor` — unmeasured (D9).
6. The fleet image's xpra 3.1.3 protocol — still OPEN (D7 recommends bypassing it).
7. Real latency over a device link — **MEASURED** (§3.5's two §8-7 paragraphs: C1 over ssh stdio 7 ms, C2 over the dial pairing 7–8 ms, a key to its echo 11–18 ms); x5 seat switches across a real link — still unmeasured (the bridge and the seats are unchanged for a remote app, but no switch was driven over a real link).
8. An app that sets `decorations` 0 without its own close button (kiosk / splash) goes seamless automatically — the escapes are always there (the hot zone, the taskbar menu, Alt), but the first time may puzzle; the global `desktop.seamless=off` is the one-switch retreat. SHIPPED with every hatch gated (lane B): the hot zone and Alt (test-desktop-xpra-window §12 (4)/(5)), the taskbar menu (§12 (8)), Settings → Seamless desktop app windows = Off (§12 (8)); a kiosk app's own window closing still closes ours (§3.2).
