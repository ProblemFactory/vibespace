# VibeSpace

> **HOW TO USE (tiered knowledge, 2026-08-13):** CLAUDE.md is the auto-loaded INDEX + LAWS. The detailed operating essays live in `docs/kb-*.md` and are read ON DEMAND:
> **kb-file-structure.md** (per-file essays — read BEFORE modifying a file) · **kb-design-lessons.md** (§1–§17 + review invariants; §-references resolve there) · **kb-features.md** (shipped-behavior reference) · **kb-patterns.md** (cross-cutting coding patterns) · **kb-api.md** (REST/WS reference) · **kb-bugfix-invariants.md** (incident essays) · **docs/history-archive.md** (ancient chronicle).
> **Contract: before changing a subsystem, read its kb entries; when your change alters behavior, update the kb file IN THE SAME COMMIT — the kb files ARE the operating manual, this file only indexes them.**
> **Size law (2026-09-15, owner: "避免 context rotting"): this file is an INDEX — every entry here is a HEAD of ≤ ~300 chars ending in a `⇒ kb-…` pointer; the full essay (invariants, incidents, measurements) lives ONLY in the kb file it points to (`kb-design-lessons.md §18` = the full three-tier routing-table rows + the long laws). Never grow an index line into an essay again: write the essay in the kb file and put one line here.**

## Project Overview
A backend-agnostic web workspace for **coding agents** — manage many concurrent agent CLI sessions with a tiling window manager, file explorer, and code editor. Each backend (Claude Code, Codex, …) is driven through a `BackendAdapter` (`src/adapters/`); the UI, window manager, chat view, and session layer stay backend-neutral. Sessions run via native agent CLIs inside **dtach** for persistence across server restarts. (Project was "Claude Code WebUI" before the 2026-06-22 rename to VibeSpace — references to the `claude`/Claude Code CLI tool itself are unchanged.)

## How to Build and Run
```bash
# One-line install (checks deps, clones, builds):
curl -fsSL https://raw.githubusercontent.com/Member Q/vibespace/master/install.sh | bash

# Or manually:
npm install
npm run build   # esbuild bundles src/client.js → public/bundle.js (~1.8MB)
node server.js  # starts on port 3456 (or PORT env var)
# ./run.sh      # supervised: build once, then respawn on exit (survives an OOM kill)

# PRODUCTION (this machine runs it this way since 2.73.0): systemd USER service
./scripts/install-service.sh        # installs+enables ~/.config/systemd/user/vibespace.service (linger on)
systemctl --user restart vibespace  # THE way to restart after a build — do NOT kill+nohup by hand
./scripts/update.sh                 # pull+install+build+restart in one step (⚙ → Update VibeSpace… runs this in a shell terminal; dtach sessions survive)
journalctl --user -u vibespace -f   # logs (no more /tmp/vibespace-server.log)
# Unit: Restart=always/RestartSec=5 (survives OOM/crash — verified via SIGKILL), OOMScoreAdjust=-500,
# StartLimitIntervalSec=0 (retries forever while the NFS workspace mounts late), **KillMode=process**
# (CRITICAL — the default control-group KillMode killed every dtach session spawned AFTER the service
# migration on each restart, while pre-migration sessions survived; real incident, one session died on
# every restart. Only the node main process may be killed on stop), and baked PATH (node dir +
# ~/.local/bin — systemd's minimal env broke every CLI spawn; real incident). The service does NOT
# build — build at deploy, then restart. dtach sessions survive restarts by design.
```

## Architecture

### ⚑ THE THREE-TIER FINAL FORM (2.319.0) — where a NEW CHANGE goes, and which test gates it

The 2026-08 campaign (docs/design-three-tier.md, R0–R6 + session-brain + closure marathon) reached its terminal shape. Before writing code, route the change:

| The change touches… | It belongs in… | Gated by (run BEFORE commit) |
|---|---|---|
| **Facts about a machine** (discovery, sysinfo, probes, usage walk, transcript parse, ctx sync, writer sweep, shell prelude) | The SHARED module (src/discovery-facts, sysinfo, machine-probes, usage-walker, transcript-service, ctx-sync, writer-sweep, remote-shell) — the daemon bundles it, one implementation runs where the facts live; ssh scripts are FALLBACK RUNGS only | The module's parity suite: test-discovery-interpret / test-sysinfo-op / test-usage-walk-parity / test-transcript-parity / test-ctx-sync / test-writer-sweep / test-remote-shell — a one-sided edit FAILS them |
| **A new device op** | src/agentd/agentd.js handler + capability in the hello-ack + client method. THREE-TOUCH RULE (2.300.0, bit twice): reply carries `op` in the client's id-keyed routing set; unsolicited pushes get their own branch BEFORE prevControl; watches re-arm on reconnect. Reply via `mux.control`, never an invented helper | A real-daemon test (test-sysinfo-op is the template); capability-gate assert (old daemons are never asked — unknown ops HANG) |
| **Live session stdout consumers** (served model, usage odometer, banners, todos…) | `claudeSideEffects` in server.js — ONE implementation fed by the parse AND the device stream through `sbSeenFirst` (parse registers, device fills gaps). Never a new inline consumer in the parse block (= src/server/stdout/claude-stream-json.js since S5 … ⇒ kb-design-lessons §18 | test-session-brain-dark (mid parity is suffix-wise — every normalizer prefixes its own session id) |
| **Session creation/supervision** | The daemon pipe-session path (R6, `agentd.localPipeSessions`) is the FINAL form; dtach is the legacy path that dies by attrition. New spawn features go through buildRemoteExec (remote) / the r6Argv seam (local). … ⇒ kb-design-lessons §18 | test-remote-shell drift guard; test-agentd-session; **test-restore-liveness** (real fault-injected daemons + a real self-upgrading daemon under an end-to-end restore) |
| **A one-shot data migration** (reshaping stores on old→new update) | src/server/migrations.js (this instance) or the DEVICE_MIGRATIONS registry in agentd.js (devices, runs at daemon boot/self-upgrade) — BOTH through the shared runner src/migration-runner.js: ledger-keyed run-at-most-once, failed = retry next boot never block … ⇒ kb-design-lessons §18 | scripts/test-migrations.mjs (157) |
| **A turn nobody typed** (auto-resume's continue, the Stop nudge, Background Work notifications, agent messages, a codex reset credit, a browser control handback — anything that starts a BILLED turn with no per-occurrence owner action) | src/spend-authorizer.js (PURE decision) + src/server/spend-guard.js (ORCH: persisted `data/spend-budget.json`, journal, inbox), constructed by the pool engine so the identity is its OWN `fireIdentityFor`. Each producer's local floor stays as UX PACING … ⇒ kb-design-lessons §18 | test-spend-paths (308) + test-architecture §44 — the grep-derived census FAILS an unwired SITE; all three money-gate layers fail CLOSED |
| **A quota READING — its shape, who may write it, who may read it** | `src/quota-model.js` (PURE: the typed LIMIT SET — an account holds a LIST of limits, each with its own windows and provenance; `limitFor` is the ONE accessor, `windowState` fences off the not-yet-started window whose "reset" slides with the clock … **B-855a c1: the /usage panel probe may only WRITE the account it PROVES — isolated CLAUDE_CONFIG_DIR per spawn + identity verified against the API-derived window before any write, else archived as `panel-identity`** **inc-mubu23bd-5vxi: a stream reading names its lane through the producer's per-window fields; `seven_day_overage_included` IS the model-cap lane, never plan** ⇒ kb-design-lessons §18 | test-fable-cap-pool-storm (360) + test-quota-model (358: the per-limitId merge that never collapses, the empty-window drift fixture … |
| **Quota/vendor calls** | NOWHERE new. Exactly two files may construct vendor requests; the quota-refresh op (human-gated, on the login-holding machine) is the ONE device-side vendor surface. Passive capture (statusline/rate_limit_event/banners) is always preferred. **auto-cli (2.329.0** … ⇒ kb-design-lessons §18 | test-vendor-whitelist (64) FAILS any new vendor call until deliberately allowlisted with its gates |
| **Auto-resume / any "continue a stopped turn by itself"** | The DESCRIPTOR's two hooks — `quota.signalFromStream` (how a LIMIT shows up) and `descriptor.resume {form, deliver}` (how a TURN is restarted) — plus the harness-neutral core in src/server/auto-resume.js. `capsOf(backend).autoResume` is DERIVED (`deriveAutoResume` … ⇒ kb-design-lessons §18 | test-harness-contract (the conformance run: every row re-derived from its descriptor, every declared verb driven arm→fire into a stub of its own channel, a harness without one proven never-armed … |
| **Pool/billing decisions** | src/account-pool-auto.js (pure) + the engine in server.js; per-session links (plan C) resolve via poolCurrentFor(pool, webuiId). Every blocked outcome must SPEAK with named buckets. … **EVERY reading AND every rejection is attributed to the credential SLOT (the token-slot-validated link), never to the OTel-observed org** — the slot is resolved ONCE PER TURN (`rejectionSlotFor` / `readingSlotFor`), every blocked outcome SPEAKS with named buckets, placement follows the REQUEST model (lock > pick > spawn > served-if-not-a-fallback); USAGE CREDITS (B-ad05): `overageState().mode==='allowed'` ranks a member LAST (last resort only), a parked pool answers `on-credits` + ONE 6 h notice, spend-guard unchanged. SOFT band + warm mid-turn ⇒ moves at its first stop. ROOT FIX DESIGNED, NOT SHIPPED: docs/design-account-hardening.md (ONE lease + ONE write choke point + ONE spend authorizer). warm-cache: a warm conversation is never moved proactively (edf held; a wall still moves it). reset credit: warm ⇒ credit first, cold ⇒ switch first; ONE credit per account wall, spent as the member the process HOLDS (`accounts.poolMemberOfSession`: credit, reading, wall, continue, ledger, badge). ⇒ kb-design-lessons §18 | test-pool-auto (197) + test-account-pool (33) + test-account-verdicts (45) + test-auto-resume (233) + test-auto-resume-loop (221) + test-login-expiry (257) + test-readings-attribution (515) + test-codex-pool (269) … |
| **A channel ADAPTER, or anything about a channel RECORD** (docs/design-communication-panel.zh.md) | The adapter goes in `src/channels/<kind>.js` behind the registry's contract (`src/channels/index.js`) and NOTHING downstream learns its name — **gate on the capability row, never on an adapter id**, exactly as `src/backend-caps.js` does for harnesses. Groups: explicit, next-turn free, a wake billed + paced … ⇒ kb-design-lessons §18 | test-channel-groups + test-msg-cli-groups + test-channels-groups-ui (heavy: -groups-e2e) + test-channel-record (incl. the grep-derived FRAME CENSUS over every hyphenated tag the tree writes) + test-channel-caps (incl. r3's `off` leg with a scratch-dir patched copy) … |
| **An INTEGRATION CREDENTIAL — a key a user or a cluster provides for a vendor (a Lark app, a Google OAuth client, a browser license)** (docs/design-communication-panel.zh.md §14, P0b) | The ROW goes in PURE `src/integration-registry.js` (fields, cluster env names, the setup block, the Test declaration, `consumers` — and it lands WITH its consumer or names its `wiredIn` phase, decision 25 … ⇒ kb-design-lessons §18 | test-integration-registry (fast, 194 — incl. the env-NAME census over the tracked-file list, PRINTED, and the BUILDER census beside it with the verifier's built-name probe as its control … |
| **A desktop application window / a display backend** | PURE `src/desktop-apps.js` (rungs, ladder, fit) + SHARED `src/desktop-display.js` + keeper / bridge / routes / views ⇒ kb-design-lessons §18 | test-desktop-apps, -display, -viewers, test-xpra-client, test-vnc-view (fast); heavy ⇒ kb-design-lessons §18 |
| **The agent browser** (spawn env / profiles / live view / takeover / mediation / trace / window targets; design-agent-browser-v2.zh.md §3.2–§7.6, P0–P10) | PURE verdicts in `src/browser-*.js` + `src/window-*.js`, ORCH in `src/server/browser-*.js` + `window-targets-engine.js`, the CLIs `vibespace-browser` / `vibespace-window` … ⇒ kb-design-lessons §18 | test-browser-* + test-window-targets (fast); test-browser-live / -mediation-chrome / -tier3-chrome + test-window-target / -binding (heavy) |
| **A server cache a client also caches** | The invalidation entry point must NOTIFY (the 2.309.0 rule) — hook on the ONE entry point, never per call site | test-remote-discovery-dirty's drift guard pattern |

**ACTIVATION SWITCHES (code complete, soak-gated — flip deliberately, in order):** ① watch `sb-parity-hit/miss` in Diagnostics (step-2/3 live parity) → ② `agentd.localDiscovery` on (after `local-disc-snap-ms` looks sane on YOUR storage) → ③ `agentd.localPipeSessions` on (R6 — only after ①② soak; the design's sequencing law). Rollback = flip off; every path falls back structurally.

**ENFORCEMENT (2.323.0; PHYSICAL since 2.325-2.326): the tier rules above are a RED TEST, not prose** — scripts/test-architecture.mjs (107 asserts — incl. §43, the GATE CENSUS: every scripts/test-*.mjs is in exactly one ci.mjs tier or in EXCLUDED with a reason; and §44 … ⇒ kb-design-lessons §18

**STANDING SWEEP (the sysinfo lesson, 2.314.0): "twin-sets = 0" is not a state, it's a metric to re-measure.** A user caught a missed local/remote twin AFTER closure. When touching any "how much X" reader, grep for its sibling on the other transport first.


### Stack
- **Backend**: Node.js + Express + WebSocket (`ws`) + `node-pty` + dtach
- **Frontend**: Vanilla JS (ES modules) bundled with esbuild, xterm.js for terminals, CodeMirror 6 for editor
- **Session persistence**: dtach — claude processes run in dtach sessions, survive server restarts
- **Data persistence**: `data/layouts.json`, `data/session-meta/*.json`, `data/sockets/cw-*`

### Data Flow
```
Terminal mode:
  Browser (xterm.js) ←→ WebSocket ←→ node-pty (dtach -a) ←→ dtach socket ←→ pty-wrapper.js ←→ claude CLI
                                                                                  ↓
                                                                            buffer file (raw PTY)

Chat mode:
  Browser (ChatView) ←→ WebSocket (msg create/edit/meta) ←→ MessageManager ←→ server ←→ chat-wrapper.js ←→ claude --stream-json
                                                                   ↓
                                                          BackendAdapter (swappable)
```

## Cross-cutting laws (the always-loaded digest — full text lives in the kb files)

- **§ban-safety**: NEVER call Anthropic on a timer with a subscription token; vendor requests live in EXACTLY two files (scripts/test-vendor-whitelist.mjs enforces); passive capture is the default; the ONE owner-approved exception is auto-cli (`claude -p /usage`: burn-aware fast rung + owner-directed slow idle rung at a WANDERING 30–60min threshold, never a fixed cadence) — do NOT generalize it. Full: kb-design-lessons §9 + the routing table above.
- **Program-use billing**: sessions run interactive PTY mode — never `-p`/`--print`/Agent SDK for inference (moves usage to metered programmatic billing).
- **Public repo hygiene**: ZERO company/cluster detail, ZERO personal identifiers (users are userL/userW/userN in docs); secret-scan CI + pre-push guard.
- **Worktree-only smokes**: NEVER run server.js/boot smokes from the repo dir — repo data/ is PRODUCTION (#127 class); git worktree + own data/ + symlinked node_modules; harnesses set VIBESPACE_SKIP_AGENT_HOOKS=1. … ⇒ kb-design-lessons §18
- **Release discipline (TWO-TIER GATE since 2026-09-07, B-4c5a — the single 11.5-min battery was a gate people routed around)**: every change = version bump + CHANGELOG + commit + push. Heavy tier (2026-09-15): parallel lanes, impact-scoped per push, FULL once per 24 h; since 2.369.104 every suite run (both tiers, every lane) and every heavy launch REAPS scratch-dir orphans (`node scripts/ci.mjs --reap` by hand). … ⇒ kb-design-lessons §18
- **CS separation**: `hostId` is a PARAMETER, never a branch — one implementation against a machine handle; new facts-about-a-machine code goes in the SHARED modules with a parity suite (routing table above).
- **Multi-client**: every persistent state change broadcasts to other clients live; UI actions chained after a store write must NOT wait for the broadcast echo.
- **Cache invalidation must NOTIFY**: a server cache a client also caches → the invalidation entry point pushes the recomputed RESULT (one dirty signal = one computation).
- **i18n quick rules**: `t()` with ENGLISH-STRING-AS-KEY on human-visible chrome ONLY (never protocol values/stored strings/agent-facing text); keep `escHtml()` around `t()`; sidebar cluster imports `t as tr`; same-spelling-different-meaning ⇒ `tc(ctx,str)`; new keys need zh+ja entries (i18n-check runs in build). Full: kb-design-lessons §16.
- **UI conventions**: theme vars only (no literal colors), `--radius`/`--radius-sm`, createModalShell for dialogs, NO native prompt/alert/confirm, SVG icons only (never emoji), `data-popover` Esc protocol, no global `.hidden`. Full: kb-design-lessons §17.
- **Injection budgets**: hook additionalContext wraps into <persisted-output> at exactly 10 KiB — tools/rules first, logs byte-budgeted last, 9600B hard cap (agent-routes).
- **Spawn hygiene**: agent sessions get `agentEnv()` (sanitized env, never raw process.env); display strings (host-labeled cwds) never reach a spawn; secrets ride env/files, NEVER argv.
- **Danger constants (memorize)**: NEVER delete data/sockets/cw-* (the socket IS the session). Production restart = `systemctl --user restart vibespace` ONLY (KillMode=process is load-bearing — the default killed every dtach session). Restarting the server from inside a Claude session is safe ONLY because of the CLAUDE_CODE_* env sanitizer in server.js (never remove it — silent transcript loss). Buffer/meta sweeps are age-based; kill-path teardown runs BEFORE activeSessions delete.
- **XSS**: NEVER interpolate user/peer-controlled strings into innerHTML unescaped (they sync to ALL clients — one miss = stored XSS fleet-wide); markdown goes through DOMPurify.sanitize(marked.parse()); build image overlays via .src property, never innerHTML.
- **Atomic persistence**: every data/*.json write goes through writeJsonAtomic (tmp+rename); stores flush on SIGINT/SIGTERM — a bare writeFileSync is silent data loss on the exact crash path the product exists to survive.
- **Listener lifecycle**: window-scoped document listeners bind to `winInfo._listenerCtl.signal` (AbortController, aborted on close); per-drag listeners get a per-drag controller — never a per-render one (tears down MID-DRAG).
- **Never block the event loop**: no sync fs/exec against mountpoints, /dav-sharing processes, or discovery sweeps — child processes/workers with timeouts only (three whole-instance outages: FUSE threadpool, execFileSync sweep freeze, device-mount self-deadlock).
- **No silent failures**: a failed USER action must reach the user (toast/dialog/server-notice) — telemetry having it ≠ reported; fetchJson never throws, so callers check `r?.error`.
- **Working mode**: route every change through the three-tier table above (it names the module AND the gating test); fixes go to git only — the user self-updates; bookkeeping via vibespace-status/-task/-ask each turn.

## File Structure (index — essays in docs/kb-file-structure.md)

**Long entries are truncated here; the ⇒ marker means the full essay (invariants, incidents) is in kb-file-structure.md under that filename. READ IT before editing the file.**

```
src/plugin-manifest.js — PURE plugin manifest validator (Plugin Ph2, 2.369.24): id=<publisher>.<name>=folder, semver, engines.vibespace min, client tier, server flag, contributes windows/agentTools/routes, declared capabilities; 2.369.43-.44 hardening = cleanText() … ⇒ kb-file-structure.md
src/server/plugin-loader.js — PLUGIN LOADER (Ph2): data/plugins/<id>/ manifests → sandboxed iframe assets at /plugins/<id>/ (opaque-origin CSP), forked server.js processes (IPC v1: route/tool/shutdown, crash backoff, parked after 5/10min), /api/plugins/<id>/x/* proxy (nosniff + sandbox CSP … ⇒ kb-file-structure.md
src/channel-record.js — PURE (imports nothing) THE ONE normalized message record every channel adapter produces (docs/design-communication-panel.zh.md §4): {id, convId, adapterId, vendorId, at, author, text, mentions, attachments, replyTo, threadKey, raw}. … ⇒ kb-file-structure.md
src/channel-caps.js — PURE (imports nothing) THE ONE place that answers "does this control exist" and "which lane is carrying this row RIGHT NOW" (design §4/§6.4/§12.5). Two axes MODELLED not flattened: how messages arrive (push|poll|scan + the per-PLATFORM … ⇒ kb-file-structure.md
src/channel-filter.js — PURE (imports only channel-record for the frame neutering; bundled into the editor like channel-caps) THE FILTER + THE ASSIGNMENT MODEL + WHAT AN AGENT IS HANDED (design §7, P2): `RULE_KINDS` a CLOSED set of eight, `matchRecord` → `{hit` … ⇒ kb-file-structure.md
src/channel-policy.js — PURE (imports only channel-record; bundled into the Outbox card) THE OUTBOX POLICY (design §9, P3): the proposal state machine (every transition names its actor; `unknown` leaves only through reconcile — a lost outcome is NEVER auto-retried) … ⇒ kb-file-structure.md
src/channel-acl.js — PURE (imports only msg-acl) AGENT REACH (design §8, P3): hidden < requestable < visible, everything hidden by default, MAX over grants, widen-only, ONE row per (principal, scope, origin) so unassign removes only its own row, a request approval = exactly one visible grant … ⇒ kb-file-structure.md
src/channel-groups.js — PURE THE AGENT-GROUP MODEL (design §22, D1/D2): record + symmetric pairKey, membership as pure transitions, `wakeVerdict` = the D2 table, `reportFor` = post-join news, the WHOLE text under budget (else fits:false); gate test-channel-groups (251) ⇒ kb-file-structure.md
src/server/groups-engine.js — ORCH agent groups: groups.json via channel-store's door, reach = msg-acl, a WAKE = THE ladder under `peer-message`; ONE persisted pacer (1/target/30 s, 8/sender/min), >5 ⇒ `--yes`, owner echo `expectWakes`; reports on USER turns only ⇒ kb-file-structure.md
src/integration-registry.js — PURE (bundled + server) THE table of integration rows (§14.2): fields, clusterEnv | delegate, setup, test, consumers; 2.369.165 `bindsPerAccount` (fake/lark/gmail: client chosen per account) + `clientHint` + `clientFieldsOf` ⇒ kb-file-structure.md
src/quota-model.js — PURE (imports nothing; CJS so the bundle and the shipped statusline carry it) THE TYPED LIMIT SET (2026-09-09): an account key holds a LIST of limits — {limitId, name, scope plan|model|credits|overage … ⇒ kb-file-structure.md
src/record-shape.js — PURE schema oracle for KNOWN record types (design-unknown-records §3–§4): SHAPES per <harness>:<carrier>:<shape>, pinned to CLI 2.1.281 (zod helpers read off an anchor); ONE drift card per shape per session; gate test-record-shape (76) ⇒ kb-file-structure.md
src/text-window.js — PURE (CJS) THE ATTACH SLAB (2.369.167 perf A/r1): tail counted in TEXT cards (24; 50–400 records, ≤128 KiB PAST the floor); `attach {slab:'floor'}` (hidden view / not first of a burst, view-visibility `attachSlab`) = tail(50); gate test-text-window ⇒ kb-file-structure.md
src/op-seq.js + src/server/session-broadcast.js — RESUME BY SEQ (2.369.167 perf D): `msg.seq` from a per-session ring; same-epoch re-attach ⇒ `attached {slab:'held', replay}`; a capable stalled socket gets ONE `lagged`; gates test-op-seq + attach-rebuild ⇒ kb-file-structure.md
src/reset-credit.js — PURE (imports nothing, CJS) THE RESET-CREDIT VERDICT (design-reset-credits §4): at a stated wall only; openai use-now, anthropic ReLU knee; the ladder forks by warmth; describeUse/offerOf; p2 dialogModel/refusalLine/rosterResetOffer (the manual use's words) ⇒ kb-file-structure.md
src/lib/reset-credit-dialog.js — THE ONE reset-credit confirm dialog (p2): roster Use… / chat wall+arm card / For-you item all call `openResetCreditDialog`; GET preview → PURE dialogModel → POST the route; refusals by name, failures toast ⇒ kb-file-structure.md
src/routes/reset-credit.js — GET/POST /api/accounts/:id/reset-credit (p2): thin over the engine's preview + `consumeResetCreditFor` (the auto rung's writer: spend ceiling + 10-min floor); codes not_supported/no_live_session/no_credits/cooldown/spend_refused; human-only ⇒ kb-file-structure.md
scripts/test-reset-credit-verdict.mjs + test-reset-credit-ui.mjs — the reset-credit gates (fast): the PURE verdict tables (openai floor, anthropic knee) + ladder-fork wiring pins; the dialog sentences, roster chip, route over the REAL engine + stub wrapper, patched-copy controls ⇒ kb-file-structure.md
src/usage-cache-write.js — THE ONE WRITE PATH for data/usage-cache/*.json (design-account-hardening §4.2): validates the typed set, merges per limit, stamps source/fetchedAt per LIMIT, writes the legacy buckets as a DERIVED VIEW beside `limits`, owns the `.window-`/`.slot-` sidecars … ⇒ kb-file-structure.md
server.js — BOOTSTRAP + WIRING ONLY (~1910 lines since the 2.325.0 拆分; the arch suite RATCHETS it ≤2100). **One HOT PATH still lives here and it is named: `refreshWebuiPids()`** — it runs IN-BAND on every kill (ws-handler, before the broadcast) and 3 s after every create … ⇒ kb-file-structure.md
scripts/ci.mjs — THE RELEASE GATE, TWO TIERS (B-4c5a, 2026-09-07): the tier table `{name, tier:'fast'|'heavy', why}` + EXCLUDED-with-a-reason, both runners, the detached `--heavy-launch`, the push-blocking `--check-heavy`, `--status`, `--census`, (round 2) the machine lock, and (2.369.104) the SCRATCH-ORPHAN REAPER — `scratchOrphans`/`reapScratchOrphans` after every suite run, at every heavy launch, `--reap` by hand; evidence-based (/tmp/vs-* root + dir gone or unowned-and-stale), never production … ⇒ kb-file-structure.md
scripts/test-ax-budget.mjs — THE AX BUDGET GATE (heavy; design-accessibility-tree §3 rows 0–1 / §4): Chrome's own getFullAXTree counts on the §1c fixture — OQ1, the band's census + growth invariant, ×1.25 pins, the setting, focus exposure, a neutered-copy control ⇒ kb-file-structure.md
scripts/huge-transcript-fixture.mjs — the §1c huge compact-mode transcript generator (inc-mubvu3a4-x8sb shape, deterministic seed, ids by fixtureSid), ONE implementation shared by test-chat-paging + test-ax-budget; never a test-*.mjs ⇒ kb-file-structure.md
src/browser-profiles.js — PURE (imports nothing) THE AGENT BROWSER'S IDENTITY, SPAWN ENV AND EVERY VERDICT (design-agent-browser-v2 §3.2–§7.6): the four spawn pairs, the (D)→(C)→(N)→none user-data-dir ladder, profiles/pins/leases/handles, the child env (rung D gets its OWN config), adopt roots ⇒ kb-file-structure.md
src/browser-facts.js — SHARED (node builtins + the PURE model) WHAT `agent-browser` IS ON THIS MACHINE: the version probe (10-min TTL, the NEGATIVE answer cached too, never throws), the daemon runtime (launch/stop/status/streamPort under the session's own pairs) ⇒ kb-file-structure.md
src/browser-serve.js — SHARED (fs/os/path + the PURE model + browser-facts; the daemon bundles it) THE `browser-serve` OP TABLE + RUNNER (P4, §3.6/§7.3): version/status/start/stop/cdp-url of a PROFILE browser WHERE IT RUNS (in-process for device #0, inside the agentd for a paired machine) ⇒ kb-file-structure.md
src/server/browser-access.js — ORCH (P4) ONE way to reach a profile browser on ANY machine (the opencode-access shape): `call(hostId, op, params)` = local in-process / the `browser-serve` agentd op / an ssh host without a daemon REFUSED `host_needs_daemon` (never a silent local fallback) ⇒ kb-file-structure.md
src/server/egress-proxy.js — ORCH (P4, §7.2.1 "enforcing, not observed") the allowlisting CONNECT/absolute-form http proxy on the hub's loopback: admitted ONLY by the PURE `egressVerdict` (re-read per request; loopback/link-local never; empty = deny), refused = 403 with the verdict's sentence ⇒ kb-file-structure.md
scripts/measure-cloak-egress.mjs — THE §7.2.1 MEASUREMENT (P4): strace over the four runs the design names (the local-oracles discipline); NEVER installs or downloads — without the binary it prints the refusal record by name (`binary_absent`, what `CLOAK_EGRESS_PROOF` carries today) ⇒ kb-file-structure.md
src/server/browser-env.js — ORCH, the half that TOUCHES things: writes + READS BACK the generated config, the scratch dir + pin symlink, the per-rung journal, `repointPin`, the conversation→key recovery, `childConfigFor` (rung D's sub-agent config), the sweep owning child files by parent key ⇒ kb-file-structure.md
src/server/browser-bindings.js — ORCH (r5) THE CONVERSATION → BROWSER-KEY BINDING IN A STORE THAT OUTLIVES THE WEBUI SESSION: `data/browser-env/bindings.json`, written at session-stdout's meta choke point and never unlinked by a kill; a fork / an implicit fork is never bound to its parent's key ⇒ kb-file-structure.md
src/server/browser-keeper.js — ORCH (P1, §3.3–§3.5) THE PROFILE REGISTRY, ITS LEASES AND THE KEEPER OF ITS BROWSERS: data/browser-profiles.json (atomic 0600, broadcast on every commit), ONE lease per (profile, browserKey), the idle/runaway/ceiling guard, the attach/pin/switch/takeover seams ⇒ kb-file-structure.md
src/browser-mediation.js — PURE (imports nothing; CJS shared by proxy/keeper/routes/CLI/bundle) THE CDP MEDIATION RULES (P6, §6.2/§6.5/D6): `judge` scopes Target.* to the lease's own tabs, refuses whole-browser acts, `browser_paused` on Input.*/page-navigation COMMANDS while the user drives (script evaluation is NOT fenced) ⇒ kb-file-structure.md
src/server/cdp-mediator.js — ORCH (P6) THE CDP-MEDIATING PROXY: one lazy loopback server serving every (profile, conversation) lease its OWN `ws://127.0.0.1:<port>/m/<token>/devtools/browser` (page endpoints in scope only); `grantFor` once per lease, revoke closes the lease's own tabs ⇒ kb-file-structure.md
src/browser-switch.js — PURE (imports nothing; CJS so keeper/routes/key consumer/bundle share ONE spelling of every refusal) THE LIVE BACKEND SWITCH AND THE KEY-CONSUMER HALF AS DECISIONS (P4 second half, §7.4–§7.6): `KEY_ROWS`, the switch verdict (direct vs a proposal), the cloak/cloud rows ⇒ kb-file-structure.md
src/server/browser-backend.js — ORCH (P4 second half, §7.5) THE KEY CONSUMER: the ONE module that declares the six registry rows' consumer, registers their Test runners with the integration store (a declared test with no runner is a dead control the census reddens) and resolves the key at launch ⇒ kb-file-structure.md
src/browser-stream.js — PURE (imports nothing; CJS so the bridge AND the bundle share it) THE LIVE VIEW'S DECISIONS (P2, §4.2/§4.4/§3.7): `streamTargetFor` (which browser a live view shows — a view is not a command), the viewer/mode records, pointer↔device mapping, the input records ⇒ kb-file-structure.md
src/browser-takeover.js — PURE (imports nothing; CJS so keeper, bridge, routes AND the bundle share it) TAKEOVER AND HANDBACK (P3, §4.3/§4.3.1): the input state of a (conversation, browser) pair, `decideTakeover` (a second live viewer is `held`), `decideHandback` with its CAUSE, the idle verdict ⇒ kb-file-structure.md
src/browser-trace.js — PURE (imports only browser-profiles' rows; CJS shared by recorder/routes/CLI/bundle) THE ACTION TRACE + HOUSEKEEPING DECISIONS (P5, D7/D8/D35): closed action table, «N chars»/«1 key» redaction (fill/type/press/select), after-frame pick, 7 d/200 MB retention PLAN ⇒ kb-file-structure.md
src/server/browser-trace.js — ORCH (P5) THE RECORDER on the bridge's TAPS + the keeper's LEASE SEAM (a tap never starts a browser): 0600 entry files under data/browser-trace/<scope>/, `trace` to viewers + `browser-trace-appended` to clients (never bytes); hourly sweep; forget RENAMES first ⇒ kb-file-structure.md
src/routes/browser-trace.js — GET /api/browser/trace (live key | bindings-resolved `conversation` | given `browserKey`; nothing to match ⇒ EMPTY, never everything), /trace/:id[/frame/:which], /housekeeping[/sweep], PATCH /profiles/:id, forget/adopt/orphans/forgotten-delete, /recordings ⇒ kb-file-structure.md
src/server/browser-handback.js — ORCH (P3 §4.3.1) THE ONE PRODUCER of the `browser-handback` turn nobody typed: hangs on `keeper.onInput`/`onConfirmation`; an EXPLICIT handback = `deliverToConversation` with `spendReason:'browser-handback'` (its single ladder site); an idle one files one inbox item ⇒ kb-file-structure.md
src/server/browser-stream.js — ORCH (P2) THE LIVE VIEW'S WS BRIDGE `GET /api/browser/stream?session=<webuiId>[&profile=…]`: cookie auth INSIDE (401 before the upgrade), ONE upstream per (session, target) FANNED OUT to N viewers, server-minted viewer ids, the `mode` record's per-viewer `mine` ⇒ kb-file-structure.md
src/routes/browser.js — `/api/browser/*` + `/api/agent/browser/*` (P1–P6): thin over the PURE model + the keeper; `host` refused by name; every failure `{error, code}`; the UI answer never carries the CDP url; `childEnvOf` (rung D's child config); `new --adopt` only under the adoptable roots ⇒ kb-file-structure.md
src/keeper-limits.js — PURE (imports nothing) THE ONE constants home every process keeper counts and bounds by (GUARD_SAMPLE_MS/CPU/RSS/SUSTAIN, RUNAWAY_COOLDOWN_MS, CONCURRENT_CAP); the browser keeper reads it here (master's desktop-app keeper + opencode-serve read the same object)
data/bin/vibespace-browser — named, persistent browser PROFILES for THIS session (P1–P6, STATIC tracked, in AGENT_TOOLS): profiles/new/use/detach/status/pin/watch/new-child/backend/blocked/`-- <agent-browser args>`; typed refusals with their remedy (`browser_paused` exits 1, never a retry) ⇒ kb-file-structure.md
src/window-targets.js — SHARED (builtins + desktop-display) WINDOW TARGETS (P9a, design §4.9/§5.1.1): AT-SPI snapshot + `@ref` minting in a BOUNDED subprocess (helper .py: wall SIGKILL, per-call timeout, node budget), closed chord vocabulary, runtime input-backend ladder, the per-verb law ⇒ kb-file-structure.md
src/window-desktop.js — PURE (imports nothing) TIER 3 = the user's REAL desktop as a window class (P10, §7.6/§6.6, D27 (b)): the consent switch `window.realDesktopTargets` (anything but true is OFF; off drops every desktop lease), the rows = the a11y bus minus ours, injection REFUSED BY NAME ⇒ kb-file-structure.md
src/server/window-targets-engine.js — ORCH (P9) ONE holder per window, the lease PERSISTED (data/window-leases.json), takeover ⇒ every verb `window_paused` (an OPAQUE tag in views), `open` = registry ids only (`exec_is_human`), never a browser row (`browser_is_human`) ⇒ kb-file-structure.md
src/server/window-live-wiring.js — the desktop scene's ONE composition (P9b): the bridge's input policy IS the engine's lease verdict, viewer-left IS the handback, holderAlive IS the socket fact, both route families + the shared announcer; boot after restoreSessions ⇒ kb-file-structure.md
src/lib/window-live-mode.js — PURE (imports nothing) the window-live pane's mode arithmetic: `windowLiveMode` (no lease ⇒ never view-only), `windowModeBadge` (the live view's exact three phrases), `leaseTransition`, `newViewerId` ⇒ kb-file-structure.md
src/routes/window-targets.js — agent-only /api/agent/window/* (targets/open/attach/detach/snapshot/act/screenshot/watch), host refused by name, STATUS covers every typed refusal (suite census) ⇒ kb-file-structure.md
data/bin/vibespace-window — the agent's window-target CLI (STATIC tracked, in AGENT_TOOLS): list/open/attach/snapshot/screenshot/click @ref|--at/type/key/watch/detach; refs per snapshot, no degrade without an action; one persisted holder per window, `window_paused` = never retry ⇒ kb-file-structure.md
scripts/mutant-copy.mjs — THE ONE writer of a negative control's patched copy: scratch dir, CJS require / ESM imports re-bound to the real module (line numbers kept), closed worlds by path, `copiesCensus` leg; never src/ (test-architecture §51) ⇒ kb-file-structure.md
scripts/git-env.mjs — the ONE sanitized git environment (25 redirector names) for tooling that runs inside somebody else's git process — shared by test-architecture §42 and by the heavy gate's detached child (a pre-push hook exports GIT_DIR/GIT_INDEX_FILE and the heavy suites run `git worktree add`)
src/ws-create.js       — the ws 'create' case family (1633 lines, 拆分P2): new/resume/fork + local/ssh/dial/daemon-pipe spawn + account resolution + breakers + writer sweep + adoption; body wrapped `do{}while(0)` so case-level `break;` semantics survived the move. … ⇒ kb-file-structure.md
src/session-schema.js  — PURE registry of all ~47 live-session `_fields` (owner module + persistence home + note, 拆分P3); scripts/test-session-schema.mjs fails any unregistered new field write and any dead row. New session state ⇒ add the row FIRST. … ⇒ kb-file-structure.md
editor-helper.sh       — Ctrl+G editor helper (overwritten by server on startup)
data/bin/editor/code — Fake "code" script for Ctrl+G (generated by server; moved OUT of data/bin in 2.267.0 — data/bin is PATH-prepended into every local session and the fake `code` shadowed real VS Code there; EDITOR= stay… ⇒ kb-file-structure.md
data/bin/pty-wrapper.js — PTY wrapper for terminal mode (runs inside dtach, survives server restarts)
data/bin/vibespace-hook-register.mjs — GENERATED (tracked; regenerate before committing a template change) remote register + CLI-config helper: ESM + createRequire, embeds src/harness-config.js as FILE TEXT, driven by VIBESPACE_CLI_CONFIG (apply / --status read-only / --uninstall = hooks only) ⇒ kb-file-structure.md
data/bin/vibespace-hook.mjs — Dual-harness hook (SessionStart→task context OR a baseline tools intro for no-task sessions, UserPromptSubmit→first-prompt context + status-override notices + per-turn micro-reminder, Stop→bookkeeping… ⇒ kb-file-structure.md
data/bin/vibespace-task — Agent task-reporting CLI (STATIC tracked file, not generated — inline template-in-template escaping was unmaintainable): show [--full]/progress "summary" [--detail]/backlog/backlog-add/backlog-done/backlog-drop → /api/agent/task* (scoped to the session's own context task). ⇒ kb-file-structure.md
data/bin/vibespace-ask — Agent CLI for the GLOBAL USER-TODO inbox (2.65.0, STATIC tracked): files items the USER must act on (decision/input/review) → POST /api/agent/user-todo (vsst_ scoped to own session). ⇒ kb-file-structure.md
data/bin/vibespace-status — Agent status tool (GENERATED at startup like data/bin/code — NEVER git-track it: tracking it dirtied the tree every boot and blocked `git pull --ff-only` in self-update (real incident 2.111.26); gitig… ⇒ kb-file-structure.md
data/bin/vibespace-job — Background Work agent CLI (STATIC tracked, 2.342.0): run/--keep-up/--every/--cron/--at/poll(--wait cap 100s)/logs/stop/start/rm/access/progress/ask/answers; auth vsst_ session token OR jbt_ job token (job may act on itself only). In AGENT_TOOLS (ships to remote hosts). **2026-09-14 TRIAGE (design §13): `list --archived`; poll/show/logs of an ARCHIVED id read through (`archived:true`, never a 404 for housekeeping); an owner-lineage poll/show/logs ACKNOWLEDGES a terminal one-shot (a jbt_ self-read never).** **2026-09-14 TRIAGE (design §13): `list --archived`; poll/show/logs of an ARCHIVED id read through (`archived:true`, never a 404); an owner-lineage poll/show/logs ACKNOWLEDGES a terminal one-shot (a jbt_ self-read never).** ⇒ kb-file-structure.md
data/bin/job-wrapper.js — Background Work detached wrapper (STATIC tracked): first act = atomic pid.json stamp {pid,starttime,bootId,argvHash}; tees child stdout→current.log (50MB rotate, never unlink live fd); exit.json on child exit; answers.jsonl→stdin tail when stdinOpen.
data/bin/vibespace-msg — agent messaging CLI (STATIC, AGENT_TOOLS): list/read, `send <agent|group> [--wake] [--yes]` (agent = the pair's group), `group list|create|invite|leave|kick|rename|archive|notify`; wakes echoed as billed turns; ambiguous ⇒ ids; jbt_ never creates ⇒ kb-file-structure.md
data/bin/vibespace-page — 页面发布+design kit CLI(2.366.0, STATIC tracked, in AGENT_TOOLS): publish <file.html> --title [--public](上传快照→/p/<id>分享链接, 同路径同URL) · kit(准备/镜像design工具包并打印base directory) · list; vsst_或jbt_. 手册vibespace-docs pages
data/bin/vibespace-channels — the agent side of the Communication panel (P3, STATIC tracked, in AGENT_TOOLS): list/read/reply/status/request; `reply` PROPOSES and never sends (the policy decides direct vs the user's approval), reach is resolved server-side FIRST … ⇒ kb-file-structure.md
  oauth-loopback.js — SHARED (http+crypto+the PURE registry) THE ONE consent-flow machine, DUAL-MODE (design §12.4, P1): `ephemeral` (Gmail — `listen(0)`, redirect_uri the bare loopback origin, exactly gmail-sync's flow) / `fixed` (Lark — the registry's `LARK_CALLBACK_URL` IMPORTED never spelled … ⇒ kb-file-structure.md
data/bin/vibespace-docs — full-manual reader for every agent CLI (2.351.0, STATIC tracked, in AGENT_TOOLS): `vibespace-docs [status|ask|task|jobs|msg|pages|browser]`, no topic = global index; fetches GET /api/agent/docs/:topic (vsst_ or jbt_) which serves docs/agent/*-manual.md from the running server's checkout — manuals never drift from the running version; budgeted context teaching carries one pointer line per surface.
data/bin/vibespace-channel.js — EXPERIMENTAL Claude Code channel server (2.344.0, STATIC tracked, dependency-free hand-rolled MCP stdio): declares claude/channel capability; VibeSpace pushes {content,meta} lines to its per-session unix socket (VIBESPACE_CHANNEL_SOCK, data/channel-socks/<webuiId>.sock) … ⇒ kb-file-structure.md
data/bin/vibespace-usage — PASSIVE usage capture (STATIC tracked file, not generated; 2.60.0). The account key is the credential LINK resolved per write (`VIBESPACE_ACCOUNT_LINK`, not the spawn-fixed key) and it lags the link by ONE REQUEST (2026-09-07 r3): `rate_limits` is the CLI's last API response … ⇒ kb-file-structure.md
data/bin/chat-wrapper.js — Chat wrapper for stream-json mode (runs inside dtach, parses JSON lines)
data/bin/codex-chat-wrapper.js — Codex chat wrapper (JSON-RPC protocol, thread management)
data/bin/vibespace-usage-scan — REMOTE-side usage ledger scanner (2.127.0, STATIC tracked; PARITY-GUARDED since 2.275.0: it is a shipped SINGLE FILE (a host has no checkout ⇒ it cannot require the local walker), so it silently lagged its twin twice — the 2.265.0 subagents/workflows walk took 6 weeks to reach it. ⇒ kb-file-structure.md
data/bin/vibespace-opencode-op — REMOTE-side OpenCode-serve op runner for checkout-less ssh hosts (S9 remainder piece (e), STATIC tracked, shipped to ~/.vibespace/bin like vibespace-usage-scan): mirrors the SHARED op table src/opencode-remote.js (parity-pinned by scripts/test-opencode-remote.mjs) … ⇒ kb-file-structure.md
data/bin/vibespace-remote-keeper — REMOTE-side persistence for remote CHAT sessions (2.124.0, STATIC tracked; distributed to remote ~/.vibespace/bin with the other tools): claude runs setsid-DETACHED on the host under a keeper daemon (… ⇒ kb-file-structure.md
data/sockets/cw-*      — dtach socket files (session anchors)
data/session-buffers/  — Per-session output buffer files (written by pty-wrapper)
data/session-meta/     — Per-session metadata JSON
data/layouts.json      — Persisted layouts and autosave state
data/drafts.json       — Chat input drafts (SyncStore, versioned, multi-client synced)
data/settings-sync.json — Settings SyncStore (future migration target)
src/
  transcript-service.js — ONE interface over the transcript parse stack (R3 of docs/design-three-tier.md; 2.283.0 step 1 = server-hosted, HTTP read family rerouted through it). ⇒ kb-file-structure.md
  client.js            — Entry point (2 lines): imports App and initializes
  ws-handler.js        — WebSocket protocol handler (all WS message cases, extracted from server.js; `default:` since Plugin Ph1 = rate-limited log + telemetry 'ws-unknown-type' + `{type:'error', code:'unknown-type'}` reply WITHOUT sessionId — a session-scoped error flips live windows read-only). ⇒ kb-file-structure.md
  sync-store.js        — SyncStore class (versioned state sync with diff broadcast)
  session-store.js — SessionMessages + JSONL parsing + session discovery helpers. **ZERO SPAWNS PER SESSION in the local sweep (2026-09-09)**: parents/children come from /proc through src/cli-identity.js, the per-lock `ps -o ppid=` and per-session `pgrep -P` (and its 15s cache) … ⇒ kb-file-structure.md
  message-manager.js — MessageManager (Claude stream-json → normalized messages with stable IDs; **2.369.120: an unhandled record type/system subtype not on the DECLARED known-ignored lists renders as the red-bordered "Unknown event" fall-back card (whole record behind the expander, history + live, fold kind `unknown` off by default) — the next upstream feature is visible in the chat, not a breadcrumb**; 2.368.30 parseBackgroundLaunch=后台启动ack→taskInfo合成, task_*系统subtype只在活流上存在, 落盘的ack+<task-notification>才是历史可用的生命周期真源, session-store taskState扫描共用同一解析器). ⇒ kb-file-structure.md
  codex-message-manager.js — CodexMessageManager (Codex JSON-RPC/rollout → normalized messages; 2026-09-06 per-message meta at each response's token_count: requestId = the ledger's own `cx:<thread>:<cumulative>` key (never a vendor id — kind-marked), msgId = the 0.153 token_usage_record response id … ⇒ kb-file-structure.md
  chat-view.js — …另外 `_swapMessageEl` 是**唯一**的消息元素原地替换点(2026-09-07 对抗验证者): 一个已渲染的消息元素携带的是其他代码**读它所用的**簿记 — `dataset.msgId`(两个trim的计数/jumpToIndex/搜索定位/minimap/`_elements`的键)、`dataset.ts`、`dataset.line`、`.chat-gap-msg`豁免、以及两个**按元素**键控的 run 折叠标记 — 而这些全部不由 renderer 产生(chat-renderers 里 … ⇒ kb-file-structure.md
  collab-row.js — PURE(零依赖, CJS进bundle) codex 多agent collab 行的标签/HTML构造器: esc/t/图标全部注入 ⇒ XSS规则可被单测证明(marker escaper); 一行的纯文本与HTML同源不漂移; 行内永不携带加密载荷; **2026-09-07 互聊活进度**(owner '连续发生应该展示连续数量 … ⇒ kb-file-structure.md
  codex-session-store.js — Codex session discovery (thread listing, JSONL parsing, forkedFrom chain merge; the merged read tags every record with its FILE's thread id — non-enumerable `__threadId`, tagRecordThread — so the normalizer's ledger keys stay per file like the walker's; fixture scripts/fixtures/codex-native-fork/)
  codex-thread-read.js — codex 0.153 `thread/read` fallback for a thread with NO rollout file (paginated history store): pure Thread→records mapper + ONE bounded app-server read behind the descriptor's `store.warmTranscript` hook; local only, cached, never a session (B-21e4). ⇒ kb-file-structure.md
  normalizers.js       — createMessageManager(backend, id) factory for backend-agnostic normalization
  mounts.js — MountManager (rclone mounts, MULTI-SOURCE: typed records s3/drive/onedrive/gmail/webdav/sftp/vibespace/rclone via _rcloneFor; detached + boot adoption; one-click rclone install (data/bin, pinned 1.65.… ⇒ kb-file-structure.md
  gmail-sync.js — GmailSync (2.134.0, backlog B-64db): Gmail-as-a-folder — a 'gmail' mount = local dir of .eml files synced READ-ONLY from the Gmail API (GYB-style; NOT FUSE). ⇒ kb-file-structure.md
  transcript-worker.js — heavy JSONL transcript work OFF the main thread (2.235.0, the userL degradation follow-up): a persistent SafeFs-hosted worker pool (SafeFs gained opts.workerPath/inlineRun generalization + in-flight r… ⇒ kb-file-structure.md
  agentd/ — CS refactor (B-5052) machine agent — ALL milestones' protocol+device side COMPLETE and acceptance-tested (2.140.0–2.146.0; docs/design-remote-cs.md + M0 addendum + milestone record). ⇒ kb-file-structure.md
  machine-mounts.js — MachineMounts (B-f3e8 2.160.0 — HostMounts 2.147.0 + DeviceMounts 2.153.0 collapsed into ONE manager; records carry dir:'push'|'pull', keyed by hostId, store data/machine-mounts.json, both legacy stor… ⇒ kb-file-structure.md
  exit-proxy.js — ExitProxyManager (task #164, 2.186.0): ON-DEMAND egress — an agent borrows a paired machine's network for a SINGLE command (a region / internal-VPN / fixed source IP), NOT the whole session. ⇒ kb-file-structure.md
  port-forward.js — PortForwardManager (B-0b60 tunnel path, 2.165.0): vscode-style port forwarding over the agentd data plane — NO frps/public exposure. ⇒ kb-file-structure.md
  plugins.js — PluginManager (2.140.0, B-2d44): generic host-capability plugins — install + PERSISTENT state (~/.vibespace/plugins/<id>/, home=PVC in fleet) + boot replay (enabled+desiredUp → restart with server) + guided setup + status. THREE built-ins: tailscale, frp … ⇒ kb-file-structure.md
  accounts.js — AccountManager (per-session billing-identity switching, backend-scoped via a `backend` field on each account — legacy records = 'claude'. ⇒ kb-file-structure.md
  (LONG-LIVED TOKENS oat, B-211a 2.253.0 — `claude setup-token` integration: env channel, expiry honesty, ambient strip, review-hardened invariant list) ⇒ kb-file-structure.md (under accounts.js)
  account-pool-auto.js — Pooled pseudo-account (B-6217, 2.251.0-2.252.0): account type 'pooled' — ONE switchable billing identity over the logged-in Claude subscriptions. ⇒ kb-file-structure.md
  rate-limit-capture.js — PASSIVE quota capture from the CLI's own `rate_limit_event` stream-json records (B-e5c9, 2.289.0; discovered by 2.1.226 binary forensics — a first-class stdout record "emitted when rate limit info changes" VibeSpace dropped as unknown forever). ⇒ kb-file-structure.md
  spend-authorizer.js — PURE (imports nothing) THE ONE CEILING ON EVERY TURN NOBODY TYPED (design-account-hardening §4.4c / P9 / D3 / D6, 2026-09-08): the closed `SPEND_REASONS` set, `budgetLimits` (the four D6 numbers as settings; an explicit 0 is a CHOICE), rolling hour/day `spendCounts` … ⇒ kb-file-structure.md
  server/spend-guard.js — the ORCH half (persisted ledger data/spend-budget.json, journal line per (reason,identity,why) per 5min, ONE "For you" item per (identity,why) per 6h, telemetry, the Stop nudge's PERSISTED cooldown). … ⇒ kb-file-structure.md
  login-state.js — THE credential-state reader (2026-09-07, SHARED, fs-only): `loginState(credsPath)` → live|expired|wiped|missing|unreadable + `since` = the instant the login stopped being able to produce a reading. `parseAuth`'s boolean collapses four states … ⇒ kb-file-structure.md
  reading-lag.js — PURE (imports nothing) WHOSE NUMBERS ARE THESE (inc-mts8a8mr-ulmm, 2026-09-08): ① `decideLagShadow` = a reading arriving after a re-point, carrying the PREVIOUS slot's window … B-855a c1: `apiWindowSidecarName` = the `.apiwin-<key>` witness (outside the mirror block) ⇒ kb-file-structure.md
  slot-transitions.js — THE credential re-point ledger (2026-09-07, SHARED): a symlink has no history, so every LATE attribution question fell back to the spawn-time org. data/slot-transitions.jsonl `{sessionId, poolId, from, to, at, why}`, sessionId null = the pool DEFAULT moved … ⇒ kb-file-structure.md
  reading-repair.js — the readings-by-slot DATA REPAIR (2026-09-07, run by migration `2026-09-reattribute-readings-by-slot`): re-attributes or archives-with-a-reason the provably-foreign entries in usage-cache / usage-anchors / attribution.ndjson … **④ c2 `repairSidecarsByApiPhase` = the STANDING identity repair (boot + POST /api/usage/repair-identity + ledger row): the API phase wins over the panel-stamped sidecar** … ⇒ kb-file-structure.md
  usage-anchors.js — Quota dead-reckoning DATA FOUNDATION (2.261.0, user-designed 惯性导航 step 1): every ground-truth usage reading (statusline/⟳/get_usage/limit-banner — any usage-cache write) becomes an ANCHOR paired with … (an anchor names an ACCOUNT, never a session … ⇒ kb-file-structure.md
  usage-estimator.js — Quota dead-reckoning ESTIMATOR (B-fcff v2, 2.263.0): est_u = anchor_u + learned_rate × ledger_cost_since_anchor, per identity per bucket. ⇒ kb-file-structure.md
  fixture-guard.js — PURE (imports nothing) THE ONE DECLARATION OF WHAT A TEST FIXTURE LOOKS LIKE, so no production reader can count one (2026-09-09): the `vs-` scratch prefix under a tmp root + the `e2e00000-0000-4000-8000-` synthetic session-id family … ⇒ kb-file-structure.md
  usage-origin-backfill.js — the one-shot BACKFILL of `origin` onto the permanent ledger (2026-09-10, migration `2026-09-usage-origin-backfill`): walks the transcript tree ONCE into rid → {origin, wf, agent, parent cwd} and names every existing row — a codex row is 'main' by rule with NO lookup … ⇒ kb-file-structure.md
  fixture-ledger-purge.js — the one-shot PURGE of test-fixture rows from the permanent ledger (2026-09-09, migration `2026-09-purge-test-fixture-ledger`): archives every fixture row with a PER-CLASS reason (fabricated vs a real CI-probe turn in a throwaway cwd — counts and TOKENS are per class so the … ⇒ kb-file-structure.md
  ledger-slot-backfill.js — one-shot RE-KEY of the ledger rows the OTel org attributed (B-f69c ②): provable rows → the credential slot; lag shadow + dead slot refuse; the rest ARCHIVED with a reason; rates re-learned; a crashed pass RESUMES (never archived twice) ⇒ kb-file-structure.md
  server/auto-cli-loop.js — the auto-cli quota refresh loop (quota r2/r3): one `claude -p /usage` per tick max; projection read ≤10 min before the line, one per BUCKET per identity; state in data/auto-cli-state.json, stamped BEFORE the spawn, future stamps clamped ⇒ kb-file-structure.md
  weekly-lanes-unfold.js — one-shot REPAIR of the plan weekly lane (inc-mubu23bd-5vxi, migration `2026-09-weekly-lanes-unfold`): an event-written plan 7d is re-derived from the newest windowed event on its slot; panel-sourced never touched; a refused repair FAILS the run ⇒ kb-file-structure.md
  usage-walker.js — THE ledger walk, ONE module (2.297.0): in-process it YIELDS every MiB (`onBudget`, perf ⑥ — readers await `scanSettled()`), the daemon child + shipped scanner stay sync; gate test-usage-walk-parity … ⇒ kb-file-structure.md
  conversation-index.js — Conversation-location index (2.297.0, R3 tail): data/conversation-index.json records which machine owns each conversation from BOTH discovery listings and transcript fetches — resume host-inference lo… ⇒ kb-file-structure.md
  account-material.js  — Device-tier credential-MATERIAL mechanics (2.298.0, data/subs formalized as device #0's account store): repointPoolSymlink (the 2.251.0 hot-swap primitive verbatim) shared by accounts.setPoolTarget AND the daemon's sealed-orders reflex (bundled). Decisions stay orchestrator-side; material acts are one implementation per machine.
  login-expiry.js — PURE (零依赖) 登录会话寿命判定 (2026-09-07): `claudeAiOauth.refreshTokenExpiresAt` 是登录会话的**绝对**截止时间, 刷新 access token 不会推迟它(实测: 30 分钟前刷新过的成员仍然只剩 17 小时); 过期后 CLI 刷新拿到 invalid_grant 并把 accessToken/refreshToken/expiresAt **清空**却保留截止时间与 scopes = 磁盘上"已登出"的形状. loginState(creds, now) … ⇒ kb-file-structure.md
  server/login-expiry-watch.js — 被动巡检(boot+5min, 只读文件, §ban-safety 零 vendor 调用): 每个登录最多三条"For you"收件箱条目(24h/1h/已过期), 点名账号与到期时刻, 账本 data/login-expiry.json 原子落盘(含 `since`=本账本开始盯梢的时刻) ⇒ 重启不重发、重新登录即静默、升级当天不会为几周前就死掉的账号刷一屏 urgent(r2: 实测该实例首次巡检发了 3 条, 全是非路由成员); 收件箱拒收就回滚该档下次重试(标成已发=静默失败) … ⇒ kb-file-structure.md
  harness-settings.js — PURE (bundle+server+daemon) THE DECLARED per-harness settings TABLES (design-harness-settings, 2.369.123): `{prefix, category, files, rows}` joined to the descriptor by identity; `apply` = spawn|server|cli-config; `coerce` closes a non-combobox enum's vocabulary (r7); checkTable/buildConfigPlan ⇒ kb-file-structure.md
  harness-config.js — SHARED (fs/path/os) THE CLI-config applier every machine runs: CAS JSON + comment-preserving TOML writers — write THROUGH a symlink, keep the mode, refuse by name, re-check every `rel` (r7); applyConfigPlan/readConfigPlan, `CFG|` receipts; embedded as FILE TEXT into the shipped helper ⇒ kb-file-structure.md
  server/harness-config-sync.js — ORCH: harnessSetting/harnessDeclares/harnessSpawnSettings (typed reads off `session.backend`, never a literal id — test-architecture §46), cliConfigPlan → syncCliConfig (hookRegistrationSafe-guarded; names a refused enum value), cliConfigStatus (fresh receipts), onSettingsWrite (live verbs by capability) ⇒ kb-file-structure.md
  backend-caps.js — PURE per-backend switching-capability registry (P4切片1, 2.368.21): {pool, hotSwitch: verified|impossible|unverified, planC, sealedOrders, resetCredit, quotaProbe} — 池引擎按能力门控, hotSwitch是实验判定(claude=verified法证三事实; codex=impossible: CODEX_HOME启动canonicalize+token驻内存, 2026-08-24实验) … Client-side PURE `composerSendModes(inputModes)` = what the two send keys do mid-turn. ⇒ kb-file-structure.md
  auto-resume-signal.js — PURE (imports nothing, 2026-09-08): does a NORMALIZED quota reading say the wall a conversation is WAITING on is gone? `windowOpened` needs THREE facts and each was a real way to spend a billed turn into a wall — the limit **LANE** matches (`snapshot.limitId` … ⇒ kb-file-structure.md
  resume-continuity.js — PURE **resume ladder** (B-6b6d, owner ruling 2026-09-07): `resumeSpawnPick({explicit, conversation, instanceDefault, resume, hasSource})` → `{value, origin: chosen|conversation|instance|harness}` — 一个对话在 resume/fork/restart 时保留**它自己的** model/effort, 实例默认值只对**新建**会话生效 … ⇒ kb-file-structure.md
  workflow-name.js — PURE (imports nothing) the NAME of a Workflow run as the chips show it (2.369.137): `workflowNameFromAck` (ack Summary line / input.name / meta.name / script basename) + `shortWorkflowName` (id-like head, first clause, word-boundary cut); the whole line rides as `summary` ⇒ kb-file-structure.md
  model-echo.js — PURE (imports nothing, CJS into the bundle) THE ONE parser of the CLI's `/model` echo (`parseSetModelEcho` → {alias, id} | null; plain, backticked (CLI ≥2.1.257), ANSI forms): status bar, card label, lock repin; gate test-model-echo (25) ⇒ kb-file-structure.md
  search-card.js — PURE web-search card helper shared server+browser (2.369.43): renderSearchOutput (a codex search completion — `web_search_end` on 0.120-0.149, `item_completed Extension web.search` on the installed 0.153.4 — → "title — url / snippet" blocks, page-action heads, 20/4KB caps … ⇒ kb-file-structure.md
  user-channel.js — PURE(零依赖, CJS进bundle) claude 自己的 agent→user 通道 `--brief` 的两个工具 SendUserMessage(别名 Brief) / SendUserFile: 分类器 `userChannelKind`(不属于通道的工具必须返回 null——高亮卡的含义就是'agent 在跟你说话', 借用样式就是撒谎)、typed record `userChannelRecord`(两个 schema **逐字 dump 自 2.1.257 二进制的 zod 定义** … ⇒ kb-file-structure.md
  permission-rules.js — PURE model + DOM-free tree renderer for the READ-ONLY "where does this rule come from" view (2026-09-07, owner ruling 10 / §2.14): the ONE typed record every harness produces (layers → rules, `kind` allow|deny|ask|value) … ⇒ kb-file-structure.md
  local-oracles.js — PURE registry of human-triggered ZERO-NETWORK CLI reads (owner ruling 6): each ORACLE carries its own PROOF (tool + date + CLI version + per-run INET connect counts) and each measured-and-REJECTED candidate (`claude auth status` 5, `claude agents --json` 5 … ⇒ kb-file-structure.md
  server/permission-rules.js — the READ-ONLY rule READER + the oracle RUNNER (ORCH): rung by caps row never by backend id (claude=settings files off disk / codex session=the session's own wrapper via the `read-permission-rules` stdin verb — instance NOT OFFERED … ⇒ kb-file-structure.md
  lib/permission-rules-view.js — the client half (tree + copy-path + the instance modal + the oracle modal), human-triggered everywhere; a transport failure becomes the SAME typed record so the honest-empty-state rule holds on every path; **“human-triggered” governs who ASKS** … ⇒ kb-file-structure.md
  channel-store.js — SHARED (fs+path only) persistence for data/channels/: json stores · APPEND-ONLY msgs ndjson · date-rolled audit; ONE loader — unreadable ⇒ set aside `<name>.corrupt-<ts>` (bytes kept) + For-you item; failed rename ⇒ writes refused `store-blocked` ⇒ kb-file-structure.md
  channels/index.js + channels/fake.js — ORCH: the adapter REGISTRY and its CONTRACT (design §4) — a declared capability must be implemented, an UNDECLARED one THROWS, every failure is a typed {code, retryable} from a closed set, `history()` returns nothing unasked-for and ADVANCES NOTHING … ⇒ kb-file-structure.md
  channels/lark.js — ORCH (design §6.3/§12.1/§13, P1a) THE LARK / 飞书 READ adapter behind the §4 contract: a USER token minted by the loopback's FIXED mode, `im/v1/chats` discovery (paged); 2026-09-22 every resolveIntegration carries the ACCOUNT's `credentialKey` … ⇒ kb-file-structure.md
  channels/gmail.js — ORCH (design §6.3/§12.2/§14.2, P1a) THE GMAIL READ adapter — the DELEGATING row: the OAuth client comes from `resolveIntegration('gmail', {credentialKey})` through the `VIBESPACE_GDRIVE_CLIENTS` presets; 2026-09-22 each ACCOUNT resolves ITS key, never the row's pick … ⇒ kb-file-structure.md
  channels/live/lane.js + live/lark.js + live/gmail.js — ORCH (design §6.4 / fence 11 / decisions 18+20, P1b) THE PUSH LANES: `lane.js` = the vendor-free CORE (liveness is POSITIVE evidence; THE ACK IS THE RETURN — a transport acks only after the engine's `onEvent()` resolved, which is after the record is durable … ⇒ kb-file-structure.md
  channels/agents.js — ORCH (design §12.3, P3) THE BUILT-IN AGENTS ADAPTER behind the §4 contract: conversations = live agent sessions, reach = msg-acl through channel-acl's ONE crosswalk, send = THE delivery ladder (spendReason peer-message — an approved outbox message is the user's own) … ⇒ kb-file-structure.md
  server/integration-store.js + integrations-wiring.js + routes/integrations.js — ORCH (§14.3/§14.4): `source` DERIVED, keyed `cluster:<k>` rung; 2.369.165 an account-bound row is not a card (404 binds-per-account, `own` retired, `presetsFor`, `legacyOwnValues`) ⇒ kb-file-structure.md
  server/channels-engine.js + channels-wiring.js + routes/channels.js — ORCH: ingest loop, log-before-anchor, one broadcast per pass; 2.369.165 the account's own client (`custom`), sign-in first, reauthorize = rebind, duplicate, remove refused by reference ⇒ kb-file-structure.md
  msg-acl.js — PURE reach ACL for agent messaging (Channels v1, 2.362.0): 组内互通 / 组externalVisibility / 会话override只放宽 / 多组并集 / 无组singleton. ⇒ kb-file-structure.md
  turn-state.js — PURE 权威回合状态(B3, design-harness-features §2.5/§3.5): isTurnState(未知态绝不塌成 idle) · turnStateEffect(state,{hasLabel})=一个态对 `_isStreaming` 与 spinner label 的含义(`requires_action`=turn 没结束只是停在用户身上; 裸 `running` 不覆盖活着的工具 label … ⇒ kb-file-structure.md
  rewind-ops.js — PURE 撤回语义(B3 §2.10/§3.2): claude `tombstone`(上游原话"渲染或持久化本流的消费者应当移除被指名的消息", 而我们两样都做⇒半截消息永远留着) 与 codex `thread_rolled_back{num_turns}`(ThreadRolledBackEvent 只有一个字段, 0.153.4 serde dump; 从 TUI 做的回溯在这里彻底隐形, 幽灵 turn 一直挂着) … ⇒ kb-file-structure.md
  harnesses/ — THE harness registry (design-harness-plugins §2.2; S4 first slice): index.js `get(id)` LOUD on unknown/empty (core never falls through to claude), `register()` for contributed harnesses; claude.js/codex.js/shell.js descriptors carry `quota` = QuotaSignalSource {normalize … ⇒ kb-file-structure.md
  opencode-serve.js — OpenCode serve-mode STORE FACTS (S9, B-03f2; SHARED): ONE lazily-started `opencode serve` per instance, **from its own empty data/opencode-serve/cwd throwaway git repo** (2.369.50: OpenCode boots an instance per DIRECTORY and indexes+watches that tree … ⇒ kb-file-structure.md
  opencode-events.js — OpenCode's LIVE lane (S9 remainder, SHARED): `/global/event` SSE (boots nothing, ~10s heartbeat ⇒ 45s of silence = dead, reconnect re-asks the locator) + an fs.watch on the sqlite store — the ONLY lane that sees another opencode process (MEASURED: a second serve's writes never … ⇒ kb-file-structure.md
  opencode-remote.js — PURE OpenCode-serve OP TABLE + runOpencodeOp (S9 remainder piece (e)): ONE machine-side implementation shared by the local rung, the `opencode-serve` agentd op (the daemon bundles the same module) … ⇒ kb-file-structure.md
  server/opencode-access.js — the ONE way to reach the OpenCode serve on ANY machine (S9 remainder, ORCH): `call(hostId, op, params)` picks the transport (local facts / `opencode-serve` agentd op / shipped ssh script), `readConversation` synthesises records once for every rung … ⇒ kb-file-structure.md
  server/opencode-pty-bridge.js — THE SERVE TERMINAL (S9 remainder piece (c), ORCH): connects SERVER-SIDE to `GET /pty/{id}/connect` and hands ws-create the {pid,onData,onExit,write,resize,kill} duck the session layer already speaks, so xterm/input/resize/the buffer file work unchanged and the serve's port … ⇒ kb-file-structure.md
  server/auto-resume.js — **2.369.108 the arm card names its TARGET (`armCauseFor` → `opts.cause` → `armNoticeFor`'s 4th arg: the soonest member + bucket, and the rejector's own discarded reset with the floor reason; chip tooltip says the same); 2.369.104 a hot switch names the member + seconds.** **2.369.97 ONE CARD PER CONTINUE: the cause rides the delivered prompt (`sendToSession(id, s, text, {note})` ⇒ `originNote` / `webui_origin_note` ⇒ card head "VibeSpace auto-resume — <cause>")** … ⇒ kb-file-structure.md
  server/cli-cmd.js — agent-CLI path resolver (boot `resolveCmd` + async twin) + `cliCmds.forSpawn`: a gone boot path is re-resolved ONCE before the local argv, single-flight per backend, handed to the adapter, logged old→new (B-a18e) ⇒ kb-file-structure.md
  server/instance-url.js — 本实例公开地址(2.367.0; 2.367.3 客户端唯一join=utils.absUrl, 地址经/api/home+instance-url广播下发, 严禁再用location.origin拼): 唯一解析者(显式frp映射 > agentd.publicUrl > env > 空)且**映射只是叠加层, 绝不写入设置**(取消映射即回落原值) + 'vibespace-instance' frp代理的唯一发布者(远程agent安装曾在背后发同名代理); 显式映射盖过设置, 安装副作用产生的auto映射只记录不改地址; auth关闭时拒绝发布. UI=Ports面板'This VibeSpace'行. ⇒ kb-file-structure.md
  server/design-kit.js — /design画布工具包(2.366.0): 从已安装CLI提取helper+payload(CLI自解压目录优先, 否则二进制流式扫描)+技能文本, 写data/design-kit/<ver>/ 并把第4步改写成vibespace-page publish(全有或全无, 缺锚点=明确不可用), 每次用helper自己--check验证; 状态栏design芯片→可见的design request→agent走kit→发布到本实例. ⇒ kb-file-structure.md
  server/published-pages.js — 发布页(2.364.0 接管artifact; 2.366.0 publishContent上传+srcKey=host:path+会话归属+onPublished唯一通知点; 2.366.1 shell/raw分离+compat prelude(randomUUID secure-context坑)+URL只由发问方Host或publicUrl决定): /p/<id>自托管可分享HTML, 默认私有+public锁, srcPath upsert稳定URL, CSP sandbox=同源XSS防线(勿加allow-same-origin). ⇒ kb-file-structure.md
  server/wrapper-files.js — chat-wrapper文件解析(2.339.2 resolveWrapperFiles: 碰撞会话经/proc argv定位真sidecar) + wrapperCaps(2.364.1: 能力=wrapper SIDECAR data/session-buffers/<id>.json的caps, 唯一读者, 无状态; 严禁从data/session-meta读caps; 2026-09-06增inputQueue=运行中wrapper是否发布队列/服务queue-op, ws层与客户端队列控件都按它门控 … ⇒ kb-file-structure.md
  server/conversation-deliver.js — THE conversation delivery ladder (2.362.0): channel socket→local inbox→owning machine's agentd peer-post op→durable stash drained at injection; jobs通知与agent消息同一实现; 2.363.0投递点即渲染peer卡片(emitPeerCard→normalizer.injectPeerCard — server-posted在CLI侧是body-less origin, 只有poster能渲染) … ⇒ kb-file-structure.md
  server/stdout/ — HARNESS STDOUT consumer registry (S5; **2026-09-07: the lazy deps are PROPERTY-ACCESSED, never called** — `mk()` Proxies over a plain `{}` are truthy but NOT callable, so `ref?.()` threw and silently dropped every permission-rule answer and every failed-peer re-stash … ⇒ kb-file-structure.md
  job-model.js — PURE Background Work model (2.342.0): schedule math (cron parse/next-fire/jitter/15min agent floor), permission predicates (conversation-lineage owner + view/control scopes, NO existence oracle), vendor-pattern vet, digest/update renderers (600B budget, never trips the 10240 wrap), panel schema validation. Gated by scripts/test-job-model.mjs (36). **TRIAGE (design §13, 2026-09-14): PURE `ackState`/`attentionOf`/`agentReadAcks`/`archiveVerdict`/`lastLineOf` — acknowledged only by a delivery that REACHED somebody, an owner-lineage read or the user opening the row; `archiveVerdict` never archives an unacknowledged failure; `ACK_LANES` = every lane the ladder answers ok:true on (message / channel / rpc-queue / user-inbox / remote-message — the codex lane was missing until 2026-09-16, census-derived now).** ⇒ kb-file-structure.md
  jobs.js — JobManager engine (2.342.0, docs/design-background-work.md): registry data/jobs.json + detached-wrapper spawns (identity pid+starttime+bootId, adopt-first boot, verified handle-kills, single-engine lock, park-after-6 crash loops, until-output cursor scan, cron ticks … **TRIAGE (2026-09-14): `job.ack` stamped at the stash DRAIN / owner-lineage read / POST /seen (a new run clears it); `run.lastLine` once at finalize; the ARCHIVE sweep (boot + 5 min: done after 24 h, failed 7 d SINCE THE ACK, never unacknowledged) writes data/jobs-archive.json BEFORE a record leaves the store — a failed archive write keeps every record and retries (2026-09-16 verifier); held notifications are TYPED and visible.** ⇒ kb-file-structure.md
  lib/jobs-layout.js — PURE (imports nothing, DOM-free) the Background Work panel's TRIAGE arithmetic (design §13, 2026-09-14; **2.369.121 session folds + `isAckable`/`ackableIds` for "Mark all seen"**): `badgeCounts`/`badgeText` = the ONE counter the rail badge + both panel summaries read (red = awaiting-user + UNACKNOWLEDGED failures, grey = "N seen"), `familyOf` (name-family rule), `foldTasks` (owner session → family → rows; expanded only for running / awaiting / unacked; persisted `jobsPanelFolds` applied after), `pruneFolds`, `heldText`. Gated by test-job-model + test-jobs-panel. ⇒ kb-file-structure.md
  peer-messaging.js — THE delivery primitive for jobs→conversation notifications (2.344.0, B-0bf4): scans the CLI's own session registry (~/.claude/sessions/<pid>.json + published <pid>.<sha>.key) and posts the documented two-frame injection (auth + stream-json user message) … ⇒ kb-file-structure.md
  otel-truth.js — PURE OTLP-logs parser (2.361.0, B-345b): the CLI's OpenTelemetry api_request events are the ONLY per-request channel that NAMES the billing org (transcript/statusline/rate_limit_event carry values, never identity). ⇒ kb-file-structure.md
  server/otel-ingest.js — loopback OTLP receiver (2.361.0; **DEMOTED TO CORROBORATION 2026-09-07 — it attributes NOTHING**): local claude sessions push their own api_request telemetry here (zero vendor calls). `organization.id` is the identity the CLI cached at SPAWN … ⇒ kb-file-structure.md
  usage-history.js — UsageHistory (2.61.0, codex 2.63.0): PERMANENT per-request token LEDGER for the Usage window; `DEFAULT_PRICING` = one row per MODEL VERSION, longest key wins (`fable-5-1`, `opus-5-5`, `mythos-5-1`); gate test-pricing-tiers (+ the binary's catalog oracle) ⇒ kb-file-structure.md
  session-status.js    — SessionStatusManager (session-level state/urgency set by the AGENT via vibespace-status CLI or by the user; user overrides of agent-set values record a pendingNotice injected as <system-reminder> into the NEXT chat-input … … the notice slot is a QUEUE since agent browser P1 (`pushNotice`/`consumeNotices`) ⇒ kb-file-structure.md
  user-todos.js — UserTodoManager (2.65.0, the GLOBAL user-facing TODO list / "For you" inbox): discrete items an AGENT filed that need the USER — the inverse of the agent's own TodoWrite; a server producer may file its words as STRUCTURE (`i18n`, a3 r4) ⇒ kb-file-structure.md
  task-groups.js — TaskGroupManager (岗位/Task-Group store; renamed from tasks.js in 2.39.0 — data/task-groups.json authoritative, migrated once from legacy data/tasks.json; ⊃ legacy groups, CRUD + bind/unbind + progress,… ⇒ kb-file-structure.md
  task-groups.js — TaskGroupManager (岗位/Task-Group store; renamed from tasks.js in 2.39.0 — data/task-groups.json authoritative, migrated once from legacy data/tasks.json; ⊃ legacy groups, CRUD + bind/unbind + progress,… … `browserProfileId` = the group's default browser profile for NEW sessions (P1) ⇒ kb-file-structure.md
  backlog-select.js — PURE backlog PRIORITY + ownership selection: `sortBacklog` orders every listing; `selectReminders` = ≤5 own claimed + unclaimed-HIGH + a count; `backlogNudge`/`nudgeTextAll` = ONE ≤500 B cleanup nudge (all groups) at `tasks.backlogNudgeAt` held, every turn ⇒ kb-file-structure.md
  webdav.js            — MountTokens (scoped bearer mount tokens, sha256-hashed, per-token chroot root + ro/rw) + registerWebdav (/dav WebDAV subset for VibeSpace↔VibeSpace mounting; auth = Bearer vsmt_ token, bypasses cookie auth, registered before json body parser)
  notification-senders.js — PURE (imports nothing, CJS) who VibeSpace speaks as when it delivers a notification (B-d963): `NOTIFICATION_SENDERS` + `isNotificationQueueItem` (the strip's old-wrapper hint); test-peer-delivery's producer census ⇒ kb-file-structure.md
  pty-duck.js — PURE (imports nothing) THE node-pty DUCK's listener SET (B-ae4b): `ptyListeners({hold})` + `pipePtyShim` for daemonPtyShim, the R6 create/re-open ducks, the OpenCode serve terminal; one slot let the consumer replace the liveness stamp. Gate test-pty-duck ⇒ kb-file-structure.md
  keeper-limits.js — PURE (imports nothing) THE ONE HOME for every process keeper's numbers (2026-09-13, the browser design's §3.5 rule made physical): GUARD_SAMPLE_MS 60 s · GUARD_CPU_PCT 150 · GUARD_CPU_SUSTAIN_MS 5 min · GUARD_RSS_BYTES 2 GiB · RUNAWAY_COOLDOWN_MS 1 h · CONCURRENT_CAP 6 — … ⇒ kb-file-structure.md
  desktop-viewers.js — PURE (imports nothing, bundled) P8-2 x5 ONE ACTIVE VIEWER per app window: `viewerState` active/blocked/watch with the agent lease, `nextActive` by recency, `viewersView` (panes, never socket ids), the client's `paneState`; gate test-desktop-viewers ⇒ kb-file-structure.md
  desktop-apps.js — PURE (imports only keeper-limits) the desktop-app MODEL (§2 row 1): registry rows + validate (an exec is a HUMAN's, §5), the `DISPLAY_BACKENDS` ladder; B-bfe6 browser rows (own 0700 profile); A3 scalePick = dpr × uiScale, relaunchVerdict ⇒ kb-file-structure.md
  desktop-display.js — SHARED (node builtins + cli-identity) machine FACTS for desktop apps (§2 row 2): `binOnPath` (YES remembered for good, NO re-checked at 60 s), `hostFacts` (`hostId` a parameter — non-local REFUSED by name in v1), `x11Env` (strips WAYLAND_DISPLAY … ⇒ kb-file-structure.md
  server/desktop-app-keeper.js — ORCH the LIFECYCLE of every desktop-app session (§2 row 3): `create({dataDir, env: agentEnv, broadcast, serverSetting, getTelemetry, singleton, limits, registryRows})` → launch/stop/keepAlive/noteInput/get/list/streamTarget/adoptAll/start/shutdown … ⇒ kb-file-structure.md
  server/desktop-stream.js — ORCH THE ONE ws bridge (§2 row 4): cookie auth INSIDE (401), 404; x5 `viewerSeats`: a BLOCKED viewer gets NO upstream, cut 4001 on a swap; Watch = the refused ALLOWLIST (keymap/display/geometry held for a takeover); lifecycle never relayed; 16 MiB cap … ⇒ kb-file-structure.md
  routes/desktop-apps.js — GET/POST /api/desktop/apps, …/:id, …/:id/stop, …/:id/keep-alive, …/:id/windows, the hosted xpra client /api/desktop/:id/xpra-ui/* (P8-2, behind our auth) + the singleton's /api/vnc routes; a non-local `host` REFUSED by name; failures `{error, code}` ⇒ kb-file-structure.md
  vnc.js — VncManager (2.104.0, in-container desktop; SINCE 2026-09-13 stream id `desktop-singleton` on the ONE bridge src/server/desktop-stream.js, its two routes in src/routes/desktop-apps.js … ⇒ kb-file-structure.md
  ssh-key-format.js — Private-key FORMAT classifier, PURE + shared server/browser (CJS pulled into the bundle like task-color-seq.js). ⇒ kb-file-structure.md
  ssh-key.js — Server-side private-key UNLOCK at import (2.246.0). ⇒ kb-file-structure.md
  ctx-sync.js — THE ctx-folder sync, ONE implementation for every machine (2.277.0). The old split — ssh=bidirectional rsync NO caps, dial=hashed sync with a SILENT ≤400-files/≤2MB cap — meant a 3MB context file reached every ssh host and never reached a dial device, invisibly. ⇒ kb-file-structure.md
  cli-identity.js — THE agent-CLI process identity, ONE rule in two spellings (B-3185 r3): `isCliProcess(pid,name)` (JS, used by discovery-facts ⇒ bundled into the daemon) beside `cliIdentityShellFns()` (the POSIX vs_is_cli the writer sweep and the ssh discovery CO leg embed verbatim … ⇒ kb-file-structure.md
  discovery-facts.js — Shared discovery INTERPRETATION (2.278.0) — deliberately tiny so the esbuild agentd bundle carries it (unlike the ssh one-file scanner, the daemon CAN share code); process identity (pidLooksClaude + the open-rollout scan … ⇒ kb-file-structure.md
  writer-sweep.js — THE pre-resume writer sweep, ONE implementation for every machine (CS separation, 2.276.0). The invariant: before resuming, no OTHER process may still be writing that conversation's transcript (two writers on one JSONL = the B-4058 corruption class) … ⇒ kb-file-structure.md
  remote-shell.js — ALSO buildRemoteExec (2.279.0): the five remote spawn command builders (ssh terminal/ssh keeper chat/ssh agentd pipe/dial pty/dial pipe) are ONE composition — `cd → pre → resolve → AMBIENT_OAT_UNS…` ⇒ kb-file-structure.md
  **CS SEPARATION RULE (2.276.0): `hostId` is a PARAMETER, never a branch.** Server-side logic is written ONCE against a machine handle; the only difference between this machine, an ssh host and a dialed-in device is the transport underneath it. `hosts.device(falsy)` returns device #0 (the local daemon has been a full DeviceManager since 2.158.0 — same binary/mux/op surface — but was unreachable through the API, so every feature was written twice BY CONSTRUCTION). Migration table: docs/design-cs-unification.md — the metric is PARALLEL IMPLEMENTATIONS (9 twin-sets → 0 unjustified as of 2.279.1) … ⇒ kb-file-structure.md
  hosts.js — HostManager (ssh host registry, connectivity test, keygen, remote discovery + first-user-message names, bootstrap, backend-status, dir-complete, fetchSessionJsonl = remote transcript → data/remote-jso… ⇒ kb-file-structure.md
  remote-fs.js         — RemoteFs (ssh-per-op remote filesystem: list/read/write/mkdir/rename/rm/stat/copy/move/download/archive/tar-stream folder transfer — mirrors /api/file* shapes; files.js dispatches on ?host=, cross-host copy = server relay)
  auth.js              — Optional password auth (scrypt, server-side cookie tokens in data/auth.json, per-IP rate limit; VIBESPACE_PASSWORD / VIBESPACE_GENERATE_PASSWORD=1). 2.103.0: optional Clerk SSO rides on top — `passwordEnabled` vs `enabled` split (Clerk alone enables auth) … ⇒ kb-file-structure.md
  clerk-auth.js — ClerkAuth (2.103.0, env-gated: VIBESPACE_CLERK_PUBLISHABLE_KEY = on-switch, frontend-API host + JWKS URL DERIVED from the key's base64 payload; VIBESPACE_CLERK_ALLOWED_EMAILS comma list, @domain = whole domain, EMPTY = reject all). ⇒ kb-file-structure.md
  agent-routes.js — setupAgentRoutes() (2.93.0 split): every agent-facing endpoint — vibespace-ask/status/task routes, task-context/prompt-context injection (incl. ⇒ kb-file-structure.md
  server/usage-probe-log.js — THE RAW /usage PROBE LOG (2.369.109, owner "把 /usage 的 raw 返回和 parse 结果存起来方便 debug"): every panel (`claude -p /usage`) and control (`get_usage`) probe appends ONE line to data/usage-probe-log.ndjson — what was sent (key, creds dir, argv, the machine-wide `~/.claude.json` oauthAccount before/after = the B-855a input; since c1 also the isolated `configDir` + its org before/after + the `identity` verdict), the verbatim reply, the parse, the write verdict (`write-refused` + `why` naming both identities = the identity gate); 8 MB × 2 ring; `GET /api/usage/probe-log?key=&rung=&limit=`. Zero vendor calls. ⇒ kb-file-structure.md
  usage-routes.js — setupUsage() (2.92.0 split): the ENTIRE usage/rate-limit cluster from server.js — passive statusline ingest, read-only OAuth token, opt-in active poll, on-demand quota refresh, codex rollout-tail summarizer (§ban-safety rules apply — see kb-design-lessons §9). **B-855a c1: `refreshViaCliPanel` spawns under an isolated `CLAUDE_CONFIG_DIR` (measured on the 2.1.274 binary: oauthAccount comes from `${CLAUDE_CONFIG_DIR || $HOME}/.claude.json`) and verifies the panel's identity against the API-derived window BEFORE any write; the ⟳ route answers `{rung, identityVerified, why, skipped}`.** ⇒ kb-file-structure.md
  opslog.js — Optional persistent ops log (2.115.0, env-gated no-op): VIBESPACE_OPSLOG_DIR → console tee into daily-rotated files + boot/exit/crash markers (VIBESPACE_OPSLOG_CEPHFS_* = server kernel-mounts the path-scoped subtree first, same mechanism as My storage). ⇒ kb-file-structure.md
  incident.js — FREEZE THE SCENE for a panic-button capture (2.239.0, the admin's correction: users troubleshoot their own problems — ssh/resume/kill — and destroy the evidence before anyone looks). ⇒ kb-file-structure.md B-8ebb: transcript identity + frozen tail for EVERY harness via the registry's store.locate/remoteFind (codex rollouts incl. .zst), never a backend ternary.
  routes/persistence.js — …also LAYOUT ROLLBACK POINTS (2.296.0, after a bug flattened a user's whole desktop layout): `writeLayouts` is the ONE choke point every layout write passes through … ⇒ kb-file-structure.md
  lib/usage-source.js — PURE (DOM-free, `t` injected) reading-PROVENANCE rules for the quota panels (2026-09-07): `readingSource(source)` names WHO produced the latest number (own /usage panel / own session / own session (limit hit) / session on another machine / **session transcript** / marked spent … ⇒ kb-file-structure.md
  lib/usage-eta.js — PURE (imports nothing; clock injected): `compactEta` = the per-donut token (65m/15h/3d), `fullEta` = per-account `2d21h38m`, `accountResetEta` = when an account is next usable (blocked ⇒ its LAST spent bucket's reset, else the EARLIEST; B-8b12 refused). ⇒ kb-file-structure.md
  lib/incident-recorder.js — "Report a problem" panic button (2.238.0, the timezone-gap fix): ALWAYS-ON client ring buffers (clicks/coarse keys — typed TEXT never recorded, only a per-burst marker; ws message TYPES both direction… inc-mubvu3a4-x8sb: `chatTraces[win].geom` (inView/unrendered cards at capture), ring tail 300. ⇒ kb-file-structure.md
  telemetry.js — Telemetry (2.84.0, local-first observability): monthly ndjson shards data/telemetry/events-YYYY-MM.ndjson of {ts,kind:error|event|boot|server-error|metric,name,detail,stack,version,ua,value}; summary(… ⇒ kb-file-structure.md
  adapters/
    base.js            — BackendAdapter + SessionHandle (abstract interface for AI backends)
    claude-code.js     — ClaudeCodeAdapter (Claude Code CLI: flags, JSONL, control protocol, format methods)
    codex.js           — CodexAdapter (Codex CLI: app-server mode, JSON-RPC, permission mapping)
    shell.js           — ShellAdapter (plain login-shell terminals — no AI; toolbar Terminal button, "Open Terminal Here")
    index.js           — createAdapterRegistry() factory
  routes/
    files.js           — File system API routes (browse, read, write, upload, download, clipboard paste, CSV streaming, format)
    persistence.js     — Persistence routes (layouts, bookmarks, themes, settings, user state, groups)
    sessions.js        — Session API routes (discovery, messages, subagents, kill)
    opencode.js        — OpenCode serve ACTION routes (S9 remainder, B-eac2): revert/unrevert, the `question` reply/reject, the serve's session list + status + todos, `/api/opencode/state`. Every route takes `host` and hands it to the ONE access layer (src/server/opencode-access.js) … ⇒ kb-file-structure.md
  lib/
    app.js             — App controller / mediator (~2000 lines; 2.82.0 split three prototype-mixin clusters out — installed at module tail, BEFORE client.js instantiates App)
    manage-agents.js — installManageAgents(App): Manage-Agents dialog + Anthropic/ChatGPT account rosters/wizard (~790 lines, split from app.js). **2026-09-14: every roster donut is a column with its OWN compact reset countdown under it (src/lib/usage-eta.js** … 2026-09-15: each row ALSO carries the account's next reset in full (`2d21h38m`, `accountResetEta`) and the two soonest rows of a list are highlighted (`markSoonRows` → `.usage-acct-soon`, a 30 s re-spell tick); 2026-09-16: a label-less donut column keeps a 10 px slot and the next-usable cell is a FIXED 12.5ch column (icon left, number right) so nothing wobbles row to row. … P4b: the CloakBrowser row (Install disabled WITH the server's reason) ⇒ kb-file-structure.md
    cli-config-chips.js — PURE (DOM-free, `t` injected) the ONE wording of a CLI-config receipt (receiptLine/offLine/applyHead) shared by the Settings chips and the Machines card ⇒ kb-file-structure.md
    usage-meter.js     — installUsageMeter(App): taskbar quota pies + usage popup + on-demand refresh (~370 lines, split from app.js); 2.369.115 `_renderUsage` first repaints the Agents roster's usage cells (`_repaintRosterUsage`) so the open panel follows the 8 s poll
    session-lifecycle.js — installSessionLifecycle(App): create/attach/resume/fork/view/kill + billing switcher + replayOpenSpec (~680 lines, split from app.js) RESUME PRESERVES THE HOME DESKTOP (2.295.0, a fleet user, inc-ms… … ⇒ kb-file-structure.md
    themes.js          — THEMES constant + ThemeManager class. **extractThemeValues probes inside a data-theme="dark" WRAPPER (B-b2d6, 2.268.9): custom properties inherit and only dark defines every var — a bare probe read live-preview inline overrides (and an applied custom theme's stylesheet values)** … ⇒ kb-file-structure.md
    browser-live-window.js — THE LIVE VIEW WINDOW (P2, §4.4/§3.7): window type `browser-live` + openSpec `{action:'openBrowserLive', sessionId, profileId}` (replays always in WATCH), the picture through `img.src` in a NET-zoom-1 container (utils' COUNTER_ZOOM), the mode bar / confirms / bind toggle ⇒ kb-file-structure.md
    browser-profile-picker.js — installBrowserProfilePicker(App): the pin's ONE entry point `session.pinBrowser` + the profile digest (P1 §3.2.5): the card-menu picker (Unpinned / profiles / the honest ADOPT item per rung), `pinBrowserProfile` (one write), the New Session dialog row ⇒ kb-file-structure.md
    browser-switcher.js — CLIENT (P4 second half, §7.4 UX / §7.5 / D33): `app.openBrowserSwitcher({profileId, sessionId?, preselect?})` = the dialog over `GET /api/browser/switcher` (every backend a row, disabled WITH its reason; the SOURCE chip; seats in three states from the server's structure) ⇒ kb-file-structure.md
    browser-trace-view.js — CLIENT (P5 client half, §4.5/§8 step 3/D35): the tool card's "Browser actions" row (thumbnails ALWAYS, list behind the button, ONE batched fetch per view), the before/after dialog with the position DRAWN, the live view's Actions pane, the Browser housekeeping panel ⇒ kb-file-structure.md
    ws.js              — WsManager (WebSocket with reconnect)
    window.js          — WindowManager (drag/resize/snap/grid; syncHiddenViews = ONE hider derivation; ≤768px synced minimize = TRUTH; P7 intoChain, setOwnerBadge; a2 resize handles aria-hidden; a drag never splits, merge ⇒ _afterUserMerge; A2 requestClose = the user-close veto) ⇒ kb-file-structure.md
    window-min-size.js — PURE a window's OWN minimum (2.369.158): the .window floor, capped at the workspace (r2), the drag clamp keeping the opposite edge, raise-to-min + keepInside, windowMinForPane; WindowManager.setMinSize = the terminal's CSS-min rule per window ⇒ kb-file-structure.md
    view-visibility.js — PURE (imports nothing) `hiddenReasons(…)`: which display:none hiders hold a window off-screen (2.369.112, inc-mu6bfv1t-4drq); `reconnectSlot` (perf ⑤b) = a hidden view's re-attach delay; gate test-hidden-view-suspend (102) ⇒ kb-file-structure.md
    reconnect-queue.js — the App's ONE reconnect queue (2.369.167 perf ⑤b, inc-mtndq0vb layer 3): displayed views re-attach at once, suspended ones by slot (active desktop first), un-hide = now, a drop resets; gate test-reconnect-storm (heavy) ⇒ kb-file-structure.md
    plugin-client.js   — PLUGIN CLIENT (Ph2): fetches /api/plugins/manifests, registers every enabled iframe plugin's windows as window types (sandboxed iframe + postMessage bridge ready/init/storage/notify/close), ⚙ menu rows; trusted-module host API incl. Ph1 `api.registerCommand/registerMenuItem/registerKeybinding/runCommand` (ids `plugin:<id>:<slug>`, all bound to the plugin's signal)
    window-types.js    — WINDOW-TYPE REGISTRY (Plugin Ph1, 2.369.19): registerWindowType({type, icon, replay}) — replayOpenSpec dispatches through it, unknown action = loud warn + telemetry 'openspec-unknown'; TYPE_ICONS is a view; core registers its 14 kinds where they live (scripts/test-window-types.mjs pins the sets)
    contributions.js   — COMMANDS + MENUS + KEYBINDINGS REGISTRY (Plugin Ph1): register{Command,MenuItem,Keybinding}/menuItems + ONE dispatcher; loud unknowns. MENUS ARE A TREE (2.369.124): submenu heads + parent, lazy resolution falls to top level, two levels max. ⇒ kb-file-structure.md
    gear-menu.js       — ⚙ menu TREE (2.369.124): heads Appearance/Tools/Communication/System/Help + Manage agents / Update / Sign out; ONE renderer: desktop left-opening flyout (120 ms hover intent, layered Esc, LIVE captions), phone/hover:none accordion (≥ 44 px rows). ⇒ kb-file-structure.md
    appearance-panel.js — the quick per-DEVICE prefs (Theme+✎ / Font size / Font / UI scale / UI font size / All Settings) moved VERBATIM out of app.js (2.369.124) = the Appearance ▸ head's panel; appearanceCaption = the head's live "Dark · 14px · 100%". ⇒ kb-file-structure.md
    tab-group.js       — Tab grouping mixin (chain, icon drag, tab bar, drag-out); SPLIT (§4.6): _normalizeChain at EVERY mutation, divider; split UX: NO drag splits (merge = the one exception), the strip button/badge, visual order, undo ⇒ kb-file-structure.md
    chain-layout.js    — PURE (CJS) the tab chain LAYOUT (P7 §4.6): normalizeChain (the ONE split validation), chainSyncKey (tabs+layout+pair), displayedPanes, ownerColor; split UX: visualTabOrder/swappedPair/splitPartner, dropSide GONE ⇒ kb-file-structure.md
    terminal.js        — TerminalSession (xterm.js wrapper, per-terminal settings). NET ZOOM 1 (2.369.118: counter-zoomed container, `_xtermPx`, `rescale()`). 2.369.125 phone key row: Copy screen key (`_copyScreen` reads the buffer, toast names the lines), SVG icon keys, the row wraps ⇒ kb-file-structure.md
    sidebar.js — Sidebar shell (filter/sort/merge pipeline, tab switching). **`LIVE_SESSION_FACTS` + `liveSessionFacts()` (2026-09-07 r3) = THE list of per-session live facts the `active-sessions` payload publishes under the names every client surface reads them by.** `_merge()` REBUILDS the row … … P2: `browserProfileActive` is its own scalar row + digest ⇒ kb-file-structure.md
    sidebar-workbench.js — Folders-tab three-zone rendering (installSidebarWorkbench mixin, ~540 lines)
    sidebar-mounts.js — Remote tab: machines (ssh hosts, Pair a device) + storage (mounts) rendering (installSidebarMounts mixin); D2: an Edit that switches a Drive/Gmail OAuth client opens Re-authorize under the new client, the token lands WITH it ⇒ kb-file-structure.md
    mounts-dialog.js — THE dialog component storage + channel accounts share (D1): mountsDialog / renderFields, oauthLinkRow, wireOAuthConnect, reauthDialog, api; storage pixel-identical; census test-oauth-field-parity (§1–§8) + test-mounts-dialog-extract ⇒ kb-file-structure.md
    channel-account-dialogs.js — the channel ACCOUNT dialogs as field specs on mounts-dialog.js (r4 chunk 3): type-first Connect, Re-authorize (+ client select), Edit (secret prefilled, client switch ⇒ reauth), Duplicate, Remove refused by name ⇒ kb-file-structure.md
    sidebar-rail.js — Sidebar activity rail (2.176.0, vscode-style, setting `sidebar.activityRail` default ON): installSidebarRail mixin — ~44px icon strip replaces the 3-tab bar; 2.369.125 `openRailPanel(app, tab, {syncId, forceWindow})` = ⚙ System…/Ports… → rail panel or ONE singleton window; a layout-sync REPLAY passes forceWindow (it must produce the window it names) ⇒ kb-file-structure.md
    sidebar-state.js   — Sidebar state mixin (star/archive/archivedFolders/rename/migration)
    sidebar-render.js  — Sidebar rendering mixin (folder groups, drag-drop)
    sidebar-tasks.js   — Sidebar tasks mixin (client task store + tasks-updated sync, task board render desktop+mobile, bind/unbind write-through, bind popover, attention aggregation)
    task-detail.js     — Task detail window (structured editor: objective/plan/progress/sessions/folders/contextDir/color)
    task-log.js — Task Group log viewer (2.85.0, openSpec openTaskLog, window type 'task'): Backlog tab (2.122.0 — status filter all/open/done/dropped, parked/resolved attribution chips, ✎ inline text+detail edit, ✓/⊘/… ⇒ kb-file-structure.md
    workflow-detail.js — Workflow detail window: the terminal wf_<runId>.json snapshot when the run is over; while it is LIVE, the same tree the chat card renders (2.369.119: /api/workflow lays the launching session's task_progress tree over the disk skeleton via PURE src/workflow-live.js) ⇒ kb-file-structure.md
    session-props.js     — Session Properties window (2.49.0: identity/state+history timeline/billing+on-resume account/config/Task-Group toggles/agent steps; openSpec openSessionProps, live re-render on broadcasts)
    channels-panel.js + channel-window.js — CLIENT: Channels panel + window; g3 IM: first screen = the GROUP LIST; Accounts = credential-first cards (health line, ↳ tracked rows, ⋯ in the storage order) + ONE Connect an account; composer by `composerMode` ⇒ kb-file-structure.md
    channel-groups-view.js + channel-group-dialogs.js — CLIENT (g3): PURE list/composer/@-autocomplete/picker arithmetic + `wakePreview` (= `wakeVerdict`); New group / Invite… (live sessions, never a Task Group whole, billed-turn echo) + group detail ⇒ kb-file-structure.md
    channel-outbox.js + channel-reach-editor.js — CLIENT (design §8 / §9.2 / §9.5, P3): ONE approval-card renderer, inline AND in the Outbox window; a4: a FIVE-line card, right-aligned reject · edit · approve, unknown = amber never red, Outbox = Awaiting | All grouped by state ⇒ kb-file-structure.md
    channel-words.js — CLIENT (a3 i18n): THE words for every channel/outbox/integration route CODE (`routeErrorText`; the engine's `error` sentence is the agent's contract), `policyModeText` via tc('policy', …), `groupTitle` (a Task Group's `title`, never a `name` it lacks) ⇒ kb-file-structure.md
    channel-chrome.js — CLIENT (a4): the DOM primitives every channel surface draws with — `icon()` (one SVG from icons.js: the feature's ONLY innerHTML), `el()` (textContent), `btn()` (house `mounts-btn`; `chan-btn` is gone), `noteLine()` (alert glyph on a warning) ⇒ kb-file-structure.md
    user-todos-panel.js — "For you" inbox UI (2.65.0, installUserTodos(app)): taskbar button #taskbar-user-todos (segmented badge, blink + click-to-jump toast on NEW items); 2.369.125 `#mobile-nav-todos` = the phone entry (same badge, same popup as a full-width sheet, exempt from the outside-tap closer); its own capture-phase Escape (defers to a popover/dialog) … ⇒ kb-file-structure.md
    jobs-layout.js — PURE (imports nothing, DOM-free) the Background Work panel's TRIAGE arithmetic (design §13, 2026-09-14; the user-todos-layout.js precedent): `attentionOf`/`badgeCounts`/`badgeText` = the ONE counter the rail badge + both panel summaries read (red = awaiting-user + UNACKNOWLEDGED failures, grey = "N seen" — jobs-panel.js used to compute it twice), `familyOf` = the name-family rule (` run`/-r<n>/-v<n>/-<n>/version tails stripped until stable, table-tested), `foldTasks` = owner session → family → rows (default-expanded ONLY for running / awaiting-user / unacked; the persisted `jobsPanelFolds` applied AFTER), `pruneFolds` (a fold outliving its group would grow user state without bound), `heldText` (the held-notification sentence from the server's STRUCTURE, words by the device's own t()). Gated by test-job-model + test-jobs-panel.
    usage-window.js — Usage dashboard window (2.61.0, ⚙ → Usage, window type 'usage', openSpec openUsage). ⇒ kb-file-structure.md
  sidebar-render-mobile.js — Mobile sidebar mixin (two-level folder/group navigation)
    mobile-nav.js — MobileNav class (window switcher, close, desktop tabs, gestures; ⚙ gear + quota chip since 2.99.0). 2.369.125: "+" long-press → the create sheet (Agent/Terminal/Files/Browser/Desktop), switcher "+" desktop tab + tab long-press rename/delete + window-row long-press → the 'window' menu, a Minimized section … ⇒ kb-file-structure.md
    session-card.js — Session card renderer (SVG icons, composite backend+mode icons). ⇒ kb-file-structure.md
    agent-meta.js      — Backend/agent metadata, SVG icon creation (createBackendIcon, createModeBackendIcon) + `effortLabel(backend, value, {capitalize})` = picker label with the harness's META `effortHints` one-liner (codex 'ultra' = 0.153 multi-agent delegation … ⇒ kb-file-structure.md
    file-explorer.js   — FileExplorer (browse, View menu, resizable columns, preview panel; uploads + ops clusters split out 2.93.0). 2.369.125 phone layout: Name-only column via `_getVisibleColumns`, the bookmark STRIP (`bk-collapsed`), 36 px toolbar, long-press Select… mode + `.file-select-bar` (the dblclick handlers are gated in the mode) ⇒ kb-file-structure.md
    file-explorer-uploads.js — installExplorerUploads: upload popover, batched multipart, inline progress + ring, synced history
    file-explorer-ops.js — installExplorerOps: context/background menus, clipboard copy/cut/paste, rename/delete/duplicate, archive compress/extract, properties; 2.369.125 the touch-only `Select…` row (first in the item menu, also on the background menu) enters the explorer's Select mode ⇒ kb-file-structure.md
    setup-flows.js     — installSetupFlows(App): onboarding wizard, Backup & migrate, password dialogs, diagnostics report (2.93.0 split from app.js)
    file-viewer.js     — FileViewer (dispatch by type via file-types registry, renderInto shared method; 2.341.0 floating ⟳ reload w/ media cache-bust)
    file-types.js      — File type registry (extension → category, icon, viewer, bypassBinary)
    code-editor.js     — CodeEditor (CodeMirror 6, Prettier format, server-side format, HTML/MD preview; 2.341.0 disk-freshness watch + ⟳ reload — kb-features §File Management)
    chat-view.js       — ChatView controller (virtual scroll, op dispatch, lifecycle; gap-seek mixin split out 2.92.0; B3 r8 `_retireLiveClaims()` = `exited` 分支**唯一**的退役者, 把这个视图关于「此刻」的每条声明一起收掉(压缩阶段 / harness 回合态芯片 / 执行中工具 run set) — 单一 owner 是因为 r7 只从分支里退役了压缩阶段, 「会话死亡要退役哪些声明」没有归宿, 于是被 terminate 的会话永远挂着一枚脉动的 waiting-for-you 芯片; 新的活声明加在这一个方法里并在 test-turn-truth-ui ⓪ b 的 CLAIMS 表加一行)
    chat-view-seek.js  — installChatSeek(ChatView): huge-JSONL continuous scroll (sentinel, slabs, teleport; `_renderGapMsg` = the 4th element path). inc-mubvu3a4-x8sb: every write/slab/trim traces, `_trimGapDom` keeps the KEEP ZONE; r1: gap slabs reserve heights + consume the carry + land by epoch ⇒ kb-file-structure.md
    chat-run-summary.js — PURE run-fold classifier + summary composer (2.369.37, imports nothing): messageKind (semantic collapseKind hint → claude tool-name map; ToolSearch = 'lookup', folds under the MCP toggle via foldToggleFor but is labelled "N tool lookups", never MCP) … ⇒ kb-file-structure.md
    chat-renderers.js  — Message rendering (user/assistant/tool/system, linkify, diffs, permissions; 2.369.43 search-kind card headers carry the query as an escaped `.chat-tool-query` chip via searchQueryChipHtml — claude WebSearch/WebFetch, codex web_search, ACP search). ⇒ kb-file-structure.md
    chat-input.js      — ChatInput (textarea, send, attachments, drafts, slash commands, TODO display; 2026-09-07 Alt+Enter=steer 和弦 + turn 进行中的发送模式提示行 + ≤768px 闪电按钮, 全部按 composerSendModes 门控绝不按 backend id, 和弦走贡献命令 `chat.steerNow` ⇒ kb-file-structure.md)
    chat-status-bar.js — ChatStatusBar (model, context%, cost, permission mode, task popup; B3 `setTurnState` draws only `requires_action`; `onSearch` chip ≤768px; Browser chip); a3 KEYED CHIPS: one <span> per key kept across renders, never a whole-bar innerHTML ⇒ kb-file-structure.md
    chat-search.js     — ChatSearch (Ctrl+F, CSS Custom Highlight API, server-side search)
    chat-minimap.js    — ChatMinimap (semantic scrollbar, turn markers, drag-to-jump); a2: the strip is `aria-hidden` (one marker per user turn of the WHOLE conversation, uncapped — 201 nodes per window → 0), the TOC button is a sibling outside it ⇒ kb-file-structure.md
    highlight.js       — Syntax highlighting (30 hljs languages, code block rendering, line numbers); a2: the `.chat-code-ln` gutter span is `aria-hidden` (a line number is paint) ⇒ kb-file-structure.md
    theme-editor.js    — ThemeEditor (floating panel for custom themes, ~50 CSS vars + 16 ANSI colors)
    hex-viewer.js      — HexViewer (binary file viewer with chunked loading)
    resizer.js         — Resizer (reusable drag-to-resize handle)
    layout.js          — LayoutManager (auto-save/restore, multi-client layout sync) … P7: tabChain persists layout+split; `chainSyncKey` carries the layout (the old tabs.join key forked silently) ⇒ kb-file-structure.md
    desktop-manager.js — DesktopManager (virtual desktops, Ubuntu-style previews, window routing) DRAG-REORDER (2.250.0): desktop preview wrappers are draggable(text/desktop-id); dropping onto another preview → reorderDeskt… ⇒ kb-file-structure.md
    stage-manager.js — StageManager (2.112.0 dynamic desktop 'Stage', default OFF via desktop.dynamicEnabled — docs/design-dynamic-desktop.md is the blueprint+progress anchor. ⇒ kb-file-structure.md
    external-editor.js — Ctrl+G split-pane CodeMirror editor (extracted from app.js)
    command-mode.js    — CommandMode (Ctrl+\ prefix key, tmux-style shortcuts, desktop switch); `v` = side by side on/off for the active tab group, `V` = swap (no group ⇒ a toast, never silent) ⇒ kb-file-structure.md (contributions.js essay)
    session-palette.js — Ctrl+K session palette (fuzzy session switcher; installSessionPalette, desktop only). ⇒ kb-file-structure.md
    icons.js           — Centralized inline SVG icon library (FILE_ICONS + UI_ICONS; `folder` is in FILE_ICONS); a2: `_s()` puts `aria-hidden` + `focusable="false"` on every <svg> — an icon is never a name, so icon-only buttons carry title/aria-label (test-ax-paint) ⇒ kb-file-structure.md
    customize-mode.js  — CustomizeMode (Firefox-style chrome edit mode: click-to-toggle + drag-to-move elements, zone arrangement, position pills)
    taskbar.js         — Taskbar rendering + window list popup + the 'window' menu registrations; split UX: Show side by side ▸ Beside {name} (on the right) in a ≥2-tab group, Unsplit / Swap left and right when split ⇒ kb-file-structure.md (contributions.js essay)
    browser-window.js  — Embedded browser window (iframe + URL bar + proxy toggle)
    vnc-view.js        — THE shared picture-view component (docs/design-desktop-apps §2 row 6, 2026-09-13): `loadRFB` (noVNC as the separate public/novnc.js ESM, dynamic-imported), COUNTER_ZOOM = inc-mtdrm922's `calc(1 / var(--ui-scale, 1))` on the canvas container (NET zoom 1, var-reactive) … ⇒ kb-file-structure.md
    picture-shell.js   — THE shared picture shell (P8-2 x2): counter-zoomed container, the ONE `.desktop-bar`, the reconnect ladder, the clipboard chrome; A1: the user's own Ctrl+C lands with no click on plain http (5 s), focus handed back; else the chip ⇒ kb-file-structure.md
    xpra-proto.js      — PURE xpra words (P8-2 x2): hello + every packet form (html5 v21 on 6.5.3), the NATIVE keymap (`x11_keycodes` Map + `query_struct`; keycodes-only is only translated onto `us` ⇒ IME text needs published rows), fit/place/kind, wheel as clicks, clipboard rule ⇒ kb-file-structure.md
    xpra-client.js     — the DOM-free xpra session over the upstream Protocol.js worker (P8-2 x2): fit-before-map + a bounded belt against the app's own resize/move, every non-popup window inside, draws in order + acked, clipboard deduped, IME publish-then-press ⇒ kb-file-structure.md
    xpra-view.js       — the xpra picture view (P8-2 x2): theme-colour pane, one canvas per window at net zoom 1, a hidden IME textarea (Ctrl+V left to the browser for its paste event), the app's title/icon to the caller, the plain-http click-to-copy chip, never silent ⇒ kb-file-structure.md
    desktop-window.js  — the singleton in-container Desktop window, REWRITTEN onto vnc-view.js (2026-09-13): decides only WHAT (the singleton, `streamUrl('/api/vnc')`) and gates connect on POST /api/vnc/start; behaviour byte-identical to the pre-extraction window (no autoReconnect — a Reconnect button, as before)
    desktop-app-window.js — window type `desktop-app`: one window per app session, view by stream KIND; x5 Resume here; P9b `window-live`; A2 closes when its app ends, outer ✕ asks the app; A3 scale chip, ⋯ Scale ▸ relaunch in place ⇒ kb-file-structure.md
    desktop-app-launcher.js — ⚙ "Desktop apps…" row + the toolbar Apps button as CONTRIBUTIONS (command `desktopApps.open`; `when` = a backend exists) and the launch dialog (createModalShell), CATALOG-FIRST since 2026-09-14: an intro line in plain words (zh+ja), the ladder verdict in the chip's words … ⇒ kb-file-structure.md
    utils.js           — Shared utilities (escHtml, createPopover, showContextMenu, fetchJson, copyText, StateSync)
    i18n.js            — i18n runtime: t(str, params?) with ENGLISH-STRING-AS-KEY, per-device lang (localStorage vibespace.lang), applyI18nToDom for index.html data-i18n/-attr; reload-on-switch (§16); `deviceLocale()` = the ONE Intl tag for every date a surface prints ⇒ kb-file-structure.md
    i18n-zh.js / i18n-ja.js — dictionaries ({'English': '翻译'}, 869 entries each; missing key = English fallback)
    autocomplete.js    — Shared directory autocomplete (setupDirAutocomplete)
    settings.js        — SettingsManager (sparse storage, server persist, WS sync, event listeners)
    settings-schema.js — Settings schema (types, defaults, categories). Claude/Codex/OpenCode sections DERIVED from src/harness-settings.js (2.369.123). `SETTINGS_CATEGORIES` IS the render census (§44; an unlisted category is unreachable); `SETTINGS_GROUPS` (2.369.132) only orders and folds it into the nav tree (§44c); `Browser` sits in the Services group (2.369.134) ⇒ kb-file-structure.md
    settings-ui.js     — SettingsUI (VS Code-style full settings, search + category nav; NON-BLOCKING same-level WINDOW, singleton-focus, 2.53.0). 2.369.123: harness sections carry an intro line + an apply chip per row; cli-config rows show the fresh local receipt and a human-triggered "Check machines…" ⇒ kb-file-structure.md
public/
  brand/               — Backend brand SVGs (claude.svg, codex.svg)
  index.html           — HTML structure
  style.css            — CSS: themes, sidebar, toolbar, workspace, windows, taskbar, settings (~2300 lines)
  chat.css             — CSS: chat view, compact mode, role indicators (~600 lines)
  viewers.css          — CSS: file explorer, media viewer, editor, hex viewer (~300 lines)
  theme-editor.css     — CSS: theme editor panel (~100 lines)
  bundle.js            — Built output (do not edit)
CLAUDE.md              — This file
docs/
  design-account-hardening.md — 2026-09-08 DESIGN (owner decisions pending, no code yet): the root fix for the 5-incident money batch (auto-resume's 130 billed continues / readings keyed by the spawn org / unseen login deadlines / unretracted warnings / inc-mts8a8mr-ulmm's in-flight-on-the-new-slot cascade). … ⇒ kb-file-structure.md
  design-split-ux.zh.md — 2026-09-23, SHIPPED 2.369.162: 拖动零并排 (tab 合并是唯一例外) + 合并后显式入口 (⫿ 按钮/菜单/命令 v) + 视觉顺序标签条 + 徽章 + 撤销; test-split-ux (85, heavy) ⇒ kb-file-structure.md
  design-accessibility-tree.zh.md — 2026-09-22 设计: AX 节点来源表 + 十步根治 + test-ax-budget; §8 精简版 SHIPPED 2.369.150(带外卡片/涂装/minimap 条 aria-hidden, chip 原地更新): 每窗 724 → 462, 不随 slab 增长; 4/5b/6/7/9 未做 ⇒ kb-file-structure.md
  design-integrations-per-account.zh.md (+ .md) — r4 SHIPPED 2.369.165: the OAuth client is chosen per account in the storage-mount dialog grammar; account cards = mount rows; Integrations window = browser keys only; D1–D8 at defaults (D2 = storage Edit reauth) ⇒ kb-file-structure.md
  design-reset-credits.zh.md — 2026-09-22 设计 + 定案 (自动消费默认永不; 有接口给按钮; 高级 auto): 收益模型 = openai 平滑递减 (撞墙即用) vs anthropic ReLU (拐点 = 剩余时间 ≥ 周额度/燃烧率), 原地补满已实测证实; 梯子按冷热分叉; 三模式 off/ask/auto; P1 codex / P2 claude 被动 / P3 待接口 ⇒ kb-file-structure.md
  design-harness-settings.zh.md — 2026-09-20 设计稿(未开工, 等 owner D1-D4): harness 级全局设置的专用区 = descriptor 声明的 PURE 表(src/harness-settings.js) + schema 派生分区 + harnessSetting 访问器(字面 harness id 归零) + 一个 base64 plan env 走既有 helper/run-cmd(不新增 daemon op) + 行下"写到哪里了"回执; 值留 data/settings.json 零迁移; 否决独立存储+迁移
  design-communication-panel-polish-audit.md — 2026-09-18 AUDIT (a1 of the comm-panel polish): every surface shot at zh/ja/en × three rail widths × 375 × dark/light by scripts/dbg-comm-surfaces.mjs; §0 root causes, §1 i18n table, §2 hierarchy, §3 measurements, §4 the plan ⇒ kb-file-structure.md
  design-browser-faces.zh.md (+ .md) — 2026-09-23 DESIGN (B-d03a): the THREE browser faces (iframe web view / agent browser / browser as a desktop app), 7 confusions named from code, directions A/B/C rendered in docs/mockups/browser-faces/ (pixel-verified PNGs), rubric ⇒ B now ⇒ kb-file-structure.md
  design-communication-panel-ui.md — 2026-09-18 DESIGN (a2): three rendered directions (A 列表优先 / B 分诊看板 / C 安静账本) as real HTML/CSS under the product's class names, a 6-criterion rubric, A chosen; §4 = the spec a4 built (inventory, chips, tokens, copy, phone) ⇒ kb-file-structure.md
  design-harness-plugins.md — 2026-09-05 调研: Codex支持缺口矩阵(P0-P3) + HarnessDescriptor抽象层/一致性套件/ACP通用第三方harness + VS Code式插件系统(manifest/派生激活/五层隔离/贡献点→现有注册表); 每节带backlog归属
  README.md            — Documentation index
  getting-started.md   — Installation, first run, quick tour
  terminal.md          — Terminal: dtach, multi-device, clipboard, idle detection, fonts
  window-manager.md    — Windows: grid, snap, command mode, presets, highlight
  sessions.md          — Sessions: discovery, groups, star/archive, drag-drop, filters
  file-explorer.md     — Files: browsing, bookmarks, viewers, right-click, drag-drop
  settings.md          — Settings: global/per-terminal, complete reference table
  mounts.md            — Shared S3 storage: my-storage, share minting, import links
  customize-ui.md      — Customize mode guide: show/hide, drag between bars, springs, alignment, extra rows
  editor.md            — Ctrl+G split-pane CodeMirror editor
  browser.md           — Embedded browser with proxy mode
  keyboard-shortcuts.md — Complete shortcut reference
  screenshot-helper.js — JS sanitizer for privacy-safe screenshots
  screenshots/         — Documentation screenshots (overview, sidebar, settings, grid)
```

## Developer Guide: Where to Find & Modify Code

### Common Tasks → File Location Map

| Task | Primary File(s) | Notes |
|------|-----------------|-------|
| **Add/change terminal behavior** | `src/lib/terminal.js` | xterm.js config, input filtering, cursor, font |
| **Session list / sidebar** | `src/lib/sidebar.js` + `src/lib/sidebar-state.js` + `src/lib/sidebar-render.js` + `src/lib/sidebar-render-mobile.js` + `src/lib/sidebar-workbench.js` (Folders tab) + `src/lib/sidebar-mounts.js` (Remote tab) + `src/lib/session-card.js` | Sidebar shell + state mixin + render mixin (desktop) + render mixin (mobile) + Folders-tab workbench + Remote-tab mounts + card factory |
| **Backend metadata / icons** | `src/lib/agent-meta.js` | Backend/agent meta, createBackendIcon, createModeBackendIcon, contrast adaptation |
| **Session discovery (RUNNING/STOPPED)** | `src/routes/sessions.js` + `src/session-store.js` | Lock-first algorithm, tmux detection, PID verification |
| **Window tiling / grid / snap** | `src/lib/window.js` | Drag, resize, grid cells, layout presets, freeform, Alt bypass, overlap switcher, pre-snap size memory, move mode |
| **Tab groups** | `src/lib/tab-group.js` + `src/lib/chain-layout.js` | Drag icon-to-icon to merge windows into tabs, Chrome-style tab bar, drag-out; side by side = the strip's button after a merge (never a drag) |
| **Custom grid presets** | `src/lib/app.js` → `_addCustomGrid()` + `src/routes/persistence.js` | + button adds, right-click removes, persisted in layouts.json |
| **Session starring** | `src/lib/sidebar.js` → `toggleStar()`, `isStarred()` | ★/☆ per session, starred first in sidebar + taskbar |
| **Task system / board** | `src/task-groups.js` + `src/lib/sidebar-tasks.js` + `src/lib/task-detail.js` | tasks.json store, board (Tasks tab), bind-as-tag, detail window, attention — design in docs/design-task-system.md |
| **Workflow detail viewer** | `src/lib/workflow-detail.js` + `src/routes/sessions.js` (`/api/workflow`) + `chat-renderers.js` (View Workflow btn) | Post-hoc dynamic-workflow/ultracode viewer; reads terminal `wf_<runId>.json` snapshot; per-agent View Log reuses subagent viewer |
| **Session rename** | `src/lib/sidebar.js` → `renameSession()` | Double-click name in sidebar, syncs to open windows via `app.syncSessionName()` |
| **Ctrl+G external editor** | `server.js` → `createEditorHelper()` + `src/lib/external-editor.js` | Fake "code" script + split-pane CodeMirror |
| **File viewer (open file)** | `src/lib/file-viewer.js` | Dispatch by type, size check, binary detection |
| **Code editor** | `src/lib/code-editor.js` | CodeMirror 6, language switching, Prettier/server-side format, markdown/HTML preview |
| **Chat mode** | `src/lib/chat-view.js` (controller) + `src/lib/chat-renderers.js` (rendering) | Virtual scroll, op dispatch, message rendering, tool cards |
| **Chat input / send** | `src/lib/chat-input.js` | Textarea, attachments, slash commands, draft persistence, TODO display |
| **Chat status bar** | `src/lib/chat-status-bar.js` | Model, context%, cost, permission mode dropdown, task popup |
| **Chat search** | `src/lib/chat-search.js` | Ctrl+F, CSS Custom Highlight API, server-side search |
| **Chat minimap** | `src/lib/chat-minimap.js` | Semantic scrollbar, turn markers, drag-to-jump |
| **Hex viewer** | `src/lib/hex-viewer.js` | Binary display with chunked loading |
| **Themes / colors** | `src/lib/themes.js` + `public/style.css` | 6 built-in themes, CSS variables `[data-theme="..."]` |
| **Theme editor** | `src/lib/theme-editor.js` + `public/theme-editor.css` | Floating panel: ~50 CSS vars + 16 ANSI, live preview, save/load/delete |
| **Layout save/restore + sync** | `src/lib/layout.js` + `src/ws-handler.js` | Auto-save debounce, layout-sync WS protocol, openSpec pattern |
| **Virtual desktops** | `src/lib/desktop-manager.js` + `src/ws-handler.js` | Create/delete/rename/switch desktops, Ubuntu-style previews, per-desktop grid, drag-to-move, multi-client sync |
| **Usage / rate limits** | `src/usage-routes.js` + `data/bin/vibespace-usage` + `src/lib/usage-meter.js` | PASSIVE statusline capture by default (§ban-safety); on-demand ⟳ + opt-in active poll + auto-cli — see §9 and the usage-routes.js entry |
| **WebSocket protocol** | `src/ws-handler.js` + `src/lib/ws.js` | All WS message types in ws-handler switch/case |
| **State sync / drafts** | `src/sync-store.js` + `src/lib/utils.js` → `StateSync` class | Versioned diff broadcast, reconnect recovery, draft persistence |
| **Syntax highlighting** | `src/lib/highlight.js` + `src/lib/chat-renderers.js` | 30 hljs languages, line numbers, language picker |
| **File routes** | `src/routes/files.js` | Browse, read, write, upload, download, clipboard paste |
| **Persistence routes** | `src/routes/persistence.js` | Layouts, bookmarks, themes, settings, user state, groups |
| **Taskbar** | `src/lib/taskbar.js` | Taskbar rendering, window list popup, right-click context menu |
| **Command mode** | `src/lib/command-mode.js` | Ctrl+\ prefix key, tmux-style window commands |
| **Embedded browser** | `src/lib/browser-window.js` | iframe + URL bar + proxy toggle |
- | **Desktop apps / a display backend** | `src/desktop-apps.js` (PURE) + `src/desktop-display.js` (SHARED) + `src/server/desktop-app-keeper.js` + `desktop-stream.js` + `src/routes/desktop-apps.js` + `src/lib/vnc-view.js` / `desktop-app-window.js` / … ⇒ kb-bugfix-invariants.md
| **Shared utilities** | `src/lib/utils.js` | escHtml, createPopover, showContextMenu, fetchJson, copyText, showImageOverlay (the ONE image-zoom overlay — chat thumbs + pending chips), StateSync |
| **i18n / translations** | `src/lib/i18n.js` + `i18n-zh.js`/`i18n-ja.js` + `scripts/i18n-extract.mjs` | t() wrapping rules + dictionary workflow in §16 — read it BEFORE adding UI strings |
| **CSS / visual styling** | `public/style.css` + `public/chat.css` + `public/viewers.css` | Split by component area |
| **HTML structure** | `public/index.html` | Sidebar, toolbar, workspace, dialogs |

### Architecture Patterns — INDEX

**Full text: docs/kb-patterns.md (moved verbatim). Cross-cutting coding patterns — the mediator rule, one-time WS handlers, agentEnv() spawn sanitization, no-native-dialogs, drag rAF coalescing, uiScale drag conversions, popover/toast conventions, sidebar poll digest. Read before writing frontend chrome or a new spawn path.** Index:

- All frontend cross-class communication goes through `App` (mediator pattern). Classes receive a…
- One-time WebSocket handler pattern: Register via `ws.onGlobal()`, match on `type` + `sessionId`,…
- Debounced auto-save: `LayoutManager.scheduleAutoSave()` waits 2s after last change. Blocked by …
- Resizer: Reusable component with `inside: true` mode for fixed-position elements (sidebar). Don'…
- Layout restore: `attachSession()` returns `winInfo` synchronously (the DOM element). Position is…
- Proportional bounds tracking: `win.gridBounds` stores position `{left, top, width, height}` as f…
- Title-bar right-click = full window menu (2.212.0): showWindowContextMenu (taskbar.js, shared wi…
- WebSocket reconnect re-attach: On WS reconnect, all active sessions are re-attached. Timeout ≠ d…
- Layout sync (multi-client): State-based — full workspace state broadcast via `layout-sync` WS me…
- Atomic openSpec: `createWindow({ openSpec })` sets the openSpec before `_notify()` fires, ensuri…
- ChatView module split: ChatView is the controller (virtual scroll, op dispatch). Rendering deleg…
- Shared utilities: `createPopover(anchor, className, opts)` handles popover positioning/dedup/clo…
- Agent sessions get a SANITIZED env, never raw `process.env` (2.227.12, `agentEnv()` in ws-handle…
- Server-side settings reads use `serverSetting(key)` (server.js; ws-handler gets it via deps) — b…
- No native dialogs: `prompt()/alert()/confirm()` are banned — use `showInputDialog`/showConfirmD…
- Drag mousemove is rAF-coalesced everywhere (window.js titlebar drag + resize, tab-group icon dra…
- Cursor→window positioning MUST convert viewport→workspace coords (2.100.3, trace-diagnosed real …
- UI scale (DPI) + UI font size (2.257.0, per-DEVICE like the language): gs-menu rows writing loca…
- Sidebar session poll (5s) pauses while `document.hidden` (30s heartbeat + immediate catch-up on …
- Tab groups (chain model): Windows can be merged into tab groups by dragging one window's icon on…
- Tab merge hit-test: Shared helper `_detectTabMergeTarget(x, y, sourceWinId, hiddenEls)` on tab-g…
- Window type icons: Each window type has an inline SVG icon (`TYPE_ICONS` in tab-group.js): termi…
- Move mode: Right-click taskbar → Move. Full-screen overlay blocks all UI interaction. Window res…
- Loading screen: Inline splash in HTML (no CSS dependency). Fades out after `app.ready` promise r…
- Resume-all boot popup (2.250.0): sessions that come back STOPPED restore as read-only history wi…
- showContextMenu takes ONE class name (add modifiers with classList.add after) · every chrome suite pre-sets `vs-onboarded` via scratch.mjs `ONBOARDED_SOURCE` before each navigate (§47 census — the wizard also skips itself on a box WITH sessions, so a miss is green locally, red on the runner) ⇒ kb-patterns.md

### Server-Side Key Functions

| Function | Location | Purpose |
|----------|----------|---------|
| `createEditorHelper()` | server.js | Generates `data/bin/code` script on startup |
| `attachToDtach()` | server.js | Creates PTY bridge to dtach socket |
| `restoreSessions()` | server.js | Reconnects to surviving dtach sessions on startup |
| `setupSessionPty()` | server.js | Wires PTY onData (chat JSON parser or terminal raw) + onExit |
| `refreshRateLimit()` | server.js | OPT-IN active usage poll (`accounts.activeUsagePolling`, default OFF — default is passive capture, §9) |
| `registerWsHandler()` | src/ws-handler.js | All WebSocket message type handling (create/attach/kill/etc.) |
| `cwdToProjectDir()` | src/session-store.js | `cwd.replace(/[/._]/g, '-')` — deterministic encoding |
| `recoverCwdFromProjDir()` | src/session-store.js | Greedy reverse: try segments as-is, `.` prefix, `_` |
| `SessionMessages` | src/session-store.js | Unified JSONL+buffer access, chatStatus, taskState |
| `SyncStore` | src/sync-store.js | Versioned key-value store with diff broadcast |
| `broadcastActiveSessions()` | server.js | Pushes session list to all WebSocket clients |

## Key Design Decisions & Lessons Learned — INDEX

**Full text: docs/kb-design-lessons.md (moved verbatim; §-numbers unchanged). Read the relevant § BEFORE touching that area; update it in the same commit.**

- **§1 dtach for session persistence** — dtach NOT tmux (zero rendering layer); `dtach -c` never `-n`; NEVER delete socket files (the socket IS the session); pty-wrapper tees output to the buffer file.
- **§2 Mouse/scroll** — no middle layer; strip `\e[I`/`\e[O` focus events from onData; 6 tmux approaches failed (docs/history-archive.md).
- **§3 Ctrl+G external editor** — fake `code` script (GUI-editor whitelist trick) + HTTP bridge; never print to the terminal from the helper; POST carries the vsst_ Bearer.
- **§4 Session discovery** — LOCK-FIRST; claimJsonls exact→tail→mtime (mtime only over no-tail-evidence files); each lock claims ≤1 JSONL; status values tmux/external/stopped.
- **§5 Project path recovery** — cwd→dir encoding `replace(/[/._]/g,'-')` is deterministic; the reverse is ambiguous, prefer forward.
- **§6/6a/6b/6c Layout** — autosave/restore + gridBounds fractions; custom grid presets; MULTI-CLIENT sync anti-ping-pong (4 guards: seq, user-dirty w/ 60s expiry, defer-while-interacting, rounding epsilon — do not regress ANY); virtual desktops (visibility:hidden model, purge-vs-preserve on close).
- **§7/7a Modular frontend + mobile** — App mediator; isMobile/isTouch; long-press=contextmenu; mobile yield-sidebar is CENTRAL.
- **§8/8a/8b xterm + rendering** — WebGL renderer (do not remove), clearTextureAtlas on font change, alt-screen awareness, query-response arbitration, self-hosted fonts (never third-party origin); hljs span-split fix; 交付夜 invariants (self-update dialog, /dav DAV class 2, archive ops, VNC probe retry, mountpoint hygiene ladder).
- **§9 Usage / rate limits — §ban-safety home** — DEFAULT = passive statusline capture; active poll is OPT-IN (accounts.activeUsagePolling); read-only token NEVER refreshes; OAuth Bearer + anthropic-beta header; approach history in docs/history-archive.md.
- **§10 Settings** — schema-driven; only add settings with working code; serverSetting() server-side (never the SyncStore).
- **§11 Chat mode dual-arch** — THE big one: chat-wrapper stream-json, permission stdio protocol, subagent virtual sessions, workflow viewer, goal loop (claude native + codex thread/goal RPC), interrupt delayed-fallback, model/effort/permission mid-session switches, model lock v2, fallback policy, task wakeups. Read §11 in the kb before ANY chat-pipeline change.
- **§12 Font discovery** — queryLocalFonts → fc-list fallback; client fonts are what matter.
- **§13 StateSync** — versioned diff broadcast + reconnect resync; register stores before set() (writes silently drop otherwise).
- **§14 Syntax highlighting** — 30 langs, EXT_TO_LANG, splitHighlightedLines carries the span stack.
- **§15 Frontend optimization** — minify+gzip; delivery-stall watchdog; stale-tab reload is PER-TAB (sessionStorage); index.html route injects ?v=mtime cache-busting.
- **§16 i18n** — see Laws digest above + kb §16 for the full wrapping rules.
- **§17 UI design conventions** — see Laws digest above + kb §17 for the token/radius/type tables.
- **2026-06-09 review invariants (v2.8.0)** + **2026-07-03 review invariants** — two do-NOT-regress lists (escHtml quotes, atomic JSON writes, AbortController listeners, XSS rules, one-time WS handler self-guards…). In the kb.

## API Reference — INDEX

**Full REST + WebSocket reference: docs/kb-api.md (moved verbatim). Adding/changing a route or WS message type ⇒ update kb-api.md in the same commit.** Quick shape: REST under /api/* (files, sessions, usage, accounts, hosts, mounts, tasks, agent-facing vsst_ endpoints), WS /ws (create/attach/input/kill + msg/layout-sync/state-sync families), /dav WebDAV, /proxy.

## Features Summary — INDEX

**Full behavior reference: docs/kb-features.md (moved verbatim). These sections describe SHIPPED behavior with embedded do-not-regress notes — read the relevant one before changing that surface.**

- **Deployment & Onboarding** — auth (password/Clerk SSO), Docker/K8s/Helm, onboarding wizard, config export/import (4-touch-point gotcha), Manage Agents, account switching + billing identity + verdicts (the accounts mega-essay), chrome customization/springs/zones, toolbar scale, mounts/storage rows, remote hosts + keeper, orphan sweep, kill-path teardown, async discovery sweep.
- **Terminal Management** — dtach sessions, per-terminal settings, bell/idle detection, clipboard image paste, CJK, multi-device size, contrast.
- **Chat Mode** — message rendering, permission cards, relative-path linkify, metadata popup, virtual scroll, minimap, search, drafts, uploads, resume bar, read-only views.
- **Window Manager** — snap/grid/presets, shake-bypass, tab groups, move mode, virtual desktops, taskbar stacking, resume-all boot popup.
- **Session Management** — Task Groups (岗位) refactor + backlog claim model + status/urgency + injection contracts + repo task files + remote ctx sync + group admin + New-Session prefills + right-click menu + Session Properties + per-session config. The 2.39.0 concept-refactor block SUPERSEDES older bullets where they conflict.
- **File Management** — explorer, multi-select, clipboard ops, archives, viewers (PDF/DOCX/XLSX/PPTX/CSV/hex/eml), uploads, cross-host.
- **UI** — themes, theme editor, embedded browser + proxy normalization, window-list popup.

### claude CLI 中途文本丢失 (2026-07-13, 上游bug — 产品无辜, 勿再向内排查)
stream-json 下 assistant 的 `thinking→text→thinking→tool_use` 三明治形状响应, 其 text 块会在**长寿会话**中被 CLI 从 stdout 流和 JSONL 转录**双双丢弃**(模型上下文保留 → 永久分叉; 全新会话不复现, 连同形状都不复现; wrapper/normalizer/renderer 逐层验证无辜)。用户视角="agent 没回复"。取证档案+判别矩阵+issue草稿: ~/workspace/AIWorkspace/SharedContext/claude-cli-text-loss.md。Agent 准则: 实质内容只放 turn 末尾消息(其后无工具调用), 中途只发可丢弃状态行。

- **2.284.0 field-test batch invariants**: message-meta billing row is THREE-state honest (host-namespaced `h:<id>:` rid suffix-match / not-yet-harvested says so + session identity / no-requestId falls back to session-level — never a silently missing row) … ⇒ kb-bugfix-invariants.md

### Bug Fixes Applied — INDEX

**Full essays: docs/kb-bugfix-invariants.md (moved verbatim; ancient one-liners in docs/history-archive.md). Each entry is an incident whose FIX carries invariants — search here before re-diagnosing a familiar symptom.** Index:

- THE TEXT WINDOW SLOWED EVERY MANY-WINDOW SHAPE (2.369.167 perf r1): ×2.4 bytes on a 19-window open, hidden ones paid too; a resume watermark stamped before the op ran. FIX = growth budget + `slab:'floor'` + applied-only watermark. 不变量=measure on many windows vs master. ⇒ kb-bugfix-invariants.md
- A COPY CENSUS DEMANDED A COPY ITS ONLY PRODUCER COULD NOT MAKE (2.369.164 r2): a depth-1 Actions checkout cannot `git show` the pre-fix ref, every control skips, `minCopies: 1` went red. FIX = the census keys on whether a control ran. 不变量=count the producers that ran ⇒ kb-bugfix-invariants.md
- THE PAGING GATE'S FLAKY REDS WERE ITS JUDGE + HARNESS (B-1192): a 720 px wheel vs a 714 px keep zone trims the reader's card BY THE RULE; boot waits fell through (viewSession). Down slabs hold on-screen cards. 不变量=a judge encodes the geometry; a wait is a deadline. ⇒ kb-bugfix-invariants.md
- THE CALIB METRIC FIRED EVERY TICK, UNATTRIBUTED; AUTO-CLI LOGGED 100pt DRIFT VS A CLOSED WINDOW (B-a5c0): FIX = `sweepAnchorGroup` emits only on a NEW anchor, {account, bucket}; past resetsAt = no control. 不变量=a measurement fires on arrival, not a clock. ⇒ kb-bugfix-invariants.md
- EVERY SCRATCH SERVER RAN THE BOOT `git pull` (2.369.163 heavy RED): 2.5–15 s before listen ⇒ 10 s suite boot windows fell through. FIX = a temp-dir server root never auto-updates; permission-rules sweeps the git listing. 不变量=a throwaway server never reaches the network at boot. ⇒ kb-bugfix-invariants.md
- THE MIRROR'S TWO REDS (2.369.167 r1): a wheel-up's own scroll re-pinned a pin-band window and the grow loop stopped after one slab (fix: `_repinVerdict`); the lagged leg's stall, §4b/§4f geometry and the pid-shaped §1c fixture were machine luck — now constructed. ⇒ kb-bugfix-invariants.md
- PINNED CHAT WINDOWS FLICKERED (inc-mudv05ja-n5rv, 2.369.160): fresh cards at the c-v placeholder, lost folds on swap, the pin chasing both, a Workflow card re-created per progress. FIX = neutral swap + foldPassMode + keyed in-place patch. 不变量=no update re-creates a card. ⇒ kb-bugfix-invariants.md
- THE CLI PATH RESOLVED AT BOOT STAYED DEAD UNTIL A RESTART (B-a18e, fleet 2026-08-11): an Update restart in the installer's window pinned a gone path. FIX = spawn-time re-check + ONE re-resolve (cli-cmd.js). 不变量=a boot fact an installer can move is re-asked where used. ⇒ kb-bugfix-invariants.md
- A REAL-CLI LEG JUDGED BEFORE THE CHILD'S TERMINAL SIGNAL (B-5f0b): a cut wire-probe capture, a timed-out strace, two fixed sleeps; no ambient-key strip. FIX = bounded wait or evidence SKIP + withoutVendorKeys (§50). 不变量=judge a live child only after its terminal signal. ⇒ kb-bugfix-invariants.md
- THE LAUNCHER RE-RAN A HEAVY TIER ALREADY GREEN (B-3ccf): a hand-run ci:heavy then the push hook's --heavy-launch = 35 min for the same sha. FIX = PURE heavyAlreadyGreen (fresh, full-or-same-range, not partial/red) ⇒ skip. 不变量=a verdict on disk is not re-bought. ⇒ kb-bugfix-invariants.md
- A SUITE'S PATCHED COPIES TURNED A NEIGHBOUR RED (B-0220; 26 more suites, batch r1): mutants in src/ (gitignored) were read by src/ censuses. FIX = scripts/mutant-copy.mjs (scratch + re-bound resolution) + test-architecture §51. 不变量=a suite never writes the checkout. ⇒ kb-bugfix-invariants.md
- AN OLD WRAPPER QUEUED BACKGROUND WORK NOTIFICATIONS AS BILLED TURNS (B-d963): a pre-2.369.63 codex wrapper ignores `kind`. FIX = the ladder reads `notificationSteer` before writing ⇒ stash; the strip says restart. 不变量=a lane comes from the running process's own advert. ⇒ kb-bugfix-invariants.md
- THREE MORE ONE-SLOT PTY DUCKS DROPPED THE LIVENESS STAMP (B-ae4b, 2.369.164): R6 create/re-open + the OpenCode terminal (its banner went to the stamp: blank terminal). FIX = one PURE listener SET (src/pty-duck.js). 不变量=instrumenting a seam audits all its implementations. ⇒ kb-bugfix-invariants.md
- A WATCH VIEWER'S configure-window RESIZED THE HOLDER'S APP (2.369.156, P8-2 r3 open item): FIX = x5, ONE ACTIVE viewer per app window (owner: block non-active clients, Resume here); others get no upstream, Watch sends no geometry. 不变量=a shared display has ONE geometry owner. ⇒ kb-bugfix-invariants.md
- A WATCH VIEWER COULD END THE APP SESSION (2.369.156, P8-2 r6): r5 relayed every non-input xpra packet — `shutdown-server` killed xpra. FIX = a refused viewer's ALLOWLIST, lifecycle never relayed, the socket refuses `request: stop`. 不变量=a refusal names what the refused MAY say. ⇒ kb-bugfix-invariants.md
- THE STATUS BAR KEPT THE OLD MODEL AFTER /model SINCE CLI 2.1.257 (2.369.151, noticed on 2.1.280): the echo gained backticks (2026-09-02) and the bar's end-anchored regex stopped matching. FIX = ONE parser src/model-echo.js + verbatim fixtures dated by each row's version. ⇒ kb-bugfix-invariants.md
- A PARKED BACKLOG ITEM VANISHED AT THE 200-ITEM CAP (2.369.150): the store sliced the NEWEST item off a full group while the route + CLI echoed a stranger's id by position. FIX = no store cap (owner), the add acknowledged only by the stored item found by identity, claim echoes by id. 不变量=a cap belongs to a read, never a write. ⇒ kb-bugfix-invariants.md
- SPEND NOTICES LIVED FOREVER AS ACTIONS (2.369.152): 13 stale spend warnings sat in the action list. FIX = expiresAt stamped by the producer + expireDue sweep + a migration. 不变量=a notice about a window dies with it; a new lane migrates what the store holds. ⇒ kb-bugfix-invariants.md
- THE COLLAB COUNTER LOST ITS SHARED LABEL ELEMENT (2.369.150): a source pin required the spinner's literal markup; the AX pass added aria-hidden, the invariant held. FIX = twin-parity pin + a live read-only leg. 不变量=pin a behaviour's own facts, never a neighbour's spelling. ⇒ kb-bugfix-invariants.md
- CLAUDE CODE'S OWN AUTO-CONTINUE IS SPAWNED OFF (2.369.155, owner 2026-09-22): arms only interactively ⇒ chat never runs it; terminal sessions lose their only continue (stop at a wall). `auto-continuation` origin is generic (10 sites) — never a quota signal. ⇒ kb-design-lessons §18
- TWO MIRROR-ONLY REDS WERE SUITE RACES (2.369.155): window-binding pressed before the zoom reset's reflow landed; paging's control ② sampled after the pin had ended. FIX = geometry settle + press assert; rule ② reads the ring; a constructed probe on both builds. ⇒ kb-bugfix-invariants.md
- A RUNNING WORKFLOW CARD SAID 已完成, ITS RUN AN UNCLICKABLE HARNESS ROW (2.369.147): the level set's transient drop soft-closed the card and nothing reopened it; FIX = the set naming it again / task_progress reopens; popup workflow rows open the run. 不变量=a guessed close is reversed by contradicting evidence. ⇒ kb-bugfix-invariants.md
- THE ACTION-TRACE ROW SAID "SERVER UNREACHABLE" ON EVERY CARD (2.369.145): EasyPrivacy's `/trace?sessionid=` blocked `/api/browser/trace?sessionId=` client-side; FIX = path `/api/browser/actions` + "not traced" said by name when the keeper never started that browser. 不变量=a fetch the server never saw is checked against the public block lists first; "no data" ≠ "not collected". ⇒ kb-bugfix-invariants.md
- A CHANNEL ACCOUNT IS BOUND TO THE CREDENTIAL THAT MINTED ITS TOKEN (2026-09-22): one record per kind refreshed with the row's CURRENT pick ⇒ client A's token sent to client B. FIX = N accounts per kind, `credentialKey` at connect (legacy: token evidence). 不变量=the pick is a DEFAULT for new accounts; a held token binds, a token-less one re-binds. ⇒ kb-file-structure.md
- THE PLAN WEEK READ THE MODEL BUCKET (inc-mubu23bd-5vxi): seven_day_overage_included is a PER-MODEL bucket (2.361.2 wrote it into the plan). FIX = 3 lanes per record; cap named by sidecar/PHASE/family, else a folding model:cap. 不变量=the TYPE alone never picks the plan lane. ⇒ kb-bugfix-invariants.md
- THE ACCESSIBILITY TREE GREW WITH THE RENDERED WINDOW (2.369.150): 82% of rendered cards sat off-screen in the tree. FIX = the reader's band + paint aria-hidden at birth + keyed chips. 不变量=exposure follows the viewport, not the rendered window; paint is not content ⇒ kb-bugfix-invariants.md
- THE WINDOWS FREEZE WAS CHROME'S ACCESSIBILITY TREE (2.369.144): browser UI thread in HandleAXEvents 39.5 s (a UIA client had the mode on; 50k accessible nodes); GPU/renderer healthy — every in-page probe blind by construction. FIX = `accessibility.exposeChat` lever + hidden windows aria-hidden. 不变量=a whole-browser stall with a healthy renderer is diagnosed one process up (chrome://tracing → perfetto). ⇒ kb-bugfix-invariants.md
- THE PROBE MISSED THE STALL THE OWNER FEELS (2.369.143): frames alive but slow (1–2 s), timers 1–3 s late — under both thresholds; PURE `jankStormVerdict` over the frame-gap ring (slow frames ≥ 6 s of 20 s) + tickLags/frameGaps in the snapshot + client `t`→`cts`. 不变量=a stall is judged by coverage of slow frames, not one gap. ⇒ kb-file-structure.md (telemetry-client.js)
- THE FREEZE PROBE CALLED A TAB RETURN A FREEZE (2.369.141): a hidden tab's throttled tick fires 4–60 s late with `hidden` already false; the self-gap verdict is PURE `selfGapIsFreeze` (visible for the whole gap). 不变量=a timer gap is a freeze only if the page was visible throughout. ⇒ kb-file-structure.md (telemetry-client.js)
- A WORKFLOW CARD LOST ITS PHASES ON EVERY SERVER RESTART (inc-muc2fmtt-5jat, 2.369.140): task_* records are live-only and the stdout ring drops them; FIX = session-meta `taskRecords` persisted by the stdout consumer, replayed silently by rebuildHistory (`MessageManager.replay`). 不变量=a live-only record a card depends on is persisted as it passes and replayed on every rebuild. ⇒ kb-bugfix-invariants.md
- A WORKFLOW CARD'S LIVE DETAILS APPEARED ONLY AFTER A RELOAD (inc-muc1hfeg-0qn2, 2.369.139): task_started spells `local_workflow` and lands before the ack; the client gate wanted `workflow`. FIX = normalizeTaskType at the entry + the ack's type wins + the gate admits a card with the tree. 不变量=a producer's vocabulary is normalized once where it enters. ⇒ kb-bugfix-invariants.md
- WINDOWS FREEZES ARE GPU/OS SIDE, AND NOW CAPTURE THEMSELVES (2.369.137): 54 renderer freezes this month, 7 beside a long task; the probe records DPR changes, screen moves, desktop switches, a GPU/canvas inventory, and auto-files an incident on every freeze ≥ 5 s; `terminal.webgl` is the A/B lever. ⇒ kb-file-structure.md (telemetry-client.js / incident-recorder.js)
- THE DESKTOP'S PASTE BUTTON WAS A SILENT NO-OP TWICE OVER (userW inc-mubu8xdg-pvwa, 2.369.136): pressed into a dead RFB, generic toast on every clipboard failure, focus left on the button. FIX = a not-connected refusal + a paste box (textarea → `paste` event) + focus back. 不变量=a failure of the clipboard API offers the route that does not need it. ⇒ kb-bugfix-invariants.md
- A WORKFLOW CHIP SAID "Workflow" (2.369.136): a scriptPath launch has no input.name; the ack's Summary line / meta.name / file basename name it; the live placeholder never overwrites. ⇒ kb-bugfix-invariants.md
- CODE AFTER A MID-LINE `//` WAS A COMMENT, AND ITS PIN MATCHED THE COMMENT (2.369.134): a wiring key appended after chunk notes was never wired; the r-fix moved `activeSessions,` there too ⇒ every browser route 404. FIX = notes on their own line + test-architecture §48 census. 不变量=a `$`-anchored pin proves nothing past a `//`. ⇒ kb-bugfix-invariants.md
- HUGE-SESSION PAGING (inc-mubvu3a4-x8sb): trims by COUNT took the anchor; the pin lacked `we ≥ total`. FIX = ONE trim by HEIGHT (keep zone), pinned ⇔ live tail, extends fold first + grow by direction; r1: wheel OVERSHOOT carried (≤ 1 viewport/landing), gap slabs measured + dropped off message 0. ⇒ kb-bugfix-invariants.md
- INSTALLING xpra BROKE EVERY DESKTOP-APP LAUNCH (2.369.131): the ladder chose the present-but-unwired xpra rung and the keeper refused. FIX = `wired` is a SELECTION input in `resolveBackend` (present + unwired ⇒ passed over, reason named). 不变量=a rung that cannot run must lose the ladder; installing a binary never turns a working rung into a refusal. ⇒ kb-bugfix-invariants.md
- FOLD-DOMINATED PAGING JUMPED TO THE TOP OF THE PREVIOUS SLAB AND FROZE CHROME (inc-mub8xwrb-z57x, 2.369.129): the 2.368.29 residual seen in the field, and its guard had never held (the fold ran AFTER the trim decision). FIX = fold first, no trim below three viewports (`FOLD_DOM_CEILING` the only bound), `_extendTop` grows by height (one gesture, one landing). 不变量=paging and trimming decide on FOLDED geometry, by height, never by record count. ⇒ kb-bugfix-invariants.md
- THE ACTIONS MIRROR WAS RED FOR 50 VERSIONS ON MACHINE-DEPENDENT GATES (2.369.125 r6–r8): the first-run wizard skips itself on a box WITH sessions (40/41 chrome suites never pre-set `vs-onboarded` → §47 census), the ghost-host heal lived past the "No sessions" early return, the roster identity line wrapped under DejaVu Sans. 不变量=a mirror-only red names the machine-dependent gate first; it becomes a census or a local control, and it is read from the uploaded artifact. ⇒ kb-bugfix-invariants.md
- AUTO-RESUME FIRED AT THE STATED RESET AND WAS REJECTED (2.369.117, owner "恢复抢跑，等一分钟"): stated resets are minute-precise and the vendor lands late; `bucketRemaining` flipped a bucket to full at the stated instant, the pool switched 45 s early and the continue was refused. FIX = `RESET_GRACE_SEC` 60 s in the PURE verdict (fresh only a minute after; `blockedUntil` + 60 s; the card names the stated instant + "约 1 分钟后"). 不变量=a stated instant is never the fire instant. ⇒ kb-bugfix-invariants.md
- THE AGENTS ROSTER SAT ON ITS OPEN-TIME NUMBERS (2.369.115, owner "agents 侧边栏的内容不会实时更新"): rows painted usage once at render + on ⟳; the 8 s poll fed only the pies/popup. FIX = every usage cell stamped with its source, `_repaintRosterUsage` on every `_renderUsage` (PURE `rosterUsageSnapshot`). 不变量=a surface that shows polled data repaints from the poll, in place. ⇒ kb-bugfix-invariants.md
- THE ANCHOR NOTHING COULD HEAL (inc-mu6djxxt-8166, 2.369.114, owner "5h限额刷新不出来了"): a pre-isolation panel stamp (no verifiedBy) named another account's weekly reset; no witness ever filled (hot-switched sessions' spawn-time OTel vetoed), the repair kept it, every true reading was archived and the ⟳ said success for an archived reading. FIX = the isolated panel may (re)anchor a LEGACY stamp when no API witness exists; a refused live rung falls to the panel; OTel vetoes only a never-switched session. ⇒ kb-bugfix-invariants.md
- THE ROSTER HIGHLIGHTED THE POOL AS AN ACCOUNT ABOUT TO RESET (2.369.113, owner "把池账号也算作即将刷新的账号"): a pool row prints its TARGET's countdown, so ranking it spent one of the two soon slots on a member already listed; FIX = `data-pooled` on the row, `markSoonRows` skips it. 不变量=a row that MIRRORS another row's datum is never ranked beside it. ⇒ kb-bugfix-invariants.md
- A PEER MESSAGE RENDERED ONLY WHEN THE RECEIVER'S TURN ENDED (inc-mu6bfv1t-4drq, 2.369.112): stdout's `command_lifecycle started` names the JSONL uuid of the peer user record — the consumer looks it up and injects the card at turn START (msg_id dedups the result rung). 不变量=when a stream omits a record, its turn-start frame may still NAME it. ⇒ kb-bugfix-invariants.md
- A WINDOW HIDDEN BY THE PHONE LAYOUT, A TAB OR MINIMIZE PAGED ITSELF INTO HISTORY (inc-mu6bfv1t-4drq, 2.369.112): display:none zeroes scrollTop and only the desktop hider suspended the view; FIX = a hidden-REASON set on the ChatView + WindowManager.syncHiddenViews over the classes it owns (PURE view-visibility.js). 不变量=every hider suspends; suspension is derived, never written. ⇒ kb-bugfix-invariants.md
- A COMPACTED "PROMPT IS TOO LONG" CARD STILL OFFERED COMPACT NOW (inc-mu6btbfr-uaxg, 2.369.112): a compaction that SUCCEEDED (live compact_end/success, or the CLI's summary record on a rebuild/page-in) resolves every card older than it — button gone, outcome stated; a card after it keeps its button. 不变量=an offered action a later record answered is withdrawn on both carriers. ⇒ kb-bugfix-invariants.md
- A TERMINAL SELECTION TOOK THE WRONG LINES UNDER THE UI SCALE (2.369.118, userW inc-mu92zsgw-6c9y): xterm maps the pointer in ZOOMED px over a cell size measured in LAYOUT px, so under body zoom a drag at row 20 selected row 20×scale (measured: 1.25 → 25). FIX = inc-mtdrm922's rule on every xterm container (counter-zoom + font × scale, net zoom 1). 不变量=a canvas-mapped surface under the body zoom sits at NET zoom 1, and the incident snapshot names the scale. ⇒ kb-bugfix-invariants.md
- "DESKTOP DISCONNECTED" WITH NOTHING IN ANY LOG (2.369.118, userW inc-mu3lmd4s-dmwf): the /api/vnc bridge had no keepalive and logged neither open nor close; the singleton never reconnected. FIX = 20 s ws pings + a NAMED close line + the singleton walks the reconnect ladder + the desktop scene in the bundle. 不变量=a long-lived silent socket pings, and every close says who closed it. ⇒ kb-bugfix-invariants.md
- THE POOL PARKED ON A MEMBER THAT BILLS PAY-PER-USE (B-ad05, 2.369.110): usage credits (`overage` with a non-rejected vendor status) were invisible until spent and the pool idled 14 h on one at 100 %. FIX = `overageState().mode` + a dim credits chip, credits members rank last / last resort, ONE 6 h parking notice, 'Exclude from pool' on the member menu; spend-guard unchanged. ⇒ kb-bugfix-invariants.md
- THE DESKTOP-APP KEEPER'S r2 — EIGHT VERIFIER FINDINGS ON A ONE-DAY-OLD FEATURE (2026-09-14, docs/design-desktop-apps.zh.md P8-1, each reproduced on the real keeper before the fix): ① a picture server whose binary vanished after the probe was an UNHANDLED spawn `error` — the … ⇒ kb-bugfix-invariants.md
- THE FABLE-CAP POOL STORM (2026-09-13 04:58-05:01Z, owner '有账号有 Fable 额度但总是提示没有了'): the owner's main conversation (spawn model `fable[1m]`) was proactively moved off the ONE member that still had Fable quota onto three whose Fable cap read 100 %, one after another … ⇒ kb-bugfix-invariants.md
- EVERY SESSION CREATE AND EVERY KILL BLOCKED THE EVENT LOOP FOR 11-17 SECONDS (2026-09-09, userW's fleet pod; read-only forensics — the pod itself was never touched): 27 of 27 creates/kills in seven days, chat or plain shell, each followed by an 11-17 s `longest loop gap` … ⇒ kb-bugfix-invariants.md — gate: test-discovery-spawn 65 + test-architecture 139
- COMM PANEL P4 VERIFIER, FOUR FINDINGS (2026-09-16): the stash drain re-clipped a refused wake's block to 400 chars (hits lost); the receipt embedded a vendor title raw (live frame); boot flush + first-pass wake carried the same held hits twice; a 403 pull reconnected forever ⇒ kb-bugfix-invariants.md
- THE HEAVY GATE WAITED 40 MINUTES FOR A SHA NOBODY COULD PUSH, THEN RAN 108 SUITES ONE AT A TIME (2.369.105, owner 2026-09-15 "heavy gate太占用时间了吧"): 4472 s → 817 s for 110 suites — parallel lanes (serial lane for the machine-global suites), supersession of abandoned shas, an impact-scoped push tier computed from the tree AT the pushed sha with a 24 h full-run net, writer-sweep narrowed to its own pids (414 s → 12 s). 不变量=a gate's wall is measured, its lock holder must be provably wanted, and an impact graph is read from the commit it judges, never the working tree. ⇒ kb-bugfix-invariants.md
- GATE SUITES LEFT A DETACHED DEVICE DAEMON PER WORKTREE SERVER — 504 OF THEM (2.369.104, owner 2026-09-16): every worktree server a suite boots spawns a setsid `vibespace-device` the suite's teardown never sees; 504 daemons + 552 scratch node + 2,137 orphan `dtach -a` clients = 86 GB, load 15, KVM blank. FIX = the evidence-based reaper in ci.mjs after every suite + at every heavy launch. 不变量=a process a suite starts is the suite's to end; when it cannot, the gate owns a standing sweep keyed on evidence (scratch root + ownership + age), never a name. ⇒ kb-bugfix-invariants.md
- THE ARM CARD NAMED 12PM AND NOT WHAT 12PM WAS (2.369.108, owner "7am 就 reset 5h 却提示 12pm"): the pool wait = MIN over members of MAX over each member's dead buckets, so the rejector's own 7am was rightly discarded (its Fable 2 % < the 5 % floor) but the card never said so; the arm now carries a CAUSE (`armCauseFor` → `opts.cause` → `armNoticeFor`) naming the soonest member + bucket and the rejector's discarded reset. 不变量=a wait computed from several windows names the one it waits for and explains the nearer one it discarded. ⇒ kb-bugfix-invariants.md
- THE ARM CARD CALLED A HOT SWITCH A RESET (2.369.104, owner "明明没到也没被中断你却提示到达上限了"): the +45 s near-arm after a pool switch was announced as `用量已达上限。已安排在 <now+45s> 重置后自动继续`; `armNoticeFor` words the arm by its CAUSE. 不变量=a sentence that names an instant must name what that instant IS. ⇒ kb-bugfix-invariants.md
- A CHROME LEG MATCHED ONE SPELLING OF A NAME THE PRODUCT COMPUTES (2.369.102, test-opencode-s9): the session-card leg found the OpenCode card by OpenCode's title, while the sidebar names the row by the first-user-message rule the moment the naming read lands — red the day naming beat the render, deterministic thereafter. 不变量=a UI leg matches the row's OWN computed name (`s.name`), never a literal the product may replace. ⇒ kb-bugfix-invariants.md
- A UNIT GUESSED FROM THE VALUE'S MAGNITUDE READ ONE PERCENT AS A HUNDRED (2.369.99, inc-mu3giy8t-7k36): `parseOAuthUsage`'s named `seven_day_<model>` loop used `> 1 ? /100 : as-is` while its sibling branches divide by 100 — the vendor's `utilization: 1` (one percent) … ⇒ kb-bugfix-invariants.md
- A SETTING READ BUT NEVER DECLARED HID THE DESKTOP BUTTON (2.369.97, userW inc-mu1qa5gj-9qe9): `toolbar.showDesktopButton` was read by `applyChromeSettings` + customize mode's `hideKey` table since 2.111.4 and never in `SETTINGS_SCHEMA` … ⇒ kb-bugfix-invariants.md
- THE INBOX ✓ SLID THE NEXT ROW UNDER THE POINTER (inc-mtw02kbq-kj96, 2.369.90): the For-you popup re-rendered on every broadcast and a resolved row left its group ⇒ rapid clicks hit the wrong items … ⇒ kb-bugfix-invariants.md — gate: test-user-todos-layout 12
- THE TEST SUITE WROTE A FABRICATED CONVERSATION INTO THE REAL ~/.claude, AND THE PRODUCTION LEDGER BILLED IT (2026-09-09, owner: a Fable conversation, and Fable usage … ⇒ kb-bugfix-invariants.md
- THE QUEUE STRIP SHOWED A MESSAGE THAT HAD RUN AN HOUR EARLIER, AND ✕ PAINTED IT RED (2026-09-09, owner report on a live codex session): buffer says `queue_changed [A]` → steer ok → `queue_changed []` at 04:28:35, then **58 min with no queue record**, a 05:20 server restart … ⇒ kb-bugfix-invariants.md
- A RESTORED SESSION WENT DARK UNTIL SOMEBODY TYPED (2026-09-09, production journal): systemd stopped vibespace 05:20:14Z for the 2.369.77 update; the new server reconnected 12 sessions 05:20:22-05:20:53Z (a 30 s loop gap inside that boot), spawned the local daemon 05:20:56Z … ⇒ kb-bugfix-invariants.md
- THE CEILING ON EVERY TURN NOBODY TYPED (2026-09-08, docs/design-account-hardening.md §4.4c / P9 + owner decisions D2/D3/D6/D8 — the design's P4 spend half): seven producers could start a BILLED turn with no per-occurrence owner action and each had only its own local floor … ⇒ kb-bugfix-invariants.md — gate: test-spend-paths 215 + test-architecture 107
- A MEMBER BECAME USABLE AND NOBODY NOTICED (2026-09-08 02:00–02:31, 生产 journal): 池"全部"全员 0%(02:00:02 "no member can serve it"), owner 在耗尽当中加了一个订阅(记录 02:03:29, 登录 02:18:27) ⇒ 02:18:33 八个 fable 会话被 per-session 切到这个**还没有任何读数**的新成员(unknown=50% 胜过 0%) … ⇒ kb-bugfix-invariants.md
- THE IDENTITY ANCHOR WAS THE PRODUCER THAT LIED (B-855a c2, 2.369.110): the window sidecar every reading is judged against was stamped by the panel that can lie. FIX = the anchor is DERIVED FROM THE API — a K=3 run of eligible slot-verified readings, never one; the same run re-anchors a MOVED window; a standing repair (boot + hourly + by hand) re-anchors sidecars and caches; overage/spend survive rebuilds. ⇒ kb-bugfix-invariants.md
- ONE WALL MARKED TWO LANES THROUGH THE TYPE THE RULE EXCUSED (B-ccaa, 2.369.110): `seven_day_overage_included` was written on arrival beside a Fable banner ⇒ 7d AND fable demoted; FIX = deferred like every unscoped weekly rejection + the turn's banner remembered. 不变量=the lane is decided by what NAMES it. ⇒ kb-bugfix-invariants.md
- THE /usage PANEL ANSWERED FOR ANOTHER ACCOUNT (B-855a c1, 2.369.110): `claude -p /usage` seeds its panel from the config dir's machine-wide `cachedUsageUtilization` whenever its own fetch fails in-band, so the probe printed another account's numbers with no marker and re-stamped the window sidecar. FIX = a per-account isolated CLAUDE_CONFIG_DIR + cwd, identity verified before any write, the ⟳ route names its rung. 不变量=a producer that can answer for somebody else is never its own proof. ⇒ kb-bugfix-invariants.md
- A LOW-USAGE ACCOUNT SUDDENLY JUMPED TO 93% (inc-mts8a8mr-ulmm, 2026-09-08, owner '一个低用量账号突然跳到 93%'): 05:27:00Z 池把 9 个会话 Member Y→Personal, 05:27:16Z 一条**39 秒前用 Member Y 的 token 发出**的请求的响应到达 … ⇒ kb-bugfix-invariants.md
- GATE SUITES CLAIMED MACHINE-GLOBAL NAMES (2.369.76, heavy RED on 40ad936d): a verifier agent ran test-chat-paging from its own checkout while the heavy tier ran it — both claimed `/tmp/vs-chatpage-smoke` + :3990 … ⇒ kb-bugfix-invariants.md
- QUOTA READINGS WERE KEYED BY THE ORG THE CLI STARTED ON (2026-09-07, owner '为啥不是从根源上解决假数据而是加 guardrail'): 一个 2026-09-02 22:55 PDT 就被清空登录的成员, 之后五天还在收 limit-banner 与 Fable 桶读数 —— 而"让登出成员拒绝读数"这个护栏被 owner 否掉了, 因为登出只是**谎言变得可见**的那一种情形. … ⇒ kb-bugfix-invariants.md — gate: test-readings-attribution 194
- STEER-ALL 把25条消息插进了turn, 屏幕上只出现1条 (2026-09-07, owner '我刚才在那个codex session里全给插入了，但是我只能看到我最后插入的一条消息'): app-server的队列属于**线程**不属于wrapper — 实测owner的buffer在新wrapper起来2秒后就是`queue_changed n=25` … ⇒ kb-bugfix-invariants.md — gate: test-architecture 80
- AUTO-RESUME FIRED 130 TIMES INTO A WALL (2026-09-07, owner决定 ut-1c6c15a2db ①④; ②③ 已关闭 B-f69c): 一个会话被自动续跑**130次**(另一个32次, 最快2次/秒), 每次都被"session limit"拒绝, 对话尾部约150条垃圾卡片; 每个循环三行逐字重复(armed 'switched to a usable account' → `per-session switch … → <link> (re-point` … ⇒ kb-bugfix-invariants.md — gate: test-auto-resume-loop 186 + test-auto-resume 128
- THE INSTANCE DEFAULT ATE THE CONVERSATION'S OWN VALUE ON EVERY RESUME (B-6b6d, owner ruling 2026-09-07 — 2.369.62 的公开残留): 客户端 `sessionEffort = effort !== undefined ? effort : defaults.effort`(model 孪生同行)在**每一次** create 上求值, 包括带 resumeId 的 ⇒ 服务端 B-21e4 的延续兑底(门控是"客户端什么都没发")永远不会触发 … ⇒ kb-bugfix-invariants.md — gate: test-codex-effort-meta 245
- B3 TURN TRUTH r2 — 三条被对抗验证者复现的真缺陷 (design-harness-features §2.5/§2.10/§2.11): ①**MAJOR 写进 DOM 的标记活不过一次重建** — 撤回的划线/隐藏(`_applyRewound`)与"正在执行"的圆点(`_onToolsInProgress`)都是在源头直接改 DOM, 而**为一条消息造元素的路径有三条**(`_onCreateMessage`(分页也走它, 经 `_renderDetached`) … ⇒ kb-bugfix-invariants.md
- THE EFFORT A TURN RAN AT (2.369.62, owner "调成了 ultra 但 metadata 显示 xhigh"): 对话 codex 侧 09-06 起就是 ultra(自己的 rollout 21/21), 但**活 buffer 1137 条里只有一条 turn_context** — 我们在 `turn/started` 合成的那条, effort 取自 spawn env(实例默认 `codex.defaultEffort=xhigh`) … ⇒ kb-bugfix-invariants.md — gate: test-codex-effort-meta 108

- THE PROCESS LIST WENT EMPTY ON THE BUSIEST MACHINES, SILENTLY (2026-09-07, found by the release gate going red): `sysinfo.read().procs` was `[]` on a box running 2,404 processes — `topProcs()` ran `ps aux` through execFile with a **4 MiB** maxBuffer while the real output … ⇒ kb-bugfix-invariants.md
- DESKTOP-RESUME PAGED A PINNED WINDOW INTO HISTORY (inc-mtq5bpjt-0o0n): 桌面切回后三个**钉在活尾**的窗口跳到历史且尾巴被trim掉(wheelAgo 1.8M-4.7M ms=零输入); 2.369.1只管住了隐藏态, 恢复**过渡态**无守卫, 而gap哨兵的IntersectionObserver是纯几何触发器——`_loadEarlierGap`的tail分支是唯一一个连意图/pin/settle/suspend门都没有的上翻入口 … ⇒ kb-bugfix-invariants.md — gate: test-chat-trim-guard 112 + test-desktop-resume-paging 35
- B-7638 七条review残留(2026-09-06 review-fixes-r2 verify, 每条先复现再修): ①over-cap adoption只扫尾部4KB — 旧delta路径splice之后还在继续追加, 标记被埋在尾窗之上 ⇒ 信任缓存字节前整文件扫描(1MiB分块+3字节carry), 门控用**meta schema marker `v:2`**而不是"没有remotePath"(round 2: 已发布的尾扫adoption会把provenance盖在它采纳的hybrid上, sameRemote对这类腐败恒真) … ⇒ kb-bugfix-invariants.md
- WRITER SWEEP KILLED READERS + ITS TWIN NEVER RAN (B-3185): ①“是不是CLI”用整条argv的子串判定(`*claude*`/`*codex*`) ⇒ 任何argv里出现`~/.claude`路径的进程都被SIGTERM — `tail -f …/.claude/projects/<id>.jsonl`、开着转录的编辑器、以及跑在`~/.claude/worktrees/`下的进程(套件自己先中招: 持有fd后匹配到自己的守卫自杀, exit 143, 当时只在测试里绕过而没修产品) … ⇒ kb-bugfix-invariants.md — gate: test-writer-sweep 239 · test-discovery-interpret 25 · test-local-discovery-device 10 · test-sysinfo-op 33
- A MEMO KEY THAT NAMED NEITHER THE PRICES NOR THE LEDGER (2.369.43): 2.369.36的区间memo键=ids|from|to|事件计数 — ①改价(tier或按账号折扣)后历史区间永远返回旧价(实测10×改价/50%折扣都冻在原值), 学习率与每条anchor的costSince被毒化 ⇒ 键里加`pricingToken()`(不能版本化价格的ledger干脆不memo) ②learnRates每次按同一顺序重走一身份的全部pair=环形访问 … ⇒ kb-bugfix-invariants.md
- THE SHORT-VIEW RESCUE THAT COULD NEVER FIRE (2.369.43): attach后'内容填不满视口就补一页'的救援门`rendered < 30`永假 — 每条attach路径都发tail(50), `_windowStart > 0`恰好等价于这50条到了, 而语义折叠只隐藏成员不删元素(33份真实转录实测50/50渲染); 于是折叠主导的窗口(50张卡→两个run header, 700px视口里240px)在触摸端彻底无路可走, 而这正是该功能存在的理由 … ⇒ kb-bugfix-invariants.md
- CODEX .ZST / DISCOVERY / REMOTE-FETCH BATCH (2.369.x): ①压缩head靠"猜压缩比"的cap(>4×即EZSTBIG→空head→threadId ''→线程从会话列表消失, 空meta还被mtime缓存; 本机40个真实rollout默认级压缩7个复现)⇒改为按压缩前缀二分取够plainBytes, 读不出就抛coded错(不可读≠空, 失败不进缓存+逐字日志) ②目录清单缓存的2s settle门在**查询时**判定⇒同一秒创建的兄弟rollout永久不列 … ⇒ kb-bugfix-invariants.md
- PLUGIN SECURITY BATCH (2.369.43 + 2.369.44第二轮, 9 reproduced findings): manifest自由文本进生成代码(CR/U+2028结束`//`注释→shim在沙箱外带session token且分发到每台主机)⇒jsLiteral+cleanText单一净化点; 声明的fs路径未按node权限模型折叠(`/./`=整个文件系统)⇒先折叠再比禁区; 2.369.44 node还认`*`通配(读到第一个`*`为止, 之后按字符串前缀授权 … ⇒ kb-bugfix-invariants.md — gate: test-plugin-security(62)
- THE FOURTH READER SITE — 每个已停止的OpenCode对话都打开成空 (2026-09-07, B-eac2): "View History"显示"No messages…"而serve里对话完整——S9的store-backed reader在`prepare()`跑之前没有字节(essay里写着), transcript-service在**三个**读者点await它, 而ws `attach {viewOnly:true}`是**第四个**且没有 … ⇒ kb-bugfix-invariants.md
- S9 REMAINDER ROUND 4 (2026-09-07, B-eac2, 三条全部先复现后修): ①**"在这个会话里开个终端"把2.369.42事故原样搬回来了** — `openPty` 把会话 cwd 当 `?directory=` 传给 `POST /pty`, 而 `?directory=` 正是让 serve 为那棵树起 instance(递归索引 + 整树 inotify)的东西 … ⇒ kb-bugfix-invariants.md — gate: test-opencode-s9 168
- S9 REMAINDER ROUND 5 (2026-09-07, B-eac2, 两条都先复现后修): ①**死掉的 ask 仍然可点 Submit** — `readConversation` 把转录里未关闭的 ask 接到活的 `que_…` 上(重载后卡片还能答), 只有热 map **为空**才去重读权威列表; 而 `/question` 出自**单个 serve 进程自己的内存**(真 1.18.29 实测: 新进程一律回 `[]`, 不管上一个持有什么) … ⇒ kb-bugfix-invariants.md — gate: test-opencode-s9 201
- S9 REMAINDER ROUND 6 (2026-09-07, B-eac2, 先复现后修): **关掉的服务仍然握着它正在挂的那个 watch** — round 5 只管住了通道**能不能开始**, 而 `lane.start()` 的两条启动路都是**先 async 后拥有**: `armWatch()` 在 `resolveDirs()` 里等 serve 自己的 `GET /path`(2s 预算, 而**恰恰在 serve parked/起不来时整份花光**——正是用户去关服务的那个状态) … ⇒ kb-bugfix-invariants.md — gate: test-opencode-s9 219
- S9 REMAINDER ROUND 7 (2026-09-07, B-eac2, 两条都先复现后修): ①**关掉的服务照样 ADOPT 了一个 serve, 并用它建了一条全新的 watch** — round 6 只让**通道**的 stop 对在飞的工作终结, 但通道不是用户点起来的: `install()` 按**keeper 发布的事实**起它(`laneWanted = st.ready || wantsAutostart()`), 而 keeper 的 `locate()` **只在入口**查 `state.stopping` … 负控=把这 6 个检查删掉的副本. ⇒ kb-bugfix-invariants.md — gate: test-opencode-s9 241
- S9 REMAINDER ROUND 8 (2026-09-07, B-eac2, 三条都先复现后修): ①**"关掉再打开"这一对动作被 Disable 杀掉的那条 ladder 接管了** — round 7 把守卫挪到了取得所有权处, 但它问的是 `state.stopping`, 而那是一个**电平不是取消**: `stop()` 抬起它, 紧接着的 `start()` 就把它放下, 于是跨 Disable→Enable 这一对(面板自己的 Stop/Start … ⇒ kb-bugfix-invariants.md — gate: test-opencode-s9 259
- S9 REMAINDER ROUND 9 (2026-09-07, B-eac2, 先复现后修): **被取消的 ladder 删掉了活 ladder 的记录, 把自己 spawn 的 serve 变成孤儿** — round 8 的 `ensuring = null` 才让 Enable 开自己的 ladder, 也因此第一次让**两条 ladder 同时活着**, 于是“盘上那条记录是我的”不再按构造成立 … ⇒ kb-bugfix-invariants.md
- S9 REMAINDER ROUND 10 (2026-09-07, B-eac2, 先复现后修): **活着却不出声的 recorded serve, 每次重启都变成孤儿** — round 7-9 防的都是**并发**的另一条 ladder, 而这一条零并发: 复用 rung 对“健康探测没应答”只拥有**一条臂**—— pid 已死(`} else if (!cancelled(epoch) && !pidAlive(rec.pid)) clearRecord(owned);`) … ⇒ kb-bugfix-invariants.md
- S9 REMAINDER ROUND 11 (2026-09-07, B-eac2, 三条都先复现后修): **永远到不了的判决是停机, 而 park 唯一没变灰的那个按钮会去杀它刚拒绝签名的那个 pid** — ①round 10 把五个结局里的两个挂在 `/proc` 读上, 而 README 里写着 full support 的 **macOS 根本没有 /proc**: 那里 `readProcCmdline` 对**每一个** pid 都回 null ⇒ `'unknown'` 拒绝对每一条 pid 被回收的陈旧记录都成立 ⇒ 永久 … ⇒ kb-bugfix-invariants.md — gate: test-opencode-remote 55
- S9 REMAINDER ROUND 3 (2026-09-07, B-eac2, 三条全部先复现后修): ①**用户自己的点击被报成别人在跑** — piece (f) 的"不是我们动的"只认"我们有活会话持有它", 于是经我们自己的 serve 路由对**已停止**对话做的每一次改动(回滚/恢复/答复或拒绝ask)都在动 `time.updated` … ⇒ kb-bugfix-invariants.md — gate: test-opencode-s9 135
- THE "HISTORY IS HIDDEN" ROW NEVER SHOWED ON THE ONE LIST THAT NEEDED IT (2.369.70, Actions mirror): sidebar 的 "No sessions" 早退分支跳过了 OpenCode 服务提示行, 于是列表**因为**历史被隐藏而为空的那台实例恰恰看不到回头路; 空分支现在也渲染该行(唯一允许的 workbench 之外调用, 紧贴 return); 本机因有无关会话而绿、Actions 因零会话而红=环境依赖的腿, push 后必看镜像; test-opencode-plugin 109
- OPENCODE SERVE INDEXED THE WHOLE MACHINE (2.369.50, owner 2.369.42生产实例: 209 CPU分钟/5.0GB RSS/30021个inotify watch/3.6GB每秒页缓存读): OpenCode按**目录**起instance并递归索引+watch整棵树; /proc逐路由实测 — v1路由(/session, /session/:id, /session/:id/message, /project)零开销 … ⇒ kb-bugfix-invariants.md
- CODEX EXCLUDED FROM THE FRAME-FILE BYPASS BY BACKEND ID (design-harness-plugins §1 P1): 帧文件旁路条件里`backend !== 'codex'`让codex的多图粘贴永远走裸pty stdin(可粉碎, 79928a2b类)且wrapper不可解析行静默`continue`(消息丢+无人知) … ⇒ kb-bugfix-invariants.md
- CODEX SANDBOX CLOSED LOOPBACK → AGENT TOOLS DEAD (2.369.17): codex沙箱网络策略默认关且seccomp连127.0.0.1也禁(AF_UNIX同样)→非yolo的codex会话里vibespace-*工具全EPERM; 修复=wrapper策略带networkAccess:true(VIBESPACE_API存在时)+终端`-c sandbox_workspace_write.network_access=true`(裸布尔)+boot探测改`codex sandbox -- true`功能探测; 不变量=产品依赖的能力要靠运行验证不靠找文件, 同一权限模式在chat/terminal必须解析成同一沙箱
- ATTACH STORM STALL → HEARTBEAT KILLED THE CLIENT → KILLS LOST (2.369.16, inc-mtndq0vb): 重启后首次attach同步convertHistory整份transcript(58会话1.35GB)→19窗口重连风暴堵循环4分钟→心跳把自己停顿期间没读到的pong当客户端死亡terminate→socket里排队的kill全丢; 修复=convertHistoryAsync分片+rebuildHistory单飞+feedLive唯一活记录入口(重建期间排队按序重放)+心跳迟到>5s不判死+kill向请求方应答killed且客户端request resend到ack; 不变量=ws handler里不做整份transcript的同步工作, 活性判定要剔除自己的停顿窗口
- STRANDED WRITES UNDER A DISCONNECTED MOUNT POINT (2.369.15): 未挂载的挂载点=裸目录, 生成的TASK.md把树重建在上面→重连"not empty"; 连接时隔离到兄弟目录`<mp>.stranded-<ts>/`(绝不删)+shadowedBy谓词(files.js 503, TASK.md写入器跳过+mounts-updated重试); 不变量=写用户路径先问shadowedBy, `--allow-non-empty`永不
- VNC POINTER OFFSET UNDER DPI ZOOM (2.369.5): body zoom下noVNC混用viewport px与layout px, 远端指针偏zoom倍; 不变量=VNC画布必须net zoom 1(容器counter-zoom, var反应式)
- RECONNECT RE-ATTACH REBUILD STORM (2.369.2): ws重连re-attach全部会话, 相同slab必须no-op(epoch/total/首尾id全同⇒跳过DOM重建, meta照常applied)
- DIALOG EATEN BY SELECT-DRAG + DESKTOP-SWITCH PAGING STORM (2.369.1): backdrop关闭必须以mousedown起点为准(click的target是共同祖先); 桌面隐藏的ChatView必须suspend(几何无意义时分页/pin机器零决策, 恢复走settle窗口)
- FOLD-DOMINATED TRIM WHITE-SCREEN (2.368.29, inc-mtajy6wr): 语义折叠让150条窗口矮于视口(sh=ch), trim掉的是唯一可见内容+每滚轮瞬移50条; 不变量=窗口矮于2视口必须生长不滑动(两个trim对称, cap升600)
- CREATOR NEVER GETS 'attached' — live state rides 'created' (2.368.4): resume的创建端历史走HTTP无meta, 只搭attach载荷的per-session状态(风格/auto-resume)永远到不了主流程; 加payload字段⇒枚举窗口诞生的每条路(create/attach/view/restart)逐一验证送达; 载荷进meta必须整体传递, 手抄键列表=第五次whitelist漂移
- HOSTED PAGE NEEDED A SECURE CONTEXT (2.366.1, owner '打开后无法加载'): crypto.randomUUID只在secure context存在, 而VibeSpace走明文http+主机名⇒undefined⇒画布每块artboard永远卡'Loading artboard…'; 127.0.0.1是可信源所以我在loopback上的验证全部无效(被owner一句'你用的是你发给我的链接访问的吗'戳破) … ⇒ kb-bugfix-invariants.md
- SHARE URL GUESSED FROM SOMEONE ELSE'S BROWSER (2.366.1): 记住的'上次浏览器origin'被当作分享URL交给agent, 写进聊天回复的是本机主机名; 不变量=服务器不知道自己被怎样访问(反代/隧道), 绝对URL只能来自publicUrl或发问请求的Host, 给agent的一律是相对路径, 由前端按location.origin linkify
- CAPABILITY GATE READ THE WRONG FILE (2.364.1, owner三次Terminate+Resume无效): 2.361.1帧文件能力门从data/session-meta(服务器记录, 从无caps)读caps, 真caps在wrapper sidecar data/session-buffers/<id>.json → 所有wrapper都判'旧', 旁路死两版, >1MB粘贴全拒且拒收文案指错方向; 修复=wrapperCaps()唯一读者(sidecar+碰撞解析器, 无状态), 只缓存正向判定, 拒收带证据+进journal; 不变量=进程宣告的能力只从该进程写的文件读, 负判定不缓存, 拒收必须举证, 读者测试要对着真写者的文件+负控, 报告与解释矛盾时解释错…
- PROXIED POST EATEN BY BODY PARSER (2.363.2, inc-mt2bpw2f userW): express.json在unblocker之上吞掉代理POST的body→目标端等Content-Length到超时, GET无恙所以页面渲染正常但按钮全死; 修复=json parser跳过/proxy/(unblocker保持在auth之后, 严禁open proxy); 不变量=流式中间件必须拿到原始请求, 全局body parser与任何pass-through路由互斥…
- SEND REFUSAL READ AS ATTACH FAILURE (2.363.1, inc-mt2arppw userW): chat-input拒收被client当attach失败→活窗口翻Resume bar+拒收文本在error字段没人读; 修复=code:'input-rejected'分流+双字段+ring补记error; 不变量=一个error类型多语义必须带code分流…
- PEER MESSAGE INVISIBLE ON LIVE STREAM (2.362.2+2.363.0, inc-mt27t0bg userW+owner): CLI收站内信stdout从不带user记录(仅JSONL), 会话发的挖result.origin补渲染; SERVER发的(jobs notify/vibespace-msg)在CLI侧连origin都body-less — 投递梯自己渲染卡片(emitPeerCard); 不变量=可见性归属掌握信息的一方…
- UNSTAGED WIRING (2.355.0, userW复报): 2.331.0纯函数+单测入库但调用点没staged — 修复死了24个版本而单测常绿; 纯函数修复必须配WIRING PIN(套件grep调用点)…
- MANUAL POOL HOT-SWITCH DEMOTED (2.355.0, userW热切换): plan C per-session links优先级盖过手动切换面 — 新间接层压过旧控制面时, 旧面每个writer都要重审为新层consumer…
- NOTIFY-CRON NEVER WOKE ITS OWNER AGENT (2.361.5): notify action只投user inbox+被动组事件, _notifyOwner从未被调用; 修复=接上标准owner投递栈+每job notifyLog投递日志环(面板Delivery log可监控)…
- RUN+ECHO REMINDER = SILENT NO-OP (2.361.4): 定时裸echo因quiet-success law永久静默+创建响应误导; 修复=该形状默认notifyOk+创建响应明说per-fire语义; 不变量=常见直觉形状要么让它工作要么大声拒绝, 绝不accept-and-ignore…
- AGENT-TIME IS UTC, BARE DATETIMES ARE SERVER-LOCAL (2.361.3): --at裸时间按服务器本地时区解析害agent追查幻影'通知链路坏了'; 修复=相对形式+2m+创建回显resolved fire time…
- MONTHLY-SPEND-CAP REJECT UNMARKED (2.361.2): seven_day_overage_included被映射成'other'→周桶+月度cap双死成员7d缓存仍0.53被池反复挑中; 修复=映射到sevenDay+按事件resetsAt标死…
- WRAPPER VERSION SKEW ATE IMAGE PASTES (2.361.1, c1206711): 2.360.0帧文件旁路对旧wrapper会话发指针行→CLI静默丢弃; 修复=wrapper boot meta广播caps.frameFile, 服务器按能力门控…
- HOT-SWITCH STALE-TOKEN MISATTRIBUTION (2.361.0, B-345b定案): 池热切换对运行中CLI不生效(≥25min) — 链接意图记账双向错位毒化odometer+学习率(爆发层-30~-49%); 修复=OTel每请求真值通道…
- SCHEMA-TWIN FALSY-0 REVERT (2.360.1, inc-mt0mozsp): Manage Agents instructions tab held a pre-2.210.0 bounds copy + `Number(v)||dflt` — explicit 0 (every-stop mode) silently…
- PROXY-SWALLOWED ASSIGNMENT (2.343.0, 7th): lazy.js get-only Proxy ate `portForwards.plugins=` since 拆分#12 — publish dead 17 releases; set trap + test-lazy gate; 跨seam赋值必须落在真实例…
- RELATIVE-PATH LOST BINDING (2.341.1, 6th): extraction carried require('./package.json') into src/server/ — call-time throw, all gates blind; test-architecture #8 now statically resolves every re…
- LATENT LOST BINDING (2.340.2, 5th): free identifier swallowed by its own catch — 'capture failed: X is not defined' logs, dead 3 days; grep journals for is-not-defined after every extraction…
- STUCK-THINKING AFTER RESTART (2.339.2): socket-derived wrapper-file reads miss counter-collision sessions — resolve via wrapper argv; attach reconciles streaming vs sidecar…
- TYPING-DRIVEN PAGING SELF-FEED (2.338.0, Windows freeze audit): autosize thrash → anchoring drift → ungated _extendTop paged 50/bounce; every ex…
- Three-tier closure marathon (2.297.0–2.300.0, owner directive "一次性完全做完"; invariants, do not regress): ① script…
- PAGING INTENT GATE (2.307.0, inc-msorcsrl — the mechanism two earlier fixes missed): after an upward page, con…
- PAGING BOUNCE / COLLAPSED GEOMETRY (2.301.0→2.306.0, inc-mso818ry + inc-msor3oax): while content-visibility le…
- SCOPED-BUCKET MERGE (2.305.0, inc-msof8i22 — the pool refused to leave an account whose Opus was spent): model…
- COUNTER CAPTURE (2.302.0, proven in fleet production data): a monotonic counter's value is CAPTURED ONCE and r…
- B-b87b batch (2.267.0 — all 16 confirmed findings from the 31-agent global review; do not regress): ①dial-sess…
- Display cwd must never reach a spawn (2.225.2, userL h200 real incident): the sidebar merge composes remote we…
- Restart-survival audit batch (2.218.0-2.219.0, user directive '整体review所有不抗重启处'; full 54-finding report: Share…
- Dead-session replay = blank window shell (2.217.0, userL ×12 real report; user directive: restart后必须能看read-onl…
- Resume guard (2.179.0, userW's duplicate-session incident): a plain claude `--resume` REUSES the conversation …
- Username migration must also re-encode claude's project dirs (2.236.1, userW's real incident): the 3.5.0 image…
- Changing the container USER ripples to EVERY `kubectl exec` (2.162.7 regression): the 3.5.0 personalized-usern…
- Dial-device (paired) sessions — FULLY FIXED + E2E-verified (B-0d70, 2.163.0–2.164.1; scripts/dbg-dial-session-…
- #workspace must be overflow:clip, NEVER a scroller (2.161.2, three-iteration live-tracer diagnosis): overflow:…
- Permission/AskUserQuestion resolution must survive restarts WITHOUT the control_response (2.109.5, real report…
- SILENT TRANSCRIPT LOSS (2026-07-03, CRITICAL): sessions spawned by a server that was itself started from INSID…
- Chat status bar empty until first reply after resume/attach: model + contextWindow came only from result.mode…
- Clipboard image paste failures: (1) `paste` event never fires — xterm.js v5 uses Clipboard API directly, bypas…
- Spontaneous terminal shrink + disconnect (mid-use, no user action): `resizeSessionToMin` min'd over ALL sessi…
- Dead settings in schema: removed 9 settings defined in `settings-schema.js` but never read by any code (enable…
- Tab merge hit-test matched occluded icons: dragging A's titlebar onto A's own position would merge with a stac…
- `/compact` leaving chat stuck on 'thinking': stream-json emits `user` messages (compact summary) but no resul…
- isStreaming derived from stale normalizer messages: old heuristic scanned normalizer messages for status==='s…
- Huge sessions (>512MB JSONL) — tail-only load + continuous seek scroll (no seam marker): readFileSync(fp,'utf…
- Codex thread vanished from session list permanently: a transient IO error in `extractCodexThreadMeta` was swal…
- Stale openSpec replay → blank chat window with outdated title: a window whose dtach session died and was resum…
- Chat drag-upload overlay permanently visible ("won't disappear"): this project has NO global .hidden {display…
- Claude fork behaved identically to resume in chat: the WebUI never captured Claude's stream-json `session_id` …
- Minimap/search accuracy (huge sessions, reconnect, gap slabs) — five related fixes + the 2.286.1 sixth, keep A…
- CodeMirror active-line highlight hid the selection on the first/last selected line: `highlightActiveLine` (fro…

